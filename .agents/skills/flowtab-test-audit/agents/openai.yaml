interface:
  display_name: "FlowTab Test Audit"
  short_description: "Stage-based FlowTab test audit and full closure"
  default_prompt: "Use $flowtab-test-audit to run the appropriate FlowTab test-audit stage, default to commit_on_stage_complete, and produce its C0, C1, or C2 handoff."

policy:
  allow_implicit_invocation: true
