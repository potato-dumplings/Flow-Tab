interface:
  display_name: "FlowTab Test Audit"
  short_description: "Replace and reconstruct FlowTab test assets from code"
  default_prompt: "Use $flowtab-test-audit to establish a rollback anchor, clear the current test-asset boundary, reconstruct it from production behavior evidence, and close the appropriate C0, C1, or C2 Stage."

policy:
  allow_implicit_invocation: true
