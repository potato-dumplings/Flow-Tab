# Evidence-Driven Synchronization Migration

Updated: 2026-07-31

## Goal

FlowTab must decide asynchronous completion, readiness, and success from observable
evidence. Machine speed, scheduler pressure, I/O latency, and animation load may
change completion latency, but must not change the functional result.

This ledger is the stable migration boundary. Each implementation slice owns one
synchronization contract and its representative production, test, fixture, or
tooling path. A slice updates its row and is committed independently with the
row ID in the commit subject.

## Repository Baseline

- Branch at audit start: `evidence-driven-sync-migration`
- Commit at audit start: `9668770830acdeaae87278502b3e04f66101a89b`
- Pre-existing worktree content:
  `M .agents/skills/flowtab-engineering/SKILL.md`,
  `M .agents/skills/flowtab-engineering/references/handoff-contract.md`,
  `M .agents/skills/flowtab-engineering/references/validation-command-cookbook.md`,
  and `?? prompts.zip`.
- Baseline ownership rule: these four entries remain unchanged and outside
  every migration commit.

## Local Evidence Lifecycle

- Each implementation slice owns its local raw evidence under
  `.build-local/evidence-driven-sync/<slice-id>/` only while that slice is
  active.
- The tracked ledger row, closure record, and independent slice commit are the
  durable migration evidence.
- After a slice reaches its recorded terminal state, remove its reproducible
  `DerivedData`, `.xcresult` bundles, fixture products, source-package and
  module caches, temporary homes, and superseded build roots before starting
  the next slice.
- A blocked slice may retain one compact diagnostic bundle only when its named
  remediation consumes that bundle. The closure record names its owner and
  cleanup boundary.
- Historical `.build-local` paths below remain provenance path intents for the
  original local attempts. They may be unavailable after terminal cleanup.

## Audit Method

The baseline combined source search with call-path review across `FlowTab`,
`FlowTabCore`, `FlowTabSpaceFixture`, `FlowTabTests`, `FlowTabUITests`, and
repository validation/release scripts.

The initial search found:

- 37 direct `sleep`, `asyncAfter`, `Timer`, or `RunLoop` occurrences in
  production, TestingSupport, and the Space fixture.
- 13 direct sleep or RunLoop occurrences in `FlowTabTests`.
- 103 direct RunLoop occurrences in `FlowTabUITests`.
- 12 shell/JXA sleep or delay occurrences in scripts.
- 52 literal XCTest watchdog arguments in `FlowTabTests` and 554 in
  `FlowTabUITests`.
- No timing or waiting candidate in `FlowTabCore/Sources` or
  `FlowTabCore/Tests`.

The review also traced elapsed-time decisions that do not contain a wait call,
including AX scan budgets, observer warm-up/throttling, retry `notBefore`
values, input suppression windows, projection generations, process identity
stability, and expiration rules. Instrumentation timestamps and ordering
timestamps that never gate progress or success are outside this ledger.

## Classification And Status

Time classifications:

- **Evidence migration**: a fixed duration currently substitutes for completion,
  readiness, or success evidence.
- **Conditional observation**: the platform exposes no usable callback or
  notification; an immediate predicate/readback check, named cancellable
  cadence, and diagnostic watchdog are allowed.
- **Domain duration**: elapsed time is the product, fixture, animation,
  expiration, batching, or pressure protocol.
- **Watchdog**: time supplies only the terminal failure bound; success comes
  from an independent Oracle.

Statuses:

- `planned`: implementation or test migration remains.
- `verification-needed`: the time classification is allowed, but ownership,
  cancellation, naming, diagnostics, or deterministic coverage remains.
- `closed-retained`: the existing policy is already evidence-driven and its
  duration is an allowed domain rule or watchdog.
- `completed`: the migration and required validation for the row passed.
- `blocked`: required validation is unavailable and the evidence is recorded in
  the row.

Risk uses `H` for user-visible, runtime-identity, long-lived observer/task, or
hot-path changes; `M` for app/test orchestration; and `L` for isolated tooling
or policy naming. Validation abbreviations are Unit, Behavior, UI, Pressure,
and Process/Tooling.

## Migration Ledger

| ID | File and owner/symbol | Current time assumption and classification | Target evidence or retained policy; lifecycle owner | Risk and required validation | Status |
| --- | --- | --- | --- | --- | --- |
| SYNC-000 | `docs/EVIDENCE_DRIVEN_SYNCHRONIZATION_MIGRATION.md`; migration ledger baseline | The repository previously had no stable, reviewable closure ledger for time-based synchronization contracts. Process contract. | Preserve the startup Git baseline, semantic classification, dependency order, per-slice validation, and local commit trace in this document. The migration task owns updates through final closure. | L; Process/Tooling (`git diff --check`, ID/path/source-scope review). | completed by the baseline commit |
| SYNC-001 | `FlowTab/Infrastructure/Runtime/RuntimeAXRemoteWindowResolver.swift`; `windowScanResult(forPID:policy:)` | An 80–250ms wall-clock budget accepts a machine-dependent partial element-ID scan as usable output. Evidence migration. | Scan a deterministic policy-owned ID range and publish complete/unavailable evidence. Any watchdog must fail the scan with last scanned ID and observed windows rather than promote a partial result. The resolver call owns cancellation/termination. | H; Unit, Behavior, affected topology UI, runtime-topology Pressure. | blocked: implementation and Unit/Behavior/Process passed; UI/Pressure environment evidence is recorded below |
| SYNC-002 | `AppDelegate.installWorkspaceLifecycleObserver`, `RuntimeAppLaunchWindowEvidenceCoordinator`, `RuntimeProjectionService.signalAppLaunched`; app-launch convergence | A fixed 800ms delay was assumed to be enough for a launched app's windows to exist. Evidence migration. | Establish the exact app/PID AX observer before launch handling, use the launch repair as initial readback, and reconcile later AX transitions or a successful delayed observer installation. The AppDelegate-owned coordinator manages per-PID generation, exact appID/PID cancellation, monitor cleanup, and the named observer-install condition cadence. | H; Unit, Behavior, launch/topology UI, runtime-topology Pressure. | blocked: implementation and Unit/Behavior/Process passed; UI/Pressure environment evidence is recorded below |
| SYNC-003 | `RuntimeReconciliationCoordinator`, `RuntimeProjectionReconciliationDrainer`, `RuntimeProjectionService`, `RuntimeTransientRepairObservationDriver`; transient-empty repair observation | Delayed `notBefore` values `[0.1, 0.3, 0.8]` waited for AX data to become non-empty, while unrelated maintenance could advance the retry. Conditional observation. | Perform the repair readback immediately, resume from an exact AX/Space/lifecycle signal, and use a service-owned cancellable condition observer only while the payload remains incomplete. Request ID, attempt, and exact appID/PID reject stale evidence. The named `[0.1, 0.3, 0.8]` cadence repeats its last value; a 30-second watchdog terminates one uninterrupted incomplete-evidence session and reports the unmet condition plus last payload. | H; Unit, Behavior, topology UI, runtime-topology Pressure. | blocked: implementation and Unit/Behavior/Process passed; UI/Pressure environment evidence is recorded below |
| SYNC-004 | `FlowTab/Infrastructure/Runtime/RuntimeAXWindowChangeMonitor.swift`; observer install and `handleAXNotification` | Events during a 750ms warm-up are discarded and events inside a 160ms throttle window can lose the final state. Evidence migration. | Install observers, take an initial AX/window readback, and publish monotonically coalesced generations with a guaranteed trailing readback. The monitor owns observer removal; its delivery coordinator owns pending coalescing cancellation. | H; Unit, Behavior, Home/topology UI, runtime-topology Pressure. | completed |
| SYNC-005 | `FlowTab/Infrastructure/Runtime/RuntimeChromeWindowFocusBridge.swift`; `focusWindowScript` | Two fixed 50ms AppleScript delays are assumed to propagate Chrome window ordering before front-window readback. Conditional observation. | Issue the Chrome focus commands once and immediately return the exact front-window ID readback. Exact equality is the bridge acceptance Oracle; mismatch, empty/invalid output, and execution error remain explicit evidence for `RuntimeActivator`. SYNC-006 owns later activation convergence under one cancellable activation generation. | H; Unit, Behavior, exact-window UI, runtime-topology Pressure. | completed |
| SYNC-006 | `FlowTab/Infrastructure/Runtime/RuntimeActivator.swift`; `scheduleFocusRecovery` | Fixed 50ms/150ms retries probe whether the exact target became focused. Conditional observation. | Preserve exact AX/CG target readback as the sole success Oracle. Check immediately, consume usable focus/window notifications, and retain a named cancellable polling fallback plus diagnostic watchdog where macOS exposes no target-specific completion event. `RuntimeActivator` owns the task and activation generation. | H; Unit, Behavior, activation UI, runtime-topology Pressure. | completed |
| SYNC-007 | `FlowTab/Infrastructure/Runtime/RuntimeWindowPreviewProvider.swift`; ScreenCaptureKit callback bridges | One-second semaphore timeouts terminate callback waits; success already comes only from callback content. Watchdog. | Retain as a named bridge-watchdog policy. The synchronous bridge state owns late-callback rejection; tests must cover callback, error, timeout, and callback-after-timeout evidence. | M; Unit, Behavior, preview Pressure. | closed-retained |
| SYNC-008 | `FlowTab/Infrastructure/Runtime/RuntimeLogging.swift`; `scheduleFlushLocked` | A 50ms delay batches disk writes and does not establish write success. Domain duration. | Retain the named batching policy and cancellable `DispatchWorkItem`; explicit snapshot/read APIs flush synchronously before returning evidence. The diagnostics store owns the work item. | M; Unit, Behavior, log-write Pressure if changed. | closed-retained |
| SYNC-009 | `FlowTab/Features/Logs/RuntimeLogsSection.swift`; `RuntimeLogLinesViewModel` | The Logs surface polls once per second to discover appended or cleared lines. Evidence migration. | Publish append/flush/clear generations from the diagnostics owner, subscribe before the initial snapshot, and reload from the later generation. View-model start/stop owns the subscription and task. | M; Unit, Behavior, Logs UI, tab-switch Pressure. | completed |
| SYNC-010 | `RuntimeLogPrivacy.swift`, `RuntimeLogsSection`; diagnostic session expiration | The 15-minute session is an explicit expiration contract; the view advances a one-second loop to notice expiry. Domain duration. | Retain the expiration deadline, use the existing injected `now` readback for rules, and schedule one cancellable deadline wakeup through an injectable clock. Expiration succeeds only when clock readback reaches the stored deadline. | M; Unit, Behavior, Logs UI. | completed |
| SYNC-011 | `FlowTab/Features/Home/HomeLandingView.swift`, `HomePermissionObservationOwner`; Home permission observation | A perpetual one-second poll discovered TCC permission changes and treated each delayed wake as the opportunity to infer current state. Evidence migration plus conditional observation. | Use the shared permission coordinator from SYNC-012: install app-activation observation before immediate readbacks, publish only exact changed TCC evidence, and retain a named cancellable one-second fallback because TCC has no unified transition notification. Home active visibility owns start/stop and cache publication. | H; Unit, Behavior, Home permission UI, tab-switch Pressure. | completed |
| SYNC-012 | `FlowTab/Features/Settings/AppSettingsView.swift`; permission prompt observation | Forty 500ms attempts inferred post-prompt TCC convergence; the first condition check occurred after a sleep and cancellation was swallowed. Conditional observation plus watchdog. | Install the shared app-activation observer before an immediate readback, take an independent post-request readback, and use a named cancellable fallback cadence for TCC transitions without callbacks. The terminal watchdog reports target, generation, readbacks, elapsed time, bundle identity, and final evidence. Settings visibility owns both target observations. | H; Unit, Behavior, permission UI, tab-switch Pressure. | completed |
| SYNC-013 | `HomeLandingView.scheduleInitialAppSummariesRefresh`, `HomeInitialProjectionObservationOwner`; initial Home app-summary readiness | A fixed 900ms startup delay assumed the runtime app-switcher projection had committed before the Home readback. Evidence migration. | Install the exact service-object projection observer before the initial readback and maintenance request. Accept an initial complete projection, a later monotonic source generation, or a same-generation completeness transition. Home active visibility owns the observation generation, subscription, cancellation, and cleanup. | H; Unit, Behavior, Home UI, runtime-topology Pressure. | completed |
| SYNC-040 | `HomeLandingView.scheduleAppSummariesRefresh`, `HomeAppSummaryProjectionObservationOwner`; general Home app-summary refresh | A fixed 220ms post-signal delay assumed app-switcher projection commits had followed workspace and runtime refresh signals. Evidence migration. | Install the exact runtime-service observer before the baseline readback. Apply committed notification readbacks only for a newly available projection, a monotonic source generation, or same-generation completeness. App activation and permission changes request service-owned maintenance, followed by an independent request-return readback. Home active visibility owns the observation generation, subscription, cancellation, and initial-owner handoff. | H; Unit, Behavior, Home UI, runtime-topology Pressure. | completed |
| SYNC-041 | `HomeLandingView.requestSelectedAppRefresh`, `requestAppDetailProjection`, `HomeAppDetailProjectionObservationOwner`; selected/scoped Home detail | Fixed 120ms and 220ms post-selection or AX-signal delays assumed the requested current-app projection had committed. Evidence migration. | Install the exact runtime-service observer before the initial readback and selected/app-window/AX-destroy signal. Filter by exact appID and projection identity; apply an exact complete baseline immediately while retaining the trigger observation, then accept a newly available projection, monotonic source generation, or same-generation completeness transition. Independent request-return readback closes synchronous completion races. Home visibility and per-app request generations own subscriptions, cancellation, and stale-event rejection. | H; Unit, Behavior, selected-app Home UI, runtime-topology Pressure. | blocked: implementation, focused Unit/Behavior, full FlowTabTests, tab-switch Pressure, and Process passed; UI automation and full Priority order-isolation evidence are recorded below |
| SYNC-014 | `FlowTab/App/AppFoundation.swift`; `AppWindowCoordinator.scheduleAccessoryPolicyRestoration` | Up to 250 polls at 20ms infer that a status-item-opened regular window is active and visible. Conditional observation plus watchdog. | Register app/window observers before presentation, perform immediate visibility/activation readback, and complete on the matching window transition. A named cancellable condition observer is the fallback; watchdog diagnostics include app-active, visible, miniaturized, and activation-policy state. `AppWindowCoordinator` owns the task/observers. | H; Unit, Behavior, status-item UI. | completed; the expanded full Priority run's assigned SYNC-032/SYNC-033 order-isolation failure is recorded below |
| SYNC-015 | `SwitcherSearchCoordinator.scheduleRebuild`, `LiveSwitcherModel.scheduleSearchComputation` | 10–45ms delays debounce query work; result publication already requires revision and query/scope equality. Domain duration. | Retain as one named search scheduling policy with injectable scheduler/clock, cancellation, and revision Oracle. Remove duplicate scheduling ownership if both paths serve the same contract. | H hot path; Unit, Behavior, Search UI, realistic/stress Search Pressure. | completed; the expanded full Priority run's assigned SYNC-032/SYNC-033 order-isolation failure is recorded below |
| SYNC-016 | `AppDelegate.setupHotkeyMonitors`, `HotkeyRegistrationObservationOwner`, `HotkeySettingsCardAppKitView.updateTakeoverStatus`; Command+Tab registration status | A 250ms delay was treated as confirmation that Command+Tab takeover stayed inactive. Evidence migration. | Publish a monotonic registration generation with exact request ID, configurations, and takeover result after reconciliation and monitor setup. Settings observes before persistence/registration, closes synchronous delivery with readback, and renders only matching evidence. Settings active visibility owns observer cleanup and stale-generation rejection. | M; Unit, Behavior, Settings UI. | completed; the expanded full Priority run's assigned SYNC-032/SYNC-033 order-isolation failure is recorded below |
| SYNC-017 | `SwitcherPanelController+InputHandling.swift`, `ModifierReleaseObservationOwner`; modifier-release confirmation and replay suppression | Hardware release was inferred from repeated samples after fixed intervals. Conditional observation. | Install exact modifier/main-key transition observation before the initial hardware readback. Transition evidence triggers immediate readback; one named cancellable fallback sample closes missed-event and stale-readback gaps. Selection confirmation requires two stable released-state readbacks; SYNC-018 replay suppression closes from one exact released-state readback. The panel controller owns observation tokens, fallback work, and generation cancellation. | H; Unit, Behavior, switcher UI, interaction Pressure. | completed; the expanded full Priority run's assigned SYNC-032/SYNC-033 order-isolation failure is recorded below |
| SYNC-018 | `OptionTabHotkeyMonitor`, `AppDelegate.setupHotkeyMonitors`, `SwitcherHotkeyInputOwner`, `SwitcherPanelController+Hotkeys.swift`, `+SelectionLifecycle.swift`; tab throttle and post-finish ignore | 16ms and 20ms windows discard input assumed to be duplicate/replayed, so scheduling can change the selected result. Evidence migration. | Give each monitor a stable source UUID and monotonic sequence; register the exact route source and callback before explicitly starting Carbon observation. Accept each route/source/sequence identity once, reject duplicate, regressed, or replaced-source delivery, and preserve every distinct immediate event. Establish modifier/main-key observation before selection commit/end and close replay suppression only from an exact released-state readback. The controller owns input generations; AppDelegate owns monitor start/stop; the release owner owns observer/fallback cancellation. | H; Unit, Behavior, switcher UI, interaction Pressure. | completed; the expanded full Priority run's assigned SYNC-032/SYNC-033 order-isolation failure is recorded below |
| SYNC-019 | `InitialPanelVisibilityObservationOwner`, `SwitcherPanelController+InitialVisibilityRecovery.swift`; initial panel visibility | A grace duration decides whether occlusion is stale and later triggers failure. Evidence migration plus watchdog. | Install panel occlusion, become-key, and expose observers during controller initialization. Start the session observation before the first order-front action, and accept only an exact visible `PanelVisibilitySnapshot` from the initial readback, matching presentation action, window event, recovery readback, or terminal watchdog readback. The presentation session owns generation cancellation and one named watchdog that reports the last event and final snapshots. | H; Unit, Behavior, switcher UI, runtime-topology Pressure. | blocked: implementation, focused Unit/Behavior, deterministic Pressure, and Process passed; cross-slice pressure reproduced a 350ms watchdog cancellation at 372.550ms and full-suite panel-expose ordering failures; the latest SYNC-032D2 isolated and full reruns passed, while dedicated remediation remains required |
| SYNC-020 | `PanelVisibilityRecoveryObservationOwner`, `SwitcherPanelController+VisibilityRecovery.swift`; visibility recovery attempts and hard reorder | Fixed `[0, 50ms, 150ms, 300ms]` attempt waits and a fixed 10ms order-out/order-front gap assumed AppKit had committed window ordering. Evidence migration plus conditional observation. | Use the controller-lifetime occlusion/key/expose observers established by SYNC-019, an immediate exact readback, and an ordered state machine that must observe `panelPresented == false` before order-front and exact `userVisible == true` before success. The local AppKit SDK has no public order-on/off notification, so the presentation session owns one named cancellable 10ms condition-readback cadence, a four-attempt action policy, matching recovery/presentation generations, and a 1-second diagnostic watchdog. | H; Unit, Behavior, switcher UI, runtime-topology Pressure. | blocked: implementation and all-target Process build passed; the shared macOS XCTest service blocked Unit/Behavior/UI execution before test start, and runtime-topology launch identity remained unobserved |
| SYNC-021 | `ActiveSpaceTransitionObservationOwner`, `SwitcherPanelController+ActiveSpaceTransition.swift`; active-Space ignore and activation-suppression windows | 350ms/500ms windows assume which Space notifications belong to FlowTab's own presentation/migration. Evidence migration plus watchdog. | Establish the observation and exact runtime-service baseline before requesting topology refresh. Resolve only after the Space projection generation advances, then correlate current-Space identity, presentation generation, and exact panel visibility. Independent request-return and projection-notification readbacks close synchronous and missed-event races. The presentation session owns replacement, cancellation, activation suppression, and a one-second diagnostic watchdog. | H; Unit, Behavior, cross-Space UI, runtime-topology Pressure. | blocked: implementation, Unit/Behavior, deterministic Pressure, and Process passed; macOS LocalAuthentication blocked cross-Space UI and real-topology Pressure before test start |
| SYNC-022 | `TerminateInterruptionProtectionObservationOwner`, `SwitcherPanelController+TerminateInterruptionProtection.swift`, `+Hotkeys.swift`, `+InputHandling.swift`; terminate interruption protection | Five-second and 500ms uptime windows inferred that termination, projection refresh, and related AppKit presentation interruptions had finished. Evidence migration plus watchdog. | Establish an exact appID/PID/request-generation and projection baseline before sending the terminate request. Latch target completion only from matching workspace termination or terminated-process readback plus exact projection removal/replacement. Cancel the completion watchdog at that point, then retain an untimed presentation latch until active-Space, panel-visibility, or consumed interruption evidence is followed by a stable visible/active readback. The presentation session owns replacement and cancellation. | H; Unit, Behavior, termination UI, runtime-topology Pressure. | completed |
| SYNC-023 | `SwitcherPanelController+Hotkeys.swift`, `AppPreferences.swift`; delayed window-layer entry | The user-configured delay is the product contract. A secondary 350ms timer currently probes projection readiness. Domain duration plus evidence migration. | Retain the persisted auto-entry deadline under a named injectable scheduler. Enter only when both the user deadline and matching projection readiness are observed; remove the secondary readiness timer and react to projection generation events. The panel session owns timer cancellation. | H; Unit, Behavior, switcher UI, interaction Pressure. | completed |
| SYNC-024 | Composite visual-timing baseline | Semantic owner review found five independent animation or degraded-reveal contracts in the baseline row. Migration routing. | Preserve the baseline ID as the parent and close each lifecycle independently through SYNC-024A–SYNC-024E. | M aggregate; child rows define required validation. | completed: SYNC-024A–SYNC-024E closed independently |
| SYNC-024A | `TerminatePressFeedbackCompletionOwner`, `SwitcherPanelController.terminateSelectedApp`, `AppTileView`; terminate press feedback | An 80ms task sleep gated the terminate request while the rendered press animation used an unrelated 120ms duration. Domain duration plus scheduler-completion evidence. | Use one named 120ms press-feedback policy for the animation and an injectable completion scheduler. The panel controller owns one generation, cancellable token, request continuation, and late-callback rejection through session cancellation. | M; Unit, Behavior, termination UI, deterministic Pressure. | completed |
| SYNC-024B | `InitialWindowOnlyPreviewRevealObservationOwner`, `SwitcherPanelController.prepareInitialWindowOnlyPanelReveal`; initial preview reveal | A 250ms task sleep forced a degraded reveal when preview readiness had not arrived. Watchdog migration. | Use preview-batch completion plus pending-capture readback as the success Oracle. The named injectable watchdog performs a final readback and reports the last event plus unmet condition before degraded reveal; the presentation session owns cancellation and generation replacement. | M; Unit, Behavior, in-app switcher UI, deterministic Pressure. | completed |
| SYNC-024C | `SwitcherAppRemovalAnimationPolicy`, `SwitcherPanelOverlayView`; app-removal transition | A 140ms ease-out duration is a visual-only product contract for app sets up to 16 items. Domain duration. | Retain it as a named Switcher removal-animation policy. SwiftUI app-strip identity owns the animation; exact process termination and projection refresh remain the tile-removal Oracle. | L; Behavior/visual policy assertion, affected switcher UI, Process/Tooling. | completed |
| SYNC-024D | `HomeControlPressAnimationPolicy`, `FlowPageActionButton`; pressed-button transition | A 120ms ease-out duration is a visual-only Home button contract. Domain duration. | Retain it as a named injectable Home control-animation policy. SwiftUI view identity owns the animation; actions continue directly from input callbacks. | L; Behavior/visual policy assertion, affected Home UI, Process/Tooling. | completed |
| SYNC-024E | `SettingsAppVisibilityNavigationAnimationPolicy`, `AppSettingsView`, `AppVisibilityManagerView`; app-visibility navigation transition | A 180ms ease-in-out duration is a visual-only Settings navigation contract. Domain duration. | Retain it as a named injectable Settings navigation-animation policy. Settings view state owns the transition; the manager exposes contained AX children, and destination visibility remains the UI Oracle. | L; Behavior/visual policy assertion, affected Settings UI, Process/Tooling. | completed |
| SYNC-025 | `PanelPresentationDiagnosticProbeOwner`, `SwitcherPanelController+PresentationDiagnostics.swift`; frame-delay probe | A 16ms sleep sampled an assumed later display frame for diagnostics only. Domain measurement. | Retain 16ms only as a named frame-sample interval in an injectable diagnostic scheduler. A generation owner sequences next-main-turn and frame-sample callbacks, while presentation begin/end owns replacement and cancellation. Probe callbacks only emit measured diagnostics, so delayed delivery changes the recorded timestamp without affecting behavior. | L; Behavior diagnostic assertion, deterministic Pressure, Process/Tooling. | completed |
| SYNC-026 | Composite fixture-topology timing baseline | Semantic review separated the fullscreen chain, desktop refocus, and application AX suppression into independent observable contracts. Migration routing. | Preserve this parent ID as the routing boundary and close each lifecycle through SYNC-026A–SYNC-026C2. | H aggregate; child rows define required validation. | completed |
| SYNC-026A | `SpaceFixtureFullscreenTransitionOwner`, `AppKitSpaceFixtureWindow`, `SpaceFixtureWindowCoordinator`; ordered fullscreen transition chain | The workflow-configured initial delay intentionally stages fixture launch, while a fixed 1.4s gap assumed each preceding fullscreen animation had settled enough to start the next window. Domain duration plus evidence migration. | Retain the configured delay only before the first transition. Install the exact-window `didEnterFullScreen` observer before `toggleFullScreen`, then begin every later window directly from the preceding completion evidence. The coordinator-owned generation cancels scheduled work and exact-window observers on replacement; window close owns observer cleanup. | H fixture topology; Unit, Behavior, real multi-fullscreen UI, deterministic Pressure, Process/Tooling. | completed |
| SYNC-026B | `SpaceFixtureDesktopRefocusOwner`, `AppKitSpaceFixtureWindow`, `SpaceFixtureWindowCoordinator`; desktop-anchor refocus | A fixed 1.2s delay after the final fullscreen callback assumes AppKit is ready to activate and key the desktop anchor. Evidence migration with controlled condition polling. | Install exact-window and active-Space observers before activation, then read back application activity, exact plan/window identity, key/main/visible/minimized/active-Space/occlusion state, and the exact on-screen CG window. Because AppKit exposes no request-acceptance callback, use an immediate-condition-first, named 100ms retry owned by the coordinator generation. Completion comes only from the exact readback; a named 15s watchdog reports the final unmet conditions and last evidence. | H fixture topology; Unit, Behavior, desktop-preserving real fixture UI, runtime-topology Pressure. | completed |
| SYNC-026C | Composite application AX-suppression routing boundary | The producer-side projection acknowledgement and fixture-side suppression lifecycle belong to different process and resource owners. Migration routing. | Close producer publication through SYNC-026C1 and fixture suppression through SYNC-026C2. | H aggregate; child rows define required validation. | completed |
| SYNC-026C1 | `FlowTabUITestProjectionAcknowledgementOwner`, `FlowTabUITestBootstrapper`, `FlowTabTestLaunchOptions`; FlowTab TestingSupport projection acknowledgement | A fixture cannot infer when prelaunched FlowTab has committed the exact fixture process and window topology. Evidence migration. | Parse explicit test-only routes, install the runtime projection observer before initial readback, and publish a distributed acknowledgement only for a complete, clean projection matching bundle ID, positive PID, and exact window count. Include monotonic acknowledgement and source generations. The app TestingSupport bootstrap owns observation and termination cleanup. | H cross-process fixture evidence; Unit, Behavior, deterministic Pressure, Process/Tooling; end-to-end UI transport joins SYNC-026C2. | completed |
| SYNC-026C2 | `SpaceFixtureApplicationAXSuppressionOwner`, `SpaceFixtureWindowCoordinator`; fixture application AX suppression | Fixed 5s and post-fullscreen 8s delays assume workflow consumers have captured the application AX window list. Evidence migration. | Install the route observer before fixture window publication, then suppress only after the exact local topology stage, exact published application AX readback, and a matching acknowledgement for the fixture PID and window count. Preserve the route-less fixture flag through the local-topology plus exact-readback contract. Require exact zero readback, publish a monotonic suppression generation for every resolved owner, and report routed terminal evidence. The coordinator owns cancellation and cleanup. | H fixture topology; Unit, Behavior, AX-suppressed real fixture UI, runtime-topology Pressure. | completed |
| SYNC-026C2R | `FlowTabUITestAXSuppressionReadbackOwner`, `SpaceFixtureAXSuppressionObservationOwner`; authorized exact application AX readback | After terminal fixture suppression evidence, `XCUIApplication.windows.count` could report “application is not running” for the still-frontmost exact PID whose root AX window collection had intentionally become empty. A direct UI-runner AX readback returned `apiDisabled`, and the surrounding raw 100ms RunLoop loop made the result depend on XCUI session timing while owning no cancellable work. Evidence-Oracle repair. | Prelaunch the authorized fixed-path FlowTab test process with explicit completion and verification routes. FlowTab installs the completion observer before fixture launch, accepts only a later monotonic exact identity/generation, then immediately reads `kAXChildrenAttribute` and `kAXWindowsAttribute` from `AXUIElementCreateApplication(PID)` and requires the exact bundle/PID to remain running. Because external AX propagation exposes no callback, an unmet readback uses one named serial cancellable 100ms condition schedule. The UI consumer installs both completion and verification observers before fixture launch and resolves only after matching independent exact evidence. FlowTab bootstrap, workflow observation, and test-case cleanup own every observer, generation, schedule, replacement, and cancellation; the 20-second UI watchdog performs a final state readback and reports the unmet evidence. | H cross-process AX topology; deterministic initial/event/cancel/stale/duplicate/watchdog owner coverage, AX-suppressed noisy real fixture UI, lifecycle Pressure, Process/Tooling. | completed |
| SYNC-027 | Composite fixture fault-latency baseline | Semantic owner review found two independent fault-injection contracts: delayed process termination and delayed exact-window close. Domain duration routing record. | Preserve the CLI compatibility boundary and route implementation, lifecycle, evidence, and validation through SYNC-027A and SYNC-027B. | M; child-slice validation. | completed: SYNC-027A and SYNC-027B closed independently |
| SYNC-027A | `SpaceFixtureAppDelegate`, `SpaceFixtureLaunchConfiguration`, `SpaceFixtureTerminationFaultOwner`; delayed process termination | `--terminate-delay-ms` intentionally keeps the fixture process alive after an application or SIGTERM request. The raw `asyncAfter` work had no cancellation owner and exposed no scheduled/applied evidence. Domain duration. | Retain the positive delay as `SpaceFixtureTerminationFaultPolicy`. A single AppDelegate-owned generation schedules through the injectable fixture scheduler, preserves the first request across duplicate sources, publishes exact scheduled/applied evidence with bundle ID and PID, and cancels the task and signal source at app termination. The optional notification route preserves existing launch configurations while enabling an observer to be established before the termination request. | M repeated async work; Unit, Behavior, representative termination UI, deterministic lifecycle Pressure, Process/Tooling. | completed |
| SYNC-027B | `SpaceFixtureWindowCloseFaultOwner`, `SpaceFixtureWindowCoordinator`, `SpaceFixtureLaunchConfiguration`; delayed exact-window close | `--close-window-delay-ms` intentionally delays fixture window removal, while the former coordinator token exposed no scheduled/applied acknowledgement and fixture launch time could race the consumer's initial topology observation. Domain duration plus evidence-driven orchestration. | Retain the named fault duration behind an exact generation, bundle/PID, plan-index, and stable-window-number contract. Install the optional trigger observer before the initial readback and scheduled evidence; begin the delay from a matching trigger, then resolve only from AppKit visibility, exact CG-window, and coordinator-topology readback. Use an immediate first readback, named cancellable 50ms retry where WindowServer exposes no close-completion event, and a diagnostic 10s watchdog. The coordinator-owned fault owner cancels every observer and token. | M repeated async work; Unit, Behavior, representative window-removal UI, deterministic lifecycle Pressure, Process/Tooling. | completed |
| SYNC-028 | Composite fixture workflow-readiness baseline | Semantic owner review separated per-process fixture readiness, multi-application readiness aggregation, and desktop-anchor orchestration. Migration routing. | Close coordinator-owned process readiness through SYNC-028A, route workflow-wide settling through SYNC-028B1, and close final desktop-anchor activation through SYNC-028B2. | H aggregate; child rows define required validation. | completed |
| SYNC-028A | `SpaceFixtureWorkflowReadinessOwner`, `SpaceFixtureWindowCoordinator`, `SpaceFixtureWindowContentView`, `launchSpaceFixtureWorkflow`; one fixture process readiness | The visible “Ready” state and shared single-process UI launcher previously advanced before fullscreen and desktop-refocus completion, then used a fixed settle duration to infer usable topology. Evidence migration. | Start a coordinator-owned readiness generation before window publication and emit exact configured, planned-window, fullscreen-completion, desktop-presentation, application-AX-exposure, and terminal-ready evidence. Update the visible state only from terminal evidence. The UI installs a unique observer before launch, captures the configured baseline, and accepts the later ready stage only for the same generation, bundle ID, PID, window plan, and fullscreen plan. Coordinator and XCTest-case lifecycles own cancellation and observer cleanup. | H fixture topology; Unit, Behavior, representative real fullscreen UI, deterministic lifecycle Pressure, Process/Tooling. | completed |
| SYNC-028B | Composite multi-application workflow-readiness routing boundary | Semantic owner review separated readiness evidence emitted independently by every fixture process from the final desktop-anchor activation and Space readback. Migration routing. | Aggregate every configured fixture process through SYNC-028B1, then establish the final anchor through SYNC-028B2. | H aggregate; child rows define required validation. | completed |
| SYNC-028B1 | `SpaceFixtureWorkflowReadinessAggregateOwner`, `launchResolvedSpaceFixtureWorkflow`, `launchResolvedEdgeInputsWorkflow`; all-process readiness aggregation | A workflow-wide `settleTimeout` and fixed RunLoop advancement inferred when independently launched fixture processes and their Space topology were ready. Evidence migration plus watchdog. | Install a unique distributed observer for every workflow app before launching any process. Capture each exact configured baseline and accept its later ready evidence only for the configured workflow app ID, bundle ID, PID, observation generation, window plan, fullscreen plan, and titles. Complete after every app has terminal evidence. The workflow invocation owns all observers and aggregate-generation cancellation; one named watchdog reports every unmet app and last observation. | H multi-process fixture topology; Unit, Behavior, standard and edge multi-application UI, deterministic lifecycle Pressure, Process/Tooling. | completed |
| SYNC-028B1F | `SpaceFixtureResolvedWorkflow.App`, `SpaceFixtureWorkflowReadinessAggregateObservationOwner`; complete fullscreen-plan identity | The resolved UI-test workflow kept only the first fullscreen window index while the fixture producer retained every fullscreen plan entry. Aggregate static matching therefore rejected both configured and ready evidence for noisy workflows containing multiple fullscreen windows, leaving only the last raw observation visible in watchdog diagnostics. Exact topology repair. | Preserve the ordered, unique fullscreen plan indices at workflow decoding and scenario-resolution boundaries while retaining the first-index compatibility accessor. Build aggregate expectations and assertions from the complete plan. Continue accepting readiness only when configured and later ready evidence match the exact full plan, process identity, generations, window indices, and titles. | H exact fixture topology; deterministic decode/compatibility/aggregate UI-owner coverage, canonical aggregate Behavior and lifecycle Pressure, dependent multi-fullscreen real fixture UI in SYNC-028B1M, Process/Tooling. | completed |
| SYNC-028B1M | `SpaceFixtureWorkflowMetadataObservationOwner`, `waitForSpaceFixtureWorkflowMetadata`, `launchResolvedSpaceFixtureWorkflow`, `launchResolvedEdgeInputsWorkflow`; visible fixture metadata readiness | A raw 100ms RunLoop loop, separate eight-second element waits, and per-app validation before aggregate readiness inferred when XCUI had exposed the exact summary and fullscreen marker. AX-suppressed fixtures expose no application children, so the same assumption could fail after terminal process readiness. Controlled condition polling plus evidence sequencing. | Treat exact aggregate terminal readiness as the workflow sequencing Oracle. For AX-visible fixtures, create a condition owner after that transition, install its named serial XCUI readback schedule before an immediate readback, and resolve only from the exact ordered-title summary plus required fullscreen marker. For AX-suppressed fixtures, terminal aggregate evidence is authoritative. Each per-app validation owns cancellation; one named eight-second watchdog performs a final readback and reports the unmet values. | H fixture/UI automation orchestration; deterministic initial/delayed/cancel/stale/watchdog UI-owner coverage, standard, edge, and AX-suppressed real fixture UI, lifecycle Pressure, Process/Tooling. | completed |
| SYNC-028B1R | `SpaceFixtureWorkflowReadinessTransport`, `SpaceFixtureWorkflowReadinessAggregateObservationOwner`, `SpaceFixtureLaunchConfiguration`; cross-process configured-evidence readback repair | Establishing the distributed observer before a synchronous `XCUIApplication.launch()` assumed the fixture could not publish configured evidence before the observer became deliverable on the UI-test main queue. A real AX-suppressed workflow missed that baseline and exhausted the aggregate watchdog even though the same fixture later published terminal readiness. Evidence-race repair. | Keep the unique distributed notification route and add a unique atomic property-list readback containing the exact configured baseline plus latest evidence. Persist before notification publication; perform one immediate readback before aggregate evaluation and feed both records through the existing generation and exact-identity state machine. Resolve path intent at the fixture resource boundary. The fixture coordinator and workflow aggregate invocation both own cleanup. | H cross-process fixture topology; Unit, Behavior, missed-notification UI Oracle, standard and edge real fixture UI, lifecycle Pressure, Process/Tooling. | completed |
| SYNC-028B1R2 | `SpaceFixtureWorkflowReadinessAggregateObservationOwner`; notification-triggered envelope refresh | A single readback before aggregate waiting assumed any configured evidence already persisted by a synchronously launched fixture would be visible at that exact consumer turn. A delivered later ready notification could therefore leave its missed configured baseline unobserved until the watchdog. Evidence-race repair. | Install every exact-route observer before an immediate initial readback. On each delivered route notification, read that route's atomic configured/latest envelope first, then feed the notification through the same aggregate generation and exact process/topology state machine. The aggregate invocation owns observers, generation replacement, cancellation, and readback cleanup; the existing watchdog remains only the diagnostic failure bound. | H cross-process fixture topology; deterministic initial/event/stale/duplicate UI-owner coverage, AX-suppressed real fixture UI, lifecycle Pressure, Process/Tooling. | completed |
| SYNC-028B1T | `scripts/testing/run-ui-tests-local.sh`, `SpaceFixtureResolvedWorkflow.configured`; prepared fixture workflow routing into the UI-test process | Rebuilding fixture variants below a custom `--build-root` was assumed to make that workflow available to the UI test. The wrapper exported only one route, and scheme-driven `test-without-building` did not transfer later shell exports or consume a patched `.xctestrun`; the UI test therefore fell back to a stale default workflow while current source was built elsewhere. Process/Tooling evidence repair. | Resolve both workflow path intents against the wrapper invocation directory, require one generated `.xctestrun` containing `FlowTabUITests`, write both absolute routes into its test-host environment, and execute that exact file. The wrapper invocation owns the generated file and custom build root. A UI-runner Oracle loads both routed workflows and requires every app bundle to share its resolved variants directory. | H shared UI validation infrastructure; targeted routing UI, representative real fixture UI, Process/Tooling. | completed |
| SYNC-028B2 | `SpaceFixtureWorkflowDesktopAnchorObservationOwner`, `launchResolvedSpaceFixtureWorkflow`, `launchResolvedEdgeInputsWorkflow`; final desktop-anchor activation | After aggregate readiness, the launchers call `activate()` and use generic foreground state as the final desktop-anchor result. Evidence migration with exact readback. | Establish workspace observers and initial readback before activation. Resolve only when the exact readiness PID is active and frontmost, XCUI reports it foreground, the exact plan-identified XCUI window frame matches the PID-scoped topmost on-screen CG window, and that CG frame satisfies the desktop-Space non-fullscreen-size readback. The workflow invocation owns generation cancellation, observers, polling, and a diagnostic terminal watchdog. | H desktop/Space topology; Unit, Behavior, affected standard and edge UI, runtime-topology Pressure, Process/Tooling. | completed |
| SYNC-029 | `FlowTabUITestInitialPresentationObservationOwner`, `FlowTabUITestBootstrapper.presentInitialUIIfNeeded`; initial UI-test Search/switcher presentation | Twenty 150ms retries and two equal snapshots infer runtime projection stability before opening Search/switcher. Evidence migration. | Install exact projection observers before the baseline readback and readiness request. Resolve from an initially complete projection, a monotonic later generation, or a same-generation completeness transition; require the exact filtered projection/session item signature and a compatible post-presentation readback. A complete empty projection is authoritative no-content. The bootstrapper owns replacement, prepare, termination, resolution, observer, generation, task, and watchdog cleanup. | H test orchestration; Behavior, UI, runtime-topology/Search Pressure. | completed; Search activation terminal evidence follows in SYNC-029A |
| SYNC-029A | `FlowTabUITestInitialSearchActivationObservationOwner`, `FlowTabUITestBootstrapper.resolveInitialPresentation`; initial Search activation terminal state | Initial presentation publishes `.presented` when the panel/session projection matches even if `searchIsActiveOrPending` is false. A real fixture run retained `mode=appCycle` and never exposed the Search input. | Install the exact committed-Search-index observer, perform the initial readback, and request Search freshness before initial panel presentation can enqueue session maintenance. After the exact panel/session is presented, perform an immediate Search-state readback and activate Search. A matching committed-index notification may retry activation. Resolve only while the panel remains presented with the exact ordered session IDs and Search is active. Bootstrapper preparation, replacement, termination, resolution, the runtime-aligned diagnostic watchdog, and the UI-test terminal bound own cancellation and cleanup. | H TestingSupport presentation Oracle; deterministic initial/pre-presentation-event/delayed/cancel/duplicate/watchdog tests, Behavior integration, affected real Search UI, lifecycle Pressure, Process/Tooling. | completed |
| SYNC-030 | Composite TestingSupport fault-injection timing boundary | Semantic owner review separated mock preview-capture latency from initial panel-occlusion staleness. Domain fixture duration routing. | Close preview-capture latency through SYNC-030A and panel-occlusion staleness through SYNC-030B. | M aggregate; child rows define required validation. | completed |
| SYNC-030A | `FlowTabUITestMockWindowPreviewLatencyOwner`, `FlowTabUITestBootstrapper.installMockWindowPreviewsIfNeeded`; mock preview I/O latency | A blocking thread sleep intentionally delays preview capture, has no cancellation owner, and exposes no TestingSupport completion evidence. Domain fixture duration. | Retain the compatible millisecond launch option as a bounded named latency policy. A TestingSupport generation owner performs a cancellable monotonic deadline wait and publishes exact owner/batch/request/outcome evidence. Preview result identity remains the success Oracle. Bootstrap preparation, replacement, termination, and deinitialization own cancellation. | M asynchronous preview path; Unit, Behavior, affected UI, deterministic lifecycle Pressure, Process/Tooling. | completed |
| SYNC-030B | Initial panel-occlusion stale hook in `FlowTabUITestBootstrapper` | A task sleep intentionally holds stale occlusion state before releasing visibility, while generation equality informally rejects replacement. Domain fixture duration. | Retain the compatible millisecond launch option as a bounded named staleness policy; inject a cancellable scheduler, publish installed/released/cancelled evidence, and make bootstrap preparation, replacement, termination, and deinitialization own cleanup. The panel visibility/recovery state remains the success Oracle. | M presentation lifecycle; Unit, Behavior, affected UI, deterministic lifecycle Pressure, Process/Tooling. | completed |
| SYNC-031 | `TabSwitchStressPolicy`, `TabSwitchStressRunner`, `TabSwitchStressEvidenceTransport`; tab-switch stress workload | Switch cadence and total duration define an exact planned pressure workload. The former wall-clock loop allowed slower scheduling to reduce the number of switches and swallowed task cancellation. Domain duration plus exact selection evidence. | Preserve the compatible launch inputs as a bounded named policy and derive `ceil(duration / cadence)` planned selections. Start with an immediate selection, count each planned target only after exact selected-tab readback, and complete after both the monotonic duration and workload evidence are satisfied. The runner owns one generation and cancellable wake; AppDelegate termination owns stop. UI establishes a unique distributed evidence route before launch. | M hot path; Unit/Behavior for runner, affected UI, deterministic and real tab-switch Pressure, Process/Tooling. | completed |
| SYNC-032 | Shared app-test asynchronous wait baseline | Semantic caller review found independent AppVisibility reload, AppDelegate delivery, Search publication, preview publication, panel presentation, termination-feedback, delayed-entry, and launch-bootstrap owners behind the two generic polling helpers. Migration routing. | Remove the generic helpers by closing each observable lifecycle through SYNC-032A–SYNC-032H. Retain a condition observer only if a caller has no callback, notification, task completion, scheduler, generation, or published state. | M aggregate; child rows define required validation. | completed: SYNC-032A–SYNC-032H closed and both generic polling helpers removed |
| SYNC-032A | `FlowTabTests+Support.waitUntil`; `AppVisibilityManagerModel.reload` tests | Two app-inventory tests polled wall-clock time and swallowed sleep cancellation while `@Published isLoading` already exposed task lifecycle. Evidence migration. | Subscribe before `reload()`, reject the initial idle value, and resolve only from the observed `loading → idle` transition. The test scope owns and releases the Combine subscription; XCTest timeout is the diagnostic failure bound. | L test synchronization; affected Unit/Behavior, Process/Tooling. | completed |
| SYNC-032B | AppDelegate notification-delivery routing boundary | Semantic caller review separated workspace lifecycle delivery from hotkey replacement delivery. Migration routing. | Close the independently emitted workspace and hotkey contracts through SYNC-032B1 and SYNC-032B2. | M aggregate; child rows define required validation. | completed: SYNC-032B1 and SYNC-032B2 closed independently |
| SYNC-032B1 | `FlowTabPriorityCoverageTests+AsyncSupport.waitUntil`; AppDelegate workspace launch and termination delivery tests | Workspace notifications are followed by generic polling of runtime-projection recording state. Evidence migration. | Establish exact app/PID recording callbacks before posting each workspace notification, then fulfill from the matching launch or termination signal and require the final recording readback. Test teardown owns callback removal. | M app lifecycle; affected Behavior, Process/Tooling. | completed |
| SYNC-032B2 | `AppDelegate.installHotkeyObserver`; AppDelegate hotkey-notification delivery tests | A hotkey-reload notification was followed by generic polling of replacement-monitor records. Its main-queue callback also deferred active-delegate validation into an unowned task, allowing later `shared` restoration to change which delegate accepted historical delivery. Evidence and lifecycle migration. | Establish the exact registration-evidence observer and generation baseline before posting. Handle the main-queue notification synchronously so the active delegate, sender, request ID, configurations, and takeover result are bound at receipt; require final evidence and exact monitor readback. AppDelegate owns observer removal at termination. | H long-lived observer lifecycle; affected Behavior, deterministic lifecycle Pressure, Process/Tooling. | completed |
| SYNC-032C | `LiveSwitcherModel.handleApplicationTerminated`; Search preservation during projection refresh | Search scheduling and termination refresh publish model/layout completion, while the regression test polled derived state twice. Evidence migration. | Establish exact Search-result publication before mutating the query and again before terminating the app; independently observe the layout refresh, then require the final projection, scope, query, and result readback. Test scope owns callback cleanup. | M Search behavior; affected Unit/Behavior, deterministic Pressure, Process/Tooling. | completed; the expanded full run's launch-bootstrap order-isolation failure is assigned to SYNC-032H |
| SYNC-032D | Preview publication and reveal routing boundary | Semantic caller review found that model preview-batch publication and controller-owned initial panel reveal have independent event sources and lifecycles. Migration routing. | Close model publication through SYNC-032D1 and controller reveal through SYNC-032D2. | M aggregate; child rows define required validation. | completed: SYNC-032D1 and SYNC-032D2 closed independently |
| SYNC-032D1 | `FlowTabPriorityCoverageTests+WindowPreviewProviders`; `LiveSwitcherModel` preview batch publication | Terminal preview success and structured failure are inferred by polling snapshots and provider call counts after requesting capture. Evidence migration. | Install the model's preview-preparation callback before the first snapshot request. Fulfill once from exact image or failure/retry state, then independently read back the final snapshot, provider route, call count, and generation. Test scope owns callback cleanup. | M repeated preview work; affected Behavior, preview Pressure, Process/Tooling. | completed |
| SYNC-032D2 | `FlowTabPriorityCoverageTests+SwitcherInteractionRegressions`; initial window-only panel reveal | The regression polls panel alpha after asynchronous preview-batch completion even though the controller owns preview-ready evidence and reveal generation. Evidence migration. | Observe the controller's exact preview-ready resolution before presentation, then require matching observation/presentation generations, zero pending captures, panel alpha, and final image readback. Test scope owns observation cleanup. | M preview presentation; affected Behavior, deterministic lifecycle Pressure, Process/Tooling. | completed |
| SYNC-032E | Search viewport and initial-presentation sizing routing boundary | Semantic caller review found that Search-result viewport scrolling and TestingSupport launch presentation have independent event sources and lifecycles. Migration routing. | Close Search state/scroll publication through SYNC-032E1 and initial launch sizing through SYNC-032E2. | M aggregate; child rows define required validation. | completed: SYNC-032E1 and SYNC-032E2 closed independently |
| SYNC-032E1 | `FlowTabPriorityCoverageTests+SessionAndPanelSearch`; Search activation and result viewport scrolling | Search activation result state and the SwiftUI scroll request were inferred by two generic condition polls even though activation publishes synchronously and the viewport exposes a completion callback plus monotonic scroll revision. Evidence migration. | Install the viewport callback before presentation. Require the immediate Search activation readback, then accept the first-focus and wrap events only when callback result ID, current scroll revision, selected index, and selected result all match the expected transition. Test scope owns callback restoration and presentation cleanup. | M Search viewport behavior; affected Unit/Behavior, deterministic Search Pressure, Process/Tooling. | completed |
| SYNC-032E2 | `FlowTabPriorityCoverageTests+SessionAndPanelSearch`; TestingSupport initial Search presentation sizing | The test polls Search-active state and panel height after launch bootstrap even though TestingSupport publishes exact initial-presentation resolution. Evidence migration. | Observe `.flowTabUITestInitialPresentationDidResolve` before bootstrap, require a matching controller object and Search-ready evidence, then read back active Search state, exact result count/layout measurements, and final panel size. Test scope owns notification cleanup and bootstrap cancellation. | M TestingSupport presentation; affected Behavior, deterministic lifecycle Pressure, Process/Tooling. | completed |
| SYNC-032F | Shared `waitUntil` callers for terminate-shortcut entry | Tests poll pending termination after the named press-feedback completion scheduler already owns the transition. Evidence migration. | Inject the manual completion scheduler or establish the terminate-request callback before input, deliver completion explicitly, and verify the exact pending app/PID generation. | M termination behavior; affected Behavior, deterministic Pressure, Process/Tooling. | completed |
| SYNC-032G | Delayed and manual window-layer entry routing boundary | Semantic caller review found a user-configured deadline contract and an independently delivered projection-readiness contract behind the remaining generic polls. Migration routing. | Close the configured deadline through SYNC-032G1 and manual projection readiness through SYNC-032G2. | M aggregate; child rows define required validation. | completed: SYNC-032G1 and SYNC-032G2 closed independently |
| SYNC-032G1 | `FlowTabPriorityCoverageTests+DelayedWindowLayerEntry`; configured delayed window-layer entry | Tests poll the window-layer mode after a named, user-configured presentation deadline even though the delayed-entry scheduler owns delivery and exposes an exact pending lifecycle. Product time contract with deterministic delivery. | Inject `ManualDelayedWindowLayerEntryScheduler`, require the configured override or preference interval and pending owner readback before firing, then deliver the deadline explicitly and verify exact selected-app projection and mode synchronously. The presentation session owns cancellation. | M panel lifecycle; affected Behavior, deterministic owner Pressure, Process/Tooling. | completed |
| SYNC-032G2 | `FlowTabPriorityCoverageTests+ManualWindowLayerEntry`; manual window-layer projection readiness | A manual entry test polls first for projection maintenance and then for mode after invoking the explicit projection-update callback. Evidence migration. | Require the exact selected-app maintenance request immediately after input, publish the committed projection, invoke the matching update callback, and verify projection generation, selected windows, mode, and delayed-owner cancellation directly. | M panel lifecycle; affected Behavior, deterministic owner Pressure, Process/Tooling. | completed |
| SYNC-032H | `waitForLaunchBootstrapSearchAndSeededLogs`; AppDelegate UI-test bootstrap | A helper polls runtime log I/O and panel state together after launch. In suite order, a second exact pair of hotkey-monitor factory records can also arrive before that polling helper returns, while the isolated test remains green. Evidence and lifecycle migration. | Establish initial-presentation, log-change, and launch hotkey-registration evidence before launch; join the exact Search session, requested seeded-log count, and launch registration generation/readback. Identify and cancel or reject any hosted-app state from an earlier test generation. The test owns observations and one diagnostic XCTest timeout. | H hosted launch orchestration; affected Behavior, deterministic lifecycle Pressure, Process/Tooling. | completed |
| SYNC-033 | App tests with direct fixed waits; semantic review routing boundary | Raw RunLoop advances and sleeps represented independently owned SwiftUI/AppKit materialization, Settings bridge publication, notification nonblocking, AX background execution, and pressure-workload contracts. Migration routing. | Close each observable lifecycle through SYNC-033A–SYNC-033E. Earlier `PanelSessionBehavior` and `SwitcherInteractionRegressions` waits closed with their SYNC-032 owners. | M aggregate; child rows define required validation. | completed: SYNC-033A–SYNC-033E closed independently |
| SYNC-033A | `FlowTabTests+CompactActionButton`, `FlowTabTests+RuntimeLogsHostedControls`; initial hosted Logs control materialization | Two generic RunLoop condition loops wait for exact AppKit controls to appear below a SwiftUI host. Semantic review shows both representables materialize during the explicit host layout pass. | Perform the layout trigger once, then require immediate exact identifier/type hierarchy readback. Keep action-binding, style, and absence-of-native-popup assertions as independent Oracles. No observer or timer is needed when the synchronous readback is complete. | L test synchronization; affected Behavior, Process/Tooling. | completed |
| SYNC-033B | `FlowTabTests+EnglishLayout`, `FlowTabTests+PreferencesAndDiagnostics`; Settings presentation, permission, and system-appearance publication routing boundary | Generic RunLoop condition loops combine three independently owned state contracts. Migration routing. | Close system-theme readback, hosted presentation bridge publication, and permission evidence through SYNC-033B1–SYNC-033B3. | M aggregate; child rows define required validation. | completed: SYNC-033B1–SYNC-033B3 closed independently |
| SYNC-033B1 | `FlowTabTests+SystemThemeReadback`; app-scoped appearance-default contamination | A generic RunLoop loop waits for a distributed system-theme notification to return after the test has already established the expected effective appearance. Semantic review shows `SystemThemeState.refreshColorScheme()` performs the complete synchronous system-appearance readback. | Apply the app-scoped default, invoke the explicit refresh, and immediately require both `NSApp.effectiveAppearance` resolution and `SystemThemeState.colorScheme` to equal the baseline system scheme. Refresh again during test-owned restoration. | L test synchronization; affected Behavior, Process/Tooling. | completed |
| SYNC-033B2 | `SettingsPresentationObservationTestSupport`, `FlowTabTests+SettingsPresentationBridge`, `FlowTabTests+SettingsPresentationLayout`; hosted language and theme publication | RunLoop loops infer that SwiftUI published presentation state has rebuilt and updated the hosted AppKit Settings bridge. SwiftUI exposes no production render-completion callback for this representable boundary. | Wrap the unchanged Settings content with a test-owned SwiftUI transaction probe. Capture its initial context generation, install a cancellable callback before each trigger, and accept only a newer exact target context before checking localized-text or appearance topology. Keep the named XCTest watchdog as a failure bound with last-evidence diagnostics. Cold-host materialization uses direct layout readback when complete. | M Settings presentation behavior; affected Unit/Behavior, deterministic Pressure, Process/Tooling. | completed |
| SYNC-033B3 | `FlowTabTests+SettingsPermissionReadback`; permission request title publication | A RunLoop loop infers that the permission coordinator's exact post-request readback reached the hosted action button. | Capture a permission-readback generation and the denied title, then install both a cancellable granted-readback callback and exact `NSButton.title` KVO before click. Require the independent post-request readback, one request, and localized granted-state title. Keep a named XCTest watchdog as the failure bound with last-readback diagnostics. | M permission presentation behavior; affected Unit/Behavior, deterministic Pressure, Process/Tooling. | completed |
| SYNC-033C | `FlowTabPriorityCoverageTests+RuntimeProjectionNotificationPublication`; background notification publication while MainActor is unavailable | A 500ms blocking wait proves publishers return, then a raw 10ms RunLoop loop assumes cleanup eventually completes. Evidence migration plus watchdog. | Enter all publishers and register DispatchGroup completion before dispatch. Keep a named blocking watchdog solely as the nonblocking failure bound, then wait for exact publisher-return completion through XCTest to release MainActor and clean up. Test scope owns the group expectation and controller lifetime; failure reports completed notification names. | M runtime-notification delivery; affected Behavior, Process/Tooling. | completed |
| SYNC-033D | `FlowTabPriorityCoverageTests+RuntimeAXBackgroundResolution`; background remote AX resolution | A 250ms main-thread sleep is treated as proof that remote AX resolution can proceed without MainActor availability. Evidence migration plus watchdog. | Establish worker-start, resolver-invoked, and fetch-completed evidence before dispatch. Block MainActor only on a named resolver-invocation watchdog, then require exact off-main-thread readback and terminal worker completion. Test scope owns semaphores and override cleanup. | M AX runtime behavior; affected Behavior, runtime-topology Pressure, Process/Tooling. | completed |
| SYNC-033E | `FlowTabPriorityCoverageTests+RuntimeSnapshotPressure`; bounded AX collection workload | Per-item thread sleep simulates I/O latency and elapsed-time comparison is treated as concurrency success. Deterministic pressure workload migration. | Replace sleep with a named, test-owned blocking workload gate. Require the configured number of workers to enter concurrently, release work from explicit evidence, and verify bounded maximum concurrency plus ordered results independently of elapsed wall time. | M runtime hot path; affected Behavior, deterministic Pressure, Process/Tooling. | completed |
| SYNC-034 | `FlowTabUITests+Support.swift`, `+WorkflowWindowObservation.swift`, `+SpaceFixtureApp.swift`, `+ScrollingSupport.swift`, `+StatusItem.swift`, and fixture assertion helpers; shared UI condition loops | RunLoop cadence advances drive XCUI/CG/AX/process predicate observation. Conditional observation. | Centralize named UI observation cadence and watchdog diagnostics, check immediately, use `waitForExistence`/predicate expectations where possible, and keep exact CG/AX/window/process readback as the sole Oracle. XCTest case lifetime owns the wait. | H test infrastructure; affected UI suites, Process/Tooling. | in progress; SYNC-034A–SYNC-034BF completed, and remaining owners are split into later slices |
| SYNC-034A | `FlowTabUITests+WorkflowWindowObservation.waitForExactFrontmostSpaceFixtureWindow`; exact frontmost fixture-window observation | Optional AX and CG window numbers can both be absent and compare equal, allowing the condition loop to return without observing an exact window. Evidence-Oracle defect discovered by the SYNC-028B2 diagnostic run. | Reuse the generation-owned desktop-anchor observer and require the exact PID to be running, active, frontmost, and XCUI-foreground; join the exact title/identifier XCUI window to a present PID-scoped topmost CG window by valid matching frames and require desktop-Space readback. Install workspace observers before initial readback, treat XCUI attachment as an observed condition, retain only the named cancellable 100ms condition cadence where no exact attachment/window callback exists, and use the caller's named watchdog as a diagnostic failure bound. The helper invocation owns cancellation and cleanup. | H test Oracle; desktop-refocus UI, runtime-topology Pressure, Process/Tooling. | completed |
| SYNC-034B | `FlowTabUITests+ConditionObservation`, `waitForFrontmostBundleIdentifier`, status-item reopen and system-app MRU activation callers; exact frontmost application | A raw 100ms RunLoop loop begins after activation and infers that the expected application eventually became frontmost. | Start a generation-owned condition observer before activation. Register `NSWorkspace.didActivateApplicationNotification` before an initial `frontmostApplication` readback, accept only the exact bundle identifier, and use the caller timeout solely as a failure bound with last-readback diagnostics. Test scope cancels the notification token; stale, duplicate, replaced, and cancelled callbacks are rejected. | H shared UI observation; affected status-item/System MRU UI, deterministic owner Pressure, Process/Tooling. | completed |
| SYNC-034C | `FlowTabUITests+ConditionObservation`, `FlowTabUITests+SystemAppMRU.triggerAndWaitForWorkflowAppOrder`; exact switcher application order | A raw 100ms RunLoop loop returns on any complete fixture-application permutation, so a stale order can fail the caller before the expected order is published. | Install the exact-order observer before an explicit post-baseline runtime-log-confirmed switcher trigger. Check immediately, then use a test-owned main-RunLoop timer at the named XCUI readback cadence because the accessibility summary exposes no publication callback. Accept only the expected complete ordered identifiers. The helper owns timer cancellation; the caller timeout is solely a failure bound with the last summary/order readback. | H shared UI conditional observation; System MRU UI, deterministic owner Pressure, Process/Tooling. | completed |
| SYNC-034D | `FlowTabUITests+SystemAppMRU.terminateFlowTabUITestApplicationAndWait`; FlowTab process termination before MRU relaunch | A raw 100ms RunLoop loop polls `XCUIApplication.state` after termination. | Own the termination trigger and wait on XCTest's exact `.notRunning` application state. Treat the caller timeout only as a terminal failure bound and report the final application state. The helper invocation owns the wait. | M UI process lifecycle; System MRU UI, Process/Tooling. | completed |
| SYNC-034E | `FlowTabUITestConditionObservationOwner.observe`, `FlowTabUITests+StatusItem.waitForFlowTabStatusElement`; reentrant XCUI readback and status-item accessibility publication | Raw 100ms RunLoop loops repeatedly query the FlowTab application and SystemUIServer; quit-menu observation begins only after the menu-open action. The first migrated UI path proves that `XCUIElement.exists` can pump the RunLoop and reenter the shared owner, allowing an outer readback to fulfill an already-resolved generation again. | Revalidate the generation and unresolved state after every client readback and condition evaluation, discarding superseded outer work. Use the shared generation-owned scheduled readback because XCUI exposes no element-publication callback. Check immediately, establish the quit-item observer before the control-click trigger, and accept only the exact status or quit element's existence. The helper owns timer cancellation; the named watchdog is solely a failure bound with per-source last-readback diagnostics. | H shared UI observation; deterministic reentrancy and scheduler Pressure, status-item reopen/quit UI, Process/Tooling. | completed |
| SYNC-034F | `FlowTabUITests+SettingsCommandTabObservation.assertCommandTabTakeoverMarker`; cross-process Command+Tab takeover marker activation and restoration | A raw 100ms RunLoop loop begins after the action and polls the shared defaults marker with an unnamed cadence. Runtime logs establish production ordering but cannot prove that the marker is already visible to another process under I/O delay. | Establish the marker observer before the settings or quit trigger, require the inverse initial baseline, and check shared defaults immediately. Because cross-process defaults expose no reliable publication callback, use a named cancellable readback cadence until the exact expected Boolean is visible. Active-registration logs and `.notRunning` remain independent ordering evidence. The helper owns timer cancellation; the named watchdog is solely a failure bound with expected and last-observed values. | H system-shortcut UI lifecycle; affected Settings UI, existing shared-owner Pressure, Process/Tooling. | completed |
| SYNC-034G | `FlowTabUITests+HittableElementObservation`, `tapFirstHittable`, and `hasHittableElement`; exact actionable XCUI query candidate | Two shared helpers use raw 100ms RunLoop loops and wall-clock deadlines to discover the first existing, hittable query candidate. | Start a generation-owned scheduled query observer, perform an immediate readback, and accept only the first exact candidate whose `exists` and `isHittable` evidence is true. XCUI exposes no candidate-publication callback, so retain the shared named cancellable readback cadence. The helper invocation owns cancellation; its caller-supplied watchdog is solely a failure bound and reports candidate count, observed existing indices, first hittable index, source, and generation. | H shared UI interaction helper; deterministic owner tests, sidebar UI and repeated-path Pressure, Process/Tooling. | completed |
| SYNC-034H | `FlowTabUITests+ElementValueObservation`, `assertValue`, and `assertValuePrefix`; exact and prefix XCUI element-value publication | Two shared assertions use raw 100ms RunLoop loops and wall-clock deadlines to discover a matching accessibility value. | Start a generation-owned scheduled value observer before its immediate readback. Accept only an existing exact element whose string value satisfies the exact or prefix predicate. XCUI exposes no value-publication callback, so retain the shared named cancellable readback cadence. The helper invocation owns cancellation; its caller-supplied watchdog is solely a failure bound and reports identifier, existence, expected predicate, actual value, source, generation, and waiter result. | H shared UI assertion helper; deterministic owner tests, affected Settings UI, owner-lifecycle Pressure, Process/Tooling. | completed |
| SYNC-034I | `FlowTabUITestConditionObservationOwner`, `FlowTabUITestConditionReadbackScheduler`; shared scheduled XCUI readback serialization | The repeating 100ms Timer can fire while an XCUI readback is pumping the RunLoop. A slower readback therefore admits nested same-generation reads, stale XCUI elements, and machine-dependent assertion outcomes. Evidence-Oracle infrastructure defect. | Keep one cancellable one-shot Timer pending, rearm it only after its callback returns, and suppress a scheduled callback while the same generation is already reading. Preserve notification reentrancy for synchronous publication races. The condition owner owns generation rejection; the serial schedule owns its current Timer and cancellation. The named cadence only controls the next observation opportunity, while each condition/readback remains the sole success Oracle. | H shared UI observation infrastructure; deterministic scheduler/notification tests, 100 Timer and 500 generation Pressure, affected hittable/value/status-item UI, Process/Tooling. | completed |
| SYNC-034J | `FlowTabUITests+HomeWindowProjectionObservation`, `homeWindowRows`, `waitForHomeWindowRow`, `waitForHomeWindowTitle`, `assertHomeWindowTitle`, and `assertHomeWindowTitlesAbsent`; persistent Home window projection | Raw 100ms RunLoop loops and wall-clock deadlines infer that the selected app's window rows and titles have appeared or that another app's titles have disappeared. | Start a generation-owned projection observer before its immediate readback. Accept only a matching exact static title, a case-insensitive title match in a stable window-row label/value projection, or the readback absence of every prohibited title. XCUI exposes no Home-projection publication callback, so retain the shared serial named cadence. The helper owns cancellation; its caller watchdog is solely a failure bound and reports generation, source, exact visible static titles, row identifiers/labels/values, and waiter result. | H shared Home UI Oracle; deterministic owner tests, 100 lifecycle Pressure, exact activation and multi-app isolation UI, Process/Tooling. | completed |
| SYNC-034K | `FlowTabUITests+SwitcherWindowTitleObservation`, `waitForSwitcherWindowCards`; exact Switcher card title multiset | A raw 100ms RunLoop loop and wall-clock deadline infer that the window-card total and per-title counts have been published, including after a real window closes. | Start a generation-owned title-projection observer before its immediate readback. Bind the matching card query once per readback, derive total count and the complete title multiset from that element projection, and accept only exact equality with the expected multiplicities. XCUI exposes no card-projection callback, so retain the shared serial named cadence. The helper owns cancellation; its caller watchdog is solely a failure bound and reports expected/observed counts, generation, source, and waiter result. | H shared Switcher UI Oracle; deterministic duplicate-title tests, 100 lifecycle Pressure, physical Control+Tab and real window-set mutation UI, Process/Tooling. | completed |
| SYNC-034L | `FlowTabUITests+SwitcherWindowCardObservation`, `switcherWindowCardObservations`, `previewImageIdentifier`, and `assertSwitcherPreviewWindowCards`; exact Switcher card identity replacement | A raw 100ms RunLoop loop and wall-clock deadline infer that a selected app's complete card projection replaced the preceding app's anchors. | Start a generation-owned full-card projection observer and capture its baseline before the Down Arrow trigger. Gate resolution until that trigger returns, then accept only a post-trigger readback with the exact expected title multiset and card total, unique identifiers, absence of excluded titles, and complete disjointness from the preceding card identifiers. Preserve value, frame, and preview-image state in the same readback for downstream assertions and watchdog evidence. XCUI exposes no projection callback, so retain the shared serial named cadence. The helper owns cancellation; its caller watchdog is solely a failure bound with exact last-card diagnostics. | H shared Switcher identity Oracle; deterministic baseline/stale/excluded/multiplicity/identity-collision tests, 100 lifecycle Pressure, real multi-app card-isolation UI, Process/Tooling. | completed |
| SYNC-034M | `FlowTabUITests+LogsProjectionObservation`, `assertLogVisibility`, and obsolete `waitForLogs`; exact seeded Logs accessibility projection | Separate raw 200ms and fixed 1.2s RunLoop advances infer that log content or absence has settled; the marker helper has no remaining caller. | Remove the obsolete marker loop. Before tapping Logs, start a generation-owned observer with immediate baseline readback, then gate success until the trigger returns. Accept only the exact seeded-row identifier multiset and total, required tab/container existence, and prohibited-row absence. XCUI exposes no projection callback, so retain the shared serial named cadence. The helper owns cancellation; its watchdog is solely a failure bound with the acceptance gate and exact last projection. | H shared Logs UI Oracle; deterministic baseline/partial/prohibited/multiplicity tests, 100 lifecycle Pressure, runtime-log-level UI, Process/Tooling. | completed |
| SYNC-034N | `FlowTabUITests+RuntimeLogFileObservation`, `+RuntimeLogObservation`, `makeRuntimeLogFileSnapshot`, and `waitForRuntimeLogFiles`; cross-process runtime-log publication | Two used raw 200ms RunLoop loops begin after their triggers and infer that marker or regex evidence has flushed to disk; the required-plus-alternative overload has no caller. | Remove the unused overload. Resolve the Application Support log path at the UI-runner resource boundary. Start a cancellable vnode observation session and capture path, byte-count, file-identity, and event-generation baselines before the trigger. On wait, register for file events before immediate post-baseline content readback; retain a named serial 200ms readback cadence for coalesced, rotated, or unavailable vnode events. Accept only post-baseline file content satisfying the exact marker/regex contract. Baseline scope owns descriptors; each wait owns event/timer registration; watchdog expiry reports the unmet contract, event generations, content size, and bounded tail. | H shared cross-process UI Oracle; real vnode append, deterministic marker/regex tests, 100 lifecycle Pressure, affected mock-runtime UI, Process/Tooling. | completed |
| SYNC-034O | `FlowTabUITests+Support.waitForFocusedWorkflowWindow`; obsolete focused-window condition loop | A raw 100ms RunLoop loop remains in shared support even though semantic call-site review finds no caller. Obsolete test infrastructure. | Remove the unreferenced helper and close its ownerless observation lifecycle. Full-target compilation and zero symbol references are the closure evidence. | L obsolete UI-test helper; Process/Tooling. | completed |
| SYNC-034P | `FlowTabUITests+Support.assertStaticTextsAbsent`; obsolete static-text absence loop | A raw 100ms RunLoop loop remains in shared support even though semantic call-site review finds no caller. Obsolete test infrastructure. | Remove the unreferenced helper and close its ownerless observation lifecycle. Existing executable absence contracts use XCTest predicate expectations through `waitForNonExistence`. Full-target compilation and zero symbol references are the closure evidence. | L obsolete UI-test helper; Process/Tooling. | completed |
| SYNC-034Q | `FlowTabUITests+WorkflowWindowActivationObservation`, `waitForFrontmostWorkflowWindow`, and five Home/search activation callers; exact fixture-window activation | A raw 100ms RunLoop loop starts after the click or search confirmation and can accept a matching title or any matching app window as frontmost success. | Start a generation-owned observer before each trigger, capture the initial exact window readback, and gate resolution until the trigger returns. Combine workspace activation and target-process AX focused/main-window notifications with the shared named serial readback cadence. Accept only the expected bundle identifier plus exact topmost on-screen CGWindowID. The helper invocation owns all observer tokens, AX registrations, scheduled readback, cancellation, and watchdog diagnostics. | H exact window-identity UI Oracle; deterministic owner regression, 100 lifecycle Pressure, real multi-window fixture UI, Process/Tooling. | completed |
| SYNC-034R | `FlowTabUITests+ScrollingSupport.tapFirstHittableAfterScrolling` and Settings option-selection callers; exact dropdown option hittability | A raw 100ms RunLoop loop intertwines scroll actions with elapsed-time advancement and treats the deadline as the completion model. | Perform an immediate target/container readback, then use one named cancellable 100ms readback opportunity because XCUI exposes no option-frame or hittability callback. Derive one serial scroll event from exact target/container geometry, register the next readback only after that event returns, and accept only the exact first hittable candidate. The helper owns schedule cancellation; a named 30-second watchdog is solely a failure bound and reports the final geometry, step count, and delta. | H shared UI interaction Oracle; deterministic geometry/initial/slow-readback/watchdog tests, 100 lifecycle Pressure, Settings hotkey persistence UI, Process/Tooling. | completed |
| SYNC-034S | `FlowTabUITests+ScrollingSupport.tapElementAfterScrollingIntoView` and `selectHomeWorkflowApp`; exact Home app-row visibility | A raw 100ms RunLoop loop intertwines scroll actions with elapsed-time advancement and infers that an app row is actionable from elapsed time. | Read the exact target and its preferred or smallest horizontally containing fallback scroll container immediately. Accept only an existing, hittable target fully contained by the selected container, or the same exact target when no container exists. XCUI exposes no frame or hittability publication callback, so use the shared named serial cadence; derive one deterministic scroll event after each unresolved readback and register the next readback only after the event returns. The helper invocation owns scheduling, cancellation, and diagnostics; the caller watchdog is solely a terminal failure bound. | H shared UI interaction Oracle; deterministic selection/geometry/initial/slow-readback/watchdog tests, 100 lifecycle Pressure, real multi-app Home UI, Process/Tooling. | completed; affected real multi-app Home UI passed under the SYNC-034T validation closure |
| SYNC-034T | `FlowTabUITests+SpaceFixtureHomeActivation.selectHomeWorkflowApp`, `FlowTabUITestHomeAppSelectionObservationOwner`; exact selected-app Home projection | An outer wall-clock loop repeatedly performs 100ms title waits, limits each row action to two seconds, waits 1.5 seconds after a click, and clicks again when the expected projection has not appeared. Scheduling therefore controls action repetition and selection success. | Establish a selection generation and exact title-projection baseline before the row action. Accept an already matching baseline. Otherwise pause conditional XCUI polling while the SYNC-034S row owner scrolls and taps, perform an immediate trigger-return readback, then enable the shared named serial cadence only while the persistent exact title condition remains unmet. The selection invocation owns the deferred schedule, cancellation, monotonic watchdog budget, and final projection diagnostics. | H multi-owner Home UI orchestration; deterministic initial/trigger/delayed/watchdog tests, 100 generation Pressure, real multi-app Home isolation UI, Process/Tooling. | completed |
| SYNC-034U | `FlowTabUITests+WorkflowWindowObservation.waitForTopmostWorkflowCGWindow`; obsolete topmost-window loop | A raw 100ms RunLoop loop remains in shared workflow support even though semantic call-site review finds no caller. Obsolete test infrastructure. | Remove the unreferenced helper and its ownerless polling lifecycle. Full-target compilation and zero symbol references are the closure evidence. | L obsolete UI-test helper; Process/Tooling. | completed |
| SYNC-034V | `FlowTabUITests+WorkflowWindowObservation.waitForExactFrontmostWorkflowCGWindow`; exact persistent fixture-window readback | A used raw 100ms RunLoop loop starts at each caller and infers that the expected fixture window is frontmost from elapsed observation opportunities. | Reuse the SYNC-034Q generation-owned observer. Register workspace activation plus target-process AX focused/main-window notifications and the named serial conditional-readback cadence before an immediate initial readback. Accept only the exact frontmost bundle identifier plus exact topmost on-screen CGWindowID. The helper invocation owns registrations, generation, schedule, cancellation, and watchdog final-readback diagnostics. | H exact window-identity UI Oracle; deterministic initial/event/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected global/in-app/search fixture UI, Process/Tooling. | completed |
| SYNC-034W | `FlowTabUITests+WorkflowSpaceWindowObservation`, `waitForFrontmostWorkflowSpaceCGWindow`; exact frontmost fullscreen-Space fixture window | A used raw 100ms RunLoop loop starts after fixture setup and infers that the expected fullscreen sibling is frontmost from elapsed observation opportunities. Its CGWindowID is not known at this boundary. | Start a narrow generation-owned owner with workspace activation, target-process AX focused/main-window notifications, and the named serial conditional-readback cadence registered before the immediate readback. Accept only the exact frontmost bundle plus a topmost CG window whose title matches or whose titleless frame satisfies the established fullscreen-Space geometry contract, and return that exact window evidence. The helper invocation owns all registrations, schedule, generation, cancellation, and watchdog diagnostics. | H fullscreen-Space UI Oracle; deterministic initial/event/titleless-fullscreen/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected Option+Tab/Control+Tab/window-search fixture UI, Process/Tooling. | completed |
| SYNC-034X | `FlowTabUITests+WorkflowWindowObservation.waitForActiveSpaceWorkflowCGWindow(windowNumber:title:app:timeout:)`; obsolete exact-ID active-Space loop | A raw 100ms RunLoop loop remains as an overload even though semantic call-site review finds no caller supplying `windowNumber`. Obsolete test infrastructure. | Remove the unreferenced overload and its ownerless polling lifecycle. Full-target compilation and zero symbol references are the closure evidence. | L obsolete UI-test helper; Process/Tooling. | completed |
| SYNC-034Y | `FlowTabUITests+WorkflowSpaceWindowObservation`, `waitForActiveSpaceWorkflowCGWindow(title:app:timeout:)`; active-Space topmost fullscreen fixture window | A used raw 100ms RunLoop loop begins after the FlowTab panel becomes ready and infers that the expected fixture fullscreen Space remains active. FlowTab may be the system-frontmost application at this boundary. | Reuse the generation-owned workflow-Space window owner with an explicit `activeSpace` scope. Register target-process AX focused/main-window events, workspace activation, and the named serial conditional-readback cadence before the immediate readback. Accept only a topmost target-process CG window whose title matches or whose titleless frame satisfies the established fullscreen-Space geometry contract; preserve the `frontmost` scope's exact bundle requirement for SYNC-034W. The helper invocation owns all observation inputs, generation, cancellation, and watchdog diagnostics. | H active-Space UI Oracle; deterministic scope/initial/event/titleless-fullscreen/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected Option+Tab/Control+Tab/window-search fixture UI, Process/Tooling. | completed |
| SYNC-034Z | `FlowTabUITests+WorkflowSpaceWindowCollectionObservation`, `waitForWorkflowSpaceContainingCGWindow`; noisy active-Space fixture-window collection | A raw 100ms RunLoop loop starts after fixture launch or panel presentation and scans every target-process on-screen CG window until one matches the expected title or titleless fullscreen geometry. The matching fixture window may sit behind another sibling. | Start a generation-owned collection observer with workspace activation, target-process AX focused/main-window notifications, and the named serial conditional-readback cadence registered before the immediate full-collection readback. Accept any exact member matching the title or established titleless fullscreen-frame contract; preserve collection ordering only as diagnostic evidence. The helper invocation owns registrations, schedule, generation, cancellation, and watchdog final-collection diagnostics. | H multi-window active-Space UI Oracle; deterministic initial/non-topmost/event/titleless-fullscreen/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected noisy Option+Tab/Control+Tab/window-search fixture UI, Process/Tooling. | completed |
| SYNC-034AA | `FlowTabUITests+Settings`, `FlowTabUITests+AverageLuminanceObservation`; Settings theme screenshot-luminance publication | Two raw 100ms RunLoop loops begin after selecting light or dark appearance and repeatedly sample the Settings screenshot until a threshold matches. | Start a generation-owned luminance observer before each theme selection and gate success until the action returns. Perform an immediate trigger readback, then retain the shared named serial XCUI cadence because screenshot pixels expose no publication callback. Accept only an existing exact Settings element whose sampled average luminance satisfies the requested threshold. The helper invocation owns generation, scheduling, cancellation, and watchdog final-readback diagnostics. | H visible Settings appearance Oracle; deterministic initial/trigger/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected theme/language Settings UI, Process/Tooling. | completed |
| SYNC-034AB | `FlowTabUITests+SwitcherDiagnosticsObservation`, `testControlTabFirstPhysicalGestureSelectsNextWindowWithVisiblePreview`; exact first-gesture diagnostics projection | Separate raw 100ms and 20ms RunLoop loops start after the physical Control+Tab action and independently accept `selectedWindow` and `previewImages`, allowing values from different readbacks to form success. | Start one generation-owned diagnostics observer before the key action, gate success until the action returns, and read the complete accessibility value once per observation. Require `selectedWindow=mock-current-secondary` and `previewImages=2` in the same exact projection. Perform an immediate trigger readback, then retain the shared named serial XCUI cadence because accessibility value publication has no callback. The helper invocation owns generation, scheduling, cancellation, and watchdog final-projection diagnostics. | H first-gesture Switcher Oracle; deterministic parsing/initial/trigger/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected physical Control+Tab UI, Process/Tooling. | completed |
| SYNC-034AC | `FlowTabUITests+SwitcherAndSearch`, shared diagnostics owner; initial and pointer-hover selection projection | A raw 100ms RunLoop helper begins after initial presentation or pointer movement and separately waits for exact app, window, or percent-encoded Search selection values. | Use the shared generation-owned diagnostics owner. The initial selected-app path performs an immediate readback; each pointer path registers before both hover actions, gates success until they return, and then performs an immediate trigger readback. Accept only the exact requested persistent key/value projection, decoding Search percent encoding as specified. Retain the shared named serial cadence where XCUI has no publication callback. Each helper invocation owns generation, scheduling, cancellation, and watchdog diagnostics. | H pointer-selection UI Oracle; deterministic raw/decoded/initial/trigger/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected Option+Tab/Control+Tab/Search pointer UI, Process/Tooling. | completed |
| SYNC-034AD | `FlowTabUITests+SpaceFixtureRuntimeTruthEntrypoints`, shared diagnostics owner; selected app and window-cycle mode | Separate raw 100ms RunLoop helpers start after app-selection evidence or after the `advanceDown` command and wait for exact `selected` or a `windowCycle` mode prefix. | Extend the shared diagnostics expectation with explicit `equals` and `hasPrefix` value-match strategies. Resolve persistent selected-app state from immediate exact readback. Install the mode observer before `advanceDown`, gate success until command/log tracing returns, and require a post-trigger `mode` value with the exact prefix. Retain the shared named serial cadence where XCUI has no publication callback. Each invocation owns generation, scheduling, cancellation, and watchdog diagnostics. | H real-fixture Option+Tab Oracle; deterministic exact/prefix/initial/trigger/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected clean/noisy Option+Tab UI, Process/Tooling. | completed |
| SYNC-034AE | `FlowTabUITests+SpaceFixtureRuntimeTruthEntrypoints.assertWindowSearchUsesCommittedGenerationIndex`; committed Search-index diagnostics projection | A raw 100ms RunLoop loop reads five diagnostics keys separately and treats their eventual conjunction as proof that Search uses the committed current-generation index. | Reuse the shared generation-owned diagnostics observer and read all five keys from one accessibility-value snapshot. Check immediately and accept only `committedGenerationValidated`, `committedGenerationResult`, non-degraded, current-generation coverage, and no outstanding freshness barrier in the same projection. Retain the shared named serial cadence where XCUI exposes no publication callback. The assertion invocation owns generation, scheduling, cancellation, and a diagnostic watchdog. | H real-fixture Search-index Oracle; deterministic atomic/initial/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected clean/noisy window-search UI, Process/Tooling. | completed |
| SYNC-034AF | `FlowTabUITests+SwitcherSearchResultObservation`, `waitForSearchWindowResult`, and RuntimeTruth result-set readiness; complete Search result projection | Two raw 100ms RunLoop loops begin after Search presentation or query delivery. One accepts a matching title/app result; the other accepts an expected per-app title set with optional exact row count. | Start a generation-owned Search-result projection owner before an immediate full readback. Parse diagnostics or exact XCUI result elements into one snapshot and accept only a matching window result or the requested app's complete title/count contract. Retain the shared named serial cadence because XCUI and diagnostics values expose no publication callback. Each invocation owns generation, schedule, cancellation, and a watchdog that reports the final complete result projection. | H real-fixture Search result Oracle; deterministic exact/fallback/initial/delayed/noisy/cancel/watchdog tests, 100 lifecycle Pressure, affected clean/noisy window-search UI, Process/Tooling. | completed |
| SYNC-034AG | `selectSearchResultForConfirmation`, shared diagnostics owner; exact selected Search result | A raw 100ms RunLoop helper begins after select-result command delivery and repeatedly percent-decodes `searchSelectedResult` until it matches the requested result ID. | Reuse the generation-owned diagnostics observer with percent-decoded exact matching. Check the persistent selection immediately, then retain the shared named serial cadence because diagnostics publication exposes no callback. The invocation owns generation, scheduling, cancellation, and a watchdog that reports the encoded and normalized final projection. | H real-fixture Search selection Oracle; deterministic percent-decoded/initial/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected clean/noisy window-search UI, Process/Tooling. | completed |
| SYNC-034AH | `FlowTabUITests+SwitcherAppProjectionObservation`, `waitForSwitcherAppsSummary`, and `waitForSwitcherAppEntry`; Switcher app-strip diagnostics projection | Two raw 100ms RunLoop loops parse the same `apps` diagnostics value. One waits for an exact `bundle:windowCount` entry; the other accepts the bundle identity regardless of count for noisy topology. | Start a generation-owned app-projection owner before an immediate full readback. Parse each entry once into raw value, bundle identity, and optional window count. Accept only the exact entry or exact bundle identity required by the caller. Retain the shared named serial cadence because diagnostics publication exposes no callback. Each invocation owns generation, schedule, cancellation, and a watchdog with the final raw and parsed projection. | H real-fixture Switcher app-strip Oracle; deterministic parsing/exact/initial/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected Option+Tab/Control+Tab and clean/noisy workflows, Process/Tooling. | completed |
| SYNC-034AI | `FlowTabUITests+SwitcherSelectedWindowTitleObservation` and both `assertSwitcherSelectedWindowTitle` overloads; selected-window diagnostics projection | Two raw 100ms RunLoop loops repeatedly read `selectedWindowTitle` and infer that an exact title or one allowed fullscreen title has become selected. | Start a generation-owned selected-title projection owner before an immediate full diagnostics readback. Accept only an existing projection whose exact title satisfies the caller's single-title or allowed-title-set contract. Retain the shared named serial cadence because diagnostics publication exposes no callback. Each assertion invocation owns generation, scheduling, cancellation, and a watchdog with the final raw projection and selected title. | H real-fixture selected-window Oracle; deterministic exact/set/initial/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected Option+Tab/Control+Tab clean/noisy window-state UI, Process/Tooling. | completed |
| SYNC-034AJ | `FlowTabUITests+SwitcherPreviewProjectionObservation`; duplicated preview waits in multi-app, RuntimeTruth, in-app window switching, and noisy Option+Tab workflows | Five used raw 100ms RunLoop loops repeatedly parse `preview`, while one subset helper has no caller. Callers require exact title-set equality, duplicate-sensitive title count plus set equality, or all standard windows plus at least one real fullscreen sibling. | Start a generation-owned preview projection owner before an immediate full diagnostics readback. Parse `preview` once and accept only the exact title set, exact count plus set, or standard/fullscreen subset-and-intersection contract requested by the caller. Remove the unused subset helper. Retain the shared named serial cadence because diagnostics publication exposes no callback. Each invocation owns generation, scheduling, cancellation, and a watchdog with the final raw preview projection and parsed titles. | H real-fixture preview Oracle; deterministic parsing/exact/count/noisy/initial/delayed/cancel/watchdog tests, 100 lifecycle Pressure, affected multi-app, Control+Tab, and Option+Tab clean/noisy fullscreen UI, Process/Tooling. | completed |
| SYNC-034AK | `FlowTabUITests+SwitcherSelectedAppPreviewObservation` and `matchedWorkflowAppForVisibleSwitcherPreview`; atomic selected-app preview resolution | A raw 100ms RunLoop loop reads `selected` and `preview` separately, then infers which unvisited workflow app owns the currently visible title set. Scheduling between reads can combine different published projections. | Read both keys from one diagnostics accessibility value in a generation-owned multi-key owner. Accept only one eligible candidate whose exact bundle identity and expected title set match the same snapshot. Check immediately; retain the shared named serial cadence because diagnostics publication exposes no callback. The invocation owns generation, scheduling, cancellation, and a watchdog with the final selected bundle, titles, raw projection, and eligible candidates. | H real-fixture app/window identity Oracle; deterministic atomic/initial/delayed/exclusion/cancel/watchdog tests, 100 lifecycle Pressure, affected multi-app window-card isolation UI, Process/Tooling. | completed |
| SYNC-034AL | `FlowTabUITestWindowLayerPreviewTransitionObservationOwner`, `performAndWaitForWindowLayerPreviewTransition`, and delayed window-layer entry UI regressions | A raw 20ms RunLoop loop starts after the trigger and fails when the exact preview marker does not appear within a 100ms grace after observing `windowCycle`, allowing scheduler load to change the result. | Start a generation-owned observer before the trigger, capture an initial readback, gate acceptance until the trigger returns, and accept only a complete readback whose diagnostics value atomically reports `windowCycle` and an active preview projection while the exact expected preview marker exists. The marker is rendered only for a nonnil preview image; keep the adjacent image count as final diagnostics because its accessibility value can publish after the marker. Preserve the prewarm-before-entry contract through an ordered runtime-log Oracle established before the trigger. XCUI exposes no projection callback, so retain the shared named serial cadence. The helper owns cancellation; its named watchdog is solely a failure bound with the final diagnostics and exact-marker evidence. | H delayed presentation and preview Oracle; deterministic initial/gated/delayed/cancel/watchdog tests, 100 lifecycle Pressure, focused and repeated delayed-entry UI, Process/Tooling. | completed |
| SYNC-034AM | `terminateFlowTabUITestApplicationIfRunning`, `FlowTabUITests.setUpWithError`, and `tearDownWithError`; shared UI application process lifecycle | `XCUIApplication.terminate()` returned before the exact process reached `.notRunning`, allowing a prior test process and its Darwin notification observers to overlap the next test launch. | Capture each exact URL or bundle target's initial state, request termination when running, immediately read back state, then use `XCUIApplication.wait(for: .notRunning)` and a final state readback. Success is determined only by the final `.notRunning` evidence. The setup/teardown invocation owns the wait; the named watchdog is solely a failure bound and reports target plus initial, post-trigger, waiter, and final states. | H shared UI process lifecycle; deterministic initial/immediate/delayed/wait-boundary/watchdog tests, repeated application lifecycle Pressure, Process/Tooling. | completed |
| SYNC-034AN | `FlowTabUITests+PointerSwitcherPresentation`, `postFlowTabUITestSwitcherTriggerAndWaitForDelivery`, Option+Tab launch arguments, `SwitcherTriggerNotificationObserver`, `openInAppSwitcherForPointerHover`, and `openSearchSwitcherForPointerHover`; pointer-test switcher presentation | Option+Tab opens the panel during launch, while Control+Tab and Search repeatedly post Darwin notifications after launch and use 1.2-second existence windows plus an eight-second deadline to infer listener delivery and presentation. | Launch every on-demand pointer path with the test trigger listener and runtime logging active. Capture the runtime-log baseline before one exact global, in-app, or Search notification, require its post-baseline `presented=1 syntheticModifierHeld=1` completion record, then require the exact diagnostics panel element and each caller's exact tile, card, or result element. The test trigger holds the mode's primary modifier through the launched process so visibility recovery observes the same input contract as a held shortcut. The invocation owns log observation and XCTest existence waits; named four- and five-second watchdogs are solely failure bounds with runtime-log and accessibility diagnostics. | H shared pointer presentation transport; affected Option+Tab/Control+Tab/Search pointer UI, repeated lifecycle Pressure, Process/Tooling. | completed |
| SYNC-034AO | `FlowTabUITestSwitcherTriggerDeliveryObservationOwner` and `postFlowTabUITestSwitcherTriggerAndWaitForDelivery`; switcher-trigger receipt and presentation completion | One four-second runtime-log watchdog waits only for the final presentation record, combining Darwin notification delivery with potentially expensive real-topology session construction. Under noisy fixture topology the listener records receipt while valid runtime collection continues beyond that bound. | Establish receipt and completion observers from one pre-trigger runtime-log baseline before posting. First require the exact notification receipt record, then require the exact `presented=1 syntheticModifierHeld=1` completion record. Retain separate named four-second receipt and 12-second presentation-completion watchdogs solely as failure bounds; each reports the missing stage and final post-baseline log projection. The trigger invocation owns both observers, schedules, cancellation, and cleanup. | H shared UI trigger transport; deterministic initial/delayed/out-of-order/cancel/watchdog tests, 100 lifecycle Pressure, affected noisy Control+Tab and representative pointer UI, Process/Tooling. | completed |
| SYNC-034AP | `terminateSpaceFixtureApplicationAndWait` and Space-fixture cleanup callers; exact fixture process termination | A raw 100ms RunLoop loop begins after callers request termination and silently exits at its wall-clock deadline, so cleanup success and the next fixture launch can depend on polling opportunities. | Reuse the shared application-termination evidence contract. Capture the exact `XCUIApplication` state before requesting termination, perform one termination request, read back immediately, use XCTest's persistent `.notRunning` state wait only while needed, and require a final `.notRunning` readback. Each cleanup invocation owns the wait; its named watchdog is solely a failure bound and reports target plus initial, post-trigger, waiter, and final states. | H shared real-fixture process lifecycle; deterministic initial/immediate/delayed/wait-boundary/watchdog tests, affected lifecycle UI, repeated cleanup Pressure, Process/Tooling. | completed |
| SYNC-034AQ | `testHomeInitialAppLayerUsesRuntimeOrderAndZeroCountsWithoutAccessibilityPermission` and `assertHomeAppRowValue`; exact Home app-row value publication | A private raw 100ms RunLoop loop and wall-clock deadline infer that each Home app row has published the exact zero-window value. | Reuse the shared generation-owned element-value observer. Perform an initial row existence/value readback, accept only the exact `"0w"` accessibility value, and retain the shared named serial cadence because XCUI exposes no value-publication callback. Each assertion invocation owns generation, scheduling, cancellation, and cleanup; the two-second watchdog is solely a failure bound with identifier, existence, expected predicate, final value, source, generation, and waiter result. | H Home value Oracle; shared deterministic owner regression and lifecycle Pressure, affected Home UI, Process/Tooling. | completed |
| SYNC-034AR | `FlowTabUITestHomeWindowProjectionObservationOwner`, `performAndWaitForHomeWindowTitlePrefix`, and `testHomeWindowListUsesSeededWindowRecency`; ordered Home window projection | A private raw 100ms RunLoop loop begins after the selected-app trigger and accepts the expected first two labels before a wall-clock deadline. | Add an exact nonempty row-label-prefix rule to the shared Home projection owner. Establish the generation-owned observer before selecting the Home app, gate baseline evidence until the trigger returns, perform an immediate trigger readback, and accept only the complete ordered prefix. XCUI exposes no projection callback, so retain the shared named serial cadence. The invocation owns generation, scheduling, cancellation, and cleanup; the 12-second watchdog is solely a failure bound with the exact ordered row projection, source, generation, and waiter result. | H real-fixture recency Oracle; deterministic initial/wrong-order/delayed/empty/cancel and 100-lifecycle Pressure, affected Home real-topology UI, Process/Tooling. | completed |
| SYNC-034AS | `testSwitcherPanelQuitShortcutKeepsRealFixtureAppUntilProcessTerminates` and `waitForApplicationToTerminate`; natural fixture process exit after termination is applied | A private raw 100ms RunLoop loop starts after exact fixture applied evidence and infers that the process eventually exits before a wall-clock deadline. | Use XCTest's exact `XCUIApplication.wait(for: .notRunning)` state observation, followed by a final `.notRunning` readback. The wait handles an already-stopped initial state and later process exit; success requires both waiter completion and final state. The test invocation owns the wait, and the eight-second watchdog is solely a failure bound reporting waiter completion and final state. Retain the fixture's 2.4-second delayed termination as an explicit named fault-injection contract with scheduled/applied generation evidence. | H real-fixture process lifecycle; affected delayed-termination UI, existing deterministic process-state regression and lifecycle Pressure, Process/Tooling. | completed |
| SYNC-034AT | `waitForSwitcherSummary`, `selectSwitcherAppDirectly`, and real-fixture window-layer entry callers; exact Switcher summary projection | A private raw 100ms RunLoop loop starts after selection or Down-key triggers, accepts substring matches, and one representative path advances the RunLoop for 200ms before Down. | Reuse the shared generation-owned Switcher diagnostics observer. Prepare the selection payload, establish observation before posting the selection notification, and require the exact `selected` application identity. Establish observation before each Down key and require the `mode` value to have the semantic `windowCycle` prefix. Perform an immediate post-trigger readback; retain only the shared named XCUI cadence because accessibility exposes no summary-publication callback. Each invocation owns generation, scheduling, cancellation, and cleanup; the watchdog is solely a failure bound with exact parsed key/value evidence. Remove the redundant `apps=` wait after exact app-tile nonexistence already proves projection removal. | H real-fixture Switcher orchestration; exact-identity regression, deterministic initial/trigger/delayed/cancel/watchdog and 100-lifecycle Pressure, affected workflow-window UI, Process/Tooling. | completed |
| SYNC-034AU | `assertInAppWindowSwitcherReady` and `waitForInAppSwitcherAppEntry`; exact noisy Control+Tab application entry readiness | A private raw 100ms RunLoop loop parses `apps` after Control+Tab delivery and treats the first matching entry as readiness. | Reuse the shared generation-owned Switcher app-projection observer. Register its named cancellable cadence before an immediate initial readback, parse each entry into an exact bundle identifier and optional count, and accept only the target bundle identity. Initial readback closes the already-delivered trigger race; later readiness comes only from exact projection readback. The helper invocation owns generation, scheduling, cancellation, and cleanup; the eight-second watchdog is solely a failure bound with final parsed entries. | H noisy real-topology readiness Oracle; exact-prefix regression, deterministic initial/delayed/cancel/watchdog and 100-lifecycle Pressure, affected noisy Control+Tab UI, Process/Tooling. | completed |
| SYNC-034AV | `terminateAllSpaceFixtureProcesses`; exact prelaunch fixture process termination | A named 50ms RunLoop polling cadence merges termination notifications with repeated `NSRunningApplication.isTerminated` readbacks until a five-second deadline. | Establish a generation-owned exact-PID termination observer before `forceTerminate`. Register `NSWorkspace.didTerminateApplicationNotification` before the initial readback, accept only when every target PID reads as terminated, and remove the polling cadence. The helper invocation owns notification cancellation and cleanup; the five-second watchdog is solely a failure bound with target, terminated, and missing PID sets. | H fixture prelaunch lifecycle; deterministic initial/duplicate/out-of-order/cancel/watchdog tests, 100-generation Pressure, affected desktop-refocus UI, Process/Tooling. | completed |
| SYNC-034AW | `selectEdgeWorkflowAppInSwitcherAppLayer`; exact Edge fixture application selection | A private raw 100ms RunLoop loop starts after posting `.selectApp` without a completion receipt and repeatedly reads the `selected` diagnostics key until a wall-clock deadline. | Prepare the exact bundle-identity payload, establish the shared generation-owned Switcher diagnostics observer before posting `.selectApp`, gate acceptance until the command completion receipt returns, and close the synchronous race with an immediate trigger readback. Accept only exact `selected` identity equality. The helper invocation owns generation, named fallback scheduling, cancellation, and cleanup; the caller timeout is solely a failure bound with the final atomic diagnostics projection. | H shared real-fixture selection Oracle; deterministic exact-identity/trigger/delayed/cancel/watchdog tests, 100-generation lifecycle Pressure, affected Edge fixture UI, Process/Tooling. | completed |
| SYNC-034AX | `FlowTabUITestSwitcherWindowCardObservationOwner`, `performAndWaitForSwitcherWindowCards`, and Edge fixture preview callers; exact window-card collection | A private raw 100ms RunLoop loop starts after Down Arrow and repeatedly rebuilds an Edge-only card collection until count, unique identifiers, and title multiset happen to match. | Reuse the shared window-card collection owner and move the Down Arrow trigger inside its lifetime. Reject matching pre-trigger evidence, request an immediate post-trigger readback, and accept only the exact card count, distinct stable identifiers, and title multiset in one snapshot. XCUI exposes no collection-publication callback, so retain the shared named cancellable cadence as fallback. The invocation owns generation, scheduling, cancellation, and cleanup; the eight-second watchdog is solely a failure bound with full card evidence. | H real-fixture identity collection Oracle; deterministic trigger/excluded-title/duplicate-identity/cancel/watchdog tests, 100-generation lifecycle Pressure, affected Edge fixture UI, Process/Tooling. | completed |
| SYNC-034AY | `FlowTabUITestSwitcherSearchResultObservationOwner`, committed Search diagnostics snapshot, and Edge fixture Search callers; exact window-result identifier collection | A private raw 100ms RunLoop loop starts after text input and repeatedly parses `app.debugDescription` until a filtered unique-identifier count happens to match. It has no evidence that the asynchronous Search computation committed the typed query, so a stale row count can satisfy the wait. | Reuse the shared generation-owned Search-result owner and move text input inside its lifetime. Read `searchResultsScope`, percent-decoded `searchResultsQuery`, and `searchResults` atomically from one diagnostics value. Accept only after trigger return when the committed scope and query exactly match and the filtered result identifiers have the exact count and are distinct. XCUI diagnostics expose no publication callback, so retain the shared named cancellable cadence as fallback. The invocation owns generation, scheduling, cancellation, and cleanup; the eight-second watchdog is solely a failure bound with the final atomic Search projection. | H real-fixture Search identity Oracle; deterministic stale-query/wrong-scope/duplicate-identity/trigger/cancel/watchdog tests, delayed-scheduler and 100-generation lifecycle Pressure, affected Edge fixture Search UI, Process/Tooling. | completed |
| SYNC-034AZ | `selectSwitcherWorkflowAppDirectly`; optional direct selection before keyboard fallback in the multi-app fixture | A fire-and-forget `.selectApp` command is followed by a private raw 80ms RunLoop loop until an exact `selected` readback matches or a 1.5-second deadline chooses the keyboard fallback. | Prepare the exact bundle payload, establish the shared generation-owned diagnostics observer before posting `.selectApp`, gate acceptance until the post returns, and request an immediate trigger readback. Accept only exact `selected` bundle identity. The invocation owns generation, named fallback scheduling, cancellation, and cleanup; the 1.5-second watchdog only chooses the existing keyboard fallback and logs the final diagnostics evidence. | H repeated real-fixture app selection; deterministic exact-identity/trigger/delayed/cancel/watchdog tests, 100-generation lifecycle Pressure, affected multi-app fixture UI, Process/Tooling. | completed |
| SYNC-034BA | `FlowTabUITestSwitcherSelectionTransitionObservationOwner` and `selectSwitcherWorkflowApp`; physical keyboard fallback between multi-app Switcher selections | The fallback reads `selected` and `mode` separately, sends Up or Right, then advances the RunLoop for a fixed 120ms or 80ms before assuming the key transition is available. | Read the baseline `selected` and `mode` atomically, establish a generation-owned transition observer before each physical key, gate acceptance until input synthesis returns, and request an immediate trigger readback. Up accepts only the same exact application leaving `windowCycle` for `appCycle`; Right accepts only a supported atomic state transition from the exact app-cycle baseline. XCUI diagnostics expose no publication callback, so retain the shared named cancellable cadence as fallback. Each key invocation owns generation, scheduling, cancellation, and cleanup; the named three-second watchdog solely terminates a missing transition with the unmet contract and final projection. | H repeated real-fixture keyboard path; deterministic exact-transition/trigger/delayed/cancel/watchdog tests, 100-generation lifecycle Pressure, affected multi-app fixture UI, Process/Tooling. | completed |
| SYNC-034BB | `exitSwitcherPreview`; real-window-card anchor workflow returning from preview to application cycle | The fixture path sends Up after validating a real application's preview, then advances the RunLoop for a fixed 200ms before beginning the next selection. | Capture an atomic `selected`/`mode` baseline and require the exact expected bundle in `windowCycle`. Establish the shared generation-owned selection-transition observer before Up, gate acceptance until input synthesis returns, request an immediate readback, and accept only the same exact bundle entering `appCycle`. XCUI diagnostics expose no publication callback, so retain the shared named cancellable cadence as fallback. Each preview-exit invocation owns generation, scheduling, cancellation, and cleanup; the named watchdog is solely a failure bound with final atomic evidence. | H repeated real-fixture window-layer exit; deterministic exact-baseline/trigger/delayed/cancel/watchdog tests, inherited 100-generation owner Pressure, affected real-window-card fixture UI path, Process/Tooling. | completed |
| SYNC-034BC | `testSwitcherPanelPreviewCyclesThroughWorkflowAppsWithoutMixingWindowCards`; initial Switcher presentation before the multi-app preview loop | The fixture relies on launch-time `--flowtab-ui-open-switcher`, foreground process state, and a later eight-second tile-existence watchdog. Under loaded real-fixture topology, the initial-presentation watchdog can finish with Home still active, while the caller receives only a missing-tile failure. | Establish the existing acknowledged on-demand Switcher trigger observer after the listener is ready and before posting the command. Require exact trigger receipt and presentation completion/readback for the requested session before entering the preview loop. The invocation owns observer registration, log baseline, cancellation, and cleanup; watchdogs terminate with exact receipt, presentation, and final UI evidence. | H initial UI orchestration; deterministic receipt/presentation ordering and cancellation coverage, affected multi-app fixture UI, Process/Tooling. | completed |
| SYNC-034BD | `testSwitcherPanelPreviewCyclesThroughWorkflowAppsWithoutMixingWindowCards`; preview exit between application selections | The fixture sends Up after validating each application's preview, then advances the RunLoop for a fixed 200ms before beginning the next selection. | After SYNC-034BC supplies deterministic initial presentation, reuse the exact-bundle preview-exit transition owner from SYNC-034BB. Establish the owner before Up and resolve only on the same bundle entering `appCycle`; the invocation owns cancellation and cleanup, and the watchdog reports final atomic evidence. | H repeated real-fixture window-layer exit; inherited deterministic transition and Pressure coverage, affected multi-app fixture UI, Process/Tooling. | completed |
| SYNC-034BE | `enterSwitcherPreview`, `waitForSwitcherMode`, and the open multi-app window-layer mutation paths; exact preview entry and retention | Two real-window mutation paths send Down before starting a raw 100ms RunLoop loop, then accept any `windowCycle` prefix as readiness. A second identical loop after the independent preview-title projection settles infers that the selected application's mode remained active. | Capture the exact selected-application `appCycle` baseline and establish the generation-owned selection-transition observer before Down. Gate acceptance until input synthesis returns, request an immediate readback, and accept only the same bundle entering `windowCycle(expectedBundle)`. After the independent exact preview-title projection resolves a window mutation, require an immediate atomic readback of the same selected bundle and exact window-cycle mode. XCUI exposes no diagnostics callback, so retain the shared named cancellable cadence only for preview entry. Each entry invocation owns generation, scheduling, cancellation, and cleanup; the named watchdog is solely a failure bound with final atomic evidence. | H real-fixture open-window-layer mutation Oracle; deterministic exact-transition/trigger/delayed/cancel/watchdog tests, inherited 100-generation owner Pressure, affected standard/fullscreen mutation UI paths, Process/Tooling. | completed |
| SYNC-034BF | `testSwitcherPanelAppSearchFindsAndActivatesRealWorkflowApp` and `waitForFrontmostWorkflowApp`; exact application activation after search confirmation | The fixture confirms the search selection before starting a raw 100ms RunLoop loop over `frontmostApplication`, allowing a fast activation notification to precede observation and leaving the loop cadence to determine when success is noticed. | Reuse the generation-owned exact frontmost-application observer. Register `NSWorkspace.didActivateApplicationNotification` and perform an initial readback before confirming the search selection, then accept only the target bundle identifier. The invocation owns notification cancellation and cleanup; the ten-second watchdog is solely a failure bound with final frontmost-bundle evidence. | H real-fixture application-search activation; inherited deterministic initial/notification/cancellation/Pressure coverage, affected application-search UI, Process/Tooling. | completed |
| SYNC-035 | Direct UI settle waits in Search, Settings, Home/Logs, MRU, Space fixture and switcher workflow tests; files enumerated in the UI audit scope below | Fixed 80ms–1.2s RunLoop advances occur between input/action and assertion, so assertion timing can change results. Evidence migration. | Remove each settle wait in favor of the affected visible element, log marker, fixture generation, process state, exact frontmost CG/AX window, or nonexistence Oracle. Observer/baseline setup precedes the action. | H; affected UI suites and matching Behavior coverage. | in progress; SYNC-035A–SYNC-035K and the Logs projection settle path are completed, with remaining owners split into later slices |
| SYNC-035A | `FlowTabUITests+SystemAppMRU.dismissSwitcherAndWait`; switcher dismissal between repeated MRU triggers | A fixed 300ms RunLoop advance after Escape assumes the previous panel has disappeared before the next trigger. | Establish an XCTest nonexistence expectation for the exact switcher summary before Escape, then require that persistent UI state to become absent. The named dismissal watchdog is only a failure bound and reports the final switcher hierarchy. The helper invocation owns the expectation. | H repeated UI presentation; System MRU UI and repeated-path Pressure, Process/Tooling. | completed |
| SYNC-035B | `FlowTabUITests+Support.commitEditing`, `+ElementValueObservation.assertTriggerMakesValue`, and `testSettingsWindowBehaviorDelayAndTogglesPersistAcrossRelaunch`; delay-input commit | A fixed 200ms RunLoop advance after Tab assumes AppKit ended editing, normalized the value, and republished its accessibility value. | Start the generation-owned element-value observer before Tab, reject a matching baseline until the trigger returns, and accept only the exact normalized `1.23` accessibility readback. XCUI exposes no value-publication callback, so retain the shared serial named cadence. The helper invocation owns cancellation; its watchdog is solely a failure bound with acceptance state and last exact value. | H Settings UI commit Oracle; deterministic owner regression, affected Settings UI, Process/Tooling. | completed |
| SYNC-035C | `FlowTabUITests+SpaceFixtureOptionTabNoisyRoundTrip.selectNoisyOptionTabWindow`; noisy Option-Tab selected-window advance | A fixed 250ms RunLoop advance after each delivered `.advanceRight` command assumes the switcher has published the next selected window before reading its title and ID. | Establish a generation-owned diagnostics observer before each command, retain the current exact CG window number as the baseline, gate acceptance until command delivery returns, and close the synchronous race with an immediate readback. Accept only one atomic diagnostics value containing window-cycle mode, a nonempty title, the canonical `cg:<pid>:<windowNumber>` stable identity, and a CG window number different from the baseline. XCUI exposes no diagnostics-publication callback, so retain the shared named serial cadence. The helper owns cancellation; its watchdog is solely a failure bound with the final atomic readback. | H noisy fullscreen/off-Space UI Oracle; deterministic identity/gating/cancel/watchdog tests, lifecycle Pressure, affected real-topology UI, Process/Tooling. | completed |
| SYNC-035D | `FlowTabUITests+SpaceFixtureInAppWindowSwitcher.relaunchInAppWindowSwitcher`; exact fixture refocus before Control+Tab reopen | A bundle-only `XCUIApplication` activation followed by a fixed 350ms RunLoop advance assumes XCTest attached to the launched fixture process and the previously selected window became frontmost. | Resolve the fixture path intent through `makeSpaceFixtureWorkflowApplication` at the fixture boundary. Install the generation-owned workflow-window activation observer before activation, gate baseline evidence until activation returns, and accept only the exact fixture bundle plus the previously selected CG window number. The helper invocation owns observer cancellation; its named watchdog reports the final workspace, CG, AX, and title readback. | H real-fixture process/window identity Oracle; deterministic activation-owner regression and lifecycle Pressure, affected clean Control+Tab round trip, Process/Tooling. | completed |
| SYNC-035E | `SearchSystemTextInputBridge.Coordinator`, `FlowTabUITests+SearchInputReadinessObservation`, and Search/Settings/Space-fixture Search entry callers; initial Search keyboard readiness | Fixed 400ms RunLoop advances after Search input existence assume AppKit has made the exact Search text view first responder in the key panel before XCTest sends keyboard input. Evidence migration plus conditional observation. | Establish window-mount and key-window observers before responder synchronization, read back `window.isKeyWindow && window.firstResponder === textView` with responder class `SearchSystemTextView`, and publish an INFO runtime marker on the false-to-true transition. UI owners establish a runtime-log baseline and file observer before launch or trigger, require that exact marker, then require the exact XCU input identity. The shared named 200ms runtime-log readback cadence closes file-event gaps; coordinator and UI helper owners manage generation, cancellation, and cleanup. The 35-second marker and five-second accessibility watchdogs report the final observed evidence. | H shared production/UI keyboard-readiness Oracle; Behavior initial/event/cancel/dedup coverage, deterministic marker owner tests, 100-generation lifecycle Pressure, affected mock/settings/real-topology Search UI, Process/Tooling. | completed |
| SYNC-035F | `FlowTabUITestSwitcherWindowSelectionTransitionObservationOwner`, `selectGlobalSwitcherWindow`, and in-app Control+Tab selection helpers; exact window-cycle selection | Fixed 250ms RunLoop advances after `.advanceRight` or `.inAppForward` delivery assume the next exact window identity and title have reached the diagnostics projection. Evidence migration plus conditional observation. | Establish the generation-owned diagnostics observer before each command, retain the current exact CG window number as baseline, gate acceptance until command delivery returns, and close the synchronous race with a trigger-return readback. Accept one atomic window-cycle projection with a canonical changed `cg:<pid>:<windowNumber>` identity and nonempty title. The helper invocation owns registration, cancellation, and cleanup; the shared named serial cadence remains the platform fallback, and the four-second watchdog reports the final atomic evidence. | H shared global/in-app exact-window Oracle; deterministic owner regression and lifecycle Pressure, affected Option+Tab and Control+Tab real-topology UI, Process/Tooling. | completed |
| SYNC-035G | `testSearchPanelWrapFromLastResultScrollsBackToFirstResult` and the shared switcher-diagnostics observation owner; exact Search result selection and wrap | Fixed 100ms and repeated 80ms RunLoop advances after Down Arrow assume each next result selection has committed before the test continues, while the final assertion checks only row hittability. Evidence migration plus conditional observation. | Establish the generation-owned diagnostics observer before every key input, gate acceptance until input delivery returns, close the synchronous race with a trigger-return readback, and accept only the exact percent-decoded `searchSelectedResult` identity for results 01 through 10 and the wrapped result 01. Preserve exact last/first row hittability as an independent scroll Oracle. XCUI exposes no diagnostics-publication callback, so retain the shared named serial readback cadence. Each helper invocation owns generation, scheduling, cancellation, and cleanup; its three-second watchdog reports the final encoded and normalized diagnostics projection. | H Search keyboard/selection/scroll Oracle; deterministic initial/percent-decoded/gated/delayed/atomic/cancel/watchdog owner tests, 100-generation lifecycle Pressure, affected Search-wrap UI, Process/Tooling. | completed |
| SYNC-035H | `SwitcherSearchViewState.resultsQuery/resultsScope`, switcher diagnostics, and `testSettingsAppVisibilityHidesMockAppFromSwitcherAndSearch`; hidden-app Search result completion | A fixed 800ms RunLoop advance after typing `Mail` assumes the debounced asynchronous Search result computation has committed before element absence is checked. The input query publishes before its result projection, so input text plus immediate nonexistence is not completion evidence. Evidence migration plus conditional observation. | Preserve the exact query and scope associated with the last accepted result-computation callback as explicit Search state. Publish them atomically with `searchResults` in the testing diagnostics. Establish the shared generation-owned diagnostics observer before typing, gate acceptance until input delivery returns, and accept only one snapshot with committed scope `app`, percent-decoded committed query `Mail`, and an empty result set; then retain exact hidden-row nonexistence as readback. XCUI exposes no diagnostics callback, so the shared named serial cadence remains the fallback. The helper invocation owns scheduling, cancellation, and a five-second watchdog with the final atomic projection. Consecutive app phases reuse the exact `.notRunning` process-state boundary from SYNC-034AM before relaunch. | H Search state/UI absence Oracle; deterministic delayed/cancelled/out-of-order/synchronous/slow-scheduler and 2,000-revision Pressure tests, shared diagnostics owner regression, affected Settings/Search UI, Process/Tooling. | completed |
| SYNC-035I | `testSwitcherPanelOptionTabHidesDesktopProvisionalCGOnlyWorkflowWindow`; manual Down result for a selected real-topology application with one eligible window | A fixed 500ms RunLoop advance after `.advanceDown` assumes command handling and its resulting switcher state have settled before negative mode and preview assertions. The fixture launch fixes the separate product auto-entry delay at 30 seconds, so 500ms is not a domain duration. | Freeze a runtime-log baseline before `.advanceDown`, wait for the post-`model.handle` state evidence `advance key=downArrow ... windows=1 mode=appCycle`, and only then read back exact diagnostics requiring app-cycle mode, inactive preview, and absence of the provisional title. The shared runtime-log observation owner uses file events plus its named cancellable readback fallback; the invocation owns cleanup, and its watchdog is solely a failure bound with the last log evidence. | H real-topology negative Oracle; existing deterministic single-window session rule, shared runtime-log owner regression, affected provisional-hidden UI, Process/Tooling. | completed |
| SYNC-035J | `HomeInitialProjectionObservationOwner.readback`, `FlowTabUITestHomeInitialProjectionApplicationBootstrap`, and `testHomeInitialAppLayerUsesRuntimeOrderAndZeroCountsWithoutAccessibilityPermission`; post-maintenance Home projection application | A fixed 1.2-second RunLoop advance after the first zero-count and frame readbacks assumes the requested maintenance projection has been applied without changing row values or order. Evidence migration. | Publish a typed exact-service notification after each accepted projection callback returns. TestingSupport observes before Home construction, atomically persists the exact observation generation, transition, completeness, source generations, ordered app IDs, and window counts, then uses a distributed notification as a wakeup. The UI owner observes before launch, performs an immediate JSON readback, and retains a named cancellable 100ms fallback for a missed cross-process wakeup. Success requires a later `appSwitcherProjectionNotification` with `sourceGenerationAdvanced`, incomplete window coverage, Mail before Browser, and both counts zero; accessibility values and frames remain independent before/after readbacks. App termination and test teardown own observer, scheduler, and file cleanup. The eight-second watchdog reports the final path/file/decode/evidence state. | H Home initial projection UI Oracle; deterministic initial/synchronous/notification/generation/cancellation/atomic-readback coverage, 2,000-generation lifecycle Pressure, affected Home UI, Process/Tooling. | completed |
| SYNC-035K | `SwitcherPointerSelectionGate`, `SwitcherPanelController` pointer-selection entry points, and the stationary-pointer Option+Tab, Control+Tab, and Search UI paths; suppression of presentation-time hover | Each UI path advances the RunLoop for one second in 100ms increments and treats an unchanged selection throughout that interval as proof that the stationary-pointer gate handled presentation. This is an absence-over-time inference rather than completion evidence. | Give each gate reset a monotonic generation and emit one blocked decision for each exact application, window, or Search-result target in that generation. The controller records a structured marker containing the exact target identity, preserved selection, and generation only for the first blocked callback. The UI test captures the post-baseline runtime log and starts its observer before launch or trigger, performs the shared immediate readback, requires that exact blocked marker, then independently reads back the unchanged selected identity and target frame. Duplicate callbacks are suppressed, reset releases prior-generation target storage, and movement reaching the existing threshold remains the sole transition to selection-enabled state. The test invocation owns observation cancellation and process cleanup. Because XCUI exposes no hover-gate callback, the shared named cancellable 200ms runtime-log cadence remains the fallback; the five-second gate watchdog is solely a failure bound with final log evidence. | H repeated pointer-input path; deterministic generation/target/dedup/reset/escaping rules, controller Behavior, shared observer event/cancel/watchdog and lifecycle Pressure, 2,000-generation gate Pressure, stationary and moved-pointer UI, Process/Tooling. | completed |
| SYNC-036 | All literal XCTest timeouts in `FlowTabTests` and `FlowTabUITests` | 606 literal durations are generally terminal bounds for an independent expectation or XCUI predicate, but policy ownership and diagnostic tiers are implicit. Watchdog. | Replace literals with named app-test and UI-test watchdog policies by operation class; preserve expectation/predicate success Oracles and include unmet condition plus last observation in custom waits. Test case/helper owner supplies cleanup. | M mechanical/test infra; Unit/Behavior/UI, Process/Tooling. | in progress through SYNC-036GX |
| SYNC-036A | `FlowTabUITests+Support.swift`; shared App foreground, tab navigation, switcher-command delivery, toggle discovery, and settings-option helpers | Thirteen direct XCTest/XCUI timeout literals and one anonymous fallback cap hide operation ownership. The launch helper discards foreground-wait evidence, while direct navigation failures omit final state. Watchdog classification. | Add one support-owned policy with compatible operation-specific bounds. Preserve `XCUIApplication.State`, exact element existence/hittability, and runtime-log command receipt as the independent Oracles. Record the foreground result/state and add unmet-condition plus final element readback diagnostics. Each helper invocation retains ownership of its existing observation cancellation and XCTest wait. | H shared UI helper boundary; deterministic policy compatibility test, representative Settings/Logs UI, Process/Tooling. | completed |
| SYNC-036B | `FlowTabTests+RuntimeLogObservation.swift`; RuntimeLog store/change observation and view-model read application | Seven one-second `fulfillment` literals are terminal bounds around observer, Combine publisher, and controlled-source callbacks, while the policy owner and final readback diagnostics are implicit. Watchdog classification. | Add one runtime-log-test-owned event-delivery watchdog. Preserve pre-trigger subscriptions, baseline generation, exact change sequence, marker lines, read-request count, generation ordering, and cancellation state as independent Oracles. Each fulfillment description identifies the unmet condition, and the subsequent readback reports final changes, lines, request count, and observer state. Observation, view model, cancellable set, and temporary-root cleanup remain owned by the test invocation. | M app behavior-test owner; policy compatibility plus all affected RuntimeLog tests, Process/Tooling. | completed |
| SYNC-036C | `testTerminateSelectedAppUnitKeepsPendingRequestWithoutSurfaceTimeout` and the pre-termination assertion in `testTerminateSelectedAppUnitRefreshesOnWorkspaceTerminateAfterPendingRequest`; pending termination state before workspace termination evidence | The negative path advances an inverted expectation for 10ms, and its positive companion advances one task-scheduler turn; both treat absence of a layout callback during that advancement as proof that a sent quit request remains pending. Evidence migration. | Install a layout-publication counter before the trigger, call the synchronous `terminateSelectedApp()` transition, and use its returned result plus exact maintenance-request, pending app/PID/generation, unchanged session, empty termination-signal, and zero layout-publication readbacks as the Oracle. The positive path then triggers exact app/PID termination evidence and waits for the resulting callback. The test invocation owns callback removal and model cancellation; no pre-termination duration or scheduler advance remains. | M app behavior-test Oracle; affected negative path plus positive workspace-termination companion, Process/Tooling. | completed |
| SYNC-036D | `testTerminateSelectedAppBehaviorKeepsAppUntilWorkspaceTerminationArrives`, `testHandleApplicationTerminatedRefreshesFromRuntimeProjectionWithoutFullSnapshot`, and `testTerminateSelectedAppUnitRefreshesOnWorkspaceTerminateAfterPendingRequest`; post-termination layout publication | Three one-second expectations wait after the synchronous `handleApplicationTerminated(appID:pid:)` call has already returned, treating its layout callback as asynchronously pending. Evidence migration. | Install a layout-publication counter before the exact app/PID trigger, establish its zero baseline, call `handleApplicationTerminated`, and require its synchronous true result plus a counter value of one. Exact termination-signal, projection-read, session membership, selection, pending-request, and terminating-identity readbacks remain the independent Oracles. Each test invocation owns callback removal and model cancellation with `defer`; no duration remains. | M app behavior-test Oracle; three affected paths plus mismatched-PID identity companion, Process/Tooling. | completed |
| SYNC-036E | `RuntimeInteractionWatchdogPolicy.keyboardReadinessEvent`, `SearchSystemTextInputBridgeTestHarness`, and the exact/immediate and delayed key-window readiness tests; Search system text-input readiness callback | Two raw one-second XCTest bounds wait for the typed keyboard-readiness callback, but their policy owner and timeout readback are implicit, and harness teardown retains the test callback. Watchdog classification and lifecycle hardening. | Keep the callback observer installed before window/synchronize triggers and preserve `isKeyWindow && firstResponder === exactTextView` plus the single `[true]` transition as the success Oracle. Retain one compatible named event-delivery watchdog solely as a terminal failure bound, with final key-window, responder identity, responder match, and readiness-transition evidence. Harness close clears the test callback and coordinator detach cancels its window observers; generation checks reject stale work. | M app behavior-test owner; policy compatibility plus initial-key, delayed-key, duplicate-event, and stale-cancellation paths, Process/Tooling. | completed |
| SYNC-036F | `RuntimeInteractionWatchdogPolicy.mainQueueTransition` and `waitForSearchInputMainQueueTurn()`; Search input main-queue barrier used by readiness duplicate, delayed-key, and stale-cancellation assertions | The helper already succeeds from an exact `DispatchQueue.main.async` callback, but its raw one-second terminal bound has no named policy or final callback-delivery readback. Watchdog classification. | Preserve FIFO main-queue callback delivery as the sole barrier Oracle, set an explicit delivery flag inside that callback, and retain the compatible one-second bound as the file-owned `mainQueueTransition` watchdog. The helper owns a cancellable dispatch work item and the expectation; timeout diagnostics report the unmet barrier condition and final delivery flag. | M app behavior-test helper; policy compatibility plus all three affected Search readiness paths, Process/Tooling. | completed |
| SYNC-036G | `FlowTabPriorityCoverageWatchdogPolicy.runtimeMaintenanceExecution` and the twelve runtime-reconciliation maintenance/freshness-barrier waits in `FlowTabPriorityCoverageTests+RuntimeReconciliationCoordinator.swift` | Each pre-trigger executor expectation uses a raw one-second terminal bound. Completion already comes from exact maintenance execution, but policy ownership is implicit and expectation descriptions are unstructured. Watchdog classification. | Retain exact executor callback fulfillment as the event Oracle and the following request ID/priority/target/reason, read-model generation/readiness, coordinator pending state, and scheduler-entry assertions as final evidence. Name the compatible bound at the Priority test-case owner and encode the unmet maintenance transition in each expectation. XCTest case and service lifetime own each wait and queue drain. | M app behavior-test owner; policy compatibility plus all twelve affected maintenance and Search freshness paths, Process/Tooling. | completed |
| SYNC-036H | `FlowTabPriorityCoverageWatchdogPolicy.windowPreviewEventDelivery` and the eight waits in `FlowTabPriorityCoverageTests+WindowPreviewSessionPinning.swift`; visible/deferred preview capture and publication | Raw one-second bounds surround preinstalled capture overrides or Combine subscriptions. Event callbacks already define completion, while policy ownership, structured unmet conditions, and explicit local cleanup are incomplete. Watchdog classification and lifecycle hardening. | Retain exact capture-start, all-captures-finished, and `objectWillChange` callback delivery as event Oracles. Subsequent ordered title/count, batch request, image/fallback snapshot, main-thread publication, provider failure/retry, and single-publication assertions provide final readback. Name one compatible preview event watchdog. Test invocation cancels the model, unblocks controlled semaphores, and clears Combine subscriptions from `defer`. | M app behavior-test owner; policy compatibility plus six affected visible/deferred/success/failure paths, Process/Tooling. | completed |
| SYNC-036I | `FlowTabPriorityCoverageWatchdogPolicy.runtimeFocusRecoveryObservation` and three focus-recovery waits in `FlowTabPriorityCoverageTests+RuntimeActivationRecovery.swift`; delayed CG visibility and late AX-window recovery | Raw one-second XCTest bounds surround a named production condition-observation policy. Success already comes from exact CG onscreen readback or an exact recovered AX-window focus attempt, but test watchdog ownership and expectation diagnostics are implicit. Watchdog classification. | Preserve the production immediate readback, named 20ms condition cadence, generation, and one-second failure watchdog. Test completion requires the preinstalled exact target-CG visibility callback or recovered AX pointer; final focus-call order, visibility history, CG/AX read counts, and activation count remain independent evidence. Name the compatible XCTest bound at the Priority owner. Coordinator completion/cancel/deinit owns polling tokens, watchdog, and notification cleanup. | M app behavior-test owner; policy compatibility plus three delayed-condition paths, Process/Tooling. | completed |
| SYNC-036J | `FlowTabPriorityCoverageWatchdogPolicy.appDelegateWorkspaceLifecycleSignal` and the launch/termination waits in `testAppDelegateSignalsRuntimeWhenWorkspaceAppsLaunchAndTerminate` | Two raw one-second XCTest bounds surround exact workspace lifecycle callbacks. Completion is event driven, while policy ownership and expectation diagnostics are implicit. Watchdog classification. | Install exact appID/PID launch and termination handlers before posting either workspace notification. Retain typed callback fulfillment plus exact recorded signal, directory entry, observer-before-launch, prepared/cancelled observation identity, and post-teardown no-delivery readbacks as the Oracle. Name one compatible lifecycle-signal watchdog. The test's existing `defer` clears handlers, terminates the delegate, restores hooks, and removes isolated defaults. | M app behavior-test owner; policy compatibility plus the affected ordered launch/termination path, Process/Tooling. | completed |
| SYNC-036K | `testRuntimeProjectionServiceReconcilesLaunchedAppAgainFromWindowEvidence`; app-launch and AX-window maintenance executor waits | Two raw one-second XCTest bounds surround the exact reconciliation-executor callbacks. Completion is event driven, but diagnostics and the existing maintenance-watchdog ownership are implicit. Watchdog classification. | Establish both executor expectations before either signal, fulfill them only for the first and second exact maintenance executions, and preserve ordered target/reason arrays as final evidence. Reuse the Priority owner’s compatible `runtimeMaintenanceExecution` watchdog solely as the failure bound and name each unmet transition. The local service/test invocation owns executor delivery and maintenance-queue drainage. | M app behavior-test owner; existing policy compatibility plus the affected ordered app-launch/window-evidence path, Process/Tooling. | completed |
| SYNC-036L | `FlowTabPriorityCoverageWatchdogPolicy.initialSearchPresentationResolution` and both waits in `FlowTabPriorityCoverageTests+InitialSearchPresentation.swift`; initial Search presentation resolution | Two raw one-second XCTest bounds surround a preinstalled typed resolution-notification observer. Completion is notification and readback driven, while the compatible failure-bound owner and unmet-condition diagnostics are implicit. Watchdog classification. | Register the resolution observer before bootstrap presentation. Preserve the observation generation, initial/readiness readbacks, exact presentation evidence, Search model state, selected app identity, panel size, and observer removal as the first Oracle. For missing committed-index state, prove the initial unresolved baseline, trigger the exact committed-index notification, and then require its resolved evidence and final Search state. Name one compatible terminal watchdog. Test `defer` removes the outer observer, cancels the generation-owned production observation/watchdog, cancels selection, and restores launch overrides. | M app behavior-test owner; policy compatibility plus immediate and delayed committed-index presentation paths, Process/Tooling. | completed |
| SYNC-036M | `FlowTabPriorityCoverageWatchdogPolicy.committedSearchIndexPublication` and `testRuntimeProjectionServicePublishesPendingSearchIndexAfterNestedWindowRepairsComplete`; committed Search index publication after nested repair | A raw one-second XCTest bound surrounds an exact-name/exact-service notification expectation. Completion is event and readback driven, while the compatible failure-bound owner is implicit. Watchdog classification. | Establish the `.runtimeCommittedSearchIndexDidUpdate` expectation for the exact service after proving the missing-index baseline and before signalling the exact app/PID window change. Retain the final committed-generation readiness/result, exact app/window entries, generation coverage, empty pending scopes, and ordered nested repair targets as independent evidence. Name one compatible terminal watchdog. XCTest owns notification observation cleanup; the local service/test invocation drains its maintenance queue. | M app behavior-test owner; policy compatibility plus the affected nested app/space repair path, Process/Tooling. | completed |
| SYNC-036N | `FlowTabPriorityCoverageWatchdogPolicy.runtimeProjectionMainThreadDelivery` and `testRuntimeProjectionNotificationPublisherDeliversBackgroundPostsOnMainThread`; background projection notification main-thread delivery | A raw one-second XCTest bound surrounds a preinstalled exact-name/exact-object observer. Completion already comes from callback delivery plus main-thread readback, while the compatible failure-bound owner and unmet-condition diagnostic are implicit. Watchdog classification. | Install the isolated notification-center observer before the global-queue trigger, accept only the exact projection name/object, assert main-thread delivery inside the callback, and reject duplicate fulfillment. Name one compatible terminal watchdog. The test invocation owns the expectation and removes the observer from `defer`; production main-queue scheduling owns event delivery. | M app behavior-test owner; policy compatibility plus the affected background-post delivery path, Process/Tooling. | completed |
| SYNC-036O | `FlowTabPriorityCoverageWatchdogPolicy.searchResultScrollPublication` and both waits in `testSwitcherPanelControllerSearchWrapRequestsScrollBackToFirstResult`; initial and wrapped Search-result scroll publication | Two raw one-second XCTest bounds surround a preinstalled test callback reached from the SwiftUI scroll task. Completion is exact revision/result callback driven, while the compatible failure-bound owner and unmet-condition diagnostics are implicit. Watchdog classification. | Install the model callback before panel presentation and keyboard triggers. For both the initial Down and last-to-first wrap, set the expected next revision and exact first-result ID before input, fulfill only their matching callback, and preserve revision, selection, and complete observed-request history as readback. Name one compatible terminal watchdog. Test `defer` restores the prior callback and cancels panel selection; the controller/view lifetime owns SwiftUI task delivery. | M app behavior-test owner; policy compatibility plus the affected initial and wrap scroll paths, Process/Tooling. | completed |
| SYNC-036P | `FlowTabPriorityCoverageWatchdogPolicy.searchComputationPublication` and the initial wait in `testLiveSwitcherModelHandleApplicationTerminatedPreservesSearchStateDuringRefresh`; initial Search computation publication | A raw one-second XCTest bound surrounds a preinstalled Search-state callback after query synchronization. Completion is exact state driven, while the compatible failure-bound owner, unmet-condition diagnostic, and explicit pending-work cleanup are implicit. Watchdog classification and lifecycle hardening. | Install the Search-state callback before synchronizing input and fulfill only when Search is active with exact app scope, query, and result titles. Preserve those model fields as final readback. Name one compatible terminal watchdog. Test `defer` cancels the generation-owned Search scheduling owner before clearing model callbacks. The separate termination layout/preserved-Search composite remains isolated for SYNC-036Q. | M app behavior-test owner; policy compatibility plus the affected initial computation and explicit cancellation path, Process/Tooling. | completed |
| SYNC-036Q | `testLiveSwitcherModelHandleApplicationTerminatedPreservesSearchStateDuringRefresh`; post-termination synchronous layout publication and asynchronous preserved-Search computation | One raw one-second composite wait treats a synchronous layout callback and an asynchronous Search-state callback as equivalent pending work. This obscures the layout transition's return boundary and leaves the Search failure-bound owner implicit. Evidence migration and watchdog classification. | Install both callbacks before the exact app/PID trigger and establish a zero layout-publication baseline. Require `handleApplicationTerminated` to return true and the layout counter to become exactly one before waiting. Fulfill the Search expectation only for the refreshed app count plus exact active scope, query, and result titles, then reuse the named Search-computation terminal watchdog and repeat all final readbacks. Test `defer` cancels pending Search work and clears both callbacks. | M app behavior-test Oracle; existing policy contract plus the affected termination-refresh path, Process/Tooling. | completed |
| SYNC-036R | `HomeInitialProjectionObservationWatchdogPolicy.maintenanceRequestDelivery` and `testHomeVisibilityOwnsInitialProjectionObservationLifecycle`; Home initial-projection maintenance request from SwiftUI visibility lifecycle | A raw one-second XCTest bound surrounds a preinstalled test-double callback, but the callback accepts any maintenance reason, the compatible failure-bound owner is implicit, and handler cleanup occurs only after a successful wait. Watchdog classification and lifecycle hardening. | Install the handler before constructing the hosted view, fulfill only for `.homeProjectionMissing`, reject duplicate fulfillment, and preserve the exact recorded maintenance-reason array plus observation-active/inactive readbacks as the Oracle. Name one compatible terminal watchdog with the unmet reason in its description. Test `defer` clears the handler on every exit; the hosted view and projection owner retain their existing visibility-driven stop lifecycle. | M Home behavior-test owner; policy compatibility plus the affected active-to-inactive SwiftUI lifecycle, Process/Tooling. | completed |
| SYNC-036S | `FlowTabPriorityCoverageWatchdogPolicy.homeProjectionEvidenceDelivery` and `testHomeAppDetailObservationDeliversBackgroundCommitOnMainActor`; exact Home detail evidence delivered from a background projection notification | A raw one-second XCTest bound surrounds the typed evidence callback. The existing guard accepts any detail transition from the expected source, while generation/readback/completeness evidence, the compatible failure-bound owner, duplicate delivery behavior, and explicit test-exit cancellation are implicit. Watchdog classification and lifecycle hardening. | Register the owner before the background post and fulfill only for the exact app ID, observation generation, readback count, notification source, source-generation advance, complete projection generation, and completion transition. Assert main-thread delivery inside the callback, reject duplicate fulfillment, and repeat exact evidence plus stopped-observation readbacks afterward. Reuse one Priority-owned Home projection event watchdog. Test `defer` cancels the app observation; owner finish/deinit own observation and notification-token cleanup. | M Home detail behavior-test owner; policy compatibility plus the affected background-to-main exact-evidence path, Process/Tooling. | completed |
| SYNC-036T | `FlowTabPriorityCoverageWatchdogPolicy.homeProjectionEvidenceDelivery` and `testHomeAppSummaryObservationDeliversBackgroundCommitOnMainActor`; exact Home summary evidence delivered from a background projection notification | A raw one-second XCTest bound surrounds the typed summary callback, whose guard accepts any evidence from the notification source. Exact observation/readback generation, summary content, compatible failure-bound ownership, duplicate delivery behavior, and explicit observation shutdown are implicit. Watchdog classification and lifecycle hardening. | Start observation before the background post and fulfill only for observation generation 1, readback 2, exact notification source, source-generation advance to complete projection generation 2, exact app identity, and exact window count. Assert main-thread delivery in the callback, reject duplicate fulfillment, and repeat evidence readbacks afterward. Reuse the named Home projection event watchdog. Require the owner to be active after delivery, then explicitly stop and prove inactive; `defer` supplies failure-path cleanup. | M Home summary behavior-test owner; existing policy contract plus the affected background-to-main exact-evidence and stop path, Process/Tooling. | completed |
| SYNC-036U | `HomePermissionObservationLifecycleWatchdogPolicy.eventDelivery` and `testHomeLandingVisibilityOwnsPermissionObservationLifecycle`; active initial permission publication and inactive cancellation of the two owned fallback observations | Two raw one-second XCTest bounds surround exact Combine publication and manual-scheduler cancellation callbacks. Policy ownership, duplicate delivery behavior, exact final permission/token state, and failure-path cleanup of the publisher, scheduler hook, and observation owner are implicit. Watchdog classification and lifecycle hardening. | Subscribe and install both two-event expectations before constructing the active Home view. Require both initial readbacks to publish, then read back false permission values, two active target observations, two available fallback tokens, and one activation observer. Before the inactive view update the cancellation expectation is already installed; require both token cancellations, then read back no target observations, no available token, and no activation observer. Name one compatible event watchdog for each phase, reject over-fulfillment, and let test `defer` clear the scheduler hook, cancel Combine, and stop the owner. | M Home permission behavior-test owner; policy compatibility plus the affected active-to-inactive two-target lifecycle, Process/Tooling. | completed |
| SYNC-036V | `HotkeyRegistrationObservationWatchdogPolicy.newerEvidenceDelivery` and `testHotkeyRegistrationObservationInstallsBeforeInitialReadback`; newer exact hotkey-registration evidence posted synchronously from the initial evidence provider | A raw one-second XCTest bound surrounds a Combine pipeline filtered only by generation. Completion is event driven, while exact request/takeover evidence, compatible failure-bound ownership, duplicate delivery behavior, and explicit publisher/owner cleanup are implicit. Watchdog classification and lifecycle hardening. | Subscribe before `owner.start()`, filter by the complete expected evidence posted synchronously during the provider readback, reject over-fulfillment, and capture that exact value. Preserve the post-wait proof that the post occurred inside readback, the owner retained generation 2 instead of the returned generation 1, and exact-request takeover state is active. Name one compatible event watchdog. Test `defer` cancels Combine and stops the generation-owned notification observation. | M hotkey observation behavior-test owner; policy compatibility plus the observer-before-readback race and exact evidence path, Process/Tooling. | completed |
| SYNC-036W | `FlowTabPriorityCoverageWatchdogPolicy.appDelegateHotkeyRegistrationPublication` and `testAppDelegateHotkeyObserverUsesPostedConfigurationsImmediately`; AppDelegate publishes exact registration evidence for a posted re-registration request | A raw one-second XCTest bound surrounds a pre-trigger exact-object notification expectation. Generation, request ID, and configuration already gate completion, while source/takeover evidence and compatible failure-bound ownership are implicit. Watchdog classification. | Establish the AppDelegate evidence-generation baseline, install the XCTest notification observer for the exact delegate before posting the request, and require baseline+1 generation, request ID/configuration match, active Command-Tab takeover, and `notification_payload` source. Reject duplicate fulfillment and repeat exact evidence, monitor replacement, IDs, starts/stops, and takeover reconciliation readbacks. Name one compatible terminal watchdog. XCTest owns notification observation cleanup; AppDelegate termination and test `defer` own monitor/observer/defaults cleanup. | M AppDelegate hotkey behavior-test owner; policy compatibility, exact posted-request path, 500-delivery active-delegate Pressure, Process/Tooling. | completed |
| SYNC-036X | `FlowTabPriorityCoverageTests+RuntimeMainTableSearchCoverage.swift` and `testRuntimeProjectionServiceSearchFreshnessBarrierCommitsAfterScheduledCoverageRepairUpdatesMainTable`; scheduled missing-window coverage repair and committed Search index | A raw one-second XCTest bound surrounds a reconciliation executor that fulfills for any request after assertions. The callback is event evidence, while exact request gating, compatible watchdog ownership, and the timeout's last-observation diagnostic are implicit. Watchdog classification and Oracle hardening. | Isolate this Search-coverage contract from the oversized main-table suite. Install the executor before requesting the Search freshness barrier, record every observed request, and fulfill only for the exact app/PID, app ID, high priority, and sole `searchFreshnessBarrier` reason. Reuse the Priority owner's compatible `runtimeMaintenanceExecution` duration solely as a terminal failure bound. On failure, report all observed request identities plus dirty apps/PIDs, pending scopes, and committed-index presence. Final exact request, empty dirty/pending state, committed-generation readiness/result, coverage, and exact window identity remain independent readbacks. The local service/test invocation owns executor delivery and maintenance-queue drainage. | M app behavior-test Oracle; existing policy compatibility plus the affected scheduled-coverage Search commit path, Process/Tooling. | completed |
| SYNC-036Y | `FlowTabPriorityCoverageWatchdogPolicy.appDelegateLaunchBootstrapEvidence` and `testAppDelegateLaunchWithUITestBootstrapArgumentsSeedsLogsAndOpensSearch`; AppDelegate UI-test bootstrap launch evidence | One raw four-second async fulfillment bound combines initial Search presentation, launch-generation hotkey registration, and clear-then-seeded-log append delivery. The three event Oracles are installed before launch, while watchdog ownership, duplicate matching delivery, and last-observation diagnostics are implicit. Watchdog classification and Oracle hardening. | Keep all three observations active before `applicationDidFinishLaunching`. Accept Search presentation only from a positive generation initial readback with presented/did-present/Search-active evidence; accept hotkey registration only at generation 1 from `application_launch` with takeover inactive; retain the recorder's clear followed by at least three appends. Matching duplicate notifications over-fulfill. Name the compatible four-second bound at the Priority owner solely as a terminal failure cap, and report the last presentation evidence, registration generation/source/takeover/signatures, and log change generation/kind/count. The test `defer` owns notification observers, log observation/recorder cancellation, delegate termination, hook/default restoration, and diagnostic cleanup. | M AppDelegate behavior-test Oracle; policy compatibility plus the affected composite bootstrap launch path, Process/Tooling. | completed |
| SYNC-036Z | `FlowTabPriorityCoverageTests+RuntimeApplicationTermination.swift` and `testLiveSwitcherModelHandleApplicationTerminatedRefreshesSessionAndKeepsPreferredNextSelection`; application-termination projection refresh and preferred-next selection | A raw one-second async fulfillment waits after `handleApplicationTerminated(appID:pid:)` has returned even though the method synchronously reloads the session and publishes `onSessionLayoutChanged` before returning. Completion is therefore inferred from elapsed opportunity rather than the method's explicit result, callback transition, and projection readback. Evidence migration. | Isolate the termination contract from the oversized mixed runtime suite. Install a layout-publication counter before the exact app/PID trigger and establish its zero baseline. Require the method's true return and exactly one callback before return, then read back the exact app/PID termination signal, second projection read, remaining ordered app identities, and preferred-next selection. The companion untracked-app path requires false return, no signal, no extra read, and unchanged selection. Each test invocation owns callback removal and session cancellation with `defer`; no duration remains. | M application-termination behavior-test Oracle; affected positive path plus untracked negative path, Process/Tooling. | completed |
| SYNC-036AA | `FlowTabPriorityCoverageTests+WindowPreviewPublication.swift`; terminal special-provider preview success and terminal-provider failure publication | Two raw one-second async fulfillment bounds surround the same `onWindowOnlyPreviewPreparationChanged` event source. The observer is installed before capture, but compatible watchdog ownership, initial capture-state evidence, duplicate exact publication, callback thread, last-observation diagnostics, and model cancellation are implicit. Watchdog classification and lifecycle hardening. | Establish empty capture-state and zero-provider-call baselines before requesting the visible range. Accept the success publication only for the exact ordered window identities, images, expected capture generation, and two special-provider requests; accept failure only for the exact window identity, absent image, special-provider-unavailable state, expected retry generation, one special-provider request, and zero generic requests. Reject duplicate matching publications and read back callback/match counts, last window/image/state evidence, exact final snapshots, provider counts, and failure state. Reuse the Priority owner's compatible `windowPreviewEventDelivery` duration solely as a terminal failure bound. Each invocation clears the callback and cancels the model from `defer`. | M preview behavior-test owner; existing policy compatibility plus exact success and provider-failure paths, Process/Tooling. | completed |
| SYNC-036AB | `testInAppWindowPanelRevealsAfterAsyncPreviewBatchCompletes`; start of the controlled initial window-only preview batch | The first of two raw one-second async fulfillment bounds accepts any invocation of the detached batch-capture override as proof that the intended initial preview work has started. The callback is observable evidence, while exact request identity, compatible watchdog ownership, duplicate delivery, initial state, final in-flight readback, and failure diagnostics are implicit. Watchdog classification and Oracle hardening. | Before presentation, establish zero override calls, zero in-flight captures, and empty capture state. Accept the callback only for one request per exact ordered window title, the current application PID, absent preferred CG identity, and enabled title-bar inference. Matching duplicate callbacks over-fulfill. Reuse `windowPreviewEventDelivery` solely as the terminal XCTest failure bound, then read back the unique invocation, final request evidence, exact in-flight count, and all capture states as in-flight before releasing the batch. The test invocation owns semaphore release and model/controller cancellation with `defer`. The independent preview-completion/reveal event remains SYNC-036AC. | M initial-preview behavior-test Oracle; existing policy compatibility plus the exact batch-start path, Process/Tooling. | completed |
| SYNC-036AC | `testInAppWindowPanelRevealsAfterAsyncPreviewBatchCompletes`; initial panel reveal after the controlled preview batch completes | The second raw one-second async fulfillment bound waits for `onWindowOnlyPreviewPreparationChanged` after releasing the batch. The observer precedes presentation and the existing guard checks readiness, while compatible watchdog ownership, duplicate exact delivery, main-thread evidence, pending-watchdog baseline, exact window order, terminal capture states, and last-observation diagnostics are implicit. Watchdog classification and Oracle hardening. | Preserve the controller observer before presentation, then establish hidden panel, active observation, pending watchdog, and empty ready/failure baselines while the batch is blocked. After release, accept the callback only on the main thread for the exact observation and presentation generations, `previewBatchCompleted`, zero pending captures, cancelled owner watchdog, visible panel, exact ordered windows with images, zero in-flight work, and all capture states succeeded. Matching duplicates over-fulfill. Reuse `windowPreviewEventDelivery` solely as the terminal XCTest failure bound. Final callback/match counts, evidence, window/image/state observations, owner state, panel visibility, and snapshot form independent readbacks. The test invocation owns callback restoration, semaphore release, and controller/model cancellation in `defer`. | M initial-preview behavior-test Oracle; owner initial/event/duplicate/generation/cancel/watchdog contracts plus the exact asynchronous reveal path, Process/Tooling. | completed |
| SYNC-036AD | `FlowTabPriorityCoverageTests+TerminatePressFeedbackEntry.swift` and `testSwitcherPanelControllerQuitFrontmostAppInAppLayerKeepsSessionAfterWorkspaceTerminationRefresh`; controller workspace-termination refresh after terminate press feedback | A raw one-second async fulfillment waits after `handleWorkspaceApplicationTerminatedForTesting(appID:pid:)` returns even though the controller synchronously invokes `handleApplicationTerminated`, which reloads the projection and publishes `onSessionLayoutChanged` before returning. Evidence migration. | Preserve the controller layout observer before the session starts, establish a zero publication baseline immediately before the exact app/PID workspace-termination trigger, and require exactly one main-thread publication by method return. Capture its exact remaining ordered app IDs, preferred-next selection, and termination signal. Final session membership/count, app/PID signal, second projection read, cleared pending request, cleared terminating identity, and inactive Search state remain independent readbacks. The test invocation restores the observer and cancels the controller/model session with `defer`; no duration remains. | M terminate-entry behavior-test Oracle; affected workspace-termination path plus terminate-feedback entry companion, Process/Tooling. | completed |
| SYNC-036AE | `FlowTabTests+UITestInitialPanelOcclusionStaleness.swift` and `testUITestInitialPanelOcclusionStalenessDeinitCleansUpInjection`; asynchronous owner-deinitialization cleanup | A raw one-second XCTest bound waits for the main-actor cleanup task created by the staleness owner's `deinit`. The preinstalled cancellation callback is the completion evidence, while compatible watchdog ownership, callback uniqueness/thread, token state, exact cleanup readback, initial baseline, and final diagnostic are implicit. Watchdog classification and Oracle hardening. | Establish active-owner, pending-release, installed-phase, zero-cancellation, unavailable-readback, and uncancelled-token baselines before releasing the owner. Accept cleanup only through the preinstalled main-thread cancellation callback after its release token is cancelled; reject duplicate callbacks and read back exactly one cleanup, the absent weak owner, removed injection, exact unavailable-to-uninstalled transition, unchanged installed-only published phase, and cancelled token. Retain one compatible named deinitialization-cleanup watchdog solely as the terminal failure bound. The owner deinitializer owns the transferred cleanup task; the test invocation owns its expectation. | M TestingSupport lifecycle-test Oracle; named-policy compatibility plus install/release/replacement/synchronous/deinit owner paths and existing 500-generation lifecycle Pressure, Process/Tooling. | completed |
| SYNC-036AF | `FlowTabTests+AppVisibilityReload.swift`; hidden and Pinyin-search App inventory reload completion | Two raw five-second XCTest bounds surround pre-trigger Combine subscriptions that already distinguish the initial idle value from a later loading-to-idle transition. The event is observable evidence, while compatible watchdog ownership, exact initial/terminal sequence, callback thread, duplicate completion, final inventory/selection evidence, subscription cancellation, and failure diagnostics are implicit. Watchdog classification and Oracle hardening. | Subscribe before `reload()`, assert the exact initial `[false]` loading baseline, and accept only a main-thread suffix transition `[true, false]`. Matching duplicate transitions over-fulfill. Capture the completion-time visible app IDs, selected app ID, and hidden count, then require exactly one complete `[false, true, false]` sequence plus identical final model readbacks for both missing-hidden and Pinyin-search paths. Retain one compatible named event-delivery watchdog solely as the terminal failure bound. Each test invocation explicitly cancels its subscription with `defer`; the model owns its reload task. | M settings behavior-test Oracle; named-policy compatibility plus both exact reload paths, Process/Tooling. | completed |
| SYNC-036AG | `FlowTabTests+UITestMockWindowPreviewLatency.swift` and `testUITestMockWindowPreviewLatencyCancellationReleasesWait`; controlled preview-latency wait cancellation | A raw one-second semaphore deadline bounds entry into the blocking test waiter, and a separate raw one-second XCTest timeout bounds publication after cancellation. The start signal and exact cancelled evidence are observable Oracles, while policy ownership, initial/waiting/final state, returned-versus-published evidence, duplicate completion, background work ownership, and final diagnostics are implicit. Watchdog classification and Oracle hardening. | Establish zero-wait, idle, zero-cancel, and empty returned/published evidence baselines before dispatch. The waiter atomically records its wait count and waiting state before signalling start. Accept completion only when the owner returns the exact generation/batch/request/policy `.cancelled` evidence, then require the published evidence to match it, the waiter to be idle with one wait and one cancel, and the controlled worker to finish. Retain one compatible named event-delivery watchdog for both terminal failure bounds. The test invocation owns a cancellable DispatchWorkItem and idempotent owner cleanup in `defer`; owner cancellation releases the waiter. The intentional one-second mock I/O policy remains the SYNC-030A domain duration. | M TestingSupport lifecycle-test Oracle; named-policy compatibility plus exact cancellation, policy/evidence, Bootstrap replacement/stop, and 500-generation Pressure paths, Process/Tooling. | completed |
| SYNC-036AH | `testRuntimeAXRemoteWindowResolverCompletesConfiguredRangeAfterResolverSuspension` and `FlowTabPriorityCoverageTests+RuntimeAXRemoteWindowScanLifecycle.swift`; controlled Remote AX scan suspension and completion | Two raw one-second semaphore deadlines bound the element-24 suspension event and final scan completion, while an immediate zero-deadline semaphore read treats non-completion as a separate Oracle. Exact visited IDs and scan completeness are already observable, but policy ownership, initial and intermediate lifecycle readbacks, transition ordering, cancellable barrier ownership, and terminal diagnostics are implicit. Watchdog classification and Oracle hardening. | Install a thread-safe lifecycle observation and controlled suspension owner before dispatch. Establish an exact idle baseline, then require `scanning → suspended(elementID: 24)` with visited IDs `0...24` before resuming. Completion requires `resumed(elementID: 24) → completed`, the exact `0..<750` visit set, `.complete(scanned: 750)`, and no transition violation. Remove the immediate absence-over-time assertion. Retain one compatible named event-delivery watchdog solely around the suspension and completion events, with the final lifecycle observation on failure. The invocation owns a cancellable work item and idempotent suspension cancellation in `defer`. Extract the lifecycle contract from the oversized topology test file into a focused test owner. | M runtime behavior-test Oracle; named-policy compatibility plus suspended and uninterrupted configured-range scans, Process/Tooling. | completed |
| SYNC-036AI | `testSwitcherPanelControllerTerminateRequestProtectsPanelResignAfterModifierRelease`; termination request after press-feedback completion | A raw one-second fulfillment waits for the real 120ms press-feedback task before checking termination protection. The request callback is observable, but elapsed scheduling currently controls when the test reaches its assertions and hides the pending completion generation, exact interval, pre-request protection state, duplicate-delivery rule, and cleanup ownership. Evidence migration. | Inject the existing manual press-feedback scheduler. Install the exact request/protection observation before session start and establish zero-generation, idle-owner, empty-schedule, empty-request, and unprepared-protection baselines. After `terminateSelectedApp()`, require pending generation 1, the named product interval, unchanged empty request evidence, and no pending terminate request. Manually deliver generation 1 and accept completion only through the exact selected-app request observed after protection preparation; then read back the request PID/generation, maintenance request, owner idle state, active protection, retained session after panel resign, and rejected duplicate delivery. The test invocation clears the weak request override and cancels the controller with `defer`. Retain 120ms only as the named press-feedback animation contract; no test watchdog remains. Move this contract from the oversized general panel file into the dedicated press-feedback entry owner. | M switcher behavior-test Oracle; exact entry/protection path, completion-owner cancellation/replacement/synchronous/500-generation Pressure companions, Process/Tooling. | completed |
| SYNC-036AJ | `AppWindowCoordinator.openHome/openLogs/openSettings` and `testAppWindowCoordinatorOpenMethodsSelectRequestedTabBeforeActivation`; main-actor tab selection and window activation completion | A raw one-second fulfillment waits for the global activation override after each unstructured main-actor Task. Although the override is observable, the Task itself has no caller-owned completion handle; a watchdog failure can replace or restore the global observer while a late task remains live, and exact task completion plus duplicate-read behavior are implicit. Evidence migration. | Return the already-created `Task<Void, Never>` as a discardable named `OpenOperation` without changing its main-actor body or existing call-site behavior. Install the activation observer before each trigger, establish the initial tab and empty-observation baseline, retain exact queued-state evidence until the current main-actor segment yields, then await the operation value. Success requires exactly one main-thread activation observing the requested tab plus the identical final tab readback; a second operation-value read must remain idempotent. The test invocation retains every operation and cancels them with `defer`, then restores the global observer and prior tab. Remove the XCTest watchdog entirely. Extract the contract from the oversized mixed test file into a focused owner. | M app-window orchestration/API evidence; all three entry points plus app-window activation companions, Process/Tooling. | completed |
| SYNC-036AK | `SpaceFixtureWorkflowReadinessAggregateObservationOwner` and its three deterministic observation tests; aggregate readiness after initial readback, notification refresh, and stale-generation duplicate pressure | Each test passes a raw two-second timeout to `waitForReady` after the complete configured/ready evidence has already been persisted or synchronously delivered. The owner performs an immediate readback before waiting, but a terminal XCTest wait can obscure whether the expected generation became ready synchronously and makes elapsed time part of an otherwise deterministic owner contract. Evidence migration. | Expose the owner’s current ready snapshot as a readback without changing the real asynchronous wait path. Install/start the owner before recording evidence and require an initially empty snapshot. The missed-initial-notification case explicitly invokes immediate readback; notification cases accept readiness only from the exact current observation generation after the notification-triggered readback. Require configured and ready stages, `isReady`, and exact generation; stale-generation and duplicate events must leave the current snapshot empty until current-generation evidence arrives. The test invocation owns cancellation and readback-file cleanup through `defer`. Remove all three test watchdogs; real fixture launches continue using the named workflow readiness watchdog solely as a failure bound with aggregate diagnostics. | M UI-test observation owner; initial readback, notification refresh, stale/duplicate generation Pressure, signed targeted UI, Process/Tooling. | completed |
| SYNC-036AL | `FlowTabUITests+RuntimeLogObservationTests.swift` and `testRuntimeLogRegexObservationReadsMockRuntimeTriggerDelivery`; mock-runtime application foreground readiness before RuntimeLog baseline and trigger | A raw ten-second timeout bounds the fixed-path app reaching `.runningForeground`. XCUI state is already the independent Oracle, but the RuntimeLog test owner does not name its compatible failure bound and the call-site assertion omits the final App-state readback. Watchdog classification and diagnostic hardening. | Add a RuntimeLog-observation-test-owned application-readiness watchdog preserving the ten-second bound. Require it to be finite, positive, and no shorter than the same owner’s file-event watchdog. The shared helper continues its immediate state readback and conditional XCUI wait; the call site accepts success only from `.runningForeground` and reports the unmet condition plus final App state. Establish the RuntimeLog file baseline only after foreground evidence and before posting the exact switcher trigger. The test invocation owns App termination and the shared RuntimeLog observation lifecycle. No polling, retry, scheduler, or product duration changes. | M UI-test watchdog owner; deterministic policy compatibility plus mock-runtime trigger-delivery UI, Process/Tooling. | completed |
| SYNC-036AM | `FlowTabUITests+ApplicationTerminationObservation.swift` and `testApplicationTerminationWaitsForDelayedNotRunningEvidence`; caller-selected termination watchdog pass-through | The deterministic delayed-termination stub passes a raw nine-second override to the shared process-lifecycle helper. The stub does not wait for elapsed time and success already depends only on the final `.notRunning` readback, but the override’s compatibility and owning policy are implicit. Retained watchdog classification. | Name the nine-second delayed-evidence override beside the existing five-second default termination watchdog. Assert its exact compatible value and pass-through to the target wait. Preserve the helper’s initial readback, termination trigger, immediate readback, exact `.notRunning` wait, and final readback; a waiter boundary alone never establishes success. The synchronous helper invocation owns the wait and leaves no observer or scheduled work. | M UI-test process-lifecycle policy; all initial/immediate/delayed/boundary/watchdog cases plus 100-target Pressure, signed targeted UI, Process/Tooling. | completed |
| SYNC-036AN | `FlowTabUITests+HomeAppSelectionObservationTests.swift` and `testHomeAppSelectionWatchdogBudgetUsesMonotonicTime`; deterministic selection-watchdog budget fixture | The injected-clock budget test passes a raw eight-second timeout and uses anonymous monotonic probe values. No real elapsed time is consumed and Home selection success already depends on exact title-projection evidence, but the retained watchdog fixture lacks an explicit named owner. Retained watchdog classification. | Add named budget timeout, monotonic origin, before-deadline elapsed, and after-deadline elapsed values to the Home-selection test policy. Require the exact compatible eight-second budget, compute remaining time from the injected monotonic clock, and clamp it to zero after the deadline. Preserve initial, trigger, scheduled, cancelled-generation, duplicate, and diagnostic evidence contracts; the invocation owns observation cancellation and scheduled-readback release. | M UI-test observation policy; all deterministic selection cases, 100-generation Pressure, signed targeted UI, Process/Tooling. | completed |
| SYNC-036AO | `FlowTabUITests+LogsProjectionObservation.swift`, `FlowTabUITestLogsProjectionPolicy`, and `assertLogVisibility`; Logs-tab navigation and exact seeded-row publication | Raw five- and eight-second values bound tab hittability and the post-trigger seeded-row projection. The independent success evidence and lifecycle owner already exist, while the retained terminal bounds have no named policy contract. Retained watchdog classification. | Name the compatible tab-navigation and exact-projection watchdogs under the Logs-projection owner. Start the projection observer before the navigation trigger, retain immediate baseline readback and trigger-completion gating, and accept only a hittable Logs tab followed by required tab/container existence, exact visible-row identifier multiplicities, exact total, and prohibited-row absence. The invocation owns observer cancellation and App termination; watchdog failure reports the unmet level and final projection diagnostics. No polling cadence, retry, scheduler, or product duration changes. | M UI-test observation policy; deterministic compatibility/exact-row/cancellation/watchdog tests, 100-lifecycle Pressure, runtime-log-level UI, Process/Tooling. | completed |
| SYNC-036AP | `FlowTabUITests+LaunchAtLoginSettings.swift` and `testSettingsLaunchAtLoginToggleDefaultsOffAndPersistsAcrossRelaunch`; Launch at Login toggle publication and persisted value readback | Two raw five-second element-existence waits bound the first-launch and relaunch Settings projections. Element existence and the exact off/on values already define success, while the repeated compatible bound and failure evidence have no local owner. Retained watchdog classification and diagnostic hardening. | Add a Launch-at-Login-settings-owned toggle-publication watchdog preserving the five-second bound. Settings navigation remains independently established first; each existence wait performs its own initial element readback and accepts only `exists`. Require exact off state on the reset launch, exact on state after the toggle trigger, and exact persisted on state after relaunch. Failure reports phase, final existence, and final accessible value. The XCTest invocation owns App termination and defaults restoration through suite teardown. No polling, retry, scheduler, or product duration changes. | M UI-test Settings policy; deterministic compatibility plus reset/toggle/relaunch persistence UI, Process/Tooling. | completed |
| SYNC-036AQ | `confirmSwitcherSearchSelection`, `confirmEdgeSwitcherSearchSelection`, and `testSearchPanelEntryAndResultActivation`; Search Return handling across standard, multi-app, and edge-title fixture paths | Each path sends Return, waits 1.2 seconds for Search-input disappearance, and sends a second Return when that deadline expires. The first Return can either activate the selected result or commit an AppKit marked-text composition, so scheduler pressure changes the input sequence and may redirect a duplicate Return. A later raw three- or four-second wait checks only final disappearance. Evidence migration plus terminal watchdog. | Establish a runtime-log file baseline and combined file-event/named cancellable readback observer before Return. Perform an immediate exact Search-input readback. Accept either exact input nonexistence or a post-baseline `markedText changed=0 active=1 inputFocused=1` marker containing the exact current query. Input disappearance has priority when both are visible. Send a second Return only after the exact marked-text-clear evidence, with a new dismissal-only observer installed first; if dismissal has already arrived, complete without another input. The shared helper invocation owns both generations, observer/scheduler cancellation, baseline cleanup, and the compatible four-second terminal watchdog. Failure reports the unmet phase, exact marker, input existence, file generations, and log tail. Enable DEBUG evidence only for affected UI launch owners. | H shared Search keyboard-input and real-fixture activation Oracle; deterministic initial/exact-query/dismissal-priority/scheduled/cancel/duplicate/stale/watchdog tests, 100-generation Pressure, all affected standard/multi-app/edge-title UI paths, Process/Tooling. | completed |
| SYNC-036AR | `TabSwitchStressUITestPolicy`, `TabSwitchStressUITestObservationOwner`, and `testTabSwitchStressCPUAndMemory`; UI-owned tab-switch Pressure protocol and its terminal bounds | The measurement count, two-second workload, 16ms cadence, five-second foreground bound, ten-second completion-notification bound, and ten-second natural-exit bound are raw literals. Exact notification evidence and process state already define success, while policy compatibility, final-state diagnostics, and failure cleanup ownership remain implicit. Retained domain duration plus watchdog classification. | Name the three-iteration, two-second, and 16ms Pressure contract and derive nanoseconds plus `ceil(duration / cadence)` required switches from it. Name readiness, completion-evidence, and natural-termination watchdogs. Establish the unique distributed observer before launch; require exact completion evidence containing policy, 125 attempts, 125 readback-confirmed switches, requested/observed tab, elapsed duration, and both satisfied terminal flags. Require exact foreground and `.notRunning` state readbacks independently. The measurement invocation owns observer cancellation and a shared exact-state cleanup fallback; watchdog failure reports final app state or all received evidence. | M UI-test hot-path policy; deterministic compatibility, signed three-iteration real UI Pressure, exact process cleanup, Process/Tooling. | completed |
| SYNC-036AS | `FlowTabUITestHomePostlaunchAppSummaryPolicy` and `testHomeObservesFixtureAppsLaunchedAfterFlowTab`; Home projection of real fixture applications launched after FlowTab | Raw ten-second Home-tab navigation and two raw 20-second application-row/value waits bound three distinct observable conditions. Hittability, row existence, and exact `"<windowCount>w"` value already define success, while the bounds and final failure evidence have no local owner. Retained watchdog classification. | Name the Home-tab navigation, application-row publication, and exact-window-count watchdogs. Preserve the shared immediate plus cancellable serial readback owners for hittability and value, and the XCTest exact-existence predicate for each application row. Require a hittable Home tab, every exact workflow application identifier, and each exact configured window count. On watchdog failure report final candidate count, existence, hittability, identifier, and accessible value. The workflow invocation owns App/fixture teardown; each shared observer invocation owns cancellation. | M real-topology UI-test projection policy; deterministic compatibility, affected postlaunch multi-app Home UI, Process/Tooling. | completed |
| SYNC-036AT | `terminateIfRunning` in `FlowTabUITests+SpaceFixtureAXSuppressionRealTopologyTests.swift`; prelaunch and teardown termination of the AX-suppression FlowTab and fixture applications | A private helper uses a raw five-second state wait after `terminate()` and accepts its waiter Boolean directly. The existing guard limits termination to foreground/background applications, while timeout failure omits the exact target and final process state. Evidence migration to shared termination owner. | Preserve the foreground/background guard, then delegate to `terminateFlowTabUITestApplication`, whose immediate pre-trigger and post-trigger readbacks plus final `.notRunning` readback decide success. Reuse its compatible named five-second terminal watchdog. Report the exact FlowTab or workflow-app target, initial/post-trigger/final state, termination request, and waiter result. The real-topology test invocation owns both deferred cleanup calls; the shared helper owns the synchronous wait. Readiness and suppression observation remain separate later slices. | M UI-test process-lifecycle caller; existing deterministic initial/immediate/delayed/boundary/watchdog/repeated-lifecycle termination suite, affected AX-suppression real-topology UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036AU | `testAXSuppressionUsesAuthorizedFlowTabReadbackForRealFixtureTopology`; FlowTab foreground readiness before permission and fixture triggers | A raw 12-second `XCUIApplication` state wait bounds foreground readiness. Its Boolean already comes from exact `.runningForeground` evidence and performs an initial state readback, while the compatible shared watchdog owner and failure-state diagnostic are implicit. Retained watchdog classification. | Reuse `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` as the compatible terminal failure bound. Preserve the shared readiness helper's immediate exact-state check, installed-App activation fallback, and final `.runningForeground` Oracle. Record the final application state when the watchdog expires. The real-topology invocation owns FlowTab teardown; the synchronous state wait owns no recurring work. AX-suppression completion remains a separate later slice. | M real-topology UI-test readiness caller; existing support-policy compatibility plus affected AX-suppression UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036AV | `SpaceFixtureAXSuppressionObservationOwner.waitForSuppression`, its direct real-topology caller, and `runRealSpaceFixtureWorkflow`; exact application AX-suppression completion | Two raw 20-second caller bounds surround the same route owner. Success already requires a matched suppression-completion notification and authorized external AX readback for exact generation, bundle ID, PID, window count, source generation, and zero exposed children/windows, while watchdog ownership is duplicated at callers. Retained watchdog classification. | Move the compatible 20-second terminal bound to `SpaceFixtureAXSuppressionObservationPolicy.resolutionWatchdog` and make it the shared owner default for both callers. Preserve pre-trigger registration of both notification observers, initial readback, per-start generation, strict monotonic completion/verification generations, exact field matching, cancellation, and final watchdog readback. Failure reports route identity, expected windows, last paired/unpaired evidence, source, generation, and waiter result. Direct and multi-app invocations own owner cancellation plus App/fixture teardown. | H shared real-topology UI-test notification owner; policy compatibility, synchronous/initial, out-of-order, cancelled/stale/duplicate/regressed, watchdog-diagnostic, 100-route lifecycle Pressure, representative authorized-readback UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036AW | `FlowTabUITestSearchSelectionWrapPolicy` and both `performAndWaitForSwitcherDiagnostics` phases in `testSearchPanelWrapFromLastResultScrollsBackToFirstResult`; Search selection advance and last-to-first wrap | A raw three-second bound is repeated for each of ten ordered Down transitions and for the final wrap. Exact percent-decoded `searchSelectedResult` diagnostics already define success, while the terminal bound has no local owner. Retained watchdog classification. | Name the compatible diagnostics-transition watchdog at the Search-wrap owner. Before every Down trigger, preserve the shared diagnostics observation registration, initial readback with post-trigger acceptance disabled, trigger readback, named cancellable XCUI readback cadence, and exact expected result ID. Success requires each ordered `app:com.flowtab.mock.wrap.01...10` transition and the final exact `.01` wrap. Failure reports expected key/value, final existence/raw/value map, source, generation, and waiter result. Each helper invocation owns scheduler and observation cancellation. | M repeated UI-test selection-transition caller; policy compatibility, shared percent-decoding/post-trigger/scheduled/watchdog/stale-generation coverage, 100-generation Pressure, affected Search-wrap UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036AX | `FlowTabUITestSearchSelectionWrapPolicy` and the diagnostics-summary publication gate in `testSearchPanelWrapFromLastResultScrollsBackToFirstResult`; Search diagnostics accessibility publication | A raw five-second `waitForExistence` bounds diagnostics-summary publication after Search readiness. Exact element existence already defines success, while the compatible terminal bound and final failure evidence have no local owner. Retained watchdog classification. | Name the compatible diagnostics-publication watchdog at the Search-wrap owner. Preserve `waitForExistence`'s immediate readback so an already-published summary completes synchronously, then accept only exact `exists` evidence. XCTest owns the platform observation and cancellation for the synchronous call. The watchdog solely terminates failure and reports the unmet publication condition, final existence, and exact accessibility identifier. Selection transitions and result hittability remain independent contracts. | M UI-test accessibility-publication caller; policy compatibility, affected Search-wrap UI including initial readback, exact process cleanup, Process/Tooling. | completed |
| SYNC-036AY | `FlowTabUITestSearchSelectionWrapPolicy` and both `hasHittableElement` phases in `testSearchPanelWrapFromLastResultScrollsBackToFirstResult`; last-result and wrapped-first-result actionable accessibility evidence | Raw two- and five-second bounds wait for the exact last result after ten Down transitions and the exact first result after wrap. The shared helper already requires an existing, hittable exact-identifier candidate, while the phase budgets and caller failure readbacks have no local owner. Retained watchdog classification. | Name compatible last-result and wrapped-first-result hittability watchdogs at the Search-wrap owner. Preserve the shared observer's preinstalled serial schedule, immediate exact candidate readback, generation rejection, and cancellable 100ms fallback because XCUI exposes no candidate-publication callback. Success requires the exact identifier query to contain an existing, hittable candidate. The helper invocation owns schedule cancellation; each watchdog solely terminates failure and reports candidate count, existing indices, first hittable index, source, generation, plus caller readback of exact identifier, final count, existence, and hittability. | M repeated UI-test hittability caller; policy compatibility, shared initial/scheduled/cancel/watchdog coverage, 100-lifecycle Pressure, affected Search-wrap UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036AZ | `FlowTabUITestHomeActivationPolicy` and the Home-tab navigation phase of `testHomePageClickingRealWorkflowWindowActivatesExactFixtureWindow`; Home activation workflow navigation readiness | A raw ten-second bound waits for the Home tab before exact fixture-App and window activation checks. The shared helper already requires an existing, hittable exact Home-tab candidate, while the scenario-compatible bound and final caller readback have no local owner. Retained watchdog classification. | Name the Home-activation navigation watchdog. Preserve the shared hittability owner's preinstalled named serial schedule, immediate exact-candidate readback, generation rejection, and cancellation because XCUI exposes no tab-publication callback. Success requires the exact Home-tab query to contain an existing, hittable candidate. The helper invocation owns schedule cancellation; the workflow invocation owns App/fixture teardown. Watchdog failure reports shared candidate evidence plus final query count, existence, and hittability. App-row publication, Home-window projection, and exact window activation remain independent later slices. | M real-topology UI-test navigation caller; policy compatibility, affected Home activation UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BA | `FlowTabUITestHomeActivationPolicy` and the fixture-App row gate in `testHomePageClickingRealWorkflowWindowActivatesExactFixtureWindow`; exact Home application-row publication | A raw 20-second `waitForExistence` bounds publication of the exact target fixture App after FlowTab launch and Home navigation. Exact row existence already defines success, while the scenario-compatible bound and final readback have no local owner. Retained watchdog classification. | Name the Home-activation App-row publication watchdog. Bind the exact bundle-derived accessibility identifier, preserve `waitForExistence`'s initial readback so launch-time publication cannot be missed, and accept only `exists`. XCTest owns the platform wait lifecycle; the workflow invocation owns FlowTab and fixture teardown. The watchdog solely terminates failure and reports App name, expected identifier, final existence, and final hittability. Home-window projection and exact window activation remain independent later slices. | M real-topology UI-test publication caller; policy compatibility, affected Home activation UI including initial readback, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BB | `performAndWaitForHomeWindowRow`, `FlowTabUITestHomeActivationPolicy`, and the fixture-App selection phase of `testHomePageClickingRealWorkflowWindowActivatesExactFixtureWindow`; exact post-selection Home window-row projection | The caller taps the fixture-App row before starting a raw 12-second title-row observer. An immediate readback can recover an already-published row, but the observer lifecycle is not established before the trigger and a matching stale projection can satisfy without a post-trigger transition. Evidence migration plus retained watchdog. | Start the shared Home projection owner before the App-row tap with acceptance disabled. Capture the baseline projection, execute the single tap, enable acceptance, and request an immediate trigger-return readback. Accept only a post-trigger snapshot containing a row whose label or value case-insensitively contains the exact target title. Retain the named cancellable serial 100ms XCUI readback fallback, owned and cancelled by the helper invocation; generation checks reject stale work. Name the compatible Home-window projection watchdog solely as a failure bound reporting acceptance, expectation, full row identifiers/labels/values, static titles, source, generation, and waiter result. Exact frontmost-window activation remains a later slice. | H shared UI observation trigger boundary and real window identity; deterministic pre-trigger/stale/synchronous/scheduled/cancel/watchdog tests, 100-lifecycle Pressure, affected real Home activation UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BC | `FlowTabUITestHomeActivationPolicy` and the final row-click phase of `testHomePageClickingRealWorkflowWindowActivatesExactFixtureWindow`; exact frontmost fixture-window activation | A raw ten-second bound terminates the shared activation observer after the exact Home row click. Exact bundle and CGWindowID already define success, while the retained watchdog policy and Home-scenario owner are implicit. Retained watchdog classification. | Name the Home exact-window activation watchdog and pass it only as the terminal bound to the SYNC-034Q generation-owned observer. Keep observer registration before the click, baseline readback behind the trigger gate, workspace and AX event readbacks, named cancellable serial fallback cadence, and exact frontmost bundle plus topmost CGWindowID as the sole success Oracle. The helper invocation owns observer, schedule, generation, cancellation, and final-readback diagnostics; the workflow invocation owns application and fixture teardown. | H exact window-identity UI caller; policy compatibility, deterministic initial/gated/exact/slow/cancel/watchdog regressions, 100-lifecycle Pressure, affected real Home fixture UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BD | `FlowTabUITestRuntimeProjectionPublicationPolicy`, `performAndWaitForCommittedSearchWindowResult`, and both post-query phases in `FlowTabUITests+RuntimeProjectionPublication`; committed window-result publication after Search input | The mock path types `Inbox` before a raw eight-second exact-row wait, while the real fixture path types `Docs` before starting a raw eight-second matching-result observer. An immediate later readback can recover publication, but a matching pre-query row can satisfy without proving that the requested scope/query projection committed. Evidence migration plus retained watchdog. | Extend the shared Search-result expectation with an atomic committed scope, decoded query, title, and App-name Oracle. Start the generation-owned observer before typing, reject baseline evidence until input delivery returns, and request an immediate trigger-return readback. Retain the named cancellable XCUI readback cadence because accessibility exposes no result-publication callback. The helper invocation owns generation, schedule, cancellation, and final projection diagnostics; the scenario owns the compatible named eight-second watchdog and App/fixture teardown. Startup foreground and Search responder readiness remain independent later contracts. | H shared Search-result owner and mock/real UI paths; policy compatibility, deterministic pre-trigger/stale-query/exact-result/slow/cancel/watchdog coverage, 100-lifecycle Pressure, affected mock and real-fixture UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BE | `prepareInitialFlowTabSearchInputReadiness`, `requireInitialFlowTabSearchInput`, and both initial Search gates in `FlowTabUITests+RuntimeProjectionPublication`; launch-time Search keyboard readiness | Raw five- and eight-second element-existence waits start after the mock or real-topology FlowTab launch. Exact accessibility existence proves publication, but not that the Search text view is first responder in the key panel before `typeText`. Evidence migration. | Establish the shared runtime-log baseline and file observer before the launch workflow. Accept an initial or later exact `keyboardReadiness ready=1` marker only when identifier, `SearchSystemTextView` responder, and key-window evidence all match, then independently read back the exact accessibility input identity. The readiness helper owns the named file-event/readback observation, generation, cancellation, and final log evidence; test teardown cancels paths that exit before consumption. Its existing 35-second marker and five-second accessibility watchdogs remain terminal failure bounds. | H keyboard-input boundary across mock and real UI; deterministic initial/event/inexact/cancel/duplicate/watchdog coverage, 100-lifecycle Pressure, affected mock and real fixture UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BF | `testWindowSearchPublishesMockWindowRowsAtLaunch` in `FlowTabUITests+RuntimeProjectionPublication`; FlowTab foreground readiness before Search evidence | A raw ten-second `XCUIApplication` state wait bounds foreground readiness. Its result already comes from exact `.runningForeground` evidence with an initial state readback and installed-App activation fallback, while policy ownership and the failure-state diagnostic are implicit. Retained watchdog classification. | Reuse `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` as the compatible terminal failure bound. Preserve the shared readiness helper's exact-state observation and activation fallback, and report the final App state when the watchdog expires. The test invocation owns App teardown; both state waits are synchronous observations with no recurring lifecycle. | M mock UI-test readiness caller; shared policy compatibility, affected mock UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BG | `launchSpaceFixtureApplicationAndWaitForForeground` plus direct fixture-launch callers in `FlowTabUITests+SpaceFixtureApp` and `FlowTabUITests+SpaceFixtureWorkflowConfiguration`; Space Fixture foreground readiness | The shared helper uses a raw three-second initial observation cap and raw ten-second default activation bound, while three direct fixture callers bypass it with independent raw ten-second waits. Every result already comes from exact `.runningForeground` state, but compatible ownership and final-state diagnostics are incomplete. Retained watchdog classification. | Add fixture-specific initial-observation and activation bounds to `FlowTabUITestSupportWatchdogPolicy`. Route all three direct launch callers through the shared helper, preserve immediate XCUI state observation before activation fallback, and report the final fixture state if the terminal bound expires. The helper owns synchronous state observation only; each UI test owns fixture termination. Workflow readiness, visible metadata, and exact label assertions remain separate evidence contracts. | H shared real-fixture launch helper and three UI paths; policy compatibility, affected configured-title, fullscreen-target, and tabbed-workflow UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BH | `SpaceFixtureConfiguredContentObservationOwner` and `testSpaceFixtureAppLoadsWorkflowConfiguredTabbedWindows`; configured fixture title, subtitle, selected-tab, and background-tab publication | Five sequential raw five-second existence waits run after workflow readiness and can observe each label at a different projection generation. Existence plus later label reads are evidence, but they do not form one atomic configured-content Oracle and each wait owns an implicit XCUI polling policy. Evidence migration plus retained watchdog. | Establish one content-observation owner before fixture launch. Require one snapshot in which all five exact accessibility identifiers exist with their exact expected labels. Because XCUI exposes no accessibility-publication callback, reuse the shared named serial 100ms readback cadence with an immediate initial readback; the owner cancels on resolution, watchdog, test teardown, or deinit. Retain a named five-second publication watchdog solely as the terminal failure bound and report every final identifier, existence value, expected label, observed label, generation, source, and wait result. Fixture foreground and workflow readiness remain independent earlier Oracles. | H repeated XCUI readback and atomic real-fixture UI Oracle; policy and exact/missing/wrong-label rules, shared initial/scheduled/cancel/watchdog regressions, lifecycle Pressure, affected tabbed-workflow UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BI | `testHomeKeepsCurrentFlowTabInApplicationDirectoryWithoutAccessibilityPermission`; FlowTab foreground readiness before Home permission-boundary navigation | A raw eight-second `XCUIApplication` state wait bounds foreground readiness. Its Boolean already comes from the shared helper's initial exact `.runningForeground` readback and installed-App activation fallback, while compatible policy ownership and final-state diagnostics are implicit. Retained watchdog classification. | Reuse `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` as the compatible terminal failure bound. Preserve the helper's exact-state observation and activation fallback, and report the final App state when readiness remains unmet. The test invocation owns App teardown; the synchronous state waits own no recurring lifecycle. Home-tab navigation and permission-boundary projection remain independent later contracts. | M Home permission-boundary UI caller; shared policy compatibility, affected visible UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BJ | `tapFirstHittableAndWaitForExistence` and the Home tab phase of `testHomeKeepsCurrentFlowTabInApplicationDirectoryWithoutAccessibilityPermission`; Home navigation completion | A raw five-second hittable-button wait is followed by a raw five-second Home-content existence wait established after the click. Each uses an exact condition, but the content observation is not installed before the navigation trigger and both compatible bounds are anonymous. Evidence migration plus retained watchdog. | Add a shared trigger-and-content helper that starts a generation-owned content-existence observation before waiting for and tapping the exact button. Accept an already-present content state through the immediate readback or a later exact existence readback. Because XCUI exposes no content-publication callback, reuse the shared named serial 100ms cadence. Use `FlowTabUITestSupportWatchdogPolicy.tabNavigation` only as the terminal bound for the exact hittable trigger and content evidence. The helper invocation owns generation, timer cancellation, and final trigger/content diagnostics; the test owns App teardown. Permission-boundary row projection remains a later contract. | H shared navigation helper with repeated XCUI readback; policy compatibility, generic initial/scheduled/cancel/watchdog/reentrancy and lifecycle Pressure, affected Home UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BK | `FlowTabUITestHomePermissionBoundaryProjectionObservationOwner` and the projection phase of `testHomeKeepsCurrentFlowTabInApplicationDirectoryWithoutAccessibilityPermission`; exact Home permission-boundary projection | A raw six-second App-row existence wait followed by two raw two-second value waits starts after Home navigation. The three sequential reads can accept the row, hidden-window summary, and hidden-App count from different projection states, and their observers are not established before the navigation trigger. Evidence migration plus retained watchdog. | Establish one generation-owned projection observer before Home navigation. Accept one initial or later snapshot only when the exact bundle-derived App-row identifier exists with value `0w hidden` and the exact hidden-App statistic identifier exists with value `1`. Because XCUI exposes no projection-publication callback, reuse the shared named serial 100ms readback cadence. Retain a scenario-owned six-second projection watchdog solely as the terminal failure bound. The owner cancels on resolution, watchdog, explicit teardown, or deinit and reports expected and final identifiers, existence, values, generation, source, and wait result. Foreground readiness and Home navigation remain independent earlier contracts. | H repeated XCUI readback and atomic visible projection Oracle; policy and exact/missing/wrong identity/value rules, initial/scheduled/cancel/watchdog regressions, generic reentrancy and lifecycle Pressure, affected Home UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BL | `testHomeInitialAppLayerUsesRuntimeOrderAndZeroCountsWithoutAccessibilityPermission`; FlowTab foreground readiness before mock Home initial-projection evidence | A raw eight-second `XCUIApplication` state wait bounds foreground readiness. Its Boolean already comes from the shared helper's immediate exact `.runningForeground` readback and installed-App activation fallback, while compatible policy ownership and final-state diagnostics are implicit. Retained watchdog classification. | Reuse `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` as the compatible terminal failure bound. Preserve exact App-state observation and activation fallback, and report the final state if readiness remains unmet. The test invocation owns App teardown; synchronous state observation creates no recurring work. Home navigation, initial visible rows, and later typed projection application remain independent contracts. | M mock Home initial-projection UI caller; shared policy compatibility, affected signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BM | `tapFirstHittableAndWaitForExistence` and the Home navigation phase of `testHomeInitialAppLayerUsesRuntimeOrderAndZeroCountsWithoutAccessibilityPermission`; mock Home initial-projection navigation completion | A raw five-second exact-button wait is followed by a raw five-second Home-content existence wait established after the click. Exact conditions define success, while the content observer ordering, compatible policy owner, cancellation, and final diagnostics are implicit. Evidence migration plus retained watchdog. | Route the caller through the shared trigger-and-content helper. Establish the generation-owned exact Home-content observer before waiting for and tapping the exact Home button. Accept an already-present initial state or a later exact existence readback through the shared named serial 100ms XCUI cadence. Use `FlowTabUITestSupportWatchdogPolicy.tabNavigation` only as the terminal bound for each exact condition. The helper invocation owns observation generation, cancellation, and final trigger/content diagnostics; the test owns App and independent projection-observer teardown. Initial visible rows and post-maintenance projection application remain later contracts. | H repeated XCUI readback in mock Home navigation; shared policy plus initial/scheduled/cancel/watchdog/reentrancy and lifecycle Pressure, affected signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BN | `FlowTabUITestHomeAppRowProjectionObservationOwner` and the initial visible-row phase of `testHomeInitialAppLayerUsesRuntimeOrderAndZeroCountsWithoutAccessibilityPermission`; exact ordered zero-count Home projection | Two raw six-second row-existence waits followed by two raw two-second value waits begin after Home navigation. Sequential success can combine Mail existence, Browser existence, both values, and frame order from different projection states; the observers are established after both launch and navigation triggers. Evidence migration plus retained watchdog. | Establish one generation-owned Home App-row projection observer before Home navigation and record its initial baseline with acceptance disabled. After exact Home navigation evidence succeeds, enable acceptance and request an immediate trigger readback; accept that readback or a later scheduled snapshot only when the exact Mail and Browser identifiers both exist, both values equal `0w`, both vertical positions are finite, and Mail precedes Browser. Reuse the named serial 100ms XCUI cadence because accessibility exposes no projection callback. Retain a named 16-second watchdog equal to the prior cumulative terminal bounds solely as failure protection, with acceptance state plus expected and final row identity/existence/value/order evidence. Resolution, expiry, explicit teardown, and deinit cancel the current generation and pending schedule. The test invocation owns App and independent typed projection-observer cleanup. | H repeated XCUI readback and ordered atomic projection Oracle; deterministic policy, exact/missing/wrong identity/value/order, initial/trigger-gate/scheduled/cancel/watchdog regressions, generic reentrancy and lifecycle Pressure, affected signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BO | `FlowTabUITestHomeAppRowProjectionObservationOwner` and the post-maintenance phase of `testHomeInitialAppLayerUsesRuntimeOrderAndZeroCountsWithoutAccessibilityPermission`; applied Home row identity/value/order/position readback | Two raw two-second row-value waits followed by direct independent frame reads assume that the application-level projection event has become one stable visible row projection. Sequential reads can combine values and positions from different SwiftUI layout states. Evidence migration plus retained watchdog. | Preserve the application observer established before launch and its atomic persisted `appSwitcherProjectionNotification` / `sourceGenerationAdvanced` evidence. After the initial exact row snapshot supplies the position baseline, establish a generation-owned row observer with acceptance disabled before consuming the application evidence. Once that exact current-launch generation evidence is accepted, enable the row observer and request an immediate readback. Resolve only from one exact Mail/Browser snapshot with both `0w` values, strict order, and both positions within one point of the initial baseline. Reuse the named serial 100ms XCUI cadence because accessibility exposes no projection callback. Retain a named four-second watchdog equal to the prior cumulative terminal bounds solely for failure termination, reporting the application generation plus expected baseline and final row evidence. Resolution, expiry, explicit teardown, and deinit own cancellation and cleanup. | H cross-process generation plus repeated XCUI readback and atomic position Oracle; deterministic policy/baseline/exactness/trigger/cancel/watchdog and shared lifecycle Pressure, affected signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BP | `testHomePermissionBannerConvergesFromOwnedTCCReadbackEvidence`; FlowTab foreground readiness before Home permission navigation and simulated TCC transition | A raw eight-second `XCUIApplication` state wait bounds foreground readiness. The shared helper already performs immediate exact `.runningForeground` observation and installed-App activation fallback, while policy ownership and final-state diagnostics are implicit. Retained watchdog classification. | Reuse `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` as the terminal failure bound. Preserve exact App-state observation and activation fallback, report the final state if readiness remains unmet, and stop this invocation before later independent navigation and permission-transition contracts. The test owns App teardown and its temporary permission-state path; this synchronous wait creates no recurring work. | M Home permission UI precondition; shared policy compatibility, affected signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BQ | `tapFirstHittableAndWaitForExistence` and the Home-navigation phase of `testHomePermissionBannerConvergesFromOwnedTCCReadbackEvidence`; exact Home permission content navigation | A raw five-second exact-button wait is followed by a raw five-second Home-content existence wait established after the click. Exact conditions define success, while observer ordering, compatible policy ownership, cancellation, and final diagnostics are implicit. Evidence migration plus retained watchdog. | Route this caller through the shared trigger-and-content helper. Establish the generation-owned exact Home-content observer before waiting for and tapping the exact Home button. Accept an already-present initial state or a later exact existence readback through the shared named serial 100ms XCUI cadence. Use `FlowTabUITestSupportWatchdogPolicy.tabNavigation` solely as the terminal bound for each exact condition. The helper invocation owns observation generation, cancellation, and final trigger/content diagnostics; the test owns App and temporary permission-state cleanup. Foreground readiness and the permission projection/transition remain independent contracts. | H repeated XCUI readback in Home permission navigation; existing shared policy, initial/scheduled/cancel/watchdog/reentrancy/lifecycle Pressure, affected signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BR | `FlowTabUITestConditionObservationOwner<Bool>` and the initial permission-action phase of `testHomePermissionBannerConvergesFromOwnedTCCReadbackEvidence`; exact Home permission-banner projection | A raw five-second element-existence wait begins after Home navigation, leaving observation ordering, policy ownership, cancellation, and final evidence implicit. The exact persistent element state is the success Oracle. Evidence migration plus retained watchdog. | Resolve the exact `permissionOpenSettings` element before Home navigation, establish its generation-owned existence observer before that trigger, perform an initial readback, and accept only an initial or later `exists=true` readback. Reuse the shared named serial 100ms XCUI cadence because Accessibility exposes no permission-projection callback. Add a named six-second permission-state projection watchdog solely as a terminal bound. Resolution, expiry, explicit teardown, and deinit own observer/schedule cancellation and diagnostics include the exact identifier, final existence, generation, source, and wait result. The test owns App and exact temporary permission-state cleanup; navigation and the later TCC transition remain independent contracts. | H repeated XCUI readback in Home permission projection; policy compatibility, initial/scheduled/cancel/watchdog/reentrancy/lifecycle Pressure, affected signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BS | `FlowTabUITestConditionObservationOwner<Bool>` and the simulated TCC transition phase of `testHomePermissionBannerConvergesFromOwnedTCCReadbackEvidence`; exact Home permission-banner removal | A raw six-second nonexistence expectation is installed after the atomic permission-state write. The persistent absence predicate defines success, while pre-trigger observation, immediate post-trigger readback, named policy ownership, cancellation, and final evidence are implicit. Evidence migration plus retained watchdog. | Establish a new generation-owned observer of the exact `permissionOpenSettings` element before the atomic permission-state write, require `exists=false`, then request an immediate trigger readback after the write and retain the shared named serial 100ms XCUI cadence for later readback because Accessibility exposes no banner-removal callback. Reuse the named six-second permission-state projection watchdog solely as the terminal failure bound. Resolution, expiry, explicit teardown, and deinit own observer/schedule cancellation; diagnostics include exact identifier, final existence, generation, source, and wait result. The pre-trigger runtime-log baseline remains an independent persistent evidence contract for the later log assertion. | H simulated TCC transition plus repeated XCUI readback; initial/trigger/scheduled/cancel/watchdog/reentrancy/lifecycle Pressure, affected signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BT | `FlowTabUITestRuntimeLogObservationPolicy`, both defaulted `waitForRuntimeLogFiles` overloads, and their marker/regex callers; shared persistent runtime-log evidence terminal bound | Two raw eight-second default arguments are terminal bounds around exact marker or regex evidence. The baseline performs immediate persisted readback, and the observation owner combines file events with a named cancellable 200ms fallback; policy ownership and compatibility are implicit. Retained watchdog classification. | Name one shared eight-second `defaultWatchdog` under the runtime-log observation owner and use it for both marker and regex overloads without changing explicit caller overrides. Preserve exact baseline-relative contents, file-event generation, all-marker or regex success predicates, and final missing-marker/pattern plus log-tail diagnostics. The invocation owns observer, file-event source, scheduled fallback, and cancellation. | H shared UI-test observation infrastructure used by marker and regex paths; policy compatibility, baseline/file-event/reset, exact marker/regex, event ordering, cancel/watchdog, lifecycle and stale-callback Pressure, representative signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BU | `openSettingsTab(in:)`; shared exact Settings-tab navigation | The helper waits for an exact hittable Settings button, taps it, and only then installs a separate exact-content existence wait. Both conditions already define success and use a named watchdog, while post-trigger observer installation leaves a scheduling race and the two failure diagnostics are split. Evidence migration plus retained watchdog. | Resolve the exact Settings content before the trigger and route the helper through `tapFirstHittableAndWaitForExistence`, which installs the generation-owned content observer before waiting for and tapping the exact Settings button. Accept an initial or later exact content-existence readback through the shared named serial 100ms XCUI cadence. Reuse the named six-second tab-navigation watchdog solely as each exact condition's terminal bound. The helper invocation owns observation generation, cancellation, and combined final trigger/content diagnostics. | H shared Settings navigation helper; support policy, exact candidate, initial/scheduled/cancel/watchdog/reentrancy/lifecycle Pressure, representative Settings and permission signed UI paths, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BV | `testEnglishPrimarySurfacesExposeUsableLayoutAnchors`; Search trigger receipt and completion evidence channel | The test enables the distributed trigger listener and selects INFO runtime logging, then requires persisted receipt and presentation-completion markers through `postFlowTabUITestSwitcherTriggerAndWaitForDelivery`. Its launch configuration omits the verbose-log switch that emits those markers, so the watchdog can expire with only the seeded baseline line even when the trigger is delivered. The same path requests Search under an AX-denied mock topology, which cannot produce the complete first committed-index generation required by the existing Search contract. Evidence-channel readiness and valid fixture topology. | Reuse `FlowTabUITestSearchInputReadinessPolicy.applicationEvidenceLaunchArguments` so the exact Search path establishes its INFO plus verbose persisted-log channel at launch. Give the mock Search phase AX-backed window coverage while retaining a denied Screen Recording permission surface. Preserve the helper's pre-trigger snapshot, preinstalled owner, exact notification-name receipt/completion markers, initial readback, file-event generations, named cancellable fallback, and final diagnostic readback. The test invocation owns App termination, owner cancellation, and log cleanup; watchdogs remain terminal failure bounds. | H signed English Search path and shared persistent-log evidence; launch-policy regression, receipt/completion observation rules and lifecycle Pressure, exact English Settings/Logs/Search UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BW | `SwitcherTriggerNotificationObserver`, `presentSearchHotkeySessionForTesting`, and `FlowTabUITestSwitcherTriggerDeliveryObservationOwner`; deferred Search trigger presentation completion | `presentSearchHotkeySessionForTesting` returns success when Search activation is pending behind the committed-index freshness barrier, and the trigger observer immediately persists `presented=1`. The UI helper can therefore treat accepted deferred work as completed presentation. Evidence migration. | Establish a Search activation observer before invoking the trigger, capture an initial readback and baseline generation, and publish completion only after exact panel presentation plus active Search state is observed. Accept immediate activation or the later committed-index-driven state transition; reject stale, duplicate, cancelled, and out-of-order observations. The trigger observer owns replacement, uninstall, and deinit cancellation. Keep the UI-side completion watchdog solely as a failure bound with pending state, generation, and final readback diagnostics. | H shared post-launch Search trigger and TestingSupport lifecycle; deterministic immediate/deferred/cancel/duplicate/out-of-order/watchdog tests, affected signed Search UI, repeated trigger Pressure, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BX | `testSettingsPermissionRequestsConvergeFromTCCReadbackEvidence`; initial denied Settings permission surface | Four independent raw five-second element waits are installed after Settings navigation and use elapsed wait opportunities to infer that both denied-state labels and both permission actions have become available. The exact persistent element projection is the success Oracle, while observation ordering, one atomic snapshot, policy ownership, cancellation, and final evidence are implicit. Evidence migration plus retained watchdog. | Resolve all four exact XCUI elements and establish one generation-owned snapshot observer before Settings navigation. Perform an immediate readback, request a post-navigation readback, and accept only one snapshot in which both denied-state labels and both exact actions exist together. Retain the named serial 100ms XCUI cadence because Accessibility exposes no projection callback. A named five-second watchdog remains solely a terminal failure bound and reports generation, source, and every final element state. Resolution, expiry, explicit teardown, and deinit own schedule cancellation; the test invocation owns the observer, App, and temporary permission-state path. The later granted-state transition remains an independent contract. | H Settings permission UI readiness and repeated XCUI readback; policy/snapshot rules, shared initial/scheduled/cancel/watchdog/lifecycle Pressure, affected signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BY | `testSettingsPermissionRequestsConvergeFromTCCReadbackEvidence`; post-request granted Settings permission projection | Two raw six-second element waits begin after both permission actions and the atomic simulated-TCC write, using later wait opportunities to infer that both granted-state labels have converged. Persistent exact labels define success, while a pre-trigger baseline, one atomic projection, cancellation ownership, and final evidence are implicit. Evidence migration plus retained watchdog. | Resolve both exact granted-state labels and start one generation-owned snapshot observer before the first permission action. Require the initial readback to remain unsatisfied, then perform both actions and the atomic permission-state write before requesting an immediate trigger readback. Accept only a later snapshot in which both exact granted-state labels exist together. Reuse the named serial 100ms XCUI cadence because Accessibility exposes no projection callback and the shared six-second `permissionStateProjection` watchdog solely as a terminal failure bound with generation, source, and both final states. Resolution, expiry, explicit teardown, and deinit own schedule cancellation; the test owns observer, App, and exact temporary path. Persisted permission-log evidence remains an independent contract. | H Settings simulated-TCC transition and repeated XCUI readback; atomic predicate/baseline, shared trigger/scheduled/cancel/watchdog/lifecycle Pressure, affected signed UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036BZ | `testSettingsCommandTabTakeoverTriggersSwitcherAndRestoresSystemShortcut`; active Command+Tab takeover status projection | A raw five-second element wait begins after hotkey selections, shared-defaults marker convergence, and runtime-log convergence, then uses later XCUI wait opportunities to infer that the exact active-takeover status is visible. The persistent exact status predicate defines success, while pre-trigger observation, an unsatisfied baseline, lifecycle ownership, and final readback diagnostics are implicit. Evidence migration plus retained watchdog. | Resolve the exact identifier plus active localized label/value predicate and establish a generation-owned observer before the modifier/key selections. Require an unsatisfied initial readback, then preserve the marker and runtime-log Oracles before requesting a trigger readback. Accept only a later exact status-existence readback. Retain the named serial 100ms XCUI cadence because Accessibility supplies no projection callback. A named five-second status watchdog remains solely a terminal failure bound and reports generation, source, final existence, and wait result. Resolution, expiry, explicit teardown, and deinit cancel the schedule; the test owns the observer and App. Trigger-log delivery, marker restoration, and process exit remain independent contracts. | H system-shortcut Settings projection and repeated XCUI readback; policy/baseline, shared trigger/scheduled/cancel/watchdog/lifecycle Pressure, affected signed takeover UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CA | `testSettingsCommandTabTakeoverTriggersSwitcherAndRestoresSystemShortcut`; Command+Tab takeover configuration log publication | A raw ten-second persistent-log wait is created after the modifier/key selections and treats later observation opportunities as the way to discover four exact configuration markers. The pre-trigger file-offset snapshot already provides a durable baseline, while owner installation, a named watchdog, explicit lifecycle, and final diagnostics remain post-trigger or implicit. Evidence migration plus retained watchdog. | Capture the runtime-log baseline and start a `FlowTabUITestRuntimeLogObservationOwner` for all four exact configuration markers before either takeover selection. Require an unsatisfied initial baseline-relative readback, then preserve the selection/value triggers and wait on the preinstalled owner. File events plus the named cancellable 200ms fallback wake readbacks; only all exact post-baseline markers satisfy the Oracle. A named ten-second configuration-log watchdog remains solely a terminal failure bound with missing markers, event generations, character count, and log tail. The test invocation owns owner and baseline cancellation. Active status, trigger delivery, marker restoration, and process exit remain independent contracts. | H system-shortcut configuration evidence channel; policy/initial/event/reset/cancel/watchdog/lifecycle Pressure, affected signed takeover UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CB | `testSettingsCommandTabTakeoverTriggersSwitcherAndRestoresSystemShortcut`; active Command+Tab key-delivery log publication | A separate raw ten-second persistent-log wait is created after synthesizing Command+Tab and uses later observation opportunities to discover the exact hotkey-press and Forward-effect markers. Its file-offset snapshot precedes the key event, while owner installation, named policy, explicit cancellation, and final evidence are post-trigger or implicit. Evidence migration plus retained watchdog. | Capture a new runtime-log baseline and start a dedicated observation owner for the two exact trigger markers before activating the App and synthesizing Command+Tab. Require an unsatisfied baseline-relative initial readback, then wait on the preinstalled owner after the key event. File events plus the named cancellable 200ms fallback wake readbacks; both exact post-baseline markers alone satisfy the Oracle. A named ten-second trigger-log watchdog remains solely a terminal failure bound with missing markers, event generations, character count, and log tail. The test owns owner and baseline cancellation. Configuration publication, active status, restored marker, and process exit remain independent contracts. | H real system-shortcut delivery evidence channel; policy/initial/event/reset/cancel/watchdog/lifecycle Pressure, affected signed takeover UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CC | `testSettingsCommandTabTakeoverTriggersSwitcherAndRestoresSystemShortcut`; graceful Command-Q application exit before takeover restoration readback | A raw eight-second application-state wait is started after Command-Q and uses its later observation opportunity to discover `.notRunning`. Exact persistent `XCUIApplication.state` is already the success Oracle, while a pre-trigger baseline, named cadence/watchdog, explicit cancellation, and final evidence are implicit. Evidence migration plus retained watchdog. | Start a generation-owned condition observer for exact application state before App activation and Command-Q synthesis. Require an immediate non-`.notRunning` baseline, request an immediate post-trigger readback, and accept only `.notRunning`; retain the shared named serial 100ms XCUI cadence because XCTest exposes no termination notification for this target. A named eight-second graceful-termination watchdog remains solely a terminal failure bound and reports generation, source, final state, and wait result. Resolution, expiry, explicit teardown, and deinit cancel the schedule; the trigger invocation owns the observer. Pending-restore marker convergence and later cleanup remain independent contracts. | H real system-shortcut process lifecycle; policy/baseline/trigger/scheduled/cancel/watchdog/lifecycle Pressure, affected signed takeover UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CD | `testSettingsCommandTabTakeoverTriggersSwitcherAndRestoresSystemShortcut` deferred App cleanup and the shared application-termination evidence helper | Two raw six-second waits attempt graceful Command-Q cleanup and then forced `terminate()` cleanup. The first waiter Boolean selects escalation and the second is discarded, so fixed bounds, final result, strategy ownership, and failure diagnostics are implicit. Evidence migration plus retained watchdogs. | Generalize the shared termination evidence helper around a caller-owned termination trigger while preserving its immediate initial/post-trigger/final state readbacks and exact XCTest `.notRunning` wait. Compose one cleanup evidence contract: attempt the graceful trigger with its named six-second upper bound, escalate only when its final state remains running, then require forced termination's exact final `.notRunning` readback under a separate named six-second failure watchdog. Initial `.notRunning` completes without either trigger. Final success depends only on exact process state; diagnostics retain both phases. The deferred test invocation owns the synchronous waits and final assertion. | H shared UI-test process lifecycle and fallback cleanup; deterministic initial/graceful/escalated/stuck/pass-through diagnostics, 100-lifecycle Pressure, affected signed takeover UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CE | `resetHotkeyDefaultsAfterCommandTabTakeoverTest`; default-reset App process exit | A raw six-second `.notRunning` wait follows `terminate()`, and its Boolean is discarded. Exact persistent application state is the available success Oracle, while the compatible bound, initial/post-trigger/final evidence, and failure diagnostics are implicit. Evidence migration plus retained watchdog. | Delegate to the shared termination evidence helper with a named six-second default-reset exit watchdog. Preserve its initial readback before `terminate()`, immediate post-trigger readback, conditional exact XCTest state wait, and final `.notRunning` readback. Assert only the final evidence and report target plus initial, post-trigger, waiter, and final states. The reset helper invocation owns the synchronous wait; no recurring work survives it. | M UI-test cleanup process lifecycle; policy compatibility, existing deterministic initial/immediate/delayed/boundary/watchdog/repeated-lifecycle termination suite, affected signed takeover UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CF | `testSpaceFixtureAppShowsConfiguredWindowTitles` and `testSpaceFixtureAppMarksFullscreenTargetWindowBeforeTransition`; Space Fixture window-mode accessibility publication | Two raw five-second existence waits are created after fixture launch and use later XCUI observation opportunities as proof that the configured standard-window and fullscreen-target markers were published. The second path reads the target label only after existence succeeds. Exact accessibility identifier plus exact mode label are the available persistent evidence, while the observer baseline, atomic predicate, named cadence/watchdog, cancellation, and final diagnostics are implicit. Evidence migration plus retained watchdog. | Resolve the exact marker query and start a generation-owned observation before fixture launch. Require the initial marker readback to be absent, then accept only a snapshot whose exact identifier exists with `Standard Window` or `Fullscreen Target` as appropriate. Request an immediate trigger readback after foreground/readiness and retain the shared named serial 100ms XCUI cadence because XCTest exposes no accessibility-publication callback. A named five-second publication watchdog remains solely a terminal failure bound and reports expected identifier/label, generation, source, final existence/value, and waiter result. Resolution, expiry, explicit teardown, and deinit cancel the schedule; each fixture test owns its observer and exact fixture-process cleanup. Workflow readiness remains an independent Oracle. | H real Space Fixture accessibility publication; policy/initial/exact-label/trigger/cancel/stale-generation/watchdog/lifecycle Pressure, both affected signed real-fixture UI paths, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CG | `assertSpaceFixtureWorkflowPermissionsAvailable`; real FlowTab permission preflight projection | The helper waits two seconds for the missing-permission action and treats its continued absence as proof that Accessibility and Screen Recording are granted. When the action appears, two later five-second waits independently inspect action hittability and dismiss-button existence. This absence-over-time success inference can change with projection scheduling, while the always-visible sidebar already exposes both exact permission statuses. Evidence migration plus retained watchdog. | Publish each sidebar permission status through its own explicit AppKit accessibility element and stable identifier. Resolve the exact card, both named rows, both named localized status elements, permission banner, action candidates, and dismiss action before the launch/navigation trigger. Start one generation-owned atomic projection observer, perform an immediate readback, request a trigger readback after exact App readiness and Home navigation, and accept only a complete projection. `granted` requires both status elements to have exact granted labels; `missing` requires two exact terminal labels with at least one missing plus the exact banner, one hittable settings action, and dismiss action. Either terminal projection resolves immediately; only `granted` returns success. Retain the shared named serial 100ms XCUI cadence because Accessibility exposes no projection callback and the shared six-second permission-state watchdog solely as a failure upper bound with App state and every final element/status readback. Resolution, expiry, explicit teardown, and deinit cancel the schedule; the helper owns it, while callers retain App/process ownership. Foreground readiness and Home navigation remain independent contracts. | H real TCC-dependent UI preflight and repeated XCUI readback; exact English/Chinese terminal rules, initial/trigger/scheduled/missing/cancel/stale-generation/watchdog/lifecycle Pressure, affected signed real-permission UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CH | `testSettingsSearchWindowScopeUnavailableWithoutAccessibilityPermission`; Settings Search app-only scope projection | The UI test taps the disabled scope control and treats the Window option's continued absence for one second as proof that missing Accessibility permission removed Window scope. Absence over an arbitrary interval cannot distinguish a correct app-only projection from a menu that has not projected. Evidence migration plus retained watchdog. | Give the existing AppKit permission-summary label a stable accessibility identifier. Resolve the exact scope control, summary, and Window-option query before Settings navigation; start one generation-owned observer and perform an immediate readback before the trigger. Accept only one atomic snapshot where the scope value is `app`, the control is disabled, the exact localized Accessibility-required summary is published, and the Window option is absent. Request a trigger readback after exact Settings navigation and retain the shared named serial 100ms XCUI cadence because XCTest exposes no projection callback. A named six-second projection watchdog is solely a terminal failure bound and reports generation, source, control existence/value/enabled state, summary existence/text, Window-option existence, and waiter result. Resolution, expiry, explicit teardown, and deinit cancel the schedule; the assertion helper owns it. App readiness and Settings navigation remain independent contracts. | M UI-only permission-dependent settings projection; exact English/Chinese rules, initial/trigger/scheduled/cancel/stale-generation/watchdog/lifecycle Pressure, affected signed mock-permission UI, Process/Tooling. | completed |
| SYNC-036CI | `testStatusItemReopensLastSelectedTabAfterWindowClose`; exact main-window close before status-item reopen | After Command-W, the test waits two seconds for Logs content to exist and treats the waiter's false result as proof that the main window closed while the process remained active. A transiently visible element fails immediately, while an unprojected element can pass without exact window identity evidence. Evidence migration plus retained watchdog. | Resolve the exact fixed `FlowTab` window title published by XCTest and the exact Logs content before Command-W, start one generation-owned observer, and require an exact initial open-Logs snapshot. After the trigger, request an immediate readback and accept only one atomic snapshot where the App is still foreground/background running and both the exact titled window and Logs content are absent. Retain the shared named serial 100ms XCUI cadence because XCTest exposes no target-window close callback. A named six-second close watchdog is solely a terminal failure bound and reports generation, source, App state/running classification, exact window existence, Logs-content existence, and waiter result. Resolution, expiry, explicit teardown, and deinit cancel the schedule; the assertion helper owns it. Finder activation, status-item reopen, last-tab restoration, and accessory-policy restoration remain independent contracts. | H AppKit window identity and status-item-visible workflow; open/closed/partial/initial/trigger/scheduled/cancel/stale-generation/watchdog/lifecycle Pressure, affected signed status-item UI, Process/Tooling. | completed |
| SYNC-036CJ | `testStatusItemReopensLastSelectedTabAfterWindowClose`; exact main-window and last-selected Logs projection after status-item activation | An eight-second Logs-content existence wait starts after the status-item trigger and frontmost-App observation. It can discover the persistent content eventually, but it has no pre-trigger closed baseline, exact window identity, trigger-return gate, named policy owner, or atomic final diagnostics. Evidence migration plus retained watchdog. | Resolve XCTest's exact fixed-title `FlowTab` window and exact Logs content before the status-item tap. After Finder activation, start one generation-owned observer, require an exact closed projection, and gate acceptance until the trigger closure returns; then perform an immediate trigger readback. Accept only an atomic snapshot where the App remains foreground/background running and both the exact window and Logs content exist. Retain the shared named serial 100ms XCUI cadence because XCTest exposes no target-window publication callback. A named eight-second reopen watchdog remains solely a failure upper bound and reports generation, source, acceptance gate, App state/running classification, exact window existence, Logs-content existence, and waiter result. Resolution, expiry, explicit teardown, and deinit cancel the schedule; the assertion helper owns it. Exact NSWorkspace frontmost identity, last-tab semantics, and regular-to-accessory policy logs remain independent contracts. | H status-item activation, AppKit window identity, and repeated XCUI readback; closed/open/partial/initial/gated-trigger/cancel/stale-generation/watchdog/lifecycle Pressure, affected signed status-item UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CK | `testStatusItemReopensLastSelectedTabAfterWindowClose`; temporary regular-to-stable accessory activation-policy publication | A baseline freezes log offsets and file-event generation before the status-item tap, but the generic marker owner is attached only after the trigger and is given a raw eight-second bound. Persistent readback prevents lost content, while concrete owner installation, exact marker order, named policy, and baseline cleanup remain implicit. Evidence migration plus retained watchdog. | Create a status-item activation-policy owner from the pre-trigger runtime-log baseline and install it before the tap. Require an unsatisfied initial readback, then accept only post-baseline contents containing the exact temporary-regular marker followed later by the exact stable-accessory marker. Request an immediate trigger-return readback; retain baseline file events plus the named cancellable 200ms fallback because the App log transport has no cross-process completion callback. A named eight-second policy-transition watchdog remains solely a failure upper bound and reports the first unmatched ordered marker plus baseline/current event generation and final log tail. Resolution, expiry, explicit teardown, and deinit cancel the owner; the test invocation explicitly cancels the baseline file source. Exact window reopen and NSWorkspace frontmost identity remain independent contracts. | H activation-policy lifecycle and cross-process log evidence; ordered/reversed/partial/initial/trigger/event/cancel/stale-generation/watchdog/lifecycle Pressure, affected signed status-item UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CL | `testStatusItemSecondaryClickMenuQuitsApp`; application exit after exact status-menu Quit activation | A raw eight-second `.notRunning` wait begins after the Quit item tap. Exact persistent XCTest process state is the success Oracle, while the pre-trigger initial state, immediate post-trigger state, named policy owner, final boundary readback, and complete failure evidence are implicit. Evidence migration plus retained watchdog. | Route the exact Quit-item tap through the shared application-termination evidence helper. Read the initial App state before the trigger, tap the already resolved exact menu item, perform an immediate post-trigger readback, conditionally invoke XCTest's exact `.notRunning` state wait, and always perform a final state readback. Success depends only on final `.notRunning`; the waiter result remains diagnostic. A named eight-second status-menu-quit watchdog is solely a terminal failure upper bound and reports target, initial state, trigger request, post-trigger state, waiter result, and final state. The synchronous test invocation owns the trigger and wait; no recurring work survives it. | H user-visible process lifecycle; named policy plus initial/immediate/delayed/boundary/watchdog/100-lifecycle shared regressions, affected signed status-menu UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CM | `testHomePermissionBannerHiddenWhenPermissionsGranted`; granted-permission Home projection after exact Home navigation | A two-second hittability wait treats the permission action remaining undiscovered for the full interval as proof that the permission banner is hidden, then reads banner absence separately. The negative result is elapsed-time evidence and the two UI elements may come from different projections. | Resolve Home content, the exact permission banner, and the exact permission action before navigation. Start one generation-owned atomic projection observer before tapping Home and gate acceptance until the navigation trigger returns. Perform an immediate trigger readback, then accept only a snapshot where the App is running, Home content is present, and both permission elements are absent. XCTest exposes no projection callback, so retain the shared named cancellable 100ms XCUI readback cadence. The shared named two-second hidden-projection watchdog remains solely a failure upper bound and reports trigger gate, App state, content existence, banner existence, action existence, generation, source, and waiter result. Resolution, expiry, explicit teardown, and deinit cancel the schedule; the assertion helper owns it. | H Home permission negative projection and repeated XCUI readback; exact truth-table/policy plus shared initial/scheduled/cancel/watchdog/reentrancy/lifecycle Pressure, affected signed Home UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CN | `testPermissionReminderTogglePersistsAcrossRelaunch`; hidden Home permission projection after persisted reminder disablement | After the Settings reminder toggle is disabled and the App is relaunched with both permissions still denied, a two-second hittability wait treats the permission action remaining undiscovered for the full interval as proof that the persisted preference hid the banner. The observer begins after relaunch and navigation, so the negative result is elapsed-time evidence without an atomic Home projection. | Generalize the granted-path observer as the shared hidden-permission-projection owner. Resolve Home content, the exact banner, and the exact permission action and start observation before the relaunch/navigation trigger. The initial readback records exact App state and avoids UI queries while it is not running. Gate acceptance until the complete trigger closure returns, perform an immediate readback, and accept only one atomic running-App snapshot with Home content present and both permission elements absent. Retain the shared named cancellable 100ms XCUI cadence because XCTest exposes no projection callback. Reuse the compatible named two-second hidden-projection watchdog solely as a failure upper bound with scenario, trigger gate, final App/UI projection, generation, source, and waiter diagnostics. The assertion invocation owns cancellation and the test owns both App lifecycles. | H persisted preference plus process/UI projection boundary; shared truth-table/policy and initial/scheduled/cancel/watchdog/reentrancy/lifecycle Pressure, both affected signed Home paths, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CO | `testPermissionDismissPersistsAcrossRelaunch`; immediate hidden Home permission projection after exact Dismiss activation | After the exact Dismiss control is tapped, a two-second hittability wait treats the permission action remaining undiscovered for the full interval as proof that the current Home banner was removed. Observation begins after the trigger and the success condition omits Home content, exact banner state, Dismiss state, and App state. | Resolve the shared hidden-permission projection before tapping Dismiss. Require an initial running-App Home snapshot with the exact Open Settings and Dismiss controls present; retain the banner container as diagnostic evidence while the exact control pair supplies the stable visible baseline. Register the observation before the exact Dismiss trigger, defer scheduled XCUI readbacks until that trigger returns, then perform an immediate trigger readback. Accept only an atomic running-App snapshot with Home content present and banner, action, and Dismiss controls absent. Reuse the shared named 100ms cancellable XCUI cadence and two-second hidden-projection watchdog; the helper reports scenario, trigger gate, App state, exact UI projection, generation, source, and waiter evidence and owns cancellation. The separate relaunch persistence assertion remains a later slice. | H current-process permission-dismiss transition; shared policy/state rules and initial/scheduled/cancel/watchdog/reentrancy/lifecycle Pressure, affected signed Dismiss UI, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CP | `testPermissionDismissPersistsAcrossRelaunch`; hidden Home permission projection after relaunch with a persisted Dismiss choice | After relaunch and Home navigation, a two-second hittability wait treats the permission action remaining undiscovered for the full interval as proof that the Dismiss choice persisted. Observation begins after both triggers and success omits exact App, Home-content, banner, and Dismiss evidence. | Resolve the shared hidden-permission projection and install its registration before relaunch. Record an immediate `.notRunning` baseline without querying unavailable UI; use relaunch plus exact Home navigation as the trigger, gate acceptance until it returns, and perform an immediate trigger readback. Accept only one atomic running-App snapshot with Home content present and the banner, Open Settings, and Dismiss elements absent. Retain the shared named cancellable 100ms XCUI cadence because XCTest exposes no projection callback and the shared two-second hidden-projection watchdog solely as a failure upper bound with scenario, trigger gate, App state, exact UI projection, generation, source, and waiter diagnostics. The assertion invocation owns cancellation and the test owns both App lifecycles. | H persisted default plus process/UI projection boundary; shared state rules and observation lifecycle regressions, all affected signed Home permission paths, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CQ | `testLogsPageShowsSeededLogsAndClearRemovesOutput`; DEBUG-to-WARN Logs projection after exact dropdown selection | After selecting WARN, two independent three-second nonexistence waits and two five-second existence waits sample debug, info, warning, and error rows separately. The selected level is read by another observer, so success can combine different UI projections and has no pre-trigger atomic baseline. | Extend the shared Logs projection evidence with the exact selected level. Before dropdown selection, resolve an atomic DEBUG baseline beginning with an immediate readback and requiring the Logs tab/container plus all four exact seeded identifiers. Install a new target generation, defer scheduled XCUI readbacks until the selection trigger returns, request an immediate trigger readback, and accept only one atomic WARN snapshot with the exact warning/error identifier multiset and debug/info prohibited. Retain the shared named cancellable 100ms cadence because XCTest exposes no accessibility-projection callback and the named eight-second exact-projection watchdog solely as a terminal failure upper bound with target, gate, level, multiplicities, generation, source, and waiter evidence. The assertion invocation owns cancellation. | H shared Logs accessibility projection and dropdown transition; selected-level/exact-row/partial/prohibited/multiplicity/initial/trigger/cancel/watchdog rules, 100-lifecycle Pressure, affected dynamic filter and four launch-level signed UI paths, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CR | `testLogsPageShowsSeededLogsAndClearRemovesOutput`; diagnostic-session inactive-to-active-to-inactive projection round trip | Toggle discovery and the post-activation/post-deactivation assertions begin independently and after their respective triggers. Three five-second waits can therefore combine switch and status evidence from different projections, and the deactivation path treats elapsed predicate waiting as its only observation owner. | Use one diagnostic-session projection owner for both directions. Resolve an atomic initial projection before each trigger, then install the target generation before the exact toggle action, defer scheduled XCUI readbacks until the action returns, and request an immediate trigger readback. Require a foreground App, Logs content, exact toggle existence/value, exact status existence, and a nonempty active status label in one snapshot. Retain the shared named cancellable 100ms XCUI cadence because XCTest exposes no accessibility-projection callback and a named five-second watchdog solely as the failure upper bound with target, gate, App state, toggle/status projection, generation, source, and waiter evidence. The assertion invocation owns cancellation; the existing 15-minute diagnostic-session duration remains the product contract owned by the production deadline coordinator and its injected clock/scheduler tests. | H persisted diagnostic state plus SwiftUI accessibility projection; active/inactive/partial/initial/trigger/cancel/watchdog rules, 100-lifecycle Pressure, affected signed Logs round trip, exact process cleanup, Process/Tooling. | completed |
| SYNC-036CS | `testLogsPageShowsSeededLogsAndClearRemovesOutput`; current-process clear-task completion and empty Logs projection | A five-second hittability wait discovers the Clear action, then a separate five-second existence wait treats the empty hint as proof that asynchronous file deletion, line publication, and button re-enablement completed. Observation begins after the trigger and can combine UI states from different projections. | Resolve one atomic populated WARN baseline with a foreground App, Logs content, an enabled exact Clear control, lines present, empty hint absent, and the exact warn/error seeded-row multiset. Install the empty target generation before tapping the already-proven Clear control, defer scheduled XCUI readbacks until the tap returns, and request an immediate trigger readback. Accept only one atomic projection with the same exact WARN level, Clear re-enabled, lines container absent, empty hint present, and no seeded rows. Retain the shared named cancellable 100ms cadence because XCTest exposes no SwiftUI accessibility-projection callback and a named five-second watchdog solely as a terminal failure bound with target, gate, App state, control state, level, row multiplicities, generation, source, and waiter evidence. The assertion invocation owns cancellation. Production `clearAndWait()` remains the file-deletion completion source; the test performs its existing direct fingerprint readback only after the UI target resolves. Relaunch persistence remains a separate lifecycle slice. | H async clear Task plus current-process accessibility projection; populated/empty/partial/level/control/initial/trigger/cancel/watchdog rules, 100-lifecycle Pressure, affected signed Logs clear path, exact disk readback and process cleanup, Process/Tooling. | completed |
| SYNC-036CT | `testLogsPageShowsSeededLogsAndClearRemovesOutput`; persisted clear result after exact source termination and relaunch | After relaunch and Logs navigation, four independent three-second nonexistence waits treat each seeded identifier remaining undiscovered for the full predicate interval as proof that clearing persisted. The waits begin after launch, can sample different projections, and do not prove the Logs content and selected level are ready. | Resolve the source App's exact `.notRunning` termination evidence, then install the relaunched App target generation before launch. Require an initial `.notRunning` readback with no XCUI queries, defer the named schedule until launch plus exact Logs navigation returns, and request an immediate trigger readback. Accept only one foreground projection with Logs content, exact Clear control present and hittable, exact DEBUG level, exactly one loaded content branch (lines or empty hint), and an empty seeded-row identity multiset; unrelated logs created by the new process remain allowed. Retain the shared named cancellable 100ms cadence because XCTest exposes no SwiftUI projection callback and a named eight-second relaunch watchdog solely as a terminal failure bound with target, gate, App/control/content/level/row state, generation, source, and waiter evidence. The assertion invocation owns cancellation and the test owns both App lifecycles. Direct disk fingerprint readback remains the independent persistence Oracle after the UI target resolves. | H cross-process persistence and accessibility projection; loaded/empty/partial/inconsistent/level/identity/prelaunch/trigger/cancel/watchdog rules, shared owner Pressure, affected signed clear/relaunch path, exact termination/disk/process evidence, Process/Tooling. | completed |
| SYNC-036CU | `testLogsPageShowsSeededLogsAndClearRemovesOutput`; initial Logs navigation, privacy notice, structured seeded-row projection, and fingerprint readiness | A five-second tab-action wait is followed by separate five/eight-second existence waits for content, privacy notice, lines, and each seeded row. Observation begins after navigation, can combine different accessibility projections, and reads each row value only after its independent existence wait; no single state proves the App, selected level, loaded branch, exact row multiset, redaction contract, and extractable fingerprints are ready together. | Install one generation-owned seeded-Logs projection observer before the exact Logs-tab trigger. Allow an initially matching projection but gate acceptance until the trigger returns, request an immediate trigger readback, and accept only one foreground snapshot with Logs content, privacy notice, exact `DEBUG`, lines present, empty hint absent, the exact four seeded row identities with no duplicates, no cleartext marker, structured-message metadata, and one nonempty fingerprint per row. Return fingerprints from the resolved snapshot, then preserve the direct disk readback as an independent Oracle. Reuse the Logs owner’s named five-second tab-navigation and eight-second exact-projection watchdogs solely as terminal failure bounds. Retain the shared named cancellable 100ms XCUI cadence because XCTest exposes no SwiftUI accessibility-projection callback; the schedule remains dormant until the trigger returns, and the assertion invocation owns cancellation. | H initial async log publication plus repeated accessibility readback; atomic/partial/duplicate/state/level/privacy/redaction/fingerprint/initial/trigger/cancel/watchdog rules, 100-lifecycle Pressure, affected signed Logs workflow, seeded-bootstrap/redaction Behavior, exact disk/process evidence, Process/Tooling. | completed |
| SYNC-036CV | `testLogsPageUpdatesFromRuntimeLogChangeEventWhileVisible`; post-command runtime-log projection | A five-second `waitForExistence` begins after the cross-process command and treats one matching label read at any time as proof that the command was consumed and the visible Logs projection completed. It has no pre-trigger empty baseline, exact multiplicity, loaded-branch consistency, selected-level evidence, or trigger generation. | Resolve one atomic foreground baseline before posting the command: Logs content present, Clear present and hittable, exact `INFO`, lines absent, empty hint present, and zero rows containing the target marker. Install the target generation before the command, gate resolution until the trigger returns, request an immediate trigger readback, and accept only one foreground projection with the same Logs/control/level evidence, lines present, empty hint absent, and exactly one matching marker row. Retain the shared named cancellable 100ms XCUI cadence because XCTest cannot subscribe to the tested App’s internal log-store generation; activate it only if the trigger readback remains unresolved. A named five-second watchdog is solely the terminal failure bound and reports the complete last projection. The assertion invocation owns registration and schedule cancellation. | H cross-process command plus repeated accessibility readback; baseline/target/partial/duplicate/state/level/initial/trigger/cancel/watchdog rules, 100-lifecycle Pressure, affected signed live-update workflow, log-store/ViewModel Behavior, exact process evidence, Process/Tooling. | completed |
| SYNC-036CW | `testLogsPageUpdatesFromRuntimeLogChangeEventWhileVisible`; current-process Clear completion before the runtime-log command | Independent five-second waits discover the seeded INFO row and Clear control, then a third five-second wait treats empty-hint existence as proof that asynchronous file deletion, line publication, and Clear re-enablement completed. Observation begins after the trigger and can combine control, content branch, level, and row evidence from different projections. | Reuse the Logs clear owner to resolve one atomic foreground baseline with Logs content, Clear present and hittable, exact `INFO`, lines present, empty hint absent, and exactly one seeded INFO identity. Install the empty target generation before tapping the already-proven Clear control, defer scheduled XCUI readbacks until the tap returns, and request an immediate trigger readback. Accept only one projection with Clear re-enabled, lines absent, empty hint present, exact `INFO`, and no seeded identities. The existing named cancellable 100ms cadence remains the fallback because XCTest exposes no SwiftUI accessibility-projection callback; the named five-second watchdog remains solely the terminal failure bound with complete final evidence. The assertion invocation owns cancellation. | H async clear Task plus repeated accessibility readback; single-INFO baseline rule, existing atomic/partial/level/control/initial/trigger/cancel/watchdog matrix and 100-lifecycle Pressure, affected signed live-update workflow, clear/store/ViewModel Behavior, exact process evidence, Process/Tooling. | completed |
| SYNC-036CX | `testLogsPageUpdatesFromRuntimeLogChangeEventWhileVisible`; Logs-tab trigger and initial single-INFO projection | A six-second button-existence wait precedes a coordinate-capable tap, then an independent six-second content-existence wait is treated as navigation completion. The exact foreground, Clear-control, selected-level, loaded-branch, and single-row projection is first observed by the following Clear owner, after the navigation trigger and both waits have completed. | Resolve the Logs elements and install the clear-projection generation before the exact tab trigger. Record an initial readback, gate acceptance until a named hittable-tab trigger returns, request an immediate trigger readback, and accept only one foreground projection with Logs content, Clear present and hittable, exact `INFO`, lines present, empty hint absent, and exactly one seeded INFO identity. Reuse the named five-second tab-hittability watchdog and eight-second exact-projection watchdog solely as terminal failure bounds. Retain the shared named cancellable 100ms cadence because XCTest exposes no SwiftUI accessibility-projection callback; keep it dormant until the trigger returns, and let the assertion invocation own cancellation. The following Clear contract independently revalidates the same populated projection before mutation. | H navigation plus initial async log publication and repeated accessibility readback; incomplete/initially-matching/pre-trigger-event/trigger/cancel/watchdog rules, shared owner Pressure, affected signed live-update workflow, seeded-bootstrap/store/ViewModel Behavior, exact process evidence, Process/Tooling. | completed |
| SYNC-036CY | `testLogsTabShowsActionButtons`; Logs navigation and atomic action-control readiness | The shared Logs helper separately waits up to five seconds for a hittable tab and five seconds for content existence. After it returns, two independent five-second existence waits followed by point-in-time hittability reads are treated as proof that Open Directory and Clear Logs are ready together. These observations can come from different SwiftUI accessibility projections, and the Clear control can transition through disabled state. | Resolve the Logs content and both exact buttons, then install one generation-owned action-projection observer before the Logs-tab trigger. Record an initial readback, gate acceptance until the named hittable-tab trigger returns, request an immediate trigger readback, and accept only one foreground projection with Logs content plus both controls present and hittable. Reuse the named five-second tab-hittability and eight-second exact-projection watchdogs solely as terminal failure bounds. Retain the shared named cancellable 100ms cadence because XCTest exposes no SwiftUI accessibility-projection callback; activate it only after the trigger returns and let the assertion invocation own cancellation. RuntimeLogsSection's hosted-control readback remains the independent Behavior Oracle. | H navigation plus repeated accessibility readback; atomic/partial/background/disabled/initially-matching/pre-trigger-event/trigger/cancel/watchdog rules, 100-lifecycle owner Pressure, affected signed Logs-actions UI, compact-control Behavior, exact process evidence, project-file and Process/Tooling validation. | completed |
| SYNC-036CZ | `testSidebarTabsSwitchContent`; exact Home, Logs, and Settings sidebar-selection projections | Each sidebar action uses a raw five-second exact-button wait followed by a separately established raw five-second target-content existence wait. The first target can already exist, while later observations do not prove in one readback that the requested content is exposed and both non-target contents are hidden. | Before each exact sidebar action, resolve all three content elements and install one target-owned projection generation. Record an initial readback, gate acceptance until the named hittable trigger returns, request an immediate trigger readback, and accept only a foreground projection with the requested content exposed and both non-target contents absent. Reuse the named six-second support tab-navigation watchdog solely as the exact trigger and projection failure bounds. Retain the shared named cancellable 100ms cadence because XCTest exposes no SwiftUI accessibility-projection callback; activate it only when the trigger readback remains unresolved. Each transition invocation owns registration and cancellation. `HomeRootView.tabContainer` accessibility visibility and `HomeTabState` selection remain the independent Behavior Oracles. | H repeated accessibility readback across three sequential transitions; target/partial/background/initially-matching/pre-trigger-event/trigger/cancel/watchdog rules, 100-lifecycle owner Pressure, affected signed sidebar UI, HomeTab selection Behavior, exact process evidence, project-file and Process/Tooling validation. | completed |
| SYNC-036DA | `openLogsTab(in:)`; shared exact Logs navigation for status-item restore, English layout, and post-clear relaunch callers | The helper uses a named exact-button watchdog, taps, then creates a separate named Logs-content wait. Observer installation follows the trigger, and success does not require one foreground readback where Logs is the uniquely exposed sidebar content. | Route the shared helper through the SYNC-036CZ sidebar projection owner. Resolve Home, Logs, and Settings content and establish the Logs target generation before the exact button trigger; gate resolution until the action returns, perform an immediate trigger readback, and accept only a foreground projection with Logs exposed and both non-target contents absent. Retain the named six-second support watchdog solely as trigger/projection failure bounds and the named cancellable 100ms cadence only while the exact projection remains unresolved. The helper invocation owns registration and cancellation; its three existing callers retain App, relaunch, status-item, and seeded-log lifecycle ownership. | H shared UI navigation helper across three caller lifecycles; inherited target/partial/background/initial/trigger/delayed/cancel/watchdog matrix and 100-lifecycle owner Pressure, all three affected signed UI paths, HomeTab selection Behavior, exact process evidence, Process/Tooling validation. | completed |
| SYNC-036DB | `testEnglishPrimarySurfacesExposeUsableLayoutAnchors`; post-`openLogsTab` Logs-content wait | A raw five-second `waitForExistence` repeats the same Logs-content condition immediately after the shared helper has already resolved an exact foreground projection with Logs uniquely exposed. Its elapsed bound contributes no independent readiness or success evidence. | Remove the duplicate wait. Treat successful return from the pre-trigger shared Logs projection owner as the navigation-completion evidence, then keep the immediate localized diagnostic-session text readback as the independent English visible-content Oracle. The shared helper retains its named watchdog, conditional cadence, and cancellation owner; this caller introduces no additional duration or lifecycle. | M removal of redundant UI wait; affected signed English navigation path, static timeout inventory, exact process evidence, Process/Tooling validation. Unit, Behavior, and Pressure not relevant because no production state rule, app orchestration, observer, timer, scheduler, or hot path changes. | completed |
| SYNC-036DC | `openSettingsTab(in:)`; shared exact Settings navigation across permission, appearance, visibility, Search, launch-at-login, and relaunch callers | The helper uses a named exact-button watchdog, taps, and only then starts a separate Settings-content existence wait. Observer installation follows the trigger, and success does not prove in one foreground readback that Settings is uniquely exposed. | Route the shared helper through the SYNC-036CZ sidebar projection owner. Resolve Home, Logs, and Settings content and establish the Settings target generation before the exact button trigger; gate resolution until the action returns, perform an immediate trigger readback, and accept only a foreground projection with Settings exposed and Home and Logs absent. Retain the named six-second support watchdog solely as trigger/projection failure bounds and the named cancellable 100ms cadence only while the exact projection remains unresolved. The helper invocation owns registration and cancellation; callers retain App, permission-state file, relaunch, runtime-log, and domain-resource cleanup. | H shared UI navigation helper across 35 call sites and nested observation/relaunch lifecycles; inherited target/partial/background/initial/trigger/delayed/cancel/watchdog matrix and 100-lifecycle owner Pressure, representative permission, English, no-readiness, and relaunch signed UI paths, HomeTab selection Behavior, exact process evidence, Process/Tooling validation. | completed |
| SYNC-036DD | `testSettingsAppearanceThemeAndLanguageUpdateVisibleUI`; post-`openSettingsTab` Settings-content wait | A raw six-second `waitForExistence` repeats the same Settings-content condition immediately after the shared helper has already resolved an exact foreground projection with Settings uniquely exposed. Its elapsed bound contributes no independent readiness or success evidence. | Remove the duplicate wait. Treat successful return from the pre-trigger shared Settings projection owner as the navigation-completion evidence, then keep the exact Chinese Appearance heading as an independent visible-content Oracle and the existing average-luminance observations as independent light/dark transition evidence. The shared helper retains its named watchdog, conditional cadence, and cancellation owner; this caller introduces no additional duration or lifecycle. | M removal of redundant UI wait; affected signed Settings appearance path, static timeout inventory, exact process evidence, Process/Tooling validation. Unit, Behavior, and Pressure not relevant because no production state rule, app orchestration, observer, timer, scheduler, or hot path changes. | completed |
| SYNC-036DE | `assertInitialDeniedSettingsPermissionProjection`, `testSettingsPermissionRequestsConvergeFromTCCReadbackEvidence`, `testSettingsPermissionActionButtonsAreVisible`, and `testSettingsSimplifiedChinesePermissionStatusesAreVisible`; initial denied Settings permission projection | The convergence path already owns one atomic pre-navigation four-element observation, while the action and localized-status paths each start two independent raw five-second waits after Settings navigation. Those callers can combine evidence from different accessibility projections and install observation after the trigger. | Extract the existing denied-state observation as the shared owner for all three paths. Resolve both exact localized denied statuses and both exact permission actions, start the generation-owned observer before Settings navigation, retain its initial readback, and request an immediate post-trigger readback. Accept only one atomic snapshot containing all four elements. Retain the named cancellable 100ms XCUI cadence because Accessibility exposes no projection callback and the named five-second watchdog solely as a failure upper bound with generation, source, waiter result, and every final field. The helper invocation owns observation cancellation; callers own App and temporary permission-state resources. | H shared UI-test permission owner across three caller paths; existing 16-state policy rule plus shared initial/scheduled/cancel/watchdog/reentrancy and lifecycle Pressure, all three affected signed UI paths, static timeout inventory, exact process evidence, Process/Tooling validation. Unit and Behavior not relevant because production state and app orchestration are unchanged. | completed |
| SYNC-036DF | `FlowTabUITestSettingsPermissionActionProjectionObservationOwner`, `testEnglishPrimarySurfacesExposeUsableLayoutAnchors`, and `testEnglishGrantedPermissionActionsUseManageLabels`; post-language-change permission action projection | Each English path starts two independent raw five-second existence waits after the language-selection trigger, then separately reads hittability, compact height, and, for the granted path, exact labels. Observation follows the trigger and success can combine multiple accessibility projections. | Resolve Settings content and both exact permission actions and install a deferred generation-owned observer before selecting English. Retain the initial readback, gate acceptance until language selection plus exact control-value readback returns, then request an immediate trigger readback. Accept only one foreground Settings snapshot where both actions exist, are hittable, and are no taller than the compact-action contract; the granted path additionally requires both exact English Manage labels in that snapshot. Retain the named cancellable 100ms XCUI cadence only while unresolved because Accessibility exposes no projection callback and a named five-second watchdog solely as the failure upper bound with target, gate, generation, source, labels, hittability, heights, and final waiter evidence. The assertion invocation owns deferred registration and observer cancellation; each test owns its App. | H shared English permission UI owner and repeated accessibility readback; policy and atomic/mismatched-label rules, pre-trigger/initial/delayed/cancel/duplicate/watchdog/lifecycle Pressure, both affected signed UI paths, exact process evidence, project-file and Process/Tooling validation. Unit and Behavior not relevant because production state and app orchestration are unchanged. | completed |
| SYNC-036DG | `FlowTabUITestSettingsAppVisibilityManagerProjectionObservationOwner`, `assertSettingsAppVisibilityManagerProjectionAfterNavigation`, `testEnglishPrimarySurfacesExposeUsableLayoutAnchors`, and `testSettingsAppVisibilityNavigationConvergesFromVisibleState`; exact forward manager page projection | Both page-only callers start a raw five- or six-second Manage-action existence wait, trigger the transition, then start a separate raw six-second manager wait. The English caller adds another raw five-second localized-title wait. Observation follows the action, individual reads can span different accessibility projections, and animation/scheduling load determines which transitional state is sampled. The six inventory-dependent transitions have an additional asynchronous installed-App readiness contract and remain assigned to SYNC-036DH. | Resolve Settings content, exact Manage action, manager container, exact Back action, and any required localized title before navigation. Install a deferred generation-owned observer, accept an already-exact initial manager projection, otherwise gate later evidence until the exact hittable Manage trigger attempt returns and perform an immediate trigger readback. Accept only one foreground Settings snapshot where the root Manage action is absent, the manager exists, the Back action exists and is hittable, and the optional exact English title exists. Retain the named cancellable 100ms XCUI cadence only while the trigger readback remains unresolved because Accessibility exposes no SwiftUI projection callback. A named six-second watchdog remains solely the trigger/projection failure bound with target, generation, source, gate, every final field, candidate count, and waiter result. The helper invocation owns deferred registration and cancellation; each caller owns its App and downstream page interaction. | H shared UI-test page-navigation owner and animation-sensitive accessibility readback; exact/partial/background/optional-title/initial/pre-trigger/delayed/cancel/duplicate/watchdog rules, 100-lifecycle Pressure, both affected signed UI paths, expanded inventory-boundary probe, exact process evidence, project-file and Process/Tooling validation. Unit and Behavior not relevant because production APIs, state transitions, animation policy, and application orchestration are unchanged. | completed |
| SYNC-036DH | `AppVisibilityManagerModel.reload`, `AppVisibilityInventoryReadiness`, the hidden-count readiness marker, and the six inventory-dependent Settings-to-App-Visibility transitions in Home and Settings Search UI coverage; installed-App inventory readiness | The callers wait for the page shell, then interact with Search or filters and start raw row waits. `reload()` completes a detached filesystem/workspace inventory scan independently of the page animation, so page existence and elapsed wait opportunities do not prove completion. The SYNC-036DG expanded probe reached the exact page projection while three Mail-row waits expired after six seconds. DH attempt 001 established that the `LazyVStack` only materializes visible rows in the Accessibility tree: Mail and the current App can remain offscreen after a completed scan, while an immediately visible fixture row resolves. DH attempt 002 established that SwiftUI does not export an accessibility value from the manager's `.contain` group. DH attempt 003 established that native XCTest text entry changed the field display before the SwiftUI query binding; focused Search entry now uses a paste action that commits through the AppKit delegate before exact row readback. Each downstream Search/filter action retains its exact materialized-row Oracle. | Publish the model-owned explicit `idle → loading → ready` transition and bind its stable identifier to the always-materialized hidden-count text without changing its visible or spoken content. Resolve the exact manager and ready marker before invoking the SYNC-036DG page-navigation helper, install a deferred generation-owned observer first, accept an already-exact foreground manager containing the ready marker, otherwise gate later evidence until navigation returns and perform an immediate trigger readback. XCTest receives no callback from the target process's published SwiftUI state, so retain a named cancellable 100ms XCUI cadence only while the exact ready marker remains absent and a named 30-second inventory watchdog solely as the terminal failure bound with target, expected marker identity, App state, manager existence, marker existence, generation, source, and waiter result. The helper invocation owns both observers and cancellation; callers retain App, defaults, Search/filter actions, and their exact row assertions. | H production async state plus six UI transitions; idle/loading/ready marker, initial/gated/delayed/cancel/duplicate/watchdog rules, model event-delivery Unit/Behavior coverage, 100-lifecycle and delayed-readback UI Pressure, all affected signed UI paths, exact process evidence, project-file and Process/Tooling validation. | completed |
| SYNC-036DI | `AppVisibilityManagerModel.updateQuery`, `queryProjectionGeneration`, the generation-bearing list accessibility marker, `FlowTabUITestSettingsAppVisibilityQueryProjectionObservationOwner`, and the four App Visibility query-result transitions in Home and Settings Search UI coverage; committed query projection | Each caller pastes into the focused native Search field and then starts a raw six-second target-row wait. Attempt SYNC-036DH-003 proved that the field can display the expected text before its SwiftUI binding and filtered list projection commit. A target row can also exist in the pre-query virtualized projection, so elapsed time, field text, or row existence alone does not establish the transition. | Make query updates an explicit model transition that advances a monotonic generation only when a different query commits. Project that generation through the list's accessibility identifier without exposing query content or changing visual/spoken behavior. Capture the exact marker baseline and install a deferred generation-owned observer before the paste trigger; gate acceptance until the trigger returns, then perform an immediate readback. Accept only one foreground manager snapshot with the exact field value, a marker under the stable prefix that differs from baseline, and the exact target row. Accessibility exposes no target-process callback, so retain the shared named cancellable 100ms cadence only while unresolved and a named six-second watchdog solely as the terminal failure bound with expected query, prefix, baseline/final marker, target identity, App/manager/field/row state, generation source, gate, and waiter result. The helper invocation owns registration and cancellation; callers own App, defaults, subsequent row action, and downstream assertions. | H production query state plus four UI transitions; changed/duplicate query generation Unit/Behavior coverage, exact/wrong/background/baseline/initial/gated/delayed/cancel/duplicate/watchdog rules, 100-lifecycle and delayed-readback UI Pressure, all four affected signed UI paths, exact process evidence, project-file and Process/Tooling validation. | completed |
| SYNC-036DJ | `AppVisibilityManagerModel.updateFilter`, `filterProjectionGeneration`, the filter-projection accessibility marker and contained filter actions, `FlowTabUITestSettingsAppVisibilityFilterProjectionObservationOwner`, and the two Hidden-filter result transitions in Settings Search UI coverage; committed Hidden filter projection | The helper tries a raw three-second exact-identifier wait, then separate two- and four-second localized button fallbacks before each caller starts a raw six-second target-row wait. Signed UI evidence from SYNC-036DI showed the filter action's child identifier collapsing into the parent SwiftUI group, so language, accessibility materialization, and accumulated wait opportunities decide how the trigger is found. Button discovery, elapsed time, or row existence alone does not prove the `.hidden` filter transition. | Make filter changes an explicit model transition that advances a monotonic generation only for a different filter. Expose filter actions as contained accessibility children and project the non-sensitive exact filter plus generation through a separate stable container identifier. Resolve the exact Hidden action, capture the `.all` marker baseline, and install a deferred generation-owned observer before the trigger. Gate acceptance until the exact action returns and perform an immediate readback; accept only one foreground manager snapshot where the exact action exists and is hittable, the marker has the `.hidden` generation prefix and differs from baseline, and the exact target row exists. Accessibility exposes no target-process callback, so retain the shared named cancellable 100ms cadence only while unresolved and a named six-second watchdog solely as the terminal failure bound with filter prefix, baseline/final marker, action/target identities, App/manager/action/row state, source, gate, and waiter result. The helper invocation owns registration and cancellation; callers own App, defaults, subsequent row action, and downstream assertions. | H production filter state, Accessibility grouping, and two UI transitions; changed/duplicate filter generation Unit/Behavior coverage, exact/wrong/background/baseline/initial/gated/delayed/cancel/duplicate/watchdog rules, 100-lifecycle and delayed-readback UI Pressure, both affected signed UI paths, exact process evidence, project-file and Process/Tooling validation. | completed |
| SYNC-036DK | `AppVisibilityManagerModel.selectApp`, `selectionProjectionGeneration`, the exact selected-App detail accessibility marker, `FlowTabUITestSettingsAppVisibilityDetailProjectionObservationOwner`, and the five App Visibility row-to-detail-toggle paths in Home and Settings Search UI coverage; selected-App detail readiness | Each caller taps an exact App row and then independently waits up to three seconds for the first Switch, followed by another raw three-second wait for the first CheckBox. The element-type fallback does not prove that the detail pane belongs to the requested App, ignores the existing stable toggle identifier, and begins observation after selection. Query and filter transitions can also select the target before the row tap, so animation, Accessibility materialization, and accumulated fallback opportunities determine which detail projection is sampled. | Route explicit and automatic selection through a model transition that advances a monotonic generation only when the selected App identity changes. Project the exact encoded App identity and generation through a contained detail marker while preserving the stable child toggle identity and visual/spoken behavior. Resolve the exact row, marker query, and toggle before selection; install a deferred generation-owned observer and perform an initial readback. Accept an already-exact target detail immediately. Otherwise gate later evidence until the exact hittable row trigger returns and perform an immediate readback. Accept only one foreground manager snapshot containing the exact target row, target-specific detail marker, and exact hittable show-toggle. Accessibility exposes no target-process callback, so retain the shared named cancellable 100ms cadence only while unresolved and a named six-second watchdog solely as the terminal failure bound with target row and marker identities, baseline/final marker, App/manager/row/toggle state, source, gate, and waiter result. The helper invocation owns registration and cancellation; callers own App, defaults, toggle mutation/readback, and downstream assertions. | H production selection state, Accessibility grouping, and five UI transitions across two caller owners; changed/duplicate/automatic selection Unit/Behavior coverage, exact/wrong/background/initial/gated/delayed/cancel/duplicate/watchdog rules, 100-lifecycle and delayed-readback UI Pressure, all five affected signed UI paths, exact process evidence, project-file and Process/Tooling validation. | completed |
| SYNC-036DL | `FlowTabUITestSwitcherAppProjectionObservationOwner`, `testSettingsAppVisibilityHidesMockAppFromSwitcherAndSearch`, and the post-preference-relaunch Switcher application projection; exact visible Browser and excluded Mail state | After the settings process terminates and a new mock-runtime process launches with the Switcher open, a raw eight-second Browser-row wait is followed by a separate two-second Mail-row nonexistence wait. Observation begins after launch and combines positive and negative evidence from separate Accessibility snapshots. Two signed DK runs reproduced three approximately 31-second snapshot evaluations at the Browser query after the exact Settings detail transition had completed. | Resolve the exact diagnostics summary and application-row evidence before launch, then install one generation-owned projection observer before the relaunch trigger. Retain an initial readback, gate acceptance until launch returns, and request an immediate trigger readback. Accept only one foreground atomic projection whose exact App identities contain Browser and exclude Mail, then retain exact row presence and absence as independent readbacks. Prefer the Switcher's committed diagnostics projection over scanning the lazy row hierarchy. Retain a named cancellable XCUI cadence only while unresolved and a named watchdog solely as the terminal failure bound with App state, summary identity/value, required and excluded App identities, exact row evidence, source, gate, and final waiter state. The relaunch phase owns registration, cancellation, and process cleanup. | H persisted preference handoff, relaunch, positive and negative Accessibility evidence, and preview-load pressure; atomic/partial/background/initial/gated/delayed/cancel/duplicate/watchdog rules, lifecycle Pressure, affected signed Settings-to-Switcher path, exact process and sample evidence, Process/Tooling. | completed |
| SYNC-036DM | `FlowTabUITestBootstrapper.presentInitialUIIfNeeded`, `prepareInitialSearchActivationIfNeeded`, `FlowTabUITestInitialPresentationResolutionTransport`, and `testSettingsSearchDisabledPreventsAutoSearchLaunchEntry`; persisted disabled-Search initial command projection | After persisting `searchEnabled=false`, relaunch uses a raw ten-second application-readiness bound and an eight-second Mail-row wait as the settle point before one Search-input absence sample. TestingSupport derives Search activation solely from the launch argument, so it installs an activation observer even while the persisted feature state prohibits activation; the UI assertion can complete before the unrelated 30-second activation watchdog reports failure and cancels the presented session. | Capture one immutable initial-Search decision from launch intent plus the persisted feature readback before installing presentation observers. For disabled Search, retain the generation-owned initial global-presentation observer and its exact notification/readback Oracle, omit the impossible Search-activation owner, publish resolution with `searchIsActiveOrPending=false`, and keep the App-cycle session presented. Before relaunch, the UI caller owns a unique absolute readback path, removes any initial file, and installs a distributed-notification plus atomic-file observer. The target writes one schema-versioned terminal receipt containing generation, evidence source, exact candidate/session/post-readback identities, panel/session mode, persisted feature state, and active/pending Search state before notifying. Success comes only from the internally consistent receipt; notification loss is covered by a named cancellable 100ms file-readback cadence, and the named eight-second watchdog is solely the terminal failure upper bound with path, decode error, last receipt, evidence source, and waiter result. The target has no lingering transport owner; the UI invocation owns observer, timer, route cleanup, and relaunch process, while the test owns the persisted defaults and first process. | H persisted configuration, TestingSupport observer lifecycle, initial presentation semantics, cross-process route/path boundary, and negative Search state; disabled/enabled intent, absolute-route parsing, exact notification/readback, initial state, stale/partial/wrong/delayed/cancel/duplicate/watchdog rules, lifecycle and three-launch UI Pressure, affected signed Settings-to-Switcher path, exact process and Process/Tooling validation. | completed |
| SYNC-036DN | `FlowTabUITestSwitcherSearchResultObservationOwner`, `performAndWaitForCommittedSearchResultRow`, and `testSettingsSearchDefaultScopePersistsAndShowsWindowThenAppResults`; committed Search result row after persisted scope selection | The Window- and App-scope phases each typed a query and then used an independent raw eight-second row-existence wait as proof that debounced Search computation, persisted scope selection, diagnostics publication, and Accessibility materialization had converged. Observation began after input, and a row alone did not bind the result to the exact committed scope, query, or result identity. | Extend the existing Search-result owner with one atomic snapshot containing foreground App state, percent-decoded committed scope/query/result IDs, and the exact visible row identity. Install its named cancellable 100ms XCUI readback schedule before typing, retain and reject an already-matching initial committed identity, gate later evidence until input returns, and request an immediate trigger readback. Accept only one foreground snapshot containing the exact Window Inbox or App Mail result identity and matching visible row. Retain the compatible named eight-second watchdog solely as a terminal failure upper bound with source, generation, scope, query, result IDs, row identity/existence, App state, and final waiter evidence. The helper invocation owns observation cancellation; each phase owns its App process and persisted defaults lifecycle. | H persisted Search preference, debounced computation, shared observation owner, and visible result projection; parser deduplication, exact/wrong/background/missing-row/gated/delayed/cancel/stale/duplicate/watchdog rules, lifecycle Pressure, affected signed three-process Settings/Search path, exact process evidence, project-file and Process/Tooling validation. Unit and Behavior are not relevant because production state and application orchestration are unchanged. | completed |
| SYNC-036DO | `assertInitialSwitcherAppProjectionAfterLaunch` and `launchMockSwitcherSearchFromUserPath`; user-triggered global Switcher Mail application projection | After the mock runtime reports application readiness, the helper posts the global Switcher trigger and waits for notification delivery, then starts an independent raw eight-second Mail-row existence wait as proof that the target has presented the Switcher. The row sample does not bind completion to the committed diagnostics App projection or foreground process state, and its observation begins after the trigger. | Reuse the generation-owned Switcher App-projection observer before posting the trigger. Retain its initial readback, gate resolution until notification delivery returns, and request an immediate trigger readback. Accept only foreground committed diagnostics containing the exact Mail bundle identity, then require its exact visible App row. Use a caller-owned named eight-second watchdog solely as the terminal failure bound; retain the named cancellable 100ms XCUI cadence only while evidence is unresolved. The helper invocation owns observer and cadence cancellation, while the calling test owns the application process and preference lifecycle. | H shared UI observation helper and persisted Window/App Search launch path; named-policy compatibility, initial/exact/partial/background/gated/delayed/cancel/stale/duplicate/watchdog owner regressions, affected signed three-process UI path, repeated signed UI Pressure, exact process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because production state and application orchestration are unchanged. | completed |
| SYNC-036DP | `launchMockSwitcherSearchFromUserPath`; foreground readiness of the already-launched mock process before the global Switcher trigger | A raw ten-second argument bounds the exact `XCUIApplication.State.runningForeground` wait. The condition is already an independent readiness Oracle, while policy ownership is implicit and the assertion drops the final application state on failure. | Retain foreground state as the sole success condition. Move the compatible ten-second bound into the Settings Search watchdog policy, attach a path-specific launch trace, and report expected plus final application state when the watchdog expires. The shared readiness helper retains its exact-state initial wait and bounded activation fallback; the caller owns the application process and XCTest owns the wait lifecycle. | H repeated two-launch Search path and foreground handoff; policy compatibility, affected signed three-process UI path, repeated signed UI Pressure, exact process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because production rules and application orchestration are unchanged. | completed |
| SYNC-036DQ | `testSettingsSearchWindowScopeUnavailableWithoutAccessibilityPermission`; permission-limited Settings process foreground readiness before exact app-only scope observation | A raw ten-second argument bounds the exact `XCUIApplication.State.runningForeground` wait before the already-migrated atomic scope projection observer is installed. Foreground state is an independent readiness Oracle, while policy ownership, path evidence, and final-state diagnostics are implicit. | Reuse the Settings Search application-foreground watchdog, attach a permission-path launch trace, and accept only `runningForeground`. On expiry report the expected and final application states. The shared readiness helper retains its exact-state initial wait and bounded activation fallback; the test owns the application process and XCTest owns the wait lifecycle. The later app-only scope observer remains a separate exact projection contract. | M permission-limited UI launch boundary; policy compatibility, affected signed permission-scope UI, repeated signed UI Pressure, exact process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because production rules, permission decisions, and application orchestration are unchanged. | completed |
| SYNC-036DR | `testSettingsSearchDisabledPreventsAutoSearchLaunchEntry`, `testSettingsSearchDefaultScopePersistsAndShowsWindowThenAppResults`, and `testSettingsSearchDefaultScopeCanSwitchBetweenAppAndWindow`; exact Search-enabled toggle projection after Settings navigation | Each path calls the shared toggle helper, whose one-second Switch wait selects a Switch or Checkbox query according to elapsed publication time, then applies a separate raw five-second existence wait to the selected query. A slowly published Switch can therefore change the chosen identity and the test result. | Resolve the exact stable Search-enabled identifier through the type-agnostic element query after the separately completed Settings-navigation projection. Perform an immediate existence readback and retain a Settings Search-owned compatible five-second `waitForExistence` only while the exact element remains absent. Success depends solely on exact identifier existence; terminal failure reports scenario, identifier, App state, existence, element type, hittability, and value. XCTest owns the wait and each test owns its App process. | M shared Settings Search control projection; policy compatibility, all three affected signed UI paths, repeated representative UI Pressure, exact process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because production toggle behavior and application orchestration are unchanged. | completed |
| SYNC-036DS | `FlowTabUITestSettingsAppVisibilityManagerProjectionObservationOwner` and `testSettingsAppVisibilityNavigationConvergesFromVisibleState`; exact manager-to-Settings root projection after Back | The test separately waits six seconds for Back existence, manager nonexistence, and Manage existence. Observation starts after Back, individual reads can span different animation projections, and manager absence can be combined with an unrelated or incomplete Settings root. | Extend the existing page snapshot with a Settings-root expectation. Start its generation-owned deferred observer before the exact Back trigger and require the initial snapshot to be the complete foreground manager projection. Gate root acceptance until the trigger returns, request an immediate readback, and accept only one foreground snapshot with Settings content and a hittable Manage action present while manager and Back are absent. Retain the named cancellable 100ms XCUI cadence only while unresolved because Accessibility exposes no SwiftUI navigation callback. The existing named six-second policy remains solely the trigger/projection failure bound with target, gate, generation, source, and every final field. The helper invocation owns registration and cancellation; the test owns its App process. | H animation-sensitive reverse UI projection and shared observation owner; exact root truth table, manager baseline, trigger gate, initial/delayed/cancel/watchdog/lifecycle Pressure, affected signed reverse-navigation UI, repeated signed UI Pressure, exact process evidence, project-file and Process/Tooling validation. Unit and Behavior are not relevant because production navigation state and orchestration are unchanged. | completed |
| SYNC-036DT | `testSettingsAppVisibilityHidesMockAppFromSwitcherAndSearch`; Search-process foreground readiness after the hidden-App Settings and Switcher processes terminate | A raw ten-second argument bounds the exact `XCUIApplication.State.runningForeground` wait before the separately owned Search-input and empty-result projections. Foreground state is already an independent readiness Oracle, while policy ownership, path evidence, terminal diagnostics, and failure-path isolation are implicit. | Reuse the Settings Search application-foreground watchdog, attach a hidden-App Search-path launch trace, and accept only `runningForeground`. On expiry report the expected and final application states and stop before consuming later independent Search Oracles. The shared readiness helper retains its immediate exact-state readback and bounded activation fallback; the test owns the Search application process and XCTest owns the wait lifecycle. Search-input readiness, diagnostics publication, and exact hidden-row absence remain independent contracts. | H third launch in a signed multi-process hidden-App path; policy compatibility, affected signed UI, repeated signed UI Pressure, exact process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because production visibility rules and application orchestration are unchanged. | completed |
| SYNC-036DU | `FlowTabUITestSwitcherDiagnosticsObservationOwner` and the committed empty-result phase of `testSettingsAppVisibilityHidesMockAppFromSwitcherAndSearch`; exact hidden-App Search projection after typing `Mail` | A standalone raw five-second element-existence wait completes before the trigger observer is installed, then a second raw five-second wait accepts the later diagnostics projection. The two phases can be separated by scheduling and give the first duration readiness meaning even though complete scope, query, and result evidence is observable in one diagnostics snapshot. | Resolve the stable diagnostics element without waiting and use the shared trigger helper to install its generation-owned observer before typing. Keep acceptance disabled for the initial readback, enable it only after the trigger returns, then request an immediate readback. Accept one snapshot only when the exact element exists and atomically reports App scope, percent-decoded `Mail`, and an empty committed result set. Retain the named cancellable 100ms XCUI cadence only while unresolved because Accessibility exposes no diagnostics-publication callback. Reuse the Settings Search eight-second committed-result watchdog solely as the terminal bound with acceptance, generation, source, expected keys, and final raw/value evidence. The helper invocation owns cancellation; the test owns its App process. Exact hidden-row absence is an independent final readback after committed empty results. | H trigger-ordering and atomic negative Search projection in a signed three-process UI path; shared owner initial/trigger/exact/percent-decode/atomic/cancel/watchdog and 100-generation Pressure coverage, affected signed UI, repeated signed UI Pressure, exact process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because production Search publication and orchestration are unchanged. | completed |
| SYNC-036DV | `FlowTabUITestSettingsCurrentAppActivationProjectionObservationOwner` and `testSettingsCurrentAppActivationPolicyAppearsAsHiddenApp`; exact Settings projection of the current App's default accessory policy | After Settings navigation, the test separately waits six seconds for the `showInCommandTab` toggle and another six seconds for the localized hidden-count text. Observation starts after navigation, the shared toggle helper can select Switch or Checkbox identity according to a one-second publication race, and the two reads can come from different Settings projections. | Resolve the stable toggle identifier through a type-agnostic query and the exact localized hidden-count element before navigation. Install a generation-owned observer with acceptance disabled, perform an initial readback, trigger exact Settings navigation, then enable acceptance and request an immediate readback. Accept one foreground snapshot only when Settings content exists, the exact toggle exists and reads off, and `已隐藏 1 个应用` exists. Retain the named cancellable 100ms XCUI cadence only while unresolved because Accessibility exposes no AppKit page-publication callback. Preserve the prior cumulative twelve-second opportunity as an owner-named watchdog solely for terminal failure with acceptance, generation, source, exact identifiers, toggle value, and final element evidence. The helper invocation owns cancellation; the test owns its App process. Later manager, Hidden-filter, and detail projections remain independent contracts. | H navigation ordering, type-agnostic control identity, repeated XCUI readback, and atomic negative Settings projection; exact truth table, initial/trigger/delayed/cancel/watchdog and 100-generation Pressure coverage, affected signed UI, repeated signed UI Pressure, exact process evidence, project-file and Process/Tooling validation. Unit and Behavior are not relevant because production activation-policy rules and Settings orchestration are unchanged. | completed |
| SYNC-036DW | `FlowTabUITestSettingsAppearanceControlsProjectionObservationOwner` and `testSettingsAppearanceTogglesCanBeChanged`; exact initial Settings Appearance controls projection | After Settings navigation, the test independently waits five seconds each for the shortcut-hint toggle, current-App activation toggle, exact current-App title, and exact explanatory text. The shared toggle helper can select Switch or Checkbox identity according to a separate one-second publication race, and sequential reads can combine different Settings projections. | Resolve both stable toggle identifiers through type-agnostic queries plus the exact localized title, description, and Settings content before navigation. Install a generation-owned observer with acceptance disabled, perform an initial readback, trigger exact Settings navigation, then enable acceptance and request an immediate readback. Accept one foreground snapshot only when Settings content and both exact toggles exist with readable values, the current-App toggle reads off, and both exact localized texts exist. Retain the named cancellable 100ms XCUI cadence only while unresolved because Accessibility exposes no AppKit page-publication callback. Preserve the prior cumulative twenty-second opportunity as an owner-named watchdog solely for terminal failure with acceptance, generation, source, identifiers, toggle values, and final element evidence. The helper invocation owns cancellation; the test owns its App process. Subsequent toggle mutations and their exact control-state readbacks remain independent synchronous interaction Oracles. | H navigation ordering, type-agnostic control identity, repeated XCUI readback, and multi-element Settings projection; exact truth table, initial/trigger/delayed/cancel/watchdog and lifecycle Pressure coverage, affected signed UI, repeated signed UI Pressure, exact process evidence, project-file and Process/Tooling validation. Unit and Behavior are not relevant because production Appearance state and Settings orchestration are unchanged. | completed |
| SYNC-036DX | `FlowTabUITestSettingsEnglishAppearanceProjectionObservationOwner` and `testSettingsAppearanceThemeAndLanguageUpdateVisibleUI`; exact Settings localization projection after selecting English | After the language-control value readback, the test independently waits five seconds for the English page subtitle, five seconds each for `Appearance` and `Theme mode`, and two seconds for the Chinese subtitle to disappear. Observation follows the language trigger, and sequential positive and negative reads can combine different localization projections. | Resolve Settings content plus the exact English `Settings`, page subtitle, `Appearance`, and `Theme mode` texts and the exact Chinese subtitle before language selection. Install a deferred generation-owned observer, retain its initial readback with acceptance disabled, select English and read back the exact language-control value, then enable acceptance and request an immediate readback. Accept one foreground snapshot only when every English element exists and the Chinese subtitle is absent. Return the exact page-subtitle element for the independent geometry Oracle. Retain the named cancellable 100ms XCUI cadence only while unresolved because Accessibility exposes no AppKit localization-publication callback. Preserve the prior cumulative seventeen-second opportunity as an owner-named watchdog solely for terminal failure with acceptance, generation, source, each localized element, and final values. The helper invocation owns deferred registration and cancellation; the test owns its App process. | H trigger ordering, atomic positive/negative localization projection, and repeated XCUI readback; exact truth table, initial/trigger/delayed/cancel/watchdog and lifecycle Pressure coverage, affected signed theme/language UI, repeated signed UI Pressure, exact process evidence, project-file and Process/Tooling validation. Unit and Behavior are not relevant because production localization rules, persistence, and Settings orchestration are unchanged. | completed |
| SYNC-036DY | `FlowTabUITestSettingsInitialAppearanceProjectionObservationOwner` and `testSettingsAppearanceThemeAndLanguageUpdateVisibleUI`; exact initial localized Appearance projection before theme changes | After Settings navigation, a raw five-second wait for the exact `外观` heading uses elapsed opportunity to infer that the localized Appearance page is ready. Observation follows navigation, while foreground application state and Settings-content identity are independently observable in the same snapshot. | Resolve Settings content and the exact `外观` heading before navigation. Install a deferred generation-owned observer with acceptance disabled, retain its initial readback, perform exact Settings navigation, then enable acceptance and request an immediate readback. Accept one foreground snapshot only when Settings content and the exact heading coexist. Retain the named cancellable 100ms XCUI cadence only while unresolved because Accessibility exposes no AppKit page-publication callback. Preserve the five-second opportunity as an owner-named watchdog solely for terminal failure with acceptance, generation, source, exact identifiers, heading value, and final element evidence. The helper invocation owns deferred registration and cancellation; the test owns its App process. Theme-control discovery and later luminance transitions remain independent contracts. | H navigation ordering, localized page identity, and repeated XCUI readback; exact truth table, initial/trigger/delayed/cancel/watchdog and lifecycle Pressure coverage, affected signed theme/language UI, repeated signed UI Pressure, exact process evidence, project-file and Process/Tooling validation. Unit and Behavior are not relevant because production localization, theme state, Settings orchestration, and module wiring are unchanged. | completed |
| SYNC-036DZ | `FlowTabUITestAverageLuminanceObservationPolicy`, `performAndWaitForAverageLuminance`, and the light/dark phases of `testSettingsAppearanceThemeAndLanguageUpdateVisibleUI`; Settings theme-transition watchdog ownership | Both evidence-driven screenshot observers receive a raw five-second `timeout` from the caller. Exact element existence and sampled luminance already determine success, while the retained duration's terminal-only role and owner remain implicit at the API boundary. | Name the compatible five-second theme-transition watchdog under the average-luminance observation owner and expose the helper parameter as `watchdog`. Preserve pre-trigger registration, trigger gating, immediate post-trigger readback, the named cancellable 100ms XCUI fallback, exact Settings-element identity, light/relative-dark thresholds, final readback diagnostics, and helper-owned cancellation. The watchdog solely terminates failure; scheduling pressure changes observation latency while threshold evidence alone determines success. | H repeated screenshot readback and visible theme transition; policy compatibility plus existing initial/trigger/delayed/cancel/watchdog and lifecycle Pressure regressions, affected signed theme/language UI, repeated signed UI Pressure, exact process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because production theme behavior and application orchestration are unchanged. | completed |
| SYNC-036EA | `FlowTabUITestSettingsWindowBehaviorControlsProjectionObservationOwner` and `testSettingsWindowBehaviorDelayAndTogglesPersistAcrossRelaunch`; exact initial Window Behavior controls projection | After Settings navigation, the test independently waits five seconds each for the delay input, auto-restore switch, and hide-minimized switch. The shared toggle helper can select Switch or Checkbox identity according to a separate one-second publication race, and sequential reads can combine different Settings projections. | Resolve Settings content, delay input, and both switches through type-agnostic exact-identifier queries before navigation. Install a deferred generation-owned observer with acceptance disabled, retain its initial readback, trigger exact Settings navigation, then enable acceptance and request an immediate readback. Accept one foreground snapshot only when Settings content and all three exact controls coexist, the delay value is non-empty, and both switch states are readable. Retain the named cancellable 100ms XCUI cadence only while unresolved because Accessibility exposes no AppKit page-publication callback. Preserve the prior cumulative fifteen-second opportunity as an owner-named watchdog solely for terminal failure with acceptance, generation, source, identifiers, values, switch states, and final element evidence. The helper invocation owns cancellation; the test owns its App process. Delay commit, toggle mutations, and relaunch persistence remain independent contracts. | H navigation ordering, type-agnostic control identity, repeated XCUI readback, and multi-element Settings projection; exact truth table, initial/trigger/delayed/cancel/watchdog and lifecycle Pressure coverage, affected signed persistence UI, repeated signed UI Pressure, exact process evidence, project-file and Process/Tooling validation. Unit and Behavior are not relevant because production Window Behavior state, persistence, and Settings orchestration are unchanged. | completed |
| SYNC-036EB | `FlowTabUITestInitialPresentationResolutionObservationOwner` and `testSettingsWindowBehaviorDefaultIncludesMinimizedAppsInInitialSwitcher`; exact default initial Switcher presentation | The original combined behavior test first launches a baseline App, then independently waits ten seconds for foreground state and eight seconds for each Mail and Minimized Notes row. Observer registration follows launch, sequential row waits can combine different Accessibility snapshots, and elapsed opportunity is treated as initial Switcher completion. The first canonical regression reproduced an absent Mail row. A signed Pressure run then showed the deeper boundary: XCTest Accessibility hierarchy queries blocked for repeated 30-second intervals and failed with `Timed out while evaluating UI query`; animation/event-loop load therefore still changed the result when XCUI was used as the completion channel. | Isolate the default baseline as its own independent UI path. Create the existing cross-process resolution route and start its owner before launch, remove any prior atomic readback, then pass the exact notification name and absolute readback path into the App. TestingSupport already observes runtime projection notifications before requesting readiness and atomically publishes terminal evidence. Success requires schema/generation/source validity, a complete global candidate containing Mail and Minimized Notes, a successful presentation attempt whose session IDs exactly equal the candidate IDs, a compatible same-or-later post-presentation generation and item readback, `panelIsPresented`, App-cycle session mode, and the configured Search state of enabled, inactive, and without pending activation. The distributed notification is the wakeup; a named cancellable 100ms file readback cadence covers a missed cross-process notification. The route/owner watchdog remains solely an eight-second failure upper bound with the last path, file/decode, generation, projection, session, panel, and Search evidence. The test invocation owns owner cancellation, readback removal, and the baseline App process. The configuration-to-filtered-projection test now resets defaults in its own Settings launch, preserving test independence; its Settings mutation, Accessibility row publication, and filtered negative projection remain later contracts. | H cross-process notification/readback reuse on another initial-launch caller plus a visible default Switcher path; deterministic initial-state/later-exact-evidence/cancellation/watchdog diagnostics, affected signed UI, repeated signed UI Pressure, exact signing/process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because the typed TestingSupport transport, production Window Behavior state, Switcher presentation state, and application orchestration are unchanged. | completed |
| SYNC-036EC | `assertSettingsWindowBehaviorControlsProjectionAfterNavigation`, `testSettingsWindowBehaviorHideMinimizedAppsCanBeEnabled`, and the Settings phase of `testSettingsWindowBehaviorHideMinimizedAppsAffectsSwitcherAppLayer`; hide-minimized control publication before mutation | After Settings navigation, the combined path performs a raw five-second wait for the exact hide-minimized control before setting it. Observer setup therefore follows the navigation trigger, and elapsed Accessibility opportunity is treated as control readiness. | Resolve Settings content and all Window Behavior controls before navigation through the existing EA owner. Establish its initial readback with acceptance disabled, trigger exact Settings navigation, enable acceptance only after the trigger returns, request an immediate readback, and activate the named cancellable XCUI fallback only while unresolved. Return the exact hide-minimized control from one accepted foreground projection, set it to enabled, and require its synchronous exact state readback. Exercise the same helper in a dedicated independent Settings-only visible path and in the combined setup path. The helper owns observer/scheduler cancellation; each test invocation owns exact App termination through `defer`. Retain the existing fifteen-second owner watchdog solely for terminal failure with the complete final controls projection. Filtered App launch and Switcher positive/negative projection remain later contracts. | H Settings navigation, repeated XCUI readback, persisted configuration mutation, and shared setup path; existing exact truth table/initial/trigger/delayed/cancel/watchdog/lifecycle Pressure owner coverage, affected signed Settings-only UI, repeated signed UI Pressure, exact signing/process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because production preferences, Settings orchestration, and module wiring are unchanged. | completed |
| SYNC-036ED | `FlowTabUITestInitialPresentationResolutionExpectation`, `assertInitialSwitcherPresentationResolution`, and the filtered phase of `testSettingsWindowBehaviorHideMinimizedAppsAffectsSwitcherAppLayer`; exact filtered initial Switcher projection | After persisting hide-minimized behavior, the filtered App launches before any observer exists, then independently waits ten seconds for foreground state, eight seconds for the Mail row, and two seconds for Minimized Notes nonexistence. Sequential Accessibility opportunities can combine different projections, and absence over two seconds is treated as filtering success. | Extend the existing initial-presentation expectation with exact excluded item IDs. Create the cross-process route, remove prior readback, and start its owner before launching each App. Accept only a valid complete global candidate containing Mail and excluding Minimized Notes, a successful presentation whose session IDs exactly equal the candidate, compatible same-or-later post-presentation generation/process/items, a presented App-cycle panel, and the configured inactive Search state. A shared assertion owns route, observer, scheduler, readback file, and exact App termination for both baseline and filtered callers. The distributed notification remains the primary wakeup and the named cancellable 100ms file cadence covers a missed cross-process notification. The eight-second owner watchdog solely terminates failure with required/excluded IDs and final path, decode, generation, projection, session, panel, and Search evidence. | H persisted configuration followed by cross-process complete positive/negative projection; excluded-item truth table including incomplete snapshots, existing initial/later/cancel/watchdog owner coverage, affected signed combined UI, repeated signed UI Pressure, exact signing/process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because typed production transport, Window Behavior state, Switcher filtering, presentation orchestration, and module wiring are unchanged. | completed |
| SYNC-036EE | `testSettingsSimplifiedChinesePermissionStatusesAreVisible`; foreground precondition before exact denied-permission Settings projection | After the canonical launch helper has already launched, activated, and bounded foreground activation with its named policy, this caller performs another raw ten-second foreground wait before creating the denied-permission projection observer. The later exact Settings navigation and atomic four-element projection are the functional success evidence. | Remove the duplicated caller-owned foreground wait and establish the existing denied-permission projection observer immediately after launch, before exact Settings navigation. The nested Settings projection requires a foreground Settings-only content state, and the denied-permission owner accepts only one snapshot containing both exact localized denied statuses and both exact actions. Its named cancellable 100ms readback cadence remains active only while unresolved and its named five-second watchdog solely terminates failure with generation, source, waiter, and final element evidence. The helper invocation owns observation cancellation; XCTest teardown owns the App process. The launch helper retains its named activation bound and exact final-state trace. | M localized permission UI caller and repeated XCUI projection; existing 16-state predicate, shared initial/trigger/scheduled/cancel/watchdog/lifecycle Pressure coverage, affected signed Simplified Chinese UI, repeated signed UI Pressure, exact process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because production localization, permission state, navigation, and module wiring are unchanged. | completed |
| SYNC-036EF | `testSettingsMainHotkeyRepresentativeMatrixTriggersSwitcher`; foreground and registration preconditions before each exact main-hotkey runtime receipt | After each persisted hotkey configuration launches a fresh App, the caller repeats a raw ten-second foreground-state wait and only then creates its runtime-log baseline. Removing that state readback reproducibly loses the immediate `Control+\`` XCTest key delivery even though both Carbon registrations returned success, while restoring the readback makes the same signed topology pass; the call is therefore an observable XCTest automation barrier whose literal terminal bound lacks a named owner. The post-launch baseline also omits registration evidence from failure diagnostics. | Start the runtime-log event source and capture file anchors/generation before launch. Require both exact main-monitor Carbon registration-success records, the persisted main-shortcut record, and launch registration generation 1 through the existing immediate readback plus filesystem events. Immediately before input, require an exact `.runningForeground` XCUIApplication readback under the shared named foreground-activation watchdog and report the final state on failure. Then send the configured key and accept only all three post-baseline hotkey, presentation-recovery, and route markers. Filesystem notifications remain the primary wakeup; the named cancellable 200ms fallback covers unavailable or missed file events, and the named eight-second watchdog solely terminates failure with generations, missing evidence, and the final log tail. The baseline and each observation invocation own cancellation; each matrix iteration owns exact App termination. | H persisted hotkey configuration, real key delivery, repeated Switcher presentation, and runtime-log observation; existing policy, initial/event/reset/order/cancel/watchdog/lifecycle Pressure owner coverage, affected signed three-case UI, repeated signed UI Pressure, exact process evidence, and Process/Tooling validation. Unit and Behavior are not relevant because production hotkey identity, registration, routing, presentation orchestration, persistence, and module wiring are unchanged. | completed |
| SYNC-036EG | `FlowTabUITestInitialPresentationResolutionReadback`, `FlowTabUITestInitialPresentationResolutionTransport`, `launchFlowTabUITestApplicationResolvingInitialPresentation`, and `testSettingsQuitHotkeyExplicitAndFallbackMatrixTerminatesSelectedApp`; exact pre-input quit-target readiness | Each persisted quit-hotkey case launches a fresh App with the Switcher requested, then performs sequential raw waits for foreground state, diagnostics summary publication, Browser row publication, and the dynamically selected App row. The selected App ID is read from a later summary snapshot, so elapsed Accessibility opportunities can combine different application, App-strip, selection, mode, and row projections before the real quit input. A first migration attempt moved these reads under the XCUI App-projection owner, but the signed regression blocked three times for about 31 seconds inside one diagnostics-value query; an outer ten-second watchdog could not bound that synchronous Accessibility call, so XCUI readback is not an acceptable primary readiness channel for this launch state. | Extend the existing App-side initial-presentation resolution schema to version 3 and atomically include the exact selected App identity plus a settled post-presentation input-readiness snapshot. Create the unique notification/readback route and start its generation-owned observer before constructing and launching the App. TestingSupport observes runtime projection evidence before requesting readiness, accepts a complete global candidate containing Browser, presents the exact session, captures its source/presentation generation and selected identity, installs the input-readiness observer before requesting maintenance, and accepts only a later projection generation with the same mode/process and exact session items/selected App, unchanged presentation generation, a presented visible panel, and settled presentation diagnostic, initial-visibility, recovery, active-space, activation-suppression, and termination-protection owners. Initial candidate completeness makes that exact later target authoritative while unrelated later repair may remain pending. Write one atomic JSON readback and post the distributed notification only after both barriers resolve. The external owner validates the schema, compatible generations, exact candidate/session/post-presentation/input-readiness identity, panel, mode, and inactive Search state before returning the live App plus typed resolution; the caller uses the same selected identity for quit receipt and disappearance Oracles. App-side notifications are observed before maintenance, with immediate readback, a named cancellable 100ms conditional cadence, and a three-second diagnostic watchdog. The distributed notification is the primary cross-process wakeup; a named cancellable 100ms file cadence covers missed delivery, and the owner-named eight-second watchdog solely terminates failure with the final decoded evidence. Owner completion/cancellation removes observers, scheduled work, watchdogs, and readback files; each matrix iteration owns its App process. Mock-runtime callers request mock previews so synthetic window identities stay within the fixture boundary. Quit input receipt, workspace termination, projection refresh, and selected-row disappearance remain a later independent contract. | H TestingSupport schema and generation owner, persisted quit-hotkey configuration, exact initial Switcher presentation and selected identity, cross-process notification/readback, shared mock-runtime launch topology, and real keyboard input; Behavior initial/event/scheduled/cancel/stale/order/watchdog/final-readback and 2,000-generation replacement Pressure, schema and selected-target UI truth tables, affected signed Search/default/hide-minimized/quit paths, repeated signed quit UI Pressure, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit is not relevant because no core rule, scheduler, path resolution, or module wiring changes. | completed |
| SYNC-036EH | `testSettingsInAppHotkeyExplicitAndFallbackMatrixStartsFocusedWindowSession`; exact in-app hotkey registration and foreground input readiness | After each persisted in-app hotkey configuration launches a fresh focused-current-App mock runtime, the caller repeats a raw ten-second foreground wait before establishing its input log baseline. The canonical baseline trace shows two adjacent `.runningForeground` reads before each key, while launch registration evidence is omitted from the input-readiness contract. | Start the runtime-log file event source and capture anchors/generation before launch. Require the exact in-app input-source registration, Carbon signature `1179932494` IDs 101 and 102, persisted in-app shortcut, and launch registration generation 1 through filesystem events plus immediate readback. Activate the App, then require a final exact `.runningForeground` XCUIApplication readback under the shared named foreground-activation watchdog immediately before input. Establish a new post-readiness baseline before typing. Accept success only from the exact pressed-route receipt, focused-current-App session with the expected bundle identity and two windows, initial advance, presentation recovery, presented-panel result, and `InApp Window Forward` receipt. The filesystem notification is primary; the existing named cancellable 200ms cadence covers missed events, and its eight-second watchdog reports missing evidence plus the final bounded log tail. Each baseline explicitly owns cancellation and each matrix iteration owns App termination through `defer`. | H persisted hotkey configuration, real keyboard delivery, frontmost automation state, focused-window session identity, and repeated presentation; existing runtime-log policy/initial/event/reset/order/cancel/watchdog/lifecycle Pressure coverage, affected signed two-case UI, repeated signed UI Pressure, exact signing/process evidence, project-file and Process/Tooling validation. Unit and Behavior are not relevant because production hotkey registration, routing, window-session construction, persistence, and module wiring are unchanged. | completed |
| SYNC-036EI | `testSettingsQuitHotkeyExplicitAndFallbackMatrixTerminatesSelectedApp`; exact post-input quit receipt, workspace termination, and projection refresh | The runtime-log baseline is established before each native quit key, and success already requires the exact selected App identity across mock termination receipt, sent quit request, workspace termination, and post-refresh records. The caller nevertheless supplies a raw ten-second timeout, leaving the retained compatibility bound and cancellation owner implicit. The later selected-row disappearance uses an independent Accessibility evidence source and remains a separate slice. | Retain ten seconds as `FlowTabUITestSettingsQuitHotkeyWatchdogPolicy.runtimeCompletion`, a named terminal failure bound for cross-process key delivery through refreshed runtime projection. Preserve the pre-input filesystem event source, initial readback, exact selected identity across all four markers, and the shared missing-marker/generation/source/waiter/final-tail diagnostic. Explicitly cancel the baseline on success and loop-scope exit. Filesystem notifications remain primary; the shared named cancellable 200ms cadence covers missed events only while unresolved. Each matrix iteration owns App termination. | H persisted quit configuration, native input, workspace termination, runtime projection, and repeated event observation; policy compatibility, existing runtime-log initial/event/reset/order/cancel/watchdog/lifecycle owner coverage, affected signed two-case UI, repeated signed UI Pressure, exact process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production termination, projection, persistence, module wiring, and path resolution are unchanged. | completed |
| SYNC-036EJ | `FlowTabUITestElementNonExistenceObservationOwner` and `testSettingsQuitHotkeyExplicitAndFallbackMatrixTerminatesSelectedApp`; exact selected-App row disappearance after refreshed termination evidence | After the exact quit receipt and post-refresh runtime markers resolve, the caller starts an `XCTNSPredicateExpectation` with a raw six-second bound for the pre-input selected row's exact Accessibility identifier to reach `exists == false`. The condition is independently observable, but its observation owner starts after the native quit input and failure diagnostics omit selected identity plus final `exists` readback. | Retain six seconds as `FlowTabUITestSettingsQuitHotkeyWatchdogPolicy.selectedRowDisappearance`. Construct the exact dynamic XCUI query and start a generation-owned observer before native key input. Perform an initial phase-gated readback that cannot resolve from pre-trigger absence, then immediately read back after the key trigger. With no platform disappearance event, activate the shared named cancellable 100ms XCUI cadence only while unresolved. Accept success exclusively from a post-trigger exact `exists == false` readback. The six-second watchdog performs a final non-resolving readback and reports selected App ID, exact row identifier, phase, generation, source, waiter result, and final existence evidence. Resolution, cancellation, replacement, and watchdog paths cancel scheduled work; each matrix iteration owns both the observer and App process. | H visible termination result, exact App identity, generation-owned XCUI observation, and repeated scheduling; policy, pre-trigger/trigger/scheduled/cancel/final-watchdog-readback coverage, 200-generation replacement Pressure, affected signed two-case UI, repeated signed UI Pressure, exact signing/process evidence, project-file and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production projection, termination, persistence, module wiring, and path resolution are unchanged. | completed |
| SYNC-036EK | `FlowTabUITestSettingsDropdownOptionObservationOwner` and `testSettingsHotkeyKeyDropdownOpensAsRightSideMenuWhenSpaceAllows`; scoped/raw option identity resolution after opening the hotkey-key dropdown | After the menu tap, a two-second `waitForExistence` on the scoped option drives a ternary choice between the exact scoped identifier and a legacy raw identifier, followed by another raw three-second existence wait. Scheduling latency therefore selects the query identity and can change the later frame/tap result. The signed baseline resolved the scoped option on its first XCTest predicate evaluation, then redundantly waited on that selected element again. Initial control readiness is an independent pre-menu contract reserved for a later slice. | Preserve both identifier forms as one compatibility candidate set with deterministic scoped-first priority in every readback. Construct both XCUI queries and start a generation-owned observer before tapping the control. Use a phase-gated initial readback, an immediate post-tap trigger readback, then the shared named cancellable 100ms XCUI cadence only while neither candidate exists because the platform exposes no menu-projection event. Retain the prior combined five seconds as a named terminal watchdog; success comes only from a post-trigger snapshot containing an existing candidate, while timeout performs a final non-resolving readback and reports both identifiers, both existence values, selected identity, phase, generation, source, and waiter result. Resolution, replacement, cancellation, and watchdog paths cancel scheduled work. The test invocation owns observer and App cleanup. | H visible dropdown placement, identifier compatibility, and generated menu-window topology; policy, scoped/raw priority, initial/trigger/scheduled/cancel/final-watchdog-readback coverage, 200-generation replacement Pressure, affected signed UI, repeated signed UI Pressure, exact signing/process evidence, project-file and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production dropdown layout, accessibility identifiers, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036EL | `testSettingsHotkeyKeyDropdownOpensAsRightSideMenuWhenSpaceAllows`; exact main-key control readiness after Settings navigation | The test constructs the main-key query and starts a raw six-second `waitForExistence` only after `openSettingsTab` has already completed its independent tab-projection contract. The signed baseline shows the control already available when XCTest eventually performs its first predicate evaluation about one second later, so the observer lifecycle and compatible terminal bound remain implicit. | Construct the exact main-key query and start a generation-owned existence observer before triggering `openSettingsTab`. Gate the initial readback from resolving pre-navigation state, request an immediate readback when navigation returns, and activate the shared named cancellable 100ms XCUI cadence only while the control is absent because no control-publication callback exists. Reuse the named six-second `FlowTabUITestSupportWatchdogPolicy.settingsControlDiscovery` solely as a terminal failure bound. Success requires a post-trigger exact `exists == true` readback; timeout performs a final non-resolving readback and reports identifier, phase, generation, source, waiter result, and final existence. Resolution, replacement, cancellation, and watchdog paths stop scheduled work. The helper invocation owns the observer while the XCTest case owns App cleanup. | H Settings navigation and visible control topology; initial/trigger/scheduled/cancel/final-watchdog-readback coverage, replacement Pressure, shared policy compatibility, affected signed UI, repeated signed UI Pressure, exact signing/process evidence, project-file and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production Settings projection, controls, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036EM | `testSettingsLanguageDropdownUsesLiveIntrinsicWidth`; exact language control readiness after Settings navigation | The test starts its exact language-control query and raw six-second `waitForExistence` only after the independent Settings-tab projection helper has returned. The bound supplies observation opportunity before live intrinsic-width readback, while trigger ordering, owner cancellation, and final existence evidence are implicit. | Reuse the generation-owned exact-element observer and construct the `flowtab.settings.appearance.app-language` query before triggering `openSettingsTab`. Preserve the phase-gated initial readback, immediate post-navigation readback, and named cancellable 100ms XCUI cadence only while the exact control is absent because Accessibility exposes no control-publication callback. Reuse the compatible six-second `FlowTabUITestSupportWatchdogPolicy.settingsControlDiscovery` only as a terminal failure bound with identifier, phase, generation, source, waiter result, and final existence diagnostics. The helper invocation owns cancellation and the XCTest case owns App cleanup. Exact live frame-width bounds remain the independent layout Oracle. | H Settings navigation and visible intrinsic-layout topology; shared initial/trigger/scheduled/cancel/final-watchdog/replacement owner coverage, shared policy compatibility, affected signed UI, repeated signed UI Pressure, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production Settings projection, localization, layout, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036EN | `testSettingsHotkeyInAppControlsDisabledWithoutAccessibilityPermission`; exact modifier/key disabled projection after Settings navigation | With Accessibility permission forced off, the test navigates to Settings and then performs sequential raw five-second existence waits for the exact in-app modifier and key controls before separately asserting both are disabled. Observation starts after navigation, the two waits can read different projections, their cumulative ten-second failure opportunity is implicit, and the terminal diagnostics omit exact existence/enabled state. | Construct both exact queries and start one generation-owned projection observer before triggering `openSettingsTab`. Gate the initial readback, immediately read both controls after navigation, and accept only one post-trigger readback in which both exact elements exist and both `isEnabled` values are false. Retain a named cancellable 100ms XCUI cadence only while unresolved because Accessibility exposes no permission-driven control-projection callback. Combine the prior cumulative ten seconds into one owner-named terminal watchdog whose final non-resolving readback reports both identifiers, existence/enabled values, acceptance, generation, source, and waiter result. Resolution, replacement, cancellation, and watchdog paths stop scheduled work. The helper invocation owns observation cancellation and the XCTest case owns App termination. | H permission-gated Settings topology, atomic positive/disabled projection, and repeated XCUI readback; exact truth table, initial/trigger/scheduled/cancel/final-watchdog/replacement coverage, policy compatibility, affected signed permission-off UI, repeated signed UI Pressure, exact signing/process evidence, project-file and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production permission rules, Settings controls, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036EO | `testEnglishPrimarySurfacesExposeUsableLayoutAnchors`; exact Search input and Mock Mail row projection after the Search trigger | After the Search trigger helper has returned, the test starts sequential raw five-second waits for the exact Search input and exact Mock Mail result row. The waits can accept different accessibility projections, observation begins after the trigger, their cumulative ten-second failure opportunity is implicit, and timeout output does not report both element identities in one final readback. The signed baseline delivered the trigger at about 8.37 seconds, while XCTest first evaluated the input and row around 9.40 and 10.51 seconds. | Construct both exact queries and start one generation-owned exact-element collection observer before posting the Search trigger. Gate the initial readback, immediately read both elements after trigger delivery returns, and accept only one post-trigger snapshot in which the expected ordered identifiers and both `exists` values are present together. Accessibility exposes no collection-publication callback, so activate the shared named cancellable 100ms XCUI cadence only while unresolved. Preserve the prior cumulative ten seconds as the English Search projection owner’s named terminal watchdog; its final non-resolving readback reports expected and observed identifiers, each existence value, phase, generation, source, and waiter result. Resolution, replacement, cancellation, and watchdog paths stop scheduled work. The helper invocation owns observation cancellation and the XCTest case owns App termination. | H visible Search topology, exact multi-element identity, and shared repeated XCUI readback infrastructure; collection truth table, initial/trigger/scheduled/cancel/final-watchdog/replacement coverage, policy compatibility, affected signed English UI, repeated signed UI Pressure, exact signing/process evidence, project-file and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production Search state, trigger delivery, configuration, module wiring, window identity, and path resolution are unchanged. | completed |
| SYNC-036EP | `testEnglishPrimarySurfacesExposeUsableLayoutAnchors` and `testEnglishGrantedPermissionActionsUseManageLabels`; FlowTab foreground readiness before English Settings and Search orchestration | Both tests pass a raw ten-second terminal bound to the shared foreground-readiness helper and assert only its Boolean result. Completion already comes from an immediate exact `.runningForeground` state readback with an installed-App activation fallback, while policy ownership and the failure-time final state are implicit. | Reuse the compatible `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` terminal failure bound in both callers. Preserve the helper’s immediate exact-state observation, optional activation fallback, and `.runningForeground` success Oracle. Capture the Boolean and report each path’s unmet foreground condition plus final `XCUIApplication.State` on expiry. Both state waits are synchronous and own no recurring work; each XCTest invocation owns App termination. | M two callers under one shared foreground-readiness contract; shared policy compatibility, both affected signed English UI paths, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and Pressure are not relevant because production code and observer/timer/scheduler/retry behavior are unchanged. | completed |
| SYNC-036EQ | `resolveSpaceFixtureWorkflowPermissions`; FlowTab foreground readiness before Home navigation and permission-projection readback | The shared real-topology permission preflight starts its permission observer before launch or resolution, then passes a raw twelve-second terminal bound to the foreground-readiness helper and asserts only its Boolean result. Completion already comes from an immediate exact `.runningForeground` state readback with installed-App activation fallback, while policy ownership and the failure-time final state are implicit. | Reuse the compatible `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` terminal failure bound. Preserve the immediate exact-state observation, optional activation fallback, and `.runningForeground` success Oracle; report the unmet Space Fixture permission-preflight condition plus final `XCUIApplication.State` on expiry before stopping the later independent Home-navigation and permission-projection contracts. The state waits are synchronous and own no recurring work. The caller continues to own the preinstalled permission observer and App cleanup. | M shared real-topology permission-preflight caller; shared policy compatibility, affected signed real-topology UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and Pressure are not relevant because production code and observer/timer/scheduler/retry behavior are unchanged. | completed |
| SYNC-036ER | `resolveSpaceFixtureWorkflowPermissions`; exact Home navigation trigger readiness before permission-projection readback | After foreground readiness, the permission preflight passes a raw ten-second bound to `tapFirstHittable` for the exact Home button query and asserts only the returned Boolean. The shared hittable owner already installs its generation and performs an immediate candidate-count/existence/hittability readback before tapping, then uses a cancellable 100ms XCUI cadence only while no exact candidate is hittable. The duration owner and caller-level final query evidence are implicit. | Add a compatible shared `FlowTabUITestSupportWatchdogPolicy.spaceFixtureNavigation` ten-second terminal bound. Construct and retain the exact Home query, preserve `tapFirstHittable` success solely from one snapshot containing an existing hittable candidate, and report the unmet permission-preflight Home trigger plus exact identifier and final candidate count. The helper invocation owns generation, scheduler cancellation, and watchdog final readback; the caller continues to own the preinstalled permission-projection observer and App cleanup. Add a direct signed UI regression for the shared permission preflight so validation stops after exact granted projection instead of depending on later fixture contracts. | M shared fixture-navigation policy and permission-preflight caller; policy compatibility, shared hittable initial/watchdog evidence, affected direct signed permission-preflight UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and Pressure are not relevant because production code and existing observer/timer/scheduler/retry mechanics are unchanged. | completed |
| SYNC-036ES | `triggerAndWaitForWorkflowAppOrder` and its initial, relaunched, and ten repeated-reopen System App MRU callers; exact switcher application order | Three raw ten-second call-site arguments, including the repeated-reopen loop, bound a generation-owned switcher diagnostics observer. The owner is already established before the global trigger, performs an immediate readback, and accepts only the exact ordered permutation of all eight expected bundle identities. A cancellable 100ms XCUI cadence handles the absence of a diagnostics-publication callback, while the compatible terminal duration remains caller-supplied. | Move the compatible ten-second `appOrderWatchdog` into `FlowTabUITestSystemAppMRUPolicy` and remove the duration parameter from the helper and all three call sites. Preserve exact ordered-identity equality as the sole success Oracle, observer-before-trigger ordering, immediate readback, and named cancellable fallback. Watchdog expiry continues to report expected order, final parsed/encoded diagnostics, generation, source, waiter result, and the switcher hierarchy. The helper invocation owns observation cancellation; the workflow owns App/fixture cleanup. | H eight-process System MRU identity and repeated presentation path; named-policy compatibility, deterministic exact-permutation and shared observation lifecycle coverage, affected signed eight-app initial/relaunch/ten-reopen UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production MRU state, window identity, module wiring, configuration, and path resolution are unchanged. The visible test’s ten reopen iterations provide repeated-path stress; separate Pressure is not relevant because observer/scheduler mechanics are unchanged. | completed |
| SYNC-036ET | `testSystemAppMRURebuildsForEveryFlowTabProcessSession`; initial-launch and process-relaunch App-rank bootstrap runtime-log evidence | Two raw ten-second arguments bound the shared runtime-log observation after each exact switcher-order projection. Each baseline already starts its file-event source before the corresponding FlowTab launch, anchors existing file identities and offsets, and later accepts only baseline-relative contents containing both `collectAppRank` and `bootstrapFallback=0`. The compatible duration remains implicit at the call sites. | Add a ten-second `appRankBootstrapLogWatchdog` to `FlowTabUITestSystemAppMRUPolicy` and use it for both phases. Preserve the pre-launch baseline, baseline-relative exact marker conjunction, immediate readback, file-event notification, named cancellable 200ms fallback, and final generation/content-tail diagnostics. The shared wait invocation owns observer and scheduled-readback cancellation; each baseline owns its file-event source through deinit, and the workflow owns FlowTab and fixture cleanup. | H eight-process launch/relaunch bootstrap evidence and shared filesystem observation; named-policy compatibility, deterministic initial/post-baseline/file-change/event/cancel/watchdog and lifecycle Pressure coverage, affected signed eight-app UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production ranking, logging, module wiring, configuration, exact window identity, and path resolution are unchanged. Separate UI Pressure is not relevant because runtime-log observation mechanics are unchanged; their existing deterministic lifecycle Pressure test remains required. | completed |
| SYNC-036EU | `terminateFlowTabUITestApplicationAndWait` and the System App MRU process-relaunch boundary; exact termination of the first FlowTab process | A private helper calls `terminate()` and then passes a raw five-second bound to XCTest’s `.notRunning` wait. Success already depends on exact process state, but the observation begins after the trigger, an initially stopped or immediate exit is not distinguished, the waiter Boolean is treated as success without a final boundary readback, and diagnostics retain only the final App state. | Replace the private helper with the shared `terminateFlowTabUITestApplication` evidence contract and its compatible named five-second default. Read the exact initial state before the termination trigger, read again immediately after it, invoke XCTest’s persistent `.notRunning` state wait only while needed, and always take a final state readback. Accept success only from final `.notRunning`; retain waiter completion as diagnostic evidence and report target, trigger decision, initial, post-trigger, waiter, and final states. The synchronous invocation owns the conditional wait and leaves no recurring work. | H FlowTab process-session identity before rebuilding System MRU; shared policy compatibility, deterministic already-stopped/immediate/delayed/boundary/watchdog and 100-target lifecycle Pressure coverage, affected signed eight-app terminate/relaunch UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production process lifecycle, MRU state, configuration, module wiring, exact window identity, and path resolution are unchanged. | completed |
| SYNC-036EV | `testSystemAppMRURebuildsForEveryFlowTabProcessSession`; relaunched FlowTab foreground readiness before the rebuilt-order trigger | After exact first-process termination and creation of the relaunched `XCUIApplication`, the shared launch helper already attempts activation under its named policy, then the caller passes a raw twelve-second terminal bound to the foreground-readiness helper and asserts only its Boolean. Success already comes from exact `.runningForeground` state with an activation fallback, while caller policy ownership and failure-time final state are implicit. | Reuse `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` for the caller wait. Preserve the helper’s exact state wait, installed-App activation fallback, and `.runningForeground` success Oracle. Capture the Boolean and report the unmet System MRU relaunch condition plus final `XCUIApplication.State` before any rebuilt-order trigger. The synchronous state waits own no recurring work; the workflow owns relaunched-App and fixture cleanup. | M process-session readiness before exact MRU reconstruction; shared policy compatibility, affected signed eight-app terminate/relaunch/order UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and Pressure are not relevant because production state, module wiring, configuration, exact window identity, path resolution, and observer/timer/scheduler/retry mechanics are unchanged. | completed |
| SYNC-036EW | `establishSystemAppOrder`; exact fixture foreground activation before initial launch and process relaunch | Each of the eight fixture bundle identities is activated in reverse order during both initial and relaunch setup. The shared frontmost owner already installs `NSWorkspace.didActivateApplicationNotification` observation before `activate()`, performs an initial readback, and accepts only exact frontmost bundle equality; the caller supplies a raw five-second terminal bound for every activation. | Add `fixtureActivationWatchdog` to `FlowTabUITestSystemAppMRUPolicy` with the compatible five-second value and use it for every order-establishment activation. Preserve observer-before-trigger ordering, exact bundle identity, initial and notification readbacks, and watchdog diagnostics containing expected plus final frontmost identity. Each helper invocation owns observer cancellation; the workflow owns every fixture App and the ordered identity list. | H sixteen exact fixture activations across initial and relaunch process sessions; named-policy compatibility, deterministic activation-notification/initial/cancel/final-watchdog and shared lifecycle Pressure coverage, affected signed eight-app UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production MRU state, activation behavior, module wiring, configuration, exact window identity, and path resolution are unchanged. The visible path supplies repeated activation stress; separate signed UI Pressure is not relevant because observer mechanics are unchanged. | completed |
| SYNC-036EX | `runNoisyOptionTabWindowStateRoundTrip`; exact four-window preview projection in the `normal1`, `fullscreen1`, `normal2`, and `fullscreen2` phases | After each phase’s global switcher/window-state readiness helper returns, the caller passes a raw eight-second bound to the shared preview-projection observer and requires exactly the workflow’s four user-window titles. Success already comes from a persistent diagnostics snapshot with the exact title set and exact count, while the phase-level duration owner is implicit. | Add a compatible `exactPreviewProjectionWatchdog` to a Noisy Option+Tab policy and use it for all four phases. Preserve exact title-set and count equality, initial readback, and the named cancellable 100ms XCUI fallback because the diagnostics element exposes no projection callback. The initial readback closes the post-trigger missed-event race for this persistent projection. Watchdog expiry retains identifier, existence, selected bundle, parsed titles, raw preview/summary, generation, source, and waiter result. Each phase helper invocation owns cancellation; the workflow owns App and fixture cleanup. | H four-phase real Space/fullscreen topology and repeated diagnostics readback; named-policy compatibility, exact-set/count/initial/scheduled/stale/watchdog rules, 100-generation preview-owner Pressure, affected signed noisy Option+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production filtering, MRU state, window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036EY | `runNoisyOptionTabWindowStateRoundTrip`; exact switcher-panel dismissal after each `normal1`, `fullscreen1`, `normal2`, and `fullscreen2` confirmation | Each phase waits for exact confirm-command delivery, then creates an `XCTNSPredicateExpectation` with a raw four-second bound and treats diagnostics-summary nonexistence as panel dismissal. Exact persistent absence is the independent Oracle, but observation starts after the trigger, pre-trigger absence is not phase-gated, lifecycle ownership is implicit, and failure retains no final existence evidence. | Add a compatible `switcherDismissalWatchdog` to the Noisy Option+Tab policy. Construct the exact diagnostics-element owner before posting confirm, perform a phase-gated initial readback that cannot resolve, mark the trigger complete only after exact command delivery, and immediately read back. Because XCUI exposes no dismissal callback, activate the shared named cancellable 100ms cadence only while unresolved. Accept success only from a post-trigger exact `exists == false` readback. The four-second watchdog performs a final non-resolving readback and reports phase, exact identifier, existence, generation, source, and waiter evidence. A phase-scoped helper owns cancellation; the workflow owns FlowTab and fixture App cleanup. | H four real confirmation/dismissal boundaries across Space/fullscreen topology; named-policy compatibility, deterministic pre-trigger/trigger/scheduled/cancel/final-watchdog rules, 200-generation replacement Pressure, affected signed noisy Option+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production confirmation, activation, filtering, exact window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036EZ | `runNoisyOptionTabWindowStateRoundTrip`; exact frontmost workflow CG-window evidence after each noisy confirmation and panel dismissal | The shared activation observer already accepts only the exact workflow bundle plus selected topmost on-screen CGWindowID, but the Noisy Option+Tab caller supplies a raw twelve-second terminal bound after each of four confirmations. Caller policy ownership is implicit even though activation success is independent of elapsed time. | Add a compatible `exactSelectedWindowActivationWatchdog` to the Noisy Option+Tab policy and use it for all four phases. Preserve the shared owner’s workspace-activation and target-process AX focused/main-window registrations before its immediate readback, plus its named cancellable 100ms serial fallback because CGWindow/Space publication has no complete callback. The activation condition is persistent, so the initial exact readback closes any event gap after confirmation. Accept success only from exact frontmost bundle and selected topmost CGWindowID; watchdog expiry reports expected identity plus the final frontmost bundle, window number/title/frame, active title, generation, source, and waiter evidence. The shared helper owns registrations, schedule, generation, cancellation, and cleanup; the workflow owns App and fixture lifecycle. | H four exact real-window activations across normal/fullscreen Spaces; named-policy compatibility, deterministic registration/initial/baseline-gate/exact-identity/slow-scheduling/cancel/final-watchdog rules, 100-generation replacement Pressure, affected signed noisy Option+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production activation, filtering, exact window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FA | `runNoisyOptionTabWindowStateRoundTrip`, `assertNoisyOptionTabWindowRequestSource`, and the per-phase topology-log baseline; exact post-confirm request and reconciliation evidence | Every phase creates one runtime-log baseline before confirm, then three sequential regex waits each use a raw eight-second bound: the exact sticky request for the selected App/CGWindowID/title, a ready nonzero affected-window topology diff with changed display/Space/window signature, and the same CGWindowID’s transition to `verifiedFocusReadback`. All three use one file-event source and lifecycle, but caller policy and baseline cancellation are implicit. | Add `postConfirmReconciliationWatchdog` to the Noisy Option+Tab policy and use it for all three exact log conditions. Calibrate its terminal bound to twelve seconds from the signed diagnosis where the eight-second final readback at `19:49:26.845` lacked the exact normal-window verification and the same PID/CGWindowID marker arrived at `19:49:26.917`. Preserve the pre-confirm baseline, filesystem events, immediate post-trigger readback, and shared named cancellable 200ms fallback for missed file delivery. Success requires each exact regex from contents after the same baseline; elapsed time never proves request or reconciliation. Watchdog expiry reports the missing regex, baseline/file-event generation, final bounded log tail, source, and waiter result. Each individual waiter owns its event registration and scheduler; the phase explicitly cancels the shared baseline after all three conditions, and baseline deinit remains a safety cleanup. The workflow owns App and fixture lifecycle. | H four real confirmation/reconciliation phases, exact selected-window identity, three ordered persistent log conditions, and repeated file observation; named-policy compatibility and calibration, regex/file-event/initial/event/reset/cancel/final-watchdog coverage, 100-iteration runtime-log lifecycle Pressure, affected signed noisy Option+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production request routing, reconciliation, topology signatures, binding confidence, exact window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FB | `runSwitcherPanelOptionTabWindowStateRoundTrip`, `runNoisyOptionTabWindowStateRoundTrip`, `assertNoisyOptionTabFilteredCGOnlyArtifactSource`, and `assertNoisyOptionTabWindowLayerSource`; pre-confirm noisy filtering and exact selected-window layer evidence | One runtime-log baseline is established before the real fixture workflow and global trigger. Every noisy phase then gives raw eight-second bounds to the persistent filtered-artifact regex and the selected CGWindowID/title sticky-layer regex. Exact log content is already the success Oracle, but caller policy is implicit and the outer baseline relies on deinitialization for cancellation. | Add a compatible `preConfirmEvidenceWatchdog` to the Noisy Option+Tab policy and use it for both exact regex waits. Preserve the pre-workflow baseline, observer registration before initial readback, filesystem events, and shared named cancellable 200ms fallback for missed delivery. Accept success only from post-baseline content matching either the nonzero filtered CG-only/fullscreen artifact source or the exact selected CGWindowID/title with `stickyBinding` and observed/inferred topology evidence. Expiry reports the missing regex, file-event generations, final bounded log tail, source, and waiter result. Each individual wait owns its observation and schedule; `runSwitcherPanelOptionTabWindowStateRoundTrip` owns the shared baseline and cancels it with `defer` after the fixture workflow, with deinitialization retained as safety cleanup. | H prelaunch through four noisy Space/fullscreen phases, one long-lived file source, and exact window identity; named-policy compatibility, regex/file-event/initial/event/reset/cancel/final-watchdog coverage, 100-iteration runtime-log lifecycle Pressure, affected signed noisy Option+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production filtering, layer projection, topology evidence, window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FC | `runSwitcherPanelOptionTabWindowStateRoundTrip.beforeFlowTabLaunch`; initial fullscreen-sibling topology readiness for ordinary and noisy Option+Tab fixtures | The ordinary branch gives a raw twelve-second bound to the exact frontmost-App/topmost-CG-window observer, while the noisy branch gives the same raw bound to the on-screen CG-window collection observer. Both run after fixture launch and before FlowTab launch, accept only persistent window evidence, and already treat elapsed time solely as an XCTest wait bound; caller policy is implicit. | Add an Option+Tab runtime-truth policy whose `optionTabInitialTopology` preserves the twelve-second terminal bound. The ordinary branch accepts only the target bundle frontmost plus a topmost CG window matching the exact title or titleless fullscreen geometry. The noisy branch accepts only a matching on-screen workflow CG window from the target bundle’s process. Preserve workspace activation and target-process AX focused/main-window registration before the initial readback plus the shared named cancellable 100ms fallback because CGWindow/Space publication has no complete callback. Each helper invocation owns observer registration, generation, scheduling, final readback, cancellation, and diagnostics; the fixture workflow owns App cleanup. | H two signed real fullscreen/Space fixture topologies and shared CGWindow hot readback; policy compatibility, deterministic exact initial/wrong-bundle/titleless/event/slow-scheduling/stale-generation/cancel/final-watchdog coverage for both single-window and collection owners, affected ordinary and noisy Option+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production fixture launch, Space topology, window matching, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FD | `runSwitcherPanelOptionTabWindowStateRoundTrip`; first ordinary Option+Tab window-cycle active-Space evidence after global-trigger and mode readiness | After exact global trigger delivery, Switcher App projection, and transition into window-cycle, the ordinary path gives a raw four-second bound to `waitForActiveSpaceWorkflowCGWindow` and requires the primary fullscreen sibling. The shared owner accepts only a persistent target-process topmost CG window matching the title or titleless fullscreen geometry, so elapsed time is already only a failure bound; caller policy is implicit. | Add `optionTabInitialWindowStateTopology` to the runtime-truth policy and preserve the four-second terminal bound. Preserve workspace activation and target-process AX focused/main-window registration before initial readback plus the named cancellable 100ms fallback because CGWindow/Space publication has no complete callback. Success requires the exact active-Space workflow window evidence while FlowTab may remain frontmost. Expiry reports expected title, final frontmost bundle, target topmost CGWindowID/title/frame, generation, source, and waiter result. The helper invocation owns registration, scheduling, final readback, cancellation, and cleanup; the real workflow owns App cleanup. | H signed real fullscreen-to-window-cycle Space path and shared CGWindow hot readback; policy compatibility, deterministic active-Space initial/event/slow-scheduling/stale-generation/cancel/final-watchdog coverage, affected ordinary Option+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production Switcher state, Space topology, window matching, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FE | `runSwitcherPanelOptionTabWindowStateRoundTrip`; ordinary Option+Tab standard/fullscreen confirmation and exact activated-window evidence | Both phases post the exact confirm command and wait for its delivery, then wait for panel dismissal, and only afterward construct an exact-window observer with a raw twelve-second bound. The persistent final bundle/window readback is independent evidence, but workspace and AX observation begin after the activation transition and the caller policy is implicit. | Start the existing `FlowTabUITestWorkflowWindowActivationObservationOwner` before each confirm through `triggerAndWaitForFrontmostWorkflowWindow`. Gate matching baseline evidence until the confirm-and-delivery trigger returns, then accept only the target bundle identifier plus the selected exact CGWindowID. Preserve workspace activation and target-process AX focused/main-window events plus the named cancellable 100ms fallback because CGWindowList has no exact publication callback and AX events may be coalesced. Add `optionTabConfirmedWindowActivation` to the runtime-truth policy and preserve twelve seconds solely as a terminal failure bound. Expiry reports the acceptance gate and final frontmost bundle, CGWindowID/title/frame, active title, generation, source, and waiter result. Each helper invocation owns generation, observation sources, schedule, cancellation, and final readback; the real workflow owns App cleanup. | H two signed real fullscreen/Space confirmations with exact window identity and shared CGWindow hot readback; policy compatibility, deterministic observer-before-readback/trigger-gate/exact-bundle-and-window/slow-scheduling/stale-generation/cancel/final-watchdog coverage, affected ordinary Option+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production confirmation, activation, window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FF | `runSwitcherPanelOptionTabWindowStateRoundTrip`; ordinary Option+Tab standard/fullscreen confirmation panel dismissal | Both phases construct a raw four-second `XCTNSPredicateExpectation` only after the exact confirm command and exact window activation have completed. Exact `exists == false` is a persistent Oracle, but the observer lifecycle begins after the trigger, the caller policy is implicit, and expiry exposes no element identifier, observation phase, generation, source, or final readback. | Add a small Option+Tab confirmation helper that starts `FlowTabUITestElementNonExistenceObservationOwner` before delegating the same confirm action to the independent exact-window activation owner. The disappearance owner gates its initial readback until activation evidence returns, immediately reads the exact diagnostics element, and enables the shared named cancellable 100ms fallback only while it remains present because XCUI exposes no disappearance notification. Add `optionTabSwitcherDismissal` to the runtime-truth policy and preserve four seconds solely as a terminal failure bound. Expiry reports the exact element identifier, phase, generation, final `exists` readback, source, and waiter result. The helper invocation owns cancellation for both owners; the real workflow owns App cleanup. | H two signed real confirmation/presentation-owner releases and a new UI-test orchestration boundary; policy compatibility, deterministic pre-trigger baseline/trigger readback/delayed scheduling/slow scheduling/cancel/stale-generation/duplicate-trigger/final-watchdog coverage, 200-iteration lifecycle Pressure, affected ordinary Option+Tab UI, exact signing/process evidence, project membership, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production dismissal, activation, confirmation, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FG | `runSwitcherPanelOptionTabWindowStateRoundTrip` and `relaunchGlobalSwitcher`; second ordinary Option+Tab presentation preserving the exact standard-window Space | The ordinary path completes global relaunch delivery and exact Switcher App/window-state readiness, then constructs an exact-window observer with a raw four-second bound to prove the previously confirmed standard window remains frontmost. Exact bundle plus CGWindowID is a persistent Oracle, but observation begins after the relaunch trigger and caller policy is implicit. | Add a small relaunch orchestration helper that starts `FlowTabUITestWorkflowWindowActivationObservationOwner` before delegating the same global relaunch. Gate a matching baseline until trigger delivery and exact switcher readiness return, then accept only the target bundle identifier plus the previously selected exact standard CGWindowID. Preserve workspace activation and target-process AX focused/main-window events plus the named cancellable 100ms fallback because CGWindowList has no exact publication callback and AX events may be coalesced. Add `optionTabRelaunchWindowTopology` to the runtime-truth policy and preserve four seconds solely as a terminal failure bound. Expiry reports the acceptance gate and final frontmost bundle, CGWindowID/title/frame, active title, generation, source, and waiter result. The helper invocation owns activation observation and propagates the relaunch result; the real workflow owns App cleanup. | H signed second-presentation Space/window-identity invariant and a new UI-test orchestration boundary; policy compatibility, deterministic observer-before-readback/trigger-gate/exact-bundle-and-window/slow-scheduling/stale-generation/cancel/final-watchdog coverage, 100-iteration lifecycle Pressure, affected ordinary Option+Tab UI, exact signing/process evidence, project membership, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production relaunch, Switcher readiness, Space topology, window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FH | `runSwitcherPanelWindowSearchRoundTrip.beforeFlowTabLaunch`; initial fullscreen-sibling topology readiness for ordinary and noisy Window Search fixtures | The ordinary branch gives a raw twelve-second bound to the exact frontmost-App/topmost-CG-window observer, while the noisy branch gives the same raw bound to the on-screen CG-window collection observer. Both run after fixture launch and before FlowTab launch, accept only persistent window evidence, and already treat elapsed time solely as an XCTest wait bound; caller policy is implicit. | Add `windowSearchInitialTopology` to the runtime-truth policy and preserve the twelve-second terminal bound. The ordinary branch accepts only the target bundle as frontmost plus its topmost CG window matching the exact title or titleless fullscreen geometry. The noisy branch accepts only a matching on-screen workflow CG window from the target process while allowing independent siblings. Preserve workspace activation and target-process AX focused/main-window registration before initial readback plus the shared named cancellable 100ms fallback because CGWindow/Space publication has no complete callback. Each helper invocation owns observer registration, generation, scheduling, final readback, cancellation, and diagnostics; the fixture workflow owns App cleanup. | H two signed real Window Search fullscreen/Space fixture topologies and shared CGWindow hot readback; policy compatibility, deterministic exact initial/wrong-bundle/titleless/event/slow-scheduling/stale-generation/cancel/final-watchdog coverage for both single-window and collection owners, affected ordinary and noisy Window Search UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production fixture launch, Search behavior, Space topology, window matching, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FI | `runSwitcherPanelWindowSearchRoundTrip`; first Window Search presentation active-Space evidence after trigger, input readiness, and committed-index readiness | The ordinary branch gives a raw four-second bound to the active-Space workflow-window observer, while the noisy branch gives the same raw bound to the on-screen workflow-window collection observer. Both accept persistent exact topology evidence after Search readiness; elapsed time is already only a failure bound and caller policy is implicit. | Add `windowSearchInitialPresentationTopology` to the runtime-truth policy and preserve the four-second terminal bound. The ordinary branch accepts only the target process's topmost active-Space window matching the exact title or titleless fullscreen geometry while FlowTab may be frontmost. The noisy branch accepts only a matching on-screen target-process window while allowing independent CG siblings. Preserve workspace activation and target-process AX focused/main-window registration before initial readback plus the shared named cancellable 100ms fallback because CGWindow/Space publication has no complete callback. Each helper invocation owns registration, generation, scheduling, final readback, cancellation, and diagnostics; the real workflow owns App cleanup. | H signed ordinary and noisy Search-presentation Space paths and shared CGWindow hot readback; policy compatibility, deterministic active-Space/collection initial/event/slow-scheduling/stale-generation/cancel/final-watchdog coverage, affected ordinary and noisy Window Search UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production Search state, committed-index readiness, Space topology, window matching, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FJ | `runSwitcherPanelWindowSearchRoundTrip` and `runNoisyWindowSearchRoundTrip`; ordinary and noisy Search confirmation with exact activated-window evidence | The two ordinary phases and four noisy phases post the exact `.searchConfirm` command, wait for panel disappearance, and only afterward construct an exact-window observer with a raw twelve-second bound. The persistent final bundle/window readback is independent evidence, but workspace and AX observation begin after the activation transition and caller policy is implicit. | Add a small Search-confirmation helper that starts `FlowTabUITestWorkflowWindowActivationObservationOwner` before posting the same command. Gate matching baseline evidence until command delivery returns, then accept only the target bundle identifier plus the selected exact CGWindowID. Preserve workspace activation and target-process AX focused/main-window events plus the named cancellable 100ms fallback because CGWindowList has no exact publication callback and AX events may be coalesced. Add `windowSearchConfirmedWindowActivation` to the runtime-truth policy and preserve twelve seconds solely as a terminal failure bound. Expiry reports the acceptance gate and final frontmost bundle, CGWindowID/title/frame, active title, generation, source, and waiter result. Each helper invocation owns generation, observation sources, schedule, cancellation, and final readback; the real workflow owns App cleanup. | H six signed real fullscreen/Space Search confirmations with exact window identity and a new UI-test orchestration boundary; policy compatibility, deterministic observer-before-readback/trigger-gate/exact-bundle-and-window/slow-scheduling/stale-generation/cancel/final-watchdog coverage, 100-iteration lifecycle Pressure, affected ordinary and noisy Window Search UI, exact signing/process evidence, project membership, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production Search confirmation, activation, window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FK | `runSwitcherPanelWindowSearchRoundTrip`, `runNoisyWindowSearchRoundTrip`, and `confirmWindowSearchSelectionAndWaitForEvidence`; Search input disappearance after all ordinary/noisy confirmations | Each phase constructs a raw four-second `XCTNSPredicateExpectation` only after the exact Search-confirm command and exact selected-window activation have completed. Exact `exists == false` is a persistent Oracle, but observation begins after the trigger, caller policy is implicit, and expiry exposes no element identifier, observation phase, generation, source, or final readback. | Extend the Search-confirmation helper so an outer `FlowTabUITestElementNonExistenceObservationOwner` starts before the nested exact-window activation owner posts the command. Gate initial absence until activation evidence returns, perform an immediate exact Search-input readback, and enable the shared named cancellable 100ms fallback only while the element remains present because XCUI exposes no disappearance notification. Add `windowSearchInputDismissal` to the runtime-truth policy and preserve four seconds solely as a terminal failure bound. Expiry reports the exact element identifier, phase, generation, final `exists` readback, source, and waiter result. The helper invocation owns cancellation for both owners; the real workflow owns App cleanup. | H six signed real confirmation/presentation-owner releases and the composed UI-test orchestration boundary; policy compatibility, deterministic pre-trigger baseline/trigger readback/delayed scheduling/slow scheduling/cancel/stale-generation/duplicate-trigger/final-watchdog coverage, 200-iteration lifecycle Pressure, affected ordinary and noisy Window Search UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production dismissal, activation, Search confirmation, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FL | `runSwitcherPanelWindowSearchRoundTrip`, `runNoisyWindowSearchRoundTrip`, and `relaunchWindowSearch`; all subsequent Window Search presentations preserving the previously confirmed exact workflow window | The ordinary path completes one relaunch and the noisy path completes three relaunches, including trigger delivery, Search-input readiness, and exact Search data/generation readbacks, before constructing an exact-window observer with a raw four-second bound. Exact bundle plus CGWindowID is a persistent Oracle, but workspace and AX observation begin after each relaunch transition and caller policy is implicit. | Add one Window-Search relaunch orchestration helper that starts `FlowTabUITestWorkflowWindowActivationObservationOwner` before invoking the existing relaunch/readiness closure. Gate a matching baseline until trigger delivery, keyboard readiness, and exact Search data/generation readbacks return, then accept only the target bundle identifier plus the previously selected exact CGWindowID. Preserve workspace activation and target-process AX focused/main-window events plus the named cancellable 100ms fallback because CGWindowList has no exact publication callback and AX events may be coalesced. Add `windowSearchRelaunchWindowTopology` to the runtime-truth policy and preserve four seconds solely as a terminal failure bound. Expiry reports the acceptance gate and final frontmost bundle, CGWindowID/title/frame, active title, generation, source, and waiter result. Each helper invocation owns generation, observation sources, scheduling, final readback, and cancellation; the real workflow owns App cleanup. | H four signed fullscreen/Space Search relaunches with exact window identity and a new UI-test orchestration boundary; policy compatibility, deterministic observer-before-readback/trigger-gate/exact-bundle-and-window/slow-scheduling/stale-generation/cancel/final-watchdog coverage, 100-iteration lifecycle Pressure, affected ordinary and noisy Window Search UI, exact signing/process evidence, project membership, and Process/Tooling validation. FlowTabCore Unit and app Behavior are not relevant because production relaunch, Search readiness, Space topology, window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FM | `runSwitcherPanelWindowSearchRoundTrip`, `relaunchWindowSearch`, and `assertWindowSearchReady`; exact diagnostics accessibility publication for every initial and subsequent Window Search presentation | After trigger delivery and exact keyboard-responder readiness, `assertWindowSearchReady` starts a raw twelve-second XCUI predicate wait for `flowtab.testing.switcher.summary`. Element existence is a persistent Oracle, but observation begins after the presentation transition, the previous presentation baseline is implicit, and timeout diagnostics omit the identifier, phase, generation, source, and final `exists` readback. | Add one presentation-readiness owner that starts the existing keyboard-readiness runtime-log observer and a `FlowTabUITestElementExistenceObservationOwner` before each Search trigger. Keep diagnostics evidence gated until the baseline-scoped keyboard-ready marker resolves, then perform an immediate exact identifier readback and enable the shared named cancellable 100ms fallback only while the element remains absent because XCUI exposes no publication notification. Add `windowSearchDiagnosticsPublication` to the runtime-truth policy and preserve twelve seconds solely as a terminal failure bound. Expiry reports the exact identifier, phase, generation, final `exists` readback, source, and waiter result. Each presentation invocation owns both observers, schedules, cancellation, and teardown; the real workflow owns App cleanup. | H six signed Search presentations and a composed UI-test readiness boundary; policy compatibility, deterministic baseline-gated keyboard event/readback plus element initial/trigger/delayed/slow-scheduling/cancel/stale-generation/duplicate-trigger/final-watchdog coverage, 200-iteration lifecycle Pressure, affected ordinary and noisy Window Search UI, exact signing/process evidence, project membership, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production presentation, accessibility identifiers, keyboard focus, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FN | `runSwitcherPanelWindowSearchRoundTrip`, `runNoisyWindowSearchRoundTrip`, `assertWindowSearchDataUsesWorkflowWindowCount`, and `assertWindowSearchUsesCommittedGenerationIndex`; committed Window Search projection for all six presentations | After keyboard readiness, two independently owned raw four-second waits first parse the workflow App's window-result set and then read five committed-index fields. Both use persistent condition/readback Oracles from SYNC-034AE/AF, but observation begins after the presentation trigger, separate accessibility-value reads may accept different projection generations, the prior presentation baseline is implicit, and the cumulative failure bound remains caller-owned. | Add one generation-owned committed-projection observer to the presentation-readiness owner before each Search trigger. Read `searchResults` plus readiness, result-state, degradation, current-generation coverage, and freshness-barrier fields from one exact diagnostics accessibility value. Gate the matching initial baseline until the exact keyboard-ready marker resolves, request one immediate post-marker readback, and retain the shared named cancellable 100ms cadence only while the atomic result-set/index condition remains unmet because XCUI exposes no diagnostics-value publication callback. Replace both literals with one named compatible eight-second terminal watchdog and report the exact expected App/titles/count plus the final identifier, raw value, parsed rows, five index fields, phase, generation, source, and waiter result. The presentation invocation owns all observation, scheduling, cancellation, and teardown; the real workflow owns App cleanup. | H six signed Search presentations, exact workflow-window identity, and a composed UI-test projection owner; policy compatibility, deterministic gated matching baseline, trigger readback, partial/mixed projection rejection, clean/noisy row-count rules, slow scheduling, cancellation, stale/duplicate generation, final-watchdog diagnostics, lifecycle Pressure, affected ordinary/noisy UI, exact signing/process evidence, project membership, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production Search indexing, window identity, accessibility publication, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FO | `searchAndSelectWorkflowWindow`, `performAndWaitForCommittedSearchWindowResult`, and all six ordinary/noisy Window Search query-result publications | Each query command first waits for delivery and only then constructs an eight-second Search-result projection wait. The shared result owner supplies a persistent immediate/readback Oracle, but this path accepts only title/App text, does not bind the accepted row to the committed `window` scope and exact query, begins its schedule after the trigger, and records no explicit pre-query baseline. | Add one query-projection owner around the shared committed-result expectation. Register it and capture one atomic scope/query/result baseline before posting the exact query command; gate evidence until delivery returns, reject an already-matching baseline until a subsequent projection transition, and request an immediate post-trigger readback. Because XCUI and diagnostics values expose no publication callback, activate the shared named cancellable 100ms cadence only while the exact committed scope/query/title/App condition remains unmet. Name the compatible eight-second runtime-truth watchdog solely as a terminal failure bound and report the baseline, transition state, expected identity, final projection, phase, generation, source, and waiter result. The helper invocation owns observation, scheduling, cancellation, and teardown; each signed workflow owns FlowTab and fixture App cleanup. | H six signed debounced queries across exact workflow-window identities plus shared UI-test observation infrastructure; policy compatibility, deterministic pre-trigger baseline, immediate event/readback, already-matching baseline transition, partial/wrong scope/query/result rejection, slow scheduling, cancellation, stale/duplicate generation, watchdog diagnostics, lifecycle Pressure, shared mock/real publication callers, affected ordinary/noisy UI, exact signing/process evidence, project membership, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production Search scheduling, indexing, diagnostics publication, configuration, module wiring, window identity, and path resolution are unchanged. | completed |
| SYNC-036FP | `selectSearchResultForConfirmation`, `postFlowTabUITestSelectSearchResultAndWaitForDelivery`, and all six ordinary/noisy Window Search result-selection applications | Each path posts `selectSearchResult`, spends up to four seconds accepting the command's generic `completed` log marker, then constructs a raw two-second diagnostics wait for the target `searchSelectedResult`. The generic completion marker is emitted even when the result ID is missing, and the exact diagnostics value commonly already matches after query publication, so observation starts after the trigger and an old selected baseline can be accepted as this command's success. | Freeze a runtime-log baseline and start one combined selection owner before writing the payload or posting the command. Require an exact post-baseline log line ending in `select search result command applied resultID=<exact ID>`, which TestingSupport emits only after model selection and accessibility-anchor synchronization, together with the exact percent-decoded `searchSelectedResult` from the same owner readback. Reuse runtime-log file events plus their named cancellable 200ms fallback because filesystem delivery can coalesce and XCUI exposes no diagnostics-value callback. Replace the sequential four-plus-two-second bounds with one compatible named six-second terminal watchdog that reports the missing applied marker, final log generation/tail, exact diagnostics value/raw projection, observation generation/source, and waiter result. The helper invocation owns the log baseline, observer, fallback schedule, cancellation, and teardown; each signed workflow owns FlowTab and fixture App cleanup. | H six signed exact-window selection commands and a TestingSupport log/readback composition boundary; policy compatibility, observer-before-readback/trigger ordering, already-matching diagnostics baseline, exact applied-marker suffix and result-ID matching, generic-completion/missed/wrong diagnostics rejection, event arrival, slow scheduling, cancellation, stale/duplicate generation, watchdog diagnostics, 200-iteration lifecycle Pressure, affected ordinary/noisy UI, exact signing/process evidence, project membership, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production selection, confirmation, window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FQ | `assertGlobalSwitcherWindowStateReady`, `testSwitcherPanelOptionTabHidesDesktopProvisionalCGOnlyWorkflowWindow`, `postFlowTabUITestSelectSwitcherAppAndWaitForDelivery`, and every shared Option+Tab `selectApp` application | Each path writes an app ID, posts `selectApp`, and spends up to four seconds accepting the command's generic `completed` marker. The shared runtime-truth path then begins a raw four-second wait for `selected=<bundle ID>`, while the provisional-hidden path proceeds directly to an app-strip projection that can already contain the target. TestingSupport emits generic completion even when the payload/session is unavailable or the app ID misses, and the exact selected value can already match the pre-command baseline. | Freeze a runtime-log baseline and start one combined app-selection owner before writing the payload or posting the command. Require an exact post-baseline log line ending in `select app command applied appID=<exact bundle ID>`, which TestingSupport emits only after session selection, accessibility-anchor synchronization, and panel-size update, together with exact `selected=<bundle ID>` diagnostics in the same owner readback. Reuse runtime-log file events plus their named cancellable 200ms fallback because filesystem delivery can coalesce and XCUI exposes no diagnostics-value callback. Replace the sequential four-plus-four-second bounds with one compatible named eight-second terminal watchdog that reports the missing applied marker, final log generation/tail, exact diagnostics value/raw projection, observation generation/source, and waiter result. The helper invocation owns the log baseline, observer, fallback schedule, cancellation, and teardown; each signed workflow owns FlowTab and fixture App cleanup. | H all shared ordinary/noisy/Space-backed/provisional-hidden signed app-selection commands and a TestingSupport log/readback composition boundary; policy compatibility, observer-before-readback/trigger ordering, already-matching diagnostics baseline, exact applied-marker suffix and bundle-ID matching, generic-completion/missed/wrong diagnostics rejection, event arrival, slow scheduling, cancellation, stale/duplicate generation, watchdog diagnostics, 200-iteration lifecycle Pressure, all affected signed UI topologies, exact signing/process evidence, project membership, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production app selection, Switcher presentation, window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FR | `assertGlobalSwitcherWindowStateReady`, `postFlowTabUITestSwitcherCommandAndWaitForDelivery(.advanceDown)`, `assertSwitcherSelectedApp`, and every shared Option+Tab App-layer-to-window-layer entry | Each path starts a mode observer before posting `advanceDown`, but the trigger first spends up to four seconds accepting the command's generic completion marker, then spends a raw four seconds accepting any `mode` value with a `windowCycle` prefix, followed by another raw four-second `selected=<bundle ID>` wait. TestingSupport emits generic completion independently of the state result, the prefix is not bound to the selected App, and the later selected readback can accept a pre-existing value from a different projection generation. | Capture one atomic `selected`/`mode` baseline after exact App selection and require the baseline to be the target bundle in `appCycle`. Establish the existing selection-transition owner before posting the raw `advanceDown` notification, gate resolution until the trigger returns, then accept only the same exact bundle in `windowCycle(<exact bundle ID>)` from one diagnostics readback. Reuse the named cancellable 100ms XCUI readback cadence because accessibility values expose no publication callback. Replace the generic delivery, prefix, and separate selected waits with one compatible named twelve-second terminal watchdog that preserves their cumulative failure opportunity and reports the expected transition, exact baseline, final identifier/raw/value map, observation generation/source, and waiter result. The helper invocation owns observation, schedule, generation, cancellation, and teardown; each signed workflow owns FlowTab and fixture App cleanup. | H shared ordinary/noisy/Space-backed Option+Tab state transition and exact App identity; policy compatibility, exact baseline rejection, observer-before-trigger and synchronous return readback, wrong/partial atomic-state rejection, slow scheduling, cancellation, stale/duplicate generation, watchdog diagnostics, 100-generation Pressure, all affected signed UI topologies, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production input handling, selection state, window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FS | `performAndWaitForSwitcherAppSelection`, `assertGlobalSwitcherWindowStateReady`, `testSwitcherPanelOptionTabHidesDesktopProvisionalCGOnlyWorkflowWindow`, and the post-`selectApp` App-strip waits | The pre-trigger selection owner spends up to eight seconds accepting an exact applied log marker plus `selected=<bundle ID>`, returns, and only then each caller starts a separate raw four-second App-strip wait. The later owner can accept an App-strip entry from a different diagnostics readback generation, and its observer is established after the selection trigger. Ordinary and provisional paths require an exact bundle/count entry while noisy paths require exact bundle identity with topology-dependent counts. | Extend the existing pre-trigger selection owner to read `selected` and `apps` from one raw diagnostics value together with the post-baseline exact applied log marker. Require the exact selected bundle and either the exact ordinary/provisional bundle-count entry or the exact noisy bundle identity in that same owner generation. Runtime-log file events request immediate readback and their named cancellable 200ms fallback covers coalesced delivery while the atomic condition remains unresolved. Replace the sequential eight-plus-four-second bounds with one named twelve-second terminal watchdog that reports applied-marker presence, expected selected and App projection, final log generation/tail, raw diagnostics/value map, observation generation/source, and waiter result. The helper invocation owns the log baseline, observer, fallback schedule, generation, cancellation, and teardown; each signed workflow owns FlowTab and fixture App cleanup. | H eight signed ordinary/noisy/Space-backed/provisional-hidden App selections and a shared log/diagnostics composition owner; policy compatibility, observer-before-trigger and initial readback, exact suffix/selected/App-entry composition, missing/wrong count/prefix-collision rejection, synchronous event and fallback readback, slow scheduling, cancellation, stale/duplicate generation, watchdog diagnostics, 200-generation Pressure, all affected signed UI topologies, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production selection, app/window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FT | `enterSwitcherPreview`, `assertGlobalSwitcherWindowStateReady`, and both open-window-layer mutation workflows; window-cycle entry plus initial preview-title publication | The pre-trigger entry owner accepts exact `selected=<bundle ID>` plus `mode=windowCycle(<bundle ID>)`, returns, and only then each caller constructs a separate raw eight- or twelve-second preview-title wait. The later owner can accept titles from a different diagnostics readback generation and begins observation after Down has already triggered the transition. Ordinary paths require the exact title set while noisy fullscreen paths require every standard title plus at least one real fullscreen title. | Extend the shared pre-trigger selection-transition owner with an entry-only preview expectation and read `selected`, `mode`, and `preview` from one raw diagnostics value. Gate acceptance until the trigger returns, then require the exact bundle/mode transition and either the exact ordinary title set or the noisy real-window rule in the same owner generation. XCUI exposes no accessibility-value publication callback, so retain the named cancellable 100ms scheduler with an immediate initial readback and explicit post-trigger readback. Replace the Runtime Truth twelve-plus-eight-second sequence with one named twenty-second terminal watchdog; replace the mutation three-plus-eight and three-plus-twelve sequences with named eleven- and fifteen-second terminal watchdogs. Expiry reports the baseline transition, expected preview rule, final identifier/raw selected/mode/preview projection, generation, source, and waiter result. The helper invocation owns observation, scheduling, generation, cancellation, and teardown; each signed workflow owns FlowTab and fixture App cleanup. | H shared runtime-truth and mutation preview-entry owner across ordinary, noisy, and Space-backed fixture topology; policy compatibility, observer-before-trigger and initial readback, trigger gating, exact atomic selected/mode/preview composition, partial/mixed/wrong-title rejection, slow scheduling, cancellation, stale/duplicate generation, watchdog diagnostics, lifecycle Pressure, all affected signed UI paths, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production selection, preview publication, exact window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FU | `performAndWaitForSwitcherAppSelection`, `assertGlobalSwitcherWindowStateReady`, and the provisional-hidden Option+Tab workflow; global Switcher diagnostics publication plus exact App selection | Both paths post the global presentation trigger, then start a raw eight-second diagnostics-existence wait, and only afterward establish the twelve-second App-selection owner before `selectApp`. Diagnostics existence is already required indirectly by the selected/App-strip projection, but observation begins after global presentation and the two failure bounds remain independent. | Establish the App-selection owner and its runtime-log baseline before the global trigger. Move the same global trigger, payload write, and `selectApp` post into its trigger closure. Require one post-trigger owner generation to contain published diagnostics plus the exact applied log suffix, selected bundle, and ordinary/provisional exact App-strip entry or noisy bundle identity. Make diagnostics publication explicit in the expectation and watchdog diagnostics. Runtime-log file events request immediate readback; the named cancellable 200ms fallback covers coalesced filesystem delivery and XCUI publication while the atomic condition remains unresolved. Replace the sequential eight-plus-twelve-second bounds with one named twenty-second terminal watchdog. The helper invocation owns the log baseline, observer, schedule, generation, cancellation, and teardown; each signed workflow owns FlowTab and fixture App cleanup. | H shared ordinary/noisy/Space-backed/provisional-hidden global-presentation/App-selection boundary; policy compatibility, observer-before-global-trigger and initial readback, unpublished diagnostics rejection, exact applied/selected/App projection, event and fallback readback, slow scheduling, cancellation, stale/duplicate generation, watchdog diagnostics, lifecycle Pressure, all affected signed UI topologies, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production presentation, selection, App/window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FV | `testSwitcherPanelOptionTabReportsUnverifiedSpaceBackedCGOnlyWorkflowActivation`; exact Switcher diagnostics dismissal after Space-backed confirmation | The path posts `confirm` and only afterward starts a raw four-second predicate wait for diagnostics nonexistence. Absence is the success Oracle, but pre-confirm existence, observer ordering, cancellation ownership, the terminal policy, and final evidence diagnostics remain implicit. The later window-request, CG activation-route, readback-mismatch, and recovery-exhaustion runtime-log contracts are independent. | Construct the exact diagnostics nonexistence owner before `confirm`, perform its immediate readback, and require the exact element to exist in that baseline. Post the existing confirmation command, mark trigger completion, then accept only a post-trigger exact `exists == false` readback. Accessibility exposes no dismissal callback, so retain the shared named cancellable 100ms XCUI cadence only while unresolved. Reuse the named four-second `optionTabSwitcherDismissal` terminal watchdog, which reports the element identity, phase, generation, source, final existence, and waiter result. The test invocation owns observer cancellation and FlowTab/fixture cleanup; later runtime-log observers retain their existing owners. | H exact Space-backed visible dismissal boundary and repeated XCUI readback; named-policy compatibility, existing initial/gated/trigger/scheduled/slow/cancel/watchdog/200-generation owner coverage, affected signed UI topology, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production confirmation, dismissal, activation, window identity, configuration, module wiring, and path resolution are unchanged. | completed |
| SYNC-036FW | `testSwitcherPanelOptionTabHidesDesktopProvisionalCGOnlyWorkflowWindow`; exact single-window App-cycle runtime marker after manual Down | The path creates a post-baseline runtime-log snapshot before Down, waits for the command-delivery Oracle, then passes a raw four-second timeout to the exact `advance key=downArrow ... windows=1 mode=appCycle` marker wait. Marker matching already determines success, and the anchored baseline plus immediate readback prevents missed-event loss, but the terminal policy and baseline cancellation owner remain implicit. Later diagnostics mode/preview assertions are independent readbacks. | Retain the pre-trigger anchored runtime-log baseline, file-event notification readback, immediate initial readback, and named cancellable 200ms fallback while the exact marker remains absent. Replace the raw four seconds with a provisional-hidden single-window-advance named terminal watchdog. Success remains solely the exact post-baseline App name, Down key, one-window count, and `appCycle` marker; timeout reports that unmet regex plus baseline/file-event generations and the final log tail. Explicitly cancel the baseline after the wait; the helper invocation owns its observer/fallback cancellation and workflow teardown owns both Apps. | H provisional-hidden Option+Tab state-machine and runtime-log lifecycle; policy compatibility, existing initial/event/slow/cancel/watchdog/100-generation runtime-log coverage, affected signed provisional topology, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production advance handling, hidden-window filtering, diagnostics projection, configuration, module wiring, exact window identity, and path resolution are unchanged. | completed |
| SYNC-036FX | `assertHiddenProvisionalCGOnlyRuntimeLog` and `testSwitcherPanelOptionTabHidesDesktopProvisionalCGOnlyWorkflowWindow`; provisional CG-only exclusion projection after FlowTab launch | The workflow creates its runtime-log baseline in `beforeFlowTabLaunch`, then after launch passes a raw eight-second timeout to an exact `<App> hidden-provisional-cg windows=1` marker wait. Marker matching already determines projection readiness, and the prelaunch file anchors plus immediate readback prevent missed-event loss, but the terminal policy and baseline cancellation owner remain implicit. Global presentation and later App-cycle evidence are independent contracts. | Retain the prelaunch anchored runtime-log baseline, file-event notification readback, immediate initial readback, and named cancellable 200ms fallback only while the exact projection marker remains absent. Replace the raw eight seconds with a provisional-hidden projection-publication named terminal watchdog. Success remains solely the exact escaped App name and one hidden provisional CG-only window count; timeout reports the unmet regex, baseline/file-event generations, and final log tail. Explicitly cancel the baseline after the assertion; the helper invocation owns observer/fallback cancellation and workflow teardown owns both Apps. | H provisional-window identity/exclusion evidence and prelaunch runtime-log lifecycle; policy compatibility, existing initial/event/file-replacement/cancel/watchdog/100-generation runtime-log coverage, affected signed provisional topology, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production filtering, projection, presentation, configuration, module wiring, exact window identity, and path resolution are unchanged. | completed |
| SYNC-036FY | `waitForSpaceBackedWindowLayerProjection` and `testSwitcherPanelOptionTabReportsUnverifiedSpaceBackedCGOnlyWorkflowActivation`; Space-backed CG-only window-layer projection before Switcher presentation | The workflow creates its runtime-log baseline in `beforeFlowTabLaunch`, then after launch passes a raw eight-second timeout to an exact window-entry regex before presenting the Switcher. Marker matching already determines readiness and binds App name, title, CG-only/AX/sticky/source state, Space evidence, and public-AX recovery, while the prelaunch anchors plus immediate readback prevent missed-event loss. The terminal policy and baseline cleanup owner remain implicit; the later selected source and activation sequence are independent consumers. | Retain the prelaunch anchored runtime-log baseline, file-event notification readback, immediate initial readback, and named cancellable 200ms fallback only while the exact projection marker remains absent. Replace the raw eight seconds with a Space-backed window-layer projection-publication named terminal watchdog. Success remains solely the exact post-baseline marker composition; expiry reports the unmet regex, baseline/file-event generations, and final log tail. Install explicit closure-scoped baseline cancellation before the first consumer so both this projection and the later source consumer share one bounded lifetime. Each wait helper owns observer/fallback cancellation; workflow teardown owns both Apps. | H Space-backed exact window identity/projection and shared prelaunch runtime-log lifecycle; policy compatibility, existing initial/event/file-replacement/cancel/watchdog/100-generation runtime-log coverage, affected signed Space-backed topology through the later independent activation-recovery boundary, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production runtime projection, presentation, activation, configuration, module wiring, exact window identity, and path resolution are unchanged. | completed |
| SYNC-036FZ | `assertSpaceBackedWindowLayerSource` and `testSwitcherPanelOptionTabReportsUnverifiedSpaceBackedCGOnlyWorkflowActivation`; exact window-layer source readback for the selected Space-backed CG-only window | After XCUI diagnostics independently select one exact title/window number, the helper searches the prelaunch anchored runtime log with a raw eight-second timeout. Its regex already binds that selected identity to the escaped App/title, CG-only/AX/sticky/source fields, Space evidence, and public-AX recovery. An initial readback may correctly resolve an already-published post-baseline marker, but the terminal policy remains implicit. The later confirmation and activation sequence are independent contracts. | Retain the closure-owned prelaunch baseline established before FlowTab launch, the immediate readback, file-event notification readback, and named cancellable 200ms fallback only while the exact selected-window source marker remains absent. Replace the raw eight seconds with a selected Space-backed window-layer source-readback named terminal watchdog. Success remains solely the exact post-baseline marker whose `id`, `cg`, and title equal the independently selected XCUI identity; expiry reports the unmet regex, baseline/file-event generations, and final log tail. The workflow closure owns shared baseline cancellation on every exit, each helper owns its observer/fallback, and teardown owns both Apps. | H selected exact window identity/source Oracle and shared prelaunch runtime-log lifecycle; policy compatibility, existing initial/event/file-replacement/cancel/watchdog/100-generation runtime-log coverage, affected signed Space-backed topology through the later independent activation-recovery boundary, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production projection, selection, activation, configuration, module wiring, exact window identity, and path resolution are unchanged. | completed |
| SYNC-036GA | `assertSpaceBackedWindowRequestSource` and `testSwitcherPanelOptionTabReportsUnverifiedSpaceBackedCGOnlyWorkflowActivation`; exact Space-backed CG-only window request published after confirmation | The workflow creates `activationLogSnapshot` after selecting the exact window and before `confirm`, then after exact diagnostics dismissal passes a raw eight-second timeout to the `window-request` marker. The regex already binds App ID, selected title/window number, CG-only/AX/sticky/source state, and public-AX recovery, but the terminal policy and shared activation-baseline cancellation owner remain implicit. Activation route, mismatch, and recovery exhaustion are later independent consumers. | Keep the observer-before-confirm anchored activation baseline, immediate readback, file-event notification readback, and named cancellable 200ms fallback only while the exact request marker remains absent. Replace the raw eight seconds with a Space-backed window-request-publication named terminal watchdog. Success remains solely the exact post-trigger App ID, selected `windowID`/title, `ax=0`, `sticky=false`, `source=nil`, and `publicAXRecovery=1` marker; expiry reports the unmet regex, baseline/file-event generations, and final log tail. Install closure-scoped activation-baseline cancellation immediately after baseline creation so all four activation consumers share one bounded lifetime. Each helper owns its observer/fallback, the dismissal owner retains its own cancellation, and workflow teardown owns both Apps. | H confirmation-to-activation exact window request and shared post-selection runtime-log lifecycle; policy compatibility, existing initial/event/file-replacement/cancel/watchdog/100-generation runtime-log coverage, affected signed Space-backed topology through the later independent activation-recovery boundary, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production confirmation, request routing, activation, configuration, module wiring, exact window identity, and path resolution are unchanged. | completed |
| SYNC-036GB | `assertSpaceBackedCGActivationRoute` and `testSwitcherPanelOptionTabReportsUnverifiedSpaceBackedCGOnlyWorkflowActivation`; exact CG activation-route publication for the selected Space-backed window | After the exact post-confirmation `window-request`, the helper searches the observer-before-confirm activation baseline with a raw eight-second timeout for `focus-attempt route=cg`. The regex already binds the selected window number through both `windowID` and `targetCG` plus the attempt PID/result, so the route marker is the independent Oracle; the terminal policy remains implicit. Binding mismatch and recovery exhaustion are later independent contracts. | Retain the shared anchored activation baseline, immediate readback, file-event notification readback, and named cancellable 200ms fallback only while the exact route marker remains absent. Replace the raw eight seconds with a Space-backed CG activation-route-publication named terminal watchdog. Success remains solely a post-trigger `focus-attempt route=cg` marker whose `windowID` and `targetCG` equal the independently selected CG window; expiry reports the unmet regex, baseline/file-event generations, and final log tail. The workflow closure owns baseline cancellation on every exit, each wait helper owns its observer/fallback, and workflow teardown owns both Apps. | H exact selected-window CG routing Oracle in the repeated activation path; policy compatibility, existing initial/event/file-replacement/cancel/watchdog/100-generation runtime-log coverage, affected signed Space-backed topology through the later independent readback/recovery boundary, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production routing, retry behavior, configuration, module wiring, exact window identity, and path resolution are unchanged. | completed |
| SYNC-036GC | `assertSpaceBackedCGActivationReadbackFailure` and `testSwitcherPanelOptionTabReportsUnverifiedSpaceBackedCGOnlyWorkflowActivation`; exact CG binding-readback mismatch publication for the selected Space-backed window | After the exact request and CG route markers, the helper searches the observer-before-confirm activation baseline with a raw eight-second timeout for `binding-readback-mismatch`. The regex already binds `route=cg`, the selected window number through `windowID` and `targetCG`, and `reason=targetCGNotVisible`, so the mismatch marker is the independent Oracle; the later recovery-exhaustion contract remains separate. | Retain the shared anchored activation baseline, immediate readback, file-event notification readback, and named cancellable 200ms fallback only while the exact mismatch marker remains absent. Replace the raw eight seconds with a Space-backed CG activation-readback-mismatch-publication named terminal watchdog. Success remains solely a post-trigger mismatch marker whose route, reason, `windowID`, and `targetCG` equal the independently selected CG window and expected failed readback; expiry reports the unmet regex, baseline/file-event generations, and final log tail. The workflow closure owns baseline cancellation on every exit, each wait helper owns its observer/fallback, and workflow teardown owns both Apps. | H exact failed-activation readback Oracle in the repeated CG retry path; policy compatibility, existing initial/event/file-replacement/cancel/watchdog/100-generation runtime-log coverage, affected signed Space-backed topology through the later independent recovery boundary, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Behavior, and FlowTabTests are not relevant because production readback, retry behavior, configuration, module wiring, exact window identity, and path resolution are unchanged. | completed |
| SYNC-036GD | `assertSpaceBackedCGActivationReadbackFailure` and `testSwitcherPanelOptionTabReportsUnverifiedSpaceBackedCGOnlyWorkflowActivation`; terminal recovery-watchdog evidence for the selected Space-backed window | The helper waits eight seconds for the legacy `focus-recovery exhausted ... attempts=` spelling after independently resolving the exact request, CG route, and readback mismatch. SYNC-006 replaced that retry chain with `RuntimeFocusRecoveryCoordinator`, whose five-second watchdog performs a final readback and publishes structured `focus-recovery state=watchdogExpired` evidence. The selected target reaches that evidence, while the stale regex waits until its own timeout and reports a false test failure. | Preserve the existing unverified-activation product semantics and shared observer-before-confirm runtime-log baseline. Accept only the structured watchdog failure whose unmet condition is `exactTargetWindowFocusedOrFrontmost`, App ID and `windowID`/`targetCG` equal the independently selected fixture target, `lastTrigger=watchdogReadback`, and final observation remains unsatisfied for a live process. Retain immediate readback, file-event notification readback, and the named cancellable 200ms fallback only while that exact terminal marker is absent. Replace the raw eight seconds with a Space-backed CG activation-recovery-failure-publication named test watchdog; it remains solely an observation failure upper bound above the production-owned five-second recovery watchdog. The workflow closure owns the shared baseline, each wait helper owns observer/fallback cancellation, the production coordinator owns its generation/observers/poll/watchdog, and workflow teardown owns both Apps. | H real Space/fullscreen activation-failure Oracle crossing the production recovery state machine and UI log consumer; existing coordinator initial/event/slow-scheduling/cancel/final-readback/watchdog Behavior, policy and shared runtime-log lifecycle coverage, affected signed Space-backed UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit is not relevant because this contract belongs to the AppKit/ApplicationServices runtime boundary. Runtime-topology Pressure is not relevant because production activation cadence, observers, retries, state, and hot-path code are unchanged. | completed |
| SYNC-036GE | `testHomeAndFreshOptionTabUseSameRuntimeAppOrder`, `testControlTabFirstPhysicalGestureSelectsNextWindowWithVisiblePreview`, both delayed window-layer entry tests, and their file-owned foreground-readiness assertion; mock-runtime FlowTab launch readiness | Four independent tests pass a raw ten-second bound to the shared application-ready helper after launch. The helper succeeds only from `XCUIApplication.State.runningForeground`, including an immediate state readback and a bounded activation fallback, but the repeated duration, operation owner, unmet condition, and final-state assertion diagnostics are implicit. Later Home, preview, log, dismissal, and repeated-presentation waits are separate contracts. | Add one file-owned `foregroundReadiness` policy retaining the compatible ten seconds solely as the initial `.runningForeground` terminal bound. Route all four call sites through one assertion owner that preserves the shared helper's immediate state readback, exact XCUIApplication state Oracle, and existing named activation fallback. Failure reports the scenario, `unmetCondition=runningForeground`, and final App state. Each test invocation owns the synchronous XCUIApplication wait and App teardown; the shared helper owns no persistent observer or timer beyond each bounded platform wait. | M mock-runtime startup boundary shared by Home/order, Control+Tab, delayed Option+Tab, and 12-cycle repeated-presentation paths; policy compatibility, all four affected signed UI tests, repeated-presentation Pressure, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, and process-level Pressure are not relevant because production launch, activation, UI behavior, state transitions, module wiring, configuration, and path resolution are unchanged. | completed |
| SYNC-036GF | `tapFirstHittableAndWaitForExistence` and the Home phase of `testHomeAndFreshOptionTabUseSameRuntimeAppOrder`; mock-runtime Home navigation completion | A raw five-second exact-button wait is followed by a raw five-second Home-content existence wait installed after the click. Exact conditions define success, while content-observer ordering, operation policy, cancellation ownership, and combined final diagnostics are implicit. Later App-row and Switcher-order projections are independent contracts. | Resolve the exact Home button query and content before the trigger, then route through the shared trigger-and-content owner. Install the generation-owned content observer before waiting for and tapping the exact hittable button. Accept an already-present initial content state or a later exact existence readback through the shared named serial 100ms XCUI cadence. Use `FlowTabUITestSupportWatchdogPolicy.tabNavigation` solely as each exact condition's terminal failure bound. The helper invocation owns observation generation, cancellation, and trigger/content diagnostics; the test owns App teardown. | H observer-before-navigation and repeated XCUI readback; existing shared policy compatibility, initial/scheduled/cancel/watchdog/reentrancy/lifecycle Pressure, affected signed Home/order UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, and process-level Pressure are not relevant because shared infrastructure, production behavior, module wiring, configuration, and path resolution are unchanged. | completed |
| SYNC-036GG | `FlowTabUITestHomeAppRowProjectionExpectation`, `FlowTabUITestHomeAppRowProjectionObservationOwner`, and the six-row phase of `testHomeAndFreshOptionTabUseSameRuntimeAppOrder`; atomic mock Home runtime-order projection | After Home navigation, six raw six-second element waits run sequentially, then six independent frame reads are sorted. Each exact row may be observed from a different projection generation, scheduling pressure can consume a later row's opportunity, and no observer exists before navigation. The test intentionally compares the two actual Home and Switcher orders rather than requiring the fixture declaration order. | Extend the shared Home-row expectation with optional expected values and an explicit unconstrained-frame-order mode. Resolve all six exact row identifiers and start one observation before Home navigation with acceptance disabled. After exact navigation completion, enable acceptance and request an immediate readback. Accept only one snapshot in which every exact identifier exists and every frame is finite, while row values and a predetermined visual order remain unconstrained; derive the actual ascending-frame identifier order from that same snapshot for the later independent Switcher comparison. Retain the shared named serial 100ms XCUI cadence because Accessibility exposes no atomic layout callback. A named 36-second watchdog equals the prior six-by-six-second cumulative failure bound and reports acceptance, expected identities, and the final atomic snapshot. Resolution, expiry, explicit teardown, and deinit own cancellation; the test owns App teardown. | H shared Home-row expectation plus observer-before-navigation repeated XCUI readback; deterministic exact/unconstrained value/order, missing/nonfinite rejection, initial/trigger/scheduled/cancel/watchdog/reentrancy/lifecycle Pressure, affected signed Home/Option+Tab order UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, and process-level Pressure are not relevant because production projection, runtime order, module wiring, configuration, and path resolution are unchanged. | completed |
| SYNC-036GH | `FlowTabUITestSwitcherAppProjectionExpectation`, `FlowTabUITestSwitcherAppProjectionObservationOwner`, and the Option+Tab phase of `testHomeAndFreshOptionTabUseSameRuntimeAppOrder`; exact mock Switcher runtime-order publication | The Option+Tab trigger runs before a raw five-second summary-existence wait is installed. Summary existence is treated as readiness, followed by one read and a parsed App-ID order comparison; a stale or intermediate projection can satisfy existence and observer ordering can miss a short-lived valid publication. Home order is already an independently resolved atomic snapshot. | Extend the shared Switcher App projection expectation with exact ordered bundle identifiers while leaving window counts unconstrained. Add a generic trigger-and-projection helper that registers its named serial 100ms XCUI readback before the trigger, performs an initial gated readback, enables acceptance only after trigger return, requests immediate readback, and succeeds only when one snapshot's ordered bundle identifiers equal the independently captured Home order. Retain a named five-second runtime-order watchdog solely as the compatible terminal failure bound; expiry reports expected order, acceptance, and final raw/parsed projection. Resolution, expiry, explicit teardown, and deinit own cancellation; the test owns modifier scope and App teardown. | H shared Switcher App projection expectation and observer-before-Option+Tab path; deterministic policy, exact order/count independence, wrong/missing/prefix-collision rejection, initial/trigger/scheduled/cancel/watchdog/reentrancy/lifecycle Pressure, affected signed Home/Option+Tab order UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant because production projection, runtime order, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GI | `FlowTabUITestSwitcherInteractionRegressionWatchdogPolicy`, `performAndWaitForSwitcherDiagnostics`, and the initial Control+Tab phase of `testControlTabFirstPhysicalGestureSelectsNextWindowWithVisiblePreview`; exact selected-window and preview-count projection | The shared trigger-and-diagnostics helper already registers before Control+Tab, gates pre-trigger evidence, performs immediate and named scheduled readbacks, and succeeds only when one summary snapshot has `selectedWindow=mock-current-secondary` and `previewImages=2`. Its raw five-second terminal bound lacks a scenario owner and compatibility test. Later exact preview-element, window-card, and runtime-log evidence are independent contracts. | Retain the existing observer-before-trigger diagnostics owner and exact two-key atomic Oracle. Move the compatible five-second terminal failure bound into the file-owned interaction watchdog policy and include it in the deterministic finite-positive compatibility rule. The helper continues to report acceptance, both expected keys, and the final summary readback on expiry. The helper invocation owns resolution/expiry cancellation, the modifier closure owns Control-key scope, and the test owns App teardown. | M policy ownership around an existing evidence-driven helper; deterministic compatible-bound test, affected signed Control+Tab UI, exact signing/process evidence, and Process/Tooling validation. Observer lifecycle Pressure, FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant because observer implementation, evidence predicates, production behavior, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GJ | `FlowTabUITestElementExistenceObservationOwner`, its generic start/assert entry points, and the selected-preview phase of `testControlTabFirstPhysicalGestureSelectsNextWindowWithVisiblePreview`; exact secondary-window preview identity | A raw five-second XCUI existence wait is created only after the Control+Tab trigger and combined diagnostics complete. Exact element identity defines success, but observer ordering, lifecycle ownership, named policy, and final readback diagnostics are implicit. The summary's `previewImages=2`, two exact window cards, and runtime logs remain independent evidence. | Resolve the exact secondary preview element and start the shared element-existence observation before Control+Tab. Require its initial gated readback to prove the exact preview absent, while scheduled reads stay deferred. After the trigger path returns, mark trigger completion, request immediate readback, then activate the named serial 100ms XCUI fallback only while the exact preview remains absent. Success requires that exact accessibility identifier to exist. Retain a file-owned five-second selected-preview watchdog solely as the compatible failure upper bound; expiry reports element identity, phase, source, and final existence. The test owns observer cancellation and App teardown; the modifier closure owns Control-key scope. | H observer-before-Control+Tab exact element evidence; deterministic compatible-bound plus shared post-trigger/delayed/slow-scheduling/cancel/watchdog/100-replacement Pressure tests, affected signed Control+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant because production behavior, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GK | `FlowTabUITestSwitcherWindowCardObservationOwner`, `performAndWaitForSwitcherWindowCards`, and the two-card phase of `testControlTabFirstPhysicalGestureSelectsNextWindowWithVisiblePreview`; atomic current-app window-card projection | A raw five-second title-projection wait is established only after Control+Tab diagnostics and exact preview evidence complete. It can accept an already-present matching collection, then the caller independently rebuilds the card collection for frame and image assertions, so readiness and visible-result evidence can come from different Accessibility snapshots. | Establish the shared card-collection owner before Control+Tab, require its immediate initial snapshot to contain zero cards, and gate matching evidence until the nested diagnostics and exact-preview trigger path returns. Request an immediate post-trigger readback and accept only one snapshot with exactly two cards, the exact `Current Primary`/`Current Secondary` title multiset, and two distinct stable identifiers. Use that resolved snapshot directly for unioned bounds and image evidence. Retain the named serial 100ms XCUI readback cadence because Accessibility supplies no atomic collection callback. A file-owned five-second card watchdog remains solely the compatible failure bound and reports expected and final atomic snapshots. The helper invocation owns cancellation; the modifier closure owns Control-key scope and the test owns App teardown. | H shared collection helper plus observer-before-Control+Tab atomic XCUI readback; deterministic trigger gating, stale/excluded/duplicate rejection, delayed evidence, cancellation, watchdog-final-readback and 100-generation lifecycle Pressure, compatible-bound policy, affected signed Control+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant because production behavior, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GL | `FlowTabUITestRuntimeLogObservationBaseline`, `FlowTabUITestRuntimeLogObservationPolicy`, `waitForRuntimeLogFiles`, and the final log phase of `testControlTabFirstPhysicalGestureSelectsNextWindowWithVisiblePreview`; post-baseline Control+Tab orchestration evidence | An explicit raw eight-second argument is a terminal bound around an already evidence-driven runtime-log wait. The baseline is constructed before Control+Tab and starts file-system observation while capturing file anchors and generation; the later condition owner performs an immediate post-baseline readback, then combines file events with a named fallback. The raw override duplicates the shared default and baseline cancellation is left to deinit. | Remove the anonymous override so the shared named eight-second `defaultWatchdog` owns the compatible terminal failure bound. Preserve the exact five-marker all-of Oracle over post-baseline bytes; initial readback closes events between trigger and waiter registration, while file-event generation and the named cancellable 200ms cadence cover later delivery. Expiry continues to report missing markers, baseline/current generation, byte count, log tail, and path. Add explicit test-owned baseline cancellation; the wait invocation already owns condition observer and scheduled fallback cancellation. | M policy/lifecycle ownership around unchanged event/readback evidence; shared policy, post-baseline append/reset/event/cancel/watchdog/lifecycle Pressure tests, affected signed Control+Tab UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant because production behavior, observer implementation, evidence predicate, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GM | `FlowTabUITestElementNonExistenceObservationOwner`, its generic trigger helper, and the dismissal phase of `testOptionTabDelayedWindowLayerEntryRepeatedPresentationPressure`; exact repeated-presentation owner release | Each of twelve iterations delivers `.confirm` before installing a raw five-second XCUI nonexistence wait. Exact summary absence defines success, but a fast dismissal can precede observer setup, an already-absent summary can pass without proving the current presentation existed, and the repeated terminal policy/lifecycle owner is implicit. Preview-transition and runtime-log evidence remain independent contracts. | Resolve the exact diagnostics-summary element and start one nonexistence observation inside each iteration before delivering `.confirm`. Require the immediate baseline readback to prove the current presentation exists. After command delivery returns, mark trigger completion, request immediate readback, and activate the shared named serial 100ms XCUI cadence only while the exact element remains present because Accessibility supplies no dismissal callback. Success requires exact summary absence. Retain a file-owned five-second dismissal watchdog solely as the compatible failure upper bound; expiry reports element identity, phase, source, and final existence. Each helper invocation owns cancellation before the next iteration; the test owns App teardown. | H reusable observer-before-confirm path exercised across twelve presentation lifecycles; deterministic compatible-bound plus shared post-trigger/delayed/slow-scheduling/cancel/watchdog/200-replacement Pressure tests, affected signed repeated-presentation UI Pressure, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant because production behavior, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GN | `FlowTabUITestSwitcherAndSearchWatchdogPolicy`, `assertElementDoesNotExistAfterTrigger`, and `testOptionTabSwitcherClickCommitsAppAndClosesPanel`; Option+Tab App-card click dismissal | The exact Mail tile is tapped before a raw two-second nonexistence wait is installed for the diagnostics summary. Absence is a persistent visible close Oracle, but a fast dismissal can precede observer setup, an already-absent baseline can pass without proving this presentation existed, and terminal policy/lifecycle ownership are implicit. The preceding exact switcher-open and Mail-tile readiness conditions remain independent. | Resolve the exact diagnostics summary and start the shared nonexistence owner before tapping the exact Mail tile. Require its immediate baseline readback to prove the current Option+Tab presentation exists. After the XCUI tap returns, mark trigger completion, request immediate readback, and activate the shared named serial 100ms XCUI cadence only while the exact summary remains present because Accessibility supplies no dismissal callback. Success requires exact summary absence. Retain a file-owned two-second App-card-click dismissal watchdog solely as the compatible failure upper bound; expiry reports element identity, phase, generation, source, final existence, and wait result. The helper invocation owns cancellation and the test owns App teardown. | H observer-before-XCUI-click exact visible dismissal; deterministic compatible-bound plus shared initial/trigger/delayed/slow-scheduling/cancel/watchdog/200-replacement Pressure coverage, affected signed Option+Tab click UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant because production behavior, input routing, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GO | `FlowTabUITestSwitcherAndSearchWatchdogPolicy`, `assertElementDoesNotExistAfterTrigger`, and `testControlTabSwitcherClickCommitsWindowAndClosesPanel`; Control+Tab window-card click dismissal | The exact secondary-window card is tapped before a raw two-second nonexistence wait is installed for the diagnostics summary. Absence is a persistent visible close Oracle, but a fast dismissal can precede observer setup, an already-absent baseline can pass without proving this presentation existed, and terminal policy/lifecycle ownership are implicit. The preceding exact in-App switcher-open and secondary-window-card readiness conditions remain independent. | Resolve the exact diagnostics summary and start the shared nonexistence owner before tapping the exact secondary-window card. Require its immediate baseline readback to prove the current Control+Tab presentation exists. After the XCUI tap returns, mark trigger completion, request immediate readback, and activate the shared named serial 100ms XCUI cadence only while the exact summary remains present because Accessibility supplies no dismissal callback. Success requires exact summary absence. Retain a file-owned two-second window-card-click dismissal watchdog solely as the compatible failure upper bound; expiry reports element identity, phase, generation, source, final existence, and wait result. The helper invocation owns cancellation and the test owns App teardown. | H observer-before-XCUI-click exact visible dismissal with exact window identity; deterministic compatible-bound plus shared initial/trigger/delayed/slow-scheduling/cancel/watchdog/200-replacement Pressure coverage, affected signed Control+Tab click UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant because production behavior, input routing, fixture topology, window identity, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GP | `FlowTabUITestSwitcherAndSearchWatchdogPolicy`, `assertElementDoesNotExistAfterTrigger`, and `testSearchPanelClickCommitsResultAndClosesPanel`; Search result-row click dismissal | The exact Browser Search result row is tapped before a raw two-second nonexistence wait is installed for the diagnostics summary. Absence is a persistent visible close Oracle, but a fast dismissal can precede observer setup, an already-absent baseline can pass without proving this Search presentation existed, and terminal policy/lifecycle ownership are implicit. The preceding exact summary/result readiness and row-frame assertions remain independent. | Resolve the exact diagnostics summary and start the shared nonexistence owner before tapping the exact Browser Search result. Require its immediate baseline readback to prove the current Search presentation exists. After the XCUI tap returns, mark trigger completion, request immediate readback, and activate the shared named serial 100ms XCUI cadence only while the exact summary remains present because Accessibility supplies no dismissal callback. Success requires exact summary absence. Retain a file-owned two-second Search-result-click dismissal watchdog solely as the compatible failure upper bound; expiry reports element identity, phase, generation, source, final existence, and wait result. The helper invocation owns cancellation and the test owns App teardown. | H observer-before-XCUI-click exact visible Search dismissal with exact result identity; deterministic compatible-bound plus shared initial/trigger/delayed/slow-scheduling/cancel/watchdog/200-replacement Pressure coverage, affected signed Search click UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant because production behavior, Search selection, input routing, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GQ | `FlowTabUITestSwitcherAndSearchWatchdogPolicy`, `launchSearchMockApplication`, and the five shared Search mock query tests; mock-runtime foreground readiness | The shared launcher passes a raw ten-second terminal bound to the existing application-ready helper after launching the mock App. The helper already succeeds only from the persistent exact `XCUIApplication.State.runningForeground` readback, including bounded activation fallback, while scenario policy ownership and final-state diagnostics are implicit. Search-input and query-result readiness remain independent contracts. | Retain the helper's immediate persistent state readback, exact `.runningForeground` success Oracle, and existing bounded activation fallback. Move the compatible ten-second terminal failure upper bound into the file-owned Search mock foreground-readiness policy and route the shared launcher through one assertion owner. Expiry reports `unmetCondition=runningForeground` and the final App state. Each launch invocation owns the synchronous platform waits and the test owns App teardown; the shared helper retains no observer or timer after return. | M policy and diagnostics around an existing exact-state readiness Oracle; compatible-policy test, all five affected signed Search mock UI paths, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, observer Pressure, and process-level Pressure are not relevant because observer infrastructure, production launch/activation behavior, Search input/results, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GR | `performAndWaitForCommittedSearchResultRow` and `testSearchPanelEntryAndResultActivation`; committed real Space-fixture App Search result publication | After typing the fixture query, the test starts a raw five-second element-existence wait for the exact App row. App-scope Search intentionally exposes App rows for an empty query, so row existence alone can be pre-trigger evidence and cannot prove the typed query reached the committed Search projection. Policy and final atomic diagnostics are also implicit. | Before typing, start the existing committed Search projection owner with acceptance gated until the trigger returns and perform an immediate baseline readback. Reject a baseline already committed to the triggered identity. After `typeText`, request an immediate readback and retain the shared named serial 100ms XCUI cadence because Accessibility supplies no atomic projection callback. Accept only one snapshot whose scope is `app`, decoded query equals the independently derived fixture query, committed result IDs contain `app:<fixture bundle ID>`, the exact fixture accessibility row exists with matching identity, and the FlowTab App is foreground. Retain a file-owned five-second publication watchdog solely as the compatible failure upper bound; expiry reports expected scope/query/result/row plus the final projection and App state. The helper invocation owns observer cancellation, while the workflow owns both Apps and fixture teardown. | H observer-before-query exact real-topology Search projection; compatible-policy plus existing query-gating, visible-row, scheduled/slow-readback, cancellation, watchdog, and stale-generation Pressure coverage, affected signed real Space-fixture Search/confirmation UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, production process Pressure, and unrelated UI are not relevant because shared owner implementation, production Search behavior, confirmation behavior, fixture identity, module wiring, configuration, and path resolution are unchanged. | completed |
| SYNC-036GS | `assertSearchMockApplicationIsForegroundReady` and `testSearchHeaderHighlightedAppChipStaysContentSizedForShortTitle`; Search-header mock-runtime foreground readiness | The layout test passes a raw ten-second bound to the shared application-ready helper after launching the same Search mock configuration owned by SYNC-036GQ. Exact `.runningForeground` state already defines readiness, but this caller bypasses the file-owned policy and final-state diagnostics. Header, highlighted-chip, and size evidence are later independent contracts. | Route this Search mock launch through the existing file-owned foreground-readiness assertion. Preserve the shared helper's immediate persistent state readback, exact `.runningForeground` success Oracle, bounded activation fallback, compatible ten-second terminal failure upper bound, and final App-state expiry diagnostic. The invocation owns synchronous platform waits and the test owns App teardown; no observer or timer remains after return. | M caller migration to an existing exact-state owner; compatible-policy test, affected signed Search-header UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, Pressure, and unrelated UI are not relevant because owner implementation, production launch/activation, layout behavior, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GT | `waitForExactElementCollection` and `testSearchHeaderHighlightedAppChipStaysContentSizedForShortTitle`; atomic Search header/highlighted-chip projection | After foreground readiness, the test performs separate raw five-second waits for the exact Search header and highlighted App chip, then reads the chip frame. Sequential success can combine elements from different Accessibility snapshots, observer setup occurs after launch presentation, and terminal policy/lifecycle diagnostics are implicit. The width bounds remain an independent layout Oracle. | Resolve both exact XCUI elements and start the existing collection owner before App launch. Require its immediate baseline readback, run launch plus exact foreground readiness as the trigger, then request an immediate atomic readback. Accept only one ordered snapshot in which both exact identifiers exist; retain the shared named serial 100ms XCUI cadence because Accessibility supplies no atomic collection callback. Use the returned resolved chip element for width readback. A file-owned ten-second projection watchdog preserves the prior two-by-five-second cumulative failure upper bound and reports both expected identifiers plus the final atomic snapshot. The helper invocation owns cancellation and the test owns App teardown. | H observer-before-launch atomic XCUI projection; compatible-policy plus existing exact/missing/order rejection, post-trigger, delayed, cancel, watchdog-final-readback, and 200-replacement Pressure coverage, affected signed Search-header layout UI, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, production process Pressure, and unrelated UI are not relevant because shared owner implementation, production launch/layout behavior, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GU | `FlowTabUITestSwitcherSearchResultObservationOwner`, `performAndWaitForCommittedSearchResultRows`, and the five shared Search mock query tests; committed mock App result-row collection | Four paths type a query before starting one raw five-second row-existence wait, while the shared `cs` path performs two sequential raw five-second waits. Empty-query App rows can exist before the trigger, sequential success can combine different projections, and row existence alone does not prove that the requested query committed. | Generalize the shared committed-result expectation and readback to a nonempty one-to-one collection of result IDs and exact accessibility row identities. Install the generation-owned observer before `typeText`, retain an initial readback, gate acceptance until the trigger returns, and request an immediate trigger readback. Accept only one foreground snapshot whose scope is `app`, decoded committed query equals the requested query, every expected result ID is present, and the full requested row-identity set exists together. Accessibility has no atomic publication callback, so retain the shared named cancellable 100ms XCUI cadence only while unresolved. Keep file-owned five-second single-result and ten-second prior cumulative multi-result watchdogs solely as terminal failure upper bounds with the final projection. The helper invocation owns cancellation and each test owns App teardown. | H shared Search snapshot/expectation plus observer-before-query atomic multi-row XCUI evidence; unique-identity/atomic/initial/gated/delayed/duplicate/out-of-order/cancel/watchdog/stale-generation Pressure rules, compatibility policy, all five affected signed mock Search paths, existing committed-result callers, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture topology, and production process Pressure are not relevant because production Search behavior, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GV | `FlowTabUITestSwitcherAndSearchWatchdogPolicy` and the six pointer hover/click tests in `FlowTabUITests+SwitcherAndSearch`; mock-runtime foreground readiness | Each pointer interaction path passes a raw ten-second terminal bound to the shared application-ready helper immediately after launch. The helper already succeeds only from the persistent exact `XCUIApplication.State.runningForeground` readback and includes a bounded activation fallback, while scenario policy ownership and final-state failure diagnostics are implicit. Switcher/Search presentation, exact card/result collection, selection, frames, and dismissal remain independent later contracts. | Add one file-owned pointer-interaction foreground-readiness watchdog retaining the compatible ten seconds solely as a failure upper bound. Route all six launches through one assertion that accepts only `.runningForeground` and reports the unmet condition plus final App state on expiry. Preserve the shared helper's immediate exact-state readback and bounded activation fallback. Each invocation owns the synchronous platform waits, each test owns App teardown, and no observer or timer survives the helper return. | M caller/policy migration around an existing state Oracle; compatible-policy test, all six affected signed pointer hover/click UI paths, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, observer Pressure, and production process Pressure are not relevant because observer infrastructure, production launch/activation/input/Search behavior, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GW | `waitForExactElementCollection` and the Option+Tab pointer hover/click paths in `FlowTabUITests+SwitcherAndSearch`; committed App-row presentation | After the notification-backed exact panel presentation helper returns, the hover path starts separate raw five-second waits for Browser then Mail, while the click path starts one raw five-second Mail wait. Observer setup occurs after the global trigger, and sequential success can combine rows from different Accessibility projections. Initial selection and later hover/click results remain independent contracts. | Resolve the requested exact App rows and start the existing generation-owned collection observer before the global trigger. Capture its immediate baseline, execute the already evidence-driven presentation helper, then request one immediate post-trigger collection readback. Accept only one ordered projection in which Browser+Mail, or Mail for the click path, exist together. Retain the shared named cancellable 100ms XCUI cadence only while unresolved because Accessibility supplies no atomic collection callback. Keep file-owned five-second single-row and ten-second prior cumulative two-row watchdogs solely as terminal failure upper bounds with final identities/existence. Each helper invocation owns cancellation and each test owns App teardown. | H observer-before-global-trigger and atomic App-row Accessibility projection; compatible-policy plus existing exact/missing/order/gated/delayed/cancel/watchdog/200-replacement Pressure rules, both affected signed Option+Tab pointer paths, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and production process Pressure are not relevant because shared owner implementation, production presentation/input behavior, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-036GX | `FlowTabUITestSwitcherDiagnosticsObservationOwner` and `testOptionTabSwitcherPointerHoverSelectsMockAppAfterMovement`; initial and moved-pointer selected-App projection | After exact Browser+Mail row presentation, the path passes separate raw three-second bounds to the shared diagnostics owner: first to infer that Browser is initially selected, then to infer that the Browser-to-Mail hover trigger committed its selection change. The exact persistent `selected` readback already determines success; terminal policy ownership is implicit. | Add one file-owned selected-App projection watchdog retaining three seconds solely as the failure upper bound. Preserve the initial helper's immediate exact diagnostics readback. For moved-pointer selection, preserve observer-before-hover setup, rejection of pre-trigger evidence, and immediate post-trigger readback. Retain the shared named cancellable 100ms XCUI cadence only while the exact value remains unresolved because Accessibility provides no diagnostics-value callback. Each helper invocation owns cancellation and the test owns App teardown; expiry reports the expected key/value and final decoded diagnostics snapshot. | H pointer-input state transition through shared diagnostics owner; compatible-policy plus existing exact parsing/match, initial, observer-before-trigger, stale-evidence rejection, delayed, cancel, watchdog-final-readback, and replacement Pressure rules, affected signed Option+Tab hover path, exact signing/process evidence, and Process/Tooling validation. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and production process Pressure are not relevant because shared owner implementation, production selection/input behavior, fixture topology, module wiring, configuration, path resolution, and hot-path work are unchanged. | completed |
| SYNC-037 | `scripts/perf/tab-switch-stress.sh`, `search-committed-index-pressure.sh`, `runtime-topology-pressure.sh`, `lib/runtime-topology-target.sh` | Sampling duration/cadence and identity stability windows are pressure/safety protocols; process termination loops use unnamed 100ms cadence and attempt bounds. Domain duration/conditional observation/watchdog. | Retain measurement durations and sample cadence as named protocol inputs. Name process polling cadence/watchdogs, check state immediately, terminate from PID/start-identity/readback, and report the final `ps`/identity/status evidence. Traps own cancellation and cleanup. | M tooling/hot path; Pressure and Process/Tooling. | completed through SYNC-037A tab-switch, SYNC-037B Search pressure, SYNC-037C runtime-topology runner-tree cleanup, SYNC-037D independent application cleanup, SYNC-037D1 exact application-process cleanup, SYNC-037E target-exit completion, and SYNC-037F target-launch readiness |
| SYNC-037A | `tab-switch-stress.sh`, `lib/process-exit-observation.sh`, and `test-process-exit-observation.sh`; tab-switch stress child termination and trap cleanup | After TERM, twenty unnamed 100ms attempts infer when the child is safe to reap or whether KILL escalation is required. A scheduling slowdown changes the observed opportunity count and diagnostics omit the expected process identity. | Capture the launched child PID and start identity, check `ps` state immediately, and accept only exact absence or zombie state before the owning shell performs `wait PID`. Retain a named 100ms conditional observation cadence while the child remains active. A named two-second monotonic watchdog only controls KILL escalation and reports the unmet exit condition, expected start identity, final PID/PPID/state/start/command record, and readback status. The synchronous trap owner supplies cancellation and reaping. Sampling duration, switch cadence, and sample cadence remain explicit pressure protocol inputs. | M perf process owner; deterministic initial/delayed/slow-scheduling/final-readback/watchdog/readback-error/identity/cancel tests, real child readiness/TERM/reap integration, normal and interrupted tab-switch Pressure, Process/Tooling. | completed |
| SYNC-037B | `search-committed-index-pressure.sh`, `lib/process-exit-observation.sh`, and `test-process-exit-observation.sh`; Search pressure test-process-tree termination and trap cleanup | After TERM and after optional KILL, two independent twenty-attempt 100ms loops watch only the root shell PID. Attempt count and scheduler availability determine escalation and completion, while surviving descendants and PID reuse lack exact evidence. | Capture and deduplicate every observed PID plus `lstart` identity before signalling. Perform an immediate exact readback before each TERM/KILL, then observe the complete captured identity set immediately and at a named 100ms cadence. Exact absence, zombie state, or a changed identity proves the captured record inactive; readback, identity-capture, and monotonic-clock failures remain unmet evidence. A named 2,000ms TERM grace only controls exact-record KILL escalation, and a separate 2,000ms watchdog bounds KILL confirmation; each reports `processTreeExited` and final PID/PPID/state/start/command evidence. The synchronous trap owns capture, signalling, observation, cancellation, `wait PID`, atomic status persistence, and cleanup. Caller-selected sampling duration and cadence remain explicit pressure protocol inputs. | M perf process-tree owner/shared helper; deterministic initial/dedup/exact-signal/slow-scheduling/cancel/final-readback/watchdog/readback-error tests, real child integration, completed and interrupted Search Pressure, Process/Tooling. | completed |
| SYNC-037C | `runtime-topology-pressure.sh`, `lib/process-exit-observation.sh`, and `test-process-exit-observation.sh`; runtime-topology UI wrapper/xcodebuild process-tree termination and trap cleanup | After TERM and after optional KILL, two independent twenty-attempt 100ms loops watch only the background wrapper shell PID. Attempt count and scheduler availability determine escalation and completion, while xcodebuild descendants and PID reuse lack exact evidence. | Capture the wrapper root immediately after launch and deduplicate every observed descendant as an exact PID/`lstart` record before signalling. Perform an immediate identity readback before TERM/KILL, then accept only collective exact-record inactivity before the owning shell performs `wait PID`. Retain a named cancellable 100ms condition cadence, a 2,000ms TERM grace used only for escalation, and a separate 2,000ms KILL-confirmation watchdog. Identity-capture, `ps`, and monotonic-clock failures remain unmet evidence with the final exact process records. The synchronous pressure-runner trap owns capture, signalling, observation, cancellation, reaping, status persistence, and cleanup. | M perf process-tree owner/shared helper; inherited deterministic initial/dedup/exact-signal/slow-scheduling/cancel/final-readback/watchdog/readback-error tests, static caller contract, signed normal and interrupted UI Pressure, Process/Tooling. | completed |
| SYNC-037D | `runtime-topology-pressure.sh`, `lib/runtime-topology-application-lifecycle.js`, `runtime-topology-evidence.py`, and application-lifecycle contract fixtures; LaunchServices-launched FlowTab/Space fixture cleanup | An interrupted xcodebuild tree prevents XCTest teardown, while the exact FlowTab target and fixture application are already reparented to launchd and therefore outlive the runner tree. Cleanup has no owner for these independent identities. | Before starting the UI wrapper, capture exact pre-existing target and every configured fixture bundle identity. Resolve the frozen target path and generated workflow paths at their owning resource boundary, then select only expected post-baseline bundle/PID/launch-date/bundle-path/executable-path identities. Register `NSWorkspaceDidTerminateApplicationNotification` before termination requests, perform an initial readback, request graceful then exact force termination when required, and accept success only from final exact absence readback. The pressure invocation owns observation, cancellation, evidence persistence, and cleanup. Named two-second graceful and force watchdogs only bound escalation/failure and report the unmet condition plus last evidence. | H independent application lifecycle and user-process safety; deterministic initial-state/event/duplicate/out-of-order/slow-scheduling/cancel/watchdog/readback rules, real same-path two-instance preservation/force integration, signed normal and interrupted fixture UI Pressure, Process/Tooling. | completed |
| SYNC-037D1 | `runtime-topology-pressure.sh`, `lib/runtime-topology-application-lifecycle.js`, `lib/runtime-topology-application-process-cleanup.js`, and `test-runtime-topology-application-lifecycle.js`; exact OS-process exit after LaunchServices cleanup | Final `NSRunningApplication` absence was treated as process exit, but six prior lifecycle-fixture applets remained active under launchd after LaunchServices stopped reporting them. Evidence migration plus conditional observation and watchdog. | Capture `/bin/ps` PID/start identity while each initial application identity is observable. After LaunchServices reaches exact absence, immediately read every captured OS identity; absence, zombie state, or changed start identity alone proves inactivity. Re-read identity immediately before exact KILL, then require a final collective readback. Readback errors remain unmet. The pressure trap owns both ordered phases, atomic evidence, cancellation, and cleanup. Retain a named cancellable 50ms cadence only while a captured process is active and a two-second monotonic watchdog solely as a terminal failure bound with the last PID/PPID/state/start/command evidence. | H user-process safety and interrupted UI cleanup; deterministic initial/classification/PID-reuse/slow-scheduling/cancel/watchdog/final-readback rules, real forced child and same-path app integration, signed normal and interrupted UI Pressure, Process/Tooling. | completed |
| SYNC-037E | `runtime-topology-pressure.sh.observe_ui_process_tree_completion_after_target_exit`; target-loss to UI-wrapper completion | A thirty-second grace is implemented as up to 300 raw 100ms iterations over only the root wrapper PID. Scheduler availability changes the observation count and a reparented or reused process lacks exact evidence. | Reuse the captured wrapper process-tree identities and collective immediate readback. Retain a named target-exit grace solely as a terminal failure bound, with a named cancellable cadence while any exact record remains active. Success comes only from exact tree inactivity followed by `wait PID`; failure reports target-loss evidence and every active process record. The sampling loop owns observation and cleanup. | M pressure orchestration; deterministic immediate/delayed/slow-scheduling/cancel/watchdog/PID-reuse tests, target-loss Pressure, Process/Tooling. | completed |
| SYNC-037F | `lib/runtime-topology-target.sh.await_target_launch`; post-request target launch and stable identity qualification | Up to 450 raw 100ms iterations combine target discovery with a five-second identity-stability interval. The stability window is a safety protocol, while attempt count makes the overall watchdog and observation opportunities scheduler-dependent. | Retain the five-second stable-identity window as a named safety protocol. Capture a pre-request identity baseline, inspect immediately after the launch trigger, and use a named cancellable cadence only because the shell/LaunchServices boundary supplies no reliable callback. Drive the launch watchdog and stability interval from monotonic time, and qualify only one post-request path/bundle/executable/designated-requirement identity that remains unchanged through final readback. Report the last candidates, baseline, elapsed stability, and readback errors. The target helper invocation owns cadence cancellation and receipt publication. | H runtime target identity/sampling boundary; deterministic initial/baseline/ambiguous/PID-reuse/slow-scheduling/cancel/watchdog tests, signed UI Pressure, Process/Tooling. | completed |
| SYNC-038 | `scripts/release/release-install.sh`, `scripts/release/uninstall-flowtab.js` | A fixed one-second delay assumes FlowTab exited before replacement or deletion. Evidence migration. | Wait on exact process absence/identity readback immediately after quit/TERM, with a named watchdog and last PID/state diagnostic before mutating installed resources. The install/uninstall command owns cleanup. | M release tooling; Process/Tooling and release contract tests. | completed through SYNC-038A installer and SYNC-038B uninstaller |
| SYNC-038A | `release-install.sh`, `lib/process-exit-observation.sh`, and `test-process-exit-observation.sh`; installed-app replacement process boundary | A fixed one-second sleep after AppleScript quit and `pkill` assumes every `FlowTab` process has exited before permission reset, build, and eventual application replacement. | Read the exact process name immediately through `pgrep`, enrich every observed PID with `ps` PID/PPID/state/start/command identity, and treat readback errors as unmet evidence. Retain a named 100ms cancellable shell cadence only while records remain. Recheck immediately before removing the installed bundle. The release command owns the synchronous wait and interruption cleanup; its ten-second watchdog reports the unmet absence condition and final process records. | M release process/tooling; deterministic immediate/delayed/watchdog/readback-error contract tests, real process-list integration, static mutation-order contract, Process/Tooling. | completed |
| SYNC-038B | `uninstall-flowtab.js`, `test-uninstall-flowtab-process-exit.js`, and `test-uninstall-flowtab-workspace-observer.jxa`; DMG uninstaller process and installed-app deletion boundary | The JXA applet advances a fixed one-second delay after quit and exact-name `pkill`, then treats elapsed time as proof that user data and the installed bundle can be removed. | Register `NSWorkspaceDidTerminateApplicationNotification` before the quit request, capture an initial exact bundle-ID readback, deduplicate exact PID/launch-date/executable identities into a monotonic generation, wake the owning RunLoop on the target event, and require a final `NSRunningApplication` absence readback. Keep the observer alive across cleanup and administrator authentication, then place an immediate exact-name `pgrep`/`ps` guard inside the privileged command before bundle removal. The applet invocation owns observer cancellation. A ten-second monotonic watchdog is only a failure bound and reports the unmet condition, final application records, and last notification. | M release process/tooling; deterministic initial/event/cancel/duplicate/out-of-order/readback-error/watchdog/slow-scheduling rules, real signed JXA fixture termination event, privileged guard present/absent integration, existing cleanup boundary, Process/Tooling. | completed |
| SYNC-039 | `RuntimeWindowRecencyTracker`; semantic fallback age and generation age | A 300-second age is an explicit expiry rule for weak semantic identity; exact identity remains preferred. Domain duration. | Retain injected clock, maximum generation age, exact-window identity precedence, and deterministic expiry tests. Tracker owns records and pruning. | H identity semantics; Unit, Behavior, relevant UI only if implementation changes. | closed-retained |

## UI Fixed-Settle Audit Scope

SYNC-034 and SYNC-035 cover the direct RunLoop waits found in:

- `FlowTabUITests+HomeAndLogs.swift`
- `FlowTabUITests+ScrollingSupport.swift`
- `FlowTabUITests+Settings.swift`
- `FlowTabUITests+SettingsSearch.swift`
- `FlowTabUITests+SpaceFixtureApp.swift`
- `FlowTabUITests+SpaceFixtureEdgeInputsAssertions.swift`
- `FlowTabUITests+SpaceFixtureEdgeInputsWorkflow.swift`
- `FlowTabUITests+SpaceFixtureInAppWindowSwitcher.swift`
- `FlowTabUITests+SpaceFixtureMultiAppWorkflow.swift`
- `FlowTabUITests+SpaceFixtureOptionTabNoisyRoundTrip.swift`
- `FlowTabUITests+SpaceFixtureOptionTabSpaceBacked.swift`
- `FlowTabUITests+SpaceFixtureRuntimeTruthEntrypoints.swift`
- `FlowTabUITests+SpaceFixtureSwitcherMultiAppWorkflow.swift`
- `FlowTabUITests+SpaceFixtureWorkflow.swift`
- `FlowTabUITests+StatusItem.swift`
- `FlowTabUITests+Support.swift`
- `FlowTabUITests+SwitcherAndSearch.swift`
- `FlowTabUITests+SwitcherInteractionRegressions.swift`
- `FlowTabUITests+SystemAppMRU.swift`
- `FlowTabUITests+WorkflowWindowObservation.swift`

Every call site will be closed as one of:

1. removed in favor of an event/notification/generation/process/window Oracle;
2. routed through the shared conditional observer because the platform exposes
   no usable event; or
3. retained as a named domain duration or terminal watchdog with explicit
   ownership and deterministic strategy.

## Dependency Order

1. Deterministic runtime evidence and shared generations: SYNC-001–SYNC-006.
2. App projection, permission, and view lifecycle consumers:
   SYNC-009–SYNC-013, the split Home contracts SYNC-040–SYNC-041, then
   SYNC-014.
3. Search, hotkey, panel, and window-layer orchestration:
   SYNC-015–SYNC-025.
4. Fixture and TestingSupport readiness: SYNC-026–SYNC-031.
5. App/UI test observation and watchdog infrastructure:
   SYNC-032–SYNC-036.
6. Pressure/release tooling: SYNC-037–SYNC-038.
7. Re-run the complete source audit and confirm all retained policies,
   including SYNC-007, SYNC-008, and SYNC-039.

Dependencies may force a later row to add its helper before an earlier consumer
is closed. Each commit still contains only one synchronization contract under
one lifecycle owner.

## Per-Slice Closure Record

When a row moves to `completed` or `blocked`, append these fields to the row or
an immediately following note:

- production/test design and independent Oracle;
- cancellation and cleanup owner;
- retained duration/watchdog policy and last-observation diagnostic;
- targeted validation commands and results by Unit, Behavior, UI, Pressure,
  and Process/Tooling;
- local commit SHA.

The final source audit must find no unclassified duration, wait, retry,
polling cadence, deadline, or timeout in the scoped paths.

### SYNC-001 Closure Record

- Design and Oracle: each named scan policy owns a deterministic element-ID
  range. The resolver publishes `complete(scanned:)` only after visiting the
  whole range, and publishes `unavailable` when the private resolver symbol
  cannot supply observations. Window absence remains authoritative only for a
  completed scan.
- Lifecycle: the synchronous resolver invocation owns the bounded traversal.
  This slice adds no observer, timer, retry, wait task, or production watchdog.
- Retained time policy: none in the production contract. Test semaphore
  deadlines are terminal failure watchdogs around event-driven
  suspend/resume evidence.
- Unit: three deterministic scan-policy, full-traversal, and
  suspend/resume tests passed in
  `.build-local/evidence-driven-sync/SYNC-001/flowtabtests-targeted-attempt-001`.
- Behavior: six inspector, absence-authority, remote-scan-decision, and
  off-Space window-binding tests passed across the targeted and expanded
  FlowTabTests attempts.
- UI and Pressure: the required Noisy Option+Tab real-topology scenario was
  attempted twice. Both attempts built fixtures, signed the runner, and
  produced valid result bundles, then macOS LocalAuthentication returned code
  `-4` (`System authentication is running`) before the test body. Evidence is
  preserved under
  `.build-local/evidence-driven-sync/SYNC-001/pressure-attempt-001` and
  `pressure-attempt-002`.
- Process/Tooling: `git diff --check` passed. The owner-scope source search
  found no elapsed clock cutoff or partial-scan completion path.
- Commit: `695f740` (`refactor(sync): migrate SYNC-001 remote AX scan`).

### SYNC-002 Closure Record

- Design and Oracle: `AppDelegate` installs the PID-scoped AX observation before
  forwarding the workspace launch event. The launch repair supplies the initial
  readback. Each later supported AX window transition supplies evidence for an
  `.axNotification` repair, while a delayed observer installation triggers an
  explicit post-install readback. Early consecutive AX transitions receive
  monotonic generations and use SYNC-004's standard trailing-readback batching.
  Observer callbacks are accepted only from the current context for the exact
  appID/PID.
- Lifecycle: `RuntimeAppLaunchWindowEvidenceCoordinator` owns observer-install
  generation and retry cancellation. Exact appID/PID termination removes one
  observer; stale termination for a reused PID is ignored. App termination
  cancels the PID retry, and AppDelegate termination cancels every retry and
  stops the monitor.
- Retained time policy: the platform exposes no readiness callback for a failed
  AX observer installation, so
  `runtimeAppLaunchObserverInstallRetryIntervalNanoseconds` retains a 250ms
  cancellable condition-observation cadence. Every attempt checks installation
  evidence directly; elapsed time never establishes success. Cancellation logs
  the last AX installation error.
- Unit and Behavior: ten immediate-install, delayed-install/readback,
  consecutive-event, cancellation, shutdown, superseded-generation, PID-reuse,
  AppDelegate-ordering, and projection-repair tests passed in
  `.build-local/evidence-driven-sync/SYNC-002/flowtabtests-targeted-attempt-005`.
  Six existing AX identity and runtime-drainer tests passed in
  `.build-local/evidence-driven-sync/SYNC-002/flowtabtests-expanded-attempt-006`.
- FlowTabCore: not relevant because the contract is owned by AppKit,
  ApplicationServices, AppDelegate, and the app runtime projection boundary.
- UI and Pressure: the real-fixture launch/termination scenario was attempted
  twice through `runtime-topology-pressure.sh`. Both attempts completed fixture
  preparation, fixed-App identity validation, runner signing, and result-bundle
  creation. macOS LocalAuthentication then returned code `-4`
  (`System authentication is running`) before the test body, so no target
  FlowTab process could be bound and no pressure samples could be attributed.
  Evidence is preserved under
  `.build-local/evidence-driven-sync/SYNC-002/pressure-attempt-001` and
  `pressure-attempt-002`; the latter was built from the final slice source
  state with private identity-manifest SHA-256
  `cf855c34361ee17c2309bedb385a0f1aca535e2dc68b02e7916bf07231a2f322`.
- Process/Tooling: `git diff --check` passed. The owner-scope source search
  found no app-launch convergence scheduler, 800ms delay, or delayed
  `.appLaunched` success path.
- Commit: `d267c49` (`refactor(sync): migrate SYNC-002 app launch readiness`).

### SYNC-003 Closure Record

- Design and Oracle: a dirty repair request is read back immediately. A
  non-empty repair result remains the sole success Oracle. An incomplete
  current-app window payload moves the request to `waitingForEvidence`; exact
  AX window, Space-topology, focus, launch, and lifecycle signals make it
  pending for an immediate readback. Unrelated app-switcher maintenance cannot
  advance it. Request ID and attempt reject stale or out-of-order callbacks,
  while exact appID/PID prevents a stale termination from cancelling a reused
  PID's request.
- Lifecycle: `RuntimeProjectionService` owns
  `RuntimeTransientRepairObservationDriver` on its maintenance queue. The
  driver owns both scheduled readbacks and the watchdog. Completion, matching
  evidence, request replacement, search-barrier promotion, exact app
  termination, and service deinitialization cancel the applicable work.
- Retained time policy: the platform does not expose a completion callback for
  an AX repair payload that is transiently empty. The named condition cadence
  is 100ms, 300ms, then 800ms repeated. It only requests another readback. A
  30-second watchdog is the terminal failure bound for one uninterrupted
  incomplete-evidence session; expiry cancels polling, preserves the stale
  projection state, and reports request ID, target, appID, attempt, the unmet
  non-empty-payload condition, the last transient-empty payload, and
  `lastObservedAt`.
- Unit and Behavior: fifteen immediate-readback, active condition-readback,
  event delivery, stale/out-of-order callback, cancellation, lifecycle,
  PID-reuse, priority, search-freshness, full-repair, cadence, and watchdog
  tests passed in
  `.build-local/evidence-driven-sync/SYNC-003/flowtabtests-targeted-attempt-010`.
  The complete `FlowTabPriorityCoverageTests` suite passed 526/526 in
  `.build-local/evidence-driven-sync/SYNC-003/flowtabtests-expanded-attempt-011`.
- FlowTabCore: not relevant because the contract is owned by the app runtime's
  AX/AppKit projection boundary and does not change the pure Core package.
- UI and Pressure: the final-source Noisy Option+Tab real-topology pressure
  attempt completed fixture preparation, fixed-App identity validation,
  current-source app installation, runner build/signing, and valid result
  bundle creation. macOS LocalAuthentication then returned code `-4`
  (`System authentication is running`) before the test body. No target FlowTab
  process could be bound and zero pressure samples were attributed. Evidence is
  preserved under
  `.build-local/evidence-driven-sync/SYNC-003/pressure-final-attempt-009`; its
  private fixed-App identity-manifest SHA-256 is
  `04c8a53b4b91212f5faa7b63c1883cacfcd6c1b19f8db3babd94d4e46d672ce4`.
- Process/Tooling: `git diff --check` and project-file `plutil -lint` passed.
  The owner-scope search found no `notBefore`, time-ready request selection,
  retry-exhaustion success/fallback, or legacy retry-policy symbol.
- Commit: `de5f778`
  (`refactor(sync): migrate SYNC-003 transient repair readiness`).

### SYNC-004 Closure Record

- Design and Oracle: `RuntimeAXWindowChangeMonitor` binds the PID generation
  and installs its observer before taking the initial `kAXWindows` readback.
  That readback compares every public switchable AX element with a distinct
  exact element in the existing live registry, while allowing the registry to
  contain remote AX windows that the public attribute omits. A matching
  baseline requires no repair. A changed or unavailable baseline and each
  observed AX transition request reconciliation; the consumer's projection
  and exact-window readback remain the sole success Oracles. Notification
  generations are monotonic per current appID/PID binding, and a quiet burst
  publishes one trailing evidence value with the latest generation and
  observed-transition count.
- Lifecycle: `RuntimeAXWindowChangeMonitor` owns AX observer registration,
  per-window notification registration, exact appID/PID bindings, and removal.
  `RuntimeAXWindowChangeDeliveryCoordinator` owns binding generations and one
  cancellable trailing task per PID. Unbind, monitor stop, PID reuse, task
  cancellation, and stale binding generations cancel or reject pending
  delivery.
- Retained time policy: `standardCoalesced` names a 160ms trailing batching
  cadence. Each new transition cancels and replaces the pending task, and an
  injected scheduler makes cancellation deterministic in tests. Scheduler
  delay changes only when the trailing readback is requested; completion and
  success remain defined by the resulting projection/window evidence.
- Unit and Behavior: the final 14-test focused set passed 14/14, covering exact
  initial identity, public-AX subset semantics, changed/unavailable readback,
  first-event capture, single and burst delivery, generation ordering,
  cancellation, stop, and PID rebinding in
  `.build-local/evidence-driven-sync/SYNC-004/flowtabtests-targeted-attempt-022`.
  The final complete `FlowTabPriorityCoverageTests` suite passed 540/540 in
  `.build-local/evidence-driven-sync/SYNC-004/flowtabtests-expanded-attempt-023`.
- FlowTabCore: not relevant because this synchronization contract belongs to
  the AppKit/ApplicationServices runtime projection boundary.
- UI: the real Home fixture mutation path
  `testRuntimeLifecycleRefreshesRealFixtureWindowSetMutation` passed 1/1 in
  23.305 seconds under
  `.build-local/evidence-driven-sync/SYNC-004/ui-home-mutation-attempt-017`.
  Permission validation reused the same prelaunched FlowTab process so every
  assertion remained bound to the tested app identity.
- Pressure: the canonical noisy-CG-sibling, cross-Space fullscreen Option+Tab
  workflow passed 1/1 in 49.844 seconds under
  `.build-local/evidence-driven-sync/SYNC-004/pressure-final-attempt-018`.
  The exact fixed-App identity contract matched across 70 checks and 5,047.861
  milliseconds, with 26 candidate observations and zero rejected transient
  identities. Resource sampling recorded CPU average/p95/max
  30.99/55.70/71.40 percent and RSS average/p95/max
  169.65/241.41/269.47 MiB. The private identity-manifest SHA-256 is
  `6d859fff6486a2b16b955d2e8739f5b9fce4a434a392d9a14a2064d635aeb800`.
- Process/Tooling: `git diff --check` and project-file `plutil -lint` passed.
  Owner-scope search found no warm-up timestamp, elapsed throttle, or
  zero-cadence delivery branch; the sole scheduled duration is the named,
  injected trailing batching policy.
- Commit: `8b593e8`
  (`refactor(sync): migrate SYNC-004 AX window evidence delivery`).

### SYNC-005 Closure Record

- Design and Oracle: the bridge issues the two existing Chrome window-index
  focus requests and `activate`, then immediately returns Chrome's exact
  front-window ID. The bridge accepts only equality with the requested Chrome
  window ID. A different ID, missing/invalid result, or AppleScript execution
  error remains explicit evidence for `RuntimeActivator`, whose existing exact
  AX/CG `verifyFocusAttempt` readback is the final activation Oracle. SYNC-006
  owns any later convergence for the activation request.
- Lifecycle: one synchronous bridge invocation owns one command/readback
  transaction. It allocates no observer, timer, retry, asynchronous task, or
  watchdog. `RuntimeActivator` owns the surrounding activation generation and
  its subsequent cancellation.
- Retained time policy: none in this bridge contract. Machine scheduling and
  Chrome event handling can change command completion latency; acceptance
  remains determined by the exact returned window ID.
- Unit and Behavior: the final nine-test focused set passed 9/9, covering
  generated AppleScript compilation and command/readback ordering, exact and
  mismatched IDs, execution failure, empty/invalid readback, browser
  applicability, and ambiguous/candidate identity rules in
  `.build-local/evidence-driven-sync/SYNC-005/flowtabtests-targeted-attempt-002`.
  The final complete `FlowTabPriorityCoverageTests` suite passed 542/542 in
  18.292 seconds under
  `.build-local/evidence-driven-sync/SYNC-005/flowtabtests-expanded-attempt-003`.
- FlowTabCore: not relevant because this contract belongs to the
  AppKit/AppleScript runtime activation boundary.
- UI and Pressure: the canonical noisy-CG-sibling, cross-Space fullscreen
  Option+Tab workflow passed 1/1 in 51.205 seconds under
  `.build-local/evidence-driven-sync/SYNC-005/pressure-attempt-006`. It
  validates the downstream exact-window activation/readback topology with the
  isolated Chrome-shaped fixture while leaving personal Chrome state
  untouched. The fixed-App identity contract matched across 72 checks and
  5,047.456 milliseconds, with 23 candidate observations and zero rejected
  transient identities. Resource sampling recorded CPU average/p95/max
  34.46/61.20/66.60 percent and RSS average/p95/max
  169.28/238.70/283.23 MiB. The private identity-manifest SHA-256 is
  `4af743703ca0a86772a81f728b4ed64f33a1c8672343b77894f1f4ed4040e1fc`.
  Attempt 005 independently reached exact AX focus verification, then its
  existing binding-transition log assertion missed the expected diagnostic;
  the clean retry closed the same path with the same source state.
- Process/Tooling: `git diff --check` and project-file `plutil -lint` passed.
  Owner-scope search found no bridge propagation duration or AppleScript
  `delay`; `NSAppleScript(source:)` compiled the generated script in the
  focused test.
- Commit: `dfc3389`
  (`refactor(sync): migrate SYNC-005 Chrome focus readback`).

### SYNC-006 Closure Record

- Design and Oracle: `RuntimeFocusRecoveryCoordinator` installs exact-PID app
  activation and termination observers plus active-Space and exact-app
  projection observers before the first activation request. It then performs
  an immediate readback. Every observed event requests another readback; only
  the named condition-polling fallback may reissue the existing recovery
  action. Success requires the exact requested AX/CG identity to be read back
  as focused, or as the frontmost valid CG window when AX focus evidence is
  unavailable. `RuntimeWindowFocusReadbackEvidence` carries the same atomic
  AX/CG observation from verification into projection reporting, so a second
  accessibility query cannot discard already-observed exact focus.
- Lifecycle: `RuntimeActivator` owns one coordinator and activation
  generation. The coordinator owns its observer registrations, scheduled poll,
  and watchdog. Supersession, exact-PID termination, explicit cancellation,
  successful readback, watchdog expiry, and deinitialization remove observers
  and cancel scheduled work. Stale generations and unrelated PID/appID events
  cannot advance the current request.
- Retained time policy: macOS exposes no target-window activation completion
  callback spanning AX, CG, Space changes, and browser focus. The named
  fallback cadence checks at 50ms, 150ms, then 500ms repeatedly; its scheduler
  is injected and cancellable, and elapsed time never establishes success. A
  five-second watchdog performs a final readback and then reports the unmet
  exact-target condition together with the last AX/CG observation.
- Unit and Behavior: the final focused set passed 15/15 in 0.365 seconds,
  covering initial-state completion, observer-before-action ordering, exact
  event delivery, unrelated/duplicate/out-of-order rejection, fallback
  cadence, cancellation, termination, supersession, watchdog diagnostics,
  slow scheduling, projection orchestration, and single-read exact-evidence
  propagation under
  `.build-local/evidence-driven-sync/SYNC-006/flowtabtests-focused-attempt-010`.
  The complete `FlowTabPriorityCoverageTests` suite passed 553/553 in 17.954
  seconds under
  `.build-local/evidence-driven-sync/SYNC-006/flowtabtests-priority-attempt-011`.
- FlowTabCore: not relevant because this contract belongs to the
  AppKit/ApplicationServices runtime activation and projection boundary.
- UI: the exact-window activation workflow passed 1/1 in 51.121 seconds under
  `.build-local/evidence-driven-sync/SYNC-006/ui-activation-attempt-013`.
  The installed fixed app was rebuilt from the final source state and validated
  against private identity-manifest SHA-256
  `65f73336adbba72ab1c9c8554952797e113bb85ef23ba377d5880d21550f49bc`.
- Pressure: the canonical noisy-CG-sibling, cross-Space fullscreen Option+Tab
  workflow passed 1/1 in 55.787 seconds under
  `.build-local/evidence-driven-sync/SYNC-006/runtime-topology-pressure-attempt-016`.
  The exact fixed-App identity contract matched across 79 checks and 5,009.848
  milliseconds, with 23 candidate observations and zero rejected transient
  identities. Resource sampling recorded CPU average/p95/max
  31.29/54.40/65.30 percent and RSS average/p95/max
  170.48/258.70/291.58 MiB.
- Process/Tooling: `git diff --check` and project-file `plutil -lint` passed.
  Owner-scope search found no legacy recovery-delay policy or retry-chain
  symbol. The retained poll cadence and watchdog are named, injected,
  cancellable policies whose tests advance by explicit scheduler delivery.
- Commit: `96daf6f`
  (`refactor(sync): migrate SYNC-006 activation convergence`).

### SYNC-009 Closure Record

- Design and Oracle: `RuntimeLogFileStore` publishes monotonic append, durable
  flush, and successful clear generations from its serialized storage queue.
  `RuntimeLogLinesViewModel` installs the observer before capturing its
  baseline generation and requesting the initial readback. Later generations
  are coalesced while a read is in flight; a read started for stale evidence
  cannot replace lines from a newer generation. The independently read and
  level-filtered persisted lines remain the only UI-content Oracle.
- Lifecycle: the diagnostics store owns observer registration and serialized
  removal. The view model owns one observation token, one coalesced reload
  task, and a view-lifecycle generation. Stop, disappearance, deinitialization,
  restart after a level change, and stale generations cancel or reject work.
  Clear keeps the established observer, applies only its returned clear
  generation, and verifies subsequent content through a new readback.
- Retained time policy: none. The former one-second refresh cadence was
  removed. XCTest and XCUI watchdogs terminate failed assertions; they never
  establish append, clear, or display success.
- Unit and Behavior: the final-source focused observation set passed 4/4 under
  `.build-local/evidence-driven-sync/SYNC-009/flowtabtests-targeted-attempt-023`.
  It covers observer-before-read ordering, an initially satisfied state,
  append/flush/clear generation ordering, clear readback, continued
  subscription after clear, cancellation, and stale, duplicate, out-of-order,
  and slow-read evidence. The complete `FlowTabTests` suite passed 252/252
  under
  `.build-local/evidence-driven-sync/SYNC-009/flowtabtests-full-attempt-021`;
  the complete `FlowTabPriorityCoverageTests` suite passed 553/553 under
  `.build-local/evidence-driven-sync/SYNC-009/flowtabtests-priority-attempt-022`.
- FlowTabCore: not relevant because this synchronization contract belongs to
  the app diagnostics storage and SwiftUI view-lifecycle boundary.
- UI: the visible Logs-page append path passed 1/1 in 11.696 seconds under
  `.build-local/evidence-driven-sync/SYNC-009/ui-live-log-attempt-014`.
  The test observes seeded persisted content, clears to the empty-state
  readback, emits a runtime log stimulus, and succeeds only when the exact new
  accessibility row appears while the page remains visible.
- Pressure: the canonical Logs-page lifecycle workload passed for 60 seconds
  at a 20ms switch cadence under
  `.build-local/evidence-driven-sync/SYNC-009/tab-switch-pressure-attempt-019-60s-20ms`.
  It completed 122 resource samples with CPU average/p95/max
  58.00/62.30/76.60 percent and RSS average/p95/max
  107.72/119.64/119.84 MiB. The last 30 seconds had a negative RSS slope; the
  last 20 seconds stabilized at 108.47 MiB with a 0.0091 MiB/sample slope.
  Same-machine parent-commit repetitions selected the same approximately
  90–121 MiB allocator high-water range, while current and parent CPU averages
  remained within 0.6 percentage points.
- Process/Tooling: the final app source built through the fixed-app installer
  and the pressure harness. `git diff --check` and project-file `plutil -lint`
  passed. Owner-scope search found no Logs-content polling interval or refresh
  sleep; the remaining diagnostic-session expiration wakeup belongs to
  SYNC-010.
- Commit: `d9bf79a`
  (`refactor(sync): migrate SYNC-009 runtime log observation`).

### SYNC-010 Closure Record

- Design and Oracle: `RuntimeDiagnosticSessionDeadlineCoordinator` replaces
  the one-second view loop with one scheduled next-state deadline. It reads an
  injected wall clock immediately when started and after every wake. A wake
  before the persisted expiration schedules the next exact display transition;
  only a clock value at or beyond the persisted expiration invokes session
  cleanup. Delayed scheduling can postpone the UI update, while the stored
  deadline and current clock readback continue to define diagnostic eligibility.
- Lifecycle: `RuntimeLogsSection` owns the coordinator as view state. The
  coordinator owns one cancellable token and one observation generation.
  Session restart, persisted-deadline replacement, user stop, view
  disappearance, deinitialization, and expiration cancel or invalidate the
  token. A stale or cancelled callback cannot clear a newer session.
- Retained time policy: `RuntimeDiagnosticSessionStore.duration` remains the
  15-minute product expiration contract. The named deadline policy retains a
  60-second displayed-minute unit solely to update the existing “about N
  minutes” text at exact value transitions. Neither duration establishes
  success. The injected clock supplies the expiration readback; the injected
  scheduler controls only when that readback occurs.
- Unit and Behavior: the focused deadline/store set passed 7/7 under
  `.build-local/evidence-driven-sync/SYNC-010/flowtabtests-targeted-attempt-002`,
  covering exact countdown transitions, an initially expired state, an early
  wake, a delayed wake, persistence cleanup, cancellation, supersession, and
  stale-callback rejection. Two hosted Logs-view integration tests passed 2/2
  under
  `.build-local/evidence-driven-sync/SYNC-010/flowtabtests-view-attempt-003`.
  The final complete `FlowTabTests` suite passed 258/258 under
  `.build-local/evidence-driven-sync/SYNC-010/flowtabtests-full-attempt-006`;
  the complete `FlowTabPriorityCoverageTests` suite passed 553/553 under
  `.build-local/evidence-driven-sync/SYNC-010/flowtabtests-priority-attempt-007`.
- FlowTabCore: not relevant because this product-duration contract belongs to
  app preferences and the SwiftUI Logs-page lifecycle.
- UI: the real Logs-page workflow passed 1/1 in 35.886 seconds under
  `.build-local/evidence-driven-sync/SYNC-010/ui-diagnostic-session-attempt-002`.
  It independently verifies seeded persisted content and filtering before
  enabling diagnostics, then requires the active-session status to appear,
  requires it to disappear after user cancellation, clears the logs, and
  verifies the cleared state survives relaunch.
- Pressure: 2,000 rapid deadline replacements retained exactly one available
  token; stopping and invoking every stale callback in reverse order produced
  no expiration under
  `.build-local/evidence-driven-sync/SYNC-010/flowtabtests-pressure-attempt-004`.
  The canonical 20-second Logs-page lifecycle workload at a 20ms switch cadence
  also passed under
  `.build-local/evidence-driven-sync/SYNC-010/tab-switch-pressure-attempt-005`.
  Its 42 samples recorded CPU average/p95/max 55.83/62.80/68.30 percent and RSS
  average/p95/max 98.08/117.02/117.17 MiB. The final 20-sample RSS slope was
  -1.014 MiB/sample, matching the same-machine SYNC-009 baseline range without
  warm-state growth.
- Process/Tooling: `git diff --check` and project-file `plutil -lint` passed.
  Owner-scope search found no one-second expiration loop. The only sleep in the
  contract is inside the injected, cancellable deadline scheduler; its result
  is always revalidated by the clock.
- Commit: `6b7a6d2`
  (`refactor(sync): migrate SYNC-010 diagnostic session deadline`).

### SYNC-012 Closure Record

- Design and Oracle: `RuntimePermissionObservationCoordinator` installs its
  app-activation observer before taking the initial TCC readback. A Settings
  permission action starts that observation before issuing the system request,
  then takes an independent request-return readback. App activation and the
  fallback cadence only trigger further readbacks; a `true` accessibility or
  screen-capture readback is the sole success Oracle. Scheduling delay can
  postpone the visible update without changing the permission result.
- Lifecycle: `AppSettingsView` owns one coordinator as view state. Each target
  has an observation generation, fallback token, and watchdog token; both
  targets share one generation-guarded activation subscription. Supersession,
  Settings deactivation, view disappearance, success, watchdog expiration,
  cancellation, and coordinator deinitialization cancel or invalidate every
  owned resource. Cancelled and out-of-order callbacks cannot update a newer
  observation.
- Retained time policy: TCC provides no unified permission-transition
  notification, so the named 500ms fallback cadence is retained as controlled
  condition polling. It follows the immediate readback, is cancellable, and is
  owned by the observation coordinator. The 20-second request watchdog is only
  a terminal failure bound; its final readback can still succeed after delayed
  scheduling, while failure diagnostics include the unmet target, generation,
  readback count, elapsed time, final evidence, bundle identifier, and bundle
  path. Existing Settings navigation animation remains a presentation
  duration.
- Unit and Behavior: the final focused permission set passed 11/11 under
  `.build-local/evidence-driven-sync/SYNC-012/flowtabtests-targeted-attempt-005`.
  It covers observer-before-readback ordering, an initially satisfied state,
  activation and fallback evidence, independent request readback, cancellation,
  supersession, stale callbacks, both owned-state transitions, delayed
  scheduling, watchdog diagnostics, dynamic fixture readback, and 2,000 rapid
  replacements. The complete `FlowTabTests` suite passed 266/266 under
  `.build-local/evidence-driven-sync/SYNC-012/flowtabtests-full-attempt-006`;
  the complete `FlowTabPriorityCoverageTests` suite passed 553/553 under
  `.build-local/evidence-driven-sync/SYNC-012/flowtabtests-priority-attempt-007`.
- FlowTabCore: not relevant because the permission request, TCC readback,
  Settings lifecycle, and test-fixture boundary all belong to the app target.
- UI: the real Settings permission workflow passed 1/1 under
  `.build-local/evidence-driven-sync/SYNC-012/ui-permission-observation-attempt-003`.
  It starts with two denied statuses, triggers both permission actions, changes
  an external atomic TCC fixture, and requires both exact granted statuses plus
  both `fallbackReadback` evidence logs. Three existing permission action,
  Simplified Chinese status, and English granted-label regressions passed 3/3
  under
  `.build-local/evidence-driven-sync/SYNC-012/ui-settings-permission-regression-attempt-004`.
- Pressure: 2,000 rapid replacements retained exactly one activation
  subscription and one fallback/watchdog pair; cancellation followed by every
  stale callback produced no additional evidence in the focused set. The
  canonical 20-second tab-switch workload at a 20ms cadence passed under
  `.build-local/evidence-driven-sync/SYNC-012/tab-switch-pressure-20ms-attempt-010`.
  Its 41 samples recorded CPU average/p95/max 61.06/66.30/118.30 percent and RSS
  average/p95/max 115.00/140.98/141.08 MB; the final 20-sample RSS slope was
  -0.438 MB/sample. The 50ms comparison passed under
  `.build-local/evidence-driven-sync/SYNC-012/tab-switch-pressure-50ms-attempt-011`
  with CPU 59.55/65.90/85.80 percent, RSS 103.21/128.64/129.16 MB, and a
  -0.686 MB/sample final slope. An exact parent-commit comparison at `6b7a6d2`
  passed under
  `.build-local/evidence-driven-sync/SYNC-012/tab-switch-parent-6b7a6d2-20ms-attempt-012`
  with CPU 60.53/73.50/83.30 percent, RSS 105.78/134.12/135.88 MB, and a
  -0.074 MB/sample final slope. The current and parent CPU averages differ by
  0.53 percentage points, and every warm-state RSS slope is negative.
- Process/Tooling: project-file `plutil -lint`, `git diff --check`, and complete
  app/UI builds passed. Owner-scope search found the legacy Settings
  `PermissionPolling` types, task registry, fixed-attempt loop, and swallowed
  cancellation removed. The remaining sleep is inside the injected,
  cancellable fallback scheduler, and every wake is revalidated by a TCC
  readback. The dynamic UI fixture accepts path intent and resolves an absolute,
  standardized file URL at the TestingSupport resource boundary.
- Commit: `d18d1b5`
  (`refactor(sync): migrate SYNC-012 permission observation`).

### SYNC-011 Closure Record

- Design and Oracle: `HomePermissionObservationOwner` reuses the shared
  `RuntimePermissionObservationCoordinator`. It installs the single
  app-activation subscription before taking immediate accessibility and
  screen-capture readbacks. Initial readback, app activation, and fallback
  delivery only request another readback; an exact changed TCC value is the
  sole state-transition Oracle. `HomeLandingView` publishes that evidence to
  its banner, sidebar permission status, runtime-summary refresh, AX-window
  monitor ownership, and static Home cache.
- Lifecycle: Home active visibility starts the owner and inactive visibility,
  disappearance, and teardown stop it. The owner manages one observation
  generation per target, two cancellable fallback tokens, and one shared
  activation subscription. Duplicate starts preserve one owned set.
  Cancellation and generation checks reject delayed, duplicate, and stale
  callbacks. The Home view retains its existing runtime projection and exact
  window-identity ownership boundaries.
- Retained time policy: TCC exposes no unified permission-transition callback,
  so `HomePermissionObservationOwner.fallbackReadbackInterval` retains a
  one-second conditional-readback cadence. The coordinator checks both targets
  immediately, owns and cancels the scheduled work, and uses every wake only
  to obtain a fresh TCC value. Home's long-lived observation mode has no
  completion watchdog because it represents current state for the duration of
  visible ownership.
- Unit and Behavior: the final focused set passed 3/3 in 0.043 seconds under
  `.build-local/evidence-driven-sync/SYNC-011/flowtabtests-targeted-attempt-003`.
  It covers observer-before-initial-readback ordering, initial evidence, app
  activation, a 60-second delayed fallback delivered through an injected
  clock/scheduler, visibility cancellation, stale callbacks, duplicate start,
  and 2,000 rapid start/stop cycles. The complete `FlowTabTests` class passed
  269/269 under
  `.build-local/evidence-driven-sync/SYNC-011/flowtabtests-class-attempt-005`;
  the complete `FlowTabPriorityCoverageTests` class passed 553/553 under
  `.build-local/evidence-driven-sync/SYNC-011/flowtabtests-priority-attempt-007`.
  A combined-class attempt retained at
  `.build-local/evidence-driven-sync/SYNC-011/flowtabtests-full-attempt-004`
  passed all 269 `FlowTabTests` and exposed one existing startup-stability
  assertion under cross-class scheduling. The exact assertion passed 1/1
  under
  `.build-local/evidence-driven-sync/SYNC-011/priority-flake-attempt-006`
  before the full Priority class passed cleanly.
- FlowTabCore: not relevant because permission readback, Home visibility, and
  SwiftUI state publication belong to the app target.
- UI: the dynamic Home permission workflow passed 1/1 in 8.431 seconds under
  `.build-local/evidence-driven-sync/SYNC-011/ui-home-permission-observation-attempt-011`.
  It starts with two denied values, observes the visible permission action,
  atomically changes the external TCC fixture, then requires that action to
  disappear and both exact `fallbackReadback` evidence logs to arrive. Four
  existing granted, unavailable-AX, app-directory, and permission-sidebar
  regressions passed 4/4 under
  `.build-local/evidence-driven-sync/SYNC-011/ui-home-permission-regressions-attempt-012`.
- Pressure: the owner-level 2,000-cycle test retained one activation
  subscription and two fallback tokens with no stale publication. The
  canonical 20-second tab-switch workload at 20ms cadence passed with 42
  samples under
  `.build-local/evidence-driven-sync/SYNC-011/tab-switch-pressure-20ms-attempt-013`.
  CPU average/p95/max was 63.73/70.10/90.30 percent and RSS was
  105.10/119.56/120.06 MB. The final 20-sample RSS slope was
  0.013 MB/sample across a 110.16-110.41 MB plateau. The 50ms comparison passed
  with 42 samples under
  `.build-local/evidence-driven-sync/SYNC-011/tab-switch-pressure-50ms-attempt-014`;
  CPU was 63.11/70.00/87.10 percent, RSS was 112.79/125.91/125.94 MB, and the
  final 20-sample slope was 0.050 MB/sample across a 116.31-117.47 MB plateau.
  Against the same-machine SYNC-012 parent baselines, CPU averages increased
  by 2.67 and 3.56 percentage points from the two immediate TCC readbacks per
  Home activation, while RSS p95/max decreased at both cadences and both
  current runs reached stable allocator ranges without continuing growth.
- Process/Tooling: the final app and UI-test targets built through their
  canonical scripts. Project-file `plutil -lint`, `git diff --check`, and
  owner-scope synchronization search passed. `HomeLandingView` no longer owns
  a permission sleep loop, poll task, or raw interval literal. The retained
  cadence is named on the extracted 118-line owner, injected through the
  coordinator scheduler in deterministic tests, and cancelled at the Home
  visibility boundary.
- Commit: `e7b209a`
  (`refactor(sync): migrate SYNC-011 home permission observation`).

### SYNC-013 Closure Record

- Design and Oracle: `HomeInitialProjectionObservationOwner` installs a
  `.runtimeAppSwitcherProjectionDidUpdate` observer filtered to the exact
  runtime projection service object before taking its initial readback or
  requesting maintenance. An initial complete projection is accepted
  immediately. Missing or incomplete evidence triggers maintenance followed
  by an independent request-return readback, closing synchronous completion
  races. Later readbacks apply only when the projection becomes available, the
  source-generation vector advances monotonically, or the same generation
  changes from incomplete to complete. Duplicate, regressed, unrelated-object,
  and out-of-order observations cannot change Home state.
- Lifecycle: Home active visibility starts one observation generation.
  Inactive visibility, disappearance, completion, supersession, and owner
  deinitialization remove its notification token and invalidate stale
  generations. `HomeLandingView` applies accepted evidence to app summaries,
  loading state, selection, cache pruning, and the AX window monitor while
  preserving the existing projection service and exact window-identity
  boundaries.
- Retained time policy: this contract retains no duration. The 900ms startup
  delay was removed. Readiness succeeds only from a projection-backed,
  complete readback. The visible Home lifetime bounds the consumer
  observation; the runtime projection repair watchdog and its last-evidence
  diagnostics remain owned by the underlying SYNC-003 repair contract.
- Unit and Behavior: the final focused owner/lifecycle set passed 7/7 under
  `.build-local/evidence-driven-sync/SYNC-013/flowtabtests-final-attempt-015`.
  It covers initially satisfied evidence, observer-before-trigger ordering,
  synchronous request completion, exact-object filtering, monotonic
  generations, same-generation completeness, duplicate and regressed
  notifications, visibility cancellation, and late-event rejection. A
  combined-class run exposed a test-side pre-inactive readback baseline and
  the previously recorded AppDelegate startup-stability scheduling
  sensitivity; both exact tests passed 2/2 after the test Oracle was anchored
  after inactive-state completion under
  `.build-local/evidence-driven-sync/SYNC-013/flowtabtests-flake-check-attempt-006`.
  The complete `FlowTabTests` class passed 270/270 under
  `.build-local/evidence-driven-sync/SYNC-013/flowtabtests-class-attempt-007`;
  the final complete `FlowTabPriorityCoverageTests` class passed 558/558 under
  `.build-local/evidence-driven-sync/SYNC-013/flowtabtests-priority-attempt-016`.
- FlowTabCore: not relevant because Home visibility, SwiftUI state
  publication, and runtime projection consumption belong to the app target.
- UI: the initial Home app-order/zero-count path and the Home overview
  counts/stats/permission-sidebar path passed 2/2 in 23.646 seconds under
  `.build-local/evidence-driven-sync/SYNC-013/ui-home-initial-attempt-009`.
  Their success Oracles are the exact visible app rows, counts, statistics,
  and permission elements.
- Pressure: 2,000 rapid owner replacements passed in 0.007 seconds under
  `.build-local/evidence-driven-sync/SYNC-013/owner-pressure-attempt-013`.
  One exact-object notification produced one readback, an unrelated object
  produced none, and a post-stop notification produced none. Canonical noisy
  cross-Space/fullscreen topology runs passed with identical functional
  outcomes at both 20ms and 50ms sampling cadences under
  `.build-local/evidence-driven-sync/SYNC-013/runtime-topology-20ms-attempt-011`
  and
  `.build-local/evidence-driven-sync/SYNC-013/runtime-topology-50ms-attempt-012`.
  The fixed-app identity contract matched all 363 and 297 checks. CPU
  average/p95/max was 100.86/137.40/157.40 and 98.10/137.60/147.60 percent;
  RSS average/p95/max was 186.09/257.86/322.38 and
  180.61/247.77/299.58 MB. A first sandboxed attempt was interrupted by
  `filecoordinationd` and `CoreSimulatorService`; the canonical run completed
  outside that failed environment.
- Process/Tooling: the final app and UI-test targets built through their
  canonical scripts. Project-file `plutil -lint`, `git diff --check`, and
  owner-scope synchronization search passed. The legacy 900ms constant,
  startup sleep, and bootstrapper were removed. `HomeLandingView` is smaller
  than its pre-slice baseline, and the extracted owner remains below the
  source-file size guardrail.
- Commit: `cf6318e`
  (`refactor(sync): migrate SYNC-013 home initial readiness`).

### SYNC-040 Closure Record

- Design and Oracle: `HomeAppSummaryProjectionObservationOwner` installs a
  `.runtimeAppSwitcherProjectionDidUpdate` observer filtered to the exact
  runtime projection service object before taking its baseline readback.
  Committed notification readbacks apply only when the projection becomes
  available, the source-generation vector advances monotonically, or the same
  generation changes from incomplete to complete. Duplicate, regressed,
  unrelated-object, and out-of-order notifications remain observable
  diagnostics without changing Home state. App activation and changed
  permission evidence request service-owned maintenance; an independent
  request-return readback closes synchronous completion races.
- Lifecycle: Home active visibility owns one long-lived observation generation.
  Inactive visibility, disappearance, supersession, and owner deinitialization
  remove the exact notification token and invalidate stale callbacks. The
  initial-readiness owner establishes the long-lived observer from inside its
  accepted completion callback, before releasing its own token, so projection
  commits cannot fall into a handoff gap. AppDelegate remains the workspace
  lifecycle owner and the runtime service remains the projection-commit owner;
  Home consumes their exact committed evidence.
- Retained time policy: the general app-summary contract retains no duration.
  Its 220ms post-signal sleep, refresh task, generic notification publisher,
  and duplicate workspace observers were removed. SYNC-041 subsequently
  removed the selected-app and app-scoped AX refresh delays. Runtime repair
  termination and final-evidence diagnostics remain owned by SYNC-003.
- Unit and Behavior: the final focused owner, lifecycle-handoff, and projection
  reader set passed 10/10 under
  `.build-local/evidence-driven-sync/SYNC-040/flowtabtests-targeted-attempt-003`.
  Coverage includes the observer-before-trigger ordering, initially available
  state, synchronous request completion, exact-object filtering, 64 duplicate
  notifications, monotonic generation acceptance, same-generation
  completeness, regressed evidence, background delivery on the main actor,
  inactive cancellation, and 2,000 rapid restarts with one live subscription.
  The complete `FlowTabTests` class passed 269/269 under
  `.build-local/evidence-driven-sync/SYNC-040/flowtabtests-class-attempt-004`;
  the complete `FlowTabPriorityCoverageTests` class passed 563/563 under
  `.build-local/evidence-driven-sync/SYNC-040/flowtabtests-priority-attempt-005`.
- FlowTabCore: not relevant because Home visibility, service-object
  observation, SwiftUI state publication, and projection maintenance requests
  belong to the app target.
- UI: the new launch-order workflow passed 1/1 in 28.111 seconds under
  `.build-local/evidence-driven-sync/SYNC-040/ui-home-summary-attempt-007`.
  FlowTab stays running while three real fixture apps launch later; the Oracle
  requires every exact app-identity row and window count. The existing
  fixture-first Home workflow passed 1/1 in 29.391 seconds under
  `.build-local/evidence-driven-sync/SYNC-040/ui-home-existing-attempt-008`.
  The first new-path attempt retained at
  `.build-local/evidence-driven-sync/SYNC-040/ui-home-summary-attempt-006`
  showed the permission probe terminating the prelaunched app before the
  assertion. Moving permission validation ahead of fixture launch preserved
  the intended process topology, after which the same product assertion
  passed.
- Pressure: the owner-level 2,000-restart test retained one exact-object
  subscription and published no post-stop evidence. Canonical noisy
  cross-Space/fullscreen topology runs passed with identical four-window
  selection and activation outcomes at 50ms and 20ms sample cadences under
  `.build-local/evidence-driven-sync/SYNC-040/runtime-topology-pressure-050ms-attempt-010`
  and
  `.build-local/evidence-driven-sync/SYNC-040/runtime-topology-pressure-020ms-attempt-011`.
  The 50ms run completed its UI scenario in 53.016 seconds with 280 matched
  identity samples; CPU average/p95/max was 99.59/137.00/153.80 percent and
  RSS average/p95/max was 179.19/270.22/296.94 MB. The 20ms run completed in
  51.484 seconds with 333 matched identity samples; CPU was
  96.61/138.90/148.20 percent and RSS was 166.63/264.72/268.09 MB. Both are
  comparable to the nearest same-machine SYNC-013 topology baselines and show
  the higher sampling pressure changing elapsed work and sample density while
  preserving the functional Oracle.
- Process/Tooling: the app and UI-test targets built through the canonical
  scripts. Project-file `plutil -lint`, `git diff --check`, owner-scope
  synchronization search, and source-size checks passed. The legacy general
  220ms sleep path, generic projection publisher, and dead general-refresh
  reader/policy symbols are absent. `HomeLandingView` decreased from its
  pre-slice line count, and the extracted owner and focused test file remain
  below the source-file size guardrail.
- Commit: `fa8ef7e`
  (`refactor(sync): migrate SYNC-040 home summary updates`).

### SYNC-041 Closure Record

- Design and Oracle: `HomeAppDetailProjectionObservationOwner` installs a
  `.runtimeCurrentAppWindowProjectionDidUpdate` observer filtered to the exact
  runtime projection service object before taking the initial readback and
  before issuing the selected-app, app-window-change, or AX-window-destroyed
  signal. Notification routing additionally requires the exact appID, and a
  readback is accepted only when its summary, candidate, and context identities
  all match that appID. An exact baseline can update Home immediately, while
  the request remains observed until later evidence becomes available, advances
  the source-generation vector monotonically, or changes the same generation
  from incomplete to complete. An independent request-return readback closes a
  synchronous service-completion race. Duplicate, regressed, unrelated-object,
  wrong-app, and wrong-projection-identity evidence cannot change Home state.
- Lifecycle: one Home-owned observation generation exists per appID.
  Supersession cancels only that app, summary projection changes retain only
  still-presented appIDs, and Home inactivity or disappearance cancels all
  observations. The shared notification token is removed when the final
  per-app observation ends and again defensively at owner deinitialization.
  Background runtime commits are delivered on the main actor before SwiftUI
  state publication. The runtime service remains the projection-commit owner,
  and SYNC-003 remains the underlying transient-repair lifecycle/watchdog
  owner.
- Retained time policy: the Home detail consumer retains no duration. The
  120ms selected-app delay, 220ms app-scoped AX delay, and their task
  dictionaries were removed. Completion and UI application depend on exact
  projection readback; runtime repair's named condition cadence and terminal
  watchdog remain classified and owned by SYNC-003.
- Unit and Behavior: the final focused set passed 9/9 under
  `.build-local/evidence-driven-sync/SYNC-041/flowtabtests-targeted-attempt-005`.
  Coverage includes exact service/appID/projection identity, observer-before-
  trigger ordering, synchronous notification and request-return completion,
  complete initial state, newly available and monotonic generation evidence,
  same-generation completeness, 64 duplicate notifications, regression and
  later-incomplete rejection, cancellation, retained-app pruning, main-actor
  delivery, 2,000 rapid supersessions with one live subscription, and inactive
  Home cancellation. The complete `FlowTabTests` class passed 269/269 under
  `.build-local/evidence-driven-sync/SYNC-041/flowtabtests-full-attempt-003`.
  The complete `FlowTabPriorityCoverageTests` class executed 570 tests under
  attempts 002 and 003; all seven SYNC-041 tests passed, while the existing
  `testAppDelegateLaunchWithUITestBootstrapArgumentsSeedsLogsAndOpensSearch`
  observed one duplicate external hotkey reload in suite order. That test
  passed 1/1 in isolation under
  `.build-local/evidence-driven-sync/SYNC-041/priority-existing-failure-rerun-attempt-001`.
  The order-dependent asynchronous test isolation remains assigned to
  SYNC-032/SYNC-033, so the required full Priority layer remains non-green.
- FlowTabCore: not relevant because the contract is owned by the app target's
  Home visibility, Runtime projection notification, and SwiftUI publication
  boundaries.
- UI: the fixed-path signed app was rebuilt successfully from the final
  implementation, and its frozen identity manifest has SHA-256
  `ab689524119b7237d011d45f248ac7c1ff4f5c4b60a878c28e1df113257261f8`.
  Three canonical attempts of
  `testRuntimeLifecycleRefreshesRealFixtureWindowSetMutation` ended before the
  test body with `Timed out while enabling automation mode`, including two
  elevated attempts and one attempt after restarting `testmanagerd`. The
  result bundles and logs are preserved under
  `.build-local/evidence-driven-sync/SYNC-041/ui-runtime-window-mutation-attempt-001`,
  `-002`, and `-003`. They provide runner-environment evidence and no FlowTab
  product assertion, so the required selected-app Home UI layer remains
  blocked.
- Pressure: the owner-level 2,000-supersession test retained one exact-object
  subscription and rejected every stale callback. The fixed-identity real
  topology attempt under
  `.build-local/evidence-driven-sync/SYNC-041/runtime-topology-pressure-050ms-attempt-001`
  remained non-green because UI automation never launched a matching target:
  identity checks and samples were both zero. Independent canonical Home
  lifecycle pressure passed at both 50ms and 20ms switch cadences under
  `.build-local/evidence-driven-sync/SYNC-041/tab-switch-pressure-50ms-attempt-001`
  and
  `.build-local/evidence-driven-sync/SYNC-041/tab-switch-pressure-20ms-attempt-001`.
  The 50ms run completed 20 seconds with app exit status 0 and 43 samples;
  CPU average/p95/max was 65.90/73.30/88.60 percent and RSS was
  107.60/122.52/122.88 MB. The 20ms run also completed with app exit status 0
  and 42 samples; CPU was 68.85/75.40/104.70 percent and RSS was
  115.55/128.28/128.30 MB. Higher scheduling pressure changed resource use
  while preserving the terminal workload result.
- Process/Tooling: the app target and focused/full test targets built through
  canonical scripts. Project-file `plutil -lint`, `git diff --check`, scoped
  fixed-delay/symbol search, and source-size checks passed. The removed
  `HomeRuntimeRefreshReader`, selected/scoped refresh tasks, and 120ms/220ms
  Home detail sleeps are absent. `HomeLandingView` is shorter than its
  pre-slice baseline; the extracted owner and focused test files remain below
  the new-file size guardrail.
- Commit: `113f9bd`
  (`refactor(sync): migrate SYNC-041 home detail updates`).

### SYNC-014 Closure Record

- Design and Oracle: `TemporaryRegularActivationRestorationOwner` installs
  exact-object application and window observers before requesting presentation,
  then takes both an initial readback and an independent presentation-return
  readback. Completion requires the exact readback state: the application is
  active and the target window is visible. App activation/unhide and target
  window key, main, miniaturization, occlusion, and close notifications only
  trigger a fresh readback. Duplicate, unrelated-object, out-of-order, and stale
  generation notifications cannot complete a later activation request.
- Lifecycle: `AppWindowCoordinator` owns one restoration owner and monotonic
  generation. Every new window-opening request cancels the prior observation,
  fallback, and watchdog before installing the next owner. The owner removes
  all exact-object notification tokens and cancels scheduled work on success,
  terminal failure, explicit cancellation, supersession, and deinitialization.
  The AppKit Home window is resolved before the owner starts, preserving the
  observer-before-trigger boundary.
- Retained time policy: a named 20ms fallback cadence remains because AppKit
  does not publish one completion callback covering every relevant application
  activation and window visibility mutation. Each cadence first reads the
  condition immediately, requests presentation only while evidence remains
  incomplete, and completes only from a later exact readback. A named
  five-second watchdog performs a final readback and terminates failure with
  source, elapsed time, app-active, visible, miniaturized, and activation-policy
  evidence. Both policies use injectable scheduler and monotonic-clock
  boundaries; cancellation belongs to the restoration owner.
- Unit and Behavior: the final focused set passed 11/11 under
  `.build-local/evidence-driven-sync/SYNC-014/flowtabtests-targeted-attempt-002`
  (eight owner tests and three status-item orchestration tests). Coverage
  includes initial satisfaction, observer-before-presentation ordering,
  presentation-return completion, exact unrelated/out-of-order/duplicate
  evidence, cancellation, coordinator supersession, conditional fallback
  presentation, watchdog diagnostics, and slow scheduling changing elapsed time
  while preserving the result. The complete `FlowTabTests` class passed 277/277
  under
  `.build-local/evidence-driven-sync/SYNC-014/flowtabtests-unit-full-attempt-001`.
  The complete `FlowTabPriorityCoverageTests` class executed 570 tests under
  `.build-local/evidence-driven-sync/SYNC-014/flowtabtests-priority-full-attempt-001`;
  all three SYNC-014 status-item tests passed, while the existing
  `testAppDelegateLaunchWithUITestBootstrapArgumentsSeedsLogsAndOpensSearch`
  observed one duplicate external hotkey reload in suite order. That test
  passed 1/1 in isolation under
  `.build-local/evidence-driven-sync/SYNC-014/priority-existing-failure-rerun-attempt-001`.
  Its order-dependent asynchronous test isolation remains assigned to
  SYNC-032/SYNC-033, so the expanded full Priority layer remains non-green.
- FlowTabCore: not relevant because the synchronization owner, AppKit evidence
  adapters, and status-item orchestration are app-target responsibilities.
- UI: the fixed-path signed app was rebuilt successfully with executable
  SHA-256
  `a0a2046dbfecc168a5b5397a34d6f25b712f0cd132e6eefc547a08e5389c9376`.
  The canonical
  `testStatusItemReopensLastSelectedTabAfterWindowClose` path passed 1/1 in
  9.609 seconds under
  `.build-local/evidence-driven-sync/SYNC-014/ui-status-item-attempt-002`.
  It verified window closure, another normal Space app becoming frontmost,
  status-item reopening of the previously selected Logs tab, the regular-to-
  accessory activation evidence, and the final FlowTab foreground state. The
  first attempt exposed that this pre-existing log Oracle requested INFO
  logging after resetting preferences without starting a diagnostic session;
  the test launch now establishes that observation precondition explicitly.
- Pressure: the deterministic owner-level 2,000-supersession test passed in the
  focused run, retained exactly one live application/window observation set,
  and delivered no stale outcome. The slow-scheduler test exercised a 100x
  elapsed-time change with the same terminal stable evidence and presentation
  count.
- Process/Tooling: the app, app-test, and UI-test targets built through the
  canonical scripts. Project-file `plutil -lint`, `git diff --check`, scoped
  synchronization search, and source-size checks passed. The legacy polling
  task, 250-attempt counter, and direct success-after-sleep path are absent;
  the remaining sleep is encapsulated by the named cancellable fallback and
  watchdog scheduler.
- Commit: `d30626e`
  (`refactor(sync): migrate SYNC-014 status item activation`).

### SYNC-015 Closure Record

- Design and Oracle: `SwitcherSearchCoordinator` is now the synchronous,
  deterministic search state machine. `LiveSwitcherModel` owns the sole
  `SwitcherSearchSchedulingOwner`, which debounces and executes search work.
  Each request captures the exact query, scope, catalog, searchable index, and
  scheduling revision. A result is published only when the scheduling revision
  remains current and the model readback still matches the captured query,
  scope, and index generation. Scheduler latency can change delivery time
  without becoming a success condition.
- Lifecycle: the model owns one scheduling owner. The owner owns and cancels
  both the pending debounce token and computation token, increments a monotonic
  revision on every request and cancellation, and rejects stale or out-of-order
  delivery. Callback registration closes the synchronous-callback-before-token-
  assignment race for injected schedulers and executors. Token cancellation,
  supersession, explicit model cancellation, and owner deinitialization clean
  up pending work.
- Retained time policy: the initial 20ms debounce and adaptive
  14/25/35/45ms debounce intervals are a named performance policy. The policy,
  scheduler, and monotonic clock are injectable. Computation duration selects
  only the next debounce interval; completion and publication depend on
  revision plus exact model readback. The duplicated coordinator-owned 10ms
  delay and its work-item lifecycle were removed.
- Unit and Behavior: the final focused set passed 11/11 under
  `.build-local/evidence-driven-sync/SYNC-015/flowtabtests-targeted-attempt-004`
  (eight Unit paths and three Priority behavior paths). Coverage includes the
  latest revision, initial synchronous execution, cancellation, duplicate and
  out-of-order completion, synchronous scheduler/executor callbacks, a 100x
  scheduler-latency change with the same result, policy boundaries, and 2,000
  supersessions retaining one current request. The complete `FlowTabTests`
  class passed 283/283 under
  `.build-local/evidence-driven-sync/SYNC-015/flowtabtests-unit-full-attempt-001`.
  The complete `FlowTabPriorityCoverageTests` class executed 570 tests under
  `.build-local/evidence-driven-sync/SYNC-015/flowtabtests-priority-full-attempt-001`;
  all targeted SYNC-015 behavior paths passed, while
  `testAppDelegateLaunchWithUITestBootstrapArgumentsSeedsLogsAndOpensSearch`
  observed the previously recorded duplicate external hotkey reload in suite
  order. Its exact isolated rerun passed 1/1 under
  `.build-local/evidence-driven-sync/SYNC-015/priority-existing-failure-rerun-attempt-001`.
  The order-isolation work remains assigned to SYNC-032/SYNC-033.
- FlowTabCore: not relevant because search state, model publication, and the
  scheduling lifecycle are app-target responsibilities.
- UI: the canonical install script rebuilt and installed the fixed-path signed
  app. `testSearchPanelChineseQueryShowsChineseMockResult` and
  `testSearchPanelEntryAndResultActivation` passed 2/2 in 37.924 seconds under
  `.build-local/evidence-driven-sync/SYNC-015/ui-search-attempt-001`, verifying
  query input, Chinese result publication, exact result activation, and the
  affected visible search path.
- Pressure: realistic Search Pressure passed under
  `.build-local/evidence-driven-sync/SYNC-015/search-pressure-realistic-attempt-002`
  with the conformant `flowtab.search.realistic.v1` rhythm, 44.446 active
  seconds, 71 valid samples, zero cadence gaps, and four successful batches.
  Stress Search Pressure passed under
  `.build-local/evidence-driven-sync/SYNC-015/search-pressure-stress-attempt-001`
  with the conformant `flowtab.search.stress.v1` rhythm, 41.852 active seconds,
  67 valid samples, zero cadence gaps, and two successful batches. Every
  committed-index batch reported `resultState=committedGenerationResult` and
  `freshnessBarrierRequests=0`.
- Process/Tooling: the app and app-test targets built through the canonical
  FlowTabTests wrapper, and the UI target built through the canonical install
  script. Project-file `plutil -lint`, `git diff --check`, scoped stale-symbol
  search, and source-size checks passed. New production and test files remain
  within the 400-line guardrail, while the touched oversized
  `LiveSwitcherModel.swift` shrank by four lines.
- Commit: `76827b2`
  (`refactor(sync): migrate SYNC-015 search scheduling`).

### SYNC-016 Closure Record

- Design and Oracle: `AppDelegate` now publishes
  `HotkeyRegistrationEvidence` only after Command+Tab reconciliation and both
  hotkey monitors have been configured. Each evidence value carries a
  monotonic generation, exact request ID, both requested configurations, and
  the resulting takeover-active readback. `AppSettingsView` prepares its
  observer before persisting or requesting registration and performs an
  independent request-return readback to close synchronous delivery races.
  `HotkeySettingsCardAppKitView` renders pending, active, or inactive solely
  from matching registration evidence.
- Lifecycle: Settings active visibility owns one
  `HotkeyRegistrationObservationOwner`. Starting installs the notification
  token before the initial readback; inactivity and disappearance remove the
  token, clear pending evidence, and advance an observation generation that
  rejects already-queued delivery. The owner accepts only strictly increasing
  evidence generations. Exact request identity or exact configuration
  equality resolves a pending request, while duplicate, regressed,
  out-of-order, and unrelated evidence cannot change the rendered result.
- Retained time policy: the 250ms inactive-status delay, delayed work items,
  and generation tokens were removed. Registration completion is synchronous
  at the AppDelegate owner boundary and therefore needs no watchdog. The
  persisted Command+Tab restoration marker remains the compatibility and crash-
  restoration state owned by `CommandTabTakeoverController`; Settings no
  longer uses it as registration-success evidence.
- Unit and Behavior: the final focused set passed 11/11 under
  `.build-local/evidence-driven-sync/SYNC-016/flowtabtests-targeted-attempt-003`
  (seven Unit paths and four Priority behavior paths). Coverage includes
  observer-before-readback delivery, an already-satisfied exact initial
  request, exact request/configuration matching, duplicate and out-of-order
  rejection, explicit stop/cancellation, scheduler-yield latency equivalence,
  exact AppDelegate publication, and 2,000 monotonic evidence generations.
  After tightening cancellation coverage to reject an already-queued
  notification delivery, the final five owner tests passed 5/5 under
  `.build-local/evidence-driven-sync/SYNC-016/flowtabtests-owner-final-attempt-001`.
  The complete `FlowTabTests` class passed 288/288 under
  `.build-local/evidence-driven-sync/SYNC-016/flowtabtests-unit-full-attempt-001`.
  The complete `FlowTabPriorityCoverageTests` class executed 570 tests under
  `.build-local/evidence-driven-sync/SYNC-016/flowtabtests-priority-full-attempt-001`;
  all four SYNC-016 behavior paths passed, while
  `testAppDelegateLaunchWithUITestBootstrapArgumentsSeedsLogsAndOpensSearch`
  observed the previously recorded duplicate external hotkey reload in suite
  order. Its exact isolated rerun passed 1/1 under
  `.build-local/evidence-driven-sync/SYNC-016/flowtabtests-known-order-isolated-attempt-001`.
  The order-isolation work remains assigned to SYNC-032/SYNC-033.
- FlowTabCore: not relevant because registration, Settings visibility,
  AppKit rendering, and notification publication are app-target boundaries.
- UI: the canonical install script rebuilt and installed the fixed-path signed
  app. The first canonical run ended before the test body when macOS timed out
  enabling automation mode. The elevated second run exposed that the new UI
  assertion read one accessibility property immediately after element
  existence; the Oracle was corrected to wait for the identified status
  element whose label or value contains the active registration evidence.
  `testSettingsCommandTabTakeoverTriggersSwitcherAndRestoresSystemShortcut`
  then passed 1/1 in 40.599 seconds under
  `.build-local/evidence-driven-sync/SYNC-016/ui-settings-command-tab-attempt-003`.
  It verified exact active-registration logging, marker compatibility, visible
  active status, Command+Tab delivery, and system-shortcut restoration after
  exit.
- Pressure: the deterministic 2,000-generation owner test passed in the
  focused run. It preserved the latest matching registration result while
  rejecting every earlier generation. The latency-equivalence test inserted
  additional scheduler yields and produced the same terminal state, so
  scheduling pressure changed delivery time without changing the result.
- Process/Tooling: the app, app-test, and UI-test targets built through the
  canonical scripts. Project-file `plutil -lint`, `git diff --check`, scoped
  stale-delay/symbol search, and source-size checks passed. New production and
  test files remain below the 400-line guardrail; the touched
  `HotkeySettingsCard.swift` shrank from 572 to 519 lines. The startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `aa62c7d`
  (`refactor(sync): migrate SYNC-016 hotkey registration status`).

### SYNC-017 Closure Record

- Design and Oracle: `ModifierReleaseObservationOwner` installs an exact
  modifier/main-key transition observer before its initial hardware readback.
  Matching local/global AppKit events and Carbon hotkey callbacks trigger an
  immediate readback. Selection confirmation requires two stable released-state
  readbacks and a pressed readback resets stability. Session-end replay
  suppression, as closed by SYNC-018, requires one exact released-state
  readback. Both decisions therefore come from current hardware state, while
  duplicate, reordered, and stale transition delivery can only request another
  readback.
- Lifecycle: `SwitcherPanelController` owns one observation owner and one
  observation generation. Each start replaces the prior event token and
  fallback token. Session cancellation, panel dismissal, search transitions,
  replay-suppression completion, and controller release cancel the applicable
  observation; generation checks reject already-queued fallback work. The
  system event-source token removes both local and global event monitors when
  its owner releases it.
- Retained time policy: `ModifierReleaseConfirmationPolicy.sampleInterval`
  remains a named 25ms fallback-readback cadence for an AppKit/global-event
  delivery gap and stale hardware readback. Each readback schedules at most one
  cancellable fallback sample, and matching transition evidence requests the
  readback immediately. Completion remains independent of elapsed duration. A
  held modifier is a valid open-ended input condition, so lifecycle
  cancellation owns termination and a watchdog does not apply.
- Unit and Behavior: the final focused set passed 13/13 under
  `.build-local/evidence-driven-sync/SYNC-017/targeted-attempt-002`
  (seven owner Unit paths and six controller behavior paths). Coverage includes
  observer-before-initial-readback ordering, an initially satisfied condition,
  event-triggered completion, missed-event fallback, pressed-state stability
  reset, duplicate transitions, explicit cancellation, stale-generation
  rejection, synchronous scheduler completion, and 2,000 transition events.
  Two additional interruption and flags-changed behavior paths passed 2/2
  under `.build-local/evidence-driven-sync/SYNC-017/behavior-attempt-001`.
  The complete `FlowTabTests` class passed 295/295 under
  `.build-local/evidence-driven-sync/SYNC-017/full-unit-attempt-001`.
  The complete `FlowTabPriorityCoverageTests` class executed 570 tests under
  `.build-local/evidence-driven-sync/SYNC-017/full-priority-attempt-001`; every
  SYNC-017 path passed, while
  `testAppDelegateLaunchWithUITestBootstrapArgumentsSeedsLogsAndOpensSearch`
  reproduced the registered suite-order failure. Its exact isolated rerun
  passed 1/1 under
  `.build-local/evidence-driven-sync/SYNC-017/priority-known-failure-isolated-001`.
  The order-isolation work remains assigned to SYNC-032/SYNC-033.
- FlowTabCore: not relevant because modifier observation, Carbon/AppKit event
  routing, hardware flag readback, and panel-session ownership are app-target
  boundaries.
- UI: the canonical install script rebuilt and installed the fixed-path signed
  app.
  `testSettingsHotkeySelectionsPersistAcrossRelaunch` passed 1/1 in 55.079
  seconds through the canonical UI wrapper, exercising persisted modifier/main
  key configuration, real hotkey delivery, modifier-release confirmation, and
  relaunch readback. Its result bundle is
  `.build-local/ui-tests/results/FlowTabUITests.xcresult`.
- Pressure: the deterministic 2,000-transition owner test passed in the
  focused run with a stable terminal result and a single live fallback token.
  Manual scheduler tests delayed and reordered fallback delivery while
  preserving the same hardware-state result. Scheduling pressure therefore
  changes observation latency while preserving selection and replay results.
- Process/Tooling: the app and app-test targets built through the canonical
  FlowTabTests wrapper, and the UI target built through the canonical install
  script. Project-file `plutil -lint`, `git diff --check`, scoped stale-delay
  search, and source-size checks passed. Both new production files and the new
  test file remain below the 400-line guardrail;
  `SwitcherPanelController+InputHandling.swift` shrank from 789 to 496 lines.
  The startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `7abc326`
  (`refactor(sync): migrate SYNC-017 modifier release observation`).
- Validation repair: `42359d455771c2f431f160b5e4d32f7ce5b8ef06`
  (`test(sync): stabilize SYNC-017 modifier event oracle`) constructs the
  flags-changed event from the configured primary modifier. Its isolated
  canonical rerun passed 1/1 under
  `.build-local/evidence-driven-sync/SYNC-017/modifier-event-oracle-attempt-001`.

### SYNC-018 Closure Record

- Design and Oracle: every `OptionTabHotkeyMonitor` owns a stable
  `HotkeyInputSourceID` and emits one monotonic sequence for each resolved
  Carbon press or release. `SwitcherHotkeyInputOwner` accepts an exact
  route/source/sequence identity once, rejects duplicate and regressed
  sequences, and rejects queued delivery from a replaced or unregistered
  source. Each accepted receipt records the global input generation, source
  registration generation, and current presentation-session generation.
  Distinct immediately adjacent input events therefore advance independently.
  Selection finish and cancellation establish replay-release observation before
  committing or ending the session. Replay suppression closes only when the
  exact modifier and main-key readback is released, either from the initial
  readback or a later transition/readback.
- Lifecycle: AppDelegate creates monitors inactive, registers their exact source
  with the controller, installs the callback, retains the monitor, and then
  explicitly starts Carbon observation. Re-registration stops the prior
  monitor and advances the route registration generation before the replacement
  can emit; old-source delivery is rejected. AppDelegate termination owns
  monitor stop and Carbon unregistration. `SwitcherPanelController` owns the
  input owner for its lifetime. `ModifierReleaseObservationOwner` owns the
  replay event token, at most one fallback token, replacement generation,
  cancellation, and local/global monitor cleanup.
- Retained time policy: the 16ms tab-advance throttle and 20ms post-finish
  ignore duration were removed. The named 25ms modifier-release fallback
  cadence from SYNC-017 remains solely for a missed AppKit/Carbon transition;
  it immediately reads the same hardware condition and elapsed time cannot
  make it succeed. A held input is a valid open-ended condition, so release
  evidence or lifecycle cancellation terminates the observation and no
  watchdog applies.
- Unit and Behavior: the final focused set passed 14/14 under
  `.build-local/evidence-driven-sync/SYNC-018/targeted-attempt-002`
  (four owner Unit paths and ten monitor/AppDelegate/controller behavior paths).
  Coverage includes exact-once identity acceptance, duplicate and out-of-order
  rejection, source replacement/unregistration, explicit monitor start
  idempotence and stop cleanup, AppDelegate start/stop ownership, distinct
  immediate presses, replay while held, initial and transition release
  readback, fallback cancellation, and the next press after release. The
  complete `FlowTabTests` class passed 299/299 under
  `.build-local/evidence-driven-sync/SYNC-018/full-unit-attempt-002`.
  The complete `FlowTabPriorityCoverageTests` class executed 572 tests under
  `.build-local/evidence-driven-sync/SYNC-018/full-priority-attempt-002`; every
  SYNC-018 path passed, while
  `testAppDelegateLaunchWithUITestBootstrapArgumentsSeedsLogsAndOpensSearch`
  observed the registered duplicate factory-record suite-order state. Its exact
  isolated rerun passed 1/1 under
  `.build-local/evidence-driven-sync/SYNC-018/priority-known-failure-isolated-attempt-001`.
  The order-isolation work remains assigned to SYNC-032/SYNC-033.
- FlowTabCore: not relevant because Carbon input routing, AppDelegate lifecycle,
  panel-session identity, hardware release readback, and TestingSupport command
  delivery are app-target boundaries.
- UI: the canonical install script rebuilt and installed the fixed-path Apple
  Development-signed app. Two final-source attempts ended before the test body
  when macOS timed out enabling automation mode. After restarting
  `testmanagerd`, one attempt entered setup and lost the service connection
  before a product assertion. The following canonical elevated run with
  unrelated Space-fixture preparation skipped passed
  `testSettingsMainHotkeyRepresentativeMatrixTriggersSwitcher` 1/1 in 93.773
  seconds. It exercised three persisted main-hotkey configurations across
  relaunch and real key delivery into the switcher.
- Pressure: the deterministic 2,000-event owner test accepted every distinct
  identity exactly once and rejected a duplicate delivery for every sequence.
  The final-source three-iteration
  `testTabSwitchStressCPUAndMemory` UI workload passed 1/1 in 32.869 seconds;
  its average monotonic iteration was 3.120 seconds, average CPU time was
  0.074 seconds, and average peak physical memory was 15,872.427 KiB.
  Immediate-event and manual release-scheduler tests preserve the same
  selection/replay result across scheduling order, so scheduling pressure
  changes completion latency while preserving the Oracle.
- Process/Tooling: the app, app-test, and UI-test targets built through the
  canonical wrappers. Project-file `plutil -lint`, `git diff --check`, scoped
  stale-symbol/duration search, and source-size checks passed. Both new files
  remain below the 400-line guardrail; touched monitor/controller files remain
  below 800 lines with a single responsibility. The startup `prompts.zip`
  remains unchanged and outside the slice.
- Commit: `44b0802`
  (`refactor(sync): migrate SYNC-018 hotkey input identity`).

### SYNC-019 Closure Record

- Design and Oracle: `SwitcherPanelController` installs panel occlusion,
  become-key, and expose observers during initialization. A presentation starts
  `InitialPanelVisibilityObservationOwner` before the first
  `makeKeyAndOrderFront`/`orderFrontRegardless` action. The owner performs an
  immediate readback and accepts completion only when the exact current
  `PanelVisibilitySnapshot.userVisible` condition is true. Matching
  presentation-action, occlusion, become-key, expose, soft-recovery, and
  watchdog readbacks can provide that evidence. Every observation carries both
  the initial-visibility generation and presentation-session generation;
  duplicate, regressed, superseded, and wrong-session delivery cannot complete
  the current session.
- Lifecycle: the panel controller owns the AppKit observer tokens from
  initialization through deinitialization. The presentation session owns one
  initial-visibility observation generation, one injected scheduler token, and
  the existing soft-recovery task. Visibility completion cancels the watchdog
  and any active recovery task. Session end, cancellation, replacement, and
  generation invalidation cancel pending work; token deinitialization
  defensively cancels its task. Hidden occlusion evidence is deferred only
  while that exact observation remains active.
- Retained time policy: `initialPresentationVisibilityWatchdogInterval` is a
  named 350ms terminal failure bound with an injectable scheduler. It never
  establishes success. When it fires, a final readback can still complete a
  panel that became visible under slow scheduling. An unmet final readback
  records the last event source/snapshot and final watchdog snapshot before
  routing the existing recoverable-interruption policy. Visibility recovery
  attempt timing and hard-reorder sequencing remain assigned to SYNC-020.
- Unit and Behavior: the focused owner and controller set passed 10/10 across
  `.build-local/evidence-driven-sync/SYNC-019/targeted-attempt-002` and
  `.build-local/evidence-driven-sync/SYNC-019/targeted-attempt-003`. It covers
  initially satisfied state, observer-before-trigger delivery, exact matching
  generations, duplicate and stale evidence, event-driven completion,
  cancellation, final-watchdog completion under delayed scheduling, watchdog
  failure diagnostics, session cancellation, and 200 rapid open/close cycles.
  The complete `FlowTabTests` class passed 305/305 under
  `.build-local/evidence-driven-sync/SYNC-019/full-unit-attempt-001`; the final
  2,000-replacement pressure test added afterward passed 1/1 under the
  owner-pressure result below. A combined rerun retained under
  `.build-local/evidence-driven-sync/SYNC-019/full-unit-attempt-002` stalled in
  the shared XCTest service before any test-case output after the UI automation
  failures and was interrupted. The complete
  `FlowTabPriorityCoverageTests` class executed 574 tests under
  `.build-local/evidence-driven-sync/SYNC-019/full-priority-attempt-001`; every
  SYNC-019 path passed, while
  `testAppDelegateLaunchWithUITestBootstrapArgumentsSeedsLogsAndOpensSearch`
  reproduced the registered duplicate factory-record suite-order state. Its
  exact isolated rerun passed 1/1 under
  `.build-local/evidence-driven-sync/SYNC-019/priority-known-failure-isolated-attempt-001`.
  The order-isolation work remains assigned to SYNC-032/SYNC-033.
- FlowTabCore: not relevant because AppKit window notifications, overlay
  readback, panel presentation, and session watchdog ownership belong to the
  app target.
- UI: the canonical install script rebuilt and installed the fixed-path Apple
  Development-signed app. Two canonical switcher attempts, including a
  restarted `testmanagerd` and elevated full-host rerun, timed out while macOS
  enabled automation mode before entering the test body. Evidence is retained
  under
  `.build-local/evidence-driven-sync/SYNC-019/ui-switcher-attempt-001` and
  `.build-local/evidence-driven-sync/SYNC-019/ui-switcher-attempt-002`; neither
  produced a product assertion.
- Pressure: the 200-cycle controller open/close test passed in the focused run
  without a stale recovery execution. A deterministic 2,000-replacement owner
  workload passed in 0.005 seconds under
  `.build-local/evidence-driven-sync/SYNC-019/owner-pressure-attempt-001`,
  retaining exactly one current watchdog and publishing no callback after
  cancellation. The canonical 50ms runtime-topology attempt built the real
  fixture variants and signed runner, then reached
  `target_launch_identity_failed` because the UI runner never launched the
  frozen-identity FlowTab app; it recorded zero samples and no product result
  under
  `.build-local/evidence-driven-sync/SYNC-019/runtime-topology-pressure-050ms-attempt-001`.
  Switcher UI and runtime-topology Pressure remain environment-blocked.
- Process/Tooling: the app, app-test, fixture, and UI-test targets built through
  canonical wrappers. Project-file lint, diff checks, scoped stale-symbol
  search, source-size checks, and exact staged-content review are recorded
  before commit. New production and test files remain below 400 lines; touched
  controller files remain below 800 lines with focused responsibilities. The
  startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `a0b746b`
  (`refactor(sync): migrate SYNC-019 initial panel visibility`).

### SYNC-020 Closure Record

- Design and Oracle: `PanelVisibilityRecoveryObservationOwner` replaces the
  fixed retry-delay loop with one explicit state machine. It first performs an
  exact `PanelVisibilitySnapshot` readback. A hard attempt issues order-out and
  must then observe `panelPresented == false` before it can issue order-front;
  completion requires an exact current `userVisible == true` readback. The
  owner consumes the controller-lifetime occlusion, become-key, and expose
  notifications installed by SYNC-019. Recovery and presentation generations
  reject stale callbacks. A visible notification received while order-out is
  still pending is retained as diagnostic evidence and cannot skip the
  required order-out/order-front transition.
- Lifecycle: `SwitcherPanelController` owns one recovery observation owner.
  The owner is installed before the first recovery action and retains at most
  one condition-readback token and one watchdog token. Replacement,
  presentation-session end, selection cancellation, successful readback,
  watchdog termination, and owner release cancel pending work. Order-out
  induced resign-active, resign-key, and hidden-occlusion callbacks are routed
  back into the active owner instead of creating a competing recovery
  generation.
- Retained time policy: AppKit in the local macOS 14.5 SDK exposes no public
  `NSWindow` order-on/order-off notification. The named
  `interruptionConditionReadbackInterval` is therefore retained at 10ms as a
  cancellable condition-observation cadence. It performs an immediate check
  before scheduling and elapsed duration never establishes success. The
  four-attempt maximum is an action-count policy rather than a duration.
  `interruptionPresentationRecoveryWatchdogInterval` is a named one-second
  terminal failure bound; its final readback can still complete recovery, and
  failure reports both the preceding evidence and final snapshot.
- Unit and Behavior: five deterministic owner tests and four controller
  behavior tests compile in the final source. Their independent Oracles cover
  initially satisfied state, order-out readback before order-front, rejection
  of a reordered visible notification, matching and stale generations,
  event-driven completion, delayed condition scheduling with an unchanged
  result, cancellation/replacement, watchdog final-readback success, watchdog
  failure diagnostics, active-Space recovery, and occlusion recovery. The
  canonical final build passed for all app, app-test, fixture, and UI-test
  targets under
  `.build-local/evidence-driven-sync/SYNC-020/build-for-testing-attempt-003`.
  Final execution under
  `.build-local/evidence-driven-sync/SYNC-020/owner-targeted-attempt-006`
  produced a result bundle but no suite-start or test-start event after a
  `testmanagerd` restart; the stalled invocation was terminated and recorded
  with xcodebuild exit 75 and log-pipeline exit 130. Unit and Behavior remain
  environment-blocked.
- FlowTabCore: not relevant because AppKit window ordering, panel snapshots,
  presentation generations, and recovery lifecycle belong to the app target.
- UI: the canonical install script rebuilt and installed the final-source
  Apple Development-signed app. The elevated canonical switcher run built and
  signed its UI runner, entered `test-without-building`, and then produced no
  test-start event. Its result bundle and status are retained under
  `.build-local/evidence-driven-sync/SYNC-020/ui-switcher-attempt-002`;
  no product assertion ran.
- Pressure: the deterministic 2,000-replacement owner test compiles and asserts
  one live condition-readback token, one watchdog token, and zero callbacks
  after cancellation. Its execution shares the Unit infrastructure blocker.
  The final-source canonical 500ms runtime-topology run reused the real Space
  fixture variants and a frozen identity manifest for the newly installed app.
  The runner reached `test-without-building`, but no matching FlowTab launch
  was observed: `identity_check_count=0`, `sample_count=0`, and no launch
  receipt was produced. Evidence is retained under
  `.build-local/evidence-driven-sync/SYNC-020/runtime-topology-pressure-500ms-attempt-003`.
- Process/Tooling: the canonical wrappers compiled all six targets and
  installed the signed UI app. Project-file lint, `git diff --check`, scoped
  stale-delay/symbol search, source-size checks, and exact staged-content
  review are recorded before commit. The three new production files and two
  new test files remain below 400 lines; the extracted presentation controller
  responsibility also reduces
  `SwitcherPanelController+Presentation.swift`. The startup `prompts.zip`
  remains unchanged and outside the slice.
- Validation correction: the rapid open/close pressure case now observes the
  initial-visibility watchdog token, recovery scheduler tokens, recovery
  generation, and the last direct recovery diagnostic across 200
  presentation lifecycles. It fires every retained manual callback after
  cancellation and verifies that recovery and watchdog state remain stable.
  This replaces the asynchronous persisted-log timing Oracle, which classified
  SYNC-020's intentional synchronous soft recovery action as stale and could
  retain only a suffix of the generated records. The corrected case passed in
  0.169 seconds under
  `.build-local/evidence-driven-sync/SYNC-021/initial-visibility-pressure-corrected-attempt-011`.
- Commit: `206afdffa78e3bc1e193033fe03e1f06b69be0e3`
  (`refactor(sync): migrate SYNC-020 panel visibility recovery`).
- Validation-correction commit:
  `b7a1b2263598dfbd5f18797501dfdc0f4e0eb549`
  (`test(sync): correct SYNC-020 cancellation pressure oracle`).

### SYNC-021 Closure Record

- Design and Oracle: `ActiveSpaceTransitionObservationOwner` captures the
  runtime Space projection generation, ordered display/current-Space
  identities, presentation generation, and exact panel visibility before
  `signalSpaceTopologyChanged()`. An independent request-return readback and
  exact-runtime-service projection notifications provide later evidence.
  Resolution requires a Space generation strictly newer than the baseline.
  A same-Space visible result closes activation suppression without recovery;
  a changed current Space or hidden panel follows the existing modifier,
  search, termination-protection, and visibility-recovery rules. Duplicate,
  stale, out-of-order, replaced-observation, and wrong-presentation evidence
  cannot resolve the active observation.
- Lifecycle: one panel controller owns the projection observer, transition
  owner, and explicit activation-suppression identity. Starting a replacement
  cancels the preceding watchdog. Projection resolution, session end,
  cancellation, recovery completion, recovery watchdog, and controller
  release clear their matching work. The runtime projection notification is
  filtered to the controller's exact service object.
- Retained time policy: `topologyReadbackWatchdogInterval` is a named
  one-second terminal failure bound with an injectable scheduler. It performs
  one final exact readback: an advanced generation can still resolve, while an
  unmet condition reports the baseline, last evidence source/readback, and
  final readback. Elapsed time never establishes transition success. A visible
  final panel safely ends suppression; a hidden final panel enters the existing
  conservative recovery path.
- Unit and Behavior: five deterministic owner tests and seven controller
  behavior tests cover observer-before-request ordering, synchronous
  request-return evidence, stale/duplicate/out-of-order rejection, stable
  current-Space notification handling, exact Space identity change, modifier
  release cancellation, termination protection, final-watchdog readback,
  lifecycle cancellation, and slow scheduling with an unchanged result. The
  final complete canonical `FlowTabTests` run passed 893/893 in 41.381 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-021/full-flowtabtests-final-attempt-018`.
- FlowTabCore: not relevant because workspace notifications, AppKit panel
  visibility, presentation sessions, and runtime projection service ownership
  belong to the app target.
- UI: the canonical install wrapper built, Apple Development-signed, validated,
  and installed the final app at `/Users/lk/Applications/Flow Tab UITest.app`.
  The final-source four-scenario run covered persisted hotkeys, main-hotkey
  presentation, in-app window presentation, and a fullscreen cross-Space
  workflow. Fixture preparation and runner signing passed, then the UI runner
  stopped before every test body with LocalAuthentication code `-4`
  (`System authentication is running`). The valid result bundle and
  machine-readable status are retained under
  `.build-local/evidence-driven-sync/SYNC-021/ui-final-source-attempt-019`.
- Pressure: the 2,000-replacement deterministic owner workload passed within
  the complete FlowTabTests run. It retains one current watchdog, cancels every
  replaced token, fires delayed callbacks after cancellation, and publishes no
  stale completion. The final-source real-topology run froze the installed app
  identity at SHA-256
  `78228fa980e358edd28a6a97efff6c0c3ee23b397c4bdeaf2ca8d44f25185264`,
  rebuilt and signed the runner, and then met the same LocalAuthentication
  failure before the app launch. Its status is
  `target_launch_identity_failed`, with valid result bundle,
  `identity_check_count=0`, and `sample_count=0`, under
  `.build-local/evidence-driven-sync/SYNC-021/runtime-topology-pressure-final-attempt-021`.
- Process/Tooling: the final all-six-target canonical build passed under
  `.build-local/evidence-driven-sync/SYNC-021/build-for-testing-final-attempt-017`.
  Project-file lint, `git diff --check`, scoped stale-symbol/duration search,
  source-size checks, and exact staged-content review are recorded before
  commit. Both new production files and both new test files remain below 400
  lines. The touched oversized panel-session test file shrinks by 151 lines as
  the focused SYNC-021 behavior coverage moves to its own file. The startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `17a0e8552fe31a64db60586cdb012a59c348c1a1`
  (`refactor(sync): migrate SYNC-021 active Space transition`).

### SYNC-022 Closure Record

- Design and Oracle: `TerminateInterruptionProtectionObservationOwner`
  replaces both uptime windows with a two-phase evidence contract. The
  controller captures projection generation, target presence, and panel
  visibility before issuing a terminate request, then commits the prepared
  observation with the accepted appID, PID, and request generation. Target
  completion requires exact projection absence or replacement together with
  either the matching workspace termination or a terminated-process readback
  whose projection evidence is newer than the baseline or removes a target
  known present at baseline. Same-bundle/different-PID, stale-generation,
  duplicate, out-of-order, and unavailable-identity observations cannot
  establish completion. Once target completion is latched, a later stale
  projection cannot revoke it.
- Presentation completion: target completion cancels its watchdog and enters
  an untimed presentation latch. Protection resolves only after the active
  Space transition or panel visibility publishes a stable readback, or after
  the owner consumes a related system interruption and reads the panel as
  user-visible with FlowTab active. Panel resign-key and app resign-active
  notifications can arrive in either order; each is recovered while the same
  observation remains active. The final real-fixture runs observed both
  notifications after the terminate request, restored the exact panel, and
  removed only the terminated fixture identity.
- Lifecycle: one panel controller owns the observation owner, exact process
  state reader, runtime-projection readbacks, and watchdog scheduler. A
  replacement request cancels the preceding token. Rejected requests cancel
  their prepared baseline. Target completion cancels the watchdog; successful
  presentation evidence, watchdog failure, presentation-session end,
  selection cancellation, and controller release clear all owned state.
  Presentation generation and exact runtime-service notification routing
  reject callbacks from replaced sessions.
- Retained time policy: the named
  `completionWatchdogInterval` remains five seconds solely as the terminal
  failure bound for target termination plus projection removal. Its wakeup
  performs a final exact readback that can still latch completion; elapsed
  time never establishes success. Failure reports the unmet
  `targetTerminationAndProjectionRemoval` condition, target identity,
  baseline, preceding evidence, and final process/projection/panel snapshot.
  The former 500ms post-refresh window is removed. Presentation completion has
  no duration and remains session-owned until evidence or cancellation.
- Unit and Behavior: the final-source targeted run passed 12/12 tests in
  0.483 seconds under
  `.build-local/evidence-driven-sync/SYNC-022/targeted-initial-readback-attempt-027`.
  Seven owner tests cover completion already satisfied by the first readback,
  observer-before-request ordering, exact identity and generation
  filtering, slow watchdog scheduling with an unchanged result, final
  watchdog readback and diagnostics, repeated/out-of-order presentation
  interruptions, and 2,000 replacement/cancellation cycles. Five controller
  tests cover observer-before-request ordering, workspace termination,
  projection removal, active-Space ordering, recovered panel/app activation,
  and failure diagnostics. The complete final-source canonical
  `FlowTabTests` run passed 902/902 in 69.157 seconds under
  `.build-local/evidence-driven-sync/SYNC-022/full-flowtabtests-initial-readback-attempt-028`.
- FlowTabCore: not relevant because terminate requests, workspace lifecycle,
  AppKit presentation events, and runtime projection ownership belong to the
  app target.
- UI: the persisted explicit/fallback quit-hotkey matrix passed in 60.821
  seconds under
  `.build-local/evidence-driven-sync/SYNC-022/ui-mock-hotkey-attempt-017`.
  The final-source real fixture path waited for the independent process-state
  and exact refreshed-element Oracles, kept the panel available through the
  termination interruption sequence, and passed in 25.854 seconds inside
  `.build-local/evidence-driven-sync/SYNC-022/runtime-topology-pressure-final-source-attempt-026`.
  Re-resolving the exact accessibility identifier after projection refresh
  prevents a pre-termination XCUI element binding from becoming the removal
  Oracle.
- Pressure: the deterministic owner workload replaces and cancels 2,000
  observations, fires retained callbacks after cancellation, and publishes no
  stale resolution. The final real-topology attempt froze the installed
  Apple Development-signed executable at SHA-256
  `48a426df29d7eb3fa59f340b85643e8252ceab0d774687bacd40c7cfbd9cf6cd`;
  its private manifest has SHA-256
  `0be5ebdc9ed84a6fc940de0c155d6426f83903569358f67d19c35ad556ad32d6`.
  Six sampled PID bindings matched the frozen identity across 5.131 seconds.
  The 500ms-sampled workload passed with CPU average/p95/max
  56.07/101.30/101.30 percent and RSS average/p95/max
  162.44/199.16/199.16 MB under
  `.build-local/evidence-driven-sync/SYNC-022/runtime-topology-pressure-final-source-attempt-026`.
- Process/Tooling: the final all-six-target canonical build passed under
  `.build-local/evidence-driven-sync/SYNC-022/build-for-testing-initial-readback-attempt-029`.
  Project-file lint, `git diff --check`, scoped stale-symbol/duration search,
  source-size checks, and exact staged-content review are recorded before
  commit. The five new production files and three new test files remain
  at or below 400 lines. The startup `prompts.zip` remains unchanged and
  outside the slice.
- Commit: `29645db31b29a4efab45d7543e2f3b13f69e5a83`
  (`refactor(sync): migrate SYNC-022 terminate interruption protection`).

### SYNC-023 Closure Record

- Design and Oracle: `DelayedWindowLayerEntryObservationOwner` implements one
  two-condition contract. The persisted monotonic deadline and an exact
  selected-app projection readback must both be satisfied before automatic
  entry. Readiness requires the same presentation generation and appID, at
  least two selected windows, a presented app layer, inactive Search, and
  `canAutoEnterWindowLayer`. A deadline wakeup only reads this state; every
  projection or layout event also compares the injected monotonic clock with
  the stored deadline, so delayed scheduler delivery changes completion
  latency while preserving the result.
- Observation ordering and evidence: the controller captures the initial
  projection generation and installs the owner before requesting selected-app
  projection maintenance. Initial readback, request-return readback,
  app-switcher projection publication, exact current-app projection
  publication, and subsequent session-layout readback close synchronous and
  missed-event races. Exact appID, observation generation, and presentation
  generation reject unrelated, duplicate, stale, and replaced evidence.
  Manual window-layer entry consumes the same exact projection publication
  directly.
- Lifecycle: the presentation controller owns one observation and one
  deadline token. Target replacement, Search activation, selection
  cancellation, session end, panel dismissal, controller release, and
  successful entry cancel or replace owned work. A preserved layout update
  keeps the original deadline only for the same app and presentation
  generation. Token cancellation and generation checks make retained
  callbacks inert.
- Retained time policy: `WindowLayerPreferencesStore` continues to own the
  user-selected 0–999.99-second auto-entry delay, rounded to hundredths with a
  0.75-second default. `DelayedWindowLayerEntryScheduler` schedules that one
  product deadline against system uptime. Once the deadline is satisfied, the
  presentation owner waits for exact projection evidence or lifecycle
  cancellation. Projection readiness and manual entry have no retry cadence
  or readiness timeout.
- Unit and Behavior: the final targeted run passed 14/14 tests in 0.279
  seconds under
  `.build-local/evidence-driven-sync/SYNC-023/targeted-attempt-006`.
  Owner tests cover initially ready state, deadline-first and
  projection-first ordering, delayed scheduler delivery, early wakeup
  rescheduling, exact app filtering, stale/duplicate/replaced evidence, and
  2,000 replacement/cancellation cycles. Controller tests cover
  observer-before-request ordering, synchronous request-return readback,
  configured-deadline cancellation, exact event entry, manual entry, and
  replacement. The complete canonical `FlowTabTests` run passed 913/913 in
  47.117 seconds under
  `.build-local/evidence-driven-sync/SYNC-023/full-flowtabtests-attempt-007`.
- FlowTabCore: not relevant because the persisted app preference, AppKit panel
  presentation, runtime projection events, and lifecycle owner belong to the
  app target.
- UI: the final signed-app visible path passed in 6.511 seconds under
  `.build-local/evidence-driven-sync/SYNC-023/ui-delayed-entry-attempt-008`.
  It observed the 300ms configured deadline, the exact five-window projection,
  and the prewarmed preview in the same window-layer transition. The dedicated
  repeated-presentation UI path completed 12/12 presentation, evidence-entry,
  confirmation, and cleanup cycles in 34.483 seconds under
  `.build-local/evidence-driven-sync/SYNC-023/ui-delayed-entry-pressure-attempt-010`.
- Pressure: the deterministic owner test replaces and cancels 2,000
  observations without stale completion. The final process-level run repeated
  the delayed-entry UI path 12 times and passed in 35.518 seconds under
  `.build-local/evidence-driven-sync/SYNC-023/delayed-entry-pressure-attempt-011`.
  Its Apple Development-signed executable SHA-256 is
  `4bce759ba9f24ea174f7a98fe1e2a69f3dd3d432f5b0cf9b9b61ef994abb326c`;
  the private manifest SHA-256 is
  `6382fb7724c1a0c3bee87063e4cb8cf8d6307414cf8f643c5a44f8ab7cb78868`.
  Forty-six 500ms samples matched the frozen identity. CPU average/p95/max was
  15.80/48.00/55.40 percent and RSS average/p95/max was
  116.39/130.88/135.91 MB. This is the local baseline for the dedicated
  delayed-entry interaction workload.
- Cross-slice pressure evidence: the first real-topology attempt completed
  three noisy cross-Space/fullscreen presentation phases, then SYNC-019's
  initial-visibility watchdog cancelled the fourth presentation after a
  372.550ms presentation exceeded its 350ms bound. The final readback recorded
  `userVisible=0`; the exact SYNC-023 observation had already been established
  and remained unrelated to the cancellation. Identity matching, 72 samples,
  and the valid failure result are retained under
  `.build-local/evidence-driven-sync/SYNC-023/runtime-topology-pressure-attempt-009`.
  The inventory assigns this reproducible scheduling-pressure result to the
  pending SYNC-019 remediation.
- Process/Tooling: the final canonical all-six-target build passed under
  `.build-local/evidence-driven-sync/SYNC-023/build-for-testing-attempt-012`.
  Project-file lint, `git diff --check`, scoped stale-symbol search, source-size
  checks, and exact staged-content review are recorded before commit. All six
  new production/test files and the affected UI test file remain below 400
  lines. The startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `6c3f3810a6a4090018b7587904417f10a4a55754`
  (`refactor(sync): migrate SYNC-023 delayed window-layer entry`).

### SYNC-024A Closure Record

- Design and Oracle: `TerminatePressFeedbackPolicy` gives the app-tile visual
  transition and terminate continuation one 120ms product duration.
  `TerminatePressFeedbackCompletionOwner` publishes an explicit scheduler
  completion carrying its generation and interval. The controller prepares
  visual state first, then sends the terminate request only from that matching
  completion. Termination success remains owned by SYNC-022's exact
  appID/PID/request/projection evidence.
- Lifecycle: the panel controller owns one completion owner and one scheduler
  token. Repeated input while a completion is pending is coalesced. Event
  monitor removal, selection/session end, and controller release cancel owned
  work; generation equality rejects retained callbacks after replacement or
  cancellation. Scheduler implementations may deliver synchronously without
  leaking a token or replaying completion.
- Retained time policy: the 120ms ease-out press feedback is an animation
  contract. The production scheduler waits for that named interval and reports
  completion; the interval no longer infers process termination, projection
  refresh, or request success. Delayed scheduler delivery changes request
  latency while preserving the selected app and resulting terminate request.
- Unit and Behavior: the final focused run passed 8/8 in 0.382 seconds under
  `.build-local/evidence-driven-sync/SYNC-024A/targeted-attempt-007`.
  Coverage includes one scheduled completion, duplicate delivery, replacement,
  cancellation, forced late callbacks, synchronous scheduler delivery,
  continuation-before-request ordering, interruption-protection preparation,
  shortcut routing, and request result readback.
- Expanded Behavior: the complete canonical run executed 918 tests under
  `.build-local/evidence-driven-sync/SYNC-024A/full-flowtabtests-attempt-003`.
  All SYNC-024A and termination paths passed. Two assertions in
  `testSwitcherInitialVisibilityObserverAcceptsWindowNotificationReadback`
  retained the SYNC-019 presenting state instead of consuming the panel-expose
  readback. Its exact isolated rerun passed 1/1 in 0.041 seconds under
  `.build-local/evidence-driven-sync/SYNC-024A/sync019-isolation-attempt-004`;
  the ledger assigns this full-suite order sensitivity to the pending SYNC-019
  remediation.
- FlowTabCore: not relevant because the visual policy, panel-session
  continuation, and AppKit/SwiftUI lifecycle belong to the app target.
- UI: the canonical install wrapper produced the fixed-path Apple
  Development-signed app. The two-case configured/fallback quit-hotkey matrix
  passed 1/1 in 76.070 seconds under
  `.build-local/evidence-driven-sync/SYNC-024A/ui-terminate-attempt-005`.
  Each case observed the exact selected app, matching mock terminate request,
  workspace-termination refresh, and selected-tile removal.
- Pressure: the deterministic 500-cycle owner workload alternated completion
  and cancellation, forcibly delivered every retained callback, published
  exactly the 250 live generations in monotonic order, and left no pending
  work. It is part of the focused result above.
- Process/Tooling: the canonical UI wrapper's all-six-target
  `build-for-testing`, signed fixed-app install, and targeted UI execution
  passed. Project-file lint, `git diff --check`, scoped stale-symbol search,
  source-size checks, and exact staged-content review are recorded before
  commit. Both new files remain below 400 lines; the touched controller remains
  below 800 lines with the completion owner extracted. The startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `f5bb3c29601866705ecf6b34afce29ef58976565`
  (`refactor(sync): migrate SYNC-024A terminate press completion`).

### SYNC-024B Closure Record

- Design and Oracle: the controller installs
  `onWindowOnlyPreviewPreparationChanged` during initialization, before any
  preview prewarm request. `InitialWindowOnlyPreviewRevealObservationOwner`
  then starts with the current presentation generation and an immediate
  pending-capture readback. Zero pending captures complete from
  `initial_readback`; later preview-batch callbacks complete only after their
  readback reports zero. A callback that arrived before owner startup is closed
  by the initial readback.
- Watchdog: `InitialWindowOnlyPreviewRevealPolicy` retains 250ms as the named
  degraded-reveal upper bound. Expiry performs a final pending-capture
  readback. A ready final snapshot publishes `watchdog_readback` evidence.
  An unready final snapshot publishes
  `InitialWindowOnlyPreviewRevealWatchdogFailure`, including the last event,
  final source, readiness flag, and pending count, before the controller
  reveals the panel in degraded mode. Watchdog expiry is never readiness or
  preview-capture success.
- Lifecycle: the presentation-session controller owns one observation owner
  and one cancellable watchdog token. Replacement increments the observation
  generation; every callback also matches the presentation generation.
  Presentation end cancels and invalidates pending work, restores panel alpha,
  and rejects retained callbacks. Synchronous scheduler delivery cancels the
  returned token without replaying completion.
- Unit and Behavior: the first targeted run exposed a test fixture that had
  neither a model session nor a presented-panel readback, so its call to the
  existing `endPresentationSession` guard correctly did not run. After
  establishing the intended presented-panel precondition, the final targeted
  run passed 11/11 in 0.628 seconds under
  `.build-local/evidence-driven-sync/SYNC-024B/targeted-attempt-005`.
  Coverage includes initial readiness, batch-event readiness, delayed
  watchdog delivery, duplicate delivery, replacement, cancellation, mismatched
  generations, synchronous delivery, final-ready readback, failure diagnostics,
  controller alpha transitions, and presentation-end cleanup.
- Expanded Behavior: the complete canonical FlowTabTests run passed 929/929 in
  63.478 seconds under
  `.build-local/evidence-driven-sync/SYNC-024B/full-flowtabtests-attempt-004`.
  The previously recorded SYNC-019 full-suite ordering failure did not recur;
  its earlier pressure and ordering evidence remains assigned to that dedicated
  remediation.
- FlowTabCore: not relevant because preview capture state, AppKit panel alpha,
  the presentation generation, and the watchdog owner all belong to the app
  target.
- UI: the canonical install wrapper produced the fixed-path Apple
  Development-signed app. The affected Control+Tab test passed 1/1 in 8.452
  seconds under
  `.build-local/evidence-driven-sync/SYNC-024B/ui-preview-attempt-003`.
  With an 80ms asynchronous preview workload, it observed the exact next-window
  selection, both preview images, the large window-preview canvas, and the
  `preview_batch_completed` readiness log.
- Pressure: the deterministic 500-cycle workload alternated cancellation and
  readiness completion, forcibly delivered every retained watchdog callback,
  published exactly the 250 live generations in monotonic order, and left no
  observation or failure pending. It is part of the targeted result above.
- Process/Tooling: the UI wrapper's all-six-target `build-for-testing`,
  fixed-app build/install/signature verification, targeted UI execution, and
  complete canonical app-test run passed. Project lint, stale-symbol search,
  source-size checks, `git diff --check`, and exact staged-content review are
  recorded before commit. The owner and both new test files remain below 400
  lines; the touched controller remains below 800 lines. The startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `845d353574cb99c384cee9eae543747d5bdfb7f8`
  (`refactor(sync): migrate SYNC-024B initial preview reveal`).

### SYNC-024C Closure Record

- Design and Oracle: `SwitcherAppRemovalAnimationPolicy` names the 140ms
  ease-out duration and the existing 16-app animation ceiling. The app strip
  resolves only an optional `Animation` from this immutable policy. Terminate
  request delivery, process state, workspace termination, runtime projection
  refresh, session membership, and tile identity never read the duration.
- Retained time policy and lifecycle: 140ms is a visual product contract for
  opacity/scale removal. SwiftUI app-strip identity owns and cancels its render
  transition as view identity changes or leaves the hierarchy. App sets above
  16 retain the existing no-animation path, preventing large-list render work.
  No callback, continuation, retry, success result, or model mutation is
  scheduled from this policy.
- Unit and Behavior: the focused canonical run passed 5/5 in 0.380 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-024C/targeted-attempt-003`.
  Policy coverage verifies the named duration, inclusive 16-app boundary,
  invalid/large-count no-animation paths, and injected policy values. The
  behavior cases prove a sent terminate request retains the app, while matching
  workspace-termination evidence refreshes the projection and removes it.
- FlowTabCore: not relevant because the animation policy and SwiftUI app-strip
  rendering belong to the app target.
- UI: the fixed-path Apple Development-signed app and real Space Fixture test
  passed 1/1 in 28.010 seconds under
  `.build-local/evidence-driven-sync/SYNC-024C/ui-real-termination-attempt-002`.
  The fixture intentionally remained alive for 2.4 seconds after the quit
  request. Its exact tile remained present throughout that interval, then
  disappeared only after process termination and the matching runtime refresh.
- Pressure: not relevant because this slice adds one immutable constant lookup
  during app-strip rendering and owns no observer, timer, retry, callback, or
  repeated asynchronous work. The existing 16-app ceiling is covered at both
  sides of its boundary.
- Process/Tooling: canonical app-test and UI wrappers built all six targets.
  The UI workflow prepared and signed real fixture variants, installed and
  verified the fixed app signature, then executed the real-process topology.
  Project lint, stale-literal scan, source-size checks, `git diff --check`, and
  exact staged-content review are recorded before commit. Both new files remain
  below 400 lines, and the touched overlay remains below 800 lines. The startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `5a3e470c7e7d6cbd8bc6eb5788d657dfe0b7f203`
  (`refactor(sync): classify SYNC-024C app removal animation`).

### SYNC-024D Closure Record

- Design and Oracle: `HomeControlPressAnimationPolicy` names the 120ms
  ease-out duration and is injected into `FlowPageActionButton`. The SwiftUI
  `Button` continues to invoke its action closure directly from the input
  callback. The duration is read only while deriving the
  `configuration.isPressed` render animation; action delivery, preference
  mutation, navigation, and success state never read it.
- Retained time policy and lifecycle: 120ms is a visual product contract for
  pressed-gradient feedback. SwiftUI button identity owns and cancels the
  render transition as its pressed state or view identity changes. The policy
  owns no callback, continuation, timer, retry, observer, or success result.
- Unit and Behavior: the focused canonical run passed 2/2 in 0.002 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-024D/targeted-attempt-001`.
  Coverage verifies the named default duration, injected policy propagation,
  and direct action-closure delivery independent of the visual duration.
- FlowTabCore: not relevant because the animation policy and SwiftUI button
  rendering belong to the app target.
- UI: the fixed-path Apple Development-signed app test passed 1/1 in 15.245
  seconds under
  `.build-local/evidence-driven-sync/SYNC-024D/ui-home-dismiss-attempt-001`.
  The affected Home action button accepted the real click, removed the
  permission banner from state evidence, and preserved the resulting
  preference across process termination and relaunch.
- Pressure: not relevant because this slice adds one immutable duration lookup
  during SwiftUI rendering and owns no observer, timer, retry, callback, or
  repeated asynchronous work.
- Process/Tooling: the canonical app-test wrapper passed, the fixed app rebuilt
  and verified its Apple Development signature, and the UI wrapper built all
  six targets before executing the affected path. Project lint,
  stale-literal scan, source-size checks, `git diff --check`, and exact
  staged-content review are recorded before commit. Both new files and the
  touched Home chrome file remain below 400 lines. The startup `prompts.zip`
  remains unchanged and outside the slice.
- Commit: `a0096673a73927dd02b1959f80b016f965f4a247`
  (`refactor(sync): classify SYNC-024D home press animation`).

### SYNC-024E Closure Record

- Design and Oracle: `SettingsAppVisibilityNavigationAnimationPolicy` names
  the 180ms ease-in-out duration and is injected into `AppSettingsView`.
  Manage and back input callbacks synchronously update
  `showsAppVisibilityManager`; the duration is read only while deriving the
  opacity animation. `AppVisibilityManagerView` explicitly contains its AX
  children so its manager and back identities form independently observable
  evidence. Navigation success is the requested destination's visible state.
- Retained time policy and lifecycle: 180ms is a visual product contract for
  the Settings/app-visibility opacity transition. Settings SwiftUI view
  identity owns and cancels the transition as state or view identity changes.
  The policy owns no callback, continuation, timer, retry, observer, success
  result, or delayed state mutation.
- Unit and Behavior: the focused canonical run passed 2/2 in 0.001 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-024E/targeted-attempt-001`.
  Coverage verifies the named default duration and injected policy propagation
  into the Settings view.
- FlowTabCore: not relevant because the animation policy, navigation state,
  and SwiftUI rendering belong to the app target.
- UI: the first navigation run under
  `.build-local/evidence-driven-sync/SYNC-024E/ui-navigation-attempt-001`
  reached the manager but exposed only its root AX group, so the stable back
  identity was absent and the action was not attempted. After assigning AX
  child containment to the manager, the fixed-path Apple Development-signed
  navigation test passed 1/1 in 14.511 seconds under
  `.build-local/evidence-driven-sync/SYNC-024E/ui-navigation-attempt-002`.
  It observed the manager and back identities, clicked back, then observed
  manager removal and the Settings manage action's return. The existing
  pinyin-search manager regression also passed 1/1 in 13.510 seconds under
  `.build-local/evidence-driven-sync/SYNC-024E/ui-manager-regression-attempt-001`,
  retaining search-field and exact application-row visibility.
- Pressure: not relevant because this slice adds immutable duration lookup and
  AX containment during SwiftUI rendering and owns no observer, timer, retry,
  callback, or repeated asynchronous work.
- Process/Tooling: the canonical app-test wrapper passed, the fixed app rebuilt
  and verified its Apple Development signature, and the UI wrapper built all
  six targets before the final affected-path execution. Project lint,
  stale-literal scan, source-size checks, `git diff --check`, and exact
  staged-content review are recorded before commit. New files remain below
  400 lines. Touched Settings source and UI-test files remain below 800 lines
  with one Settings-view or Settings-test responsibility each. The startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `0e682598d51861495185b6bf2fdeed038504d4c7`
  (`refactor(sync): classify SYNC-024E settings navigation animation`).

### SYNC-025 Closure Record

- Design and Oracle: `PanelPresentationDiagnosticProbeOwner` replaces the
  anonymous task with a generation-scoped sequence. An injectable scheduler
  first publishes a next-main-turn probe, then schedules the named frame
  sample. Each callback records its actual monotonic timestamp in
  `PanelPresentationDiagnosticProbe`; neither callback provides a readiness or
  success result, and both only feed the existing diagnostic log.
- Retained time policy and lifecycle: 16ms is retained as
  `PanelPresentationDiagnosticPolicy.frameSampleInterval`, a diagnostic
  sampling contract for observing an approximately subsequent display frame.
  `SwitcherPanelController` owns the probe owner, and every presentation begin
  or invalidation cancels its current token and advances generation. Replaced,
  canceled, and late callbacks cannot publish into another presentation
  session.
- Unit and Behavior: the final focused canonical run passed 4/4 in 0.088
  seconds under
  `.build-local/evidence-driven-sync/SYNC-025/targeted-attempt-002`.
  Coverage verifies the named 16ms cadence, independently injected clock,
  actual callback timestamps, replacement, explicit cancellation, late
  callback rejection, and the representative global `show` path. Ending that
  presentation canceled the pending frame sample and preserved the last
  accepted probe.
- FlowTabCore: not relevant because the probe observes app-target panel
  presentation diagnostics and introduces no core-domain contract.
- UI: not relevant because the slice changes no rendered state, accessibility
  state, input result, recovery decision, or user-visible behavior. The
  representative AppKit presentation path is covered by the behavior test.
- Pressure: the deterministic 500-cycle workload alternated cancellation and
  deliberately delayed callback delivery. It published frame samples for
  exactly the 250 live generations in monotonic generation order, rejected
  every canceled callback, and left no pending work. The delayed clock values
  changed only probe timestamps.
- Process/Tooling: the canonical wrapper built all six targets and passed the
  focused tests. Project lint, stale anonymous-sleep scan, source-size checks,
  `git diff --check`, and exact staged-content review are recorded before
  commit. The new owner and test files remain below 400 lines. Existing
  visibility diagnostic value types moved from the oversized panel controller
  into its dedicated diagnostics file, leaving the controller below 800
  lines. The startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `b95e5998e8701c9ef6a84bb5e089c47165e1ae27`
  (`refactor(sync): classify SYNC-025 presentation diagnostic sampling`).

### SYNC-026A Closure Record

- Design and Oracle: `SpaceFixtureFullScreenObservation` installs an
  exact-`NSWindow` `didEnterFullScreen` observer before invoking
  `toggleFullScreen`. `SpaceFixtureFullscreenTransitionOwner` retains the
  configured launch delay only before the first window, then starts each later
  window directly from the preceding exact-window completion. Accepted
  evidence includes the owner generation, ordered sequence index, total count,
  and fixture window-plan index. Application AX publication and downstream
  fixture work begin only after accepted completion evidence.
- Retained time policy and lifecycle: `enterFullscreenDelayMilliseconds`
  remains a fixture scenario input that intentionally stages the first
  transition. After observer installation, an already-fullscreen readback
  completes synchronously without toggling the window. The coordinator owns
  its injected scheduler, transition generation, scheduled token, and current
  exact-window observation. Starting another launch cancels and invalidates all
  retained tokens. Each AppKit fixture window owns its single-shot notification
  token and removes it on completion, cancellation, close, or deinitialization.
  Duplicate, out-of-order, replaced, and canceled callbacks cannot advance
  another sequence.
- Unit and Behavior: the final focused canonical run passed 7/7 in 0.010
  seconds under
  `.build-local/evidence-driven-sync/SYNC-026A/targeted-attempt-004`.
  Coverage verifies configured first-delay scheduling, initial readback
  completion with synchronous callback delivery, first and subsequent event
  delivery, direct two-window chaining, duplicate late callback rejection,
  replacement, cancellation, retained canceled-callback delivery, downstream
  desktop/AX scheduling only after chain completion, and the representative
  coordinator paths.
- FlowTabCore: not relevant because the fixture scheduler, AppKit fullscreen
  notification, and topology orchestration belong to the dedicated fixture and
  app-test targets.
- UI: the canonical install wrapper rebuilt and verified the fixed-path Apple
  Development-signed app. The noisy Control+Tab real-topology test passed 1/1
  in 45.643 seconds under
  `.build-local/evidence-driven-sync/SYNC-026A/ui-attempt-003`. Its fixture
  created two separate fullscreen windows. Exact CG window identities and
  full-screen-sized frames observed both Spaces, and the workflow selected and
  activated each fullscreen sibling across Spaces before process cleanup.
- Pressure: the deterministic 500-cycle owner workload varied the configured
  initial delay on every cycle, alternated accepted completion and
  cancellation, forcibly delivered canceled callbacks, published exactly the
  250 live completion generations in monotonic order, and left no running
  transition. Scheduler timing changed only when the first transition began.
- Process/Tooling: the canonical app-test and UI wrappers built all six
  targets; fixture preparation, UI runner signing, fixed-app installation, and
  targeted execution passed. Project-file lint, stale 1.4-second spacing
  search, source-size checks, `git diff --check`, and exact staged-content
  review are recorded before commit. New production and test files remain
  below 400 lines, and touched coordinator/test files remain below 800 lines.
  The startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `e934fd2a5abc61bb0fdbd8d745e6d5993884ccf9`
  (`refactor(sync): migrate SYNC-026A fullscreen transition chain`).

### SYNC-026B Closure Record

- Design and Oracle: the final fullscreen completion now starts desktop-anchor
  recovery immediately. `SpaceFixtureDesktopRefocusOwner` installs
  exact-window and active-Space observers before activation, captures an
  initial readback, arms one generation-bound watchdog, invokes activation and
  `show(isKey: true)`, and captures a trigger-return readback. Accepted evidence
  must match the owner generation and planned desktop-anchor identity and must
  read back the application as active, the exact AppKit window as key, main,
  visible, unminimized, on the active Space, and occlusion-visible, plus that
  exact window number as an on-screen CG window owned by the fixture process.
  Application AX publication resumes only after that evidence resolves.
- Event gap, retry, and lifecycle: exact `NSWindow` key, main, occlusion, and
  deminiaturize notifications, application activation, and active-Space
  notifications all drive fresh readback. AppKit supplies no acceptance
  callback for activation/order-front requests, so a named 100ms retry first
  checks the full condition, repeats the request only while it remains unmet,
  and reads back again after the request. The coordinator-owned generation
  cancels its observer, retry, and watchdog on success, failure, replacement,
  or launch cancellation. Each AppKit fixture window also cancels its retained
  observation on close. Duplicate, stale-identity, replaced, canceled, and
  out-of-order callbacks cannot resolve another generation.
- Failure bound: the named 15s watchdog is terminal diagnostic policy only. Its
  final readback can still resolve success. A remaining failure records the
  generation, expected plan identity, last evidence source and snapshot, final
  snapshot, and every unmet exact condition.
- Unit and Behavior: the final focused canonical run passed 9/9 with zero
  failures in 0.016 seconds under
  `.build-local/evidence-driven-sync/SYNC-026B/targeted-attempt-007`.
  Coverage verifies observer-before-trigger ordering, initially satisfied
  state, synchronous trigger-return readback, independent key/main/CG
  conditions, exact identity and stale-generation rejection, retry after a
  rejected request, slow scheduling, terminal failure diagnostics,
  replacement/cancellation, and the representative coordinator path.
- FlowTabCore: not relevant because this contract belongs to the AppKit fixture
  topology and app-test targets and does not change a core-domain API.
- Pressure: the deterministic 500-cycle workload alternated successful
  observation and replacement/cancellation, forcibly delivered canceled
  event, retry, and watchdog work, accepted exactly the 250 live generations
  in monotonic order, produced no watchdog failures, and left no observation
  or scheduled work active. Delayed retry delivery changed only resolution
  time. The real two-window topology then exercised the final fullscreen
  completion, exact desktop-anchor refocus, and non-fullscreen CG readback;
  scheduling affected elapsed time while preserving the selected window and
  final Space result.
- UI: the required install wrapper rebuilt, signed, installed, and verified
  `/Users/lk/Applications/Flow Tab UITest.app`. The test resolves the fixture
  from the active Build Products boundary, removes prior fixture processes
  through observer plus process-state readback, and observes the exact
  frontmost PID, title/identifier XCUI window, matching CG window, and desktop
  frame through the SYNC-034A owner. The canonical wrapper passed
  `testSpaceFixtureRefocusesExactDesktopAnchorAfterFullscreenCompletion` 1/1
  with zero failures in 4.916 seconds under
  `.build-local/evidence-driven-sync/SYNC-034A/ui-attempt-002`.
- Oracle follow-up: the SYNC-028B2 diagnostic execution reached
  `testSpaceFixtureRefocusesExactDesktopAnchorAfterFullscreenCompletion`.
  Its shared `waitForExactFrontmostSpaceFixtureWindow` helper returned without
  a present exact window because absent AX and CG window numbers compared
  equal, and the test's independent nonnil assertion failed in 1.859 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-028B2/ui-diagnostic-desktop-refocus-attempt-001`.
  SYNC-034A replaced that Oracle and its affected path now resolves only from
  present exact evidence.
- Process/Tooling: project-file lint, stale 1.2-second refocus-delay search,
  source-size checks, `git diff --check`, and exact staged-content review are
  recorded before commit. New production, test, and UI support files remain
  below 400 lines; the touched shared UI observation file remains below 800
  lines. Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `8c5554912551fe1b5f5edea0e4e85c0ad4e29a78`
  (`refactor(sync): migrate SYNC-026B desktop refocus`).

### SYNC-026C1 Closure Record

- Design and Oracle: FlowTab TestingSupport accepts explicit distributed
  acknowledgement routes containing a notification name, fixture bundle ID,
  and expected window count. It installs the app-switcher projection observer
  before its initial readback. A route publishes only when the committed
  projection is complete and clean and contains the exact bundle ID, a
  positive PID, and the expected window count. Published evidence includes
  monotonic observation and acknowledgement generations, exact PID and window
  count, and the complete runtime source-generation tuple. A distinct PID or
  source generation can publish fresh evidence while an identical notification
  is de-duplicated.
- Lifecycle: `FlowTabUITestProjectionAcknowledgementBootstrap` owns one
  route-specific observation owner for the application TestingSupport
  lifecycle. Reconfiguration cancels the prior observation before replacement.
  Application termination, explicit stop, owner cancellation, and empty routes
  remove the notification token and invalidate the active observation
  generation. Duplicate, canceled, replaced, and stale-generation callbacks
  cannot publish evidence.
- Retained time policy: none. This producer contract uses projection
  notifications plus immediate readback and introduces no sleep, polling
  cadence, timer, retry, deadline, or watchdog. The consumer-side terminal
  failure bound belongs to SYNC-026C2.
- Unit and Behavior: the final focused canonical run passed 6/6 with zero
  failures in 0.004 seconds under
  `.build-local/evidence-driven-sync/SYNC-026C1/targeted-attempt-004`.
  Coverage verifies route validation and de-duplication,
  observer-before-readback ordering, initially satisfied projection evidence,
  rejection of incomplete, dirty, wrong-bundle, and wrong-window-count
  snapshots, later matching notification delivery, distinct PID publication,
  duplicate suppression, cancellation, and stale-generation rejection. The
  projection adapter independently verifies the exact PID, window count,
  source generation, and clean-complete rule.
- FlowTabCore: not relevant because the contract is test-only orchestration at
  the FlowTab application/runtime-projection boundary and adds no core-domain
  API.
- UI: the producer route remains dormant until SYNC-026C2 supplies the
  per-fixture notification route and consumes its cross-process evidence.
  End-to-end distributed transport and visible topology validation therefore
  join that consumer slice.
- Pressure: the deterministic 500-cycle lifecycle workload alternated matching
  evidence and cancellation, forcibly delivered stale callbacks, published
  exactly the 250 live acknowledgement generations in monotonic order, and
  left no observer active. Delayed evidence delivery changed only when
  acknowledgement occurred.
- Process/Tooling: the canonical wrapper built all six targets and passed the
  focused tests. Project-file lint, source-size checks, `git diff --check`, and
  exact staged-content review are recorded before commit. New production and
  test files remain below 400 lines; touched shared files remain below 800
  lines. Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `ea0b73d19ea0c1d725938a3c97d44648c6100d61`
  (`refactor(sync): publish SYNC-026C1 projection acknowledgement`).

### SYNC-026C2 Closure Record

- Design and Oracle: each workflow fixture receives a unique pair of
  distributed-notification routes. The fixture installs its projection
  acknowledgement observer before publishing windows, then records the exact
  local topology stage after the no-fullscreen path, the final ordered
  fullscreen transition, or the exact desktop-refocus readback. Suppression
  begins only when the local stage is complete, both application AX attributes
  expose the exact locally published count, and FlowTab has acknowledged the
  same bundle ID, positive PID, and exact projected window count. Completion
  requires both application AX attributes to read back zero and publishes a
  monotonic suppression generation together with the accepted projection
  generation and source generation. The fixture completion evidence carries
  the exact zero readback for both application AX attributes. The independent
  UI Oracle matches the exact running bundle/PID and reads the fixture's XCUI
  window collection as empty. The route-less compatibility mode resolves from
  the same exact local topology, published-count, and zero-readback evidence;
  its local owner generation closes without cross-process publication.
- Lifecycle: `SpaceFixtureWindowCoordinator` owns one
  `SpaceFixtureApplicationAXSuppressionOwner`. Starting a fixture replaces and
  cancels the prior observer, readback retry, and watchdog generation.
  Coordinator cancellation and application termination remove every token.
  Duplicate, regressed, mismatched, canceled, replaced, and stale-generation
  acknowledgements cannot request suppression or publish completion. The UI
  workflow installs all completion observers before launching FlowTab or any
  fixture and removes them at workflow cleanup.
- Retained time policy: AppKit and cross-process AX readback provide no
  delivery callback, so an unmet local AX condition uses an
  immediate-condition-first, named 100ms retry cadence. The suppression owner
  owns and cancels that retry; time never establishes success. Its named 15s
  watchdog performs a final readback and reports every unmet condition plus
  the previous and final evidence. The UI's 20s bound is terminal failure only
  and reports the latest completion and independent AX readback.
- Unit and Behavior: the final focused canonical run passed 13/13 with zero
  failures in 0.015 seconds under
  `.build-local/evidence-driven-sync/SYNC-026C2/targeted-attempt-007`.
  Coverage verifies the exact SYNC-026C1 transport parser,
  observer-before-initial-readback ordering, synchronously delivered and
  initially satisfiable evidence, wrong/duplicate/out-of-order
  acknowledgements, delayed local publication, delayed zero readback,
  cancellation, replacement, stale callback rejection, route-less
  configuration compatibility, missing-acknowledgement watchdog diagnostics,
  no-fullscreen suppression, ordered multi-fullscreen completion,
  single-fullscreen completion, and exact desktop-refocus gating. Slower retry
  delivery changes completion latency while preserving the result.
- Full FlowTabTests: the final canonical wrapper rebuilt all six targets and
  passed 966/966 with zero failures in 43.669 seconds under
  `.build-local/evidence-driven-sync/SYNC-026C2/full-attempt-005`. Attempt 003
  reproduced the assigned SYNC-032/SYNC-033 order-isolation failure in
  `testSwitcherPanelControllerQuitFrontmostAppInAppLayerKeepsSessionAfterWorkspaceTerminationRefresh`;
  the isolated diagnostic passed 1/1 in 0.242 seconds before the complete green
  rerun.
- FlowTabCore: not relevant because this contract belongs to the standalone
  fixture, application TestingSupport transport, and UI orchestration
  boundaries and adds no core-domain API.
- Pressure: the deterministic 500-generation lifecycle workload synchronously
  acknowledged each exact fixture identity, alternated immediate and delayed
  zero readback, published completion generations 1 through 500 exactly once,
  canceled every watchdog and retry, produced no failure, and left no active
  observation. The real-topology pressure run then passed the representative
  AX-suppressed cross-Space/fullscreen test 1/1 in 33.911 seconds under
  `.build-local/evidence-driven-sync/SYNC-026C2/runtime-topology-pressure-attempt-004`.
  Its frozen application identity contract matched across 44 checks and all 44
  CPU/RSS samples, with zero rejected transient identities. The default noisy
  Option+Tab scenario in attempt 002 completed C2 suppression and its first
  visible phase, then reproduced a missing fresh acknowledgement for a repeated
  Darwin trigger. That independent shared UI transport contract remains
  assigned to SYNC-034.
- UI: after the install wrapper prepared and verified the configured UI
  application, the representative AX-suppressed cross-Space/fullscreen test
  passed 1/1 with zero failures in 33.974 seconds under
  `.build-local/evidence-driven-sync/SYNC-026C2/ui-attempt-005`. The fixture's
  terminal notification carried the exact PID, acknowledgement/source
  generations, and zero counts for both application AX attributes. The UI
  independently matched that running bundle/PID, observed zero XCUI windows,
  and completed both visible cross-Space selection phases.
- Process/Tooling: `plutil -lint` passed for the Xcode project; the canonical
  wrappers compiled the new fixture, test, and UI sources in their intended
  targets. The UI wrapper now incrementally rebuilds every fixture variant from
  the current source boundary for each invocation; this replaced a stale
  July 28 variant that lacked the C2 routes, and `bash -n` validates the
  wrapper. Stale-symbol and literal review confirms removal of the fixed 5s
  and post-fullscreen 8s suppression policies. Source-size review keeps every
  new source below 400 lines, reduces the touched coordinator test file from
  763 to 556 lines, reduces the touched multi-app UI orchestration file from
  1130 to 1103 lines, and reduces the UI wrapper from 809 to 670 lines.
  `git diff --check` and exact staged-content review are recorded before
  commit. Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `4d3a6222de4c38034625f5a62ca4efc23fb67375`
  (`refactor(sync): migrate SYNC-026C2 AX suppression`).

### SYNC-026C2R Closure Record

- Design and Oracle: every suppression route now contains a producer
  completion notification and a distinct authorized-readback verification
  notification. The fixed-path FlowTab test process installs its completion
  observer at bootstrap before fixture launch. For each later monotonic
  suppression generation, it first reads the exact bundle/PID process state,
  `kAXChildrenAttribute`, and `kAXWindowsAttribute`; it publishes verification
  only when that exact process remains running and both authorized AX
  readbacks succeed with zero elements. The UI workflow installs both route
  observers before launching any fixture and resolves only when the producer
  completion and independent FlowTab verification match suppression,
  acknowledgement, source, bundle, PID, configured window count, and both
  producer-side and external zero counts.
- Failure evidence and boundary correction: the initial real UI attempt
  received exact terminal producer evidence for the still-running fixture
  PID, while direct UI-runner reads of both AX attributes returned
  `AXError.apiDisabled` (`-25211`). That concrete identity-boundary evidence
  moved the independent readback into FlowTab TestingSupport, whose stable
  signed application identity already owns the repository's Accessibility
  permission. The final real regression launches the four-window noisy
  fixture directly after both observers are installed, then closes only from
  producer completion plus the authorized verification route.
- Lifecycle: `FlowTabUITestAXSuppressionReadbackBootstrap` owns one route
  owner for the launched FlowTab process. Each start replaces the prior
  observation generation and cancels every completion observer and pending
  readback token; application termination calls `stop`. Each UI route owner
  owns both distributed observers, its condition expectation, monotonic
  completion and verification state, replacement generation, watchdog
  readback, and cleanup. Wrong identity/count, malformed, stale, duplicate,
  regressed, canceled, and out-of-order evidence cannot close the contract.
- Retained time policy: AppKit exposes no notification for external
  application-root AX propagation after the fixture removes its attributes.
  FlowTab therefore performs an immediate readback for every accepted
  completion and retains a named, serial, cancellable 100ms propagation
  cadence only while the exact condition remains unmet. Time never establishes
  success. The workflow's named 20-second watchdog is terminal failure only;
  its final state readback reports the last producer completion and authorized
  verification, including exact AX errors and counts.
- Unit and Behavior: the canonical FlowTabTests wrapper rebuilt all six
  targets and passed 4/4 focused tests with zero failures in 0.003 seconds
  under `.build-local/app-tests`. Coverage verifies complete unique route
  parsing, observer registration before synchronously delivered completion,
  immediate authorized success, retry only until exact convergence, full
  cancellation, and 100 stale/duplicate/regressed replacement generations.
- UI-test infrastructure regression and Pressure: the canonical UI wrapper's
  final pure-slice run passed all four focused consumer tests as part of the
  5/5 zero-failure run under
  `.build-local/evidence-driven-sync/SYNC-026C2R/pure-ui-attempt-003`.
  Coverage includes synchronous dual-observer registration, both event
  orderings, exact external evidence gating, 100 canceled/replaced generations
  with stale and duplicate delivery, and watchdog diagnostics carrying
  `apiDisabled` plus the last exact counts.
- UI: the install wrapper rebuilt, Apple Development-signed, and verified
  `/Users/lk/Applications/Flow Tab UITest.app` from the current production
  source under
  `.build-local/evidence-driven-sync/SYNC-026C2R/pure-app-001`.
  The dedicated noisy real-fixture regression passed with zero failures in
  18.990 seconds; the combined final run passed 5/5 in 19.955 seconds under
  `.build-local/evidence-driven-sync/SYNC-026C2R/pure-ui-attempt-003`.
  It exercised the true fixture topology, projection acknowledgement,
  producer suppression completion, authorized exact-PID AX readback, and UI
  consumer closure. A broader diagnostic Control+Tab run also crossed the
  repaired AX boundary, then exposed the separately owned remaining
  SYNC-035C reopen synchronization path.
- Process/Tooling: the complete UI build compiled all six targets and the
  fixed-path install verified the stable Team ID and designated requirement.
  Swift parsing, project plist validation, unique project references,
  source-size checks, and `git diff --check` pass. The production
  TestingSupport implementation is split by evidence/transport, scheduling,
  and observation/bootstrap ownership at 227, 110, and 371 lines. The UI
  consumer and regression files remain below 400 lines. Startup
  `prompts.zip` remains unchanged and outside the slice.
- FlowTabCore: not relevant because the repair belongs to application
  TestingSupport and UI orchestration boundaries and adds no core-domain API.
- Commit: pending current-slice local commit
  (`fix(sync): repair SYNC-026C2R AX readback`).

### SYNC-027A Closure Record

- Design and Oracle: a positive `--terminate-delay-ms` value is represented by
  `SpaceFixtureTerminationFaultPolicy`. The AppDelegate routes both
  `applicationShouldTerminate` and SIGTERM through one
  `SpaceFixtureTerminationFaultOwner`, which accepts the first request and
  publishes exact scheduled and applied evidence containing a monotonic
  generation, request source, configured duration, bundle ID, and PID. The
  optional distributed-notification route keeps existing launch
  configurations compatible. The representative UI path independently
  verifies the exact running process and FlowTab projection removal after the
  applied evidence.
- Lifecycle: the AppDelegate owns the fault owner, its single scheduled token,
  and the SIGTERM dispatch source. Application termination cancels the active
  generation and token, releases the signal source, and restores the default
  signal disposition. Duplicate application and signal requests share the
  first accepted generation. Cancellation and replacement reject late
  callbacks, and applied evidence is published before the termination action.
- Retained time policy: the configured positive duration is the fixture's
  intentional termination-latency fault contract. Scheduling publishes only
  scheduled evidence; applied evidence, exact process state, and FlowTab
  projection readback establish progress and completion. The named fault owner
  manages cancellation, so machine or scheduler delay changes when the fault
  applies while preserving the resulting termination semantics.
- Unit and Behavior: the final focused canonical run passed 7/7 with zero
  failures in 0.007 seconds under
  `.build-local/evidence-driven-sync/SYNC-027A/targeted-attempt-003`.
  Coverage verifies strict evidence transport parsing, scheduled-to-applied
  transition order, mixed-source duplicate handling, cancellation, stale
  callback rejection, synchronous scheduler reentrancy, and launch
  configuration compatibility for both simple and workflow routes.
- Full FlowTabTests: the final canonical wrapper rebuilt all six targets and
  passed 972/972 with zero failures in 47.539 seconds under
  `.build-local/evidence-driven-sync/SYNC-027A/full-attempt-002`.
- FlowTabCore: not relevant because this fault policy belongs to the standalone
  fixture, app-test transport, and UI orchestration boundaries and adds no
  core-domain API.
- Pressure: the deterministic 500-generation lifecycle workload accepted each
  generation exactly once, published every scheduled and applied transition
  in order, executed every accepted action once, canceled every token, and
  left no active generation. Scheduler delivery timing changed only the
  transition latency.
- UI: after the install wrapper refreshed and verified the signed UI
  application, the representative real-fixture quit path passed 1/1 with zero
  failures in 25.245 seconds under
  `.build-local/evidence-driven-sync/SYNC-027A/ui-attempt-002`. The observer was
  installed before fixture launch and the termination trigger. Attempt 001
  established that the real FlowTab quit path reaches the fixture through
  SIGTERM; the final test therefore validates the exact signal source, duration,
  bundle ID, PID, and generation across scheduled and applied evidence before
  independently observing process exit and projection removal.
- Process/Tooling: `plutil -lint` passed for the Xcode project, the canonical
  wrappers compiled the new fixture, test, and UI sources in their intended
  targets, and stale raw AppDelegate `asyncAfter` and pending-reply state are
  absent. Each new source remains below 400 lines; touched shared sources retain
  a clear single responsibility. `git diff --check` and exact staged-content
  review are recorded before commit. Startup `prompts.zip` remains unchanged
  and outside the slice.
- Commit: `44cb4dc3b9676cbb9444b9041017757912dff570`
  (`refactor(sync): retain SYNC-027A termination fault policy`).

### SYNC-027B Closure Record

- Design and Oracle: `SpaceFixtureWindowCloseFaultPolicy` retains the configured
  fixture fault latency, while `SpaceFixtureWindowCloseFaultOwner` establishes
  an exact request generation, bundle ID, PID, target plan index, and stable
  AppKit window number. The optional distributed trigger observer is installed
  before the initial topology readback and scheduled evidence. A matching
  trigger begins the configured delay after the consumer has independently
  observed its baseline. The route-less launch contract remains compatible and
  begins the delay after fixture publication. Applied evidence is emitted only
  after the target is absent from the coordinator plan indices, the AppKit
  window is invisible, and the exact CG window is off-screen.
- Lifecycle: the window coordinator owns the fault owner. Each active generation
  owns its trigger observer, configured-delay token, readback-retry token, and
  watchdog token. Replacement and cancellation remove every owned resource and
  reject stale callbacks. Duplicate, out-of-order, wrong-generation,
  wrong-bundle/PID, and wrong-plan triggers cannot advance the state machine.
  The target window retains its last positive AppKit window number so post-close
  WindowServer readback preserves exact-window identity.
- Retained time policy: `--close-window-delay-ms` is the intentional latency of
  the fixture's window-removal fault. The owner performs an immediate
  post-action readback. WindowServer provides no public exact-window
  close-completion callback, so a named cancellable 50ms condition cadence
  repeats that readback. A named 10-second watchdog is only a terminal failure
  bound and reports the final unmet conditions plus the last and final exact
  topology evidence. Scheduler and compositor load therefore change completion
  time while the same readback establishes the result.
- Unit and Behavior: the final focused canonical run rebuilt all six targets and
  passed 12/12 with zero failures in 0.016 seconds under
  `.build-local/evidence-driven-sync/SYNC-027B/targeted-attempt-007`.
  Coverage verifies strict evidence and trigger transport parsing, both simple
  and workflow launch routes, matching-trigger gating, initial satisfaction,
  default route compatibility, cancellation and replacement, delayed CG
  convergence, watchdog diagnostics, synchronous scheduling, and coordinator
  integration.
- Full FlowTabTests: the canonical wrapper rebuilt all six targets and passed
  981/981 with zero failures in 46.348 seconds under
  `.build-local/evidence-driven-sync/SYNC-027B/full-attempt-001`.
- FlowTabCore: not relevant because this contract belongs to the standalone
  fixture, app-test transport, and UI orchestration boundaries and adds no core
  domain API.
- Pressure: the deterministic 500-generation lifecycle workload resolved every
  generation from exact topology evidence, rejected every replaced callback,
  canceled every owned observer and token, emitted one terminal transition per
  accepted request, and left no active request.
- UI: attempt 001 supplied concrete regression evidence for the former
  orchestration: under the observed build and automation load, the configured
  7.5-second delay removed window 2 before FlowTab captured the initial
  two-window projection, yielding `1w` at the baseline assertion. Attempt 002
  was stopped before test-body execution by macOS UI-runner authentication
  cancellation (`LocalAuthentication Code=-2`). The elevated canonical rerun
  then passed the representative real-fixture mutation path 1/1 with zero
  failures in 21.348 seconds under
  `.build-local/evidence-driven-sync/SYNC-027B/ui-attempt-003`. Its observer was
  installed before fixture launch; FlowTab independently displayed `2w` before
  the exact trigger, and the test then observed matching applied evidence,
  window-2 nonexistence, retained window 1, and FlowTab's `1w` readback.
- Process/Tooling: `plutil -lint` passed for the Xcode project, and canonical
  wrappers compiled the fixture, app-test, and UI-test sources in their intended
  targets. New production and test sources remain below 800 lines with a single
  window-close fault responsibility; the UI observation helper remains below
  400 lines. The obsolete coordinator token and scheduling symbol are absent.
  `git diff --check` and exact staged-content review are recorded before commit.
  Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `80254968b03675f233928159d55e03c80e98e2d1`
  (`refactor(sync): migrate SYNC-027B window close fault`).

### SYNC-028A Closure Record

- Design and Oracle: `SpaceFixtureWindowCoordinator` starts one
  `SpaceFixtureWorkflowReadinessOwner` generation before showing any window.
  The owner publishes a configured baseline followed by evidence for the exact
  planned-window indices, ordered fullscreen completion, exact desktop-anchor
  presentation, and exact zero application-AX exposure when suppression is
  configured. It publishes terminal ready evidence only when every configured
  condition is satisfied, then updates the visible workflow label and summary.
  The shared single-process UI launcher installs a unique distributed observer
  before fixture launch, captures the configured generation, and accepts only a
  later ready stage with the same bundle ID, PID, exact window plan, and exact
  fullscreen plan. The independent UI Oracle also matches that PID to the
  running application and reads back the visible Ready label, workflow summary,
  fullscreen marker, and FlowTab's three exact window titles.
- Lifecycle: the window coordinator owns readiness replacement, cancellation,
  and application-lifetime cleanup together with the fullscreen, desktop, and
  AX evidence producers. One application identity snapshot supplies readiness,
  AX suppression, and window-fault evidence for the launch. Generations reject
  canceled, replaced, duplicate, out-of-order, and stale transitions.
  `SpaceFixtureWorkflowReadinessObservationOwner` owns the distributed token;
  the XCTest invocation starts it before launch and removes it on every exit.
  The readiness owner clears its completed generation before publishing the
  terminal callback, preserving a synchronously installed replacement.
- Retained time policy: the configured pre-fullscreen delay remains the fixture
  staging contract classified by SYNC-026A and is driven by the fixture
  scheduler. Readiness itself introduces no sleep, retry, timer, or polling
  cadence. `SpaceFixtureWorkflowReadinessUITestPolicy` supplies a named 20-second
  terminal bound, extended only by an explicitly configured fullscreen staging
  duration. Success comes exclusively from exact evidence and visible
  readback. A failure reports all observed stages, their unmet conditions,
  identity and generations; the final visible-state watchdog also reports its
  last label and terminal evidence.
- Unit and Behavior: the final focused canonical run rebuilt all six targets and
  passed 17/17 with zero failures in 0.029 seconds under
  `.build-local/evidence-driven-sync/SYNC-028A/targeted-attempt-009`.
  Coverage verifies strict transport parsing, malformed ready rejection, every
  exact topology prerequisite, initially satisfied no-fullscreen and route-less
  AX paths, ordered multi-fullscreen and desktop-refocus integration, routed AX
  completion, duplicate and out-of-order rejection, cancellation, replacement,
  stale generation rejection, synchronous publication reentrancy, launch-route
  compatibility, and one shared application identity snapshot.
- Full FlowTabTests: the canonical wrapper rebuilt all six targets and passed
  986/986 with zero failures in 58.560 seconds under
  `.build-local/evidence-driven-sync/SYNC-028A/full-attempt-001`.
- FlowTabCore: not relevant because this contract belongs to the standalone
  fixture, app-test transport, and UI orchestration boundaries and adds no core
  domain API.
- Pressure: the deterministic 500-generation lifecycle workload replaced every
  prior owner, delivered every stale generation, completed only the final
  generation once, and left no active observation. The focused state-machine
  test also delivered prerequisites late and out of order; changed delivery
  timing altered completion latency while preserving the terminal result.
- UI: the install wrapper rebuilt and verified the signed UI application. The
  final UI wrapper then rebuilt the current fixture and runner and passed
  `testHomePageShowsRealSpaceFixtureWorkflowWindows` 1/1 with zero failures in
  21.570 seconds under
  `.build-local/evidence-driven-sync/SYNC-028A/ui-attempt-002`. The observer was
  installed before launch. With a configured five-second fullscreen staging
  delay, the fixture completed exact fullscreen and desktop-refocus evidence,
  published the matching Ready generation, and FlowTab independently exposed
  all three planned windows.
- Process/Tooling: `swiftc -parse` passed for every touched Swift source,
  `plutil -lint` passed for the Xcode project, and canonical wrappers compiled
  the fixture, app-test, and UI-test sources in their intended targets. The new
  readiness owner and UI observer remain below 400 lines; the evidence
  transport and focused test source remain between 400 and 800 lines with one
  clear responsibility. Touched shared sources remain below 800 lines.
  `git diff --check` and exact staged-content review are recorded before
  commit. Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `9a8363f9f6e78704aeecf644d5e2f16a8e907563`
  (`refactor(sync): migrate SYNC-028A fixture readiness`).

### SYNC-028B1 Closure Record

- Design and Oracle:
  `SpaceFixtureWorkflowReadinessAggregateObservationOwner` creates one unique
  distributed-notification route per configured workflow app and starts every
  observer before the first fixture process launches. Its pure aggregate owner
  records the exact configured baseline for each workflow app ID, then accepts
  a terminal ready snapshot only when bundle ID, PID, fixture observation
  generation, later transition generation, planned-window indices,
  fullscreen-window indices, and ordered titles match that baseline. Readiness
  evidence delivered before its configured notification is buffered and
  validated against the later baseline, closing cross-process notification
  reordering without relaxing identity. The aggregate completes once after
  every configured process supplies exact terminal evidence. Standard and edge
  launchers now proceed from that aggregate snapshot.
- Lifecycle: the workflow invocation owns all notification tokens, the
  aggregate generation, the XCTest expectation, and cleanup on every return.
  Starting a replacement first cancels all prior tokens and invalidates the
  prior generation. Cancellation and generation equality reject retained
  delivery; duplicate terminal evidence cannot over-fulfil the aggregate. The
  owner clears a completed generation before invoking its callback, preserving
  a synchronously installed replacement.
- Retained time policy: the serialized `settleTimeoutMs` key remains accepted
  for workflow configuration compatibility and now supplies a lower bound for
  the named `readinessWatchdog`. The default 20-second policy also accounts for
  configured fullscreen staging. Completion comes exclusively from the exact
  aggregate evidence. Watchdog expiry reports every unmet workflow app stage
  and the last parsed identity, generations, topology, titles, and readiness
  conditions.
- Unit and Behavior: the final focused canonical run rebuilt all six targets
  and passed 9/9 with zero failures in 0.027 seconds (0.032 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-028B1/targeted-attempt-002`.
  Coverage includes exact all-process completion, ready-before-configured
  notification reordering, incorrect static identity rejection, duplicate
  completion, unmet-condition diagnostics, cancellation, replacement, stale
  aggregate generations, synchronous callback replacement, and per-process
  readiness transport and state-machine behavior.
- Full FlowTabTests: the canonical wrapper passed 990/990 with zero failures in
  47.883 seconds (48.047 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-028B1/full-attempt-001`.
- FlowTabCore: not relevant because the aggregate consumes fixture test
  evidence and belongs to the app-test/UI-test orchestration boundary; it adds
  no core domain API.
- UI: the standard three-application workflow passed
  `testHomePageShowsMultipleRealSpaceFixtureWorkflowAppsAndWindowCounts` 1/1
  with zero failures in 27.290 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1/ui-attempt-001`. The edge
  workflow then passed
  `testSwitcherPanelPreviewKeepsIdenticalRealWorkflowWindowsDistinct` 1/1 with
  zero failures in 18.166 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1/ui-attempt-002`. Both executions
  installed all process observers before launching any fixture, aggregated the
  exact Finder/Chrome/Notes identities and topologies, and preserved the
  independently asserted Home counts or duplicate-window identities.
- Pressure: the deterministic 500-generation aggregate workload replaced every
  prior owner, delivered all stale generations, completed only the final
  generation, and left no active observation. The real three-process UI paths
  exercised independent process scheduling and fullscreen topology; delivery
  latency changed elapsed time while the exact aggregate result remained
  stable.
- Slice boundary: SYNC-028B2 owns the final desktop-anchor activation and exact
  foreground/Space/window readback after aggregate readiness.
- Process/Tooling: canonical app-test and UI wrappers compiled the fixture,
  app-test, and UI-test sources in their intended targets. `plutil -lint`,
  scoped obsolete-settle search, Swift parse checks, source-size checks,
  `git diff --check`, and exact staged-content review are recorded before
  commit. All three new sources remain below 400 lines. Startup `prompts.zip`
  remains unchanged and outside the slice.
- Commit: `025ef62938cc3b930b520722e66a7a6a929eba76`
  (`refactor(sync): migrate SYNC-028B1 workflow readiness`).

### SYNC-028B1F Closure Record

- Failure evidence: the AX-suppressed noisy workflow reached the aggregate
  watchdog with `configuredApps=[] readyApps=[]` while its last raw Chrome
  evidence was terminal ready with expected and completed fullscreen plan
  indices `[2, 3]` under
  `.build-local/evidence-driven-sync/SYNC-028B1M/noisy-ui-attempt-006`.
  Semantic tracing from the workflow source through fixture launch and
  readiness publication confirmed that the producer preserved `[2, 3]`.
  `SpaceFixtureResolvedWorkflow.App` exposed only its first index, so the
  consumer built static aggregate expectations for `[2]`; both configured and
  ready evidence failed exact topology matching.
- Design and Oracle: workflow decoding and scenario resolution now preserve the
  complete ordered, unique fullscreen plan. Aggregate expectations and final
  assertions use that complete plan together with workflow app ID, bundle ID,
  positive PID, observation and transition generations, all window indices,
  and ordered titles. The pure aggregate still completes only after configured
  and later terminal evidence from the same exact process and topology.
- Compatibility and lifecycle: the existing `fullscreenWindowIndex` accessor
  returns the first preserved index for callers that model one fullscreen
  window. Construction from the legacy single-index initializer derives the
  equivalent one-element plan. This slice adds no observer or asynchronous
  resource; the workflow aggregate invocation continues to own generation,
  observer, readback, expectation, and cancellation cleanup.
- Unit and Behavior: the canonical aggregate run rebuilt all six targets and
  passed 4/4 with zero failures in 0.005 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1F/app-tests-attempt-001`.
  It verifies exact all-process completion, canceled and stale generation
  rejection, synchronous replacement, and 500-generation lifecycle pressure.
- Deterministic UI-owner coverage: the configured noisy source resolves to
  fullscreen indices `[2, 3]` and both ordered titles; the compatibility
  initializer preserves a single index; and configured plus ready evidence for
  the complete two-index plan resolves the aggregate exactly once. These three
  tests passed 3/3 with zero failures in 0.785 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1F/targeted-ui-attempt-001`.
- Dependent real fixture UI: the current noisy workflow performs the separately
  owned fixture-metadata Oracle before aggregate waiting. SYNC-028B1M moves
  that Oracle behind terminal readiness and owns the final real
  multi-fullscreen Option+Tab validation with this exact-plan repair present.
- Pressure and retained time policy: the canonical aggregate workload replaces
  its owner 500 times and accepts only the current exact generation. This slice
  introduces no duration, timer, retry, polling cadence, or watchdog; process
  scheduling changes evidence arrival time while the complete topology remains
  the success condition.
- FlowTabCore: not relevant because the repaired identity belongs to UI-test
  workflow resolution and fixture-readiness orchestration and adds no
  core-domain API.
- Process/Tooling: final source rebuilt all six targets successfully under
  `.build-local/evidence-driven-sync/SYNC-028B1F/process-build-001`; the
  generated UI runner was signed and verified with the configured Apple
  Development identity. Swift parse, project property-list validation,
  source-size checks, canonical FlowTabTests and UI-wrapper execution,
  `git diff --check`, and exact staged-content review passed. Extracting the
  87-line workflow-app model reduced the existing oversized workflow source.
  The 165-line focused test source remains below 400 lines. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: local slice commit
  (`fix(sync): preserve SYNC-028B1F fullscreen plan`).

### SYNC-028B1M Closure Record

- Design and Oracle: both multi-application launchers now wait for exact
  aggregate terminal readiness before validating visible fixture metadata.
  For AX-visible fixtures, each workflow app receives an independent
  `SpaceFixtureWorkflowMetadataObservationOwner`; completion requires one
  visible summary whose value exactly equals the configured ordered titles
  and, when requested, the exact fullscreen marker value. AX-suppressed
  fixtures expose no application children by contract, so their exact
  aggregate terminal readiness is the metadata boundary.
- Race and lifecycle: the owner installs the shared serial XCUI readback
  schedule before its initial readback. Every scheduled callback carries the
  current condition-owner generation. Resolution, replacement, cancellation,
  watchdog failure, and deinitialization cancel the one-shot timer chain; a
  stale callback cannot resolve a later generation. Each per-app validation
  owns its owner and cancels it with scoped cleanup.
- Retained time policy: XCUI exposes no notification when an application
  accessibility label becomes queryable. The named shared
  `xcuiReadbackCadence` retains a 100ms condition-polling rhythm, begins with
  an immediate readback, and schedules one cancellable non-repeating timer at
  a time. Time never establishes success. The named eight-second metadata
  watchdog is a terminal failure bound; its final readback reports every
  observed summary and the fullscreen marker together with expected values.
- Deterministic owner coverage and Pressure: five focused tests cover
  registration before initial readback, initial completion, delayed summary
  and marker completion, explicit cancellation, stale and duplicate callbacks
  across 100 replacement generations, and final watchdog diagnostics. They
  passed with zero failures as part of the 8/8 canonical UI-wrapper run in
  `.build-local/evidence-driven-sync/SYNC-028B1M/targeted-ui-attempt-001`.
- Behavior and UI: the same 8/8 run passed the standard three-application
  workflow in 24.160 seconds, the edge workflow with two distinct same-title
  windows in 17.848 seconds, and the four-window noisy AX-suppressed workflow
  in 18.289 seconds. The suppressed path reached
  `workflow.afterReadiness`, used terminal aggregate readiness as its metadata
  boundary, then closed the independent suppression contract at
  `workflow.afterAXSuppressionCheck`. The complete suite finished with zero
  failures in 61.250 seconds.
- Process/Tooling: the install wrapper rebuilt the current FlowTab source under
  `.build-local/evidence-driven-sync/SYNC-028B1M/app-build-001`, installed the
  fixed-path app, and verified its Apple Development signature, Team ID, and
  designated requirement. The canonical UI wrapper compiled all six targets.
  Swift parsing, project property-list validation, unique project references,
  source-size checks, and `git diff --check` pass. Extracting the 310-line
  metadata owner reduced the shared fixture-app support file to 364 lines; the
  247-line focused test file remains below 400 lines.
- Unit and FlowTabCore: no core-domain or application production behavior
  changes in this UI-automation orchestration slice. Deterministic state,
  scheduler ordering, cancellation, and lifecycle rules are exercised by the
  focused XCTest owner coverage in the owning UI-test module.
- Commit: pending current-slice local commit
  (`test(sync): migrate SYNC-028B1M metadata readiness`).

### SYNC-028B1R Closure Record

- Failure evidence: the AX-suppressed real workflow reached the aggregate
  watchdog with `configuredApps=[] readyApps=[]` while its final Chrome
  observation was already `stage=ready`. Unified process logs recorded
  configured publication at 21:24:05.400, the synchronous launch returning
  about 57 milliseconds later, and terminal readiness at 21:24:12.876 under
  `.build-local/evidence-driven-sync/SYNC-028B1R/noisy-ui-attempt-001`.
  The producer therefore emitted during `XCUIApplication.launch()` while the
  UI-test main queue could not deliver its already-installed distributed
  observer.
- Design and Oracle: each workflow app retains its unique notification route
  and now receives a unique readback file. Before publishing a notification,
  `SpaceFixtureWorkflowReadinessTransport` atomically writes a binary property
  list containing the exact configured baseline and latest evidence. A new
  configured stage resets the envelope; every later stage preserves that
  baseline and replaces only the latest record. Immediately before aggregate
  evaluation, the UI owner reads both records once and supplies them to the
  existing pure aggregate owner under the active aggregate generation.
  Completion still requires the exact workflow app ID, bundle ID, positive
  PID, fixture observation generation, later transition generation, window
  plan, fullscreen plan, titles, and terminal readiness.
- Path and compatibility boundary: the optional readback route preserves
  notification-only launch configurations. Absolute path intents are
  standardized; relative intents are resolved beneath the fixture process
  temporary directory where the resource becomes accessible. UI workflow
  routes pass an absolute standardized path, preserving the repository's path
  intent boundary.
- Lifecycle: the fixture coordinator removes its readback on launch
  replacement and cancellation. The aggregate observation owner removes every
  app's readback on cancellation and deinitialization, clears the active
  generation, and continues to reject stale or duplicate evidence through the
  existing generation owner. Atomic replacement prevents a reader from
  observing a partially written envelope.
- Retained time policy: this repair adds no duration, retry, polling cadence,
  or success deadline. The SYNC-028B1 readiness watchdog remains solely the
  diagnostic failure upper bound. Success comes from notification delivery or
  the immediate exact readback through the same aggregate state machine.
- Unit and Behavior: the final focused canonical run passed 6/6 with zero
  failures in 0.004 seconds (0.005 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-028B1R/targeted-attempt-004`.
  Coverage includes relative-path resolution, exact configured/latest
  persistence, transition-generation preservation, file cleanup, missing-file
  readback, aggregate completion, cancellation, stale generations, readiness
  replacement, and exact topology rules.
- Expanded FlowTabTests: the final source rebuilt all six targets and passed
  1032 test cases; one unrelated AppDelegate launch-Search test produced two
  duplicate hotkey-registration assertions during the ordered full run under
  `.build-local/evidence-driven-sync/SYNC-028B1R/full-attempt-001`. Its isolated
  canonical rerun passed 1/1 in 0.745 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1R/full-failure-retry-attempt-001`.
  The focused readiness tests, all 446 `FlowTabTests`, and every other priority
  coverage test passed in the expanded run. The failure path executes before
  and has no dependency on the fixture-readiness sources changed by this
  slice.
- FlowTabCore: not relevant because the readback transport belongs to the
  fixture and UI-test orchestration boundary and adds no core-domain API.
- UI: the deterministic missed-notification Oracle wrote configured and ready
  evidence without publishing either notification, then passed 1/1 in 0.365
  seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1R/readback-owner-ui-attempt-002`.
  The standard three-application workflow passed
  `testHomePageShowsMultipleRealSpaceFixtureWorkflowAppsAndWindowCounts` 1/1
  in 26.855 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1R/standard-ui-attempt-004`.
  The duplicate-title workflow passed
  `testSwitcherPanelPreviewKeepsIdenticalRealWorkflowWindowsDistinct` 1/1 in
  18.270 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1R/edge-ui-attempt-002`,
  preserving distinct CG window identities.
- Pressure: the two focused lifecycle workloads passed 2/2 with zero failures
  in 0.008 seconds (0.009 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-028B1R/pressure-attempt-001`.
  Repeated replacement and cancellation leave only the final generation able
  to complete; slower scheduling changes elapsed time while the exact result
  remains stable.
- Dependent validation: the original AX-suppressed reproduction also traverses
  the separately owned metadata-readiness sequencing contract. Its complete
  end-to-end rerun is assigned to SYNC-028B1M after that dependent slice is
  restored.
- Process/Tooling: the canonical install wrapper produced and verified the
  fixed-path Apple Development-signed UI app. The UI build-for-testing,
  fixture variant rebuilds, Swift parse checks, source-size checks,
  `git diff --check`, and exact staged-content review are recorded for this
  slice. Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: local slice commit
  (`refactor(sync): repair SYNC-028B1R readiness readback`).

### SYNC-028B1R2 Closure Record

- Failure evidence: the current custom-build-root fixture reproduction failed
  with `configuredApps=[] readyApps=[]` and terminal Chrome ready evidence
  under
  `.build-local/evidence-driven-sync/SYNC-028B1M/noisy-ui-diagnostic-attempt-005`.
  While that process was alive, its command line identified the exact
  per-invocation fixture executable and
  `--workflow-readiness-readback-path`. The file at that path contained the
  configured transition 1 and ready transition 5 for the same bundle ID, PID,
  observation generation, window plan, fullscreen plan, and titles. Unified
  logging independently recorded configured, window-topology, fullscreen,
  application-AX, and ready publication. These observations proved that the
  producer and readback transport carried both generations. Subsequent
  sequencing past the metadata Oracle exposed the independent complete
  fullscreen-plan identity mismatch assigned to SYNC-028B1F.
- Design and Oracle: `start()` installs every exact distributed-notification
  observer and then performs an immediate initial readback. Each delivered
  notification refreshes only its entry's atomic configured/latest envelope
  before the notification evidence enters the aggregate owner. Both evidence
  forms retain the captured aggregate generation and the existing exact
  process/topology matching rules; readiness still requires configured and
  later ready evidence from the same fixture identity.
- Lifecycle: the workflow aggregate invocation owns the observer tokens,
  active aggregate generation, XCTest expectation, ready snapshot, and every
  readback file. Replacement calls the same cancellation path, removes all
  observers and files, invalidates the prior generation, and makes queued
  callbacks harmless. Deinitialization removes the observer tokens and
  remaining files.
- Unit and Behavior: the four canonical aggregate state tests passed 4/4 in
  0.005 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1R2/app-tests-attempt-001`.
  They cover ready-before-configured ordering, exact process/topology
  matching, cancellation, stale generations, synchronous replacement, and
  500-generation lifecycle pressure.
- Deterministic UI-owner coverage: the extracted owner tests passed 3/3 in
  0.674 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1R2/owner-ui-attempt-002`.
  The independent Oracles cover initially available configured/latest
  readback, an empty initial read followed by a notification-triggered
  envelope refresh, and 100 stale plus 100 current/duplicate callback
  deliveries. Only the current generation completes.
- UI: final source passed the standard three-application real fixture workflow
  in 26.481 seconds and the duplicate-title edge workflow in 16.955 seconds,
  2/2 with zero failures in 43.435 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1R2/representative-ui-attempt-001`.
  The edge Oracle preserved the two exact CG-backed window identities.
- Dependent AX-suppressed path: the original noisy Option+Tab reproduction
  reached the separately owned fixture-metadata Oracle under
  `.build-local/evidence-driven-sync/SYNC-028B1R2/noisy-ui-attempt-001`.
  The launcher evaluated that Oracle before aggregate waiting and recorded its
  metadata-visibility failure at
  `FlowTabUITests+SpaceFixtureApp.swift:308`. SYNC-028B1M owns the corrected
  sequencing and final end-to-end validation with the SYNC-028B1F complete-plan
  identity repair present.
- Pressure: the UI-owner workload performs event-triggered file readback under
  stale and duplicate callback pressure, while the pure aggregate workload
  replaces its owner 500 times. Delayed callback delivery changes elapsed time;
  exact generation, identity, and envelope state continue to determine the
  result.
- FlowTabCore: not relevant because this repair remains within fixture/UI-test
  cross-process orchestration and adds no core-domain API.
- Retained time policy: this slice introduces no duration, retry cadence, or
  success delay. The existing aggregate watchdog remains the named terminal
  failure bound and reports every unmet app plus its last exact observation;
  it does not produce readiness.
- Process/Tooling: final source rebuilt all six targets successfully under
  `.build-local/evidence-driven-sync/SYNC-028B1R2/process-build-002`, and the
  generated UI runner was signed and verified with the configured Apple
  Development identity. Swift parse, project property-list validation,
  source-size checks, `git diff --check`, canonical FlowTabTests execution,
  fixture rebuilding, exact generated `.xctestrun` execution, and
  staged-content review passed.
  The owner implementation is 313 lines and its extracted test file is 229
  lines. Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: local slice commit
  (`fix(sync): refresh SYNC-028B1R2 readiness readback on events`).

### SYNC-028B1T Closure Record

- Failure evidence: the restored metadata-readiness reproduction failed under
  `.build-local/evidence-driven-sync/SYNC-028B1M/noisy-ui-attempt-002` and
  `noisy-ui-diagnostic-attempt-003` with
  `configuredApps=[] readyApps=[]` while its last Chrome evidence was ready.
  The wrapper had rebuilt current fixture source below the invocation's custom
  build root, while a live process readback identified the launched executable
  below the repository-wide default
  `.build-local/space-fixture-workflow/variants`. Its unique readiness
  readback file was absent throughout that process lifetime. This isolated the
  defect to prepared-workflow routing before changing metadata observation.
- Path and execution boundary: the wrapper captures its invocation directory,
  resolves the baseline and system-app-MRU workflow intents into absolute
  runtime-accessible paths, and locates exactly one generated `.xctestrun`
  containing the `FlowTabUITests` entry. It writes both routes into that test
  host's `EnvironmentVariables` and invokes `test-without-building` through
  the exact generated file. A missing, ambiguous, or structurally invalid
  file terminates with the observed product directory in the diagnostic.
- Lifecycle and compatibility: each build root owns its generated
  `.xctestrun`, resolved workflows, and app variants. The wrapper refreshes
  the environment after every build and before every separate
  `test-without-building` invocation. Default and custom build-root callers
  retain their existing CLI inputs; workflow parsing, app identity, and
  per-resource relative-path resolution remain in their established owners.
- Independent Oracle: the UI-runner regression requires both environment
  routes to be present and absolute, loads each resolved workflow, requires a
  nonempty app set, and verifies that every exact app URL has the same parent
  as its routed workflow. This establishes that the test process consumes the
  variants produced for that invocation.
- Diagnostic progression: `routing-ui-attempt-001` demonstrated that a shell
  export alone was absent from the UI-test process.
  `routing-ui-attempt-002` demonstrated that scheme-driven
  `test-without-building` bypassed the correctly patched generated file.
  Explicit generated-file execution then passed the routing Oracle 1/1 with
  zero failures in 0.465 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1T/routing-ui-attempt-003`.
- UI: the standard three-application workflow passed
  `testHomePageShowsMultipleRealSpaceFixtureWorkflowAppsAndWindowCounts` 1/1
  with zero failures in 27.453 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1T/standard-ui-attempt-001`.
  Finder, Chrome, and Notes were loaded from the custom build root, and the
  independently asserted Home identities and window counts remained exact.
  The final combined `test` action rebuilt final source and both workflow
  variant sets in a second custom root, configured and consumed its generated
  file, and passed the two-workflow routing Oracle 1/1 in 0.441 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B1T/combined-ui-attempt-001`.
- Unit and Behavior: not relevant because this slice changes the canonical
  UI wrapper's generated test-host configuration and adds no production or
  domain state rule. The executable Oracle runs inside the configured UI-test
  host and reads the prepared artifacts directly.
- FlowTabCore: not relevant because the change remains in Process/Tooling and
  UI-test orchestration.
- Pressure: not relevant because configuration occurs once per wrapper
  invocation and owns no observer, timer, retry, repeated asynchronous work,
  or runtime hot path. Separate and combined executions across two custom
  build roots produced the same exact routing result.
- Retained time policy: this slice introduces no duration, cadence, retry,
  deadline, or success timeout. Completion comes from build exit status,
  property-list structure, exact path readback, workflow decoding, and app URL
  identity.
- Process/Tooling: `bash -n`, Swift parse, `plutil` target-entry validation,
  full UI build-for-testing, fixture rebuilding, runner signing, separate and
  combined generated-file execution, source-size checks, and
  `git diff --check` passed. The helper source is 333 lines; the canonical
  wrapper remains one 762-line responsibility. Startup `prompts.zip` remains
  unchanged and outside the slice.
- Commit: local slice commit
  (`fix(sync): route SYNC-028B1T prepared fixtures`).

### SYNC-028B2 Closure Record

- Design and Oracle:
  `SpaceFixtureWorkflowDesktopAnchorObservationOwner` installs exact
  application-activation and active-Space observers before invoking
  `XCUIApplication.activate()`, then performs an initial readback and a
  trigger-return readback. A snapshot resolves only when the aggregate
  readiness PID still identifies a running, active fixture process; XCUI
  reports that process foreground; `NSWorkspace` reports the same bundle and
  PID frontmost; and the PID-scoped topmost on-screen CG window has a valid
  desktop-sized frame. The matching XCUI window must have the same frame
  within a two-point WindowServer/UI-session tolerance and contain one exact
  configured plan accessibility identifier. The plan index, title, and
  identifier therefore preserve workflow and window identity across standard
  and duplicate-title edge fixtures.
- Event gap and lifecycle: application activation and active-Space
  notifications each trigger a fresh complete readback. Initial and
  trigger-return readbacks close already-satisfied and synchronous transition
  gaps. The workflow invocation owns the observation owner, which owns its
  notification tokens, main-run-loop timer, XCTest expectation, and pure
  observation generation. Success, watchdog, replacement, cancellation, and
  deinitialization remove observers and invalidate polling. Generation
  equality rejects stale delivery; taking the active generation before the
  callback preserves synchronous replacement.
- Retained time policy: AppKit and XCUIApplication expose no exact
  activation-completion callback for this cross-process desktop/Space
  condition. The owner therefore retains the named 100ms
  `conditionPollInterval`, after the immediate readback, and cancels it with
  the workflow invocation. The named 15-second `watchdog` is a terminal
  failure bound. Its final readback may still resolve success; a failure
  reports every unmet condition plus distinct last and final process,
  frontmost, XCUI plan-window/frame, CG window/frame, and desktop-Space
  evidence.
- Diagnostic design evidence: UI attempts 001 and 002 showed that focused/main
  application AX attributes and the raw application AX window tree were
  unavailable to the UI runner while the exact process, frontmost application,
  and CG window were present. The final Oracle uses the XCUITest session's
  exact plan-identified window tree joined to the PID-scoped CG frame; UI
  attempts 003 through 006 then resolved immediately from that observable
  evidence, with the final two also requiring the observed accessibility label
  to match the configured title.
- Unit and Behavior: after the evidence/owner source split, the final focused
  canonical run rebuilt all six targets and passed 9/9 with zero failures in
  0.019 seconds (0.021 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-028B2/targeted-attempt-009`.
  Coverage includes initially satisfied state, exact process/frontmost/plan/
  XCUI/CG/desktop requirements, mismatched frames, cancellation, replacement,
  stale delivery, synchronous replacement, terminal diagnostics, slow
  conditional observation, and the existing desktop-refocus retry contract.
  The final staged-review supplement passed 2/2 with zero failures in 0.002
  seconds (0.003 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-028B2/targeted-attempt-010`,
  explicitly verifying duplicate post-completion delivery rejection and the
  retained last/final watchdog diagnostic.
- Full FlowTabTests: the canonical wrapper passed 996/996 with zero failures in
  49.814 seconds (49.988 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-028B2/full-attempt-001`.
- FlowTabCore: not relevant because the contract belongs to fixture UI-test
  orchestration and adds no core-domain API or runtime dependency.
- UI: the canonical install wrapper rebuilt and verified the fixed-path Apple
  Development-signed app before the final executions. The standard
  three-application workflow passed
  `testHomePageShowsMultipleRealSpaceFixtureWorkflowAppsAndWindowCounts` 1/1
  with zero failures in 27.368 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B2/ui-attempt-005`. The
  duplicate-title edge workflow passed
  `testSwitcherPanelPreviewKeepsIdenticalRealWorkflowWindowsDistinct` 1/1
  with zero failures in 18.151 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B2/ui-attempt-006`. Both paths
  activated the exact readiness PID and resolved the configured plan window on
  a desktop Space before continuing to their independent visible Oracles.
- Pressure: the deterministic lifecycle workload installed 500 replacement
  generations, delivered every stale generation, held the final generation
  across 50 incomplete condition readbacks, then resolved exactly once from
  final evidence and left no active owner. The two real multi-process UI paths
  exercised distinct workflow identities and scheduling; added observation
  latency changed completion time while preserving the exact result.
- Audit follow-up: the separate diagnostic run of the existing SYNC-026B UI
  path exposed the shared optional-window-identity defect registered as
  SYNC-034A. SYNC-028B2 uses a present XCUI plan window joined to a present CG
  window and its unit coverage rejects missing or mismatched identities.
- Process/Tooling: canonical wrappers compiled the evidence model, pure owner,
  UI lifecycle owner, and tests in both intended app-test targets.
  `plutil -lint`, Swift parse checks, source-size checks, scoped obsolete-wait
  review, `git diff --check`, and exact staged-content review are recorded
  before commit. All four new sources remain below 400 lines; the touched
  shared UI observation source remains below 800 lines. Startup `prompts.zip`
  remains unchanged and outside the slice.
- Commit: `397ab8c3f3d70537ff3619754d04181d2a38acba`
  (`refactor(sync): migrate SYNC-028B2 desktop anchor`).

### SYNC-034A Closure Record

- Defect and independent Oracle: the existing shared helper compared optional
  AX and CG window numbers directly, so two absent observations could return
  success. The independent nonnil assertion in the SYNC-026B real fixture UI
  reproduced the false positive in 1.859 seconds under
  `.build-local/evidence-driven-sync/SYNC-028B2/ui-diagnostic-desktop-refocus-attempt-001`.
  `SpaceFixtureWorkflowDesktopAnchorSnapshot.exactWindowIdentityMatches`
  now requires a present CG window number plus valid matching XCUI and CG
  frames; focused coverage rejects absent frames, missing CG identity, and
  mismatched frames before accepting exact evidence.
- Design and Oracle:
  `waitForExactFrontmostSpaceFixtureWindow` now delegates to
  `SpaceFixtureWorkflowDesktopAnchorObservationOwner`. Resolution requires the
  requested bundle and PID to be running, active, frontmost, and
  XCUI-foreground; the exact requested title and accessibility identifier must
  identify the XCUI window whose frame matches the present PID-scoped topmost
  on-screen CG window; and that window must have a desktop-Space frame. The
  snapshot reads `XCUIApplication.state` first and treats `.notRunning` as
  incomplete attachment evidence without querying the window tree. A later
  attachment therefore changes only completion time.
- Observation and lifecycle: the reused owner installs application-activation
  and active-Space observers before its initial readback. AppKit and
  XCUITest expose no exact callback for automation attachment plus the joined
  cross-process window condition, so the owner retains its named immediate-
  check-first 100ms condition poll. The helper invocation owns the owner and
  cancels observers, timer, expectation, generation, and pending delivery on
  success, failure, or return. The caller-owned named 25-second watchdog is a
  terminal failure bound whose final readback reports every unmet condition
  and the last/final process, XCUI, CG, frame, and Space evidence.
- Implementation cleanup: the private-symbol AX window-number bridge and its
  recursive identifier walk had no remaining owner after the exact
  plan-window/CG-frame join, so their source and project entries were removed.
  The shared UI observation source shrank while preserving its other AX and CG
  helpers.
- Unit and Behavior: the focused canonical wrapper rebuilt all six targets and
  passed 5/5 with zero failures in 0.004 seconds (0.005 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-034A/targeted-attempt-001`.
  Coverage includes absent and mismatched exact-window evidence, initially
  satisfied exact evidence, independent process/window/desktop conditions,
  cancellation, replacement, duplicate delivery rejection, and final watchdog
  diagnostics. The full canonical wrapper then passed 997/997 with zero
  failures in 49.190 seconds (49.345 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-034A/full-attempt-001`.
- FlowTabCore: not relevant because this contract belongs to fixture UI-test
  orchestration and changes no core-domain API or runtime dependency.
- UI: the first owner-backed run correctly rejected completion while the
  NSWorkspace-launched fixture was not yet attached to XCUITest, exposing the
  attachment-readiness query race. After attachment became explicit snapshot
  evidence, the desktop-refocus path passed 1/1 in 4.916 seconds under
  `.build-local/evidence-driven-sync/SYNC-034A/ui-attempt-002`. Expanded shared
  infrastructure regression passed the standard three-application workflow
  in 27.587 seconds and the duplicate-title edge workflow in 19.643 seconds,
  2/2 with zero failures in 47.229 seconds (47.231 seconds total), under
  `.build-local/evidence-driven-sync/SYNC-034A/ui-shared-regression-attempt-001`.
- Pressure: the reused owner retained the deterministic 500-generation
  replacement/cancellation workload, including 50 incomplete condition
  readbacks and forced stale delivery, and the SYNC-026B owner retained its
  independent 500-cycle retry/observer/watchdog workload. The three final real
  UI paths covered desktop refocus, independently scheduled multi-process
  readiness, and duplicate-title window identity without result drift.
- Process/Tooling: the canonical install, app-test, and UI wrappers rebuilt the
  intended six targets and verified signing before execution. Project-file
  lint, Swift parse checks, obsolete bridge/reference review, source-size
  checks, `git diff --check`, and exact staged-content review are recorded
  before commit. The new focused test remains below 400 lines; touched shared
  UI sources remain below 800 lines. Startup `prompts.zip` remains unchanged
  and outside the slice.
- Commit: `8092758e0e0fca7eb0d9a6345f504b9c50b900db`
  (`refactor(sync): migrate SYNC-034A exact window oracle`).

### SYNC-029 Closure Record

- Design and initial Oracle:
  `FlowTabUITestInitialPresentationObservationOwner` installs the exact
  runtime-service projection routes before its baseline readback and readiness
  request. Global presentation derives its item signature from the complete
  app-switcher projection after window-recency and app-visibility filtering.
  In-app presentation derives the focused app PID, app ID, and recency-ordered
  window IDs from the complete current-app projection. An initially complete
  nonempty projection can present immediately; an initially complete empty
  projection resolves as authoritative no-content. Later evidence must be a
  newly available projection, a component-wise monotonic generation, or a
  same-generation incomplete-to-complete transition. Regressed generations,
  duplicate candidates, and stale owner generations cannot present.
  `RuntimeReadModelGeneration` owns the shared component-wise monotonic
  comparison in Runtime infrastructure, so Home and TestingSupport consume one
  dependency-safe generation contract.
- Presentation readback: success requires the controller to report a presented
  session whose exact ordered item signature equals the accepted projection
  candidate. The post-action projection must preserve mode, PID, and item
  identity at the same or a component-wise monotonic later generation.
  Session-start maintenance may temporarily mark that later projection
  incomplete while preserving the accepted identity, so scheduling latency
  changes freshness convergence time without changing the presentation
  result. A changed PID or item signature cancels the presentation and waits
  for fresh evidence.
- Observation and lifecycle: the global route observes app-switcher projection
  commits; the in-app route observes both current-app and app-switcher
  projection commits. Each notification performs a full readback. One static
  bootstrap owner replaces and cancels the prior generation during app
  preparation, and AppDelegate termination cancels it before runtime service
  teardown. Resolution, no-content, watchdog failure, explicit cancellation,
  replacement, and deinitialization remove notification tokens; the owner
  cancels its watchdog and rejects every late generation.
- Retained time policy: the former presentation retry cadence and stable-
  snapshot count were removed. The named three-second `watchdog` remains only
  as a terminal failure bound. It performs one final readback that can still
  resolve success, then reports the unmet projection conditions and distinct
  last/final generation, completeness, PID, item, dirty-scope, presentation,
  Search, and post-readback evidence.
- Diagnostic correction: UI attempt 001 passed the real fixture path and
  exposed a mock Search failure where successful `startSession` maintenance
  advanced the projection before the immediate post-readback. The log recorded
  the exact unchanged six-app signature at a later temporarily incomplete
  generation. The final compatibility Oracle above removes that
  scheduler-dependent result while retaining exact identity checks.
- Unit and Behavior: the final focused canonical wrapper passed 8/8 with zero
  failures in 0.389 seconds (0.390 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-029/targeted-attempt-006`.
  Coverage includes initial complete and complete-empty states, observer-before-
  trigger ordering, same-generation completeness, later monotonic generation,
  regressed and stale generations, exact session mismatch, post-readback
  identity mismatch, duplicate candidate rejection, fresh retry, explicit
  cancellation, replacement, final-watchdog success, and last/final watchdog
  failure diagnostics. AppDelegate behavior verifies one later complete
  projection notification presents the exact two-app session and that
  complete-empty evidence performs no maintenance request.
- Full FlowTabTests: the canonical wrapper passed 1002/1002 with zero failures
  in 47.781 seconds (47.926 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-029/full-attempt-002`.
- FlowTabCore: not relevant because this owner exists only in the app
  `FLOWTAB_TESTING` boundary and adds no core-domain API or dependency.
- UI: the canonical install wrapper rebuilt and verified the fixed-path Apple
  Development-signed app for the final source state. The real fixture standard
  switcher path passed in 21.953 seconds and the mock window-Search launch path
  passed in 6.739 seconds, 2/2 with zero failures in 28.692 seconds (28.693
  seconds total), under
  `.build-local/evidence-driven-sync/SYNC-029/ui-attempt-003`. Their independent
  Oracles were the exact fixture app tile and exact mock Inbox window result.
- Pressure: the deterministic owner workload replaced 500 generations,
  force-delivered every cancelled watchdog and stale generation, then resolved
  the final generation exactly once. Its explicit-cancellation phase delivered
  a cancelled watchdog after a later ready snapshot and produced no callback.
  The post-presentation regression also advances to a later incomplete
  generation with the same identity and preserves the exact result.
- Process/Tooling: canonical wrappers compiled the four TestingSupport sources
  and three test sources in their intended targets. Project-file lint, Swift
  parse checks, scoped obsolete-wait review, source-size checks,
  `git diff --check`, and exact staged-content review are recorded before
  commit. Every new production source remains within the 400-line guardrail;
  the touched oversized AppDelegate lifecycle test source shrank. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `1c4dae662a0a9179b8edde21fb9ce8adc34e4385`
  (`refactor(sync): migrate SYNC-029 initial UI readiness`).

### SYNC-029A Closure Record

- Design and ordering:
  `FlowTabUITestInitialSearchActivationObservationOwner` installs the exact
  `.runtimeCommittedSearchIndexDidUpdate` observer and performs an initial
  Search-state readback before invoking its readiness trigger. Bootstrap
  preparation uses that trigger to request the Search freshness barrier before
  panel presentation can enqueue switcher-session maintenance on the runtime
  owner queue. A synchronous committed-index notification is retained through
  the observation generation even when it arrives before presentation.
- Terminal Oracle: after SYNC-029 presents the exact projection, the Search
  owner captures the ordered session app IDs, performs a presentation readback,
  invokes `enterSearchModeIfPossible`, and reads back again. A matching
  committed-index notification performs a full readback and may retry
  activation. Resolution requires the panel to remain presented, the current
  session IDs to equal the accepted ordered IDs, and `isSearchActive` to be
  true. Pending activation is diagnostic state only.
- Lifecycle: `FlowTabUITestBootstrapper` owns the one active Search observation.
  Preparation replaces it; presentation resolution transfers the accepted
  session signature; success, watchdog failure, explicit stop, replacement,
  AppDelegate termination, and deinitialization remove the notification route
  and cancel pending watchdog work. Observation generations reject delayed,
  duplicate, and out-of-order callbacks.
- Retained time policy: the named
  `searchIndexReadinessWatchdog` reuses the runtime transient-repair
  observation's 30-second terminal bound. It performs a final readback that can
  still resolve success, then reports the unmet exact session/Search condition
  and distinct last/final evidence. The affected real Search UI paths use the
  named 35-second `watchdogFailureObservationTimeout`, leaving five seconds for
  the application watchdog diagnostic and process cleanup. Elapsed time cannot
  satisfy either owner.
- Failure evidence: the first real app-Search run reproduced a three-second
  application watchdog with `committedIndexGeneration=0`, the exact presented
  session, `searchActive=0`, and `searchPending=1`. A diagnostic run showed the
  freshness request queued behind real-app switcher-session maintenance. The
  first window-Search run reproduced the same three-second cancellation. After
  the application watchdog was aligned with runtime readiness, the eight-second
  UI bound terminated the still-observing application before its terminal
  event. These observations produced the final observer-before-request ordering
  and named UI terminal policy.
- Unit and Behavior: the final focused canonical wrapper passed 8/8 with zero
  failures in 0.356 seconds (0.359 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-029A/targeted-attempt-004`.
  Coverage includes an initially satisfied readback, a delayed committed-index
  event, a synchronous pre-presentation event, explicit cancellation,
  replacement and stale-generation rejection across 100 iterations, duplicate
  delivery, final-readback success, watchdog last/final diagnostics, and
  bootstrap integration that verifies the first freshness request occurs while
  the observation is active and before panel presentation.
- Full FlowTabTests: the final canonical wrapper passed 1032/1032 with zero
  failures in 51.667 seconds (51.844 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-029A/full-attempt-005`.
  Full attempt 003 exposed one suite-order failure in the existing Search wrap
  publication test. Its exact isolated rerun passed 1/1, and a repeated
  test-without-building run passed 20/20 under
  `.build-local/evidence-driven-sync/SYNC-029A/search-wrap-pressure-attempt-001`
  before the final full pass.
- FlowTabCore: not relevant because the implementation is confined to the app
  `FLOWTAB_TESTING` and UI-test boundaries and adds no core-domain API or
  dependency.
- UI: the canonical install wrapper rebuilt, signed, and verified the fixed-path
  Apple Development app for the final source state under
  `.build-local/evidence-driven-sync/SYNC-029A/ui-install-final-001`. The real
  window-Search workflow passed 1/1 in 32.133 seconds under
  `.build-local/evidence-driven-sync/SYNC-029A/ui-window-search-attempt-004`;
  its independent Oracles were the accessible Search input, exact `Docs`
  fixture result, exact CG window number, frontmost fixture identity, and panel
  dismissal. The real app-Search workflow passed 1/1 in 29.394 seconds under
  `.build-local/evidence-driven-sync/SYNC-029A/ui-app-search-attempt-005`;
  its independent Oracles were the accessible Search input, exact Chrome
  fixture app result, frontmost bundle identity, and panel dismissal.
- Pressure: the owner regression replaces and cancels 100 generations, delivers
  stale committed-index callbacks, and resolves only the final generation.
  The Search wrap publication path also passed 20 consecutive XCTest
  iterations with identical result and scroll-revision Oracles.
- Process/Tooling: the canonical wrappers compile the TestingSupport,
  FlowTabTests, and FlowTabUITests sources in their intended targets. Project
  plist validation, unique project references, Swift parse checks, scoped
  obsolete-path review, source-size checks, `git diff --check`, and exact
  staged-content review are recorded before commit. A final-source compiler
  pass exposed actor isolation at destructor cancellation; the owner now
  transfers its watchdog token to a main-actor cancellation task while removing
  its notification observer synchronously. The two new production sources are
  381 and 202 lines, and the new test source is 396 lines. The controller
  behavior source is 350 lines, and the shared test-double source is 799 lines
  with one test-support responsibility. The UI support source is 638 lines and
  owns the named terminal policy; the oversized multi-app workflow remains at
  its 966-line slice baseline. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Commit: `0581ea0c9b03755618432586df7f8d2d86c94e6a`
  (`fix(sync): require SYNC-029A initial search activation`).

### SYNC-030A Closure Record

- Classification and policy:
  `--flowtab-ui-mock-window-preview-delay-ms` remains compatible and is
  classified as intentional mock I/O latency. The named
  `FlowTabUITestMockWindowPreviewLatencyPolicy` preserves the previous one-to-
  1,000 millisecond bounds. Elapsed time releases the injected capture work;
  it never establishes preview success.
- Owner and evidence:
  `FlowTabUITestMockWindowPreviewLatencyOwner` assigns exact owner and batch
  generations, request count, bounded policy, and `elapsed` or `cancelled`
  outcome to every wait. Its deadline waiter uses monotonic `DispatchTime`,
  registers each blocking capture, and signals all registered waiters on
  cancellation. Bootstrap preparation and replacement cancel the current
  owner; AppDelegate termination and owner deinitialization provide the final
  cleanup boundaries. Stopping also removes the installed model batch override.
  A cancelled batch returns no mock capture result.
- Independent Oracle: the existing preview pipeline accepts only its capture
  result, exact resolved window identity, current preview generation, cache
  application, and visible preview readback. TestingSupport now logs latency
  evidence independently. The representative UI test requires both exact
  preview-image identifiers and the `preview_batch_completed` state in addition
  to the injection marker.
- Unit and Behavior: the final focused canonical wrapper passed 5/5 with zero
  failures in 0.012 seconds (0.014 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-030A/targeted-attempt-003`.
  Coverage verifies policy bounds, exact elapsed evidence, an in-flight
  cancellation released by owner cancellation, the existing launch-option
  contract, Bootstrapper replacement/stop generation and override cleanup, and
  500 owner replacements retaining one final elapsed outcome.
  Attempt 001 stopped at compilation because two new test callbacks captured
  mutable arrays under `@Sendable`; the final tests store callback evidence
  behind an explicit lock.
- Expanded Behavior: the final complete canonical wrapper passed 1006/1006
  with zero failures in 48.409 seconds (48.565 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-030A/full-attempt-002`. Attempt 001
  reproduced the previously recorded suite-order duplicate hotkey-registration
  pair; its exact isolated rerun passed 1/1, and the final complete execution
  passed the same case in suite order.
- FlowTabCore: not relevant because the fault injection is confined to
  TestingSupport, the app-test target, and the UI-test path.
- UI: attempt 001 was blocked before the test body when macOS timed out while
  enabling automation mode. The canonical rerun passed
  `testControlTabFirstPhysicalGestureSelectsNextWindowWithVisiblePreview` 1/1
  with zero failures in 8.095 seconds under
  `.build-local/evidence-driven-sync/SYNC-030A/ui-attempt-003`. It observed the
  80ms elapsed marker, selected the exact secondary current-app window, exposed
  both exact preview images, and recorded preview-batch completion.
- Pressure: the deterministic 500-generation workload cancelled every prior
  owner, force-observed each stale owner as `cancelled`, and allowed only
  generation 500 to report `elapsed`. In-flight cancellation used explicit
  start and release evidence, so scheduler speed changed only completion time.
- Process/Tooling: project-file lint, Swift parse checks, scoped timing review,
  source-size checks, `git diff --check`, and exact staged-content review are
  recorded before commit. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Commit: `65a74c1b672aa62fc4a0ed7ca4ec1c7bc7eaa4fd`
  (`refactor(sync): migrate SYNC-030A preview latency`).

### SYNC-030B Closure Record

- Classification and policy:
  `--flowtab-ui-initial-panel-occlusion-stale-ms` remains compatible and is
  classified as an intentional TestingSupport staleness interval. The named
  `FlowTabUITestInitialPanelOcclusionStalenessPolicy` bounds the retained
  interval from one to 5,000 milliseconds. Elapsed time releases the injected
  stale state; exact panel readback and the existing recovery state establish
  the result.
- Owner and evidence:
  `FlowTabUITestInitialPanelOcclusionStalenessOwner` assigns monotonic owner and
  transition generations and publishes `installed`, `released`, or `cancelled`
  evidence with the bounded policy plus panel-availability, override-installed,
  and override-visible readback. The injected scheduler returns one cancellable
  release token. Replacement cancels the preceding generation before installing
  the next override, and a force-delivered stale callback is rejected by exact
  generation. Bootstrap preparation and explicit stop own synchronous cleanup;
  AppDelegate termination stops the owner, and owner deinitialization schedules
  token and injection cleanup on the main actor.
- Independent Oracle: the representative UI path requires exact installed and
  released evidence for the 260ms policy, the existing
  `presentationRecovery trigger=global_show action=complete` evidence, and the
  exact mock-browser switcher tile. The duration remains isolated from the
  panel-visibility and recovery success Oracle.
- Unit and Behavior: the final focused canonical wrapper passed 5/5 with zero
  failures in 0.108 seconds (0.109 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-030B/targeted-attempt-003`.
  Coverage verifies policy bounds, installed/released readback, explicit
  cancellation, replacement, force-delivered stale work, synchronous scheduler
  delivery, and deinitialization cleanup. Attempt 001 captured a compiler
  diagnostic for main-actor cleanup from `deinit`; the final implementation
  transfers the captured token and mutation into a main-actor cleanup task.
- Expanded Behavior: the complete canonical wrapper passed 1011/1011 with zero
  failures in 48.035 seconds (48.192 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-030B/full-attempt-001`.
- FlowTabCore: not relevant because this fault-injection policy and owner are
  confined to app TestingSupport, app tests, and the UI-test launch protocol.
- UI: the current signed test app installed successfully. The canonical
  `testSwitcherInitialPresentationStaleOcclusionDoesNotHardRecover` execution
  passed 1/1 with zero failures in 4.994 seconds (4.997 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-030B/ui-attempt-001`. It observed both
  exact staleness phases, completed presentation recovery, and found the exact
  visible switcher item.
- Pressure: the deterministic 500-generation replacement workload cancelled
  generations 1 through 499, force-delivered all of their callbacks, and
  accepted a release only for generation 500. Every scheduler token finished
  cancelled, so scheduling latency changes completion time while the exact
  generation determines the outcome.
- Process/Tooling: project-file lint, Swift parse checks, scoped timing review,
  source-size checks, `git diff --check`, and exact staged-content review are
  recorded before commit. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Commit: `e6ee592e35033f64b478b0a740e370e992e7a9ab`
  (`refactor(sync): migrate SYNC-030B occlusion staleness`).

### SYNC-031 Closure Record

- Classification and workload contract: the compatible
  `--flowtab-tab-stress-duration` and
  `--flowtab-tab-stress-interval-ms` inputs remain domain-duration pressure
  controls. `TabSwitchStressPolicy` bounds and converts them to monotonic
  nanoseconds, then derives an exact `ceil(duration / cadence)` selection
  workload. The first selection is immediate. Every later selection uses the
  named cadence, while completion requires both the requested duration and the
  complete planned workload.
- Owner and evidence: `TabSwitchStressRunner` owns one monotonic run
  generation, one transition generation, and at most one cancellable scheduler
  token. Each attempt records the requested target and reads back the exact
  selected tab. A matching readback advances the planned sequence; a mismatch
  retains the same target for a later attempt. Completion publishes the policy,
  attempts, confirmed switches, final requested/observed target, elapsed time,
  and both terminal conditions before requesting application termination.
  Explicit stop, AppDelegate termination, owner replacement, and
  deinitialization cancel the owned wake and reject stale callbacks.
- Independent Oracle: elapsed time releases cadence/deadline scheduling.
  Selection success comes from the exact `HomeTabState.selectedTab` readback.
  UI establishes a unique distributed-notification observer before launching
  the app, accepts the exact completed evidence, and then requires the process
  state to become `notRunning`. The stdout evidence route gives the pressure
  workflow the same exact workload and readback record.
- Unit and Behavior: the final focused canonical wrapper passed 10/10 with zero
  failures in 0.020 seconds (0.022 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-031/targeted-attempt-002`.
  Coverage verifies compatible input normalization, exact workload plus
  duration completion, immediate first selection, five-second delayed
  scheduling with the same target sequence and count, mismatched-readback
  retry, explicit cancellation, stale-generation rejection, synchronous
  scheduler delivery, deinitialization cleanup, and AppDelegate stop ownership.
- Expanded Behavior: the complete canonical wrapper passed 1020/1020 with zero
  failures in 48.257 seconds (48.440 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-031/full-attempt-001`.
- FlowTabCore: not relevant because this pressure workload is confined to the
  app TestingSupport boundary and its app/UI test targets.
- UI: the current Apple Development-signed test app installed successfully.
  The canonical `testTabSwitchStressCPUAndMemory` execution passed 1/1 with
  zero failures in 59.702 seconds (59.705 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-031/ui-attempt-001`. Each of its three
  measured launches established the observer before launch, accepted exactly
  125/125 readback-confirmed selections for the two-second/16ms policy, and
  observed exact process termination.
- Pressure: the deterministic 5,000-selection run preserved the exact cyclic
  sequence, count, five-second duration, one terminal result, and cancellation
  of every scheduler token. The 1,000-generation replacement run
  force-delivered every cancelled wake and allowed only generation 1,001 to
  complete. The real 20-second/20ms workload completed 1,000/1,000 exact
  selections in 87.399 seconds with 175 samples under
  `.build-local/evidence-driven-sync/SYNC-031/tab-switch-pressure-20ms-attempt-001`.
  The 20-second/50ms comparison completed 400/400 exact selections in 35.279
  seconds with 71 samples under
  `.build-local/evidence-driven-sync/SYNC-031/tab-switch-pressure-50ms-attempt-001`.
  Both recorded zero process/build failures and true duration/workload terminal
  evidence. The scheduling load changed completion latency while preserving
  the exact workload result.
- Process/Tooling: canonical app-test, install, UI, and pressure wrappers built
  the intended targets. Project-file lint, Swift parse checks, scoped timing
  review, source-size checks, `git diff --check`, and exact staged-content
  review are recorded before commit. Every new production and test source
  remains within the 400-line guardrail; the touched oversized switcher UI test
  source shrank. Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `3b4668a26ae3ab89f957b6a66aa5f6a07848269e`
  (`refactor(sync): migrate SYNC-031 tab stress`).

### SYNC-032A Closure Record

- Design and Oracle: both AppVisibility tests subscribe to the model's
  `@Published isLoading` stream before invoking `reload()`. The subscription
  first requires an observed loading value, then accepts the following idle
  value exactly once. The initial idle state therefore remains a baseline and
  cannot satisfy completion. Final assertions independently read the loaded
  app inventory, hidden count, selected app, and filtered Pinyin result.
- Lifecycle: each async test scope owns its Combine cancellable set. The
  one-shot publisher completes after the matching transition and test teardown
  releases the subscription. The five-second XCTest timeout is only a failure
  bound for a missing lifecycle transition.
- Unit and Behavior: the focused canonical wrapper passed 2/2 with zero
  failures in 0.531 seconds (0.533 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032A/targeted-attempt-002`.
  It covers the mock-runtime searchable inventory and a persisted hidden app
  absent from inventory.
- Expanded Behavior: removing the `FlowTabTests+Support.waitUntil` wall-clock
  loop affected the shared test target, so the complete canonical wrapper was
  run and passed 1020/1020 with zero failures in 46.723 seconds (46.870 seconds
  total) under
  `.build-local/evidence-driven-sync/SYNC-032A/full-attempt-003`.
  Diagnostic attempt 002 executed all 1020 tests and captured one transient
  duplicate hotkey-registration readback in the AppDelegate launch-bootstrap
  test before either extracted AppVisibility test ran. The exact failing test
  then passed 1/1 in
  `.build-local/evidence-driven-sync/SYNC-032A/bootstrap-rerun-001`; the
  preserved failure remains input to the planned AppDelegate delivery and
  launch-bootstrap slices.
- FlowTabCore, UI, and Pressure: not relevant because the slice changes only
  app-test synchronization around an existing published task lifecycle and
  does not change runtime behavior or a hot path.
- Process/Tooling: the canonical wrapper compiled all app, app-test, UI-test,
  fixture, and core targets. `plutil` project lint and Swift parse passed.
  Scoped timing review confirms that
  `FlowTabTests+Support` no longer contains a date deadline, task sleep, polling
  interval, or cancellation-swallowing wait. The focused new source is 72
  lines; the touched pre-existing oversized preferences source shrank from
  1,733 to 1,679 lines, and support shrank from 568 to 551 lines.
  `git diff --check` and exact staged-content review are recorded before commit.
  Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `83dc6d6adba43da1f3d4c5bf8ce7aa9c57718ed2`
  (`test(sync): migrate SYNC-032A app visibility reload`).

### SYNC-032B1 Closure Record

- Design and Oracle: the runtime-projection recording double exposes exact
  launch and termination callbacks. The test installs both callbacks before
  posting either workspace notification, accepts only the current appID/PID
  instance, and additionally requires the launch directory entry to carry that
  same identity. Callback expectations establish delivery; independent final
  arrays establish exact one-event readback. The existing coordinator assertion
  also proves that launch-window observation was prepared before the launch
  signal.
- Lifecycle: the test scope removes both recording callbacks in explicit
  teardown and in `defer`. It terminates the AppDelegate, posts launch and
  termination notifications again, and verifies that both recording arrays
  remain unchanged, proving workspace-observer cleanup.
- Retained time policy: the one-second XCTest expectation timeout is a terminal
  failure watchdog. Delivery succeeds only from the exact callback. The
  expectation descriptions identify the unmet transition and the following
  array assertions expose the final observed appID/PID evidence.
- Unit and Behavior: the final focused canonical wrapper passed 1/1 with zero
  failures in 0.038 seconds (0.039 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032B1/targeted-attempt-002`.
  The final complete canonical wrapper passed 1020/1020 with zero failures in
  47.899 seconds (48.046 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032B1/full-attempt-002`.
- FlowTabCore, UI, and Pressure: not relevant because this slice changes only
  app-test observation around existing synchronous workspace notification
  delivery and does not change runtime behavior, visible behavior, repeated
  asynchronous work, or a hot path.
- Process/Tooling: project-file lint, Swift parse, scoped timing review,
  `git diff --check`, and exact staged-content review are recorded before
  commit. The focused source is 169 lines. The touched pre-existing oversized
  AppDelegate test source shrank from 1,337 to 1,225 lines; the shared
  test-double source remains below its 800-line guardrail at 785 lines. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `65ef5cb4561d8a88a2a6fe0d07aee8a29b0d124d`
  (`test(sync): migrate SYNC-032B1 workspace delivery`).

### SYNC-032B2 Closure Record

- Pre-change evidence: complete FlowTabTests runs independently reproduced the
  launch-bootstrap assertion with the exact duplicate registration signatures
  `[FTAB, FTWN, FTAB, FTWN]` under
  `.build-local/evidence-driven-sync/SYNC-032A/full-attempt-002` and
  `.build-local/evidence-driven-sync/SYNC-032B2/full-attempt-001`. The hotkey
  observer received on the main queue but deferred its active-delegate check
  into an unowned `MainActor` task. Restoring `AppDelegate.shared` could
  therefore let an older delegate accept a historical notification through
  later global test hooks.
- Design and Oracle: the observer now enters the AppDelegate main-actor boundary
  directly from its declared main operation queue and handles the notification
  before delivery returns. It accepts only the delegate active at receipt,
  rejects self delivery and missing payloads, and applies the exact request.
  The test establishes a registration-evidence expectation before posting,
  filters by baseline-plus-one generation, request ID, and both configurations,
  then independently reads back registration evidence, the two exact monitor
  signatures and IDs, monitor starts, prior-monitor stops, and takeover result.
- Lifecycle and scheduling: AppDelegate owns the observer token from launch
  through termination and removes it synchronously. Notification delivery no
  longer creates asynchronous work that can outlive that token. A two-delegate
  regression changes `shared` only after delivery and yields the executor; the
  original receipt owner and exact generation remain unchanged. After both
  delegates terminate, a further notification leaves every generation and
  monitor record unchanged.
- Unit: not relevant because no pure domain rule or normalization changed; the
  contract is AppDelegate notification orchestration and is exercised at the
  Behavior layer.
- Behavior: the final focused canonical wrapper passed 2/2 with zero failures
  in 0.117 seconds (0.118 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032B2/targeted-attempt-005`.
  The final complete wrapper passed 1021/1021 with zero failures in 49.062
  seconds (49.232 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032B2/full-attempt-003`. The
  previously failing launch-bootstrap test passed in the same suite order.
- UI: not relevant because the normal Settings path already performs exact
  registration synchronously through `requestHotkeyReload`; this slice changes
  multi-delegate notification ownership and app-test observation without
  changing a visible control, persisted configuration, or single-delegate user
  journey.
- Pressure: the focused two-delegate lifecycle test alternated 500 exact
  notifications between active owners in 0.080 seconds. It verified 500
  per-delivery request-ID matches, exact per-owner generations, and 1,000
  replacement monitor records, then yielded scheduling and posted after both
  observer owners terminated without any additional evidence. This is a new
  deterministic
  local lifecycle baseline rather than a CPU/RSS regression comparison; the
  path has bounded work per infrequent configuration notification.
- FlowTabCore: not relevant because the observer and regression remain inside
  the app and app-test targets and add no core API or dependency.
- Process/Tooling: the canonical wrapper compiled all app and app-test sources.
  Project-file lint, Swift parse, scoped obsolete-wait review, source-size
  checks, `git diff --check`, and exact staged-content review are recorded
  before commit. The focused test source is 375 lines; production AppDelegate
  remains a single-responsibility 631-line source, and the touched pre-existing
  oversized lifecycle test shrank from 1,225 to 1,123 lines. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `06f26034464bc2529b920c2a33a70aaea12cbcbc`
  (`refactor(sync): migrate SYNC-032B2 hotkey delivery`).

### SYNC-032C Closure Record

- Design and Oracle: the termination-refresh regression now installs
  `onSearchStateChanged` before submitting the initial query and accepts only
  the active app scope, exact `bro` query, and exact `Browser` result. It then
  replaces that callback and installs `onSessionLayoutChanged` before changing
  the runtime projection or delivering the exact terminated app/PID. The
  second Search callback requires the refreshed app count and the same exact
  Search state. Both matching publications establish completion; independent
  final model readback confirms the committed projection, scope, query, and
  result.
- Lifecycle and retained time policy: the test scope clears both model
  callbacks in `defer`. One-shot guards reject repeated callback delivery.
  The one-second XCTest expectations are terminal failure watchdogs for the
  named missing Search or layout publication and never establish success.
  Both 10ms polling loops and their cancellation-swallowing sleeps were
  removed.
- Unit, Behavior, and Pressure: the focused canonical wrapper passed 3/3 with
  zero failures in 0.408 seconds (0.409 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032C/targeted-attempt-001`. The
  migrated termination-refresh Behavior test passed together with the
  scheduling owner's cancelled/out-of-order completion test and deterministic
  2,000-mutation latest-owner pressure test. Scheduler delay and superseded
  work cannot change the accepted revision or final result.
- Expanded Behavior: the complete canonical wrapper executed 1,021 tests and
  reported one independent suite-order failure in
  `testAppDelegateLaunchWithUITestBootstrapArgumentsSeedsLogsAndOpensSearch`
  after 49.513 seconds (49.681 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032C/full-attempt-001`. Its exact
  monitor signatures were `[FTAB, FTWN, FTAB, FTWN]`. The same test passed 1/1
  in 0.511 seconds under
  `.build-local/evidence-driven-sync/SYNC-032C/bootstrap-isolated-attempt-001`.
  The hosted-launch observation and lifecycle contract is assigned to
  SYNC-032H; the goal remains active.
- FlowTabCore and UI: not relevant because this slice changes only app-test
  observation of the existing model Search and projection callbacks. It adds
  no core-domain API, production behavior, visible control, or UI topology.
- Process/Tooling: the canonical wrapper compiled all six targets. Project-file
  lint, Swift parse, source-size review, and `git diff --check` passed. The
  focused source is 104 lines, and extracting the regression reduced the
  touched pre-existing oversized test source from 946 to 891 lines. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `a8469a684c5e7f504f598e78d9aeb3f00905674f`
  (`test(sync): migrate SYNC-032C termination search refresh`).

### SYNC-032H Closure Record

- Pre-change evidence: the complete Priority class reproduced the launch test's
  duplicate monitor signatures `[FTAB, FTWN, FTAB, FTWN]` with one failure
  among 585 tests under
  `.build-local/evidence-driven-sync/SYNC-032H/priority-source-diagnostic-attempt-002`.
  Permanent registration evidence identified generation 2 as
  `source=settings_view` with the same default configurations. Resetting
  stored preferences during UI-test bootstrap caused a hosted Settings view
  to request an already-applied registration, while the polling helper made
  total factory-record count a scheduling-sensitive success Oracle.
- Design and Oracle: `HotkeyRegistrationEvidence` now carries a stable source,
  including a shared `application_launch` value. The launch regression
  establishes three independent observers before
  `applicationDidFinishLaunching`: exact initial-presentation resolution for
  the target panel and active/pending Search, launch registration generation 1
  with both exact configurations, and a log clear followed by the requested
  append count. Completion joins those three signals. Final readbacks
  independently require Search session state, the exact two launch monitor
  signatures, registration generation/configuration/source, preference reset,
  seeded-log count and redaction, removal of the prelaunch log, and stress
  runner startup.
- Hosted Settings lifecycle: stored-value synchronization now performs an
  immediate registration-evidence readback and suppresses a reload when both
  registered configurations already match. Explicit user edits retain the
  existing unconditional persistence and reload path. The main-queue
  observation accepts evidence synchronously, so stopping the owner removes
  delivery and state without an unowned task. Matching-readback and stop
  regressions cover both paths.
- Lifecycle and retained time policy: the test scope owns both notification
  tokens, the runtime-log observation, and its thread-safe one-shot recorder;
  `defer` cancels or removes all of them and terminates the delegate. Initial
  presentation publishes its exact resolution before releasing its
  bootstrap-owned observation owner. The four-second XCTest timeout is a
  terminal failure watchdog for the three named missing signals; it never
  establishes success. The launch-specific 100ms polling cadence, deadline,
  and cancellation-swallowing sleep were removed.
- Unit and Behavior: the final focused canonical wrapper passed 6/6 with zero
  failures in 0.763 seconds (0.766 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032H/targeted-attempt-003`. It covers
  matching initial readback, synchronous stop cleanup, launch orchestration,
  initial-presentation readbacks, and both affected pressure contracts.
  The complete Priority class then passed 585/585 with zero failures in
  15.552 seconds (15.664 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032H/priority-final-attempt-001`,
  closing the original suite order. The complete canonical wrapper passed
  1022/1022 with zero failures in 49.010 seconds (49.170 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032H/full-attempt-001`.
- Pressure: the focused run retained the latest exact registration across
  2,000 generations and allowed only the final resolution across 500 replaced
  initial-presentation generations, including forced delivery of cancelled
  scheduler tokens. Scheduling changed completion latency while the accepted
  generation and final result remained exact. The complete 585-test Priority
  lifecycle run additionally exercised the hosted Settings/AppDelegate order
  that originally failed.
- FlowTabCore and UI: not relevant because this app/TestingSupport slice adds
  no core API and preserves the existing visible Settings controls,
  persistence format, explicit edit path, launch arguments, Search
  presentation, and fixture topology.
- Process/Tooling: the first sandboxed focused attempt did not enter testing
  because CoreSimulator XPC startup failed with xcodebuild exit 133 and no
  result bundle under
  `.build-local/evidence-driven-sync/SYNC-032H/targeted-attempt-001`; the
  canonical wrapper succeeded once granted normal Xcode service access.
  Project-file lint, Swift parse, scoped obsolete-helper/source-literal review,
  source-size checks, and `git diff --check` passed. The extracted launch test
  is 398 lines and its support recorder is 84 lines. The touched oversized
  lifecycle source shrank to 994 lines, the shared async support source shrank
  to 24 lines, and AppDelegate remains a single-responsibility 643-line
  source. Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `6c308e8c5fbc809283e341883725d59cce243378`
  (`refactor(sync): migrate SYNC-032H launch bootstrap`).

### SYNC-032D1 Closure Record

- Design and Oracle: both model-only regressions install
  `onWindowOnlyPreviewPreparationChanged` before the first snapshot request.
  The success path fulfills once only when both exact visible preview items
  contain images. The failure path fulfills once only when the special
  provider has one call and the model publishes
  `specialProviderUnavailable` with the exact next retry generation.
  Independent final readbacks verify visible item count/image state, special
  and generic provider call counts, failure reason, and retry generation.
- Lifecycle and retained time policy: each test clears its model callback in
  `defer`; a one-shot guard rejects duplicate batch delivery. The one-second
  XCTest expectation is a terminal failure watchdog whose description names
  the missing success or structured-failure publication. The former 50ms
  polling cadence, deadline, and cancellation-swallowing sleep were removed
  from both paths.
- Unit and Behavior: the final focused canonical wrapper passed 7/7 with zero
  failures in 0.696 seconds (0.700 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032D1/targeted-attempt-002`. The
  complete canonical wrapper passed 1022/1022 with zero failures in 48.498
  seconds (48.657 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032D1/full-attempt-001`.
- Pressure: the focused run covered a 1,000-window provider batch constrained
  to the visible page, explicit in-flight release, batch failure, structured
  provider failure, and 500 initial-reveal replacement/cancellation cycles.
  Every success remained tied to final image/failure state and the exact
  accepted owner generation.
- FlowTabCore and UI: not relevant because this slice changes app-test
  observation only. It preserves the preview resolver, capture scheduling,
  provider fallback behavior, visible panel behavior, core APIs, and fixture
  topology.
- Process/Tooling: focused attempt 001 stopped at compilation because the
  extracted file omitted its `FlowTabCore` import; no test ran. The import was
  restored before attempt 002. Project-file lint, Swift parse, scoped
  `waitUntil` review, source-size checks, `git diff --check`, and exact staged
  review passed. Extraction reduced the pre-existing 802-line preview-provider
  source to 657 lines; the new publication test source is 284 lines. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `3857a8576ac7cb805654f60a72113cc3fc74334f`
  (`test(sync): migrate SYNC-032D1 preview publication`).

### SYNC-032D2 Closure Record

- Design and Oracle: the reveal owner now retains the exact accepted
  `InitialWindowOnlyPreviewRevealEvidence` before invoking its ready callback
  and clears that readback when the next generation starts. The regression
  installs its preview-batch observer before presentation, preserves the
  controller's existing observer, and holds the capture batch behind an
  explicit gate. Releasing the gate must publish
  `previewBatchCompleted` for the exact observation and presentation
  generations. Success additionally requires zero pending and in-flight
  captures, completed owner/watchdog cleanup, panel alpha 1, the unchanged
  presentation generation, and one image for every exact final window row.
- Lifecycle and retained time policy: the test scope owns the batch gate,
  one-shot expectation, and callback wrapper. `defer` releases any blocked
  capture, restores the controller callback, and ends the presentation
  session. The one-second XCTest expectations are terminal failure watchdogs
  naming the missing batch-start or matching reveal evidence. The production
  250ms degraded-reveal watchdog remains the named SYNC-024B presentation
  policy and reports its final pending-capture readback; successful completion
  in this slice comes only from the matching preview-batch evidence. The
  former 80ms capture delay and generic 5ms polling cadence were removed.
- Unit and Behavior: the focused canonical wrapper passed 11/11 with zero
  failures in 0.214 seconds (0.216 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032D2/targeted-attempt-001`. It
  covers initial readback, batch completion, final watchdog readback,
  diagnostic watchdog failure, replacement, cancellation, synchronous
  watchdog delivery, controller reveal/degraded reveal, presentation-end
  cleanup, and the migrated regression. The first complete run executed
  1022 tests under
  `.build-local/evidence-driven-sync/SYNC-032D2/full-attempt-001`; both
  assertions belonged to
  `testSwitcherInitialVisibilityObserverAcceptsWindowNotificationReadback`,
  whose independent SYNC-019 owner remained in `presenting` after one
  `Task.yield()`. Its exact isolated rerun passed 1/1 in 0.037 seconds under
  `.build-local/evidence-driven-sync/SYNC-032D2/sync019-isolated-attempt-001`.
  The final complete canonical rerun passed 1022/1022 with zero failures in
  48.208 seconds (48.378 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032D2/full-attempt-002`.
- Pressure: the focused run exercised 500 preview-reveal
  replacement/cancellation generations and rejected stale watchdog work. The
  migrated regression also holds an arbitrarily slow capture at an explicit
  gate; release timing changes completion latency while the accepted
  generations, images, cleanup, and visible result remain exact.
- FlowTabCore and UI: not relevant because the retained evidence readback and
  regression belong to the app target and preserve the existing panel reveal,
  preview capture, core API, launch configuration, and fixture topology. The
  real visible path and degraded watchdog remain covered by the completed
  SYNC-024B UI slice.
- Process/Tooling: Swift parse, scoped `waitUntil`/fixed-delay review,
  source-size checks, `git diff --check`, and exact staged review passed. The
  production owner is 333 lines and the touched single-responsibility
  regression source is 610 lines. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Commit: `be645c87c6972aa72c609a0ba9b01a64fabccec2`
  (`test(sync): migrate SYNC-032D2 preview reveal`).

### SYNC-032E1 Closure Record

- Design and Oracle: the Search viewport callback is installed before panel
  presentation and preserves any prior test observer. `enterSearchMode()`
  must immediately publish a non-empty result state, which is verified by
  direct readback. Before the first down-arrow and the final wrap action, the
  test records the exact next `searchResultScrollRevision` and first-result
  ID. Each transition resolves only from the real SwiftUI scroll callback
  while the model still reports that revision and ID. Waiting for the
  first-focus callback before issuing later inputs establishes a clean
  generation baseline; final readbacks require selected index zero and the
  exact first result.
- Lifecycle and retained time policy: the test scope owns two one-shot
  expectations and the callback wrapper. `defer` restores the prior callback
  and ends the presentation session. Each one-second XCTest timeout is a
  terminal failure watchdog naming the missing first-focus or exact wrap
  publication. The two generic 50ms condition-poll cadences and their
  two-second deadlines were removed; no product duration is introduced or
  changed.
- Unit and Behavior: the focused canonical wrapper passed 6/6 with zero
  failures in 0.646 seconds (0.648 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032E1/targeted-attempt-001`. It
  covers hosted SwiftUI viewport delivery, first/last result transitions,
  model-level wrap semantics, slow-scheduler invariance, cancelled and
  out-of-order completion rejection, and latest-owner pressure. The complete
  canonical wrapper passed 1022/1022 with zero failures in 49.952 seconds
  (50.110 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032E1/full-attempt-001`.
- Pressure: the focused run replaced 2,000 Search scheduling generations,
  force-fired 1,999 cancelled entries, and retained only the latest work. A
  separate manual-clock case changed scheduler/executor latency while
  preserving the exact final query, revision, result selection, and cleanup.
- FlowTabCore and UI: not relevant because this slice migrates app-test
  synchronization around existing model and SwiftUI evidence without
  changing Search behavior, core APIs, launch configuration, accessibility,
  or fixture topology. The hosted Behavior test executes the affected
  viewport task and exact visible scroll route.
- Process/Tooling: project-file lint, Swift parse, scoped remaining-`waitUntil`
  review, source-size checks, `git diff --check`, and exact staged review
  passed. Extraction reduced the pre-existing 1,251-line mixed session/Search
  source to 1,194 lines; the new focused viewport publication source is 167
  lines. Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `454c63574e270eba9a2b8b7cccfd40748fc71a27`
  (`test(sync): migrate SYNC-032E1 search scroll publication`).

### SYNC-032E2 Closure Record

- Design and Oracle: the regression installs
  `.flowTabUITestInitialPresentationDidResolve` for the exact panel controller
  before invoking launch bootstrap. It accepts only a positive observation
  generation resolved as `presented` from `initialReadback`, with global-mode
  baseline/candidate snapshots, the complete ordered app-ID set, a successful
  presentation attempt, Search active or pending, and a matching
  post-presentation readback. At notification delivery, independent controller
  readbacks require active Search, exact result count and selected app kind,
  current layout measurements, final panel height, and a released bootstrap
  owner. The same state is read back after expectation completion.
- Lifecycle and retained time policy: the test scope owns the notification
  token, launch option overrides, presentation session, and bootstrap
  observation cleanup; the temporary-preferences helper restores its keys.
  The one-second XCTest expectation is a terminal failure watchdog, and a
  rejected notification is retained for full evidence diagnostics. The
  TestingSupport owner's named three-second watchdog remains its terminal
  failure bound with final projection readback. The generic 50ms condition
  cadence and two-second polling deadline were removed; neither watchdog
  establishes success.
- Unit and Behavior: targeted attempt 001 executed all seven tests; the five
  owner/lifecycle tests and independent Search sizing test passed, while the
  migrated Oracle compared a stable Search UI ID (`app:<bundleID>`) with a raw
  bundle ID and correctly rejected the notification. The Oracle now matches
  `SwitcherSearchResultKind.app(appID:)`. The final focused canonical wrapper
  passed 7/7 with zero failures in 0.305 seconds (0.306 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032E2/targeted-attempt-002`. It
  covers initial satisfied state, observer-before-trigger delivery, fresh
  attempts, watchdog final readback, exact Search sizing, cancellation, and
  replacement. The complete canonical wrapper passed 1022/1022 with zero
  failures in 49.552 seconds (49.717 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032E2/full-attempt-001`.
- Pressure: the focused run replaced 500 initial-presentation generations,
  delivered every stale event against the active owner, force-fired cancelled
  watchdog work, and accepted only the final matching generation. Scheduling
  changes completion time while controller identity, item set, Search state,
  layout, and cleanup remain exact.
- FlowTabCore and UI: not relevant because this slice migrates app-test
  synchronization around existing TestingSupport evidence without changing
  core APIs, launch arguments, Search presentation behavior, accessibility,
  or fixture topology. The Behavior test hosts the real controller, SwiftUI
  content, bootstrap owner, and panel sizing path.
- Process/Tooling: project-file lint, Swift parse, scoped remaining-`waitUntil`
  review, source-size checks, `git diff --check`, and exact staged review
  passed. Extraction reduced the pre-existing mixed session/Search source
  from 1,194 to 1,142 lines; the new focused initial-Search presentation
  source is 192 lines. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Commit: `6f00bc18461643a8ccb023d0c71de66c506c4def`
  (`test(sync): migrate SYNC-032E2 initial search presentation`).

### SYNC-032F Closure Record

- Design and Oracle: both quit-shortcut regressions construct the controller
  with `ManualTerminatePressFeedbackScheduler` before starting the
  presentation. Key input must first leave
  `TerminatePressFeedbackCompletionOwner` pending at generation one, schedule
  the named policy interval, and publish no termination request. Explicitly
  firing that completion must synchronously clear the owner and publish a
  `PendingTerminateRequest` with exact selected app ID, PID 42100, and request
  generation one. Independent readbacks verify the termination animation,
  projection-maintenance sequence, unchanged pre-notification session, and
  exact post-workspace-notification layout and selected app.
- Lifecycle and retained time policy: the controller owns the completion
  owner/token and presentation-end cancellation. The second regression wraps
  and restores the controller's existing layout callback; both tests end the
  presentation in `defer`. The 120ms
  `TerminatePressFeedbackPolicy.completionInterval` remains the named,
  injected visual press-feedback contract. It determines when completion is
  delivered, while request success comes only from the exact scheduler
  completion and request readback. The one-second layout expectation is a
  terminal failure watchdog. Both generic 10ms condition cadences and
  one-second terminate-entry polling deadlines were removed.
- Unit and Behavior: the focused canonical wrapper passed 8/8 with zero
  failures in 0.138 seconds (0.141 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032F/targeted-attempt-001`. It
  covers both quit entry paths, scheduled completion identity, single
  delivery, replacement, cancellation, synchronous scheduler delivery, exact
  controller request publication, and lifecycle pressure. The complete
  canonical wrapper passed 1022/1022 with zero failures in 49.089 seconds
  (49.241 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032F/full-attempt-001`.
- Pressure: the focused owner workload delivered 500 generations, explicitly
  cancelled every even generation, force-fired cancelled work, and accepted
  exactly the 250 current odd generations in monotonic order. Duplicate fire
  was rejected and synchronous scheduler delivery cleaned its returned token,
  so scheduler latency changes completion time without changing request
  identity or result.
- FlowTabCore and UI: not relevant because this slice migrates app-test
  synchronization and shares existing scheduler test support without changing
  core APIs, hotkey configuration, animation duration, termination behavior,
  accessibility, launch options, or fixture topology. The Behavior tests
  exercise the real key-routing, visual-feedback owner, projection
  maintenance, workspace refresh, and panel layout paths.
- Process/Tooling: project-file lint, Swift parse, scoped stale-`waitUntil`
  review, source-size checks, `git diff --check`, and exact staged review
  passed. Extraction reduced the mixed session/Search source from 1,142 to
  1,027 lines and the completion-owner test source from 306 to 227 lines; the
  new focused quit-entry source is 294 lines and shared scheduler support is
  84 lines. Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `cf4890e2c57d4024add168e91698899e1f4adc4d`
  (`test(sync): migrate SYNC-032F terminate feedback entry`).

### SYNC-032G1 Closure Record

- Design and Oracle: the three configured auto-entry regressions now inject
  `ManualDelayedWindowLayerEntryScheduler` before presentation. Each path
  requires an app-layer session, one pending
  `DelayedWindowLayerEntryObservationOwner`, and the exact configured
  override or preference interval before deadline delivery. The complete
  selected-app projection is read back while the mode remains `.appCycle`;
  explicitly firing the owned deadline must then synchronously enter
  `.windowCycle` for the same app ID and clear the pending owner.
- Lifecycle and retained time policy: the presentation session owns the
  deadline token and cancels it on session end. The configurable
  `windowLayerPresentationDelay` remains a user-visible product contract.
  Scheduler latency determines when deadline evidence arrives; selected app
  identity, committed windows, and the resulting layer come from exact
  readbacks. The three one-second polling watchdogs and their 10ms cadences
  were removed.
- Unit and Behavior: after extracting the regressions into the focused owner
  source, the first compile identified a file-private assertion helper; the
  final test now reads the recording service directly. The final focused
  canonical wrapper passed 7/7 with zero failures in 0.159 seconds (0.160
  seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032G1/targeted-attempt-003`. It
  covers override and preference durations, initial complete projection,
  deadline delivery, session cancellation, and owner pressure. The complete
  canonical wrapper passed 1022/1022 with zero failures in 41.565 seconds
  (41.714 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032G1/full-attempt-002`.
- Pressure: the focused owner test replaced the deadline observation 2,000
  times, retained one current token, cancelled it, force-fired all stale
  scheduler work, and accepted no completion. The result therefore depends
  on current generation and final readback across arbitrary scheduler load.
- FlowTabCore and UI: not relevant because this slice migrates app-test
  synchronization around an existing injected product scheduler without
  changing core APIs, preference compatibility, window identity,
  accessibility, visual output, launch behavior, or fixture topology. The
  Behavior tests execute the real controller, projection service, and panel
  session path.
- Process/Tooling: Swift parse, scoped remaining-`waitUntil` review,
  source-size review, `git diff --check`, and exact diff review passed. The
  extraction reduced `PanelSessionBehavior.swift` from 2,394 to 2,240 lines;
  the focused delayed-entry source is 631 lines with one responsibility. Two
  remaining calls are assigned to SYNC-032G2. Startup `prompts.zip` remains
  unchanged and outside the slice.
- Commit: `a88bdde79a5e1be066282f4827d5bf1a04d8f3cc`
  (`test(sync): migrate SYNC-032G1 delayed entry deadline`).

### SYNC-032G2 Closure Record

- Design and Oracle: the manual-entry regression establishes the controller
  and injected `ManualDelayedWindowLayerEntryScheduler` before presentation,
  then records both projection and delayed-owner generation baselines.
  `.downArrow` must synchronously publish one exact selected-app maintenance
  request with the current PID, advance both generations once, and retain the
  app-layer session. A mismatched app event is rejected without another
  projection read. Publishing the complete matching projection and invoking
  the explicit update callback must advance the projection generation again,
  enter `.windowCycle` for the same app ID, expose the two exact window IDs,
  and cancel the pending delayed owner.
- Lifecycle and retained time policy: the presentation session owns the
  delayed-entry token and cancellation. The test's 30-second override proves
  that matching projection evidence completes manual entry independently of
  the later user-configured auto-entry deadline; the injected scheduler never
  advances. Both one-second polling watchdogs and 10ms polling cadences were
  removed.
- Unit and Behavior: the first focused attempt exposed an absolute generation
  assumption after session startup had already established generation one.
  Capturing baselines before input made the Oracle lifecycle-relative. The
  final focused canonical wrapper passed 6/6 with zero failures in 0.168
  seconds (0.169 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032G2/targeted-attempt-002`. It
  covers model replay, controller commit, mismatched and matching events,
  delayed-owner cancellation, and replacement pressure. The complete
  canonical wrapper passed 1022/1022 with zero failures in 41.957 seconds
  (42.110 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-032G2/full-attempt-001`.
- Pressure: the focused owner test replaced the observation 2,000 times,
  retained only the current generation, cancelled it, and rejected every
  force-fired stale deadline. The representative callback also proves that
  the explicit projection event cancels its still-pending 30-second token.
- FlowTabCore and UI: not relevant because this slice migrates app-test
  synchronization and source organization without changing core APIs,
  preferences, panel visuals, accessibility, launch behavior, or fixture
  topology. The Behavior tests execute the real model, projection service,
  controller input route, and panel-session owner.
- Process/Tooling: project-file lint, Swift parse, repository-wide app-test
  `waitUntil` absence, source-size review, `git diff --check`, and exact diff
  review passed. `PanelSessionBehavior.swift` shrank from 2,240 to 2,140
  lines; the new focused source is 210 lines. The generic async helper and its
  Xcode project reference were replaced by the focused regression. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Commit: `171cee6eceed016b72233d84e6c31e7ec94dbe0f`
  (`test(sync): migrate SYNC-032G2 manual entry evidence`).

### SYNC-033C Closure Record

- Design and Oracle: the test enters every background publisher and registers
  DispatchGroup completion on the main queue before dispatch. Each task records
  its exact notification name only after `NotificationCenter.post` returns.
  The named 500ms blocking watchdog remains solely as the proof boundary that
  background publication does not synchronously wait for MainActor delivery.
- Lifecycle and diagnostics: the DispatchGroup completion expectation is
  delivered through XCTest for deterministic cleanup after the blocking proof.
  This allows pending MainActor work to run. The test scope owns the group,
  expectation, completed-name evidence, and
  controller lifetime. Watchdog failure reports the sorted completed and
  expected notification names.
- Unit and Behavior: the final focused canonical wrapper passed 1/1 with zero
  failures in 0.022 seconds under
  `.build-local/evidence-driven-sync/SYNC-033C/targeted-attempt-002`. The
  complete canonical wrapper passed 1022/1022 with zero failures in 42.386
  seconds (42.549 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-033C/full-attempt-001`.
- UI and Pressure: not relevant because the slice changes only the test
  synchronization and diagnostics around the existing notification-delivery
  behavior; no runtime implementation, visible behavior, or repeated hot-path
  scheduling changes.
- Process/Tooling: Swift parse and scoped source-literal review passed. The
  first focused attempt passed 1/1 but exposed Swift 6 warnings for blocking and
  lock operations in an async context. The final synchronous MainActor test and
  lock-protected `@unchecked Sendable` evidence recorder compiled without those
  warnings. Final `git diff --check` and exact staged-content review are
  recorded before commit. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Commit: `4322115a746fb82a0930b7d6d066fb4d8470e27a`
  (`test(sync): migrate SYNC-033C notification publication`).

### SYNC-033D Closure Record

- Design and Oracle: the test establishes one-shot worker-start,
  resolver-invoked, and fetch-completed evidence before dispatching the
  background read. Once the worker has started, MainActor blocks only on the
  resolver-invocation watchdog. Success requires that exact signal, an
  off-main-thread resolver identity, and terminal fetch completion; no elapsed
  duration participates in the success Oracle.
- Lifecycle and diagnostics: the test scope owns both semaphores, the
  completion expectation, the locked evidence recorder, and all AX/permission
  override restoration. Named two-second watchdogs are terminal failure bounds.
  Every failure path reports the last worker-start, resolver-thread, and
  fetch-completion readback.
- Unit and Behavior: the final focused canonical wrapper passed 1/1 with zero
  failures in 0.003 seconds (0.004 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-033D/targeted-attempt-002`. The
  complete canonical wrapper passed 1022/1022 with zero failures in 49.480
  seconds (49.644 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-033D/full-attempt-001`.
- Pressure: 100 consecutive repetitions passed with zero failures in 0.101
  seconds (0.112 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-033D/pressure-attempt-001`. Each
  iteration independently required the exact background resolver and terminal
  completion evidence.
- UI: not relevant because this slice changes only app-test synchronization
  around the existing off-main AX resolver path and does not change visible
  behavior.
- FlowTabCore: not relevant because the slice changes no core-domain source or
  API.
- Process/Tooling: project-file lint, Swift parse, source-literal review, and
  source-size review passed. Focused attempt 001 passed 1/1 but exposed a Swift
  6 warning for capturing `NSRunningApplication` in a Sendable closure; the
  worker now performs that read in its own context and attempt 002 compiled
  without the warning. Extracting the test reduced the pre-existing oversized
  Runtime Space classification source from 1,169 to 1,116 lines; the focused
  source is 139 lines. Final `git diff --check` and exact staged-content review
  are recorded before commit. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Commit: `a70b527363f21a3810b7db0ee410dbd3fc96ce07`
  (`test(sync): migrate SYNC-033D AX background resolution`).

### SYNC-033E Closure Record

- Design and Oracle: the test installs a condition-backed workload gate before
  starting collection. The first four workers must enter and remain blocked,
  proving the configured concurrency from exact in-flight evidence. Explicit
  release then permits all 28 tasks to finish. Success requires maximum
  in-flight count four, every exact index, zero remaining work, and the original
  ordered result; no sleep or elapsed-time threshold participates in the
  Oracle.
- Lifecycle and diagnostics: the test scope owns the gate, DispatchGroup,
  result recorder, release, and idempotent cancellation. A dedicated regression
  cancels a saturated gate and requires all blocked work to exit. Named
  two-second saturation and completion watchdogs are terminal failure bounds;
  failures report entered indices, current/maximum in-flight counts,
  release/cancellation state, and the final result readback.
- Unit and Behavior: the focused canonical wrapper passed 2/2 with zero
  failures in 0.002 seconds (0.003 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-033E/targeted-attempt-001`. The
  complete canonical wrapper passed 1023/1023 with zero failures in 48.699
  seconds (48.877 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-033E/full-attempt-001`.
- Pressure: 100 repetitions of both explicit-release and cancellation paths
  passed 200/200 with zero failures in 0.076 seconds (0.095 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-033E/pressure-attempt-001`.
  Scheduling changed only when workers reached the gate; every iteration
  retained the exact concurrency bound, completion, and ordered-result Oracle.
- UI and FlowTabCore: not relevant because this slice changes only app-test
  workload synchronization around an unchanged runtime coordinator.
- Process/Tooling: Swift parse, scoped obsolete-sleep/elapsed-literal review,
  source-size review, and compiler-warning review passed. The focused source is
  217 lines with one pressure-workload responsibility. Final
  `git diff --check` and exact staged-content review are recorded before commit.
  Startup `prompts.zip` remains unchanged and outside the slice.
- Commit: `c76b13c9c24f515fe7be223cfbdb835d38bc54c5`
  (`test(sync): migrate SYNC-033E AX collection pressure`).

### SYNC-033A Closure Record

- Design and Oracle: both hosted Logs control tests now perform one explicit
  layout pass and immediately require the exact AppKit control type and
  accessibility identifier from the resulting hierarchy. The action-button
  test independently verifies localized titles, metrics, font, and
  appearance-derived style. The dropdown test independently verifies the
  bound log-level update, selected identifier, and absence of a native
  `NSPopUpButton`. The scoped paths retain no duration, observer, timer,
  retry, polling cadence, or timeout.
- Behavior targeted: 2/2 passed in 0.020/0.021 seconds through the canonical
  FlowTabTests runner at
  `.build-local/evidence-driven-sync/SYNC-033A/targeted-attempt-001`.
- Pressure: 100 repeated iterations of each path, 200/200 total, passed in
  1.956/1.987 seconds at
  `.build-local/evidence-driven-sync/SYNC-033A/pressure-attempt-001`.
- Behavior full: 1023/1023 passed in 49.004/49.157 seconds at
  `.build-local/evidence-driven-sync/SYNC-033A/full-attempt-001`.
- Process/Tooling: changed Swift files parse, the Xcode project plist is
  valid, changed-file compiler diagnostics contain no warnings, scoped
  literal search confirms the removed waits do not remain, and source-file
  sizes are 142 lines for `FlowTabTests+CompactActionButton`, 83 lines for
  `FlowTabTests+RuntimeLogsHostedControls`, and 1639 lines for the pre-existing
  oversized `FlowTabTests+PreferencesAndDiagnostics` after extracting the
  migrated test. Final `git diff --check` and exact staged-content review are
  recorded before commit. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Unit, UI, and FlowTabCore: not relevant because this slice changes only
  app-test synchronization around existing hosted controls.
- Commit: `b07c75cb7f2adf6b70df3b543d46a621f5fa62f2`
  (`test(sync): migrate SYNC-033A hosted Logs controls`).

### SYNC-033B1 Closure Record

- Design and Oracle: after applying the app-scoped
  `AppleInterfaceStyle` value, the test explicitly invokes
  `SystemThemeState.refreshColorScheme()` and immediately requires both the
  current `NSApp.effectiveAppearance` resolution and the state-owned
  `colorScheme` to match the captured system baseline. Cleanup restores the
  app appearance and defaults before another explicit refresh. The scoped
  path retains no observer, duration, retry, polling cadence, or watchdog.
- Behavior targeted: 1/1 passed in 0.002/0.003 seconds through the canonical
  FlowTabTests runner at
  `.build-local/evidence-driven-sync/SYNC-033B1/targeted-attempt-001`.
- Pressure: 200/200 repeated direct-readback iterations passed in
  0.187/0.207 seconds at
  `.build-local/evidence-driven-sync/SYNC-033B1/pressure-attempt-001`.
- Behavior full: 1023/1023 passed in 42.003/42.159 seconds at
  `.build-local/evidence-driven-sync/SYNC-033B1/full-attempt-001`.
- Process/Tooling: changed Swift files parse, the Xcode project plist is
  valid, changed-file compiler diagnostics contain no warnings, scoped
  literal search confirms the extracted path contains no time logic, and
  source-file sizes are 65 lines for `FlowTabTests+SystemThemeReadback` and
  1597 lines for the pre-existing oversized
  `FlowTabTests+PreferencesAndDiagnostics` after extraction. Final
  `git diff --check` and exact staged-content review are recorded before
  commit. Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, UI, and FlowTabCore: not relevant because this slice changes only one
  app behavior test around an existing synchronous state readback.
- Commit: `3d09a602ed242809944ba4439e2e726af0c82a4c`
  (`test(sync): migrate SYNC-033B1 system theme readback`).

### SYNC-033B2 Closure Record

- Design and Oracle: the unchanged Settings content is wrapped only in the
  app-test target with a zero-size SwiftUI transaction probe. The recorder
  assigns monotonic context generations. Each hot language, explicit-theme,
  or follow-system test captures an initial generation and exact initial
  context/view readback, installs a cancellable callback, then performs the
  trigger. A callback is accepted only for a newer matching context; final
  success independently requires one exact AppKit Settings container plus
  localized text, target appearance, dark card topology, container rebuild
  identity, or layout frames as appropriate. Cold-dark materialization uses
  the probe's initial context and immediate hierarchy readback. The probe,
  recorder, callback, and cancellation token are test-owned and leave
  production modules unchanged.
- Timeout policy: `SettingsPresentationObservationPolicy.watchdogTimeout`
  is the named one-second XCTest failure bound. Expiry reports the baseline
  generation and last observed context evidence. Observer cancellation runs
  through `defer`; matching completion, duplicate callbacks, and test
  cancellation cannot leave a registered handler. The migrated paths retain
  no sleep, timer, RunLoop advancement, retry, or polling cadence.
- Unit and Behavior targeted: the recorder generation/cancellation test plus
  five hosted presentation paths passed 6/6 in 3.465/3.467 seconds through
  the canonical FlowTabTests runner at
  `.build-local/evidence-driven-sync/SYNC-033B2/targeted-attempt-003`.
  Attempt 001 stopped at compilation because the extracted files lacked
  explicit `FlowTabCore` imports; the imports were added before attempts 002
  and 003 passed.
- Pressure: 20 repeated iterations of all six paths, 120/120 total, passed
  in 66.177/66.200 seconds at
  `.build-local/evidence-driven-sync/SYNC-033B2/pressure-attempt-001`.
- Behavior full: 1024/1024 passed in 43.035/43.192 seconds at
  `.build-local/evidence-driven-sync/SYNC-033B2/full-attempt-001`.
- Process/Tooling: changed Swift files parse, the Xcode project plist is
  valid, the scoped time-literal search is clean, and compiler diagnostics
  contain no warnings for the new or extracted Settings presentation files.
  New files are 182, 294, 205, and 294 lines; the extraction reduces
  `FlowTabTests+EnglishLayout` from 913 to 726 lines and the pre-existing
  oversized `FlowTabTests+PreferencesAndDiagnostics` from 1597 to 1462
  lines. Final `git diff --check` and exact staged-content review are
  recorded before commit. Startup `prompts.zip` remains unchanged and outside
  the slice.
- UI and FlowTabCore: not relevant because this slice changes app-test
  synchronization and test-file organization around existing Settings
  presentation behavior.
- Commit: `c87d1483fad7505e70375e4beb771de17157e4ea`
  (`test(sync): migrate SYNC-033B2 Settings presentation evidence`).

### SYNC-033B3 Closure Record

- Design and Oracle: hosted Settings layout first establishes an exact denied
  permission readback generation and localized request-title baseline. The
  test then installs a generation-filtered granted-readback callback and an
  exact `NSButton.title` KVO observation before clicking. The request stub
  changes the trusted readback while deliberately returning a different
  request result, so success requires the coordinator's independent
  post-request readback, one request, a newer granted generation, and the
  localized manage title.
- Lifecycle and timeout policy: the test scope owns the hosted view,
  permission recorder, callback token, and KVO token. `defer` cancels the
  recorder callback, invalidates KVO, restores every permission override,
  restores presentation language, and restores the app-language default.
  `SettingsPermissionReadbackPolicy.watchdogTimeout` is the named one-second
  XCTest failure bound; diagnostics retain the baseline generation, last
  permission readback, and last observed title. The migrated path contains no
  sleep, RunLoop advancement, timer, retry, or condition polling.
- Unit and Behavior targeted: the recorder generation/cancellation test and
  hosted permission path passed 2/2 in 0.353/0.354 seconds through the
  canonical FlowTabTests runner at
  `.build-local/evidence-driven-sync/SYNC-033B3/targeted-attempt-004`.
  Attempt 002 stopped before build when sandboxed Xcode/CoreSimulator services
  terminated. The attempt-003 compiler scan exposed a Swift 6 captured-state
  warning in KVO delivery; moving title evidence into a MainActor-owned
  recorder removed the warning before attempt 004.
- Pressure: 100 repeated iterations of both paths, 200/200 total, passed in
  17.326/17.350 seconds at
  `.build-local/evidence-driven-sync/SYNC-033B3/pressure-attempt-003`.
- Behavior full: 1025/1025 passed in 42.412/42.578 seconds at
  `.build-local/evidence-driven-sync/SYNC-033B3/full-attempt-003`.
- Process/Tooling: changed Swift files parse, the Xcode project plist is
  valid, compiler diagnostics contain no warnings for the new permission
  readback file, and scoped searches confirm the two extracted Settings files
  contain no raw RunLoop or sleep synchronization. The new file is 275 lines;
  extraction reduces the pre-existing oversized
  `FlowTabTests+PreferencesAndDiagnostics` from 1462 to 1379 lines. Final
  `git diff --check` and exact staged-content review are recorded before
  commit. Startup `prompts.zip` remains unchanged and outside the slice.
- UI and FlowTabCore: not relevant because this slice changes app-test
  synchronization around the existing permission observation and Settings
  presentation path.
- Commit: `413038061ae0e6eaa8808405e4dc75676bfa8b4e`
  (`test(sync): migrate SYNC-033B3 permission readback`).

### SYNC-034B Closure Record

- Design and Oracle: a generation-owned UI condition observer registers
  `NSWorkspace.didActivateApplicationNotification` before its initial exact
  `frontmostApplication.bundleIdentifier` readback. Status-item reopen and
  system-app MRU activation establish the observer before their trigger.
  Resolution cancels the notification token; replacement, cancellation,
  stale generation, duplicate delivery, and watchdog termination retain
  explicit ownership. A watchdog expiry records one final readback for
  diagnostics and remains a terminal failure. The status-item path also
  performs an independent exact foreground readback after its visible and
  runtime-log evidence.
- UI owner and Pressure: UI-target tests cover an initially satisfied
  readback, exact activation-notification delivery, an unmatched event,
  500 iterations of replacement/cancellation/stale/duplicate delivery, and a
  watchdog whose final readback becomes satisfied only after expiry. All 4/4
  passed in 0.990/0.992 seconds at
  `.build-local/evidence-driven-sync/SYNC-034B/owner-targeted-attempt-005`.
- UI affected paths: status-item reopen passed 1/1 in 15.235/15.238 seconds
  at `.build-local/evidence-driven-sync/SYNC-034B/status-item-attempt-001`.
  The real eight-application System MRU topology, FlowTab process relaunch,
  and ten switcher reopen iterations passed 1/1 in 43.948/43.951 seconds at
  `.build-local/evidence-driven-sync/SYNC-034B/system-mru-attempt-002`.
- Automation recovery evidence: after local Automation Mode authentication,
  the identity probe passed 1/1 in 1.570 seconds at
  `.build-local/evidence-driven-sync/SYNC-034B/automation-probe-attempt-001`.
  Earlier attempts retain the LocalAuthentication and Automation Mode timeout
  diagnostics that preceded authentication. The first System MRU attempt
  also records the custom-build-root fixture-path fallback; the passing run
  uses the wrapper's canonical default build root and a fresh evidence output
  root.
- Process/Tooling: the changed Swift files parse, the Xcode project plist is
  valid, `git diff --check` passes, the two new files are 277 and 149 lines,
  the removed raw helper has no remaining references, and UI compilation has
  no diagnostics for the new files. The fixed-path app exists at
  `{user-home}/Applications/Flow Tab UITest.app`; it and
  `/Applications/Flow Tab.app` have the same
  `io.github.potato-dumplings.flowtab` Apple Development designated
  requirement and Team identifier.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only UI-test observation infrastructure and its two existing UI call
  paths.
- Commit: `74afd40ea55345a74f70918606af9a44d1bb6a85`
  (`test(sync): migrate SYNC-034B frontmost activation`).

### SYNC-034C Closure Record

- Design and Oracle: the shared condition owner now distinguishes notification
  and scheduled readbacks. A System MRU order observer is installed before
  each explicit switcher trigger, performs an immediate accessibility-summary
  readback, then uses a main-RunLoop timer at
  `FlowTabUITestConditionObservationPolicy.xcuiReadbackCadence` only because
  the XCUI summary exposes no publication callback. Success requires the
  complete fixture identifiers in their exact expected order. Trigger
  delivery and panel presentation remain independently confirmed by the
  existing runtime-log acknowledgement.
- Lifecycle and timeout policy: the helper owns the timer token and cancels it
  on success, replacement, explicit cancellation, watchdog expiry, or
  deinitialization. The caller-supplied timeout is only a failure bound; one
  final readback records summary presence, raw application value, parsed
  order, generation, source, and waiter result.
- UI owner and Pressure: exact-order parsing, initial readback, notification
  delivery, scheduled readback, watchdog diagnostics, 500 stale/duplicate/
  cancellation iterations, and 100 real Timer lifecycle iterations passed
  7/7 in 3.647/3.649 seconds at
  `.build-local/evidence-driven-sync/SYNC-034C/owner-targeted-attempt-003`.
- UI affected path: the real eight-application System MRU topology, FlowTab
  process relaunch, explicit initial/relaunch triggers, and ten switcher
  reopen iterations passed 1/1 in 43.644/43.647 seconds at
  `.build-local/evidence-driven-sync/SYNC-034C/system-mru-attempt-003`.
  Attempt 001 exposed that two callers still passed identifier-set order
  where the new contract requires the exact expected order. Attempt 002
  retained `diagnosticsSummary=<missing>` after launch activation dismissed
  an otherwise resolved initial presentation. The final path installs its
  observer before an explicit acknowledged trigger and is independent of
  launch-presentation timing.
- Process/Tooling: changed Swift files parse, the Xcode project plist is
  valid, compiler diagnostics contain no warnings for the changed files, and
  `git diff --check` passes. The shared observer, observer tests, and System
  MRU files are 306, 245, and 225 lines. Final exact staged-content review is
  recorded before commit. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only UI-test conditional observation and its existing real-topology path.
- Commit: `9838f69b36a5fa812878ec6446126590f659e490`
  (`test(sync): migrate SYNC-034C app order observation`).

### SYNC-034D Closure Record

- Design and Oracle: the System MRU helper now owns both the FlowTab
  termination trigger and XCTest's exact `.notRunning` application-state
  wait. Already-terminated state is accepted by the initial state readback;
  later termination completes only from the process-state transition. The
  caller timeout is solely a terminal bound and failure reports the last
  `XCUIApplication.state`.
- UI affected path: the real eight-application System MRU topology reached
  `Not Running` before its FlowTab relaunch, then preserved the expected
  rebuilt order across ten switcher reopens. The test passed 1/1 in
  37.372/37.375 seconds at
  `.build-local/evidence-driven-sync/SYNC-034D/system-mru-attempt-001`.
- Process/Tooling: the changed Swift file parses, the Xcode project plist is
  valid, compiler diagnostics contain no warning for the changed file, the
  old deadline/RunLoop process loop has no remaining reference in
  `FlowTabUITests+SystemAppMRU`, and `git diff --check` passes. Final exact
  staged-content review is recorded before commit. Startup `prompts.zip`
  remains unchanged and outside the slice.
- Unit, Behavior, FlowTabCore, and Pressure: not relevant because this slice
  delegates one UI-test process-state wait to XCTest and creates no reusable
  state rule, observer, timer, or repeated asynchronous worker.
- Commit: `f53afd3aa00fa895dbcef98ede4d626a8cbc9c16`
  (`test(sync): migrate SYNC-034D process termination`).

### SYNC-034E Closure Record

- Design and Oracle: the shared condition owner now revalidates its generation
  and unresolved state after both client-controlled readback and condition
  evaluation. If either operation pumps the RunLoop and a nested callback
  resolves or replaces the generation, the superseded outer work is
  discarded before evidence publication or expectation fulfillment. The
  status-item caller checks the FlowTab application and SystemUIServer
  immediately, then accepts only existence of the exact status or quit
  accessibility element.
- Lifecycle and timeout policy: the status-element helper owns the shared
  scheduled-readback owner and always cancels it on return. Because XCUI
  exposes no element-publication callback, it uses the named
  `xcuiReadbackCadence`. The quit-element observer is established before the
  control-click trigger; an already-present initial readback skips the
  trigger. `elementWatchdog` is solely a failure bound and reports the latest
  per-source existence evidence.
- Deterministic owner and Pressure: the explicit reentrant-readback
  regression, initial/event/scheduled/watchdog paths, exact-order rule, 500
  stale/duplicate/cancellation iterations, and 100 real Timer lifecycle
  iterations passed 8/8 in 2.178/2.180 seconds at
  `.build-local/evidence-driven-sync/SYNC-034E/owner-regression-attempt-001`.
- UI affected paths: status-item restoration from Finder and secondary-click
  quit-menu publication passed 2/2 in 32.748/32.750 seconds, with 33.354
  seconds total test operation time, at
  `.build-local/evidence-driven-sync/SYNC-034E/affected-ui-attempt-002`.
  Attempt 001 retained the original
  `multiple calls made to XCTestExpectation fulfill` API violation from a
  reentrant status-item readback; the quit path independently passed in that
  run.
- Process/Tooling: the canonical UI app installation succeeded, changed Swift
  files parse, the Xcode project plist is valid, compiler diagnostics contain
  no warning for the changed files, the status-item raw deadline/RunLoop loops
  have no remaining reference, and `git diff --check` passes. The condition
  owner, owner tests, and status-item helper are 318, 281, and 140 lines.
  The touched oversized Home/Logs file decreases by one line. Final exact
  staged-content review is recorded before commit. Startup `prompts.zip`
  remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  UI-test observation infrastructure and its existing UI call paths.
- Commit: `442de518dff8a35b4fa9318b39d365ee53a34cbf`
  (`test(sync): migrate SYNC-034E status observation`).

### SYNC-034F Closure Record

- Design and Oracle: the test creates each marker owner before its settings or
  quit trigger, installs the scheduled readback, and requires the inverse
  initial marker baseline. Success comes only from the exact expected Boolean
  becoming visible in the shared defaults suite. The active-registration log
  and XCTest `.notRunning` state remain independent evidence that production
  reached the ordered activation and restoration boundaries.
- Lifecycle and timeout policy: cross-process defaults expose no reliable
  publication callback, so each helper invocation uses the named 100ms marker
  readback cadence. It owns and cancels the Timer on initial resolution,
  scheduled resolution, watchdog failure, or return. The named five-second
  watchdog is solely a terminal failure bound and reports expected, baseline,
  last value, source, and generation. I/O delay can extend observation time
  without changing the Boolean success Oracle. The existing test scope owns
  its termination fallback and cleanup launch.
- UI affected path: real Command+Tab configuration, system-shortcut takeover,
  switcher trigger, process termination, marker restoration, and cleanup
  passed 1/1 in 40.435/40.438 seconds, with 41.835 seconds total test
  operation time, at
  `.build-local/evidence-driven-sync/SYNC-034F/settings-command-tab-attempt-002`.
  Attempt 001 demonstrated the nominal single-readback path; subsequent
  semantic review identified that ordered runtime logs do not guarantee
  cross-process defaults visibility under I/O delay, leading to the
  preinstalled conditional owner in the passing closure run.
- Pressure: the unchanged shared owner retains the SYNC-034E coverage of 500
  stale/duplicate/cancellation iterations and 100 real Timer lifecycle
  iterations. This affected path additionally exercised two real marker
  owners across activation and restoration.
- Process/Tooling: both changed Swift files parse, the new source is present
  exactly once in the UI target, the Xcode project plist is valid, compiler
  diagnostics contain no warning for the changed files, the marker helper's
  raw deadline/RunLoop loop has no remaining reference, and
  `git diff --check` passes. The new owner/test file is 205 lines; extraction
  reduces Settings from 876 to 775 lines. Final exact staged-content review is
  recorded before commit. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Unit, Behavior, and FlowTabCore: not relevant because production marker
  ordering and restoration logic are unchanged; this slice migrates their UI
  observation contract.
- Commit: `483ff018f5f877b62ab82e5ec05adc23aabe0b57`
  (`test(sync): migrate SYNC-034F takeover marker`).

### SYNC-034G Closure Record

- Design and Oracle: `tapFirstHittable` and `hasHittableElement` now create a
  generation-owned query observer. It installs its scheduled readback before
  the immediate check and resolves only with the first indexed XCUI candidate
  whose `exists` and `isHittable` readbacks are both true. Tapping occurs only
  after that exact evidence resolves; Boolean probing uses the same evidence
  without an action.
- Lifecycle and timeout policy: XCUI exposes no candidate-publication callback,
  so the owner reuses the named 100ms XCUI readback cadence. Each helper
  invocation owns and cancels its Timer on initial resolution, scheduled
  resolution, watchdog failure, or return. The caller-supplied timeout remains
  only a terminal failure bound. A failure reports candidate count, observed
  existing indices, first hittable index, source, generation, and waiter result.
  Scheduling delay can extend observation without changing the exact
  candidate Oracle.
- Deterministic regression: initial exact-candidate resolution, a preinstalled
  scheduled readback, automatic cancellation, and watchdog final evidence
  passed 2/2 in 0.988/0.989 seconds, with 0.991 seconds total test operation
  time, at
  `.build-local/evidence-driven-sync/SYNC-034G/owner-regression-attempt-003`.
  Attempt 001 recorded the sandbox-denied SwiftPM temporary-file evidence and
  was rerun outside the sandbox through the canonical wrapper. Attempt 002
  exposed and led to the explicit escaping-closure capture fix before the
  passing run.
- UI and Pressure: a fresh real Home/Logs/Settings sidebar path passed 1/1 in
  9.935/9.936 seconds, with 9.938 seconds total test operation time, at
  `.build-local/evidence-driven-sync/SYNC-034G/affected-ui-attempt-001`.
  Five consecutive repetitions then passed 5/5 in 45.338/45.340 seconds, with
  45.342 seconds total test operation time, at
  `.build-local/evidence-driven-sync/SYNC-034G/sidebar-pressure-attempt-002`.
  This covers 15 real query-owner lifecycles and complements the unchanged
  shared owner's 100 Timer and 500 generation pressure iterations.
- Pressure diagnostic evidence: the first ten-iteration attempt completed
  iterations 1–8 before the XCUI application-assistance connection was lost
  during iteration 9; iteration 10 inherited a UI-testing authorization
  failure. No FlowTab or runner crash report was generated. The exited runner
  left xcodebuild waiting on result finalization, so the resolved single
  xcodebuild process was terminated after more than one minute and the
  partial result bundle and wrapper status were retained at
  `.build-local/evidence-driven-sync/SYNC-034G/sidebar-pressure-attempt-001`.
  The fresh 1/1 and final 5/5 runs establish that the failure was scoped to the
  exhausted XCUI session.
- Process/Tooling: the changed Swift files parse and compile, the new source is
  present exactly once in the UI target, the Xcode project plist is valid,
  passing logs contain no changed-file compiler warning, the two migrated raw
  deadline/RunLoop loops have no remaining definition, and `git diff --check`
  passes. The extracted owner file is 147 lines, Support decreases from 1,203
  to 1,174 lines, and the regression file remains within the 400-line
  guardrail at 395 lines. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  UI-test interaction infrastructure and its existing UI paths.
- Commit: `65bbc1e7ad96c9a5f45e4bcccf2c4b2eb57b99cc`
  (`test(sync): migrate SYNC-034G hittable queries`).

### SYNC-034H Closure Record

- Design and Oracle: `assertValue` and `assertValuePrefix` now create a
  generation-owned value observer. It installs its scheduled readback before
  the immediate check and resolves only when the exact XCUI element exists and
  its string value satisfies the exact or prefix predicate. An already
  satisfied value resolves from the initial readback. A later persistent value
  remains observable even when its publication preceded helper entry.
- Lifecycle and timeout policy: XCUI exposes no value-publication callback, so
  the owner reuses the named 100ms XCUI readback cadence. Each helper invocation
  owns the Timer and cancels it on initial resolution, scheduled resolution,
  watchdog failure, or return. The caller-supplied timeout remains solely a
  terminal failure bound. Its final readback reports identifier, existence,
  expected predicate, actual value, source, generation, and waiter result.
  Scheduling and accessibility-publication delay can extend observation without
  changing the value predicate Oracle.
- Deterministic regression: initial exact-value evidence, preinstalled
  scheduled prefix readback, automatic cancellation, and watchdog final
  evidence passed 2/2 in 0.629/0.629 seconds, with 0.630 seconds total test
  operation time, at
  `.build-local/evidence-driven-sync/SYNC-034H/owner-regression-attempt-001`.
- UI affected paths: the real Settings hotkey popup exercised an exact-value
  transition and the window-behavior path exercised prefix publication before
  and after a process relaunch. Both passed 2/2 in 25.189/25.190 seconds, with
  individual durations of 10.106 and 15.083 seconds and 26.852 seconds total
  test operation time, at
  `.build-local/evidence-driven-sync/SYNC-034H/affected-ui-attempt-001`.
- Pressure: 50 iterations of each deterministic path exercised 100 owner
  lifecycles, including immediate resolution, scheduled resolution,
  cancellation, and watchdog final evidence. All 100/100 passed in
  19.857/19.872 seconds, with 20.680 seconds total test operation time, at
  `.build-local/evidence-driven-sync/SYNC-034H/owner-pressure-attempt-001`.
  This complements the unchanged shared owner's 100 Timer and 500 generation
  pressure coverage.
- Process/Tooling: the changed Swift files parse and compile, the two new
  sources are present exactly once in the UI target, the Xcode project plist is
  valid, and all three canonical-wrapper status files report completed with
  zero final/build/signing/test exit codes and present result bundles. Compiler
  logs contain no warnings for changed files; the initial build reports three
  existing warnings in unrelated Space-fixture workflow files. Scoped search
  confirms the two raw value deadline/RunLoop loops have no remaining
  definition, and `git diff --check` passes. The new owner and regression files
  are 128 and 114 lines; extraction reduces Support from 1,174 to 1,152 lines.
  Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes only
  UI-test assertion infrastructure and its existing Settings UI paths.
- Commit: `1c1656ba686b56f831c3265ac6fc1b0d0478d703`
  (`test(sync): migrate SYNC-034H element values`).

### SYNC-034I Closure Record

- Discovery evidence: a Home projection diagnostic prototype exercised a
  multi-row XCUI readback that exceeded the shared 100ms cadence. The exact
  window activation path passed in 28.402 seconds, then the multi-app title
  path accumulated nested accessibility reads and failed while resolving an
  unrelated AppKit `ThemeWidgetControlViewService.xpc` bundle identifier.
  The 2-test run retained 1 pass and 1 unexpected failure in
  67.997/68.000 seconds at
  `.build-local/evidence-driven-sync/SYNC-034I/affected-ui-attempt-001`.
  This established same-generation Timer reentrancy as a shared scheduler
  defect before the Home contract proceeds in its own slice.
- Design and Oracle: the scheduler now maintains one pending one-shot Timer and
  rearms it only after the scheduled readback callback returns. The condition
  owner suppresses a scheduled callback while the same generation is already
  reading. Notification callbacks remain eligible during an active readback,
  preserving the synchronous-publication race closure proven by the existing
  reentrant-notification regression. Initial, notification, scheduled, and
  watchdog readbacks retain their existing condition predicates; elapsed time
  never resolves a condition.
- Lifecycle and retained cadence: the serial schedule owns exactly one current
  cancellation token. Success, replacement, explicit cancellation, watchdog
  termination, and deinitialization invalidate that token and prevent rearm.
  The named 100ms XCUI cadence remains a conditional-observation policy that
  selects the next readback opportunity after prior work completes. Scheduler
  pressure can therefore extend completion time without introducing concurrent
  XCUI reads or changing the result.
- Deterministic scheduler and Timer Pressure: scheduled reentrancy suppression,
  single pending one-shot behavior, cancellation after rearm, notification
  reentrancy compatibility, named scheduled resolution, and 100 real Timer
  lifecycles passed 5/5 in 2.264/2.265 seconds, with 2.944 seconds total test
  operation time, at
  `.build-local/evidence-driven-sync/SYNC-034I/scheduler-regression-attempt-002`.
- Generation Pressure: 500 replacement/cancellation/stale/duplicate callback
  iterations passed 1/1 in 0.443/0.443 seconds, with 1.214 seconds total test
  operation time, at
  `.build-local/evidence-driven-sync/SYNC-034I/generation-pressure-attempt-001`.
- UI affected paths: real Settings prefix-value publication, Home/Logs/Settings
  sidebar hittable queries, and status-item quit-menu publication and process
  exit passed 3/3 in 56.083/56.085 seconds. Individual durations were 20.157,
  9.338, and 26.588 seconds; total test operation time was 56.783 seconds at
  `.build-local/evidence-driven-sync/SYNC-034I/affected-ui-attempt-002`.
- Process/Tooling: all changed Swift files parse and compile, both new sources
  are present exactly once in the UI target, the Xcode project plist is valid,
  passing wrapper status files report completed with zero final/build/signing/
  test exit codes and present result bundles, and `git diff --check` passes.
  Compiler logs contain no changed-file warning; three existing warnings remain
  in unrelated Space-fixture workflow files. The condition owner, extracted
  scheduler, and scheduler regressions are 316, 99, and 106 lines. Scoped
  review confirms the scheduler uses a nonrepeating Timer and rearms only after
  callback return. Startup `prompts.zip` remains unchanged and outside the
  slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes only
  shared UI-test observation scheduling and its existing UI callers.
- Commit: `c6be9c9c9aee8708481976cd6a450b8ef1abbee9`
  (`test(sync): migrate SYNC-034I serial readbacks`).

### SYNC-034J Closure Record

- Design and Oracle: one generation-owned owner now reads the persistent Home
  projection for a row containing a title, a visible title, or the absence of
  every prohibited title. Success comes only from an exact static-text
  existence readback or the case-insensitive label/value projection of rows
  whose identifiers begin with `flowtab.home.window.`. An exact static title
  short-circuits row enumeration; row evidence remains available for custom
  accessibility layouts and exact CG-window identifiers.
- Lifecycle and retained cadence: the helper starts the owner before its
  immediate readback and owns cancellation through success, watchdog expiry,
  and scope exit. Home projection is persistent and XCUI exposes no reliable
  publication callback, so the shared serial 100ms cadence remains the named
  conditional-observation policy. The watchdog is solely a failure bound and
  reports its unmet expectation, generation, source, exact static titles,
  row identifiers/labels/values, and waiter result.
- Deterministic regression and Pressure: initial matching-row evidence,
  delayed static-title evidence, initial and delayed prohibited-title absence,
  cancellation, stale callbacks, and 100 owner lifecycles passed 2/2 in
  0.473/0.474 seconds, with 1.937 seconds total test operation time, at
  `.build-local/evidence-driven-sync/SYNC-034J/owner-regression-attempt-002`.
  Attempt 001 was stopped before compilation by the restricted sandbox's
  SwiftPM temporary-file denial; the canonical wrapper then passed outside
  that boundary.
- UI affected paths: after installing the current stable signed app through
  the canonical script, the real multi-app fixture paths for activating the
  exact selected Home window and showing only the selected application's
  resolved titles passed 2/2 in 63.485/63.490 seconds. Individual durations
  were 30.127 and 33.358 seconds; total test operation time was 64.170 seconds
  at
  `.build-local/evidence-driven-sync/SYNC-034J/affected-ui-attempt-001`.
  This reproduces the slow multi-row workload that exposed SYNC-034I without
  the nested XCUI/AppKit failure.
- Process/Tooling: both passing wrapper status files report `completed`, zero
  final/build/signing/test exit codes, and present result bundles. The changed
  Swift files parse and compile, both new sources occur exactly once in the UI
  target, the project plist is valid, and `git diff --check` passes. Compiler
  logs contain no changed-file warning; three existing warnings remain in
  unrelated Space-fixture workflow files. The owner and regression files are
  307 and 178 lines, while extraction reduces the already oversized shared
  Support file by 101 lines. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes only
  shared UI-test Home projection infrastructure and its existing UI paths.
- Commit: `ed81aabb1c3e38159797d11f6922cce1cbe0ce6e`
  (`test(sync): migrate SYNC-034J Home projection`).

### SYNC-034K Closure Record

- Design and Oracle: one generation-owned owner observes the exact Switcher
  window-card total and title multiset, preserving duplicate-title
  multiplicity. Each readback binds the card query to one indexed element
  projection and derives both total and complete title counts from that
  projection. Success requires exact equality with the expected card count and
  title counts; elapsed time cannot satisfy or reject a candidate projection.
- Lifecycle and retained cadence: the helper installs the serial scheduled
  observer before its immediate readback and owns cancellation through success,
  watchdog expiry, and scope exit. XCUI exposes no card-projection callback, so
  the named 100ms cadence remains a conditional-observation policy. The caller
  watchdog is solely a failure bound and reports expected and observed counts,
  generation, source, and waiter result.
- Deterministic regression and Pressure: duplicate-title rejection and later
  exact resolution, initial and delayed success, cancellation, stale callbacks,
  and 100 owner lifecycles passed 2/2 in 0.596/0.597 seconds, with 1.374 seconds
  total test operation time, at
  `.build-local/evidence-driven-sync/SYNC-034K/owner-regression-attempt-002`.
- UI affected paths: after installing the current stable signed app through
  the canonical script, the physical Control+Tab window-preview path and the
  real fixture path that closes one window while the Switcher remains open
  passed 2/2 in 34.761/34.765 seconds. Individual durations were 7.484 and
  27.277 seconds; total test operation time was 35.436 seconds at
  `.build-local/evidence-driven-sync/SYNC-034K/affected-ui-attempt-002`.
  The latter retained two-card evidence until the independent fixture close
  and runtime reconciliation published the exact one-card title projection.
- Process/Tooling: both final wrapper status files report `completed`, zero
  final/fixture/build/signing/test exit codes, and present result bundles.
  Both new Swift files parse and compile, both sources occur exactly once in
  the UI target, the project plist is valid, and `git diff --check` passes.
  Final compiler logs contain no warnings. The owner and regression files are
  153 and 139 lines, and extraction reduces the already oversized shared
  Support file by another 42 lines. Startup `prompts.zip` remains unchanged
  and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes only
  shared UI-test Switcher title-projection infrastructure and its existing UI
  paths.
- Commit: `f5d9dbb8369635a7742187bd347213d8b0991d51`
  (`test(sync): migrate SYNC-034K Switcher titles`).

### SYNC-034L Closure Record

- Design and Oracle: one generation-owned owner binds the Switcher card query
  once per readback, deduplicates exact accessibility identifiers, and preserves
  each card's title, value, frame, and preview-image state. Success requires the
  exact title multiset and card count, one unique identifier per card, absence
  of every excluded title, and a card identifier set disjoint from the
  preceding application's anchors. Elapsed time cannot promote a candidate
  projection.
- Lifecycle and retained cadence: the helper installs the serial scheduled
  observer and records its initial baseline before sending Down Arrow.
  Resolution stays gated while XCUI dispatches that trigger and is enabled only
  after the trigger returns, so even a matching baseline cannot prove the
  operation succeeded. The helper owns cancellation through success, watchdog
  expiry, and scope exit. XCUI exposes no card-projection callback, so the
  shared named 100ms cadence remains a conditional-observation policy. The
  caller watchdog is solely a failure bound and reports the acceptance gate,
  expected contract, exact last card projection, generation, source, and
  waiter result.
- Deterministic regression and Pressure: matching-baseline rejection before
  trigger completion, stale identifier rejection, excluded title rejection,
  reused-identifier rejection, duplicate-title multiplicity, initial and
  scheduled resolution, cancellation, stale callbacks, watchdog final readback,
  and 100 owner lifecycles passed 2/2 in 0.475/0.476 seconds. Total test
  operation time was 1.667 seconds at
  `.build-local/evidence-driven-sync/SYNC-034L/owner-regression-attempt-003`.
- UI affected path: after installing the current stable Apple
  Development-signed app through the canonical script, the real three-fixture
  multi-app card-isolation workflow passed 1/1 in 65.572/65.575 seconds, with
  66.212 seconds total test operation time, at
  `.build-local/evidence-driven-sync/SYNC-034L/affected-ui-attempt-002`.
  For every fixture, the owner recorded its baseline before Down Arrow and then
  accepted only the explicitly selected app's exact two-card projection with
  unique identifiers disjoint from the preceding fixture's anchors.
- Process/Tooling: both wrapper status files report `completed`, zero final,
  fixture, signing, build, and test exit codes where applicable, and present
  result bundles. All changed Swift files parse and compile, both new sources
  occur exactly once in the UI target, the project plist is valid, and
  `git diff --check` passes. The changed files emit no compiler warnings; the
  build retained two warnings in unchanged future migration paths. The owner
  and regression files are 227 and 286 lines, and extraction reduces the
  already oversized shared Support file by 44 lines to 965. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes only
  shared UI-test Switcher card-identity infrastructure and its existing UI
  path.
- Commit: `b5e3302588a9d56ad12b49edae0c90029ae1cd10`
  (`test(sync): migrate SYNC-034L Switcher cards`).

### SYNC-034M Closure Record

- Design and Oracle: one generation-owned owner reads the Logs tab content,
  lines container, and complete seeded-row accessibility projection. Success
  requires both containers, the exact visible identifier multiset and row
  total, and absence of every prohibited identifier. Elapsed time cannot make
  a partial, duplicated, or prohibited projection succeed. The obsolete,
  uncalled marker loop has been removed.
- Lifecycle and retained cadence: the helper starts the observer and performs
  its immediate baseline readback before tapping Logs. Resolution remains
  gated until that trigger returns, so an already matching baseline cannot
  prove the operation completed. The helper owns cancellation through success,
  watchdog expiry, and scope exit. XCUI exposes no accessibility-projection
  callback, so the shared serial named 100ms cadence remains a conditional
  observation policy. The caller watchdog is solely a failure bound and reports
  the acceptance gate, expected contract, exact last identifier counts,
  generation, source, and waiter result.
- Deterministic regression and Pressure: matching-baseline rejection before
  trigger completion, partial/prohibited/duplicate projection rejection,
  initial and scheduled resolution, cancellation, stale callbacks, watchdog
  final readback, and 100 owner lifecycles passed 2/2 in 0.686/0.690 seconds.
  Total test operation time was 6.730 seconds at
  `.build-local/evidence-driven-sync/SYNC-034M/owner-regression-attempt-001`.
- UI affected path: after installing the current stable Apple
  Development-signed app through the canonical script, the DEBUG, INFO, WARN,
  and ERROR launch scenarios passed 1/1 in 24.372/24.374 seconds, with 25.848
  seconds total test operation time, at
  `.build-local/evidence-driven-sync/SYNC-034M/affected-ui-attempt-001`. Each
  scenario accepted only its exact seeded-row identifier multiset after the
  Logs-tab trigger.
- Process/Tooling: both wrapper status files report `completed`, zero final,
  signing, build, and test exit codes, and present result bundles. Both new
  Swift sources parse and compile, occur exactly once in the UI target, the
  project plist is valid, and `git diff --check` passes. The changed files emit
  no compiler warnings; the build retains three warnings in unchanged future
  migration paths. The owner and regression files are 235 and 205 lines, and
  extraction reduces the already oversized shared Support file by 83 lines to
  882. Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes only
  shared UI-test Logs projection infrastructure and its existing UI path.
- Commit: `e2a81f52783595306a552b890401bb4633b45cc2`
  (`test(sync): migrate SYNC-034M Logs projection`).

### SYNC-034N Closure Record

- Design and Oracle: `FlowTabUITestRuntimeLogObservationBaseline` resolves the
  Application Support log path at the UI-runner resource boundary and captures
  byte count, file number or creation date, and vnode event generation before
  the trigger. Every wait registers its file-event callback before an immediate
  post-baseline readback. Rotation, replacement, and truncation restart the
  affected file at byte zero. Success requires the post-baseline bytes to
  satisfy the complete marker set or compiled regular expression; elapsed time
  cannot establish publication. The uncalled required-plus-alternative helper
  has been removed, and callers that intentionally inspect the whole persisted
  store now use an explicit all-content readback.
- Lifecycle and retained cadence: the baseline owns directory and file vnode
  sources and closes their descriptors on cancellation or deinitialization.
  Each wait owns one event registration and one shared serial scheduled
  readback registration through success, watchdog expiry, or scope exit.
  Cross-process writes, log creation, and rotation expose no complete reliable
  callback contract, so the named 200ms cadence remains a fallback for
  coalesced or unavailable vnode delivery. The caller watchdog is solely a
  failure bound and reports the missing contract, baseline/current event
  generations, character count, and a bounded content tail.
- Deterministic regression and Pressure: a real temporary log-file append
  resolved from vnode notification evidence; truncation and atomic replacement
  reset the byte offset; marker and regex contracts resolved only from matching
  content; and initial/scheduled resolution, cancellation, stale callbacks,
  watchdog final diagnostics, and 100 owner lifecycles passed 4/4 in
  1.671/1.673 seconds. Total test operation time was 12.669 seconds at
  `.build-local/evidence-driven-sync/SYNC-034N/owner-regression-attempt-004`.
- UI affected paths: the real stale-occlusion mock-runtime path exercised
  cross-process all-marker publication and passed 1/1 in 4.334/4.337 seconds,
  with 5.031 seconds total operation time, at
  `.build-local/evidence-driven-sync/SYNC-034N/affected-ui-attempt-001`.
  The focused mock-runtime trigger path exercised exact-notification-name regex
  publication and passed 1/1 in 2.714/2.716 seconds, with 4.058 seconds total
  operation time, at
  `.build-local/evidence-driven-sync/SYNC-034N/affected-regex-ui-attempt-003`.
- Superseded diagnostics: the broader Settings caller attempt stopped at its
  existing non-hittable `z` dropdown option before reaching either regex wait;
  its result bundle remains at
  `.build-local/evidence-driven-sync/SYNC-034N/affected-regex-ui-attempt-001`
  for the remaining scrolling/hittability owner. The first focused regex
  attempt successfully observed the target log and then failed an additional
  switcher-card projection assertion outside this contract; the final focused
  path validates only the runtime-log publication contract.
- Process/Tooling: the three final wrapper status files report `completed`,
  zero final, signing, build, and test exit codes, and present result bundles.
  Nine changed Swift files parse and compile, each of the three new sources
  occurs exactly once in the UI target, the project plist is valid, and
  `git diff --check` passes. No legacy dictionary snapshot type or empty
  snapshot all-content read remains. The earlier full build retains two
  warnings in unchanged future migration paths; the touched
  `SpaceFixtureInAppWindowSwitcher` warning is closed. The extracted source,
  owner, and regression files are 382, 278, and 393 lines, and extraction
  reduces the already oversized shared Support file by 125 lines to 757. Its
  remaining 200ms `commitEditing` settle belongs to the later direct-UI owner.
  Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes only
  shared UI-test cross-process runtime-log observation infrastructure and its
  existing UI callers.
- Commit: `336c50afdee2345ff79c94e552b23332e392881e`
  (`test(sync): migrate SYNC-034N runtime log observation`).

### SYNC-034O Closure Record

- Design and Oracle: semantic call-site review found
  `waitForFocusedWorkflowWindow` only at its declaration. Removing the helper
  closes its ownerless deadline and raw 100ms RunLoop cadence. The executable
  synchronization surface now omits this unused contract.
- Lifecycle and retained time policy: the removed helper owned no external
  observer or resource. This path now allocates no timer, retry, watchdog, or
  polling cadence.
- Validation: Process/Tooling passed. Semantic `rg` reports zero remaining
  `waitForFocusedWorkflowWindow` references; `swiftc -parse` passes for the
  722-line shared support file; `plutil -lint` passes for the Xcode project;
  and `git diff --check` passes. The canonical UI wrapper's
  `process-attempt-001` completed signing and full-target build-for-testing
  with zero exit codes and `** TEST BUILD SUCCEEDED **`. Its selected
  one-test execution was blocked before any test body by XCTest automation
  mode initialization (`Timed out while enabling automation mode`, exit 65);
  the result bundle is present. The build reports the two unchanged warnings
  for unused `searchInput` and mutable `runtimeLogSnapshot`.
- Unit, Behavior, UI, FlowTabCore, and Pressure: not relevant because this
  slice removes unreachable UI-test support without changing an executable
  test or application runtime path.
- Commit: `57681626afe97e17005bb1ad598af8a6c746a742`
  (`test(sync): remove SYNC-034O obsolete focus wait`).

### SYNC-034P Closure Record

- Design and Oracle: semantic call-site review found
  `assertStaticTextsAbsent` only at its declaration. Removing the helper closes
  its ownerless deadline and raw 100ms RunLoop cadence. Executable element
  absence contracts continue to use `waitForNonExistence`, whose success
  Oracle is the exact XCTest `exists == false` predicate.
- Lifecycle and retained time policy: the removed helper owned no external
  observer or resource. This path now allocates no timer, retry, watchdog, or
  polling cadence.
- Validation: Process/Tooling passed. Semantic `rg` reports zero remaining
  `assertStaticTextsAbsent` references; `swiftc -parse` passes for the
  705-line shared support file; `plutil -lint` passes for the Xcode project;
  and `git diff --check` passes. The canonical UI wrapper's
  `process-attempt-001` completed with all fixture-skipped signing,
  build-for-testing, logging, and test-without-building exit codes zero; the
  result bundle is present. Full-target compilation passed and its selected
  deterministic test passed 1/1 in 0.674 seconds (0.676 seconds suite time).
  The build reports the two unchanged warnings for unused `searchInput` and
  mutable `runtimeLogSnapshot`.
- Unit, Behavior, UI, FlowTabCore, and Pressure: not relevant because this
  slice removes unreachable UI-test support without changing an executable
  test or application runtime path.
- Commit: `44d7063f062bca398ed3ddde59d0d25be905d42f`
  (`test(sync): remove SYNC-034P obsolete absence wait`).

### SYNC-034Q Closure Record

- Design and Oracle: all five Home and Switcher-search activation paths start
  `FlowTabUITestWorkflowWindowActivationObservationOwner` before their click
  or confirmation trigger. Registration precedes the initial readback, and
  an acceptance gate remains closed through the trigger. Workspace activation
  and target-process AX focused/main-window notifications cause immediate
  readback. Success requires the exact target bundle identifier and the
  expected topmost on-screen `CGWindowID`; CG, AX, and XCUI titles remain
  diagnostic evidence.
- Lifecycle and retained cadence: the helper invocation owns its generation,
  workspace observer token, AX observer, run-loop source, notification
  registrations, serial scheduled readback, expectation, and watchdog. Every
  completion, expiry, cancellation, and scope exit removes those resources.
  `FlowTabUITestConditionObservationPolicy.xcuiReadbackCadence` remains the
  named 100ms fallback because CGWindowList exposes no exact-window
  publication callback and AX events can be unavailable or coalesced. The
  caller timeout is solely a terminal failure bound; expiry reports the
  acceptance gate and last exact bundle, window number, frame, and title
  readback.
- UI-test infrastructure regression and Pressure:
  `owner-regression-attempt-002` passed 5/5 tests in 1.114 seconds
  (1.116 seconds suite time). Coverage includes observer-before-readback
  ordering, a matching initial state held behind the trigger gate, wrong
  bundle and wrong window rejection, exact event resolution after five
  delayed scheduled readbacks, 100 cancel/restart/duplicate-event iterations,
  and watchdog final-readback diagnostics. All canonical wrapper exit codes
  are zero and the result bundle is present.
- Affected UI: the freshly installed signed UI-test app and rebuilt real
  three-application fixture topology passed
  `testHomePageClickingRealWorkflowWindowActivatesExactFixtureWindow` 1/1 in
  29.383 seconds (29.386 seconds suite time), with 30.109 seconds total test
  operation time, at `affected-ui-attempt-001`. Fixture generation, logging,
  signing, build-for-testing, and test-without-building exit codes are zero;
  the result bundle is present.
- Process/Tooling: all eight changed Swift files parse; the Xcode project plist
  is valid; the removed helper has zero references; all five executable
  callers use `triggerAndWaitForFrontmostWorkflowWindow`; the implementation
  and test files are 362 and 293 lines; and `git diff --check` passes. The
  final owner and affected UI builds have no warnings. The initial full owner
  build retained the existing unused `searchInput` and mutable
  `runtimeLogSnapshot` warnings.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only the UI-test Oracle and its real fixture activation callers.
- Commit: `01c3786f6fe9d15b859fc79645d3bf599aeaa1d2`
  (`test(sync): migrate SYNC-034Q exact window activation`).

### SYNC-034R Closure Record

- Design and Oracle: `tapFirstHittableAfterScrolling` performs an immediate
  readback of the exact option query and its owning scroll container. A
  generation-owned schedule then reads the same candidate projection before
  each optional scroll action. Target/container geometry determines one named
  directional scroll delta, and the next readback is registered only after
  XCUI returns from that action. Success requires the exact first candidate
  whose `exists` and `isHittable` evidence is true.
- Lifecycle and retained time policy: the helper invocation owns the
  condition owner, one pending one-shot timer, scroll-step evidence,
  cancellation, and cleanup. XCUI exposes no option-frame or hittability
  publication callback, so
  `FlowTabUITestConditionObservationPolicy.xcuiReadbackCadence` remains the
  named 100ms conditional-readback cadence. The Settings option-selection
  owner supplies a named 30-second watchdog solely as a terminal failure
  bound. Expiry reports the readback source, candidate indices, first target
  frame, container frame, step count, and last scroll delta.
- UI-test infrastructure regression and Pressure: the final split source
  passed 5/5 tests in 1.256 seconds under
  `.build-local/evidence-driven-sync/SYNC-034R/owner-regression-attempt-009`.
  Coverage includes an already-hittable initial state with no scroll,
  deterministic below/above/unknown geometry, exact resolution after five
  delayed readbacks, 100 cancel/restart/stale-callback iterations, and
  watchdog final-readback diagnostics.
- Affected UI: the canonical wrapper passed
  `testSettingsHotkeySelectionsPersistAcrossRelaunch` 1/1 in 93.136 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-034R/affected-ui-attempt-008`.
  The real Settings topology observed `x`, `y`, and `z` become hittable after
  serial scroll events, clicked only after that evidence, and verified all
  five hotkey values after relaunch. The subsequent source split moved the
  unchanged owner declarations into their single-responsibility file; the
  final UI target and owner suite compile that split successfully. Attempts
  009–012 then terminated at `app.launch()` because macOS left the signed
  fixed-path app in `Running Background`; all four failures occurred before
  the Settings page or scrolling helper, and their independent result bundles
  are retained with the passing affected-path evidence.
- Process/Tooling: the final UI-test target builds through the canonical
  wrapper. The Xcode project plist is valid, `git diff --check` passes, and
  the observation, support, and regression files are 266, 198, and 265 lines.
  The migrated helper contains no raw fixed RunLoop advance. The remaining
  raw loop in `tapElementAfterScrollingIntoView` is a separate owner and
  remains assigned to the next SYNC-034 slice. Startup `prompts.zip` remains
  unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test interaction Oracle and its representative real Settings
  path.
- Commit: `16382e8be956c752351807640891e015ef2bc37f`
  (`test(sync): migrate SYNC-034R dropdown scrolling`).

### SYNC-034S Closure Record

- Design and Oracle: `tapElementAfterScrollingIntoView` performs an immediate
  readback of the exact app row, its preferred scroll container, and available
  fallbacks. Existing preferred-container ownership has precedence; otherwise
  the smallest usable fallback whose horizontal frame contains the target
  midpoint is selected. Success requires the exact target to exist, be
  hittable, and be fully contained by that container. When no container
  exists, exact target existence and hittability are sufficient. Geometry
  determines one serial scroll action after an unresolved readback; the next
  readback is registered only after XCUI returns from that action. The caller
  verifies the resulting selected-app Home title projection independently.
- Lifecycle and retained time policy: the helper invocation owns the
  condition generation, one pending one-shot timer, scroll-step evidence,
  cancellation, and cleanup. XCUI exposes no frame or hittability publication
  callback, so
  `FlowTabUITestConditionObservationPolicy.xcuiReadbackCadence` remains the
  named 100ms conditional-readback cadence. The caller timeout is solely a
  terminal failure bound. Expiry performs a final exact readback and reports
  target existence, hittability, frame, selected container source and frame,
  full-visibility evidence, scroll count, and last delta.
- UI-test infrastructure regression and Pressure: the canonical wrapper
  passed 13/13 tests in 2.886 seconds under
  `.build-local/evidence-driven-sync/SYNC-034S/owner-regression-attempt-001`.
  Coverage joins the shared serial scheduler and SYNC-034R regressions with
  six focused cases: initially satisfied evidence, preferred and smallest
  fallback selection, deterministic above/below/invalid/occluded geometry,
  five delayed readbacks followed by exact resolution, 100
  cancel/restart/stale-callback iterations, and watchdog final-readback
  diagnostics.
- Affected UI: the canonical wrapper rebuilt the real fixture variants and UI
  target under
  `.build-local/evidence-driven-sync/SYNC-034S/affected-ui-attempt-001`.
  Fixture generation, signing, and build-for-testing all returned zero and
  produced a result bundle. UI execution stopped at
  `FlowTabUITests+SpaceFixtureApp.swift:120` because the first
  `com.example.fixture.finder` fixture remained `Running Background` for the
  full foreground-activation watchdog. The test did not reach the Home page
  or this helper; the wrapper recorded
  `stage=xcodebuild_test-without-building_failed` and exit 65. This persistent
  macOS GUI-activation environment blocker leaves the Goal active for a later
  environment-confirmed affected-path run.
- Process/Tooling: the UI-test target builds through the canonical wrapper.
  The Xcode project plist is valid, `git diff --check` passes, and the shared
  scheduler is reused by both scrolling owners. The migrated helper contains
  no raw fixed RunLoop advance. New and touched focused sources remain below
  the 400-line guardrail. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test interaction Oracle and its representative multi-app Home
  path.
- Commit: `ac3bb50fd4dec7265ae355edb90ccdaa8c328778`
  (`test(sync): migrate SYNC-034S scrolling visibility`).

### SYNC-034T Closure Record

- Design and Oracle: `selectHomeWorkflowApp` establishes a Home-selection
  generation and reads the exact expected title projection before touching the
  app row. A matching initial projection closes an already-selected state.
  Otherwise the owner gates projection acceptance while the SYNC-034S owner
  derives exact row/container visibility and performs one tap. After the tap
  returns, an immediate `.triggerReadback` closes synchronous SwiftUI
  publication races. A persistent expected-title match is the sole selection
  success Oracle; the caller's unique-title precondition and subsequent full
  title/absence assertions remain independent isolation evidence.
- Lifecycle and retained time policy: the selection invocation owns the
  deferred readback registration, projection generation, row-action owner,
  cancellation, and cleanup. XCUI exposes no window-projection publication
  callback. The shared named 100ms serial readback cadence is activated only
  after an unresolved trigger-return readback and is cancelled on resolution,
  watchdog expiry, restart, or owner teardown. A monotonic watchdog budget
  bounds the complete row-action and projection contract; expiry performs a
  final exact readback and reports phase, acceptance gate, expected title,
  visible static titles, and exact row identifiers/labels/values.
- UI-test infrastructure regression and Pressure: six focused tests cover an
  already matching initial projection, trigger-return publication, delayed
  scheduled publication, duplicate delivery, 100 cancel/restart/stale
  generations, watchdog final-readback diagnostics, and deterministic
  monotonic-budget exhaustion. Two existing Home projection tests and three
  shared condition-owner tests were selected as regressions. The canonical
  wrapper compiled the entire UI-test target without warnings in
  `.build-local/evidence-driven-sync/SYNC-034T/owner-regression-attempt-001`
  and `owner-regression-attempt-002`.
- Validation closure: after UI automation authentication became available, the
  six focused owner tests passed 6/6 with zero failures in 1.324 seconds under
  `.build-local/evidence-driven-sync/SYNC-034T/validation-owner-current-001`.
  This includes the 100-generation cancellation workload and monotonic
  watchdog-budget coverage. The real three-application Home isolation path
  passed 1/1 in 31.495 seconds under
  `.build-local/evidence-driven-sync/SYNC-034T/validation-affected-ui-current-001`.
  Finder's matching initial projection resolved directly; Chrome and Notes
  each performed one row action, then exposed only their exact expected
  window-title set. Both canonical wrapper status files report
  `stage=completed`, zero final/signing/test exit codes, and present result
  bundles.
- Process/Tooling: the Xcode project plist is valid, the migrated selection
  helper contains no raw `Date` deadline or fixed RunLoop advance, and new and
  touched focused sources remain below the 400-line guardrail. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only UI-test selection orchestration and its test-owned observation
  infrastructure.
- Commit: `4ea39c42683aea779b3866a6a0f1d38a3d758075`
  (`test(sync): migrate SYNC-034T Home app selection`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034T Home app selection`).

### SYNC-034U Closure Record

- Design and Oracle: semantic call-site review found no invocation of
  `waitForTopmostWorkflowCGWindow`. Removing the helper closes an ownerless
  frontmost-bundle, CG-window, XCTest-title, and AX-title polling loop without
  changing any executable UI path.
- Lifecycle and retained time policy: the removed helper owned neither an
  observer nor a cancellable schedule. Its wall-clock deadline and raw 100ms
  RunLoop cadence are removed. No duration remains for this contract.
- Validation: zero repository references remain for the symbol. The canonical
  UI wrapper's build-for-testing action compiles the complete UI-test target
  under
  `.build-local/evidence-driven-sync/SYNC-034U/process-build-attempt-001`.
  The build retains the pre-existing unused mutable `runtimeLogSnapshot`
  warning in `FlowTabUITests+SpaceFixtureRuntimeTruthEntrypoints.swift`;
  Unit, Behavior, UI execution, Pressure, and FlowTabCore are not relevant
  because no caller or executable behavior exists. The Xcode project plist is
  unchanged, `git diff --check` passes, and the touched pre-existing support
  file is reduced by 38 lines. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Commit: `d3d8d55a04eb5a856a574c9d144ffdef1a9b936a`
  (`test(sync): remove SYNC-034U obsolete topmost wait`).

### SYNC-034V Closure Record

- Design and Oracle: `waitForExactFrontmostWorkflowCGWindow` now reuses the
  SYNC-034Q generation-owned activation observer. Workspace activation and
  target-process AX focused/main-window event sources are registered before
  the immediate initial readback. Success requires the exact expected
  frontmost bundle identifier and exact topmost on-screen CGWindowID. This
  persistent condition accepts an already-satisfied initial state, while all
  fourteen executable call sites retain their independent title, frame,
  topology, and workflow assertions.
- Lifecycle and retained time policy: each helper invocation owns the
  workspace token, AX observer registrations, one pending serial readback,
  generation rejection, cancellation, and cleanup. CGWindow/Space publication
  exposes no complete notification, so the shared named 100ms serial cadence
  remains a conditional readback opportunity. The caller timeout is solely a
  terminal failure bound; expiry performs a final readback and reports source,
  generation, frontmost bundle, topmost CG window number/title/frame, active
  title, and expected-title observability.
- UI-test infrastructure regression and Pressure: the shared owner has six
  focused tests covering observer registration before readback, exact initial
  state, post-baseline event resolution, exact bundle/window identity, five
  delayed observations, 100 cancel/restart/stale-generation iterations, and
  watchdog final-readback diagnostics. The canonical wrapper compiled these
  tests and the complete UI-test target successfully under
  `.build-local/evidence-driven-sync/SYNC-034V/process-build-attempt-001`.
- Validation closure: the shared window-evidence batch passed 18/18 with zero
  failures in 3.316 seconds under
  `.build-local/evidence-driven-sync/SYNC-034V-W-Y-Z/owner-current-001`,
  including all six exact-activation tests and their 100-generation
  cancellation workload. The clean Option+Tab cross-Space path passed 1/1 in
  39.206 seconds under
  `.build-local/evidence-driven-sync/SYNC-034V-W-Y-Z/clean-optiontab-ui-current-001`;
  both normal and fullscreen confirmations resolved only after the exact
  bundle and selected CGWindowID became frontmost. The noisy four-window path
  independently passed four exact confirmations under
  `.build-local/evidence-driven-sync/SYNC-035C/affected-ui-current-attempt-001`.
  All wrapper status files report `stage=completed`, zero final/signing/test
  exit codes, and present result bundles.
- Process/Tooling: the canonical build status reports `stage=completed`,
  final exit zero, signing exit zero, and build-for-testing exit zero. The
  build retains the existing unused `searchInput` warning in
  `FlowTabUITests+SpaceFixtureEdgeInputsWorkflow.swift`. The migrated helper
  contains no raw deadline or fixed RunLoop advance; the four remaining raw
  loops in the same support file are independently owned later slices.
  Observation implementation and regression files are 374 and 331 lines;
  the pre-existing support file is 708 lines with one workflow-window
  observation responsibility. The Xcode project is unchanged,
  `git diff --check` passes, and startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only a UI-test exact-window Oracle and reuses test-owned observation
  infrastructure.
- Commit: `6e1441a8ae82106ab56b2dc107fce9a462b7b0aa`
  (`test(sync): migrate SYNC-034V exact CG window wait`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034V exact CG window wait`).

### SYNC-034W Closure Record

- Design and Oracle: the three executable
  `waitForFrontmostWorkflowSpaceCGWindow` call sites now create a narrow
  generation-owned owner before their first readback. Success requires the
  exact target bundle to be frontmost and its topmost on-screen CG window to
  match the expected fixture title or the established titleless
  fullscreen-Space frame contract. The owner returns that exact
  `WorkflowCGWindowObservation`, preserving downstream window identity,
  topology, and fixture assertions in the Option+Tab, Control+Tab, and window
  search workflows.
- Lifecycle and retained time policy: each helper invocation owns the
  workspace activation token, target-process AX focused/main-window
  registrations, one pending serial readback, generation rejection,
  cancellation, and cleanup. CGWindow/Space publication exposes no complete
  notification, so the shared named 100ms cadence remains a conditional
  readback opportunity. The caller timeout is solely a terminal failure
  bound; expiry performs a final readback and reports generation, source,
  expected bundle/title, frontmost bundle, and the final topmost window
  number/title/frame.
- UI-test infrastructure regression and Pressure: six focused tests cover
  registration before an already-satisfied initial readback, rejection of a
  mismatched frontmost bundle and window title, titleless fullscreen-frame
  evidence, resolution after five delayed scheduled readbacks, 100
  cancel/restart/stale-generation iterations, duplicate notification
  delivery, and watchdog final-readback diagnostics. Both the owner and tests
  compile in the complete UI-test target.
- Validation closure: the shared owner batch passed all 18 focused tests in
  3.316 seconds under
  `.build-local/evidence-driven-sync/SYNC-034V-W-Y-Z/owner-current-001`.
  SYNC-034W's cases cover initially satisfied evidence, exact-frontmost
  rejection, title and titleless-fullscreen matching, five delayed readbacks,
  event delivery, cancellation, duplicate and stale generations, 100
  lifecycle iterations, and watchdog final-readback diagnostics. The clean
  real-topology Option+Tab test passed 1/1 in 39.206 seconds under
  `.build-local/evidence-driven-sync/SYNC-034V-W-Y-Z/clean-optiontab-ui-current-001`.
  It captured the exact frontmost fixture bundle and normal/titleless
  fullscreen sibling before FlowTab launch, then returned to the exact
  fullscreen window after confirmation. Both wrapper status files report
  `stage=completed`, zero final/signing/test exit codes, and present result
  bundles.
- Process/Tooling: the final canonical build at
  `.build-local/evidence-driven-sync/SYNC-034W/process-build-attempt-002`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. The build retains the existing unused `runtimeLogSnapshot` and
  `searchInput` warnings in independently owned future migration paths. The
  project plist is valid, all new project references are unique,
  `git diff --check` passes, and the focused owner and regression files are
  131 and 275 lines. Extraction reduces the pre-existing workflow-window
  support file from 708 to 699 lines. Its three remaining raw loops are
  independently owned later slices. Startup `prompts.zip` remains unchanged
  and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test fullscreen-Space Oracle and test-owned observation
  infrastructure.
- Commit: `3eeaea6bea7288a46ea2eea8f6b25e354a5207d6`
  (`test(sync): migrate SYNC-034W frontmost Space window`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034W frontmost Space window`).

### SYNC-034X Closure Record

- Design and Oracle: semantic call-site review found no invocation of the
  `waitForActiveSpaceWorkflowCGWindow` overload whose first label is
  `windowNumber`. All three executable calls use the separately owned
  title/fullscreen-Space overload. Removing the unused overload closes its
  ownerless topmost-window loop without changing an executable UI path.
- Lifecycle and retained time policy: the removed overload owned no observer
  or cancellable schedule. Its wall-clock deadline and raw 100ms RunLoop
  cadence are removed, leaving no duration for this contract.
- Validation: the overload pattern has zero Swift references. The canonical
  UI wrapper's build-for-testing action compiles and signs the complete
  UI-test target under
  `.build-local/evidence-driven-sync/SYNC-034X/process-build-attempt-001`,
  with zero final, signing, and build exit codes. The build retains the
  existing unused `runtimeLogSnapshot` warning in a future migration path.
  Swift parsing and `git diff --check` pass, and the pre-existing
  workflow-window support file is reduced from 699 to 667 lines. Unit,
  Behavior, UI execution, Pressure, and FlowTabCore are not relevant because
  the removed overload has no caller. Startup `prompts.zip` remains unchanged
  and outside the slice.
- Commit: `911cfa9216aaba12962002526849977d22a3eab1`
  (`test(sync): remove SYNC-034X obsolete active-Space wait`).

### SYNC-034Y Closure Record

- Design and Oracle: all three executable
  `waitForActiveSpaceWorkflowCGWindow(title:app:timeout:)` calls now reuse the
  workflow-Space window owner with an explicit `activeSpace` scope. They run
  after the FlowTab panel is ready, so the scope accepts FlowTab as the
  system-frontmost application and succeeds only when the target fixture's
  topmost on-screen CG window matches the expected title or established
  titleless fullscreen-frame contract. The `frontmost` scope continues to
  require the exact target bundle for SYNC-034W.
- Lifecycle and retained time policy: each helper invocation installs
  workspace activation and target-process AX focused/main-window event sources
  before the immediate readback. It owns one pending serial readback,
  generation rejection, cancellation, and cleanup. CGWindow/Space publication
  exposes no complete notification, so the shared named 100ms cadence remains
  a conditional readback opportunity. The caller timeout is solely a terminal
  failure bound; expiry performs a final readback and reports scope, expected
  title, generation, source, frontmost bundle, and final topmost window
  number/title/frame.
- UI-test infrastructure regression and Pressure: the shared owner now has
  seven focused tests. A new initial-state case proves that `activeSpace`
  accepts exact target-window evidence while FlowTab is frontmost. Existing
  coverage preserves frontmost-bundle rejection, title and titleless
  fullscreen matching, observer-before-readback ordering, five delayed
  readbacks, duplicate delivery, 100 cancel/restart/stale-generation
  iterations, and watchdog final-readback diagnostics. The complete UI-test
  target compiles all seven tests.
- Validation closure: the shared owner batch passed all 18 focused tests in
  3.316 seconds under
  `.build-local/evidence-driven-sync/SYNC-034V-W-Y-Z/owner-current-001`.
  Its seven workflow-Space cases include the `activeSpace` initially
  satisfied path while FlowTab is system-frontmost, title and
  titleless-fullscreen matching, exact-scope separation, five delayed
  readbacks, event delivery, cancellation, duplicate and stale generations,
  100 lifecycle iterations, and watchdog final-readback diagnostics. The
  clean real-topology Option+Tab test passed 1/1 in 39.206 seconds under
  `.build-local/evidence-driven-sync/SYNC-034V-W-Y-Z/clean-optiontab-ui-current-001`.
  After the FlowTab panel became ready, the observer resolved the expected
  target-process fullscreen Space window while preserving FlowTab's
  system-frontmost role. Both wrapper status files report `stage=completed`,
  zero final/signing/test exit codes, and present result bundles.
- Process/Tooling: the canonical build at
  `.build-local/evidence-driven-sync/SYNC-034Y/process-build-attempt-001`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. The build retains the existing unused `runtimeLogSnapshot` and
  `searchInput` warnings in independent future slices. Swift parsing and
  `git diff --check` pass. The shared owner and regression files are 188 and
  310 lines, and this extraction reduces the pre-existing workflow-window
  support file from 667 to 658 lines. Its sole remaining raw loop is the
  separately owned multi-window Space-containing contract. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test active-Space Oracle and test-owned observation
  infrastructure.
- Commit: `8ef8de839b7bb59f9c2509caa3fed372449948f6`
  (`test(sync): migrate SYNC-034Y active-Space window`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034Y active-Space window`).

### SYNC-034Z Closure Record

- Design and Oracle: all five executable
  `waitForWorkflowSpaceContainingCGWindow(title:app:timeout:)` calls now use a
  generation-owned full-window-collection observer. These paths deliberately
  launch noisy sibling windows, so success comes from any target-process
  on-screen CG window whose title matches the expected value or whose
  titleless frame satisfies the established fullscreen-Space geometry
  contract. Window ordering remains diagnostic evidence and a matching
  non-topmost sibling resolves immediately.
- Lifecycle and retained time policy: each helper invocation installs
  workspace activation and target-process AX focused/main-window event sources
  before the immediate full-collection readback. It owns one pending serial
  readback, generation rejection, cancellation, and cleanup. CGWindow/Space
  publication exposes no complete notification, so the shared named 100ms
  cadence remains a conditional readback opportunity. The caller timeout is
  solely a terminal failure bound; expiry performs a final readback and reports
  every observed on-screen window number, title, and frame.
- UI-test infrastructure regression and Pressure: five focused tests cover an
  initially matching non-topmost sibling with observer-before-readback
  ordering, five delayed readbacks followed by event resolution, titleless
  fullscreen matching, 100 cancel/restart/stale-generation iterations with
  duplicate events, and watchdog final-collection readback. The complete
  UI-test target compiles all five tests and the five affected noisy
  Option+Tab, Control+Tab, and window-search call paths.
- Validation closure: the shared owner batch passed all 18 focused tests in
  3.316 seconds under
  `.build-local/evidence-driven-sync/SYNC-034V-W-Y-Z/owner-current-001`.
  Its five collection cases cover an initially matching non-topmost sibling,
  observer-before-readback ordering, five delayed readbacks, event delivery,
  titleless fullscreen geometry, cancellation, duplicate and stale
  generations, 100 lifecycle iterations, and watchdog final-collection
  diagnostics. The affected noisy four-window Option+Tab path passed 1/1 in
  58.704 seconds under
  `.build-local/evidence-driven-sync/SYNC-035C/affected-ui-current-attempt-001`.
  The same real topology passed its pressure repetition 1/1 in 59.254 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-035C/runtime-topology-pressure-current-attempt-001`,
  with 83 matching identity checks and zero transient mismatches. These runs
  resolve the normal, non-topmost, and titleless fullscreen fixture members
  from the complete target-process window collection before and after panel
  presentation. Both wrapper status files report `stage=completed`, zero
  final/signing/test exit codes, and present result bundles.
- Process/Tooling: the canonical build at
  `.build-local/evidence-driven-sync/SYNC-034Z/process-build-attempt-004`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. The first full compile records the existing unused
  `runtimeLogSnapshot` and `searchInput` warnings for independent future
  slices; the final incremental compile adds no diagnostic. A restricted
  attempt 003 stopped during SwiftPM manifest loading when sandboxed temporary
  file creation was denied, before source compilation. Swift parsing, project
  plist validation, and `git diff --check` pass. The project references are
  unique. The focused owner and regression files are 111 and 226 lines, and
  the workflow-window support file is reduced from 658 to 651 lines with no
  raw time-driven loop remaining. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test noisy-window collection Oracle and test-owned observation
  infrastructure.
- Commit: `9f9fec8f7c1e39327a473c0dd195da9d4bc00ad4`
  (`test(sync): migrate SYNC-034Z Space window collection`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034Z Space window collection`).

### SYNC-034AA Closure Record

- Design and Oracle: both screenshot-luminance assertions in
  `testSettingsAppearanceThemeAndLanguageUpdateVisibleUI` now establish a
  generation-owned observer before selecting light or dark appearance.
  Evidence remains gated while the action runs. The action-return readback or
  a later scheduled readback succeeds only when the exact Settings content
  element exists and its sampled average sRGB luminance satisfies the named
  light or relative-dark threshold. The existing exact theme-control value
  assertions remain independent Oracles.
- Lifecycle and retained time policy: each helper invocation owns its
  observation generation, one serial scheduled readback, trigger gate,
  cancellation, and cleanup. It performs an immediate `.triggerReadback`.
  Screenshot pixels expose no publication callback, so the shared named 100ms
  XCUI cadence remains a conditional observation opportunity. The five-second
  caller watchdog is solely a terminal failure bound and reports acceptance
  state, element identifier/existence, expected threshold, final luminance,
  generation, source, and waiter result.
- UI-test infrastructure regression and Pressure: five focused tests cover a
  matching initial readback, rejection before trigger completion followed by
  immediate trigger readback, five delayed scheduled readbacks, 100
  cancel/restart/stale-generation iterations with duplicate delivery, and
  watchdog final-readback diagnostics. The complete UI-test target compiles all
  five tests and the affected real Settings path.
- Validation closure: the shared owner batch passed all 43 tests in 10.724
  seconds under
  `.build-local/evidence-driven-sync/SYNC-034AA-AK/owner-current-001`.
  SYNC-034AA's five cases cover matching initial evidence, trigger gating,
  five delayed readbacks, cancellation, duplicate and stale generations, 100
  lifecycle iterations, and watchdog final-luminance diagnostics. The real
  Settings appearance and language path passed 1/1 in 23.223 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AA/affected-ui-current-001`.
  Its exact theme-control values, sampled light/dark luminance thresholds, and
  persistent translated labels all matched. Both wrapper status files report
  `stage=completed`, zero final/signing/test exit codes, and present result
  bundles.
- Process/Tooling: the canonical build at
  `.build-local/evidence-driven-sync/SYNC-034AA/process-build-attempt-001`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. Its only source warnings are the existing unused `searchInput`
  and mutable `runtimeLogSnapshot` candidates in independent future slices.
  Swift parsing, project plist validation, unique project-reference checks,
  zero raw `Date`/RunLoop loops in `FlowTabUITests+Settings`, and
  `git diff --check` pass. The owner and regression files are 196 and 213
  lines; the Settings file remains within its existing single-responsibility
  guardrail at 786 lines. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test screenshot appearance Oracle and test-owned observation
  infrastructure.
- Commit: `dfbb8bba780b32e5abd308c514b79c900aeb980a`
  (`test(sync): migrate SYNC-034AA Settings luminance`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AA Settings luminance`).

### SYNC-034AB Closure Record

- Design and Oracle: the first physical Control+Tab test now establishes one
  generation-owned diagnostics observer before sending the key action. Each
  readback captures the full accessibility value once and succeeds only when
  `selectedWindow=mock-current-secondary` and `previewImages=2` coexist in that
  exact projection. The selected preview element, exact card-title multiset,
  card geometry, image presence, and runtime-log sequence remain independent
  Oracles.
- Shared parsing boundary: `switcherPanelDiagnosticsValue` moves from the
  oversized Space multi-app workflow file into the shared diagnostics
  observation file while preserving its existing raw and percent-decoded value
  semantics for all 53 existing call sites. The source workflow file is
  reduced by 12 lines.
- Lifecycle and retained time policy: the helper installs its schedule before
  the trigger and gates matching evidence until the key action returns. It then
  performs an immediate `.triggerReadback`. XCUI accessibility-value
  publication exposes no callback, so the shared named 100ms serial cadence
  remains a conditional readback opportunity. The helper owns generation,
  schedule, cancellation, and cleanup. Its five-second watchdog is solely a
  terminal failure bound and reports acceptance state, expected and observed
  key/value pairs, raw projection, generation, source, and waiter result.
- UI-test infrastructure regression and Pressure: five focused tests cover
  shared raw and percent-decoded parsing, matching initial projection,
  pre-trigger rejection followed by immediate trigger readback, five delayed
  partial projections, 100 cancel/restart/stale-generation iterations with
  duplicate delivery, and watchdog final-projection diagnostics. The complete
  UI-test target compiles all five tests and the affected physical Control+Tab
  path.
- Validation closure: the shared owner batch passed all 43 tests in 10.724
  seconds under
  `.build-local/evidence-driven-sync/SYNC-034AA-AK/owner-current-001`.
  The eight diagnostics-owner cases cover raw and percent-decoded parsing,
  matching initial evidence, trigger gating, exact and prefix strategies,
  five delayed partial projections, atomic committed-index projection,
  cancellation, duplicate and stale generations, 100 lifecycle iterations,
  and watchdog final-projection diagnostics. The physical Control+Tab path
  passed 1/1 in 6.398 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AB/affected-ui-current-001`.
  Its atomic `selectedWindow=mock-current-secondary` and `previewImages=2`
  projection matched the exact two-card set, selected preview image, geometry,
  and runtime-log sequence. Both wrapper status files report
  `stage=completed`, zero final/signing/test exit codes, and present result
  bundles.
- Process/Tooling: the canonical build at
  `.build-local/evidence-driven-sync/SYNC-034AB/process-build-attempt-001`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. Its only source warnings are the existing unused `searchInput`
  and mutable `runtimeLogSnapshot` candidates in independent future slices.
  Swift parsing, project plist validation, unique project-reference checks,
  single shared parser-definition checks, and `git diff --check` pass. The
  owner and regression files are 229 and 257 lines. The interaction-regression
  file is 323 lines and now retains only the separately owned window-layer
  transition/grace contract. The oversized Space workflow file is reduced to
  1,255 lines. Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test diagnostics projection Oracle and test-owned observation
  infrastructure.
- Commit: `4925d6e27aa5a57b3df2a91318ad8ca6f9ad4375`
  (`test(sync): migrate SYNC-034AB switcher diagnostics`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AB switcher diagnostics`).

### SYNC-034AC Closure Record

- Design and Oracle: all four calls to the local
  `waitForSwitcherDiagnosticsValue` helper now reuse the shared diagnostics
  owner. Initial Option+Tab selection resolves from exact
  `selected=com.flowtab.mock.browser` readback. App, window-card, and Search
  pointer paths install the owner before both hover operations and succeed only
  after those operations return with the exact final persistent selection.
  Search preserves percent-decoded
  `searchSelectedResult=app:com.flowtab.mock.browser` semantics.
- Lifecycle and retained time policy: initial readiness performs an immediate
  readback. Triggered paths gate success during pointer actions and perform an
  immediate `.triggerReadback` afterward. XCUI accessibility-value publication
  exposes no callback, so the shared named 100ms serial cadence remains a
  conditional readback opportunity. Each invocation owns generation, schedule,
  cancellation, and cleanup. Its three-second watchdog is solely a terminal
  failure bound with the full expected/observed/raw projection diagnostic.
- UI-test infrastructure regression and Pressure: the shared suite now has six
  focused tests, including an owner-level percent-decoded projection case in
  addition to initial, trigger, five delayed partial readbacks, 100
  cancel/restart/stale-generation iterations with duplicate delivery, and
  watchdog diagnostics. The complete UI-test target compiles all six tests and
  the initial Option+Tab, app-hover, Control+Tab window-hover, and Search-hover
  paths.
- Validation closure: the six owner tests passed 6/6 with zero failures in
  1.735 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AC/owner-current-001`. They cover
  matching initial evidence, percent-decoded Search identity, post-trigger
  gating, five delayed partial readbacks, 100 cancel/restart generations with
  stale and duplicate delivery, and watchdog final-projection diagnostics.
  The current-source Option, Control, and Search moving-pointer paths passed
  3/3 with exact final selection evidence under
  `.build-local/evidence-driven-sync/SYNC-034AN/ui-contracts-final-003`.
  Their case durations were 7.559, 7.510, and 8.301 seconds. The same installed
  application also passed ten repeated Option presentations and ten repeated
  Control presentations under the SYNC-034AN real-process Pressure runs.
  Scheduling pressure changed observation latency while preserving every
  exact selected application, window, and Search-result identity.
- Process/Tooling: the canonical build at
  `.build-local/evidence-driven-sync/SYNC-034AC/process-build-attempt-001`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. Its sole source warning is the existing mutable
  `runtimeLogSnapshot` candidate in an independent future slice. Swift parsing,
  zero local `waitForSwitcherDiagnosticsValue` references, and
  `git diff --check` pass. Shared owner and regression files are 306 and 291
  lines. The pre-existing oversized `SwitcherAndSearch` file remains at its
  baseline 839 lines while the local loop owner is removed. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only UI-test pointer-selection Oracles and test-owned observation
  infrastructure.
- Commit: `0b350c14f5e12d5532ead279e58540a4e2f906b1`
  (`test(sync): migrate SYNC-034AC pointer diagnostics`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AC pointer diagnostics`).

### SYNC-034AD Closure Record

- Design and Oracle: both local RuntimeTruthEntrypoints diagnostics helpers now
  reuse the shared owner. `assertSwitcherSelectedApp` resolves only from the
  exact persistent fixture bundle identifier. Window-state entry establishes
  its observer before `advanceDown` and accepts only a post-command `mode`
  value with the `windowCycle` prefix. Existing app-strip membership,
  selected-app cards, clean/noisy preview-title sets, workflow-Space windows,
  and runtime-log traces remain independent Oracles.
- Shared predicate model: diagnostics expectations now carry an explicit
  equatable `equals` or `hasPrefix` match strategy. Percent decoding remains an
  orthogonal readback normalization. This keeps success semantics
  deterministic and makes the exact predicate visible in watchdog output.
- Lifecycle and retained time policy: persistent selected state checks
  immediately. The mode owner installs before command and tracing work, gates
  success until they return, then performs `.triggerReadback`. XCUI
  accessibility-value publication exposes no callback, so the shared named
  100ms serial cadence remains a conditional readback opportunity. Each
  invocation owns generation, schedule, cancellation, and cleanup. Its
  four-second watchdog is solely a terminal failure bound with expected match
  strategy and final raw projection.
- UI-test infrastructure regression and Pressure: the shared suite now has
  seven focused tests. A new owner-level prefix case joins exact,
  percent-decoded, initial, trigger, five delayed partial readbacks, 100
  cancel/restart/stale-generation iterations with duplicate delivery, and
  watchdog diagnostics. The complete UI-test target compiles all seven tests
  and the affected clean/noisy real-fixture Option+Tab paths.
- Validation closure: the prefix-specific owner test passed 1/1 in 0.446
  seconds under
  `.build-local/evidence-driven-sync/SYNC-034AD/owner-current-001`. Together
  with the six shared-owner cases closed by SYNC-034AC, all seven exact,
  percent-decoded, prefix, initial, trigger-gated, delayed, cancellation,
  100-generation Pressure, and watchdog contracts passed on the same current
  UI-test build. The canonical fixture-enabled run rebuilt the current fixture
  variants and passed both real-topology paths 2/2 with zero failures in
  95.994 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AD/affected-ui-current-001`.
  The clean case completed in 37.827 seconds. The noisy case completed in
  58.167 seconds while preserving exact selection and activation across two
  normal windows, two fullscreen siblings, and additional CG sibling
  projections. Both paths required the exact fixture bundle, post-command
  `windowCycle` prefix, preview-title set, CG window identity, runtime-log
  delivery, and final fixture readback.
- Process/Tooling: the canonical build at
  `.build-local/evidence-driven-sync/SYNC-034AD/process-build-attempt-001`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. Its source warnings are the existing unused `searchInput` and
  mutable `runtimeLogSnapshot` candidates in independent future slices. Swift
  parsing, zero local `waitForSwitcherDiagnosticsValue` references, and
  `git diff --check` pass. Shared owner and regression files are 392 and 322
  lines. The oversized RuntimeTruthEntrypoints file is reduced from 899 to 880
  lines. Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only UI-test real-fixture diagnostics Oracles and test-owned observation
  infrastructure.
- Commit: `c3d1b4e8c155b7fcc35837c54321b4e9b04417e9`
  (`test(sync): migrate SYNC-034AD runtime diagnostics`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AD runtime diagnostics`).

### SYNC-034AE Closure Record

- Design and Oracle:
  `assertWindowSearchUsesCommittedGenerationIndex` now reuses the shared
  diagnostics owner. It reads readiness, result state, degradation,
  current-generation coverage, and freshness-barrier state from one exact
  accessibility-value snapshot. Success requires all five committed-index
  values in that same projection, including an initially satisfied readback.
- Lifecycle and retained time policy: the assertion invocation owns the
  observation generation, named serial schedule, cancellation, and cleanup.
  XCUI diagnostics publication exposes no callback, so the shared 100ms
  cadence remains only a conditional readback opportunity. The four-second
  watchdog is solely a terminal failure bound and reports the complete final
  projection plus every unmet expectation.
- UI-test infrastructure regression and Pressure: the shared suite now has
  eight focused tests. The new atomic Search-index case supplies two
  interleaved partial projections whose individual matching fields never form
  success, followed by one complete snapshot that resolves. Existing tests
  cover initial and delayed evidence, trigger gating, cancellation, duplicate
  and stale generation rejection across 100 iterations, and watchdog final
  evidence. The complete UI-test target compiles the full suite and all
  affected clean/noisy window-search paths.
- Validation closure: the atomic committed-index owner test passed 1/1 in
  0.465 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AE/owner-current-001`. Its two
  interleaved partial snapshots remained unresolved until one snapshot
  carried all five required fields. Together with the seven shared-owner
  cases closed by SYNC-034AC and SYNC-034AD, all eight initial, trigger-gated,
  delayed, cancellation, 100-generation Pressure, exact/prefix/decoded,
  atomic, and watchdog contracts passed on the current build. The canonical
  fixture-enabled run passed both clean and noisy real-topology window-search
  paths 2/2 with zero failures in 113.697 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AE/affected-ui-current-001`.
  The clean case completed in 39.818 seconds and the noisy case in 73.879
  seconds. One initial Search input projection took about 11 seconds to become
  observable; both paths still required one complete committed-index
  snapshot, exact result identity, exact CG window identity, and final fixture
  selection readback for every normal and fullscreen activation.
- Process/Tooling: the canonical build at
  `.build-local/evidence-driven-sync/SYNC-034AE/process-build-attempt-002`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes and no compiler warning or error. Swift parsing and
  `git diff --check` pass. The regression file is 392 lines, and the oversized
  RuntimeTruthEntrypoints file is reduced from 880 to 878 lines. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test committed-index Oracle and its shared test-owned
  observation regression.
- Commit: `2fea9b3fedbae46f434bcba663692f7c6d02db90`
  (`test(sync): migrate SYNC-034AE Search index diagnostics`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AE Search index diagnostics`).

### SYNC-034AF Closure Record

- Design and Oracle: one Search-result projection owner now serves both
  previously independent loops. `waitForSearchWindowResult` accepts only an
  exact title/app pair from parsed diagnostics or the established
  case-insensitive XCUI fallback text. RuntimeTruth result-set readiness
  accepts only the target app's expected title set, with the exact workflow
  row count for clean topology and intentional extra-row allowance for noisy
  topology. Every condition is evaluated against one complete result snapshot.
- Ownership boundary: `SwitcherSearchWindowResultObservation` moved from the
  oversized multi-app workflow into the new owner file with its window-number
  and fallback matching semantics unchanged. Diagnostics parsing and the
  XCUI-element fallback remain at their existing resource boundary.
- Lifecycle and retained time policy: each wait installs the shared serial
  observation schedule before an immediate readback. The trigger may have
  returned before these persistent-result waits begin; the initial readback
  closes that publication race. XCUI and diagnostics value publication expose
  no callback, so the named 100ms cadence remains only a conditional readback
  opportunity. Each invocation owns generation, scheduling, cancellation, and
  cleanup. Its caller timeout is solely a terminal failure bound with the full
  final result projection.
- UI-test infrastructure regression and Pressure: six focused tests cover an
  exact initial result, fallback accessibility text, delayed completion of an
  exact app title/count projection, allowed noisy sibling rows, 100
  cancel/restart/stale-generation iterations with duplicate delivery, and
  watchdog final-projection diagnostics. The complete UI-test target compiles
  all six tests and the affected clean/noisy real-fixture Search paths.
- UI-test infrastructure validation and Pressure: the current-source owner run
  at
  `.build-local/evidence-driven-sync/SYNC-034AF/owner-current-001`
  passes all six tests in 1.920 seconds. It verifies exact initial evidence,
  fallback accessibility text, the complete clean title/count projection,
  noisy-sibling allowance, 100 cancel/restart/stale-generation iterations with
  duplicate delivery, and watchdog final-projection diagnostics.
- Affected UI: the current-source clean/noisy real-fixture Search run at
  `.build-local/evidence-driven-sync/SYNC-034AE/affected-ui-current-001`
  passes both paths in 113.697 seconds. Each path requires the complete Search
  result projection and final exact-window activation. The noisy path's longer
  Search publication interval changes completion time while preserving the
  result.
- Process/Tooling: the canonical build at
  `.build-local/evidence-driven-sync/SYNC-034AF/process-build-attempt-002`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes and no compiler warning or error. Swift parsing, project plist
  validation, unique project references, single observation-struct and
  wait-helper definitions, removal of the two local raw loops, and
  `git diff --check` pass. Owner and regression files are 297 and 296 lines.
  The oversized multi-app workflow shrinks from
  1,255 to 1,187 lines, and RuntimeTruthEntrypoints shrinks from 878 to 863
  lines. Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only UI-test Search result Oracles and test-owned observation
  infrastructure.
- Commit: `28c46cff506942c8d05fcdf437be1a455e0cb2f1`
  (`test(sync): migrate SYNC-034AF Search result projection`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AF Search result projection`).

### SYNC-034AG Closure Record

- Design and Oracle: `selectSearchResultForConfirmation` now reads the
  persistent `searchSelectedResult` through the shared diagnostics owner. The
  exact requested result ID is compared after percent decoding, preserving
  encoded accessibility publication while making the normalized stable result
  identity the sole success condition. The raw local helper and loop are
  removed.
- Lifecycle and retained time policy: the select-result delivery command
  returns before this persistent-state wait begins, and the owner checks
  immediately to close a synchronous or already-published result. Diagnostics
  publication exposes no callback, so the shared named 100ms serial cadence
  remains only a conditional readback opportunity. The invocation owns
  generation, scheduling, cancellation, and cleanup. Its two-second watchdog
  is solely a terminal failure bound with the expected normalized identity,
  encoded value, raw projection, source, and generation diagnostics.
- UI-test infrastructure regression and Pressure: the focused
  percent-decoded test now also verifies that owner diagnostics record the
  normalization policy. The eight-test shared suite retains initial and
  delayed evidence, trigger gating, atomic multi-key projection, prefix
  matching, 100 cancel/restart/stale-generation iterations with duplicate
  delivery, and watchdog final-projection coverage. The complete UI-test
  target compiles the suite and all affected clean/noisy Search paths.
- UI-test infrastructure validation and Pressure: the current-source focused
  owner run at
  `.build-local/evidence-driven-sync/SYNC-034AG/owner-current-001`
  passes the percent-decoded projection test in 0.456 seconds. The shared
  current-source owner run at
  `.build-local/evidence-driven-sync/SYNC-034AC/owner-current-001`
  passes all six selected lifecycle tests in 1.735 seconds, including initial
  and delayed evidence, 100 cancel/restart/stale-generation iterations with
  duplicate delivery, and watchdog diagnostics.
- Affected UI: the current-source clean/noisy real-fixture Search run at
  `.build-local/evidence-driven-sync/SYNC-034AE/affected-ui-current-001`
  passes both paths in 113.697 seconds. Both paths require the percent-decoded
  exact selected-result identity before confirmation and final exact-window
  activation.
- Process/Tooling: the final canonical build at
  `.build-local/evidence-driven-sync/SYNC-034AG/process-build-attempt-002`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. Its sole source warning is the existing mutable
  `runtimeLogSnapshot` candidate in an independent future slice. Swift parsing,
  zero `waitForSwitcherSearchSelectedResult` references, and
  `git diff --check` pass. The shared regression file is 391 lines. The
  oversized multi-app workflow shrinks from 1,187 to 1,167 lines, while
  RuntimeTruthEntrypoints remains at its 863-line baseline. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test Search selection Oracle and its existing test-owned
  observation regression.
- Commit: `0b75dba39115a011e2e55cfa47aa78c6a57ff20b`
  (`test(sync): migrate SYNC-034AG Search selection`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AG Search selection`).

### SYNC-034AH Closure Record

- Design and Oracle: both Switcher app-strip waits now use one
  generation-owned app-projection observer. Each `apps` accessibility value is
  parsed once into raw entry, exact bundle identity, and optional window count.
  Clean-topology callers accept only the exact `bundle:windowCount` entry;
  noisy-topology callers accept only the requested bundle identity. Success
  comes from that complete parsed readback.
- Lifecycle and retained time policy: the owner checks immediately so an
  already-published projection is accepted. Diagnostics publication exposes no
  callback, so the shared named 100ms serial cadence remains only a conditional
  readback opportunity. Each invocation owns its generation, scheduled work,
  cancellation, and cleanup. Caller watchdogs are terminal failure bounds and
  report the final raw value, parsed entries, source identifier, and element
  existence evidence.
- UI-test infrastructure regression and Pressure: six focused tests cover
  parser output, initially satisfied evidence with registration/cancellation
  ordering, delayed bundle publication after five missed observations, wrong
  exact count followed by the requested count, 100 cancel/restart/stale
  generation iterations with duplicate delivery, and watchdog diagnostics for
  final parsed entries. The suite passed 6/6 in 1.656 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AH/owner-current-001`.
- Affected UI: clean and noisy Option+Tab window-state workflows passed 2/2
  in 95.994 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AD/affected-ui-current-001`.
  The clean Control+Tab workflow passed 1/1 in 43.896 seconds under
  `.build-local/evidence-driven-sync/SYNC-035D/affected-clean-current-001`;
  the noisy Control+Tab workflow passed 1/1 in 99.477 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AO/affected-noisy-current-001`.
  Every path requires the requested clean exact-count or noisy exact-bundle
  app-strip projection before selecting exact workflow-window identities.
- Process/Tooling: the canonical build at
  `.build-local/evidence-driven-sync/SYNC-034AH/process-build-attempt-001`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. Its source warnings are the existing mutable
  `runtimeLogSnapshot` candidate and unused `searchInput` binding in independent
  future slices. Swift parsing, project plist validation, unique project
  references, single wait-helper definitions, removal of both local raw loops,
  and `git diff --check` pass. Owner and regression files are 224 and 216
  lines. The oversized multi-app workflow shrinks from 1,167 to 1,126 lines.
  Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only UI-test Switcher app-projection Oracles and test-owned observation
  infrastructure.
- Commit: `967d337c804fedaee359ce7173ffabc7f4c37be7`
  (`test(sync): migrate SYNC-034AH app projection`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AH app projection`).

### SYNC-034AI Closure Record

- Design and Oracle: both selected-window assertions now use one
  generation-owned diagnostics projection owner. A single complete
  accessibility-value readback supplies element existence, raw diagnostics,
  and `selectedWindowTitle`. Success requires that exact title to equal the
  requested title or belong to the caller's exact allowed-title set.
- Lifecycle and retained time policy: the owner installs its cancellable
  schedule before an immediate readback, so an already-published persistent
  selection is accepted. Diagnostics publication exposes no callback, so the
  shared named 100ms serial cadence remains only a conditional readback
  opportunity. Each assertion invocation owns generation, scheduling,
  cancellation, and cleanup. Its caller-supplied watchdog is a terminal
  failure bound that reports the expected contract, final selected title, raw
  projection, element existence, source, and generation.
- UI-test infrastructure regression and Pressure: five focused tests cover
  exact initially satisfied evidence with registration/cancellation ordering,
  an allowed title after five missed observations, rejection of a wrong exact
  title, 100 cancel/restart/stale-generation iterations with duplicate
  delivery, and watchdog final-title/raw-projection diagnostics. The suite
  passed 5/5 in 1.539 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AI/owner-current-001`.
- Affected UI: clean and noisy Option+Tab window-state workflows passed 2/2
  in 95.994 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AD/affected-ui-current-001`.
  The clean Control+Tab workflow passed 1/1 in 43.896 seconds under
  `.build-local/evidence-driven-sync/SYNC-035D/affected-clean-current-001`;
  the noisy Control+Tab workflow passed 1/1 in 99.477 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AO/affected-noisy-current-001`.
  These paths exercise both the exact-title contract and the noisy
  allowed-fullscreen-title-set contract before exact window selection.
- Process/Tooling: the fresh canonical build at
  `.build-local/evidence-driven-sync/SYNC-034AI/process-build-attempt-001`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. The changed owner, regression, and multi-app workflow files have
  no compiler diagnostic; unrelated existing warnings remain in independent
  files. Swift parsing, project plist validation, unique project references,
  single public assertion definitions, removal of both local title loops, and
  `git diff --check` pass. Owner and regression files are 207 and 194 lines.
  The oversized multi-app workflow shrinks from 1,126 to 1,059 lines. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only UI-test selected-window Oracles and test-owned observation
  infrastructure.
- Commit: `9f8755d9c25b027a4b3f82b0ced0fc2ec00f270e`
  (`test(sync): migrate SYNC-034AI selected window title`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AI selected window title`).

### SYNC-034AJ Closure Record

- Design and Oracle: all single-key `preview` waits now share one
  generation-owned projection owner. A complete accessibility-value readback
  supplies element existence, raw diagnostics, the raw preview value, and one
  parsed title array. Explicit expectations preserve exact title-set equality,
  duplicate-sensitive title count plus set equality, and the noisy-topology
  requirement for all standard titles plus at least one real fullscreen title.
  The unused subset helper is removed.
- Lifecycle and retained time policy: the owner installs its cancellable
  schedule before an immediate readback, accepting an already-published
  persistent projection. Diagnostics publication exposes no callback, so the
  shared named 100ms serial cadence remains only a conditional readback
  opportunity. Each invocation owns generation, scheduling, cancellation, and
  cleanup. Its caller watchdog is a terminal failure bound that reports the
  expected policy, parsed titles, raw preview and diagnostics values, element
  existence, source, and generation.
- UI-test infrastructure regression and Pressure: seven focused tests cover
  active/inactive/malformed parsing, initially satisfied evidence with
  registration/cancellation ordering, five delayed mismatches, rejection of a
  missing duplicate title, noisy standard/fullscreen acceptance, 100
  cancel/restart/stale-generation iterations with duplicate delivery, and
  watchdog final-projection diagnostics. The complete suite passed 7/7 in
  2.059 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AJ/owner-current-001`.
- Affected UI: the real three-application preview-cycle workflow passed 1/1
  in 28.490 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AJ/affected-multi-app-current-001`,
  requiring each selected app's complete preview title set without titles from
  another app. Clean and noisy Option+Tab window-state workflows passed 2/2
  in 95.994 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AD/affected-ui-current-001`.
  Clean and noisy Control+Tab workflows passed 1/1 in 43.896 seconds under
  `.build-local/evidence-driven-sync/SYNC-035D/affected-clean-current-001`
  and 1/1 in 99.477 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AO/affected-noisy-current-001`.
  These paths exercise exact sets, duplicate-sensitive counts, and the noisy
  standard/fullscreen subset contract.
- Process/Tooling: build attempt 002 preserved the Objective-C selector
  collision between the Set and array overloads. The count-sensitive entry
  now uses the distinct `toExactlyMatch:` selector. The fresh canonical build
  at
  `.build-local/evidence-driven-sync/SYNC-034AJ/process-build-attempt-003`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. Changed files have no compiler error; RuntimeTruth retains its
  existing mutable `runtimeLogSnapshot` warning. Swift parsing, project plist
  validation, unique project references, shared-helper uniqueness, removal of
  five used raw loops and one unused helper, and `git diff --check` pass. The
  remaining preview loop atomically matches both selected app identity and
  preview titles and is assigned to a separate next slice. Owner and regression
  files are 291 and 261 lines. Multi-app shrinks from 1,059 to 996 lines,
  RuntimeTruth from 863 to 834, in-app switching from 735 to 717, and noisy
  Option+Tab from 330 to 313. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only UI-test Switcher preview Oracles and test-owned observation
  infrastructure.
- Commit: `4c0ab8757b3bf2658f8d56cb971db28cc84b671e`
  (`test(sync): migrate SYNC-034AJ preview projection`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AJ preview projection`).

### SYNC-034AK Closure Record

- Design and Oracle: selected bundle identity and preview titles now come from
  one complete diagnostics accessibility-value readback. The
  generation-owned owner resolves only when one eligible workflow candidate's
  exact bundle identifier and expected title set match that atomic snapshot.
  Candidate exclusion is established before observation begins, preserving
  the unvisited-application contract.
- Lifecycle and retained time policy: the owner installs its cancellable
  schedule before an immediate readback. Diagnostics publication exposes no
  callback, so the shared named 100ms serial cadence remains a conditional
  readback opportunity. Each invocation owns generation, scheduling,
  cancellation, and cleanup. The named 12-second watchdog is a terminal
  failure bound that reports eligible candidates, selected bundle, parsed
  titles, raw projection, element existence, source, and generation.
- UI-test infrastructure regression and Pressure: six focused tests cover an
  atomic initially satisfied projection with registration/readback/cancellation
  ordering, five delayed mixed-projection rejections followed by exact
  resolution, eligible-candidate exclusion, ambiguous-candidate rejection, 100
  cancel/restart/stale-generation iterations with duplicate delivery, and
  watchdog final-projection diagnostics. The suite passed 6/6 in 1.807 seconds
  under `.build-local/evidence-driven-sync/SYNC-034AK/owner-current-001`.
- Affected UI: the real three-application window-card anchor isolation
  workflow passed 1/1 in 62.119 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AK/affected-window-cards-current-001`.
  Each cycle requires the same app's atomic selected bundle and title set,
  exact real window-card identifiers, and preview-image anchors while
  excluding previously visited candidates. The complementary preview-cycle
  workflow passed 1/1 in 28.490 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AJ/affected-multi-app-current-001`.
- Process/Tooling: the fresh canonical build at
  `.build-local/evidence-driven-sync/SYNC-034AK/process-build-attempt-004`
  reports `stage=completed`, with zero final, signing, and build-for-testing
  exit codes. Changed files have no compiler warning or error. Swift parsing,
  project plist validation, unique project references, one matcher definition,
  removal of its raw 100ms loop and 12-second deadline, and
  `git diff --check` pass. The owner and regression files are 178 and 255
  lines. The shared preview owner grows from 291 to 306 lines to carry the
  atomic selected-bundle field, while the oversized multi-app workflow shrinks
  from 996 to 966 lines. Startup `prompts.zip` remains unchanged and outside
  the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test multi-key Oracle and test-owned observation infrastructure.
- Commit: `87dee36e054510aa17762686d115cbbeb5c6e9b1`
  (`test(sync): migrate SYNC-034AK selected app preview`).
- Validation closure commit: pending current-slice local commit
  (`test(sync): validate SYNC-034AK selected app preview`).

### SYNC-034AL Closure Record

- Design and Oracle: each delayed window-layer transition now uses one
  generation-owned owner established before the trigger. Its initial readback
  is retained behind a trigger-completion gate, then an immediate trigger
  readback closes the synchronous race. Resolution requires one diagnostics
  accessibility value to report `windowCycle` and a nonempty preview
  projection while the exact expected preview-image marker exists. The marker
  is emitted only for a nonnil `WindowPreviewItem.image`; the adjacent
  `previewImages` value remains final diagnostic evidence because its
  accessibility publication can lag the marker.
- Lifecycle and retained time policy: the shared named 100ms serial XCUI
  readback cadence remains because the accessibility projection exposes no
  callback. The helper owns registration, generation, trigger gating,
  cancellation, and cleanup. Its named five-second watchdog is solely a
  terminal failure bound and records acceptance state, source, generation,
  mode, preview projection, image count, expected and observed marker
  identifiers, marker existence, and the full raw diagnostics value. The
  named eight-second runtime-log watchdog is likewise a terminal bound. The
  configured 300ms window-layer delay remains the user-visible product
  contract exercised by this regression.
- Ordered log Oracle: each call captures its runtime-log baseline before the
  trigger and requires the exact mock-browser `AutoEnter pending` record with
  `prewarmed=5` to precede the matching `windowCycle` entry. Entry-source
  variants remain diagnostic, so event scheduling can select
  `sessionLayoutChanged` or `deadlineReadback` without changing the result.
- Failure evidence: the first real run showed that reading an absent XCUI
  diagnostics element's identifier turns an initial readback into a query
  failure; absent snapshots now carry an explicit `unavailable` identifier.
  The next run observed the exact image marker with an accessibility
  `previewImages=0`, confirming the marker as the direct image-readiness
  evidence. A later run reached the marker and exposed
  `sessionLayoutChanged` as the valid entry source. The first repeated run
  then showed that re-observation can report `prewarmed=0` while the active
  pending projection reports `prewarmed=5`; the ordered Oracle now follows the
  pending state owned by the delayed-entry request.
- UI-test infrastructure regression and deterministic Pressure: the final
  canonical owner run passed 5/5 with zero failures in 4.258 seconds (4.260
  seconds total) under
  `.build-local/evidence-driven-sync/SYNC-034AL/owner-attempt-002`.
  Coverage includes registration-before-initial-readback ordering, initially
  complete evidence even while the adjacent image count is stale, pre-trigger
  evidence gating, 20 delayed partial readbacks followed by exact resolution,
  100 cancel/restart generations with stale and duplicate callbacks, and
  watchdog final-evidence diagnostics.
- UI and real-topology Pressure: the canonical install wrapper rebuilt,
  signed, and verified the fixed-path Apple Development app for the final
  production source under
  `.build-local/evidence-driven-sync/SYNC-034AL/ui-install-final-001`.
  The final combined run passed 2/2 with zero failures in 30.854 seconds
  (30.856 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-034AL/ui-final-attempt-002`.
  The focused Option+Tab path completed in 4.734 seconds. The repeated path
  completed 12 exact preview transitions, ordered prewarm/entry checks,
  confirmations, and presentation-owner releases in 26.121 seconds.
- Process/Tooling: the final complete UI-test target build at
  `.build-local/evidence-driven-sync/SYNC-034AL/process-build-attempt-007`
  reports `stage=completed` with zero build, signing, and final exit codes.
  Swift parsing, project plist validation, unique project references, removal
  of the raw 20ms loop and 100ms grace, scoped time-logic review, source-size
  checks, `git diff --check`, and exact staged-content review are recorded
  before commit. The owner and regression files are 183 and 256 lines; the
  interaction-regression source remains below 400 lines, and the shared
  diagnostics owner remains below 400 lines. Startup `prompts.zip` remains
  unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes one
  UI-test Oracle and its test-owned observation infrastructure, with no
  application or core-domain runtime behavior change.
- Commit: pending current-slice local commit
  (`test(sync): migrate SYNC-034AL preview transition`).

### SYNC-034AM Closure Record

- Design and Oracle: shared UI-test setup and teardown now resolve every
  configured application target by its exact installed URL or bundle
  identity, capture the initial `XCUIApplication.State`, request termination,
  perform an immediate post-trigger readback, wait for `.notRunning` when
  needed, and take a final readback. Only the final `.notRunning` state
  satisfies cleanup. A waiter that crosses its terminal boundary while the
  process finishes still succeeds when the final readback observes
  `.notRunning`, so scheduler timing changes elapsed time while preserving the
  result.
- Lifecycle and retained time policy: the synchronous setup or teardown
  invocation owns the entire wait and leaves no observer or scheduled work.
  `FlowTabUITestApplicationTerminationPolicy.watchdogFailureObservationTimeout`
  is a named five-second terminal failure bound. Its stable diagnostics
  include the exact target, initial state, termination-request flag, immediate
  post-trigger state, waiter result, and final state. URL and bundle targets
  are both checked, closing the process lifetime before defaults restoration
  or the next test launch.
- Deterministic owner regression: the final canonical run passed 5/5 with zero
  failures in 1.086 seconds (1.088 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-034AM/owner-final-002`. Coverage
  includes an initially stopped target, synchronous termination, delayed
  termination, a wait-boundary final readback, and a watchdog retaining the
  last running state.
- UI and process-lifecycle Pressure: five consecutive granted-permission Home
  launches and teardowns passed 5/5 with zero failures in 34.409 seconds
  (34.418 seconds total) under
  `.build-local/evidence-driven-sync/SYNC-034AM/process-pressure-current-002`.
  The five launches used distinct PIDs and every teardown completed through
  the exact final-state assertion before the next setup.
- Slice-boundary evidence: the first candidate listener-driven pointer run
  completed four of five iterations and used a distinct app PID on every
  iteration. Its failed iteration presented a switcher summary without the
  required exact window card under
  `.build-local/evidence-driven-sync/SYNC-034AM/process-pressure-current-001`.
  That full-panel projection and trigger-delivery contract remains owned by
  SYNC-034AC; its evidence is retained for the next independent slice.
- Process/Tooling: the final complete UI-test target build passed with signing
  and final exit codes equal to zero under
  `.build-local/evidence-driven-sync/SYNC-034AM/process-build-final-002`.
  The initial compile attempt identified an SDK-invalid suspended-background
  state, and the first owner attempt identified XCTest's unstable default
  state rendering. The final implementation uses the four macOS SDK states
  and stable diagnostic labels. Project plist validation, unique project
  references, source-size checks, scoped time-logic review,
  `git diff --check`, and exact staged-content review are recorded before
  commit. Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, FlowTabCore, and production UI behavior: not relevant
  because this slice changes only shared UI-test process cleanup and its
  test-owned deterministic model.
- Commit: pending current-slice local commit
  (`test(sync): migrate SYNC-034AM process termination`).

### SYNC-034AN Closure Record

- Design and Oracle: Option+Tab, Control+Tab, and Search pointer paths now
  launch with the test trigger listener and runtime logging active. Each
  presentation captures the runtime-log baseline before posting one exact
  Darwin notification, requires its post-baseline
  `presented=1 syntheticModifierHeld=1` completion record, then requires the
  exact diagnostics panel element and the caller's exact application tile,
  window card, or Search result. Option+Tab presentation is therefore
  on-demand under the same contract as the other modes.
- Lifecycle and retained time policy: the helper invocation owns the
  post-baseline log observation and XCTest accessibility waits. The
  process-owned Darwin observer clears both testing overrides before holding
  the mode's primary modifier, releases both immediately when presentation is
  rejected, and otherwise retains the hold until the exact process
  termination established by SYNC-034AM. The named four-second trigger
  delivery and five-second panel presentation watchdogs are solely terminal
  failure bounds; completion is determined by the exact log and
  accessibility evidence.
- Failure evidence: the first affected nine-test run failed the Control
  pointer path after the panel summary appeared without its exact primary
  window card. A standalone replay passed, while a later nine-test run and
  repeated Option path exposed the reciprocal panel disappearance. The exact
  runtime log recorded trigger receipt, a ready seven-window session,
  successful `presented=1` completion, a panel awaiting window-server
  occlusion evidence, then visibility-watchdog cancellation with
  `reason=modifierReleased`. Darwin delivery carries no hardware key state,
  so scheduling pressure could previously cancel a correctly presented
  session before the accessibility projection completed.
- Behavior regression: the canonical FlowTabTests wrapper passed 14/14 with
  zero failures in 0.425 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AN/app-tests-final-001`.
  Coverage includes initially satisfied visibility, notification delivery,
  stale and duplicate events, delayed final readback, cancellation, watchdog
  diagnostics, rapid replacement Pressure, and the visibility policy's
  deterministic state transitions.
- UI affected paths: the final canonical combined run passed 14/14 with zero
  failures in 91.837 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AN/ui-contracts-final-002`.
  It covers the five runtime-log observation contracts plus Option, Control,
  and Search click, moving-pointer, and stationary-pointer paths against the
  installed fixed-path application. After tightening the shared delivery
  Oracle to require the held-modifier field, the rebuilt exact-log regression
  plus one moving-pointer path for each mode passed 4/4 in 25.950 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-034AN/ui-contracts-final-003`.
- Real-process Pressure: the post-fix Option moving-pointer path passed 10/10
  in 82.499 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AN/option-hover-pressure-final-001`.
  The post-fix Control moving-pointer path passed 10/10 in 78.188 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-034AN/control-hover-pressure-final-001`.
  Every iteration launched and terminated an exact application process while
  preserving the expected tile or window-card result.
- Process/Tooling: the canonical install wrapper rebuilt, signed, installed,
  and verified the current fixed-path application under
  `.build-local/evidence-driven-sync/SYNC-034AN/ui-test-app-final-001`.
  The final complete UI-test target build and runner signing passed under
  `.build-local/evidence-driven-sync/SYNC-034AN/process-build-final-001`.
  The build log contains no warning or error for a changed source. Project
  plist validation, unique project references, scoped fixed-time review,
  source-size checks, `git diff --check`, and exact staged-content review are
  recorded before commit. The new presentation helper is 93 lines; the shared
  bootstrapper remains within its 800-line single-responsibility guardrail at
  785 lines, the shared UI support remains below that guardrail at 656 lines,
  and the previously oversized pointer workflow shrinks from 839 to 837
  lines. Startup `prompts.zip` remains unchanged and outside the slice.
- Unit and FlowTabCore: not relevant because this slice changes a
  TestingSupport-only trigger contract and UI-test presentation Oracle. The
  deterministic behavior and UI layers cover its affected state machine and
  visible paths.
- Commit: pending current-slice local commit
  (`test(sync): migrate SYNC-034AN pointer presentation`).

### SYNC-034AO Closure Record

- Failure evidence: the noisy Control+Tab runtime-truth path failed twice at
  the same trigger boundary under
  `.build-local/evidence-driven-sync/SYNC-034AH/affected-control-current-001`
  and
  `.build-local/evidence-driven-sync/SYNC-034AH/affected-control-noisy-current-002`.
  The post-baseline log records exact notification receipt while real runtime
  window collection continues through the four-second completion watchdog.
  The test terminates the application before a completion record can publish.
- Design and Oracle: one trigger-delivery owner creates independent receipt
  and completion observers from the same baseline and starts both before the
  Darwin notification. Receipt requires the exact notification name.
  Completion requires the same exact name with
  `presented=1 syntheticModifierHeld=1`. Unrelated, stale, or partial records
  cannot satisfy a stage; completion-before-receipt remains gated by the
  receipt requirement before the helper returns.
- Lifecycle and retained time policy: the trigger invocation owns both
  runtime-log event sources, serial fallback schedules, generations,
  cancellation, and cleanup. The named four-second receipt watchdog and
  12-second presentation-completion watchdog are terminal failure bounds.
  Expiry reports the missing stage plus the final post-baseline log
  projection.
- Deterministic regression and lifecycle Pressure: four focused owner tests
  passed 4/4 in 1.252 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AO/owner-current-002`. They cover
  five missed readbacks followed by completion-before-receipt ordering,
  initially complete projection, 100 cancel/restart iterations with stale and
  duplicate callbacks, and a completion-watchdog failure carrying the final
  receipt-only projection.
- Affected UI: the noisy real-fixture Control+Tab topology passed 1/1 in
  99.477 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AO/affected-noisy-current-001`.
  It completed two normal/full-screen cross-Space round trips with exact CG
  window identities. The representative Control+Tab pointer presentation and
  hover path passed 1/1 in 7.929 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AO/affected-pointer-current-001`.
- Process/Tooling: the canonical complete UI-test target build and runner
  signing passed under
  `.build-local/evidence-driven-sync/SYNC-034AO/process-build-current-003`;
  every recorded final, build, and signing exit code is zero, and changed
  sources have no compiler warning or error. Swift parsing,
  `git diff --check`, and source-size checks pass. The pointer helper,
  runtime-log owner, owner regression, and shared support files are 96, 372,
  671, and 692 lines respectively. The restricted-service build attempt under
  `.build-local/evidence-driven-sync/SYNC-034AO/process-build-current-002`
  stopped before compilation when `filecoordinationd` and
  `CoreSimulatorService` became unavailable; the complete-environment rerun is
  the authoritative result. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  shared UI-test trigger observation and its regression infrastructure.
- Commit: pending current-slice local commit
  (`test(sync): migrate SYNC-034AO trigger completion`).

### SYNC-034AP Closure Record

- Design and Oracle: every Space-fixture cleanup caller delegates to the
  shared application-termination evidence contract. The helper reads the
  exact `XCUIApplication` state before one termination request, reads back
  immediately, enters XCTest's persistent `.notRunning` state wait only when
  needed, and succeeds only when the final readback is `.notRunning`.
- Lifecycle and watchdog: one cleanup invocation owns the synchronous state
  wait through return. The named five-second default is solely a terminal
  failure bound. Failure reports the target identity and initial,
  post-trigger, waiter, and final states; an already stopped target completes
  from its initial readback without another termination request.
- Deterministic regression and Pressure: the final-source owner run passes
  6/6 with zero failures in 1.289 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AP/owner-tests-current-002`.
  It covers initially stopped, immediate completion, delayed state evidence,
  final readback at the wait boundary, terminal failure diagnostics, and 100
  independent immediate/delayed termination lifecycles.
- UI affected path: the real fixture launch, Home projection, exact process
  termination, runtime lifecycle publication, and Home removal path passes
  1/1 in 21.613 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AP/affected-lifecycle-current-002`.
- Process/Tooling: the final complete UI-test target build and runner signing
  pass under
  `.build-local/evidence-driven-sync/SYNC-034AP/process-build-current-002`.
  The five changed Swift files parse, the retired helper name and its raw
  RunLoop loop are absent, the shared termination owner is 301 lines, the
  Space-fixture helper is 390 lines, the existing main workflow shrinks to
  786 lines, the oversized multi-app workflow shrinks by two lines, and
  `git diff --check` passes. The slice root is owned by the immediate
  post-commit cleanup step.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  UI-test process cleanup and its deterministic test-owned evidence helper,
  with no application or core-domain runtime behavior change.
- Commit: pending current-slice local commit
  (`test(sync): migrate SYNC-034AP fixture termination`).

### SYNC-035A Closure Record

- Design and Oracle: the System MRU path establishes an XCTest predicate
  expectation for nonexistence of the exact switcher summary before sending
  Escape. Dismissal succeeds only when that persistent accessibility element
  is absent, including an initially absent readback. The next trigger begins
  only after this evidence is observed.
- Lifecycle and timeout policy: each helper invocation owns its expectation.
  `FlowTabUITestSystemAppMRUPolicy.switcherDismissalWatchdog` is solely a
  terminal failure bound; expiry reports the final switcher accessibility
  hierarchy through `switcherDebugSummary`.
- UI affected path and Pressure: the real eight-application System MRU
  topology completed ten consecutive Escape dismissal and switcher reopen
  iterations, while preserving the exact expected application order. The
  test passed 1/1 in 42.311/42.313 seconds, with 43.655 seconds total test
  operation time, at
  `.build-local/evidence-driven-sync/SYNC-035A/system-mru-attempt-001`.
- Process/Tooling: the changed Swift file parses, the Xcode project plist is
  valid, compiler diagnostics contain no warning for the changed file, the
  fixed 300ms RunLoop advance has no remaining reference in
  `FlowTabUITests+SystemAppMRU`, the file is 264 lines, and
  `git diff --check` passes. Final exact staged-content review is recorded
  before commit. Startup `prompts.zip` remains unchanged and outside the
  slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  only one UI-test presentation lifecycle and its existing real-topology
  path.
- Commit: `9e84dbe7675f0d9c070c3b074ecbe9c58c779dbf`
  (`test(sync): migrate SYNC-035A switcher dismissal`).

### SYNC-035B Closure Record

- Design and Oracle: `assertTriggerMakesValue` starts the existing
  generation-owned element-value observer before sending Tab. Its acceptance
  gate remains closed through the trigger, so an already matching baseline
  cannot prove commit completion. Success requires the delay field to exist
  and publish the exact normalized accessibility value `1.23`; the relaunch
  readback independently proves persistence.
- Lifecycle and retained cadence: the helper invocation owns the serial
  scheduled observation and cancels it on success, watchdog expiry, or scope
  exit. XCUI exposes no text-field value publication callback, so the shared
  named 100ms readback cadence remains a conditional observation policy. The
  five-second caller watchdog is solely a failure bound and reports identifier,
  existence, acceptance state, expected predicate, actual value, generation,
  source, and waiter result.
- Deterministic regression: matching baseline rejection before trigger
  completion, scheduled post-trigger resolution, initial exact-value
  resolution, prefix resolution, cancellation, and watchdog final evidence
  passed 3/3 in 1.145/1.150 seconds. Total test operation time was 7.360
  seconds at
  `.build-local/evidence-driven-sync/SYNC-035B/owner-regression-attempt-002`.
- UI affected path: the real Settings path entered `1.2345`, observed exact
  post-Tab normalization to `1.23`, toggled both adjacent preferences, and
  verified all three persisted values after relaunch. It passed 1/1 in
  21.712/21.716 seconds, with 22.441 seconds total operation time, at
  `.build-local/evidence-driven-sync/SYNC-035B/affected-ui-attempt-001`.
- Process/Tooling: both final wrapper status files report `completed`, zero
  final, signing, build, and test exit codes, and present result bundles. Four
  changed Swift files parse and compile, the fixed 200ms helper and its sole
  caller are removed, the project plist is valid, and `git diff --check`
  passes. The build retains two warnings in unchanged future migration paths.
  The owner and regression files are 162 and 152 lines; the Settings test file
  remains within the 800-line single-responsibility guardrail at 779 lines,
  and Support shrinks to 753. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, FlowTabCore, and Pressure: not relevant because this slice
  changes one UI-test commit Oracle, reuses the pressure-validated shared
  serial scheduler, and does not change application runtime behavior.
- Commit: `881cca7be035f367bf8f72cae23bf32d37c70ed1`
  (`test(sync): migrate SYNC-035B settings commit`).

### SYNC-035C Closure Record

- Design and Oracle: every noisy Option-Tab `.advanceRight` now establishes a
  generation-owned diagnostics observer before command delivery. The current
  exact CG window number is the baseline and acceptance remains gated until
  delivery returns. An immediate trigger readback closes the synchronous
  completion race. Success requires one atomic diagnostics value with
  window-cycle mode, a nonempty title, the canonical
  `cg:<pid>:<windowNumber>` identity with positive components, and a window
  number different from the baseline. The existing ordered-prefix and exact
  activation assertions remain the independent product Oracle.
- Lifecycle and retained cadence: each helper invocation owns registration,
  generation, waiting, cancellation, and cleanup. XCUI exposes no publication
  callback for the diagnostics accessibility value, so the shared named 100ms
  serial readback cadence remains the conditional-observation policy. The
  named four-second watchdog is solely a terminal failure bound and reports
  acceptance state, baseline identity, mode, selected identity/title/number,
  completeness, generation, source, and the final raw diagnostics value.
- Deterministic regression and lifecycle Pressure: the canonical owner run
  passed 5/5 with zero failures in 1.030 seconds under
  `.build-local/evidence-driven-sync/SYNC-035C/owner-current-attempt-001`.
  Coverage includes observer registration before the initial readback,
  pre-trigger evidence gating, the synchronous trigger race, complete and
  malformed CG identities, same-title identity transitions, 100
  cancel/restart generations with stale and duplicate callbacks, and watchdog
  final-evidence diagnostics.
- UI affected path: the current-source fixed-path app was rebuilt, signed, and
  verified with Team `96PUA726W9` under
  `.build-local/evidence-driven-sync/SYNC-035C/app-build-current-001`.
  The four-window noisy fullscreen/off-Space round trip passed 1/1 in 58.704
  seconds under
  `.build-local/evidence-driven-sync/SYNC-035C/affected-ui-current-attempt-001`.
  It selected and confirmed both normal windows and both fullscreen siblings,
  preserving the exact app-local order and activation result across repeated
  switcher presentations.
- Real-topology Pressure: the same four-window scenario passed 1/1 in 59.254
  seconds with a 500ms sampling cadence under
  `.build-local/evidence-driven-sync/SYNC-035C/runtime-topology-pressure-current-attempt-001`.
  All 83 PID identity checks matched the frozen fixed-App contract across
  5,232.261ms, with 22 candidate observations and zero rejected transient
  identities. CPU average/p95/max was 93.02/141.20/152.70 percent and RSS
  average/p95/max was 167.72/226.27/279.09 MiB. These values remain within the
  recent same-machine SYNC-040 range for the same topology path; scheduling
  pressure changed elapsed work and sampling density while preserving every
  exact selection and activation result. The private identity-manifest
  SHA-256 is
  `a1884a7130536a45a2febfce94089a57423abc62a7163c40f9c93d52b04a8808`.
- Process/Tooling: all three canonical wrapper status files and the nested
  Pressure UI status report `stage=completed`, zero final/build/signing/test
  exit codes, valid result bundles, successful sampling, and exact identity
  matching. The three changed Swift files parse, the Xcode project plist is
  valid, each new source has one file/build reference and one group/Sources
  membership, and the raw 250ms RunLoop advance is absent. The owner,
  regression, and migrated caller files are 243, 287, and 323 lines.
  `git diff --check` passes. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes one
  UI-test Oracle and its test-owned observation infrastructure, with no
  application or core-domain runtime behavior change.
- Commit: pending current-slice local commit
  (`test(sync): migrate SYNC-035C window selection`).

### SYNC-035D Closure Record

- Failure evidence: the current-source clean Control+Tab runtime-truth path
  completed its first presentation, exact app-strip projection, window
  selection, and exact normal-window activation. It then created a new
  bundle-only `XCUIApplication`, called `activate()`, and failed after 97.80
  seconds because XCTest never attached a process identifier. The same
  failure reproduced in an isolated run at
  `.build-local/evidence-driven-sync/SYNC-034AH/affected-control-clean-current-002`.
- Design and Oracle: fixture refocus now resolves the workflow app's stored
  path intent through `makeSpaceFixtureWorkflowApplication` and reuses the
  generation-owned workflow-window activation contract. The observer is
  installed before activating the exact fixture application. Acceptance stays
  gated until activation returns and requires the fixture bundle plus the
  previously selected CG window number in one readback before the second
  Control+Tab trigger.
- Lifecycle and retained time policy: the helper invocation owns workspace,
  AX, and named serial condition observation through
  `triggerAndWaitForFrontmostWorkflowWindow`; scope exit cancels every source.
  The 12-second caller watchdog is solely a terminal failure bound and reports
  the final frontmost bundle, topmost CG window, active title, title
  observability, generation, and source.
- Deterministic regression and lifecycle Pressure: the six current-source
  workflow-window activation owner tests pass 6/6 in 1.508 seconds under
  `.build-local/evidence-driven-sync/SYNC-035D/owner-current-001`. They cover
  initial exact evidence, registration-before-readback and trigger gating,
  exact bundle/window rejection, five delayed readbacks, 100
  cancel/restart/stale-generation iterations with duplicate delivery, and
  watchdog final-readback diagnostics.
- Affected UI: the isolated clean Control+Tab runtime-truth path passes 1/1 in
  43.896 seconds under
  `.build-local/evidence-driven-sync/SYNC-035D/affected-clean-current-001`.
  It restores the exact normal-window CG identity before the second
  presentation, then selects and activates the exact fullscreen sibling. The
  pre-migration path failed at the same boundary after 97.80 seconds.
- Process/Tooling: the complete UI-test target build and runner signing pass
  under
  `.build-local/evidence-driven-sync/SYNC-035D/process-build-current-002`.
  The first compile attempt identified the second noisy-workflow caller; the
  corrected final build compiles both clean and noisy paths. Swift parsing,
  the absence of the scoped 350ms settle, source-size review at 732 lines,
  and `git diff --check` pass. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes one
  UI-test fixture refocus Oracle and its real-topology regression path.
- Commit: pending current-slice local commit
  (`test(sync): migrate SYNC-035D fixture refocus`).

### SYNC-035E Closure Record

- Design and Oracle: the production coordinator establishes window-mount and
  key-window observers before synchronizing first responder. Its exact
  readiness readback is `window.isKeyWindow && window.firstResponder ===
  textView`, where the responder class is `SearchSystemTextView`. A
  false-to-true transition writes the persistent INFO marker
  `keyboardReadiness ready=1 identifier=flowtab.switcher.search.input
  responder=SearchSystemTextView windowKey=1`. Each UI caller establishes its
  runtime-log baseline and observer before launch or trigger, waits for that
  exact marker, and then requires the exact XCU input identity.
- Lifecycle and retained cadence: the production coordinator owns its
  notification tokens, weak window/view references, synchronization generation,
  and detach/deinit cancellation. The UI helper owns the runtime-log baseline,
  file observation, wait, and teardown cancellation. The shared named 200ms
  runtime-log readback cadence closes file-event gaps, and success remains tied
  to the exact marker. The 35-second marker watchdog and five-second
  accessibility-publication watchdog are terminal failure bounds with the
  final runtime-log evidence.
- Behavior: the current-source FlowTabTests run passes 3/3 in 0.669 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-035E/behavior-readiness-current-013`.
  It covers initially satisfied exact readiness, duplicate key-window
  notifications, delayed key-window evidence, cancellation, and stale
  generation rejection.
- Deterministic owner regression and lifecycle Pressure: the current-source
  owner run passes 5/5 in 2.654 seconds under
  `.build-local/evidence-driven-sync/SYNC-035E/owner-log-current-002`. Coverage
  includes observer-before-readback ordering, initial and later marker
  evidence, exact identifier/responder/key-window rejection, 100
  cancel/restart generations with stale and duplicate callbacks, and watchdog
  final-evidence diagnostics.
- UI affected paths: the fixed-path app was rebuilt, signed, and verified with
  Team `96PUA726W9` under
  `.build-local/evidence-driven-sync/SYNC-035E/install-build-current-004`.
  The Chinese mock Search path passes 1/1 in 7.518 seconds under
  `.build-local/evidence-driven-sync/SYNC-035E/affected-mock-current-002`.
  The Settings default-scope persistence path passes 1/1 in 33.262 seconds
  under
  `.build-local/evidence-driven-sync/SYNC-035E/affected-user-path-current-006`.
  The real Space-fixture Search entry, exact application result, activation,
  and panel dismissal path passes 1/1 in 27.502 seconds under
  `.build-local/evidence-driven-sync/SYNC-035E/affected-real-topology-current-001`.
- Process/Tooling: the complete UI-test target build and runner signing pass
  under
  `.build-local/evidence-driven-sync/SYNC-035E/owner-build-current-005`.
  All ten changed Swift files parse, the Xcode project plist and new source
  references validate, scoped 400ms Search waits and the retired helper have
  no remaining references, the 758-line production bridge retains one text
  input responsibility, and `git diff --check` passes. Startup `prompts.zip`
  remains unchanged and outside the slice.
- Unit and FlowTabCore: not relevant because the synchronization owner is the
  AppKit bridge; its deterministic state, event, cancellation, and ordering
  rules are covered by the Behavior tests above.
- Commit: pending current-slice local commit
  (`refactor(sync): migrate SYNC-035E Search input readiness`).

### SYNC-035F Closure Record

- Design and Oracle: each global Option+Tab or in-app Control+Tab selection
  helper establishes the generation-owned diagnostics observer before posting
  `.advanceRight` or `.inAppForward`. It records the current exact CG window
  number as the baseline, gates acceptance until command delivery returns,
  and performs an immediate trigger-return readback. Success requires one
  atomic window-cycle diagnostics value with a nonempty title and a canonical
  changed `cg:<pid>:<windowNumber>` identity. Each caller advances the exact
  baseline returned by the preceding successful transition.
- Lifecycle and retained cadence: the helper invocation owns registration,
  generation, waiting, cancellation, and cleanup. XCUI exposes no publication
  callback for the diagnostics accessibility value, so the shared named 100ms
  serial readback cadence remains the conditional-observation policy. The
  named four-second watchdog is solely a terminal failure bound and reports
  acceptance state, baseline identity, mode, selected identity/title/number,
  completeness, generation, source, and the final raw diagnostics value.
- Deterministic regression and lifecycle Pressure: the final-source owner run
  passes 5/5 with zero failures in 2.149 seconds under
  `.build-local/evidence-driven-sync/SYNC-035F/owner-tests-current-002`.
  Coverage includes registration before initial readback, the synchronous
  trigger race, complete changed CG identity, 100 cancel/restart generations
  with stale callbacks, and watchdog final-evidence diagnostics.
- UI affected paths: the real fullscreen/off-Space fixture topology passes
  2/2 with zero failures in 91.311 seconds under
  `.build-local/evidence-driven-sync/SYNC-035F/affected-real-topology-current-002`.
  The exact normal and fullscreen CG identities are selected and activated
  through both Control+Tab and Option+Tab paths.
- Process/Tooling: the final-source UI-test target build and runner signing
  pass under
  `.build-local/evidence-driven-sync/SYNC-035F/process-build-current-002`.
  All four changed Swift files parse, both scoped 250ms RunLoop advances are
  absent, the extracted observation owner is 344 lines, the existing 812- and
  770-line workflow callers did not grow, and `git diff --check` passes.
  Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes a
  UI-test Oracle and its test-owned observation infrastructure, with no
  application or core-domain runtime behavior change.
- Commit: pending current-slice local commit
  (`test(sync): migrate SYNC-035F window selection`).

### SYNC-035G Closure Record

- Design and Oracle: every Down Arrow input now has a generation-owned
  diagnostics observer established before the input is delivered. Acceptance
  is gated until delivery returns, followed by an immediate trigger readback.
  The test requires the exact percent-decoded `searchSelectedResult` identity
  for results 01 through 10 and then result 01 after wrap. Exact last-result
  and first-result row hittability remain independent evidence that selection
  scrolling reached each boundary.
- Lifecycle and retained cadence: each shared-helper invocation owns its
  generation, readback schedule, wait, cancellation, and cleanup. XCUI exposes
  no callback for diagnostics accessibility-value publication, so the shared
  named 100ms serial readback cadence remains the conditional-observation
  policy. The three-second watchdog is solely a failure bound and reports the
  encoded and normalized final diagnostics projection.
- Deterministic regression and lifecycle Pressure: the current-source
  diagnostics-owner run passes 8/8 with zero failures in 2.578 seconds under
  `.build-local/evidence-driven-sync/SYNC-035G/owner-tests-current-001`.
  Coverage includes matching initial evidence, percent decoding, prefix
  matching, post-trigger gating, five delayed readbacks, atomic multi-key
  projection, 100 cancel/restart generations with stale callbacks, and
  watchdog final-readback diagnostics.
- UI affected path: the Search-wrap mock path passes 1/1 with zero failures in
  11.072 seconds under
  `.build-local/evidence-driven-sync/SYNC-035G/affected-search-wrap-current-001`.
  It observes all ten exact selection identities, the exact wrap to result 01,
  and hittable result rows at both scroll boundaries.
- Process/Tooling: the complete UI-test target build and runner signing pass
  under
  `.build-local/evidence-driven-sync/SYNC-035G/process-build-current-001`.
  Both changed Swift files parse, the Xcode project plist is valid, the new
  99-line source has one file reference, one group membership, one build
  reference, and one Sources membership. The scoped 100ms/80ms fixed advances
  are absent, the existing oversized owner shrank to 821 lines, all three
  wrapper status files report `stage=completed` with zero final, signing,
  build, and test exit codes, and `git diff --check` passes. The fixed-path app
  remains current because this slice changes only UI-test code. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes one
  UI-test Oracle and its test-owned call path, with no application or core
  runtime behavior change.
- Commit: `a52e5e7bf9dff745a750d2ad7ab84f3bdae4260d`
  (`test(sync): migrate SYNC-035G Search selection wrap`).

### SYNC-035H Closure Record

- Design and Oracle: `SwitcherSearchViewState` records `resultsQuery` and
  `resultsScope` only when the current revision accepts a result-computation
  callback. Testing diagnostics publish those fields atomically with
  `searchResults`. The affected UI path establishes the shared diagnostics
  observer before typing `Mail`, gates acceptance until input delivery
  returns, and accepts only one complete snapshot with scope `app`,
  percent-decoded committed query `Mail`, and an empty result set. Exact
  nonexistence of the hidden Mail row remains the independent visible
  readback.
- Lifecycle and retained cadence: each shared-helper invocation owns its
  generation, readback schedule, wait, cancellation, and cleanup. XCUI exposes
  no callback for diagnostics accessibility-value publication, so the shared
  named 100ms serial readback cadence remains the conditional-observation
  policy. The five-second watchdog is solely a failure bound and reports the
  final atomic diagnostics projection. Consecutive settings, Switcher, and
  Search phases reuse the SYNC-034AM process-state helper; each phase verifies
  exact `.notRunning` readback before the next launch.
- Failure evidence: the first two affected-path runs stopped before the new
  Search observation because the Browser row was unavailable after a raw
  process termination and relaunch. Routing those two phase boundaries through
  exact process-state readback allowed the same current-source path to reach
  and satisfy the Search evidence on the next run.
- Behavior and Pressure: the current-source scheduling run passes 6/6 with
  zero failures in 0.155 seconds under
  `.build-local/evidence-driven-sync/SYNC-035H/behavior-tests-current-001`.
  Coverage proves query publication precedes result completion, exact
  query/scope advancement on the accepted callback, canceled and out-of-order
  rejection, synchronous completion, slow-scheduler result equivalence, named
  policy boundaries, and 2,000 revision replacements.
- Shared observation owner: the current-source diagnostics-owner UI run passes
  8/8 with zero failures in 3.017 seconds under
  `.build-local/evidence-driven-sync/SYNC-035H/owner-ui-tests-current-001`.
  It covers matching initial evidence, percent decoding, prefix matching,
  post-trigger gating, delayed readback, atomic multi-key projection, 100
  cancel/restart generations with stale callbacks, and watchdog final-readback
  diagnostics.
- UI affected path: the settings-to-Switcher-to-Search mock topology passes
  1/1 with zero failures in 29.349 seconds under
  `.build-local/evidence-driven-sync/SYNC-035H/affected-settings-search-current-003`.
  It confirms the hidden Mail identity is absent from the Switcher and from the
  Search projection after the exact committed-query evidence arrives.
- Process/Tooling: fixed-path app installation succeeds from
  `.build-local/evidence-driven-sync/SYNC-035H/install-build-current-001`.
  The complete UI-test target build and Apple Development runner signing pass
  under
  `.build-local/evidence-driven-sync/SYNC-035H/process-ui-build-current-002`.
  All four changed Swift files parse, the scoped 800ms fixed advance is absent,
  the four successful wrapper status files report `stage=completed` with zero
  relevant exit codes, and `git diff --check` passes. Startup `prompts.zip`
  remains unchanged and outside the slice.
- FlowTabCore: not relevant because the accepted Search projection belongs to
  the application orchestration layer and preserves existing core-domain
  behavior.
- Commit: `40dbe442ab21ff81a515ea62b1fa77294437f65d`
  (`refactor(sync): migrate SYNC-035H Search result completion`).

### SYNC-034AQ Closure Record

- Design and Oracle: both Home zero-window assertions now reuse
  `FlowTabUITestElementValueObservationOwner`. Each invocation performs an
  immediate existence and accessibility-value readback and resolves only on
  the exact `"0w"` value. Runtime row order, stable frames, and the later
  zero-count readbacks remain independent assertions in the affected path.
- Lifecycle and retained cadence: each `assertValue` invocation owns its
  generation, one serial readback schedule, wait, cancellation, and cleanup.
  XCUI exposes no accessibility-value publication callback, so the shared
  named 100ms cadence remains the conditional-observation policy. The
  two-second watchdog is solely a failure bound and reports identifier,
  existence, expected predicate, final value, source, generation, and waiter
  result.
- Deterministic owner regression: the current-source element-value owner run
  passes 3/3 with zero failures in 2.603 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AQ/owner-tests-current-001`.
  It covers matching-baseline rejection until trigger completion, initially
  satisfied exact evidence, delayed prefix evidence, cancellation, and
  watchdog final-readback diagnostics. The shared scheduler and generation
  Pressure contract is unchanged from SYNC-034H/SYNC-034I.
- UI affected path: the mock-runtime Home path passes 1/1 with zero failures in
  9.917 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AQ/affected-home-current-001`.
  It accepts both exact zero-window values while preserving runtime app order,
  stable row frames, and the no-accessibility-permission boundary.
- Process/Tooling: the complete UI-test target build and Apple Development
  runner signing pass under
  `.build-local/evidence-driven-sync/SYNC-034AQ/process-ui-build-current-001`.
  The changed Swift file parses, the scoped private 100ms loop is absent, the
  existing oversized file shrank from 844 to 829 lines, all three wrapper
  status files report `stage=completed` with zero relevant exit codes, and
  `git diff --check` passes. The fixed-path app remains current because this
  slice changes only UI-test code. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, FlowTabCore, and new Pressure: not relevant because this
  slice routes one UI-test Oracle through already validated shared
  infrastructure without changing application or core-domain runtime
  behavior.
- Commit: `bd3564cb5024b7fa69eb00799145d53af89e5241`
  (`test(sync): migrate SYNC-034AQ Home app value`).

### SYNC-034AR Closure Record

- Design and Oracle: the shared Home projection expectation now supports a
  nonempty exact row-label prefix. The real recency path establishes the
  projection owner before selecting the Chrome fixture app, rejects baseline
  evidence until the tap returns, performs an immediate trigger readback, and
  resolves only when the complete row snapshot starts with `Draft`, `Inbox`.
  The preceding exact CG window activation remains an independent Oracle for
  the recency input.
- Lifecycle and retained cadence: each helper invocation owns its generation,
  serial readback schedule, wait, cancellation, and cleanup. XCUI exposes no
  Home projection callback, so the shared named 100ms cadence remains the
  conditional-observation policy. The 12-second watchdog is solely a failure
  bound and reports acceptance state, ordered row identifiers/labels/values,
  source, generation, and waiter result.
- Deterministic regression and Pressure: the current-source Home projection
  owner run passes 3/3 with zero failures in 1.808 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AR/owner-tests-current-001`.
  Coverage includes initial and scheduled evidence, trigger-gated wrong-order
  rejection, delayed exact-prefix acceptance, empty-prefix rejection,
  cancellation, and 100 owner lifecycles without post-resolution reads.
- UI affected path: the signed Finder/Chrome real-fixture topology passes 1/1
  with zero failures in 47.586 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AR/affected-home-recency-current-001`.
  It activates the exact `Draft` CG window, returns to FlowTab, and observes
  the exact `Draft`, `Inbox` Home prefix before fixture and FlowTab process
  cleanup.
- Process/Tooling: the complete UI-test target build and Apple Development
  runner signing pass under
  `.build-local/evidence-driven-sync/SYNC-034AR/process-ui-build-current-001`.
  The affected wrapper also rebuilds and signs all current fixture variants.
  All three changed Swift files parse, the final raw 100ms loop in
  `FlowTabUITests+HomeAndLogs.swift` is absent, the existing oversized file
  shrank from 829 to 816 lines, all three wrapper status files report
  `stage=completed` with zero relevant build, fixture, signing, and test exit
  codes, and `git diff --check` passes. The fixed-path app remains current
  because this slice changes only UI-test code. Startup `prompts.zip` remains
  unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice extends a
  UI-test projection Oracle without changing application or core-domain
  runtime behavior.
- Commit: `6b63588a945da5c785cc153ea0ca4a102da3a807`
  (`test(sync): migrate SYNC-034AR Home window order`).

### SYNC-034AS Closure Record

- Design and Oracle: after the fixture publishes exact applied termination
  evidence, the quit-shortcut test starts XCTest's persistent observation for
  the exact fixture application to reach `.notRunning`. The wait accepts an
  already-stopped initial state and a later process exit, then the test requires
  a final `.notRunning` readback. Waiter completion and final state are both
  included in failure diagnostics.
- Lifecycle and retained time policy: the test invocation owns the process
  wait and its eight-second watchdog. The fixture's 2.4-second delay remains
  the named `SpaceFixtureTerminationFaultPolicy` fault-injection contract.
  Its owner publishes scheduled and applied evidence with a monotonic
  generation and owns cancellation. Scheduler load can extend delivery time;
  exact applied evidence and process state define progress and completion.
- Deterministic regression and Pressure: the shared process-state owner run
  passes 6/6 with zero failures in 1.305 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AS/process-state-owner-current-001`.
  Coverage includes an already-stopped initial state, immediate post-trigger
  completion, delayed `.notRunning` evidence, final readback at the wait
  boundary, repeated exact-target lifecycles, and watchdog final-state
  diagnostics.
- UI affected path: the signed delayed-termination real-fixture topology
  passes 1/1 with zero failures in 24.363 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AS/affected-delayed-termination-current-001`.
  The path verifies the exact fixture identity and scheduled/applied
  termination generation before observing its natural process exit and the
  corresponding FlowTab projection removal.
- Process/Tooling: the complete UI-test target build and Apple Development
  runner signing pass under
  `.build-local/evidence-driven-sync/SYNC-034AS/process-ui-build-current-001`.
  The affected wrapper rebuilt and signed all current fixture variants. The
  changed Swift file parses, the private raw 100ms process loop is absent, the
  existing oversized workflow file shrank to 783 lines, all three wrapper
  status files report `stage=completed` with zero relevant build, fixture,
  signing, and test exit codes, and `git diff --check` passes. The fixed-path
  app remains current because this slice changes only UI-test code. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice replaces
  one UI-test process Oracle with already validated XCTest process-state
  observation and does not change application or core-domain runtime behavior.
- Commit: `f62fa5288b309e07e1533135d175217d3f6bbc01`
  (`test(sync): migrate SYNC-034AS fixture process exit`).

### SYNC-034AT Closure Record

- Design and Oracle: direct Switcher selection writes the exact bundle-ID
  payload, establishes the shared diagnostics owner, then posts the selection
  notification. Acceptance requires the parsed `selected` value to equal the
  exact bundle ID. Window-layer callers establish the same owner before Down,
  gate acceptance until input delivery returns, perform an immediate
  post-trigger readback, and require the parsed `mode` value to have the
  semantic `windowCycle` prefix. Exact app-tile nonexistence remains the
  independent removal Oracle.
- Lifecycle and retained cadence: each helper invocation owns its generation,
  serial readback schedule, wait, cancellation, and cleanup. XCUI exposes no
  diagnostics-value publication callback, so the shared named 100ms cadence
  remains the conditional-observation policy. The caller watchdog is solely a
  failure bound with final identifier, existence, raw value, parsed values,
  source, generation, and waiter result.
- Deterministic regression and Pressure: the current-source diagnostics-owner
  run passes 9/9 with zero failures in 3.482 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AT/owner-tests-current-007`.
  Coverage includes matching initial evidence, percent decoding, semantic
  prefix matching, exact identity rejection for a longer bundle-ID collision,
  post-trigger gating, five delayed readbacks, atomic multi-key projection,
  100 cancel/restart generations with stale callbacks, and watchdog
  final-readback diagnostics.
- UI affected path: the signed real Space fixture workflow passes 1/1 with
  zero failures in 23.323 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AT/affected-workflow-current-001`.
  It observes exact selected-app evidence before entering the window layer,
  exact `windowCycle` evidence after Down, and all three real fixture window
  cards before process cleanup.
- Authentication incident: owner attempts 001–006 stopped before the first
  test body with LocalAuthentication code `-4`. Unified logs showed runner
  signing complete followed by the `"Enable UI Automation"` request failing
  to acquire authentication ownership while the desktop was locked. The
  failure summary and canonical reproduction command were captured before the
  3.2GB reproducible artifacts were deleted. After desktop unlock and
  authentication, attempts 007 and the affected-path run both completed.
- Process/Tooling: the fixed-path app was installed and verified through
  `install-ui-test-app.sh`. The successful owner wrapper rebuilt the complete
  current-source UI-test target and signed the runner with Apple Development;
  the affected wrapper rebuilt and signed all current fixture variants. Both
  status files report `stage=completed` with zero relevant build, fixture,
  signing, and test exit codes. All three changed Swift files parse,
  `waitForSwitcherSummary` and the scoped raw RunLoop advances are absent, the
  workflow file shrank from 783 to 772 lines, and `git diff --check` passes.
  Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice replaces
  UI-test orchestration with already validated shared observation
  infrastructure and preserves application and core-domain runtime behavior.
- Commit: `6ee6db40b4b1403668739d771293254021ed4be2`
  (`test(sync): migrate SYNC-034AT Switcher summary`).

### SYNC-034AU Closure Record

- Design and Oracle: Noisy Control+Tab readiness now reuses the shared
  Switcher app-projection owner. It registers the named observation cadence,
  performs an immediate initial readback, parses each `apps` entry into an
  exact bundle identifier plus optional window count, and resolves only when
  the target bundle identity is present. The already-delivered trigger race is
  closed by initial readback. Exact selected-bundle and complete preview-title
  assertions remain independent readiness Oracles.
- Lifecycle and retained cadence: each helper invocation owns its generation,
  serial readback schedule, wait, cancellation, and cleanup. XCUI exposes no
  app-projection publication callback, so the shared named 100ms cadence
  remains the conditional-observation policy. The eight-second watchdog is
  solely a failure bound with final identifier, existence, raw projection,
  parsed entries, source, and generation.
- Deterministic regression and Pressure: the current-source shared-owner run
  passes 7/7 with zero failures in 2.103 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AU/owner-tests-current-001`.
  Coverage includes entry parsing, matching initial evidence, delayed exact
  bundle evidence, rejection of a longer bundle-ID prefix collision, wrong
  exact-count rejection, 100 cancel/restart generations with stale callbacks,
  and watchdog final-entry diagnostics.
- UI affected path: the signed Noisy Control+Tab real fixture topology passes
  1/1 with zero failures in 90.847 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AU/affected-noisy-control-current-001`.
  It satisfies the exact app projection on initial presentation and three
  relaunches while round-tripping normal and fullscreen windows across Spaces
  with suppressed application AX windows and noisy CG siblings.
- Process/Tooling: the owner wrapper rebuilt the complete current-source UI
  target and signed the runner with Apple Development. The affected wrapper
  rebuilt and signed all fixture variants. Both status files report
  `stage=completed` with zero relevant build, fixture, signing, and test exit
  codes. Both changed Swift files parse; the private helper, raw RunLoop
  advance, and wall-clock deadline are absent; the in-app workflow file shrank
  from 770 to 750 lines; and `git diff --check` passes. The fixed-path app
  remains current because this slice changes only UI-test code. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice routes one
  UI-test readiness Oracle through already validated shared observation
  infrastructure and preserves application and core-domain runtime behavior.
- Commit: `f919e8152bea1ecaf6d4e52f7555d692cf59e11d`
  (`test(sync): migrate SYNC-034AU in-app readiness`).

### SYNC-034AV Closure Record

- Design and Oracle: prelaunch fixture cleanup now creates an exact target-PID
  set, registers `NSWorkspace.didTerminateApplicationNotification`, and
  performs an initial `NSRunningApplication.isTerminated` readback before
  issuing `forceTerminate`. Each matching notification triggers one fresh
  readback. Resolution requires the terminated-PID set to equal the complete
  target set; the later launch callback and exact desktop-anchor window remain
  independent workflow Oracles.
- Lifecycle and timeout: one generation-owned helper invocation owns the
  workspace token, expectation, state readbacks, cancellation, and cleanup.
  Notification completion removes the token. The five-second watchdog is
  solely a failure bound and reports exact target, terminated, and missing PID
  sets plus the last evidence source and waiter result. The former 50ms polling
  cadence has no retained owner.
- Deterministic regression and Pressure: the current-source process-owner run
  passes 4/4 with zero failures in 2.745 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AV/owner-tests-current-001`.
  Coverage includes an already-terminated initial set, duplicate and
  out-of-order partial notifications, cancellation and stale callback
  rejection across 100 generations, and watchdog missing-PID diagnostics.
- UI affected path: the signed Desktop Refocus real fixture path passes 1/1
  with zero failures in 6.493 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AV/affected-desktop-refocus-current-001`.
  It completes exact prelaunch process cleanup, the asynchronous
  `NSWorkspace.openApplication` callback, fullscreen transition, and exact
  non-fullscreen desktop-anchor window readback.
- Process/Tooling: the owner wrapper rebuilt the complete current-source UI
  target and signed the runner with Apple Development. The affected wrapper
  rebuilt and signed all fixture variants. Both status files report
  `stage=completed` with zero relevant build, fixture, signing, and test exit
  codes. The changed Swift file parses; its raw RunLoop advance, deadline, and
  polling policy are absent; the 413-line file retains one clear Desktop
  Refocus responsibility; and `git diff --check` passes. The fixed-path app
  remains current because this slice changes only UI-test code. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice replaces a
  UI-test fixture-cleanup loop with workspace process-lifecycle evidence and
  preserves application and core-domain runtime behavior.
- Commit: `1e27acbce9840ce3067ee53e9ac9548afb08ef61`
  (`test(sync): migrate SYNC-034AV fixture cleanup`).

### SYNC-035I Closure Record

- Design and Oracle: the provisional-hidden real-topology path freezes a
  runtime-log baseline before `.advanceDown`. The shared observer accepts only
  the post-`model.handle` state line for the exact action with
  `windows=1 mode=appCycle`; command completion and the subsequent
  accessibility readback then confirm inactive preview state and absence of
  the provisional CG-only title. Scheduler latency can delay the evidence but
  cannot satisfy the test with another mode or window count.
- Lifecycle and retained time policies: the invocation owns the log baseline,
  file-event registration, named fallback readback, cancellation, and cleanup.
  Its four-second watchdog is solely a failure bound with the last regex/log
  evidence. The fixture's named 30-second auto-entry preference remains a
  separate product duration and is unchanged. The former 500ms settle has no
  retained owner.
- Unit: the canonical FlowTabCore command passed the deterministic
  `testDownInAppCycleDoesNotEnterWindowLayerWhenSingleWindow` rule 1/1 with
  zero failures in 0.002 seconds under
  `.build-local/flowtabcore-tests`.
- Shared owner regression and Pressure: the current-source runtime-log
  observation run passed 5/5 with zero failures in 5.103 seconds under
  `.build-local/evidence-driven-sync/SYNC-035I/owner-current-001`. It covers
  regex matching, post-baseline file append, file replacement, event
  resolution, cancellation, stale callbacks, and 100 owner lifecycles.
- UI affected path: the signed provisional-hidden Space fixture path passed
  1/1 with zero failures in 15.427 seconds under
  `.build-local/evidence-driven-sync/SYNC-035I/affected-current-001`. It
  observed the exact one-window app-cycle evidence and kept the provisional
  CG-only window out of the visible preview topology.
- Process/Tooling: the current-source fixed-path app build and install passed
  under
  `.build-local/evidence-driven-sync/SYNC-035I/install-current-001`, signed
  with Apple Development Team `96PUA726W9`. The owner and affected wrapper
  status files report `stage=completed`, present result bundles, and zero
  relevant build, fixture, signing, and test exit codes. The changed
  298-line Swift file parses, the scoped 500ms RunLoop advance is absent, and
  `git diff --check` passes. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Behavior: not relevant because this slice changes a UI-test synchronization
  Oracle and preserves application orchestration behavior; the post-handle
  rule is covered by FlowTabCore and the real-topology UI path.
- Commit: `321569f0868a690347ba1fe215ccf113b1b88fee`
  (`test(sync): migrate SYNC-035I provisional hidden state`).

### SYNC-034AW Closure Record

- Design and Oracle: `selectEdgeWorkflowAppInSwitcherAppLayer` prepares the
  exact bundle-identity payload, then establishes the shared
  generation-owned diagnostics observer before posting `.selectApp`.
  Acceptance remains disabled through the command-completion receipt, and an
  immediate trigger readback closes synchronous delivery. Resolution requires
  exact equality of the atomic `selected` value; scheduler latency can only
  delay that evidence.
- Lifecycle and timeout: each helper invocation owns its observation
  generation, named serial XCUI fallback, command receipt, cancellation, and
  cleanup. The caller's ten-second watchdog is solely a failure bound and
  reports the expected predicate plus final atomic diagnostics projection.
  The private wall-clock deadline and raw 100ms RunLoop cadence have no
  retained owner.
- Deterministic regression and Pressure: the current-source shared diagnostics
  owner run passed 9/9 with zero failures in 1.977 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AW/owner-current-001`. Coverage
  includes initially satisfied evidence, exact bundle-identity rejection,
  post-trigger gating, scheduled slow readback, atomic multi-key state,
  cancellation, stale callbacks across 100 generations, and watchdog final
  evidence.
- UI affected paths: both signed real Edge fixture callers passed 2/2 with
  zero failures in 37.881 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AW/affected-current-001`. After
  exact Chrome fixture selection, duplicate-title windows retained distinct
  stable identities and minimized public AX state remained represented in the
  preview topology.
- Process/Tooling: the owner wrapper rebuilt the complete current-source UI
  target and signed the runner with Apple Development; the affected wrapper
  rebuilt and signed all fixture variants. Both status files report
  `stage=completed`, present result bundles, and zero relevant build, fixture,
  signing, and test exit codes. The 189-line changed Swift file parses, the
  scoped selection deadline and raw cadence are absent, and
  `git diff --check` passes. The fixed-path app remains current because this
  slice changes only UI-test code. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice routes one
  UI-test selection Oracle through already validated shared observation
  infrastructure and preserves application and core-domain runtime behavior.
- Commit: `bb6b16213c484783d1cf1a5baa5a6442ac8004f6`
  (`test(sync): migrate SYNC-034AW Edge app selection`).

### SYNC-034AX Closure Record

- Design and Oracle: both Edge preview callers now establish the shared
  window-card collection owner before sending Down Arrow. Matching evidence
  observed before the trigger returns is rejected, and an immediate
  post-trigger readback closes synchronous publication. Resolution requires
  one atomic snapshot with the exact card count, exact title multiset, and
  distinct stable window identifiers; scheduling latency can only delay that
  evidence.
- Lifecycle and timeout: each helper invocation owns its observation
  generation, named serial XCUI fallback, cancellation, and cleanup.
  XCUI exposes no collection-publication callback, so the shared cancellable
  cadence remains the platform fallback. The eight-second watchdog is solely
  a failure bound and reports the expected collection plus the last observed
  card snapshot. The private Edge wall-clock deadline and raw 100ms RunLoop
  cadence have no retained owner.
- Deterministic regression and Pressure: the current-source owner run passed
  2/2 with zero failures in 0.738 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AX/owner-current-001`. Coverage
  rejects matching pre-trigger evidence, prior identities, excluded titles,
  and duplicate identities; it verifies immediate trigger readback,
  cancellation, stale scheduled callbacks, watchdog final evidence, and 100
  owner generations under delayed scheduling.
- UI affected paths: both signed real Edge fixture callers passed 2/2 with
  zero failures in 42.334 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AX/affected-current-001`.
  Duplicate-title windows retained distinct stable identities, and minimized
  public AX state remained represented in the preview topology.
- Process/Tooling: the owner wrapper rebuilt the complete current-source UI
  target and signed the runner with Apple Development; the affected wrapper
  rebuilt and signed all fixture variants. Both status files report
  `stage=completed`, present result bundles, and zero relevant build, fixture,
  signing, and test exit codes. The changed Swift files contain 285, 286,
  437, and 142 lines and parse successfully; the scoped Edge card deadline,
  raw cadence, and duplicate collection helper are absent, and
  `git diff --check` passes. The fixed-path app remains current because this
  slice changes only UI-test code. Startup `prompts.zip` remains unchanged and
  outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice migrates
  one UI-test collection Oracle through already validated shared observation
  infrastructure and preserves application and core-domain runtime behavior.
- Commit: `4f954069d6b3bca58fbeb1025140b2cbf526bb9e`
  (`test(sync): migrate SYNC-034AX Edge window cards`).

### SYNC-034AY Closure Record

- Design and Oracle: both Edge Search callers now establish the shared
  Search-result observation owner before typing. The readback captures
  `searchResultsScope`, percent-decoded `searchResultsQuery`, and
  `searchResults` from one diagnostics value, then parses stable result and
  window identities. Matching evidence observed before the trigger returns is
  rejected. Resolution requires the exact committed `window` scope, exact
  typed query, exact filtered result count, and distinct accessibility
  identifiers in the same snapshot.
- Lifecycle and timeout: each helper invocation owns its observation
  generation, named serial XCUI fallback, cancellation, and cleanup.
  Diagnostics expose no publication callback, so the shared cancellable
  cadence remains the platform fallback. The eight-second watchdog is solely
  a failure bound and reports the expected query/identity contract plus the
  final atomic Search projection. The private Edge wall-clock deadline,
  raw 100ms RunLoop cadence, and hierarchy regex parser have no retained
  owner.
- Deterministic regression and Pressure: the current-source Search owner run
  passed 9/9 with zero failures in 2.104 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AY/owner-current-001`. Coverage
  includes percent-decoded exact identity parsing, initially satisfied and
  delayed evidence, pre-trigger rejection, stale query, wrong scope,
  duplicate identity, filtered count, immediate trigger readback,
  cancellation, watchdog final evidence, and stale callbacks across 100
  generations.
- UI affected paths: both signed real Edge fixture Search paths passed 2/2
  with zero failures in 80.897 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AY/affected-current-001`.
  Duplicate titles retained distinct Finder and Chrome result identities; the
  punctuation query returned the exact fixture window identifier, recovered
  its CG window number, and activated that real long-title window.
- Process/Tooling: the owner wrapper rebuilt the complete current-source UI
  target and signed the runner with Apple Development; the affected wrapper
  rebuilt and signed all fixture variants. Both status files report
  `stage=completed`, present result bundles, and zero relevant build, fixture,
  signing, and test exit codes. The changed Swift files contain 530, 523,
  454, 77, and 958 lines and parse successfully; the already oversized
  multi-app workflow was reduced by moving diagnostics parsing to the
  dedicated Search observation owner. The scoped Edge Search deadline, raw
  cadence, and debug-hierarchy parser are absent, and `git diff --check`
  passes. The fixed-path app remains current because this slice changes only
  UI-test code. Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice migrates
  one UI-test Search Oracle through existing testing diagnostics and preserves
  application and core-domain runtime behavior.
- Commit: `c53ef2a5a233eb76c8d9bd63c99c26743f03da39`
  (`test(sync): migrate SYNC-034AY Edge Search results`).

### SYNC-034AZ Closure Record

- Design and Oracle: the optional multi-app direct-selection path prepares the
  exact bundle payload, then establishes the shared generation-owned
  diagnostics observer before posting `.selectApp`. Matching evidence remains
  gated until notification delivery returns, and an immediate trigger
  readback closes synchronous publication. Resolution requires exact equality
  of the atomic `selected` bundle identifier; scheduling latency can only
  delay that evidence.
- Lifecycle and timeout: each invocation owns its observation generation,
  named serial XCUI fallback schedule, cancellation, and cleanup. The
  1.5-second watchdog solely selects the existing keyboard fallback and logs
  the expected identity plus final atomic diagnostics projection. The direct
  selection path has no private deadline or raw RunLoop cadence.
- Deterministic regression and Pressure: the current-source shared diagnostics
  owner run passed 9/9 with zero failures in 2.143 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AZ/owner-current-002`. Coverage
  includes initially satisfied evidence, percent-decoded and prefix matches,
  exact selected-app identity, post-trigger gating and immediate readback,
  delayed scheduling, atomic multi-key state, cancellation, watchdog final
  evidence, and stale callbacks across 100 generations.
- UI affected path: the signed real multi-app fixture path passed 1/1 with zero
  failures in 62.529 seconds under
  `.build-local/evidence-driven-sync/SYNC-034AZ/affected-current-002`. It
  traversed the exact Finder, Chrome, and Notes application identities,
  exercised evidence-bearing watchdog selection of the existing keyboard
  fallback, and verified each app's distinct exact window-card anchors.
- Process/Tooling: the owner wrapper rebuilt the complete current-source UI
  target and signed the runner with Apple Development; the affected wrapper
  rebuilt and signed all fixture variants. Both status files report
  `stage=completed`, present result bundles, and zero relevant build, fixture,
  signing, and test exit codes. The changed Swift files contain 930, 484, and
  456 lines and parse successfully. Moving the direct-selection owner into
  the dedicated diagnostics observation file reduced the oversized multi-app
  workflow below its prior 958-line size. Scoped search confirms the direct
  deadline and raw 80ms cadence are absent, and `git diff --check` passes.
  The fixed-path app remains signed by Team `96PUA726W9`; this slice changes
  only UI-test code. Startup `prompts.zip` remains unchanged and outside the
  slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice routes one
  UI-test selection Oracle through already validated shared observation
  infrastructure and preserves application and core-domain runtime behavior.
- Commit: `a756ca219a5cf8763fa26c0cdbdf1e417736f75a`
  (`test(sync): migrate SYNC-034AZ multi-app selection`).

### SYNC-034BA Closure Record

- Design and Oracle: the keyboard fallback now reads `selected` and `mode`
  atomically and captures that exact baseline before every physical Up or
  Right input. A generation-owned transition observer is installed first,
  acceptance remains gated until input synthesis returns, and an immediate
  trigger readback closes synchronous publication. Up resolves only when the
  same exact bundle leaves `windowCycle` for `appCycle`. Right resolves only
  when a supported atomic selection/mode state differs from the exact
  app-cycle baseline.
- Lifecycle and timeout: each physical key invocation owns one observation
  generation, the shared named serial XCUI fallback schedule, cancellation,
  and cleanup. The named three-second watchdog is solely a failure bound and
  reports the unmet transition, target bundle, attempt, and final atomic
  projection. The former 120ms and 80ms RunLoop advances have no retained
  owner.
- Deterministic regression and Pressure: the current-source transition-owner
  run passed 5/5 with zero failures in 1.385 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BA/owner-current-001`. Coverage
  includes exact atomic Up/Right rules, rejected wrong identity and mode,
  post-trigger gating, immediate and delayed readback, single cancellation,
  duplicate callback suppression, final watchdog evidence, and stale
  callbacks across 100 generations.
- UI affected path: the signed real multi-app fixture path passed 1/1 with zero
  failures in 86.289 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BA/affected-current-001`. It
  exercised repeated Right application transitions, exact Up exits from
  incidental window-cycle states, Finder/Chrome/Notes bundle selection, and
  each selected app's distinct exact window-card anchors.
- Process/Tooling: the owner wrapper rebuilt the complete current-source UI
  target and signed the runner with Apple Development; the affected wrapper
  rebuilt and signed all fixture variants. Both status files report
  `stage=completed`, present result bundles, and zero relevant build, fixture,
  signing, and test exit codes. The dedicated 383-line implementation and
  291-line test files keep the existing diagnostics source at 424 lines and
  reduce the oversized multi-app workflow from 930 to 892 lines. All changed
  Swift files parse, the project file passes `plutil -lint`, scoped search
  confirms the selection path contains no 120ms or 80ms fixed advance, and
  `git diff --check` passes. The fixed-path app remains signed by Team
  `96PUA726W9`; this slice changes only UI-test code. Startup `prompts.zip`
  remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice migrates a
  UI-test physical-input synchronization Oracle and preserves application and
  core-domain runtime behavior.
- Commit: `0c700c183c2abe91b09f8e3e9d46420053f67903`
  (`test(sync): migrate SYNC-034BA keyboard selection`).

### SYNC-034BB Closure Record

- Design and Oracle: the real-window-card anchor workflow now captures
  `selected` and `mode` atomically before leaving each application's preview.
  The baseline must identify the exact expected bundle in `windowCycle`. A
  generation-owned transition observer is installed before Up, acceptance
  remains gated until input synthesis returns, and an immediate trigger
  readback closes synchronous publication. Resolution requires the same exact
  bundle entering `appCycle`, so scheduling latency can only delay completion.
- Lifecycle and timeout: each preview-exit invocation owns one observation
  generation, the shared named serial XCUI fallback schedule, cancellation,
  and cleanup. XCUI diagnostics expose no publication callback, so the shared
  cancellable 100ms cadence remains the platform fallback. The named
  three-second watchdog is solely a failure bound and reports the expected
  bundle, unmet transition, and final atomic projection.
- Deterministic regression and Pressure: the current-source transition-owner
  run passed 6/6 with zero failures in 2.901 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BB/owner-current-002`. Coverage
  requires an exact selected-window-cycle baseline and includes exact atomic
  transition rules, post-trigger gating, immediate and delayed readback,
  cancellation, final watchdog evidence, and stale callbacks across 100
  generations.
- UI affected path: the signed real multi-app window-card path passed 1/1
  with zero failures in 91.004 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BB/affected-current-004`. Finder,
  Chrome, and Notes each exposed two distinct exact window-card identities,
  cycled the real cards, and completed the exact preview-exit transition
  before the next application selection.
- Slice-boundary evidence: two diagnostic runs of the sibling preview path
  stopped at initial presentation with the exact Home accessibility surface
  active before any preview-exit trigger. SYNC-034BC owns that initial
  presentation contract, and SYNC-034BD owns the caller's remaining 200ms
  preview-exit migration.
- Process/Tooling: the owner wrapper rebuilt the complete current-source UI
  target and signed the runner with Apple Development; the affected wrapper
  rebuilt and signed all fixture variants. Both completion status files
  report `stage=completed`, present result bundles, and zero relevant build,
  fixture, signing, and test exit codes. The dedicated implementation and
  regression files contain 113 and 368 lines; the multi-app workflow contains
  891 lines, one line below its pre-slice size. All changed Swift files parse,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  fixed-path app remains signed by Team `96PUA726W9`; this slice changes only
  UI-test code. Startup `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice migrates
  one UI-test physical-input synchronization Oracle through already validated
  shared observation infrastructure and preserves application and core-domain
  runtime behavior.
- Commit: `0dd8c4a8d03a3751f87348872e46a382931f7f82`
  (`test(sync): migrate SYNC-034BB preview exit`).

### SYNC-034BC Closure Record

- Design and Oracle: the multi-app preview path now launches with the
  Switcher-trigger listener and verbose DEBUG runtime evidence, then presents
  the global Switcher through an explicit post-launch command. The helper
  captures the runtime-log baseline and starts both receipt and completion
  observers before posting the Darwin notification. It requires the exact
  notification-name receipt and the exact
  `presented=1 syntheticModifierHeld=1` completion marker, followed by the
  persistent Finder fixture tile and diagnostics accessibility readbacks
  before selection begins.
- Lifecycle and timeout: the trigger helper invocation owns the runtime-log
  baseline, file-event observers, named 200ms fallback readback schedules,
  cancellation, and cleanup. The shared four-second receipt and twelve-second
  presentation watchdogs are failure bounds with final post-baseline log
  evidence. The eight-second persistent tile and diagnostics waits are
  failure bounds whose success remains the exact accessibility state.
- Deterministic regression and Pressure: the current-source trigger-delivery
  owner run passed 6/6 with zero failures in 3.375 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BC/owner-current-001`. Coverage
  includes file-event publication, initial completed projection, separated
  and out-of-order receipt/completion evidence, cancellation, final watchdog
  diagnostics, and stale callbacks across 100 generations.
- UI affected path: the signed real multi-app fixture path passed 1/1 with
  zero failures in 35.727 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BC/affected-current-001`. The
  explicit trigger completed after all three fixture applications were ready,
  then Finder, Chrome, and Notes each supplied an isolated two-window preview.
  This path had reproduced the Home-surface initial-presentation failure in
  two consecutive pre-slice diagnostic runs.
- Process/Tooling: the owner wrapper rebuilt the complete current-source UI
  target and signed the runner with Apple Development; the affected wrapper
  rebuilt and signed all fixture variants. Both status files report
  `stage=completed`, present result bundles, and zero relevant build, fixture,
  signing, and test exit codes. The changed workflow remains at its
  pre-slice 891-line size, parses successfully, and `git diff --check` passes.
  The fixed-path app remains signed by Team `96PUA726W9`. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice migrates
  one UI-test initial-presentation synchronization contract through existing
  validated TestingSupport evidence and preserves application and core-domain
  runtime behavior.
- Commit: `457ab9fbdaf55767626b84e4308f2ae8cab8bd7f`
  (`test(sync): migrate SYNC-034BC initial presentation`).

### SYNC-034BD Closure Record

- Design and Oracle: the remaining multi-app preview loop now uses the shared
  exact-bundle preview-exit transition contract after each Finder, Chrome, or
  Notes window preview. It atomically requires the selected application and
  `windowCycle(expectedBundle)` baseline, establishes the generation owner
  before Up, gates acceptance until input synthesis returns, requests an
  immediate readback, and resolves only when that exact bundle enters
  `appCycle`. Scheduling latency can therefore only delay resolution.
- Lifecycle and timeout: each call owns its observation generation,
  cancellation, and cleanup. XCUI diagnostics expose no publication callback,
  so the shared named cancellable 100ms readback cadence remains the platform
  fallback. The named three-second watchdog is solely a failure bound and
  reports the expected bundle, unmet transition, and final atomic projection.
- Deterministic regression and Pressure: the current-source transition-owner
  run passed 6/6 with zero failures in 1.416 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BD/owner-current-001`. Coverage
  includes exact baseline and transition rules, post-trigger gating, immediate
  and delayed readback, cancellation, final watchdog evidence, and stale
  callbacks across 100 generations.
- UI affected path: the signed real multi-app fixture path passed 1/1 with
  zero failures in 31.060 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BD/affected-current-001`. The
  explicit initial presentation completed first, then Finder, Chrome, and
  Notes each supplied an isolated two-window preview and exact preview-exit
  state before the next selection began.
- Process/Tooling: the owner wrapper rebuilt the complete current-source UI
  target and signed the runner with Apple Development; the affected wrapper
  reused that current-source build and rebuilt and signed all fixture
  variants. Both status files report `stage=completed`, present result
  bundles, and zero relevant build, fixture, signing, and test exit codes.
  The workflow now contains 890 lines and no 200ms preview-exit advance. All
  changed Swift files parse, `git diff --check` passes, and the fixed-path app
  remains signed by Team `96PUA726W9`. Startup `prompts.zip` remains unchanged
  and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice migrates
  one sibling UI-test physical-input synchronization contract through already
  validated shared observation infrastructure and preserves application and
  core-domain runtime behavior.
- Commit: `bfd54538131228e9b172277b85cfe6637d6abf51`
  (`test(sync): migrate SYNC-034BD preview exit`).

### SYNC-034BE Closure Record

- Design and Oracle: the two open-window-layer mutation paths now capture the
  exact selected-application `appCycle` baseline and establish the shared
  generation-owned transition observer before Down. Acceptance remains gated
  until input synthesis returns, an immediate trigger readback closes
  synchronous publication, and resolution requires the same bundle in the
  exact `windowCycle(expectedBundle)` mode. After the independent exact
  preview-title projection resolves each real window mutation, an immediate
  atomic selected/mode readback proves that the same preview remains active.
- Lifecycle and timeout: each preview-entry invocation owns one observation
  generation, the shared named serial XCUI readback schedule, cancellation,
  and cleanup. XCUI diagnostics expose no publication callback, so the
  cancellable 100ms cadence remains only as an observation opportunity. The
  named three-second watchdog is solely a failure bound and reports the exact
  expected transition plus final atomic projection. The post-mutation readback
  creates no timer, observer, or asynchronous owner.
- Process identity Oracle: the first affected run showed both preview
  contracts completing before a newly constructed bundle-only
  `XCUIApplication` reported each still-managed fixture as `.notRunning`.
  Each path now captures the exact fixture PID before the mutation and later
  requires that PID to retain its bundle identity and nonterminated process
  state through `NSRunningApplication`. This preserves the intended liveness
  assertion without depending on cross-Space XCUI attachment state.
- Deterministic regression and Pressure: the final current-source owner run
  passed 7/7 with zero failures in 2.156 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BE/owner-current-002`. Coverage
  includes exact preview-entry and preview-exit baselines, exact atomic
  transition rules, post-trigger gating, immediate and delayed readback,
  cancellation, final watchdog evidence, and stale callbacks across 100
  generations.
- UI affected paths: the standard Chrome window-set mutation passed 1/1 with
  zero failures in 40.516 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BE/affected-current-003`; the
  Notes fullscreen target-window mutation passed 1/1 with zero failures in
  42.018 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BE/affected-current-004`. Both
  paths proved exact preview entry, exact remaining-title projection, exact
  selected/mode retention, the runtime AX-destroyed record, and exact
  PID/bundle liveness.
- Diagnostic isolation: the combined current-source run first proved the
  fullscreen path while its following standard path stopped before the slice
  trigger because launch-time Switcher presentation did not expose the
  diagnostics summary. The isolated standard rerun then passed. This sibling
  initial-presentation transport remains outside this slice's preview-entry
  owner and is retained as evidence for the next semantic audit.
- Process/Tooling: the affected wrapper rebuilt the complete current-source UI
  target and every fixture variant. The final owner and both isolated affected
  status files report `stage=completed`, present result bundles, and zero
  relevant fixture, signing, and test exit codes. The changed Swift files
  contain 403, 252, 473, 875, and 692 lines; the already oversized workflow
  shrank from 890 to 875 lines and contains no `waitForSwitcherMode` helper.
  All changed Swift files parse and `git diff --check` passes. The fixed-path
  app remains signed by Team `96PUA726W9`. Startup `prompts.zip` remains
  unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes
  UI-test synchronization and process readback Oracles while preserving
  application and core-domain runtime behavior.
- Commit: `3b0180137fd4b242dae32da53f2bed3631eb60a1`
  (`test(sync): migrate SYNC-034BE preview entry`).

### SYNC-034BF Closure Record

- Design and Oracle: the application-search fixture now establishes the shared
  generation-owned frontmost-application observer before confirming the
  selected search result. Registration of
  `NSWorkspace.didActivateApplicationNotification` precedes an initial
  `frontmostApplication` readback, and resolution accepts only the exact
  target bundle identifier.
- Lifecycle and timeout: the search-confirmation invocation owns the
  notification token, generation, cancellation, and cleanup. The existing
  ten-second limit is solely a failure bound and reports the expected bundle
  plus final frontmost-bundle evidence. Activation publication timing can
  change completion latency while leaving the accepted identity unchanged.
- Deterministic regression and Pressure: the current-source owner run passed
  4/4 with zero failures in 1.553 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BF/owner-current-001`. Coverage
  includes observer-before-readback ordering, initial satisfaction, exact
  activation-notification evidence, cancellation and replacement across 500
  generations, duplicate event rejection, and final watchdog readback
  diagnostics.
- UI affected path: the signed real Space fixture application-search path
  passed 1/1 with zero failures in 33.142 seconds under
  `.build-local/evidence-driven-sync/SYNC-034BF/affected-current-001`. Search
  confirmation activated the exact Chrome-style fixture application and
  resolved from its frontmost bundle evidence.
- Process/Tooling: the fixed-path app was rebuilt from current source and
  installed through the canonical installer. The UI wrapper rebuilt the
  complete current-source test target, and the affected invocation rebuilt
  the current fixture variants. Both wrapper status files report
  `stage=completed`, present result bundles, and zero relevant fixture,
  signing, build, and test exit codes. The fixed-path app is signed by Team
  `96PUA726W9`. The workflow file shrank from 875 to 859 lines and contains no
  local `waitForFrontmostWorkflowApp`, raw RunLoop advance, or deadline loop.
  The changed Swift file parses and `git diff --check` passes. Startup
  `prompts.zip` remains unchanged and outside the slice.
- Unit, Behavior, and FlowTabCore: not relevant because this slice changes one
  UI-test orchestration path through already validated shared observation
  infrastructure while preserving application and core-domain runtime
  behavior.
- Commit: `476e431013e5a39f7bee6bb8efcc51b3312f8748`
  (`test(sync): migrate SYNC-034BF search activation`).

### SYNC-035J Closure Record

- Design and Oracle: `HomeInitialProjectionObservationOwner` publishes one
  typed application notification only after an accepted, projection-backed
  `onEvidence` callback has returned. A complete projection first closes the
  initial observation and its handoff; an incomplete projection remains under
  the same Home-owned generation. TestingSupport binds the exact runtime
  service before Home construction and converts each application into an
  atomic JSON readback containing observation/readback generations, source,
  transition, request reason, completeness, all source generations, and the
  ordered app ID/window-count projection. The distributed notification only
  wakes the reader. The UI success Oracle is the persisted later
  `appSwitcherProjectionNotification` with
  `sourceGenerationAdvanced`, incomplete window coverage, Mail before Browser,
  and both exact window counts equal to zero. Exact accessibility values,
  row order, and frame positions are read before and after that evidence.
- Lifecycle and retained time policy: the UI installs its distributed
  observer before launching the app and performs an immediate readback before
  scheduling work. A named main-RunLoop condition scheduler reads the atomic
  file every 100ms when the cross-process wakeup is unavailable; the JSON
  condition alone establishes success. The bootstrap owns the in-process
  forwarder through app termination. The test invocation owns its observer,
  scheduler, and exact temporary file through teardown. The eight-second
  watchdog is solely the terminal failure bound and reports the final
  path/file/decode/evidence state.
- Unit, Behavior, and Pressure: ten deterministic initial-readback,
  synchronous-maintenance, request-return, exact application forwarding,
  atomic JSON, monotonic generation/completeness, cancellation, Home
  visibility/handoff, route validation, and 2,000-restart lifecycle tests
  passed 10/10 with zero failures in 0.075 seconds under
  `.build-local/evidence-driven-sync/SYNC-035J/behavior-current-007`.
- UI affected path: the fixed signed app's no-Accessibility mock-runtime Home
  path passed 1/1 with zero failures in 9.581 seconds under
  `.build-local/evidence-driven-sync/SYNC-035J/affected-current-006`. The test
  retained the initial zero-count/runtime-order readback, resolved the later
  maintenance application from exact persisted evidence, and matched the
  final values and positions to the initial state.
- Process/Tooling: the app was rebuilt and installed through the canonical
  installer under
  `.build-local/evidence-driven-sync/SYNC-035J/ui-install-current-005`; its
  fixed-path signature reports Team `96PUA726W9`. All changed Swift files
  parse, the project file passes `plutil -lint`, and `git diff --check` passes.
  The new TestingSupport and UI files contain 314 and 381 lines, the existing
  Home/Logs UI file contains 776 lines, and the Home test contains no raw
  1.2-second RunLoop advance. Startup `prompts.zip` and the three pre-existing
  local Skill/reference edits remain unchanged and outside the slice.
- FlowTabCore: not relevant because this contract is owned by the app Home
  projection callback, TestingSupport transport, and UI-test readback boundary.
- Commit: `78d9efaf2e044c2ffe62ea67cd95c65165af71af`
  (`refactor(sync): migrate SYNC-035J home initial projection`).

### SYNC-035K Closure Record

- Design and Oracle: each `SwitcherPointerSelectionGate.reset` starts a
  monotonic generation and clears the preceding generation's exact target
  set. While the gate is unarmed, the first application, window, or Search
  result callback for an exact target returns blocked evidence; duplicates
  return a nonpublishing decision. The controller records one structured
  marker containing target kind, percent-escaped target identity, exact
  application identity where applicable, preserved selection, and generation.
  Reaching the existing movement threshold remains the explicit transition to
  the allowed state.
- Lifecycle and retained time policy: each UI path captures a runtime-log
  baseline and starts the file observer before launching the validation app or
  posting the Search trigger. The shared owner performs an immediate readback,
  observes post-baseline file events, and retains its named cancellable 200ms
  readback cadence for file-event gaps. Success requires the exact blocked
  marker and an independent diagnostics readback of the unchanged selected
  identity; the target frame separately proves that the pointer remained over
  the intended element. Each test invocation owns cancellation and process
  cleanup. The named five-second gate watchdog is solely a failure bound and
  reports missing markers, generations, and the final post-baseline log tail.
- Unit, Behavior, and deterministic Pressure: the final current-source run
  passed 8/8 with zero failures in 0.186 seconds under
  `.build-local/evidence-driven-sync/SYNC-035K/behavior-current-002`. Five
  rule tests cover initial blocking, exact generation, per-target duplicate
  suppression, reset and movement transitions, stable percent-escaped
  diagnostics, and 2,000 generations with eight duplicate callbacks per
  generation. Three controller tests preserve application, exact-window, and
  Search-result orchestration before and after pointer movement.
- Observer lifecycle: the shared current-source runtime-log owner run passed
  3/3 with zero failures in 0.715 seconds under
  `.build-local/evidence-driven-sync/SYNC-035K/owner-current-001`. It covers a
  real post-baseline file event, initial and event readback, cancellation on
  resolution, callbacks after cancellation, 100-generation lifecycle
  pressure, and watchdog diagnostics containing the final evidence.
- UI affected paths: the final current-source stationary-pointer run passed
  3/3 with zero failures in 43.785 seconds under
  `.build-local/evidence-driven-sync/SYNC-035K/affected-current-002`.
  Option+Tab application, Control+Tab window, and Search-result presentation
  each resolved from its exact gate marker and retained the independently read
  selected identity. The corresponding moved-pointer paths passed 3/3 with
  zero failures in 29.141 seconds under
  `.build-local/evidence-driven-sync/SYNC-035K/affected-moving-current-002`,
  confirming that application, window, and Search selection still proceeds
  after real pointer movement.
- Process/Tooling: the fixed application was rebuilt and installed through the
  canonical installer under
  `.build-local/evidence-driven-sync/SYNC-035K/ui-install-current-002` with
  Apple Development identity `gobestsoft@qq.com (RF9WCUVKH8)`; the installed
  app reports Team `96PUA726W9`, and all three UI invocations completed with
  signed runners and present result bundles. All changed Swift files parse,
  the project file passes `plutil -lint`, its four new object identifiers have
  the expected reference counts, and `git diff --check` passes. The extracted
  test files contain 169 and 351 lines; the existing Search unit and combined
  pointer UI files shrink to 1,809 and 606 lines. The three stationary-pointer
  paths contain no one-second assertion window or raw RunLoop advance.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside the slice.
- FlowTabCore: not relevant because the gate, controller, runtime-log
  transport, and affected tests remain inside the application and UI-test
  boundaries.
- Commit: `f430fc67ccd49378c13fcf6c4c8a4225b46b6fe1`
  (`refactor(sync): migrate SYNC-035K stationary pointer gate`).

### SYNC-038A Closure Record

- Design and Oracle: `release-install.sh` now delegates its process boundary
  to a release-owned observation helper. The first readback runs immediately
  after AppleScript quit and exact-name `pkill`. `pgrep -x` supplies the exact
  process-name set, and `ps` enriches each PID with parent PID, state, start
  identity, and command. A PID that exits between those reads remains pending
  as `identity=unavailable` until the next observation, while a process-list
  readback error remains explicit unmet evidence. Success is established only
  by an empty exact-name readback.
- Lifecycle and retained time policy: the release command synchronously owns
  the observation loop, so shell interruption terminates the current sleep
  and retains no background observer or timer. The condition is checked before
  the clock or any polling work. A named 100ms cadence is retained only while
  process records remain. The ten-second watchdog is solely a terminal failure
  bound and reports `processAbsent`, the exact process name, and the final
  PID/state/start/command evidence. A second invocation immediately before
  `rm -rf` closes a process relaunch gap during the Release build.
- Deterministic and integration regression: the release observation contract
  test covers initial satisfaction with zero polls, later satisfaction after
  two named polling opportunities, terminal watchdog diagnostics, a real
  exact-name absence readback, the two required caller boundaries, mutation
  ordering, and removal of the one-second sleep. The first sandboxed real
  readback returned `sysmond service not found`; the helper preserved that as
  `readbackError` instead of accepting absence. The same test then passed at
  the release script's Terminal process-list boundary.
- Process/Tooling: `bash -n` accepts the installer, observation helper, and
  contract test; the contract test passes; `release-install.sh --help` exits
  successfully without mutation; and `git diff --check` passes. The full
  installer is intentionally outside this slice's non-destructive validation
  because it resets TCC records and replaces `/Applications/Flow Tab.app`.
  Unit, Behavior, UI, and Pressure are not relevant because this slice changes
  only the release command's bounded process observation and does not alter
  application runtime code, a user journey, or a sustained hot path. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside the slice.
- Commit: `1c0d3d65f7bda18ce7cba71b44a655e3c4ec9f5d`
  (`refactor(sync): migrate SYNC-038A release process exit`).

### SYNC-038B Closure Record

- Design and Oracle: the DMG uninstaller now creates its
  `NSWorkspaceDidTerminateApplicationNotification` observer before requesting
  FlowTab exit. It captures an initial exact bundle-ID readback, records only
  target termination notifications as PID/launch-date/executable identities,
  deduplicates repeated identities into a monotonic generation, and stops and
  wakes the owning RunLoop when that generation advances. Completion still
  requires a fresh `NSRunningApplication` readback with no active target
  records. A readback error remains unmet evidence.
- Lifecycle and mutation boundary: the applet invocation owns the observer
  from pre-trigger setup through user-data cleanup and administrator
  authentication, and removes it in `finally` on success, failure, or
  cancellation. The privileged command starts with an exact-name `pgrep`
  readback immediately before installed-bundle removal. A present process
  aborts with PID/PPID/state/start/command evidence; a process-list failure
  aborts with the status and output. The ten-second monotonic watchdog is only
  a terminal failure bound and reports `applicationAbsent`, the exact bundle
  ID, the final application records, and the last notification. Normal
  completion has no polling cadence or fixed RunLoop advance.
- Deterministic regression: the Node contract covers initial satisfaction
  without a clock or wait, target-event completion, slow scheduling that
  overshoots the nominal deadline before a satisfied readback, cancellation,
  duplicate and unrelated notifications, PID reuse with a distinct launch
  identity, readback errors, watchdog evidence, pre-trigger observer ordering,
  and cleanup on both success and failure. It also independently reads a
  present Finder PID and an absent unique process name to verify the
  privileged process guard and its mutation order.
- JXA integration: the process test compiles the production uninstaller and a
  temporary ad-hoc-signed applet, launches that fixture through
  `NSWorkspace`, requires the initial readback to contain its exact PID,
  establishes the production observer before force termination, and requires
  one exact termination generation plus final absence. The event wakes the
  wait owner inside the named five-second integration watchdog; the final run
  completed in under one second. The fixture process and temporary bundles
  are removed by the test owner.
- Process/Tooling: Node syntax checks accept the production and Node test
  files; the existing uninstaller path-boundary cleanup test passes; the new
  process-exit test passes at the Terminal Workspace/process-list boundary;
  production `osacompile` validation is part of that test; and
  `git diff --check` passes. Running the destructive interactive uninstaller
  is outside this slice because it removes `/Applications/Flow Tab.app`,
  resets TCC records, and clears user data. Unit, Behavior, UI, and Pressure
  are not relevant because this slice changes only the DMG release tool's
  bounded process observation and does not change application runtime code, a
  user journey inside FlowTab, or a sustained hot path. Startup `prompts.zip`
  and the three pre-existing local Skill/reference edits remain unchanged and
  outside the slice.
- Commit: `b3122466393409d71aca76e73a1af9743ddec98e`
  (`refactor(sync): migrate SYNC-038B uninstaller process exit`).

### SYNC-037A Closure Record

- Design and Oracle: the tab-switch pressure runner now captures its launched
  child PID and `lstart` identity. The perf-owned observation helper reads
  PID, PPID, state, start time, and command immediately. Exact absence or
  zombie state resolves observation; the parent shell's subsequent
  `wait PID` remains the exit-status Oracle. An identity change is a terminal
  safety failure, and `ps` or monotonic-clock errors remain unmet evidence.
- Lifecycle and retained time policy: sampling duration, switch cadence, and
  sample cadence remain caller-selected pressure protocol inputs. The
  synchronous signal/exit trap owns TERM, conditional observation, optional
  KILL escalation, `wait`, status persistence, and interruption cleanup. Its
  named 100ms cadence exists only while the child readback remains active and
  is cancelled by shell interruption. The named 2,000ms monotonic watchdog
  decides only when to escalate TERM to KILL; it performs a final readback and
  reports `processExited`, expected start identity, final condition, and final
  process record.
- Deterministic and process regression: the helper contract covers initial
  absence without clock or poll work, delayed absence at the exact injected
  cadence, a satisfied readback after scheduling overshoots the nominal
  deadline, final-readback watchdog diagnostics, readback errors, immediate
  identity-change failure, and poll cancellation. A real child installs its
  TERM handler, publishes readiness through a named pipe before the trigger,
  exits on TERM, and is reaped by the test owner. The static caller contract
  verifies the tab-switch runner sources the owner, calls the evidence wait,
  and contains no attempt-count or unnamed 100ms cleanup loop.
- Pressure: a real 2s/20ms/0.2s run completed with app exit status 0,
  sampling status `completed`, and 55 preserved samples. A second 30s run was
  interrupted during active sampling. Its atomic status recorded
  `stage=interrupted`, final exit code 130, app exit code 143, and 94 samples;
  the exact child PID 91567 from `app.log` then produced an empty `ps`
  readback.
- Process/Tooling: `bash -n` accepts the runner, helper, and contract test;
  the contract test passes at the Terminal process boundary;
  `tab-switch-stress.sh --help` exits without mutation; and
  `git diff --check` passes. Unit, Behavior, and UI are not relevant because
  this slice changes only the pressure command's child-process lifecycle.
  Pressure is covered by both natural completion and signal interruption.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside the slice.
- Commit: `eb1bbc1921670bbdec8d84ef2ab4845f61a76eb1`
  (`refactor(sync): migrate SYNC-037A tab stress process exit`).

### SYNC-037B Closure Record

- Design and Oracle: the Search pressure runner captures the background test
  shell identity immediately after launch, then captures and deduplicates the
  current descendant tree as exact PID/`lstart` pairs before sending TERM.
  Every signal is preceded by a fresh PID/PPID/state/start/command readback and
  is delivered only while that same identity is active. Collective completion
  requires every captured identity to be absent, a zombie awaiting its owner,
  or replaced by a different identity; the owning shell then performs
  `wait PID` as the independent exit-status Oracle. A failed identity capture,
  `ps` readback, or monotonic-clock read remains unmet evidence.
- Lifecycle and retained time policy: the synchronous signal/exit trap owns
  identity capture, exact-record signalling, observation, optional escalation,
  reaping, status persistence, cancellation, and cleanup. The named 100ms
  cadence runs only while at least one exact record remains active. The named
  2,000ms TERM grace controls KILL escalation, and the separate 2,000ms KILL
  confirmation watchdog bounds terminal failure; neither establishes success.
  Both perform a final readback and report `processTreeExited`, the final
  condition, and every still-active exact record. The caller-selected 30-second
  scenario duration and 0.5-second sample cadence remain the explicit Search
  pressure protocol; scheduler load may extend active sampling to finish a
  batch but cannot change its completion Oracle.
- Deterministic regression: the shared contract covers immediate single-record
  and tree satisfaction without clock or polling work, deduplication, recovery
  of a start identity from an immediate full readback, signalling only a
  matching active identity, delayed completion, completion after an injected
  scheduler overshoots the nominal deadline, cancellation, final-readback
  watchdog diagnostics, readback errors, and exact identity replacement. A
  real child publishes readiness through a named pipe, handles TERM, exits, and
  is reaped by the test owner. The static caller contract requires the Search
  runner to source the shared owner, capture the root identity, use the
  collective evidence wait, and contain no attempt-count or unnamed 100ms
  cleanup loop.
- Pressure: the completed reproduction
  `./scripts/perf/search-committed-index-pressure.sh 0.5 --scenario realistic
  --scenario-duration-seconds 30 --build-root
  ./.build-local/evidence-driven-sync/SYNC-037B/build --output-dir
  ./.build-local/evidence-driven-sync/SYNC-037B/normal-current-001` recorded
  `stage=completed`, final/test-process exit code 0, four successful batches,
  71 samples, 70 valid samples against a minimum of 25, 45.047 active seconds,
  zero cadence gaps, and `termination_timed_out=false`. The exact six PIDs in
  its final sample subsequently produced an empty `ps` readback.
- Interrupted Pressure: the same command with output leaf
  `interrupted-current-003` was interrupted during the second active test
  batch. Its atomic status recorded `stage=interrupted`, final exit code 130,
  test-process exit code 143, one completed batch, 26 samples,
  `sampling_failed=false`, and `termination_timed_out=false`. Exact final
  sample PIDs 99997, 1677, 1678, 1686, 1687, and 1758 subsequently produced an
  empty `ps` readback, and no command containing the slice root remained.
- Process/Tooling: `bash -n` accepts the runner, shared helper, and contract
  test; the contract test passes at the Terminal process boundary;
  `search-committed-index-pressure.sh --help` exits without mutation; and
  `git diff --check` passes. The pressure runner invokes FlowTabTests only
  through `run-flowtabtests-local.sh`; its build-for-testing and every
  test-without-building batch used isolated slice roots. Unit, Behavior, and UI
  are not relevant because this slice changes only pressure-tool process
  ownership; the embedded committed-index FlowTabTests plus completed and
  interrupted external runs supply Process/Tooling and Pressure evidence.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside the slice. Rebuildable `.xcresult`, DerivedData,
  caches, and logs are confined to the recorded `SYNC-037B` root for exact
  post-commit cleanup.
- Commit: `d30d4a3d340767ee1adb0204d32ede6d731d7beb`
  (`refactor(sync): migrate SYNC-037B search pressure process exit`).

### SYNC-037C Closure Record

- Design and Oracle: the runtime-topology pressure runner captures its
  background UI-wrapper shell identity immediately after launch, then captures
  and deduplicates the current wrapper/xcodebuild descendant tree as exact
  PID/`lstart` pairs before signalling. Every TERM or KILL is preceded by a
  fresh PID/PPID/state/start/command readback and reaches only the same active
  identity. Collective completion requires every captured identity to be
  absent, a zombie awaiting its owner, or replaced by a different identity;
  the pressure runner then performs `wait PID` as the independent exit-status
  Oracle. Identity-capture, `ps`, and monotonic-clock errors remain unmet
  evidence.
- Lifecycle and retained time policy: the synchronous signal/exit trap owns
  exact identity capture, signalling, observation, escalation, reaping, status
  persistence, cancellation, and cleanup. Its named 100ms cadence exists only
  while one of the captured records remains active. The named 2,000ms TERM
  grace controls KILL escalation, and the separate 2,000ms KILL-confirmation
  watchdog bounds terminal failure; both perform a final readback and report
  `processTreeExited` plus every active record. Caller-selected sampling
  cadence remains a pressure protocol input and never establishes process
  completion.
- Deterministic regression: the shared helper contract covers immediate
  single-record and collective satisfaction without clock or polling work,
  identity deduplication and readback recovery, exact-record-only signalling,
  delayed completion, completion after injected scheduler overshoot,
  cancellation, final-readback watchdog diagnostics, readback errors, and PID
  identity replacement. A real child publishes readiness, handles TERM,
  exits, and is reaped. The runtime-topology static caller contract requires
  the runner to source the shared owner, capture the wrapper root identity,
  call the collective evidence wait, and contain no attempt counter or raw
  100ms cleanup sleep.
- Interrupted UI Pressure: a signed default-topology run in
  `interrupted-current-001` was interrupted during active sampling. Its atomic
  status recorded `stage=interrupted`, final exit code 130, UI wrapper/process
  exit code 1, log exit code 0, 12 samples, a matched target identity, and
  `termination_timed_out=false`. The incomplete result-bundle directory was
  retained as interruption evidence. Final process-table readback found no
  runtime-topology runner or xcodebuild descendant. Exact FlowTab PID 13943 and
  Chrome fixture PID 14759 had already been reparented to launchd and survived
  outside that tree; both were terminated by exact PID and then read back as
  absent. Their missing independent lifecycle owner is recorded separately as
  SYNC-037D.
- Completed UI Pressure: the same canonical runner executed the signed
  `testEnglishGrantedPermissionActionsUseManageLabels` UI path for 13.595
  seconds in `normal-owner-current-002`. The top-level status recorded
  `stage=completed`, final/wrapper/log/process/summary exit code 0, a valid
  `.xcresult`, 11 samples and identity checks, `identity_verdict=matched`, and
  `termination_timed_out=false`. The launch receipt recorded 20 observations,
  5,065.127ms of stable identity, no rejected transient identity, and the
  frozen manifest hash
  `ef50b1a68b0e89c42d6910664b9e456516b4c3f23674ebf6cd4231724e07fa6b`.
  Final exact PID and command-pattern readback found no target, UI runner,
  xcodebuild, fixture, or pressure-runner process.
- Open validation evidence: two signed default real-topology attempts produced
  valid result bundles, matched target identity throughout 40 and 36 samples,
  completed process cleanup without a termination watchdog, and both stopped
  at the same independent runtime-log watchdog because the expected
  `verifiedFocusReadback` binding-confidence marker was absent. A second
  log-dependent representative path stopped at its switcher-trigger receipt
  watchdog with an empty observed log projection. These reproducible business
  evidence failures remain open for diagnosis before the overall migration
  goal can close; they do not alter the completed runner-tree exit Oracle.
- Process/Tooling: the fixed UI app was installed through
  `install-ui-test-app.sh` and signed as
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` for TeamIdentifier
  `96PUA726W9`; its executable and designated-requirement hashes were frozen in
  the private manifest. `bash -n` accepts the runtime-topology runner, target
  helper, shared process owner, and contract test; the deterministic contract
  passes at the Terminal process boundary; the runner help path exits without
  mutation; and `git diff --check` passes. Unit and Behavior are not relevant
  because this slice changes only pressure-tool process ownership. UI and
  Pressure are covered by signed completed and interrupted canonical-runner
  paths. Startup `prompts.zip` and the three pre-existing local Skill/reference
  edits remain unchanged and outside the slice. Rebuildable `.xcresult`,
  DerivedData, fixture variants, caches, and logs are confined to the recorded
  `SYNC-037C` root for exact post-commit cleanup.
- Commit: `dc4b9d55304716b12ee16af8890b8b27443b3ec7`
  (`refactor(sync): migrate SYNC-037C topology pressure process exit`).

### SYNC-037D Closure Record

- Design and Oracle: before the UI wrapper can launch an application, the
  pressure owner records every running target and configured fixture as an
  exact bundle identifier, PID, launch date, symlink-resolved bundle path, and
  symlink-resolved executable path. Cleanup resolves the frozen target App and
  generated fixture workflows at their owning resource boundaries, then
  selects only expected path identities absent from that baseline. A
  `NSWorkspaceDidTerminateApplicationNotification` observer is registered
  before the first termination request. Its initial readback closes the
  registration race, notification generation wakes subsequent observation,
  and an exact final `NSRunningApplication` readback is the success Oracle.
  Duplicate, unrelated, baseline, and post-cancellation notifications cannot
  advance the generation.
- Lifecycle and retained time policy: the runtime-topology invocation's
  synchronous signal/exit trap owns baseline capture, graceful and force
  requests, observation, atomic evidence persistence, cancellation, and final
  status. The observer is removed in `finally` on success and failure. The
  named two-second graceful-termination window only decides whether to request
  exact force termination; the separate named two-second force watchdog is a
  terminal failure bound. Both begin with immediate readback and perform final
  readback after scheduler overshoot, so scheduling pressure can extend elapsed
  work without establishing the result. Failure reports
  `postRequestApplicationsAbsent`, every remaining exact identity, the last
  notification, and any readback error.
- Deterministic regression: the Node/JXA contract covers baseline exclusion,
  exact path selection, record deduplication, initial absence without clock or
  wait work, event-driven final readback, duplicate/unrelated/out-of-order
  notifications, cancellation, readback failure, watchdog diagnostics, and a
  scheduler jump beyond the deadline followed by successful evidence. The
  workflow helper deterministically validates, deduplicates, and sorts bundle
  identifiers, while the static caller contract proves baseline capture
  precedes the UI trigger and observer registration precedes initial readback.
  A real signed same-path, same-bundle two-instance application fixture uses a
  named ten-second quit-handler delay to exercise the graceful watchdog and
  force path; exact post-request absence is observed while the pre-request
  identity remains active, and final test cleanup removes that baseline
  fixture.
- Interrupted UI Pressure: an initial signed attempt in
  `interrupted-current-001` reached the independent cleanup owner before the
  target appeared; its empty initial/final readback and cancelled observer
  cover the early-failure path while its launch-watchdog result remains input
  to SYNC-037F. A cached signed run in `interrupted-current-002` was interrupted
  during active default-topology sampling. Atomic status recorded
  `stage=interrupted`, final exit code 130, UI wrapper/process exit code 1, log
  exit code 0, 20 samples and identity checks, a matched target receipt, both
  application phases at exit code 0, and no cleanup failure. Initial cleanup
  readback found exact FlowTab PID 37001 and Chrome fixture PID 37795; both
  accepted graceful termination, one workspace generation arrived, and final
  readback was empty with the observer cancelled. The incomplete result-bundle
  directory remains interruption evidence. Final executable-path process
  readback found no target, fixture, UI runner, xcodebuild, or pressure runner.
- Completed UI Pressure: the same canonical owner ran the signed
  `testEnglishGrantedPermissionActionsUseManageLabels` path in
  `normal-owner-current-001`; the UI test passed in 12.342 seconds. Top-level
  status recorded `stage=completed`, every final/wrapper/log/process/summary
  exit code as 0, a valid `.xcresult`, nine samples and identity checks,
  `identity_verdict=matched`, target terminal exit readback, and both
  application phases at exit code 0. XCTest had already removed the target, so
  independent cleanup observed empty initial/final sets without clock-bound
  work and cancelled its observer. Final executable-path process readback was
  empty.
- Process/Tooling: the fixed UI App was installed through the canonical script
  and signed as `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` for
  TeamIdentifier `96PUA726W9`. Its executable hash
  `d61d33a05a8a2050bec88467c1250ccca09686c7c5b737c55887c899d0154094`,
  designated-requirement hash
  `d68a18f9e330df6c06ec7eeb6cf7830243ed67ce7049457bf1f97f9e109aff9f`,
  and manifest hash
  `8e102565c225762ba40e9718b16552a22dd63b8d5febdd106f40cce80817d25c`
  were frozen for both runs. Shell and JavaScript syntax checks, Python source
  compilation, deterministic/real lifecycle integration, the shared process
  owner regression, runner help, and `git diff --check` pass. Unit and Behavior
  are not relevant because the slice changes only pressure-tool application
  ownership. UI and Pressure are covered by the signed completed and
  interrupted canonical-runner paths. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside the
  slice. Rebuildable `.xcresult`, DerivedData, fixture variants, caches, and
  logs are confined to the recorded `SYNC-037D` root for exact post-commit
  cleanup.
- Commit: `0634b5203e20b1e3a3e17ae74e595b57514a8165`
  (`refactor(sync): migrate SYNC-037D topology application cleanup`).

### SYNC-037D1 Closure Record

- Discovery and classification: after SYNC-037D's deterministic application
  integration had reported final `NSRunningApplication` absence, exact process
  inspection found PIDs 28825, 28839, 29005, 29008, 29094, and 29097 still
  active with PPID 1 from three prior `flowtab-runtime-app-exit-*` fixtures.
  Their PID/start/path identities were recorded, each process was removed by
  exact PID, and final `ps` readback was empty. This established that
  LaunchServices absence is application-lifecycle evidence while kernel
  process exit requires a separate Oracle.
- Design and Oracle: every initial post-baseline application record now
  captures its `/bin/ps` start identity before any termination request. An app
  that exits during LaunchServices enumeration is excluded from the active
  set, and termination notifications use their immutable application metadata
  so a notification arriving after process exit remains a valid wakeup. After
  the LaunchServices phase reaches exact absence, the new process-cleanup owner
  immediately reads every captured PID/start pair. Exact absence, zombie
  state, or a changed start identity proves that record inactive; a `ps`
  failure while `kill -0` still finds the PID remains unmet evidence. Exact
  KILL is issued only after a fresh identity readback, and success requires a
  final collective readback. The atomic application evidence now contains
  initial/final process observations, exact force requests, readback errors,
  `processVerdict`, and cancellation state.
- Lifecycle and retained time policy: the runtime-topology trap owns ordered
  LaunchServices cleanup, OS-process cleanup, final status persistence, and
  cancellation. Process observation first checks synchronously and performs no
  clock or wait work when already satisfied. A named 50ms RunLoop cadence is
  retained only while an exact record remains active because macOS supplies no
  callback for the post-LaunchServices kernel boundary. The named two-second
  monotonic watchdog is a terminal failure bound and reports
  `postRequestApplicationProcessesAbsent` plus final
  PID/PPID/state/start/command evidence. Scheduler overshoot followed by a
  satisfied readback succeeds; once the watchdog observes its unmet terminal
  bound, a later readback cannot replace that failure. The analogous
  LaunchServices force-watchdog rule is now enforced as well.
- Deterministic and real regression: the contract covers initial absence
  without clock/wait work, absent and zombie records, PID reuse, readback
  failure, slow scheduling followed by satisfied evidence, cancellation,
  watchdog diagnostics, and preservation of watchdog failure across a later
  absent readback. A real Perl child ignores TERM, publishes readiness through
  a named bounded condition check, and proves fresh-identity KILL plus final OS
  absence. The signed same-path/two-instance application fixture proves exact
  post-request cleanup while preserving the pre-request baseline, then proves
  final baseline cleanup. Static caller checks require LaunchServices cleanup
  before process cleanup and initial process readback before KILL. The final
  regression command passed with no application/process fixture residue.
- Signed UI and Pressure: the macOS `Enable UI Automation` authentication was
  completed after system logs identified the LocalAuthentication request. The
  canonical minimal smoke path then passed in 15.658 seconds. The final
  interrupted run `interrupted-current-004` was stopped during active default
  topology sampling and recorded `stage=interrupted`, final exit 130, 20
  samples and matched identity checks, a target launch receipt, no sampling or
  termination-watchdog failure, and both cleanup phases at exit code 0. Initial
  application evidence captured FlowTab PID 83762 with start identity
  `Fri Jul 31 23:12:09 2026` and Chrome fixture PID 84522 with start identity
  `Fri Jul 31 23:12:15 2026`. Both graceful requests were accepted, the exact
  fixture termination notification advanced generation 1, final LaunchServices
  readback was empty, and initial/final OS readback proved both captured PIDs
  absent with both observers cancelled. The normal run `normal-current-001`
  passed `testEnglishGrantedPermissionActionsUseManageLabels` in 12.039
  seconds; top-level status was `completed`, all wrapper/log/process/summary
  exit codes were 0, its `.xcresult` was valid, eight samples and identity
  checks matched, target terminal exit was observed, and both cleanup phases
  reached empty initial/final readback. Exact final process inspection after
  both runs found no target, fixture, UI runner, xcodebuild, pressure runner, or
  lifecycle/process fixture residue.
- Process/Tooling: the fixed UI App remained signed as
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` for TeamIdentifier
  `96PUA726W9`; executable, designated-requirement, and identity-manifest
  SHA-256 values were
  `0046df5c1c007a6c1958c9e93dcce557399845c017efc5b31d61cdafb26676ff`,
  `d68a18f9e330df6c06ec7eeb6cf7830243ed67ce7049457bf1f97f9e109aff9f`,
  and `13ef181bbbf3f3fac702fc6e6a2990dc145019877b960d5bbd9cf0627ded890a`.
  JavaScript and shell syntax, runner help, the complete deterministic/real
  lifecycle regression, the shared immediate/delayed/cancelled/watchdog/
  identity/real-child process regression, and `git diff --check` pass. Unit and
  Behavior are not relevant because this slice changes only pressure-tool
  lifecycle ownership; signed UI and both normal/interrupted Pressure paths
  provide the required higher layers. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside the
  slice. Rebuildable `.xcresult`, DerivedData, fixture variants, caches, and
  logs are confined to the recorded `SYNC-037D1` root for exact post-commit
  cleanup.
- Commit: `f1bf64cf5e2e88e982d7009343f54fe13154fdd3`
  (`refactor(sync): close SYNC-037D1 application process exit`).

### SYNC-037E Closure Record

- Design and Oracle: when exact target sampling reports
  `identity_match_lost` or `sample_unavailable`, the sampling owner retains the
  UI-wrapper PID and launch-time `lstart` identity, captures and deduplicates
  the currently observable descendant tree, and performs an immediate
  collective process readback. Exact absence, zombie state, or changed start
  identity makes a captured record inactive. Completion requires every exact
  record to be inactive followed by the owning shell's `wait PID`; a capture,
  `ps`, or monotonic-clock error remains unmet evidence. Failure adds the
  triggering target-termination verdict to the shared active-record
  diagnostics.
- Lifecycle and retained time policy: the sampling invocation owns identity
  capture, observation, cancellation, reaping, and the subsequent wrapper
  status readback. The retained 100ms cadence is named
  `TARGET_EXIT_COMPLETION_POLL_INTERVAL_SECONDS` and runs only while a captured
  identity remains active. The retained 30,000ms
  `TARGET_EXIT_COMPLETION_WATCHDOG_MILLISECONDS` is a terminal failure bound;
  scheduling overshoot followed by exact inactive readback succeeds. The
  shared observer reports `processTreeExited`, the final condition, and every
  still-active PID/start/PPID/state/command record.
- Offline regression: shared deterministic contracts pass for immediate and
  delayed satisfaction, scheduler overshoot followed by satisfied evidence,
  cancellation, final-readback watchdog diagnostics, capture failure, record
  deduplication, and exact PID replacement. The runtime-topology caller
  contract also proves the ordered sequence of wrapper-identity retention,
  descendant capture, collective observation, and reaping, and rejects root
  liveness, attempt counters, or raw sleeps inside this owner. Shell syntax,
  runner help, and `git diff --check` pass.
- Real process and signed Pressure: the complete shared process regression
  passes at the Terminal process boundary, including its named-pipe-ready real
  child, exact start identity, TERM, absence readback, and `wait PID`. The
  canonical runtime-topology command used the fixed signed App and isolated
  `SYNC-037E/normal-current-001` root. The UI test
  `testEnglishGrantedPermissionActionsUseManageLabels` passed in 16.738
  seconds. Atomic status recorded `stage=completed`, every final/wrapper/log/
  process/summary exit code as 0, a valid `.xcresult`, 14 matched samples and
  identity checks, `target_termination_verdict=identity_match_lost`, and
  `target_terminal_exit_observed=true`. That terminal flag is reachable only
  after the exact wrapper-tree observation succeeds and the owner performs
  `wait PID`. Target PID 7712 matched the launch receipt after 22 candidate
  observations and 5,134.399ms of stable identity evidence.
- Cleanup and identity evidence: application baseline readback was empty. Both
  LaunchServices and exact OS-process cleanup recorded empty initial/final
  sets, no readback errors, `verdict=absent`, `processVerdict=absent`, and both
  observers cancelled. Final exact-name process inspection found no FlowTab,
  FlowTab UI runner, xcodebuild, or Space fixture process. The fixed App passed
  strict out-of-sandbox signature verification for TeamIdentifier
  `96PUA726W9`. Its executable, designated-requirement, and private-manifest
  SHA-256 values were
  `0046df5c1c007a6c1958c9e93dcce557399845c017efc5b31d61cdafb26676ff`,
  `d68a18f9e330df6c06ec7eeb6cf7830243ed67ce7049457bf1f97f9e109aff9f`,
  and `13ef181bbbf3f3fac702fc6e6a2990dc145019877b960d5bbd9cf0627ded890a`.
- Validation scope and artifact ownership: Unit and Behavior are not relevant
  because this slice changes only pressure-tool completion ownership. Shared
  deterministic/real process contracts provide Process/Tooling, while the
  signed target-loss run provides UI and Pressure. Startup `prompts.zip` and
  the three pre-existing local Skill/reference edits remain unchanged and
  outside the slice. Rebuildable `.xcresult`, DerivedData, generated fixture
  variants, caches, logs, and the private manifest are confined to
  `.build-local/evidence-driven-sync/SYNC-037E` for exact post-commit cleanup.
- Commit: `de94837b0b705587d14b954db09eafb6b9cbe21b`
  (`refactor(sync): migrate SYNC-037E target exit completion`).

### SYNC-037F Closure Record

- Design and Oracle: the pressure owner captures every exact matching
  PID/start record before the UI trigger, records request wall and monotonic
  clocks immediately before launching the UI wrapper, and performs the first
  post-request process readback before any cadence wait. Each readback requires
  the frozen executable path and SHA-256, an active non-zombie process, and a
  parseable `lstart` identity. Pre-request identities are excluded exactly.
  Readback failure, malformed evidence, multiple candidates, an identity
  change, or a start time preceding the request resets or rejects the candidate.
  Success requires one unchanged post-request PID/start identity for the full
  stability interval and publication of the atomic launch receipt. The private
  manifest and pre-trigger resource-boundary readback independently freeze the
  resolved App path, bundle identifier, executable hash, designated-requirement
  hash, binding policy, and stability protocol before process observation.
- Lifecycle and retained time policy: the helper invocation owns baseline
  capture, observation, receipt publication, cancellation, and failure
  diagnostics. The retained `TARGET_LAUNCH_POLL_INTERVAL_SECONDS=0.1` cadence
  runs only while launch evidence is unmet because the shell/LaunchServices
  boundary has no reliable target-launch callback. The retained 5,000ms
  manifest stability window is a named safety protocol. The named 45,000ms
  launch watchdog begins at the pre-trigger monotonic generation and is solely
  a terminal failure bound. A readback reached after scheduler overshoot can
  still establish an unchanged candidate, so load changes completion latency
  while exact identity evidence determines the result. Failure reports the
  unmet condition, baseline, final candidates, candidate generation and
  stability, last process/readback evidence, clock error, watchdog, cadence,
  and stability policy.
- Deterministic regression: the Bash 3.2 contract covers immediate readback
  before polling, an initially present candidate, delayed discovery, slow
  scheduling beyond the nominal watchdog, final discovery followed by the
  required stability observation, ambiguous candidates, same-PID start-identity
  reuse, transient and terminal readback errors, exact baseline filtering,
  pre-request start rejection, runner exit, cadence cancellation, clock and
  receipt failure, and final watchdog diagnostics. Its static caller check
  requires baseline capture and request-clock capture before the background UI
  trigger, then launch observation, and rejects attempt counters, the former
  450-iteration bound, or an unnamed raw sleep. Shell syntax, runner help, and
  `git diff --check` pass.
- Signed UI and Pressure: the final isolated run
  `SYNC-037F/normal-current-002` used the fixed signed App and the canonical
  runtime-topology runner. The UI test
  `testEnglishGrantedPermissionActionsUseManageLabels` passed in 13.495
  seconds. Atomic status recorded `stage=completed`, every final/wrapper/log/
  process/summary exit code as 0, one valid `.xcresult` test with zero failures,
  11 matched samples and identity checks, and exact target terminal-exit
  evidence. Target PID 17085 qualified after 22 observations and 5,127.775ms
  of unchanged PID/start/path/executable evidence; the launch receipt and
  summary both recorded `identityContractVerdict=matched` with zero rejected
  transient identities.
- Cleanup, identity, and validation scope: application and OS-process cleanup
  both recorded empty initial/final sets, no readback errors, `verdict=absent`,
  `processVerdict=absent`, and cancelled observers. Final exact-name process
  readback found no FlowTab, FlowTab UI runner, xcodebuild, or Space fixture
  process. The fixed App passed strict out-of-sandbox signature verification
  for TeamIdentifier `96PUA726W9`; executable, designated-requirement, and
  private-manifest SHA-256 values were
  `0046df5c1c007a6c1958c9e93dcce557399845c017efc5b31d61cdafb26676ff`,
  `d68a18f9e330df6c06ec7eeb6cf7830243ed67ce7049457bf1f97f9e109aff9f`,
  and `13ef181bbbf3f3fac702fc6e6a2990dc145019877b960d5bbd9cf0627ded890a`.
  Unit and Behavior are not relevant because this slice changes only pressure
  tooling; deterministic Process/Tooling plus the signed UI and Pressure path
  cover the affected boundary. Startup `prompts.zip` and the three pre-existing
  local Skill/reference edits remain unchanged and outside the slice.
  Rebuildable `.xcresult`, DerivedData, fixture variants, caches, logs, and the
  private manifest are confined to `.build-local/evidence-driven-sync/SYNC-037F`
  for exact post-commit cleanup.
- Commit: `b7e84cd7e5f3b3076a46e9ea4837b57a68bcda88`
  (`refactor(sync): migrate SYNC-037F target launch readiness`).

### SYNC-036A Closure Record

- Design and Oracle: `FlowTabUITestSupportWatchdogPolicy` now owns compatible
  operation bounds for foreground activation, fallback activation, switcher
  command delivery, tab navigation, settings-control discovery, brief element
  discovery, scoped-option discovery, and generic-option discovery. Completion
  remains defined by `XCUIApplication.State.runningForeground`, exact tab or
  control existence and hittability, exact option identity, or the existing
  post-baseline runtime-log command receipt. The foreground helpers retain the
  boolean result and final App state; navigation and option failures report the
  unmet condition plus final candidate counts, existence, element type, and
  title-candidate readbacks.
- Lifecycle and retained time policy: these durations are named terminal
  watchdog bounds for XCUI and XCTest observations. Every helper first uses the
  existing XCUI immediate readback behavior, and success remains condition
  driven. The calling test invocation owns App lifecycle, XCTest observation,
  runtime-log cancellation, and teardown. This slice introduces no timer,
  observer, scheduler, retry loop, or repeated asynchronous work.
- Deterministic regression and Process/Tooling: the policy compatibility test
  requires the eight established operation bounds, finite positive values, and
  a fallback activation bound no greater than the primary activation bound.
  The final canonical `policy-current-002` invocation rebuilt the UI test
  bundle and passed 1 test with 0 failures in 0.367 seconds. Its status ledger
  recorded `stage=completed`, final, signing, build-for-testing, and
  test-without-building exit codes of 0, with a present `.xcresult`; the result
  bundle independently reported `status=succeeded`, 1 test, and 0 failures.
  `plutil -lint FlowTab.xcodeproj/project.pbxproj`, the Support-file literal
  timeout scan, and `git diff --check` pass. The helper remains 789 lines and
  therefore within the existing single-responsibility guardrail.
- UI validation: the final cached-build `visible-current-002` invocation
  passed `testSettingsAppearanceThemeAndLanguageUpdateVisibleUI` in 24.112
  seconds and `testSidebarTabsSwitchContent` in 10.339 seconds. The former
  exercised settings navigation, radio-group and pop-up option discovery,
  exact selected-value readback, luminance evidence, and localized visible
  state; the latter exercised exact Home, Logs, and Settings content evidence.
  The status ledger recorded `stage=completed`, final, signing, and
  test-without-building exit codes of 0, with a present `.xcresult`; the result
  bundle independently reported `status=succeeded`, 2 tests, and 0 failures.
- Validation scope and cleanup: Unit and Behavior are not relevant because the
  slice changes only shared UI-test helpers and their UI-target policy.
  Pressure is not relevant because no observer, timer, repeated asynchronous
  work, or hot path changed. The fixed App passed strict signature validation
  as `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` for TeamIdentifier
  `96PUA726W9`. Final exact-name readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036A` for exact post-commit cleanup.
- Commit: `1678fe6c541491c53bbb27b2c83dd6999dbbf510`
  (`test(sync): migrate SYNC-036A UI support watchdogs`).

### SYNC-036B Closure Record

- Design and Oracle: all seven RuntimeLog `fulfillment` waits now use the
  test-owner policy `eventDelivery`. Observer and Combine subscriptions remain
  established before append, clear, generation emission, or view-model start.
  Success requires the independent Oracle for each operation: exact monotonic
  append/flush/clear changes, the initial or post-trigger marker in the current
  line projection, the expected controlled-source read-request count, the
  fresh-line projection, or the post-cancellation observer/read-count state.
  No elapsed duration establishes completion or success.
- Diagnostics, lifecycle, and retained time policy: the one-second duration is
  retained as a named terminal failure bound around XCTest event delivery.
  Every expectation description records its unmet condition, and the immediate
  readback assertion records the final change list, marker and line projection,
  read-request count, published lines, or observer state. Each invocation owns
  `RuntimeLogChangeObservation.cancel()`, `RuntimeLogLinesViewModel.stop()`, its
  local `AnyCancellable` set, controlled continuations, and temporary directory
  cleanup. This slice adds no scheduler, polling cadence, retry, or product
  timer.
- Unit and Behavior validation: the policy compatibility test requires the
  established one-second bound to be finite and positive. The actual store
  test covers baseline generation, ordered event delivery, clear readback, and
  cancellation. The two view-model tests cover initial readback, append and
  post-clear events, stale and duplicate generation rejection, fresh evidence,
  and cancelled observation. The canonical
  `behavior-current-001` invocation passed all 4 tests with 0 failures in 0.635
  seconds: policy 0.001, store 0.004, initial/append/clear 0.320, and
  stale/duplicate/cancel 0.309 seconds.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 4 tests. The file is 366 lines, its direct literal
  timeout scan is empty, and `git diff --check` passes. UI is not relevant
  because the slice changes only app-test synchronization policy and
  diagnostics. Pressure is not relevant because no production observer,
  repeated asynchronous work, or hot path changed. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process,
  and no `FlowTabLogObservation-*` or `FlowTabLogViewModel-*` temporary root
  remained. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036B` for exact post-commit cleanup.
- Commit: `92ca82236fc4cb8d4465b156d4902fa09ad59d6c`
  (`test(sync): migrate SYNC-036B runtime log watchdogs`).

### SYNC-036C Closure Record

- Design and Oracle: `terminateSelectedApp()` is a synchronous state
  transition, and `RecordingRuntimeProjectionService` records its maintenance
  request synchronously. The negative test is therefore synchronous as well.
  A layout-publication counter is installed before the trigger, then the
  returned `.updatedSession` result, exact maintenance reasons, unchanged app
  projection, pending app/PID/generation, terminating identity, empty
  termination-signal set, and zero layout-publication count establish the
  post-request state. The positive companion uses the same immediate readback
  before it supplies exact app/PID termination evidence.
- Lifecycle and removed time policy: the inverted 10ms expectation and the
  companion's single `Task.yield()` scheduler advance are removed. The negative
  test is no longer async. Its invocation clears `onSessionLayoutChanged` and
  cancels the model from `defer`, so assertion exits retain cleanup ownership.
  No duration, scheduler, observer, retry, or polling policy remains in this
  pre-termination contract. The separate post-termination callback watchdog is
  retained for a later owner-specific SYNC-036 slice.
- Unit and Behavior validation: the final canonical
  `behavior-current-003` invocation passed the synchronous pending-state
  negative test in 0.004 seconds and the explicit workspace-termination
  positive companion in 0.010 seconds, for 2 tests with 0 failures in 0.014
  seconds. Together they prove that a quit-request receipt preserves the exact
  instance and layout until termination evidence arrives, and that exact
  termination evidence publishes one layout refresh and removes the instance.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, and the build log
  contains no warning for `FlowTabTests+RuntimeInteraction.swift`. UI is not
  relevant because this slice changes only a deterministic app-test Oracle.
  Pressure is not relevant because production code, observer lifecycle,
  repeated asynchronous work, and hot paths are unchanged. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable `.xcresult`,
  DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036C` for exact post-commit cleanup.
- Commit: `dc274a33d989d427402229adb99bda17f4a1b3bb`
  (`test(sync): migrate SYNC-036C pending termination oracle`).

### SYNC-036D Closure Record

- Design and Oracle: `handleApplicationTerminated(appID:pid:)` publishes the
  resulting session layout synchronously before returning. Each affected test
  now installs its layout-publication counter before the exact app/PID trigger,
  establishes the zero baseline where a pending quit request exists, and then
  requires the handler's true result and exactly one publication. Exact
  termination signals, projection-read counts, session membership, selected
  application, pending request, and terminating identity provide independent
  post-transition readbacks. The same-bundle/different-PID companion preserves
  the exact pending PID and generation when the termination identity does not
  match.
- Lifecycle and removed time policy: all three one-second waits and their
  expectation objects are removed, and the tests are synchronous. Each test
  invocation clears `onSessionLayoutChanged` and cancels the model from
  `defer`, including assertion-exit paths. No duration, observer, scheduler,
  retry, polling cadence, or watchdog remains in this layout-publication
  contract.
- Unit and Behavior validation: the final canonical
  `behavior-current-001` invocation passed all four targeted tests with 0
  failures in 0.015 seconds. The direct runtime-projection refresh passed in
  0.012 seconds, the behavior path in 0.001 seconds, the mismatched-PID identity
  companion in 0.002 seconds, and the pending-request positive path in 0.001
  seconds. Together they cover exact event arrival, baseline state, single
  publication, and window-instance identity isolation.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 4 tests. `git diff --check` passes, the source remains
  622 lines, and the build log contains no warning for
  `FlowTabTests+RuntimeInteraction.swift`. UI is not relevant because this
  slice changes only deterministic app-test Oracles. Pressure is not relevant
  because production code, observer lifecycle, repeated asynchronous work,
  and hot paths are unchanged. Exact final process readback found no FlowTab,
  UI runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable `.xcresult`, DerivedData, caches, and logs are
  confined to `.build-local/evidence-driven-sync/SYNC-036D` for exact
  post-commit cleanup.
- Commit: `a7864bb5693acf311e89b782e6b405640800af1f`
  (`test(sync): migrate SYNC-036D termination layout oracle`).

### SYNC-036E Closure Record

- Design and Oracle: the test callback is installed before either window or
  synchronization trigger. The already-key path begins with an empty readiness
  history and an exact non-responder baseline; the delayed-key path establishes
  an empty history after the queued synchronization attempt, then supplies the
  exact window notification. Success in both paths remains the typed callback
  plus independent `isKeyWindow`, exact text-view first-responder, accessibility
  identifier, and single `[true]` transition readbacks. Duplicate key-window
  notifications retain that single transition, while a deactivated generation
  cannot publish stale readiness.
- Lifecycle and retained time policy: `RuntimeInteractionWatchdogPolicy` owns
  the compatible one-second `keyboardReadinessEvent` duration solely as the
  terminal XCTest failure bound. Timeout diagnostics report the unmet exact
  readiness condition and the final key-window bit, responder match and type,
  and complete readiness-transition history. Harness teardown clears its test
  callback before coordinator detach cancels the window observers; both event
  tests read back the test-observer installation before triggering and its
  absence from `defer` after close. Existing coordinator generation checks own
  stale queued-work cancellation. No polling cadence or retry is introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The final canonical
  `behavior-current-002` invocation passed 4 tests with 0 failures in 0.664
  seconds: policy 0.001, stale-cancellation 0.223, already-key plus duplicate
  notification 0.219, and delayed-key evidence 0.222 seconds. Scheduling
  affects only callback arrival time; every success assertion remains tied to
  the exact readiness event and readback.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 4 tests. `git diff --check` passes, and the build log
  contains no warning for either touched Swift file. UI is not relevant because
  the runtime readiness implementation is unchanged and the lifecycle change
  is confined to its DEBUG test harness. Pressure is not relevant because no
  production observer, timer, repeated asynchronous work, or hot path changed.
  Exact final process readback found no FlowTab, UI runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036E` for exact post-commit cleanup.
- Commit: `25f1f229765eee03f9f2399795869f3f1de73d46`
  (`test(sync): migrate SYNC-036E keyboard readiness watchdog`).

### SYNC-036F Closure Record

- Design and Oracle: `waitForSearchInputMainQueueTurn()` enqueues its barrier
  after the Search coordinator work being observed. FIFO delivery of that exact
  main-queue callback is the sole transition Oracle. The callback records its
  delivery before fulfilling the expectation, and the helper immediately reads
  back that flag. The affected callers continue to use independent readiness
  history, key-window, and exact first-responder assertions for their domain
  results.
- Lifecycle and retained time policy: the compatible one-second duration is
  retained as the file-owned `mainQueueTransition` watchdog and can only bound
  terminal test failure. Each helper invocation owns the expectation and a
  cancellable `DispatchWorkItem`; `defer` cancels the work item on every return
  path. Timeout diagnostics report `searchInputMainQueueTransition` and the
  final callback-delivery bit. There is no polling cadence, retry, or elapsed-
  time success rule.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The final canonical
  `behavior-current-002` invocation passed 4 tests with 0 failures in 0.659
  seconds: policy 0.001, stale-cancellation 0.220, exact readiness plus duplicate
  notification 0.221, and delayed-key evidence 0.217 seconds. These paths invoke
  the barrier four times across active, duplicate-event, inactive, and delayed
  notification transitions.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 4 tests. `git diff --check` passes, the file has no
  direct literal timeout remaining, and its build log contains no file-specific
  warning. UI is not relevant because this slice changes only an app-test
  barrier helper. Pressure is not relevant because production code, observer
  lifecycle, repeated asynchronous work, and hot paths are unchanged. Exact
  final process readback found no FlowTab, UI runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036F` for exact post-commit cleanup.
- Commit: `75f1a3252e957ef3e0f6ae34f5a12325da3e1e6d`
  (`test(sync): migrate SYNC-036F main queue watchdog`).

### SYNC-036G Closure Record

- Design and Oracle: all twelve maintenance/freshness waits still establish the
  executor expectation before requesting maintenance. Exact executor callback
  delivery fulfills the wait; subsequent assertions independently read back
  request IDs, priority, targets, reasons, bounded drain order, dirty scope,
  committed generation/readiness, coordinator pending state, and observation-
  scheduler entries. The two multi-request cases retain their exact fulfillment
  counts and ordered result assertions.
- Lifecycle and retained time policy: the Priority test-case owner now exposes
  the compatible one-second `runtimeMaintenanceExecution` watchdog. Each
  expectation names the exact unmet maintenance transition, while its following
  readbacks report the last executed requests, store state, or coordinator and
  scheduler evidence. The local test case and `RuntimeProjectionService`
  lifetime own expectation completion and maintenance-queue drainage. No
  polling cadence, retry, scheduler policy, or elapsed-time success rule is
  added.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The final canonical
  `behavior-current-001` invocation passed the policy plus all twelve affected
  maintenance/freshness tests, 13 tests with 0 failures in 0.032 seconds. The
  slowest repaired-generation path completed in 0.010 seconds; all remaining
  paths completed between 0.000 and 0.004 seconds. Scheduling changes callback
  arrival time while exact executor and read-model evidence determine results.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 13 tests. `git diff --check` passes, all twelve direct
  timeout literals are removed, the pre-existing oversized reconciliation file
  remains exactly 3,587 lines, and neither touched test file emitted a warning.
  UI is not relevant because this slice changes only app-test watchdog
  ownership. Pressure is not relevant because production code, observer
  lifecycle, repeated asynchronous work, and hot paths are unchanged. Exact
  final process readback found no FlowTab, UI runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036G` for exact post-commit cleanup.
- Commit: `1e6be861ac5d738039c9cf3f2f3a1ae8a0eac856`
  (`test(sync): migrate SYNC-036G maintenance watchdogs`).

### SYNC-036H Closure Record

- Design and Oracle: every affected capture override or Combine subscription is
  established before the visible-snapshot request or controlled batch release.
  Capture-start, all-captures-finished, and `objectWillChange` callbacks provide
  event completion. Ordered capture titles/counts, bounded visible requests,
  exact image or fallback snapshots, main-thread and single-publication counts,
  and provider failure/retry state independently establish the result. No
  elapsed duration can promote an incomplete preview state.
- Lifecycle and retained time policy: the Priority test-case owner now exposes
  the compatible one-second `windowPreviewEventDelivery` watchdog. All eight
  expectations name the exact unmet capture or publication transition, and the
  following assertions report final request, publication, snapshot, and failure
  evidence. Each affected test cancels its model from `defer`; controlled batch
  tests also signal their semaphore on every exit path, and Combine tests clear
  their cancellable sets explicitly. No polling cadence or retry policy is
  introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The final canonical
  `behavior-current-001` invocation passed the policy plus all six affected
  preview tests, 7 tests with 0 failures in 0.609 seconds. Individual behavior
  paths completed in 0.066, 0.106, 0.102, 0.110, 0.113, and 0.111 seconds;
  the policy test completed in 0.001 seconds. Scheduling changes event delivery
  latency while callback and exact preview-state evidence determine results.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 7 tests. `git diff --check` passes, all eight direct
  timeout literals are removed, the affected file remains 549 lines, and
  neither touched test file emitted a warning. UI is not relevant because this
  slice changes only app-test watchdog ownership and cleanup. Pressure is not
  relevant because production preview work, concurrency policy, and hot paths
  are unchanged. Exact final process readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036H` for exact post-commit cleanup.
- Commit: `9545e798c0488d6e6d99d499c632803911f6d00b`
  (`test(sync): migrate SYNC-036H preview event watchdogs`).

### SYNC-036I Closure Record

- Design and Oracle: all three expectations are installed before activation.
  The first two fulfill only when the exact target CG window readback becomes
  onscreen; the third fulfills only when recovery attempts the newly discovered
  exact AX-window pointer. Final focus-call order, false-to-true visibility
  history, CG/AX read counts, and absence of an unrelated public activation
  remain independent result evidence.
- Lifecycle and retained time policy: the production focus-recovery owner keeps
  its named 20ms conditional-readback cadence and one-second terminal watchdog.
  It performs the initial readback immediately, correlates every observation by
  generation and exact target identity, and cancels polling, watchdog, and
  notification registrations on completion, explicit cancel, or coordinator
  deinitialization. The test owner now names its compatible one-second terminal
  bound `runtimeFocusRecoveryObservation`; each expectation records the exact
  unmet visibility or AX-recovery condition. Success remains condition driven.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The final canonical
  `behavior-current-001` invocation passed the policy and all three delayed
  focus-recovery paths, 4 tests with 0 failures in 0.332 seconds. The CG bridge,
  AX-to-CG visibility, and late AX-window paths completed in 0.111, 0.106, and
  0.113 seconds; the policy test completed in 0.002 seconds.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 4 tests. `git diff --check` passes, all three direct
  timeout literals are removed, the pre-existing oversized activation file
  remains exactly 1,822 lines, and neither touched test file emitted a warning.
  UI and Pressure are not relevant because this slice changes only app-test
  watchdog ownership; production polling, observer lifecycle, and hot paths are
  unchanged. Exact final process readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036I` for exact post-commit cleanup.
- Commit: `4b3cf8067c7a5d0930054460f0f196f93f9a04d9`
  (`test(sync): migrate SYNC-036I focus recovery watchdogs`).

### SYNC-036J Closure Record

- Design and Oracle: exact appID/PID launch and termination handlers are
  installed before either synthetic workspace notification. Their typed
  callbacks complete the waits; exact recorded launch/termination arrays,
  directory entry, observer-before-launch history, prepared and cancelled
  launch-observation identities, and post-termination no-delivery readback
  independently establish the ordered lifecycle result.
- Lifecycle and retained time policy: the Priority test-case owner now exposes
  the compatible one-second `appDelegateWorkspaceLifecycleSignal` duration
  solely as a terminal XCTest failure bound. Expectation diagnostics name the
  unmet exact app-instance transition. The test's existing `defer` clears both
  handlers, terminates the delegate, restores workspace and app-launch hooks,
  and removes its isolated defaults. No polling cadence, retry, or elapsed-time
  success rule is introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed the policy and ordered workspace lifecycle path, 2 tests
  with 0 failures in 0.039 seconds. The lifecycle path completed in 0.038
  seconds and the policy test in 0.000 seconds. Scheduling changes callback
  delivery latency while exact typed events and readbacks determine results.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, both direct timeout
  literals are removed, the affected lifecycle file remains 179 lines, and
  neither touched test file emitted a warning. The complete build log retains
  pre-existing warnings in untouched files. UI is not relevant because this
  slice changes only app-test watchdog ownership. Pressure is not relevant
  because production observers, timers, repeated asynchronous work, and hot
  paths are unchanged. Exact final process readback found no FlowTab, UI
  runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable `.xcresult`, DerivedData, caches, and logs are
  confined to `.build-local/evidence-driven-sync/SYNC-036J` for exact
  post-commit cleanup.
- Commit: `2cb7e5a0d454e6de12c96d86d1ddfc287054ac86`
  (`test(sync): migrate SYNC-036J workspace lifecycle watchdog`).

### SYNC-036K Closure Record

- Design and Oracle: both expectations exist before construction of the local
  service and therefore before either app-launch or window-change signal. The
  reconciliation executor fulfills only for execution counts one and two;
  over-fulfillment is rejected. After each callback the test drains the exact
  maintenance queue, then independently reads back the first app-launch reason
  and the final ordered pair of exact PID targets and reasons.
- Lifecycle and retained time policy: this path reuses the Priority owner’s
  compatible one-second `runtimeMaintenanceExecution` watchdog solely as the
  terminal XCTest failure bound. Each expectation names its unmet transition.
  The local service/test invocation owns executor delivery, locked request
  history, and maintenance-queue drainage. No duration, polling cadence, retry,
  or elapsed-time success rule is added.
- Unit and Behavior validation: the existing policy contract requires the
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed that contract and the ordered app-launch/window-evidence
  path, 2 tests with 0 failures in 0.002 seconds; each test completed in 0.001
  seconds. Scheduling changes executor callback latency while exact request
  identity and reason readbacks determine results.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, both direct timeout
  literals are removed, the affected source remains within the 400-line
  guardrail, and neither exercised test file emitted a warning. The complete
  build log retains pre-existing warnings outside this slice. UI is not
  relevant because this slice changes only an app-test watchdog reference and
  diagnostics. Pressure is not relevant because production observers, timers,
  repeated asynchronous work, and hot paths are unchanged. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable `.xcresult`,
  DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036K` for exact post-commit cleanup.
- Commit: `ca570d52194e5ec5f2056eb0cd1a76a5e7998491`
  (`test(sync): migrate SYNC-036K app launch maintenance watchdogs`).

### SYNC-036L Closure Record

- Design and Oracle: each typed resolution observer is registered before
  `presentInitialUIIfNeeded`. The production owner installs its routed
  projection observers before taking the baseline, performs the initial
  readback, triggers readiness, and closes the synchronous race with another
  readback. The immediate path accepts only matching generation, source,
  resolution, exact app identities, successful presentation, committed Search
  state, selected app, panel height, and completed observer lifecycle. The
  delayed path first proves the missing-index unresolved state, then posts the
  exact committed-index notification and requires resolved evidence plus final
  Search activation.
- Lifecycle and retained time policy: the Priority test-case owner now exposes
  the compatible one-second `initialSearchPresentationResolution` duration
  solely as a terminal XCTest failure bound; each expectation names its unmet
  exact transition. The production owner retains its separately named
  three-second watchdog, generation checks, initial/final evidence, and
  cancellation. Test `defer` removes the outer observer, stops the production
  owner and its watchdog/notification registrations, cancels selection, and
  restores launch overrides. No polling cadence, retry, or elapsed-time success
  rule is introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed the policy plus both initial-presentation paths, 3 tests
  with 0 failures in 0.345 seconds. The policy, immediate presentation, and
  committed-index-delayed paths completed in 0.001, 0.193, and 0.151 seconds.
  Scheduling changes notification delivery latency while exact evidence and
  readback determine results.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 3 tests. `git diff --check` passes, both direct timeout
  literals are removed, the affected files remain 63 and 354 lines, and
  neither touched Swift file emitted a compiler warning. The run also emitted
  eight SwiftUI background-publication diagnostics from the unchanged
  production path; this test-only diff does not alter publication scheduling.
  UI is not relevant because this slice changes only app-test watchdog
  ownership and diagnostics. Pressure is not relevant because production
  observers, schedulers, repeated asynchronous work, and hot paths are
  unchanged. Exact final process readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036L` for exact post-commit cleanup.
- Commit: `bab94459cb4399426be515a42ffb21de444cfc80`
  (`test(sync): migrate SYNC-036L initial Search presentation watchdog`).

### SYNC-036M Closure Record

- Design and Oracle: the exact-name/exact-service notification expectation is
  installed after the test proves `.missingCommittedIndex` and before it sends
  the appID/PID window-change signal. Only
  `.runtimeCommittedSearchIndexDidUpdate` from that service completes the wait.
  After notification delivery the service queue is drained and exact committed
  readiness/result state, app and window entries, generation coverage, empty
  pending scopes, and ordered `.app(pid)` then `.spaceTopology` repairs
  independently establish the result.
- Lifecycle and retained time policy: the Priority test-case owner now exposes
  the compatible one-second `committedSearchIndexPublication` duration solely
  as a terminal XCTest failure bound. XCTest owns the notification expectation
  registration and cleanup, while the local service/test invocation owns the
  repair executor, read-model state, and maintenance-queue drainage. No polling
  cadence, retry, or elapsed-time success rule is introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed the policy and nested-repair publication path, 2 tests with
  0 failures in 0.013 seconds. The policy and behavior paths completed in 0.002
  and 0.012 seconds. Scheduling changes main-queue notification latency while
  exact event identity and committed-generation readback determine results.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, the scoped raw
  timeout is removed, the affected files remain 73 and 739 lines, and neither
  touched Swift file emitted a compiler warning. The separate background-post
  main-thread delivery timeout remains isolated for SYNC-036N. UI is not
  relevant because this slice changes only app-test watchdog ownership.
  Pressure is not relevant because production notification dispatch, repeated
  asynchronous work, and hot paths are unchanged. Exact final process readback
  found no FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable `.xcresult`, DerivedData,
  caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036M` for exact post-commit cleanup.
- Commit: `57e82b98242a3e04c45f5d46b0c1d9f004f8931e`
  (`test(sync): migrate SYNC-036M committed Search publication watchdog`).

### SYNC-036N Closure Record

- Design and Oracle: the isolated notification-center observer is installed
  before dispatching the trigger from the global queue and filters by the exact
  app-switcher projection name and object. Only callback delivery fulfills the
  expectation, and the callback independently asserts that the production
  publisher delivered it on the main thread. Over-fulfillment rejection makes
  duplicate delivery observable.
- Lifecycle and retained time policy: the Priority test-case owner now exposes
  the compatible one-second `runtimeProjectionMainThreadDelivery` duration
  solely as a terminal XCTest failure bound. Its expectation names the unmet
  main-thread delivery condition. The test invocation owns the expectation and
  removes its observer from `defer`; production `DispatchQueue.main.async`
  scheduling owns delivery. No polling cadence, retry, or elapsed-time success
  rule is introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed the policy and background-to-main delivery path, 2 tests
  with 0 failures in 0.106 seconds. The policy and behavior paths completed in
  0.001 and 0.104 seconds. Scheduling changes callback latency while exact
  notification identity and main-thread readback determine results.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, no direct literal
  timeout remains in the affected publication file, the touched files remain
  83 and 747 lines, and neither emitted a compiler warning. UI is not relevant
  because this slice changes only app-test watchdog ownership and diagnostics.
  Pressure is not relevant because production notification scheduling,
  repeated asynchronous work, and hot paths are unchanged. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable `.xcresult`,
  DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036N` for exact post-commit cleanup.
- Commit: `19d5560b7277edcb7d62da6293527155c745ed49`
  (`test(sync): migrate SYNC-036N projection main-thread delivery watchdog`).

### SYNC-036O Closure Record

- Design and Oracle: the model callback is installed before panel presentation
  and all key events, then each phase sets the expected next scroll revision and
  exact first-result identity before sending Down. Only the matching revision
  and result callback from the SwiftUI scroll task fulfills that phase. Exact
  revision, last-result and wrapped selection indices, selected result identity,
  and the complete callback history independently establish the result.
- Lifecycle and retained time policy: the Priority test-case owner now exposes
  the compatible one-second `searchResultScrollPublication` duration solely as
  each phase's terminal XCTest failure bound. Expectations name their unmet
  initial or wrapped exact transition and reject over-fulfillment. Test `defer`
  restores the prior model callback and cancels panel selection; the controller
  and view lifetime own SwiftUI task delivery. No polling cadence, retry, or
  elapsed-time success rule is introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed the policy and complete initial-to-last-to-wrap path, 2
  tests with 0 failures in 1.039 seconds. The policy and behavior paths
  completed in 0.001 and 1.037 seconds. The full path exceeded one second while
  both individual event waits still resolved from exact callbacks, directly
  demonstrating that surrounding scheduling load changes latency rather than
  the result.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, both direct timeout
  literals are removed, the touched files remain 93 and 171 lines, and neither
  emitted a compiler warning. The run also emitted four SwiftUI background-
  publication and 70 InputMethodKit/NSXPC secure-decoding diagnostics from
  unchanged production/system paths; this test-only diff does not alter those
  paths. UI is not relevant because the visible implementation is unchanged.
  Pressure is not relevant because production callbacks, SwiftUI tasks,
  repeated asynchronous work, and hot paths are unchanged. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable `.xcresult`,
  DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036O` for exact post-commit cleanup.
- Commit: `28c4d4b50c17b17909e3682790682917974d2cff`
  (`test(sync): migrate SYNC-036O Search scroll publication watchdog`).

### SYNC-036P Closure Record

- Design and Oracle: the Search-state callback is installed before query
  synchronization and fulfills only after the model reports active Search with
  the exact application scope, `bro` query, and `Browser` result. The same
  Search-active, scope, query, and result-title fields are read back after event
  delivery, so callback latency cannot establish success.
- Lifecycle and retained time policy: the Priority test-case owner now exposes
  the compatible one-second `searchComputationPublication` duration solely as
  a terminal XCTest failure bound. The expectation names the unmet exact Search
  publication and rejects over-fulfillment. Test `defer` explicitly cancels the
  model's generation-owned pending Search computation before clearing Search
  and layout callbacks. No polling cadence, retry, or elapsed-time success rule
  is introduced. The separate post-termination layout and preserved-Search
  composite remains isolated for SYNC-036Q.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed the policy and termination-refresh behavior path, 2 tests
  with 0 failures in 0.323 seconds. The policy and behavior paths completed in
  0.000 and 0.323 seconds. Scheduling changes callback latency while the exact
  Search state and final readbacks determine the result.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, the scoped initial
  raw timeout is removed, the touched files remain 103 and 111 lines, and
  neither emitted a compiler warning. The run emitted four SwiftUI background-
  publication diagnostics from an unchanged production path; this test-only
  diff does not alter that path. UI is not relevant because the visible
  implementation is unchanged. Pressure is not relevant because production
  Search scheduling, repeated asynchronous work, and hot paths are unchanged.
  Exact final process readback found no FlowTab, UI runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036P` for exact post-commit cleanup.
- Commit: `6815a9ed568ef91d6b1b8a6965ab2bce03e4e145`
  (`test(sync): migrate SYNC-036P Search computation watchdog`).

### SYNC-036Q Closure Record

- Design and Oracle: the layout callback and preserved-Search callback are
  installed before the exact application-ID/PID termination trigger. A zero
  layout-publication baseline is established immediately before the trigger.
  The synchronous contract requires `handleApplicationTerminated` to return
  true and the counter to read exactly one before any wait. The asynchronous
  expectation fulfills only after the refreshed app count and exact active
  Search scope, query, and result titles all match; those values and the stable
  layout count are read back again after delivery.
- Lifecycle and retained time policy: the asynchronous phase reuses the
  Priority owner's one-second `searchComputationPublication` watchdog solely as
  a terminal failure bound, with a structured unmet-condition description and
  over-fulfillment rejection. Test `defer` cancels generation-owned pending
  Search work and clears both callbacks. The synchronous layout phase has no
  duration, polling cadence, retry, or scheduler advance.
- Unit and Behavior validation: the existing policy contract requires the
  established bound, finiteness, and positivity. The canonical
  `behavior-current-001` invocation passed that contract and the complete
  termination-refresh path, 2 tests with 0 failures in 0.326 seconds. The
  policy and behavior paths completed in 0.000 and 0.326 seconds. Scheduling
  changes preserved-Search callback latency while the return boundary, exact
  callback state, and final readbacks determine the result.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, no numeric timeout
  remains in the affected 111-line file, and it emitted no compiler warning.
  The run emitted four SwiftUI background-publication diagnostics from an
  unchanged production path; this test-only diff does not alter that path. UI
  is not relevant because the visible implementation is unchanged. Pressure is
  not relevant because production Search scheduling, repeated asynchronous
  work, and hot paths are unchanged. Exact final process readback found no
  FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable `.xcresult`, DerivedData,
  caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036Q` for exact post-commit cleanup.
- Commit: `497bd5b3f6efdd8eb9eaacc5344e709407b674a9`
  (`test(sync): migrate SYNC-036Q termination Search refresh oracle`).

### SYNC-036R Closure Record

- Design and Oracle: the maintenance-request handler is installed before the
  hosted Home view is constructed and accepts only the exact
  `.homeProjectionMissing` reason. Over-fulfillment rejection exposes duplicate
  requests. The service's complete recorded-reason array is read back after
  delivery, while `projectionOwner.isObserving` proves the active state and its
  later false value plus an unchanged projection-read count prove inactive
  lifecycle teardown.
- Lifecycle and retained time policy: the file-owned compatible one-second
  `maintenanceRequestDelivery` duration is solely a terminal XCTest failure
  bound, and its expectation names the unmet condition and expected reason.
  Test `defer` clears the handler on success or failure. The hosted view and
  projection owner continue to own visibility-driven observation start and
  stop. No polling cadence, retry, or elapsed-time success rule is introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed the policy and full active-to-inactive Home lifecycle, 2
  tests with 0 failures in 0.039 seconds. The policy and behavior paths
  completed in 0.001 and 0.038 seconds. Scheduling changes SwiftUI callback
  latency while the exact request reason, recorded array, and observation
  readbacks determine the result.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, the direct numeric
  timeout is removed, and the affected file remains 305 lines. The compiler
  repeated three Swift 6 concurrency warnings in unchanged application-
  forwarder test code at the current lines 200–202; this slice does not alter
  those closures. No SwiftUI, InputMethodKit, or NSXPC runtime diagnostic was
  emitted. UI is not relevant because production and visible behavior are
  unchanged. Pressure is not relevant because production observers, repeated
  asynchronous work, and hot paths are unchanged. Exact final process readback
  found no FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable `.xcresult`, DerivedData,
  caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036R` for exact post-commit cleanup.
- Commit: `0c3fd1b38d3b3133ae46da931c959f20c0986fc8`
  (`test(sync): migrate SYNC-036R Home maintenance watchdog`).

### SYNC-036S Closure Record

- Design and Oracle: the Home detail observation is registered before the
  background projection notification. Its callback fulfills only for the exact
  app ID, observation generation 1, readback 3, current-app-window notification
  source, source-generation advance to projection generation 2, complete
  projection, and completion transition. Main-thread delivery is asserted in
  the callback. The complete captured evidence is read back afterward together
  with the owner's stopped-observation state.
- Lifecycle and retained time policy: the Priority test owner now exposes the
  compatible one-second `homeProjectionEvidenceDelivery` duration solely as a
  terminal XCTest failure bound. The expectation names the unmet exact
  background-to-main evidence condition and rejects over-fulfillment. Test
  `defer` cancels the app observation on every exit; successful evidence also
  invokes the owner's generation-checked finish, and owner deinit removes its
  notification token. No polling cadence, retry, or elapsed-time success rule
  is introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed the policy and exact background-notification delivery path,
  2 tests with 0 failures in 0.106 seconds. The policy and behavior paths
  completed in 0.000 and 0.106 seconds. Scheduling changes delivery latency
  while typed evidence and final observation readbacks determine the result.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, the direct numeric
  timeout is removed, the touched files remain 114 and 246 lines, and neither
  emitted a compiler warning. No SwiftUI, InputMethodKit, or NSXPC runtime
  diagnostic was emitted. UI is not relevant because production and visible
  behavior are unchanged. Pressure is not relevant because production
  observation scheduling, repeated asynchronous work, and hot paths are
  unchanged. Exact final process readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036S` for exact post-commit cleanup.
- Commit: `5278ea007ace11176b3b31d476af7448de51ad0a`
  (`test(sync): migrate SYNC-036S Home detail evidence watchdog`).

### SYNC-036T Closure Record

- Design and Oracle: the Home summary observation is started before the
  background projection notification. Its callback fulfills only for
  observation generation 1, readback 2, the exact app-switcher notification
  source, source-generation advance to complete projection generation 2, the
  exact `com.example.home-observed` identity, and window count 2. Main-thread
  delivery is asserted in the callback. Captured evidence is read back after
  delivery, followed by exact active, explicit stop, and inactive owner states.
- Lifecycle and retained time policy: the path reuses the Priority owner's
  compatible one-second `homeProjectionEvidenceDelivery` duration solely as a
  terminal XCTest failure bound. The expectation names the unmet exact
  background-to-main summary condition and rejects over-fulfillment. Explicit
  `stop` removes the active observation and notification token; test `defer`
  repeats cleanup on failure exits. No polling cadence, retry, or elapsed-time
  success rule is introduced.
- Unit and Behavior validation: the shared policy contract requires the
  established bound, finiteness, and positivity. The canonical
  `behavior-current-001` invocation passed that contract and the exact summary
  delivery/stop path, 2 tests with 0 failures in 0.109 seconds. The policy and
  behavior paths completed in 0.000 and 0.109 seconds. Scheduling changes
  delivery latency while typed evidence and explicit owner state determine the
  result.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, the direct numeric
  timeout is removed, the affected file remains 349 lines, and it emitted no
  compiler warning. No SwiftUI, InputMethodKit, or NSXPC runtime diagnostic was
  emitted. UI is not relevant because production and visible behavior are
  unchanged. Pressure is not relevant because production observation
  scheduling, repeated asynchronous work, and hot paths are unchanged. Exact
  final process readback found no FlowTab, UI runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036T` for exact post-commit cleanup.
- Commit: `70544028b4a9cd714eda8071b57a82ac8d5f9f46`
  (`test(sync): migrate SYNC-036T Home summary evidence watchdog`).

### SYNC-036U Closure Record

- Design and Oracle: both two-event expectations are installed before the
  active Home view is constructed. Exact Combine publication completes the
  initial phase, followed by false accessibility/screen-capture values, two
  active target observations, two available fallback tokens, and one activation
  observer readback. The cancellation expectation also precedes the inactive
  view update. Both token-cancellation callbacks complete that phase, followed
  by zero target observations, zero available tokens, and zero activation
  observers.
- Lifecycle and retained time policy: the file-owned compatible one-second
  `eventDelivery` duration is solely each phase's terminal XCTest failure bound.
  Both expectations name their unmet two-target condition and reject
  over-fulfillment. Test `defer` clears the scheduler cancellation hook,
  cancels the Combine subscription, and stops the permission owner on every
  exit. The owner and coordinator retain token/activation-observer ownership.
  No polling cadence, retry, or elapsed-time success rule is introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed the policy and complete active-to-inactive two-target
  lifecycle, 2 tests with 0 failures in 0.033 seconds. The policy and behavior
  paths completed in 0.000 and 0.033 seconds. Scheduling changes event latency
  while exact callback counts and final owner/resource state determine the
  result.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, both direct numeric
  timeouts are removed, the affected file remains 369 lines, and it emitted no
  compiler warning. No SwiftUI, InputMethodKit, or NSXPC runtime diagnostic was
  emitted. UI is not relevant because production and visible behavior are
  unchanged. Pressure is not relevant because production scheduling,
  observation resources, and the existing 2,000-cycle lifecycle path are
  unchanged. Exact final process readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable `.xcresult`, DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036U` for exact post-commit cleanup.
- Commit: `988fb5414bea1810c9ce71ae16cf15ba9eeb7b7a`
  (`test(sync): migrate SYNC-036U Home permission lifecycle watchdogs`).

### SYNC-036V Closure Record

- Design and Oracle: the Combine subscription is installed before
  `owner.start()`. The injected evidence provider synchronously posts complete
  generation 2 evidence and then returns generation 1. Only the complete
  generation 2 request/takeover value passes the subscriber, and duplicate
  fulfillment is rejected. Final readbacks prove the post occurred during
  provider readback, the subscriber and owner retained the exact newer value,
  and the matching request's takeover state is active.
- Lifecycle and retained time policy: the file-owned compatible one-second
  `newerEvidenceDelivery` duration is solely a terminal XCTest failure bound,
  with the missed observer-before-readback condition in its description. Test
  `defer` cancels the Combine subscription and stops the owner; owner stop bumps
  its observation generation, removes the notification observer, and clears
  evidence state. No polling cadence, retry, or elapsed-time success rule is
  introduced.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed the policy, synchronous post/readback race, and independent
  stop/cancel state path, 3 tests with 0 failures in 0.005 seconds. The race
  path completed in 0.005 seconds and the policy/cancellation paths in 0.000
  seconds. Scheduling changes delivery latency while generation ordering and
  exact evidence determine the result.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 3 tests. `git diff --check` passes, the direct numeric
  timeout is removed, the affected file remains 324 lines, and it emitted no
  compiler warning. No SwiftUI, InputMethodKit, or NSXPC runtime diagnostic was
  emitted. UI is not relevant because production and visible behavior are
  unchanged. Pressure is not relevant because production observer scheduling
  and the existing 2,000-generation path are unchanged. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable `.xcresult`,
  DerivedData, caches, and logs are confined to
  `.build-local/evidence-driven-sync/SYNC-036V` for exact post-commit cleanup.
- Commit: `853df0ee7f288f95b4a2b083249c2cfeafc980b8`
  (`test(sync): migrate SYNC-036V hotkey evidence watchdog`).

### SYNC-036W Closure Record

- Design and Oracle: the baseline AppDelegate registration generation is read
  before XCTest installs an exact-object notification observer. The observer is
  active before the re-registration post and accepts only baseline+1 generation,
  exact request ID/configurations, active Command-Tab takeover, and the
  `notification_payload` source. Over-fulfillment rejection exposes duplicates.
  The AppDelegate evidence and the complete new-monitor signatures,
  configurations, IDs, start counts, old-monitor stop counts, and takeover
  reconciliation are read back afterward.
- Lifecycle and retained time policy: the Priority test owner now exposes the
  compatible one-second `appDelegateHotkeyRegistrationPublication` duration
  solely as a terminal XCTest failure bound. XCTest owns its notification
  observer lifetime. AppDelegate termination and test `defer` own hotkey
  monitor, notification registration, shared/static hook, and isolated-defaults
  cleanup. No polling cadence, retry, or elapsed-time success rule is
  introduced.
- Unit, Behavior, and Pressure validation: the policy contract requires the
  established bound, finiteness, and positivity. The canonical
  `behavior-pressure-current-001` invocation passed the policy, exact posted-
  request path, and 500-delivery alternating active-delegate Pressure, 3 tests
  with 0 failures in 0.144 seconds. The pressure, behavior, and policy paths
  completed in 0.112, 0.031, and 0.000 seconds. Scheduling changes notification
  latency while exact generation/request evidence and monitor readbacks
  determine every result.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 3 tests. `git diff --check` passes, the direct numeric
  timeout is removed, the touched files remain 125 and 384 lines, and neither
  emitted a compiler warning. No SwiftUI, InputMethodKit, or NSXPC runtime
  diagnostic was emitted. UI is not relevant because production and visible
  behavior are unchanged. Exact final process readback found no FlowTab, UI
  runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable `.xcresult`, DerivedData, caches, and logs are confined
  to `.build-local/evidence-driven-sync/SYNC-036W` for exact post-commit cleanup.
- Commit: `8c497381daf3dfcf4c2924f3723ad87a71d0c093`
  (`test(sync): migrate SYNC-036W AppDelegate hotkey watchdog`).

### SYNC-036X Closure Record

- Design and Oracle: the Search freshness-barrier executor is installed before
  the request. It records every execution and fulfills only for the exact
  running-app PID, app ID, high priority, and sole `searchFreshnessBarrier`
  reason. Duplicate matching execution is rejected. Final readbacks require one
  exact request, empty dirty app/PID/CG-window and pending-scope state, a
  committed-generation-validated Search result covering the current generation,
  and the exact CG-stable window identity.
- Lifecycle and retained time policy: the existing compatible one-second
  `runtimeMaintenanceExecution` duration remains solely a terminal XCTest
  failure bound. `XCTWaiter` returns before the service-owned maintenance queue
  is drained, allowing timeout diagnostics to report every last observed request
  plus dirty apps/PIDs, pending scopes, and committed-index presence. The local
  service/test invocation owns executor delivery, expectation lifetime, locked
  evidence storage, and queue drainage. No polling cadence, retry, or elapsed-
  time success rule is introduced.
- Unit and Behavior validation: the established policy compatibility test and
  extracted scheduled-coverage Search commit path passed through the canonical
  `behavior-current-002` invocation, 2 tests with 0 failures in 0.019 seconds.
  Scheduling changes executor-delivery latency while exact request evidence and
  read-model state determine the result. UI is not relevant because production
  and visible behavior are unchanged. Pressure is not relevant because no
  production scheduling, observer, timer, retry, or hot path changed.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` and project-file plist
  validation pass. Direct numeric timeouts are absent from both scoped files;
  the oversized source decreased from 3,854 to 3,781 lines and the isolated
  file is 133 lines. Neither touched Swift file emitted a compiler warning, and
  no SwiftUI, InputMethodKit, or NSXPC runtime diagnostic was emitted. Xcode
  emitted 34 pre-existing `_CFURLCopyResourcePropertyValuesAndFlags` no-scheme
  infrastructure diagnostics without changing build or test status. Exact final
  process readback found no FlowTab, UI runner, xcodebuild, or Space fixture
  process. Startup `prompts.zip` and the three pre-existing local Skill/reference
  edits remain unchanged and outside this slice. Rebuildable `.xcresult`,
  DerivedData, caches, and logs occupy 471 MB under
  `.build-local/evidence-driven-sync/SYNC-036X` for exact post-commit cleanup.
- Commit: `ef92b3a00d56d3c1f9b35892f9f633d5b2b7c602`
  (`test(sync): migrate SYNC-036X Search coverage repair watchdog`).

### SYNC-036Y Closure Record

- Design and Oracle: Search-presentation, hotkey-registration, and runtime-log
  observations are all installed before `applicationDidFinishLaunching`. Search
  completion requires a positive observation generation, `initialReadback`, a
  presented/did-present result, and Search active or pending. Hotkey completion
  requires generation 1, `application_launch`, and inactive Command-Tab
  takeover. Log completion requires a clear followed by at least three appends.
  Matching duplicate notifications over-fulfill; final defaults, Home tab,
  stress-runner start, hotkey configurations/signatures, Search model/session,
  clear/append snapshot, and exact seeded-log lines remain independent readbacks.
- Lifecycle and retained time policy: the Priority test owner exposes the
  compatible four-second `appDelegateLaunchBootstrapEvidence` duration solely
  as a terminal XCTest failure bound. Each expectation identifies its unmet
  condition; final assertions report the last presentation evidence, last
  registration generation/source/takeover plus record signatures, and last log
  change generation/kind/count. Test `defer` cancels the log recorder and
  observation, removes both notification observers, terminates the delegate,
  restores all hooks/defaults, and clears diagnostics.
- Unit and Behavior validation: the policy contract requires the established
  bound, finiteness, and positivity. The canonical `behavior-current-001`
  invocation passed that contract and the full composite AppDelegate bootstrap
  path, 2 tests with 0 failures in 1.135 seconds. The launch path completed in
  1.134 seconds and the policy path in 0.001 seconds. Scheduling changes event
  latency while exact notification content, ordered log changes, and final
  readbacks determine every result. UI is not relevant because production and
  visible behavior are unchanged. Pressure is not relevant because no
  production observer, scheduler, timer, retry, or hot path changed.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, the direct numeric
  timeout is removed, the touched files are 435 and 136 lines, and neither
  emitted a compiler warning. No SwiftUI, InputMethodKit, or NSXPC diagnostic
  was emitted. Xcode emitted 68 pre-existing no-scheme CFURL infrastructure
  diagnostics, and CFPrefs emitted one last-known shared-memory-state update
  diagnostic; neither changed build or test status. Exact final process readback
  found no FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable `.xcresult`, DerivedData,
  caches, and logs occupy 460 MB under
  `.build-local/evidence-driven-sync/SYNC-036Y` for exact post-commit cleanup.
- Commit: `bd04f3df0c68e6ca22df745b3a784e22007a287a`
  (`test(sync): migrate SYNC-036Y AppDelegate bootstrap watchdog`).

### SYNC-036Z Closure Record

- Design and Oracle: the termination-layout callback is installed before the
  exact app/PID trigger and its zero baseline is asserted. The synchronous
  method result must be true and the callback count exactly one at return.
  Final readbacks require the exact app/PID termination signal, a second
  app-switcher projection read, ordered Mail/Browser membership, and Browser as
  the preferred-next selection. The companion untracked-app path requires a
  false result, no signal, no extra projection read, and unchanged selection.
- Lifecycle and retained time policy: each test invocation owns callback
  removal and session cancellation with `defer`. The raw XCTest duration and
  asynchronous wait are removed; no polling cadence, retry, watchdog, or
  elapsed-time success rule remains. The termination tests are isolated in a
  focused source file, reducing the oversized mixed runtime owner.
- Unit and Behavior validation: Unit is not relevant because the production
  synchronous state rule is unchanged and the application behavior boundary is
  the direct contract under test. The canonical `behavior-current-002`
  invocation passed the exact positive and untracked negative paths, 2 tests
  with 0 failures in 0.005 seconds. The paths completed in 0.001 and 0.004
  seconds. Machine scheduling can change invocation latency while the explicit
  result, callback transition, exact signal, and final session state determine
  each outcome. UI is not relevant because production and visible behavior are
  unchanged. Pressure is not relevant because production scheduling,
  callbacks, observers, and hot paths are unchanged.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` and project-file plist
  validation pass. Direct numeric timeouts and async fulfillment are absent
  from the isolated 95-line file; the oversized mixed source decreased from
  891 to 819 lines. Neither touched Swift file emitted a compiler warning, and
  no SwiftUI, InputMethodKit, or NSXPC runtime diagnostic was emitted. Xcode
  emitted 34 pre-existing `_CFURLCopyResourcePropertyValuesAndFlags` no-scheme
  infrastructure diagnostics without changing build or test status. Exact
  final process readback found no FlowTab, UI runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  `.xcresult`, DerivedData, caches, and logs occupy 461 MB under
  `.build-local/evidence-driven-sync/SYNC-036Z` for exact post-commit cleanup.
- Commit: `da27f7be3fdc6f53c13b837287c2651caa68d0c8`
  (`test(sync): migrate SYNC-036Z termination refresh oracle`).

### SYNC-036AA Closure Record

- Design and Oracle: both callback observers are installed before the preview
  request, after empty capture-state and zero-provider-call baselines. Success
  requires exact ordered window IDs, images, expected capture generation, and
  two special-provider requests. Failure requires the exact first window, no
  image, `specialProviderUnavailable`, expected retry generation, one special-
  provider request, and zero generic requests. Exact matching duplicates
  over-fulfill. Final callback/match counts, last window/image/state evidence,
  exact snapshots, provider counts, and failure state are independent
  readbacks.
- Lifecycle and retained time policy: the existing compatible one-second
  `windowPreviewEventDelivery` duration is solely a terminal XCTest failure
  bound. Each test `defer` clears the callback before cancelling the model,
  invalidating preview work and releasing the session. No polling cadence,
  retry, or elapsed-time success rule is introduced.
- Unit and Behavior validation: the established watchdog policy contract and
  both exact publication paths passed through canonical
  `behavior-current-003`, 3 tests with 0 failures in 0.214 seconds. Failure,
  success, and policy paths completed in 0.110, 0.103, and 0.001 seconds.
  Machine scheduling changes callback latency while exact generation, state,
  identity, provider, and snapshot evidence determine each result. The first
  calibration run exposed an added style assertion outside this synchronization
  contract; source readback showed that style projection is independently
  feature-gated, so the Oracle was narrowed. The next passing run exposed one
  new trailing-closure compiler warning, which was removed before the final
  run. UI is not relevant because production and visible behavior are
  unchanged. Pressure is not relevant because production scheduling,
  callbacks, capture concurrency, and hot paths are unchanged.
- Process/Tooling and cleanup: the final FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 3 tests. `git diff --check` passes, both direct numeric
  timeouts are removed, and the affected file remains 368 lines. The final run
  emitted no affected-file compiler warning and no SwiftUI, InputMethodKit, or
  NSXPC runtime diagnostic. Xcode emitted 34 pre-existing
  `_CFURLCopyResourcePropertyValuesAndFlags` no-scheme infrastructure
  diagnostics without changing build or test status. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable `.xcresult`,
  DerivedData, caches, and logs occupy 890 MB under
  `.build-local/evidence-driven-sync/SYNC-036AA` for exact post-commit cleanup.
- Commit: `eac6cfbe53d102935422c4359dec4e8b24517127`
  (`test(sync): migrate SYNC-036AA preview publication watchdogs`).

### SYNC-036AB Closure Record

- Design and Oracle: the detached batch-capture override is installed before
  presentation, after zero-call, zero-in-flight, and empty-state baselines. It
  fulfills only for the two exact ordered window titles, the current process
  identifier, absent preferred CG identities, and enabled title-bar inference.
  Matching duplicates over-fulfill. The main-actor readback requires one
  invocation, preserves all final request fields in the failure diagnostic,
  and independently requires two in-flight captures with every capture state
  at `.inFlight` while the controlled batch remains blocked.
- Lifecycle and retained time policy: the existing compatible one-second
  `windowPreviewEventDelivery` duration is solely a terminal XCTest failure
  bound. Scoped locking owns cross-thread evidence access. The test invocation
  owns semaphore release, callback restoration, and model/controller
  cancellation in `defer`. The independent preview-completion/reveal event and
  its remaining direct bound are assigned to SYNC-036AC.
- Unit and Behavior validation: the established policy compatibility test and
  exact batch-start behavior path passed through canonical
  `behavior-current-002`, 2 tests with 0 failures in 0.150 seconds. The behavior
  and policy paths completed in 0.150 and 0.000 seconds. Machine scheduling can
  change detached-task delivery latency while exact request and in-flight state
  evidence determine success. The first passing calibration run exposed two
  new asynchronous-context lock/unlock warnings; scoped locking removed both
  before the final run. UI is not relevant because production and visible
  behavior are unchanged. Pressure is not relevant because production
  scheduling, preview concurrency, and hot paths are unchanged.
- Process/Tooling and cleanup: the final FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes. The batch-start
  bound now uses the named policy; the sole remaining direct one-second bound
  in the 707-line focused regression file belongs to SYNC-036AC. The final log
  emitted no affected-file compiler warning, CFURL no-scheme diagnostic,
  SwiftUI, InputMethodKit, or NSXPC runtime diagnostic. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable `.xcresult`,
  DerivedData, caches, and logs occupy 462 MB under
  `.build-local/evidence-driven-sync/SYNC-036AB` for exact post-commit cleanup.
- Commit: `f175ae4d432c1296ca8bc135459109ef317c4fbe`
  (`test(sync): migrate SYNC-036AB preview batch start watchdog`).

### SYNC-036AC Closure Record

- Design and Oracle: the test observer wraps the controller observer before
  presentation. While the controlled batch is blocked, the panel is hidden,
  the exact observation is active with a pending watchdog, and ready/failure
  evidence is empty. After release, only a main-thread callback carrying the
  exact observation and presentation generations, `previewBatchCompleted`,
  zero pending captures, cancelled owner watchdog, visible panel, exact ordered
  windows with images, zero in-flight work, and a nonempty all-succeeded capture
  state readback fulfills. Matching duplicates over-fulfill. Final callback and
  match counts, evidence, window/image/state observations, owner state, panel
  alpha, and snapshot are independent readbacks.
- Lifecycle and retained time policy: the existing compatible one-second
  `windowPreviewEventDelivery` duration is solely a terminal XCTest failure
  bound. The test invocation restores the controller observer, releases the
  semaphore, and cancels the controller/model session from `defer`. The shared
  owner retains generation invalidation and watchdog cancellation.
- Unit, Behavior, and Pressure validation: canonical `behavior-current-003`
  passed 12 tests with 0 failures in 0.179 seconds. The exact asynchronous path
  and policy completed in 0.148 and 0.000 seconds; ten deterministic owner,
  controller-lifecycle, and pressure paths completed in 0.030 seconds. Those
  paths cover initial-ready readback, event completion, duplicate delivery,
  replaced and mismatched generations, cancellation, synchronous watchdog
  delivery, final watchdog-ready readback, watchdog failure diagnostics, and a
  500-cycle readiness/cancellation pressure loop. The first full calibration
  run exposed a capture-state count assumption; a focused diagnostic run showed
  four successful entries because each window preserves its initial and
  resolved-CG cache keys. The Oracle now requires the contract-level nonempty
  all-succeeded state evidence. Machine scheduling can change callback latency
  while generation, readiness, identity, capture state, and panel readbacks
  determine the result. UI is not relevant because production and visible
  behavior are unchanged.
- Process/Tooling and cleanup: the final FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 12 tests. `git diff --check` passes, no direct numeric
  timeout remains in the 765-line focused regression file, and the final log
  emitted no affected-file compiler warning, CFURL no-scheme diagnostic,
  SwiftUI, InputMethodKit, or NSXPC runtime diagnostic. The result bundle
  retains 60 pre-existing build warning summaries from unrelated files. Exact
  final process readback found no FlowTab, UI runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  `.xcresult`, DerivedData, caches, and logs occupy 1.3 GB under
  `.build-local/evidence-driven-sync/SYNC-036AC` for exact post-commit cleanup.
- Commit: `7a87196cbb33f6223b7e60505243b3ad87272eaf`
  (`test(sync): migrate SYNC-036AC initial preview reveal watchdog`).

### SYNC-036AD Closure Record

- Design and Oracle: the controller layout observer is preserved before the
  session starts, and a zero-publication baseline is asserted immediately
  before the exact app/PID workspace-termination trigger. Because the
  controller synchronously reloads and publishes the layout, method return
  must expose exactly one main-thread callback carrying the exact remaining
  ordered app IDs, preferred-next selection, and termination signal. Final
  readbacks independently require the remaining session membership/count,
  exact app/PID signal, a second projection read, cleared pending request,
  cleared terminating identity, and inactive Search state.
- Lifecycle and retained time policy: each test invocation restores the
  controller observer and cancels the controller/model session with `defer`.
  The raw XCTest duration and asynchronous wait are removed; no polling
  cadence, retry, watchdog, or elapsed-time success rule remains.
- Unit and Behavior validation: Unit is not relevant because the production
  synchronous state rule is unchanged and the application behavior boundary is
  the direct contract under test. The canonical `behavior-current-001`
  invocation passed the exact workspace-termination path and terminate-feedback
  entry companion, 2 tests with 0 failures in 0.120 seconds. The paths
  completed in 0.102 and 0.018 seconds. Machine scheduling can change method
  invocation latency while callback count, app/PID identity, ordered session,
  selection, signal, and owner-state readbacks determine the result. UI is not
  relevant because production and visible behavior are unchanged. Pressure is
  not relevant because production scheduling, observers, and hot paths are
  unchanged.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 2 tests. `git diff --check` passes, no direct numeric
  timeout remains in the 319-line file, and the affected file emitted no
  compiler warning. The result bundle retains 77 pre-existing build warning
  summaries from unrelated files. The workspace-termination path emitted four
  known SwiftUI background-publication diagnostics from the unchanged Search
  preference refresh path, matching the four-diagnostic evidence already
  recorded by SYNC-036P, SYNC-036Q, and SYNC-036O. No InputMethodKit, NSXPC, or
  CFURL no-scheme diagnostic was emitted. Exact final process readback found no
  FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable `.xcresult`, DerivedData,
  caches, and logs occupy 460 MB under
  `.build-local/evidence-driven-sync/SYNC-036AD` for exact post-commit cleanup.
- Commit: `4db05a9a84e225bc46e63c34cfbe5f046b9858db`
  (`test(sync): migrate SYNC-036AD termination layout oracle`).

### SYNC-036AE Closure Record

- Design and Oracle: the cleanup expectation is installed before the owner is
  released, after exact active-owner, pending-release, installed-phase,
  zero-cancellation, unavailable-readback, and uncancelled-token baselines.
  Only the main-thread cancellation callback can fulfill, and only after the
  release token reports cancelled. Matching duplicates over-fulfill. Final
  readbacks independently require the weak owner to be absent, exactly one
  cleanup callback, the injection removed, the exact uninstalled/invisible
  readback, the installed-only evidence phase, and the cancelled token.
- Lifecycle and retained time policy: the owner deinitializer transfers the
  token and cleanup mutation to one main-actor task without retaining itself.
  The test invocation owns the expectation. The compatible one-second
  `deinitializationCleanup` duration is retained solely as the terminal XCTest
  failure bound; callback and state evidence determine success. The 260ms
  panel-occlusion staleness interval remains the previously classified
  TestingSupport fault-injection product contract.
- Unit, Behavior, and Pressure validation: Unit is not relevant because the
  deterministic TestingSupport owner and scheduler rules are unchanged. The
  canonical `behavior-current-001` invocation passed 6 tests with 0 failures
  in 0.113 seconds (0.115 seconds total). The policy and deinitialization paths
  completed in 0.002 and 0.109 seconds; initial release, replacement, and
  synchronous release completed in 0.000 seconds each. The same invocation's
  500-generation replacement Pressure path completed in 0.002 seconds and
  continued to reject every force-delivered stale generation. Scheduling can
  change cleanup latency while callback, token, generation, and exact readback
  evidence determine the result. UI is not relevant because production and
  visible launch behavior are unchanged.
- Process/Tooling and cleanup: the FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 6 tests. `git diff --check` passes, no direct numeric
  timeout remains in the single-responsibility 443-line lifecycle test file,
  and that file emitted no compiler warning. The result bundle retains 77
  pre-existing build warning summaries from unrelated files. No SwiftUI,
  InputMethodKit, NSXPC, or CFURL no-scheme runtime diagnostic was emitted.
  Exact final process readback found no FlowTab, UI runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  `.xcresult`, DerivedData, caches, and logs occupy 462 MB under
  `.build-local/evidence-driven-sync/SYNC-036AE` for exact post-commit cleanup.
- Commit: `9c1a793bf697074feca3d27434ee6d460442615d`
  (`test(sync): migrate SYNC-036AE occlusion cleanup watchdog`).

### SYNC-036AF Closure Record

- Design and Oracle: each Combine subscription is installed before `reload()`
  and immediately establishes the exact `[false]` idle baseline. Only a
  main-thread `[true, false]` suffix fulfills, and a second matching transition
  over-fulfills. At completion the callback captures visible app IDs, selected
  app identity, and the hidden count. Final readbacks require exactly one
  `[false, true, false]` sequence plus identical model state for the persisted
  missing-hidden application and Pinyin-search paths.
- Lifecycle and retained time policy: each test invocation explicitly cancels
  its subscription with `defer`; the model continues to own its single reload
  task. The compatible five-second `eventDelivery` duration is retained solely
  as the terminal XCTest failure bound. Loading-transition and final inventory
  evidence determine success.
- Unit and Behavior validation: Unit is not relevant because production reload
  scheduling and state transitions are unchanged. The final canonical
  `behavior-current-003` invocation passed the named policy and both exact
  reload paths, 3 tests with 0 failures in 0.637 seconds (0.640 seconds total).
  Pinyin search, missing-hidden inventory, and policy completed in 0.424,
  0.212, and 0.001 seconds. Scheduling and I/O load can change transition
  latency while the exact loading sequence, inventory identity, selection, and
  hidden-count readbacks determine each result. UI and Pressure are not
  relevant because production behavior, scheduling, observers, and hot paths
  are unchanged.
- Calibration: `behavior-current-001` stopped during test compilation because
  two diagnostic strings escaped the `"nil"` expression inside Swift string
  interpolation. The compiler reported the exact two unmatched interpolation
  and unterminated-string pairs. Correcting those diagnostics passed a direct
  Swift parse before the complete canonical rerun.
- Process/Tooling and cleanup: the final FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 3 tests. `git diff --check` passes, no direct numeric
  timeout remains in the focused 161-line file, and that file emitted no
  compiler warning. The result bundle retains 60 pre-existing build warning
  summaries from unrelated files. No SwiftUI, InputMethodKit, NSXPC, or CFURL
  no-scheme runtime diagnostic was emitted. Exact final process readback found
  no FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Both calibration and final rebuildable
  result bundles, DerivedData, caches, and logs occupy 468 MB under
  `.build-local/evidence-driven-sync/SYNC-036AF` for exact post-commit cleanup.
- Commit: `67c365edf2511204088a43bd96290fe75bd79ba8`
  (`test(sync): migrate SYNC-036AF app visibility watchdogs`).

### SYNC-036AG Closure Record

- Design and Oracle: the cancellation test establishes zero-wait, idle,
  zero-cancel, and empty returned/published evidence baselines before dispatch.
  The controlled waiter atomically records its wait count and active state
  before signalling start. Completion accepts only the exact owner, batch,
  request, policy, and `.cancelled` outcome evidence returned by the owner;
  matching duplicates over-fulfill. Final readbacks require the same published
  evidence, one completed wait, an idle waiter, and exactly one cancellation.
- Lifecycle and retained time policy: the test invocation owns its cancellable
  `DispatchWorkItem`, expectation, and idempotent owner cancellation in
  `defer`; the latency owner releases the active waiter during cancellation.
  The compatible one-second `eventDelivery` duration is retained solely as the
  terminal bound around observable start and exact cancellation delivery. The
  bounded 1,000ms preview-latency setting remains the SYNC-030A intentional
  mock-I/O domain duration; exact preview identity, generation, outcome, and
  cache readbacks continue to determine product success.
- Unit, Behavior, and Pressure validation: Unit is not relevant because the
  existing deterministic production latency owner, monotonic deadline waiter,
  and cancellation rules are unchanged. The canonical
  `behavior-current-001` invocation passed the named policy, exact cancellation,
  policy/evidence, Bootstrap replacement/stop, and replacement Pressure paths,
  5 tests with 0 failures in 0.013 seconds (0.015 seconds total). The exact
  cancellation path completed in 0.000 seconds and the 500-generation
  replacement Pressure path completed in 0.001 seconds. Scheduling and mock
  I/O load can change delivery latency while exact identity, generation,
  outcome, publication, and owner-state evidence determine each result. UI is
  not relevant because the SYNC-030A visible preview path and production
  behavior are unchanged.
- Process/Tooling and cleanup: direct Swift parsing and `git diff --check`
  pass. The FlowTabTests status ledger recorded `stage=completed`, final,
  xcodebuild, and xcodebuild-log exit codes of 0 with a present `.xcresult`;
  the result bundle independently reported `status=succeeded` and 5 tests. No
  direct numeric timeout remains in the focused 429-line lifecycle test file,
  and that file emitted no compiler warning. The result bundle retains 77
  pre-existing build warning summaries from unrelated files. No SwiftUI
  background-publication, InputMethodKit, NSXPC, or CFURL no-scheme runtime
  diagnostic was emitted. Exact final process readback found no FlowTab, UI
  runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable `.xcresult`, DerivedData, caches, and logs occupy 461
  MB under `.build-local/evidence-driven-sync/SYNC-036AG` for exact post-commit
  cleanup.
- Commit: `d851e1ac3bcdaed91b93956ff78db74b32120fff`
  (`test(sync): migrate SYNC-036AG preview latency watchdogs`).

### SYNC-036AH Closure Record

- Design and Oracle: the lifecycle observation and controlled suspension owner
  are installed before dispatch after an exact idle, empty-visit,
  no-completeness, and no-violation baseline. The element-24 event is accepted
  only with phase `suspended`, visited IDs `0...24`, and no transition
  violation. After an explicit resume, completion requires phase `completed`,
  the exact `0..<750` visited-ID set, `.complete(scanned: 750)`, and an empty
  violation list. The immediate zero-deadline non-completion assertion is
  removed; synchronized state and final completeness are the Oracles. The
  suspension owner additionally reads back exactly one resume signal before
  and after repeated cancellation.
- Lifecycle and retained time policy: the test invocation owns the work item
  and cancels it with `defer`. Its suspension owner supplies an idempotent
  resume/cancel operation that releases the controlled barrier on every exit.
  The compatible one-second `eventDelivery` duration is retained solely as the
  terminal bound around observable suspension and completion signals; each
  failure reports the last phase, visit count and range, completeness, and
  transition violations. No polling cadence, retry, or elapsed-time success
  rule remains.
- Unit and Behavior validation: Unit is not relevant because the production
  scan range, completeness rule, and synchronous resolver loop are unchanged.
  The final canonical `behavior-current-002` invocation passed the named policy,
  exact suspended scan, uninterrupted 1,000-ID configured scan, and scan-policy
  companion, 4 tests with 0 failures in 0.003 seconds (0.005 seconds total).
  The suspension and uninterrupted scan paths each completed in 0.001
  seconds. Scheduling can change signal latency while phase transitions,
  visited IDs, and exact completeness determine the result. UI is not relevant
  because production and visible behavior are unchanged. Pressure is not
  relevant because the production hot path, repeated work, and resource
  lifetimes are unchanged; the new observation and barrier exist only in this
  one controlled test invocation.
- Process/Tooling and cleanup: both affected Swift files parse directly,
  `plutil` accepts the project file, and `git diff --check` passes. The
  FlowTabTests status ledger recorded `stage=completed`, final, xcodebuild, and
  xcodebuild-log exit codes of 0 with a present `.xcresult`; the result bundle
  independently reported `status=succeeded` and 4 tests. No direct numeric
  timeout remains in either affected file. The dedicated lifecycle owner is
  296 lines, while extraction reduced the pre-existing oversized topology file
  from 1,116 to 1,078 lines. The new file emitted no compiler warning; the
  result bundle retains 60 pre-existing build warning summaries from unrelated
  files. No SwiftUI background-publication, InputMethodKit, NSXPC, or CFURL
  no-scheme runtime diagnostic was emitted. Exact final process readback found
  no FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable `.xcresult`, DerivedData,
  caches, and logs occupy 462 MB under
  `.build-local/evidence-driven-sync/SYNC-036AH` for exact post-commit cleanup.
- Commit: `96019e0415947d1739ab255676c8f3f8d1f3f131`
  (`test(sync): migrate SYNC-036AH remote AX scan watchdogs`).

### SYNC-036AI Closure Record

- Design and Oracle: the weak terminate-request observation is installed before
  session start after exact idle owner, generation zero, empty schedule, empty
  request, and unprepared-protection baselines. `terminateSelectedApp()` must
  first leave generation 1 pending at the named policy interval, with the
  selected app marked for animation and no request sent. Manually firing that
  exact generation synchronously accepts only the selected-app request on the
  main thread after interruption protection is prepared. Final readbacks
  require owner idle, the exact request app/PID/generation, active protection,
  the lifecycle maintenance request, no termination signal, and a retained
  session after panel resign. A second delivery is rejected and leaves both
  request and protection evidence unchanged.
- Lifecycle and retained time policy: each invocation clears the weak request
  override and cancels the controller in `defer`; controller cancellation owns
  completion-token and interruption-protection cleanup. The 120ms
  `TerminatePressFeedbackPolicy.default.completionInterval` remains the named
  visual press-feedback product contract. The manual scheduler preserves that
  exact interval while event delivery and state readbacks determine test
  success. The raw one-second watchdog and asynchronous fulfillment are
  removed; no polling, retry, or test duration remains.
- Unit, Behavior, and Pressure validation: Unit is not relevant because the
  production policy, completion owner, scheduler, and controller transitions
  are unchanged. The final canonical `behavior-current-002` invocation passed
  the exact protection path, two press-feedback entry companions, completion,
  cancellation, replacement, synchronous-delivery, and request-evidence rules,
  9 tests with 0 failures in 0.162 seconds (0.164 seconds total). The migrated
  protection path completed in 0.014 seconds. The same invocation's
  500-generation replacement/cancellation Pressure path completed in 0.003
  seconds and preserved monotonically ordered accepted generations. Scheduler
  load can change real delivery latency while generation, exact request, and
  owner/protection readbacks determine the result. UI is not relevant because
  production and visible behavior are unchanged.
- Calibration: `behavior-current-001` stopped during test compilation because
  the migrated test initially referenced the source file's private projection
  assertion helper. The compiler reported that protection level and the
  resulting missing maintenance-reason type context. Reusing the dedicated
  entry owner's accessible session-read helper resolved both diagnostics before
  the complete canonical rerun.
- Process/Tooling and cleanup: both affected Swift files parse directly and
  `git diff --check` passes. The final FlowTabTests status ledger recorded
  `stage=completed`, final, xcodebuild, and xcodebuild-log exit codes of 0 with
  a present `.xcresult`; the result bundle independently reported
  `status=succeeded` and 9 tests. No direct numeric timeout remains in either
  affected file; repository inventory now has one direct one-second literal in
  the final independent window-navigation owner. Extraction reduced the
  pre-existing oversized panel file from 2,149 to 2,101 lines, while the
  dedicated press-feedback entry file remains a single-responsibility 470
  lines. Neither affected file emitted a compiler warning. The result bundle
  retains 77 pre-existing build warning summaries from unrelated files. The
  workspace-termination companion emitted four known SwiftUI
  background-publication diagnostics from the unchanged Search preference
  refresh path, matching the four-diagnostic evidence recorded by SYNC-036AD,
  SYNC-036P, SYNC-036Q, and SYNC-036O. No InputMethodKit, NSXPC, or CFURL
  no-scheme diagnostic was emitted. Exact final process readback found no
  FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Calibration and final rebuildable result
  bundles, DerivedData, caches, and logs occupy 467 MB under
  `.build-local/evidence-driven-sync/SYNC-036AI` for exact post-commit cleanup.
- Commit: `e1b7f66c043ad9090f80c7856ed4219d70591d5f`
  (`test(sync): migrate SYNC-036AI termination feedback oracle`).

### SYNC-036AJ Closure Record

- Design and Oracle: each app-window entry point returns the same discardable
  `Task<Void, Never>` that owns its existing main-actor tab-selection and
  activation body. The test installs the activation observer before each
  trigger and establishes the requested case's initial tab plus an empty
  observation baseline. While the current main-actor segment remains active,
  the operation must still be queued with the initial tab unchanged. Awaiting
  the returned operation is the completion evidence; success then requires the
  exact requested tab readback and exactly one main-thread activation that
  observed that tab. Awaiting the completed operation again must leave the
  single observation unchanged.
- Lifecycle and retained time policy: the test invocation retains all returned
  operations, cancels them in `defer`, and restores both the prior global
  activation override and selected tab. Existing production callers may ignore
  the discardable handle, preserving their scheduling and behavior. The raw
  one-second XCTest watchdog and fulfillment are removed. This contract has no
  polling, retry, deadline, or retained duration.
- Behavior validation: the canonical `behavior-current-001` invocation passed
  all three migrated entry points plus status-item activation, visible-panel
  suppression, and temporary-status-item observation companions, 6 tests with
  0 failures in 0.446 seconds (0.447 seconds total). The migrated three-case
  test completed in 0.444 seconds. Unit is not relevant because tab-selection
  and activation rules are unchanged; the new handle only exposes completion
  of the already-existing task. UI is not relevant because the task body and
  visible window behavior are unchanged. Pressure is not relevant because the
  change creates no additional or repeated work and the same single task
  remains the operation owner. Scheduling pressure can change when the task
  completes while operation completion, exact tab state, and activation
  evidence determine the result.
- Process/Tooling and cleanup: all three affected Swift files parse directly,
  `plutil` accepts the project file, and `git diff --check` passes. The
  FlowTabTests status ledger recorded `stage=completed`, final, xcodebuild, and
  xcodebuild-log exit codes of 0 with a present `.xcresult`; the result bundle
  independently reported `status=succeeded` and 6 tests. The migrated contract
  contains no direct numeric timeout. The focused owner is 90 lines;
  extraction reduced the pre-existing oversized mixed test file from 1,027 to
  995 lines, while `AppFoundation.swift` remains a single-responsibility 434
  lines. Affected sources emitted no compiler warning; the result bundle
  retains 77 pre-existing build warning summaries from unrelated files. No
  SwiftUI background-publication, InputMethodKit, NSXPC, or CFURL no-scheme
  runtime diagnostic was emitted. Exact final process readback found no
  FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable `.xcresult`, DerivedData,
  caches, and logs occupy 462 MB under
  `.build-local/evidence-driven-sync/SYNC-036AJ` for exact post-commit cleanup.
- Commit: `8d23286015b22d236ff73d8bbffc9bfc9c0da5bb`
  (`refactor(sync): migrate SYNC-036AJ app window operation evidence`).

### SYNC-036AK Closure Record

- Design and Oracle: the observation owner exposes its current ready snapshot
  without advancing a clock or entering `XCTWaiter`. Every test starts the
  owner before recording evidence and requires an empty initial snapshot. The
  initial-readback case explicitly asks the owner to read its atomic evidence
  envelope. The notification cases synchronously exercise the existing
  notification-triggered readback. Success requires the exact current
  observation generation, configured and ready stages, and `isReady`. One
  hundred stale-generation events leave the current snapshot empty; one
  hundred current-generation duplicates produce the same single terminal
  snapshot.
- Lifecycle and retained time policy: each test invocation cancels the owner in
  `defer`; the owner removes all distributed-notification tokens, clears its
  observation generation, releases the expectation, and removes every
  readback file. The three raw two-second test watchdogs are removed. Real
  cross-process fixture launches retain `workflow.readinessWatchdog`, a named
  launch-derived terminal failure bound whose success Oracle remains the exact
  aggregate snapshot and whose failure includes `diagnosticSummary`.
- UI and Pressure validation: the fixed-path app was rebuilt and signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The final fresh-root `ui-current-003` invocation passed the
  initial readback, notification refresh, and stale/current duplicate paths, 3
  tests with 0 failures in 0.947 seconds (0.949 seconds total). The initial
  readback path completed in 0.557 seconds, the 100-by-100 generation Pressure
  path in 0.211 seconds, and notification refresh in 0.179 seconds. Unit and
  Behavior are not relevant because production state rules and app behavior
  are unchanged; these deterministic contracts belong to the UI-test
  observation owner. A real-topology visible-path run is not relevant because
  the asynchronous fixture wait path and its Oracle are unchanged.
- Environment calibration: `ui-current-001` stopped before any test method
  because macOS timed out while enabling Automation Mode. After the user
  completed the system authentication, `ui-auth-calibration-002` passed all 3
  tests with 0 failures in 2.204 seconds. The final run then passed without
  further authentication on a separate build and output root, so the initial
  result is classified as an environment initialization signal rather than a
  FlowTab assertion failure.
- Process/Tooling and cleanup: both affected Swift files parse directly and
  `git diff --check` passes. No direct numeric timeout remains in either file.
  The remaining UI-target inventory is 520 direct numeric occurrences across
  33 files, so parent SYNC-036 remains open. The observation owner is 322
  lines, and its tests are 244 lines. The final UI status ledger records
  `stage=completed`, every fixture/sign/build/test/log
  exit code as 0, and a present result bundle; the xcresult independently
  reports `status=succeeded` and `testsCount=3`. Affected files emitted no
  warning or error. The build log retains 80 warning lines representing 35
  unrelated pre-existing diagnostics. The successful runner log contains 48
  xcodebuild CFURL no-scheme messages and six CFPrefs shared-memory refresh
  messages, with no SwiftUI background-publication, InputMethodKit, NSXPC, or
  main-thread-checker diagnostic. Exact final process readback found no
  FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Failed-authentication, calibration, and
  final rebuildable result bundles, DerivedData, fixture variants, caches, and
  logs occupy 1.5 GB under
  `.build-local/evidence-driven-sync/SYNC-036AK` for exact post-commit cleanup.
- Commit: `bd27debcaee8822728aa5a3e6327fa3b646d00f3`
  (`test(sync): migrate SYNC-036AK readiness aggregate readbacks`).

### SYNC-036AL Closure Record

- Design and Oracle: the RuntimeLog observation test owner now names its
  ten-second application-readiness watchdog. The shared launch helper performs
  its existing immediate App-state readback and conditional XCUI wait. The
  call site continues only after `.runningForeground`, independently reads
  that exact final state, and reports
  `unmetCondition=applicationState.runningForeground` plus the last App state
  on failure. Only after readiness does the test capture the RuntimeLog file
  baseline; the exact switcher trigger is then accepted through its
  post-baseline completion marker.
- Lifecycle and retained time policy: the ten-second duration remains solely a
  named terminal failure bound owned by
  `FlowTabUITestRuntimeLogObservationTestPolicy`. Its deterministic contract
  requires a finite value no shorter than the same owner’s file-event
  watchdog. The test invocation terminates the App in `defer`; the shared
  RuntimeLog helper owns observation cancellation and file-snapshot cleanup.
  This slice changes no polling cadence, retry, scheduler, timer, or product
  duration.
- UI validation: the fixed-path app was rebuilt and signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The fresh-root `ui-current-001` invocation passed the policy and
  controlled-event contract in 1.160 seconds and the real mock-runtime
  foreground/readback/trigger-delivery path in 4.113 seconds, 2 tests with 0
  failures in 5.273 seconds (5.291 seconds total). Unit and Behavior are not
  relevant because only a UI-test-owned bound and failure diagnostic changed.
  Pressure is not relevant because no observer, repeated work, timer, or hot
  path changed.
- Process/Tooling and cleanup: the affected Swift file parses directly,
  `git diff --check` passes, and no direct numeric timeout remains in the
  697-line single-responsibility test owner. The remaining UI-target inventory
  is 519 direct numeric occurrences across 32 files. The UI status ledger
  records `stage=completed`, every fixture/sign/build/test/log exit code as 0,
  and a present result bundle; the xcresult independently reports
  `status=succeeded` and `testsCount=2`. The affected file emitted no warning
  or error. The build log retains 80 warning lines from unrelated pre-existing
  sources. The successful runner log contains 48 xcodebuild CFURL no-scheme
  messages and five CFPrefs shared-memory refresh messages, with no SwiftUI
  background-publication, InputMethodKit, NSXPC, or main-thread-checker
  diagnostic. Exact final process readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable result bundle, DerivedData, fixture variants, caches, and
  logs occupy 560 MB under
  `.build-local/evidence-driven-sync/SYNC-036AL` for exact post-commit cleanup.
- Commit: `6673d485d23aa892a2b08b1261852bb9440d4b81`
  (`test(sync): migrate SYNC-036AL RuntimeLog readiness watchdog`).

### SYNC-036AM Closure Record

- Design and Oracle: the application-termination policy now names the
  nine-second delayed-evidence override used to verify caller-selected
  watchdog pass-through. The helper still captures the exact initial state,
  requests termination when required, performs an immediate post-trigger
  readback, waits only for `.notRunning`, and performs a final readback. Only
  the final `.notRunning` state satisfies termination; a waiter result alone
  cannot establish success.
- Lifecycle and retained time policy: the existing five-second default and the
  nine-second delayed-evidence override are named terminal failure bounds owned
  by `FlowTabUITestApplicationTerminationPolicy`. The deterministic stub
  records the exact state and timeout argument without advancing elapsed time.
  Each synchronous helper invocation owns its conditional wait and leaves no
  observer, timer, retry, or scheduled work.
- UI and Pressure validation: the fixed-path app was rebuilt and signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The fresh-root `ui-current-001` invocation passed all initially
  stopped, immediate, delayed, wait-boundary, watchdog-diagnostic, and repeated
  lifecycle paths, 6 tests with 0 failures in 2.349 seconds (2.351 seconds
  total). The named override/pass-through path completed in 0.344 seconds; the
  100-target Pressure path completed in 0.386 seconds. Unit and Behavior are
  not relevant because this is a UI-test process-lifecycle policy and
  deterministic target model. A visible application path is not relevant
  because the shared real termination algorithm is unchanged.
- Process/Tooling and cleanup: the affected Swift file parses directly,
  `git diff --check` passes, and no direct numeric timeout remains in the
  313-line owner. The remaining UI-target inventory is 518 direct numeric
  occurrences across 31 files. The UI status ledger records
  `stage=completed`, every fixture/sign/build/test/log exit code as 0, and a
  present result bundle; the xcresult independently reports
  `status=succeeded` and `testsCount=6`. The affected file emitted no warning
  or error. The build log retains 80 warning lines from unrelated pre-existing
  sources. The successful runner log contains 48 xcodebuild CFURL no-scheme
  messages and twelve CFPrefs shared-memory refresh messages, with no SwiftUI
  background-publication, InputMethodKit, NSXPC, or main-thread-checker
  diagnostic. Exact final process readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable result bundle, DerivedData, fixture variants, caches, and
  logs occupy 558 MB under
  `.build-local/evidence-driven-sync/SYNC-036AM` for exact post-commit cleanup.
- Commit: `df4fbf953c924f1847fe406516eb9fb005e08081`
  (`test(sync): migrate SYNC-036AM termination watchdog override`).

### SYNC-036AN Closure Record

- Design and Oracle: the Home-selection test policy now owns the eight-second
  budget watchdog plus its monotonic origin, before-deadline elapsed value, and
  after-deadline elapsed value. The deterministic test injects the monotonic
  clock, requires the exact compatible initial budget, derives the remaining
  value before expiry, and requires a zero clamp after expiry. Selection
  success remains defined only by the exact expected title projection at
  initial, trigger, or scheduled readback.
- Lifecycle and retained time policy: the existing 10ms observation watchdog
  remains a named terminal diagnostic bound, and the eight-second value is a
  named real-path budget fixture exercised without wall-clock waiting. The
  observation owner defers scheduled-readback registration until trigger
  completion; cancellation releases both the downstream schedule and
  generation state. No anonymous polling cadence, retry, or completion delay
  is introduced.
- UI and Pressure validation: the fixed-path app was rebuilt and signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The fresh-root `ui-current-001` invocation passed initial,
  synchronous-trigger, scheduled, cancelled-generation, watchdog-diagnostic,
  and injected-clock budget contracts, 6 tests with 0 failures in 2.539
  seconds (2.541 seconds total). The budget path completed in 0.318 seconds;
  the 100-generation stale/duplicate/cancellation Pressure path completed in
  0.468 seconds. Unit and Behavior are not relevant because production and
  core state rules are unchanged; these deterministic contracts belong to the
  UI-test observation owner. A visible real-topology run is not relevant
  because the selection observation algorithm is unchanged.
- Process/Tooling and cleanup: the affected Swift file parses directly,
  `git diff --check` passes, and no direct numeric timeout remains in the
  263-line owner. The remaining UI-target inventory is 517 direct numeric
  occurrences across 30 files. The UI status ledger records
  `stage=completed`, every fixture/sign/build/test/log exit code as 0, and a
  present result bundle; the xcresult independently reports
  `status=succeeded` and `testsCount=6`. The affected file emitted no warning
  or error. The build log retains 80 warning lines from unrelated pre-existing
  sources. The successful runner log contains 48 xcodebuild CFURL no-scheme
  messages and twelve CFPrefs shared-memory refresh messages, with no SwiftUI
  background-publication, InputMethodKit, NSXPC, or main-thread-checker
  diagnostic. Exact final process readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable result bundle, DerivedData, fixture variants, caches, and
  logs occupy 559 MB under
  `.build-local/evidence-driven-sync/SYNC-036AN` for exact post-commit cleanup.
- Commit: `17e2f9a07cf4bf523be00c0921fee3a29da4904a`
  (`test(sync): migrate SYNC-036AN Home selection budget`).

### SYNC-036AO Closure Record

- Design and Oracle: the Logs-projection policy now owns the five-second tab
  navigation watchdog and eight-second exact-projection watchdog. The
  projection observer starts before the navigation trigger and performs its
  immediate baseline readback. Resolution remains gated until the trigger
  returns and then requires the Logs tab and lines container to exist, the
  exact seeded-row identifier multiplicities and total to match, and every
  prohibited identifier to remain absent. Tab navigation independently
  requires a hittable Logs control.
- Lifecycle and retained time policy: both compatible durations are named
  terminal failure bounds owned by `FlowTabUITestLogsProjectionPolicy`.
  XCTest exposes no accessibility-projection publication callback, so the
  existing shared named serial readback cadence remains the next observation
  opportunity; it never establishes success. Each helper invocation owns the
  observer cancellation and App termination through `defer`. Projection
  watchdog expiry reports the log level, acceptance gate, expected multiset,
  and final exact projection. No retry, product duration, or anonymous delay
  is introduced.
- UI and Pressure validation: the fixed-path app was rebuilt and signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The fresh-root `ui-current-001` invocation passed all four
  runtime-log-level launches plus watchdog compatibility, partial,
  prohibited, multiplicity, exact-row, cancellation, and diagnostic paths, 4
  tests with 0 failures in 24.250 seconds (24.252 seconds total). The real
  four-level visible path completed in 22.999 seconds, the 100-owner lifecycle
  Pressure path in 0.486 seconds, the exact-row contract in 0.360 seconds, and
  policy compatibility in 0.405 seconds. Unit and Behavior are not relevant
  because production state rules and App behavior are unchanged; these
  contracts belong to the UI-test observation owner.
- Process/Tooling and cleanup: both affected Swift files parse directly and
  `git diff --check` passes. No direct numeric timeout remains in the 245-line
  owner or its 237-line tests. The remaining UI-target inventory is 515 direct
  numeric occurrences across 29 files, so parent SYNC-036 remains open. The
  UI status ledger records `stage=completed`, every fixture/sign/build/test/log
  exit code as 0, and a present result bundle; the xcresult independently
  reports `status=succeeded` and `testsCount=4`. Affected files emitted no
  warning or error. The build log retains 80 warning lines representing 35
  unrelated pre-existing file/message diagnostics. The successful runner log
  contains 48 xcodebuild CFURL no-scheme messages and nine CFPrefs
  shared-memory refresh messages, with no SwiftUI background-publication,
  InputMethodKit, NSXPC, or main-thread-checker diagnostic. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable result bundle,
  DerivedData, fixture variants, caches, and logs occupy 558 MB under
  `.build-local/evidence-driven-sync/SYNC-036AO` for exact post-commit cleanup.
- Commit: `65e9496863b18616245ed6d9f0319d7e48ff2b70`
  (`test(sync): migrate SYNC-036AO Logs projection watchdogs`).

### SYNC-036AP Closure Record

- Design and Oracle: the Launch at Login Settings owner now names its
  five-second toggle-publication watchdog. Settings-tab navigation completes
  through the shared exact tab-content evidence first. Each toggle wait then
  performs its immediate XCUI existence readback and accepts only `exists`.
  The behavior path independently requires the reset launch to read off, the
  post-tap state to read on, and the relaunch state to read the persisted on
  value.
- Lifecycle and retained time policy: the compatible duration remains solely
  a named terminal failure bound owned by
  `FlowTabUITestLaunchAtLoginSettingsPolicy`. Failure records the first-launch
  or relaunch phase, final existence, and final accessible value. The XCTest
  invocation owns process termination and user-default restoration through
  suite teardown. No observer, timer, retry, scheduler, polling cadence, or
  product duration changes.
- UI validation: the fixed-path app was rebuilt and signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The fresh-root `ui-current-001` invocation passed watchdog
  compatibility and the real reset/off, tap/on, terminate, relaunch, and
  persisted/on path, 2 tests with 0 failures in 17.180 seconds (17.183 seconds
  total). The policy contract completed in 0.810 seconds and the visible
  persistence path in 16.370 seconds. Unit and Behavior are not relevant
  because production state rules and Launch at Login service behavior are
  unchanged. Pressure is not relevant because this slice changes no observer,
  repeated asynchronous work, timer, or hot path.
- Process/Tooling and cleanup: the affected 110-line Swift file parses
  directly, `git diff --check` passes, and no direct numeric timeout remains.
  The remaining UI-target inventory is 513 direct numeric occurrences across
  28 files, so parent SYNC-036 remains open. The UI status ledger records
  `stage=completed`, every fixture/sign/build/test/log exit code as 0, and a
  present result bundle; the xcresult independently reports
  `status=succeeded` and `testsCount=2`. The affected file emitted no warning
  or error. The build log retains 80 warning lines representing 35 unrelated
  pre-existing file/message diagnostics. The successful runner log contains
  48 xcodebuild CFURL no-scheme messages and five CFPrefs shared-memory refresh
  messages, with no SwiftUI background-publication, InputMethodKit, NSXPC, or
  main-thread-checker diagnostic. Exact final process readback found no
  FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable result bundle, DerivedData,
  fixture variants, caches, and logs occupy 558 MB under
  `.build-local/evidence-driven-sync/SYNC-036AP` for exact post-commit cleanup.
- Commit: `f456aee744d8c9448eb7146592e0a917ca833d7a`
  (`test(sync): migrate SYNC-036AP Launch at Login watchdog`).

### SYNC-036AQ Closure Record

- Design and Oracle: the shared Search-confirmation owner now freezes a
  runtime-log baseline and installs file-event plus serial-readback observation
  before Return. Its initial readback accepts exact Search-input disappearance
  or the post-baseline marked-text-clear marker for the exact current query,
  with disappearance taking priority. A second Return is authorized only by
  marked-text-clear evidence and only after a new dismissal-only observer has
  started; that observer's initial readback suppresses the input if dismissal
  already completed. Standard, multi-app, fullscreen, window-title, and
  punctuation/Unicode edge-title workflows retain their exact result and
  activation Oracles.
- Lifecycle and retained time policy: the compatible four-second duration is
  a named terminal failure bound owned by
  `FlowTabUITestSwitcherSearchConfirmationPolicy`. XCTest exposes no Search
  input dismissal callback, so the shared runtime-log file event and named
  cancellable 200ms serial readback provide observation opportunities; elapsed
  cadence never establishes success. Each helper invocation owns both
  observation generations, their cancellation, and the runtime-log baseline.
  Watchdog failure reports the unmet phase, exact expected marker, final input
  existence, file-event generations, wait result, and bounded log tail.
- UI and Pressure validation: the fixed-path app and runner were signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The final deterministic invocation passed policy,
  dismissal-priority, exact-query rejection, initial-readback cancellation,
  delayed scheduled readback, watchdog final evidence, and lifecycle Pressure,
  6 tests with 0 failures in 1.371 seconds (1.373 seconds total). The
  100-generation stale/duplicate/cancellation path completed in 0.186 seconds,
  and the scheduled-readback path in 0.184 seconds. The standard visible path
  passed 1/1 in 26.655 seconds. A clean real-topology invocation then passed app
  Search, fullscreen cross-Space activation, ordinary window activation, and
  the Unicode/punctuation edge title, 4 tests with 0 failures in 121.968 seconds
  (121.972 seconds total). Unit and Behavior are not relevant because this
  slice changes only the UI-test synchronization owner and its representative
  callers; product and core state transitions are unchanged.
- Calibration evidence: the first deterministic attempt passed 5/6 and exposed
  that `XCTWaiter.Result` has no stable platform spelling; the final diagnostic
  contract therefore requires the stable `waitResult=` field and passed. In the
  first five-path topology group, the standard workflow stopped at the existing
  fixture-readiness PID readback before Search while the other four paths
  passed. In the second group, the standard path passed and app Search stopped
  at its existing exact-result publication assertion before the confirmation
  helper while the remaining three paths passed. The isolated standard result
  and final four-path result provide clean exit-code-zero coverage for every
  affected visible path without changing either prerequisite contract.
- Process/Tooling and cleanup: all affected Swift files parse directly, the
  project file passes `plutil -lint`, and `git diff --check` passes. The three
  duplicated 1.2-second decision waits and their three raw terminal waits are
  removed. The remaining app/UI test inventory is 507 direct numeric
  timeout/deadline occurrences across 27 files, so parent SYNC-036 remains
  open. The deterministic, standard, and four-path status ledgers all record
  `stage=completed`, zero required child exit codes, and present result bundles;
  xcresult independently reports `status=succeeded` with test counts 6, 1, and
  4. The fresh build retained 80 pre-existing warning lines. New observation
  files emitted no warning; the one warning in a touched workflow file is at
  an unchanged line outside this slice. The three clean runner logs contain 80
  xcodebuild CFURL no-scheme messages and 27 CFPrefs shared-memory refresh
  messages, with no SwiftUI background-publication, InputMethodKit, NSXPC, or
  main-thread-checker diagnostic. Exact final process readback found no FlowTab,
  UI runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable result bundles, DerivedData, fixture variants, caches,
  and logs occupy 1.8 GB under
  `.build-local/evidence-driven-sync/SYNC-036AQ` for exact post-commit cleanup.
- Commit: `97c9f294f8b43bb4f00e191c749308fc38631b08`
  (`refactor(sync): migrate SYNC-036AQ search confirmation evidence`).

### SYNC-036AR Closure Record

- Design and Oracle: `TabSwitchStressUITestPolicy` now owns the compatible
  three measured launches, two-second workload, and 16ms switch cadence. It
  derives the launch arguments, nanosecond evidence, and 125 required switches
  from that single contract. The unique distributed-notification observer
  starts before each launch and accepts only completed evidence containing the
  exact policy, 125 attempts, 125 readback-confirmed switches, matching
  requested/observed Logs tab, elapsed duration, and both satisfied terminal
  flags. Foreground readiness and natural exit are independently accepted only
  by final `.runningForeground` and `.notRunning` state readbacks; waiter
  results remain diagnostic evidence.
- Lifecycle and retained time policy: two seconds and 16ms remain named domain
  durations for the Pressure protocol. Five-second readiness, ten-second
  completion-evidence, and ten-second natural-termination values are named
  terminal failure bounds. Each measurement invocation creates its own route,
  observation owner, and App handle, installs cancellation before launch, and
  cancels observation on scope exit. A deferred shared exact-state termination
  helper cleans up any failed launch or workload while an already stopped App
  requires no action. Completion success never comes from elapsed watchdog
  time.
- UI and Pressure validation: the fixed-path App and final runner were signed
  by `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The final `ui-current-002` invocation passed the deterministic
  policy contract and the three-iteration real workload, 2 tests with 0
  failures in 59.619 seconds (59.622 seconds total). The policy contract took
  0.264 seconds and the real Pressure path 59.355 seconds. Its measured
  monotonic values were 9.531794, 9.273422, and 9.430734 seconds; every launch
  still required and reported 125/125 exact switches plus natural process
  exit. Unit and Behavior are not relevant because production, TestingSupport,
  and core workload rules remain unchanged; the modified contract belongs to
  the UI-test Pressure owner.
- Process/Tooling and cleanup: both affected Swift files parse directly, the
  project file passes `plutil -lint`, `git diff --check` passes, and the sources
  remain within the 400-line guardrail at 398 and 104 lines. All three raw
  timeout arguments are removed from the owner. The remaining app/UI test
  inventory is 504 direct numeric timeout/deadline occurrences across 26
  files, so parent SYNC-036 remains open. The status ledger records
  `stage=completed`, every required fixture/sign/build/test/log exit code as 0,
  and a present result bundle; xcresult independently reports
  `status=succeeded` and `testsCount=2`. The final incremental build emitted no
  warning or error in either affected file; the fresh initial build retained
  80 warning lines from unrelated pre-existing sources. The final runner log
  contains sixteen xcodebuild CFURL no-scheme messages and five CFPrefs
  shared-memory refresh messages, with no SwiftUI background-publication,
  InputMethodKit, NSXPC, or main-thread-checker diagnostic. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable result bundles,
  DerivedData, fixture variants, caches, and logs occupy 804 MB under
  `.build-local/evidence-driven-sync/SYNC-036AR` for exact post-commit cleanup.
- Commit: `dc1d91f37a1349a3efdd77a8005cbaee9f0e609c`
  (`test(sync): migrate SYNC-036AR tab stress policy`).

### SYNC-036AS Closure Record

- Design and Oracle: `FlowTabUITestHomePostlaunchAppSummaryPolicy` now owns
  compatible terminal bounds for Home-tab navigation, application-row
  publication, and exact window-count publication. Navigation succeeds only
  from a hittable Home-tab candidate. Each workflow application then requires
  the exact accessibility identifier to exist and the exact configured
  `"<windowCount>w"` value to be readable. The XCTest wait result is retained
  only as diagnostic evidence; a final existence readback decides row
  publication. Failure evidence includes candidate count, final
  existence/hittability, exact identifier, waiter result, and accessible
  value.
- Lifecycle and retained time policy: ten seconds for Home-tab navigation and
  20 seconds each for application-row and exact-value publication remain named
  terminal failure bounds. The shared hittability and value owners perform an
  immediate readback before their cancellable serial observation and cancel on
  scope exit. The real-space workflow invocation owns FlowTab and fixture App
  teardown. Elapsed watchdog time never establishes navigation, publication,
  or the window count.
- UI and Pressure validation: the fixed-path App and final runner were signed
  by `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The final `ui-current-002` invocation passed the deterministic
  policy contract and the affected real three-application postlaunch Home
  projection, 2 tests with 0 failures in 24.720 seconds (24.722 seconds total).
  The real-topology path took 24.506 seconds and the policy test 0.214 seconds.
  Unit and Behavior are not relevant because this slice changes only the
  UI-test synchronization policy and diagnostics. Pressure is not relevant
  because it adds no observer, timer, repeated asynchronous work, or hot-path
  behavior; the existing shared observation owners remain unchanged.
- Calibration evidence: the first signed invocation passed both tests, with
  the real path taking 26.602 seconds and the policy test 0.418 seconds, and
  exposed one warning in the new failure diagnostic because its value helper
  already returns a nonoptional string. The final diagnostic records that
  value through `debugDescription`; the final incremental build emitted no
  warning or error.
- Process/Tooling and cleanup: the affected Swift source parses directly,
  `git diff --check` passes, remains within the 400-line guardrail, and contains
  no direct numeric timeout. The remaining app/UI test inventory is 501 direct
  numeric timeout/deadline occurrences across 25 files, so parent SYNC-036
  remains open. The final status ledger records `stage=completed`, every
  required fixture/sign/build/test/log exit code as 0, and a present result
  bundle; xcresult independently reports `status=succeeded` and
  `testsCount=2`. The final runner log contains five CFPrefs shared-memory
  refresh messages, with no CFURL no-scheme, SwiftUI background-publication,
  InputMethodKit, NSXPC, or main-thread-checker diagnostic. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable result bundle,
  DerivedData, fixture variants, caches, and logs occupy 804 MB under
  `.build-local/evidence-driven-sync/SYNC-036AS` for exact post-commit cleanup.
- Commit: `930f32e55c0bcb1e8ecdb845a25fc59f6b596864`
  (`test(sync): migrate SYNC-036AS Home postlaunch watchdogs`).

### SYNC-036AT Closure Record

- Design and Oracle: the AX-suppression real-topology test preserves its exact
  foreground/background guard, then delegates active FlowTab and fixture
  termination to the shared application-termination evidence owner. That owner
  reads state before the request and immediately after it, conditionally waits
  for `.notRunning`, and performs a final state readback. Only the final
  `.notRunning` state establishes success; the waiter Boolean is diagnostic.
  Failure identifies the exact FlowTab or workflow-app target and reports the
  initial, post-trigger, and final states plus whether termination and waiting
  occurred.
- Lifecycle and retained time policy: the compatible five-second duration is
  the shared termination owner's named terminal failure bound. The
  real-topology invocation owns prelaunch fixture cleanup and both deferred
  teardown calls. The synchronous shared helper owns each wait and returns its
  complete evidence before the caller continues. Applications outside the
  existing foreground/background boundary retain their prior no-action
  behavior.
- UI and Pressure validation: the fixed-path App and runner were signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The first signed invocation passed all six shared termination
  paths and the affected real AX-suppression topology, 7 tests with 0 failures
  in 21.334 seconds (21.342 seconds total). Initial-state, immediate,
  delayed-state, boundary-readback, and watchdog-diagnostic cases all passed.
  The 100-iteration repeated lifecycle Pressure case completed in 0.176
  seconds, and the real topology completed in 19.268 seconds with exact fixture
  and FlowTab termination. Unit and Behavior are not relevant because this
  slice changes only a UI-test process-lifecycle caller; the shared evidence
  rules remain unchanged.
- Process/Tooling and cleanup: the affected 136-line Swift source parses
  directly and `git diff --check` passes. Its raw termination timeout is
  removed; the independent readiness and suppression bounds remain for later
  slices. The remaining app/UI test inventory is 500 direct numeric
  timeout/deadline occurrences across 25 files, so parent SYNC-036 remains
  open. The status ledger records `stage=completed`, every required
  fixture/sign/build/test/log exit code as 0, and a present result bundle;
  xcresult independently reports `status=succeeded` and `testsCount=7`. The
  fresh build retains 80 warning lines from unrelated pre-existing sources and
  the affected file emits none. The runner log contains 48 xcodebuild CFURL
  no-scheme messages and fifteen CFPrefs shared-memory refresh messages, with
  no SwiftUI background-publication, InputMethodKit, NSXPC, or
  main-thread-checker diagnostic. Exact final process readback found no
  FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable result bundle, DerivedData,
  fixture variants, caches, and logs occupy 804 MB under
  `.build-local/evidence-driven-sync/SYNC-036AT` for exact post-commit cleanup.
- Commit: `e694637639f67b2d9c6a6195dd5d33e2b9f75577`
  (`test(sync): migrate SYNC-036AT AX suppression termination evidence`).

### SYNC-036AU Closure Record

- Design and Oracle: the AX-suppression real-topology caller now uses the
  shared support policy for FlowTab readiness. The readiness helper checks the
  exact application state immediately, waits for `.runningForeground`, and
  retains its installed-App activation fallback before a second exact-state
  wait. Only `.runningForeground` evidence establishes readiness. The caller
  records the final `XCUIApplication.State` when the readiness watchdog
  expires before permission or fixture triggers can run.
- Lifecycle and retained time policy: the compatible 12-second duration is
  `FlowTabUITestSupportWatchdogPolicy.foregroundActivation`, a terminal failure
  bound shared by App activation and real-topology readiness. The existing
  four-second fallback activation cap remains owned by the same policy. Both
  waits are synchronous exact-state observations with no timer or recurring
  work to cancel. The test invocation owns deferred FlowTab termination.
- UI validation: the fixed-path App and runner were signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The first signed invocation passed the shared watchdog-policy
  compatibility test and the affected real AX-suppression topology, 2 tests
  with 0 failures in 18.970 seconds (18.972 seconds total). The real readiness
  and topology path took 18.729 seconds and the policy test 0.241 seconds.
  Unit and Behavior are not relevant because this slice changes only a UI-test
  caller. Pressure is not relevant because no observer, timer, repeated
  asynchronous work, or hot-path behavior changes.
- Process/Tooling and cleanup: the affected 142-line Swift source parses
  directly and `git diff --check` passes. Its raw readiness timeout is removed;
  the independent suppression bound remains for the next slice. The remaining
  app/UI test inventory is 499 direct numeric timeout/deadline occurrences
  across 25 files, so parent SYNC-036 remains open. The status ledger records
  `stage=completed`, every required fixture/sign/build/test/log exit code as 0,
  and a present result bundle; xcresult independently reports
  `status=succeeded` and `testsCount=2`. The fresh build retains 80 warning
  lines from unrelated pre-existing sources and the affected file emits none.
  The runner log contains 48 xcodebuild CFURL no-scheme messages and five
  CFPrefs shared-memory refresh messages, with no SwiftUI
  background-publication, InputMethodKit, NSXPC, or main-thread-checker
  diagnostic. Exact final process readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable result bundle, DerivedData, fixture variants, caches, and
  logs occupy 802 MB under `.build-local/evidence-driven-sync/SYNC-036AU` for
  exact post-commit cleanup.
- Commit: `55fe5e25f459e92ccb8b32e7d183600e439228bb`
  (`test(sync): migrate SYNC-036AU AX suppression readiness watchdog`).

### SYNC-036AV Closure Record

- Design and Oracle: `SpaceFixtureAXSuppressionObservationPolicy` now owns the
  compatible resolution watchdog used by both the direct authorized-readback
  test and the shared multi-application workflow. Each route owner registers
  suppression-completion and authorized external-AX observers before fixture
  launch and performs an initial readback. Success requires matching current
  generations, bundle identifier, PID, configured window count, source
  generation, producer counts, successful external AX reads, a running target
  process, and zero externally exposed application children and windows.
  Notification order, waiter completion, and elapsed time never establish
  suppression independently.
- Lifecycle and retained time policy: 20 seconds remains a named terminal
  failure bound owned by the shared suppression observation policy. Every
  `start()` cancels the previous generation, starts a route owner for each
  exact notification route, and each invocation cancels the aggregate owner
  from `defer`; cancellation removes both distributed observers. Monotonic
  completion and verification generations reject duplicate or regressed
  evidence, while owner generations reject stale callbacks. The final
  watchdog readback cannot resolve the condition and reports the last paired
  or partial evidence, observation source, generation, and waiter result. No
  polling cadence is used.
- UI and Pressure validation: the fixed-path App and runner were signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The first signed invocation passed policy compatibility,
  synchronous registration, out-of-order evidence, watchdog diagnostics, the
  100-route cancelled/stale/duplicate/regressed lifecycle Pressure case, the
  direct authorized-readback topology, and the multi-application Control+Tab
  representative workflow, 7 tests with 0 failures in 50.670 seconds (50.676
  seconds total). Pressure completed in 0.254 seconds, the direct topology in
  18.329 seconds, and the multi-application path in 30.398 seconds. Unit and
  Behavior are not relevant because this slice changes the UI-test observation
  policy and its callers rather than product or core state transitions.
- Process/Tooling and cleanup: all four affected Swift sources parse directly
  and `git diff --check` passes. The shared owner, deterministic tests, and
  direct topology files remain within the 400-line guardrail at 278, 365, and
  141 lines; the pre-existing oversized multi-application orchestrator shrinks
  to 1,077 lines. Both raw suppression timeouts are removed. The remaining
  app/UI test inventory is 497 direct numeric timeout/deadline occurrences
  across 24 files, so parent SYNC-036 remains open. The status ledger records
  `stage=completed`, every required fixture/sign/build/test/log exit code as 0,
  and a present result bundle; xcresult independently reports
  `status=succeeded` and `testsCount=7`. The fresh build retains 80 warning
  lines from unrelated pre-existing sources and all affected files emit none.
  The runner log contains 48 xcodebuild CFURL no-scheme messages and sixteen
  CFPrefs shared-memory refresh messages, with no SwiftUI
  background-publication, InputMethodKit, NSXPC, or main-thread-checker
  diagnostic. Exact final process readback found no FlowTab, UI runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable result bundle, DerivedData, fixture variants, caches, and
  logs occupy 804 MB under `.build-local/evidence-driven-sync/SYNC-036AV` for
  exact post-commit cleanup.
- Commit: `fbdaa7219d903c9abd8007d0dbf4c0f38d344e19`
  (`test(sync): migrate SYNC-036AV AX suppression watchdog`).

### SYNC-036AW Closure Record

- Design and Oracle: `FlowTabUITestSearchSelectionWrapPolicy` now owns the
  compatible terminal bound for every ordered Down transition and the final
  last-to-first wrap. Before each key trigger, the shared diagnostics owner
  starts observation and performs an initial readback while post-trigger
  acceptance remains disabled. The trigger then enables acceptance and causes
  an immediate readback. Success requires the percent-decoded
  `searchSelectedResult` to equal each exact
  `app:com.flowtab.mock.wrap.01...10` identity in order and finally the exact
  `.01` identity after wrap.
- Lifecycle and retained time policy: three seconds remains a named terminal
  failure bound owned by the Search-wrap test. XCUI exposes no value-change
  notification for the diagnostics element, so the shared condition owner
  retains its named 100ms serial readback cadence after the immediate trigger
  readback. Cadence supplies observation opportunities only. Each helper
  invocation owns scheduler and observer cancellation through `defer`, and
  generation checks reject stale callbacks. Watchdog failure performs a final
  non-resolving readback and reports expected key/value, element existence,
  raw/value map, source, generation, and waiter result.
- UI and Pressure validation: the fixed-path App and runner were signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The first signed invocation passed policy compatibility,
  percent decoding, post-trigger gating, delayed scheduled readback, watchdog
  diagnostics, 100-generation stale-event Pressure, and the affected visible
  Search-wrap path, 7 tests with 0 failures in 21.962 seconds (21.971 seconds
  total). The visible path completed all eleven exact transitions in 20.700
  seconds, and Pressure completed in 0.194 seconds. Unit and Behavior are not
  relevant because this slice changes only a UI-test caller policy; the shared
  diagnostics owner and product selection rules remain unchanged.
- Process/Tooling and cleanup: the affected 116-line Swift source parses
  directly and `git diff --check` passes. Both raw diagnostics-transition
  timeouts are removed; diagnostics existence and result hittability remain
  independent later slices. The remaining app/UI test inventory is 495 direct
  numeric timeout/deadline occurrences across 24 files, so parent SYNC-036
  remains open. The status ledger records `stage=completed`, every required
  fixture/sign/build/test/log exit code as 0, and a present result bundle;
  xcresult independently reports `status=succeeded` and `testsCount=7`. The
  fresh build retains 80 warning lines from unrelated pre-existing sources and
  the affected file emits none. The runner log contains 48 xcodebuild CFURL
  no-scheme messages and fifteen CFPrefs shared-memory refresh messages, with
  no SwiftUI background-publication, InputMethodKit, NSXPC, or
  main-thread-checker diagnostic. Exact final process readback found no
  FlowTab, UI runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable result bundle, DerivedData,
  fixture variants, caches, and logs occupy 802 MB under
  `.build-local/evidence-driven-sync/SYNC-036AW` for exact post-commit cleanup.
- Commit: `e24f0b17751fc48195ddae05dcf17677f2d90f48`
  (`test(sync): migrate SYNC-036AW Search wrap transitions`).

### SYNC-036AX Closure Record

- Design and Oracle: `FlowTabUITestSearchSelectionWrapPolicy` now owns the
  compatible diagnostics-summary publication bound. The existing
  `waitForExistence` condition performs the initial accessibility readback and
  accepts only exact element existence, including when the summary is already
  published. Timeout failure now reports the unmet `expectedExists=true`
  condition, final existence readback, and exact accessibility identifier.
  Ordered selection transitions and result hittability remain separate
  synchronization contracts.
- Lifecycle and retained time policy: five seconds remains a named terminal
  failure bound owned by the Search-wrap test. XCTest owns the platform
  observation, cancellation, and synchronous return of `waitForExistence`; no
  custom observer, scheduler, polling cadence, timer, or retry was introduced.
  Scheduling and accessibility-server load can extend completion only until
  the terminal failure bound, while success remains determined solely by the
  exact existence evidence.
- UI validation: the fixed-path App and runner were signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The first signed invocation passed the expanded policy
  compatibility test and the affected visible Search-wrap path, 2 tests with
  0 failures in 20.903 seconds (20.905 seconds total). The visible path passed
  in 20.684 seconds and the policy test in 0.219 seconds. Unit and Behavior are
  not relevant because this slice changes only a UI-test caller policy.
  Pressure is not relevant because the one-shot XCTest-managed existence wait
  adds no observer, repeated asynchronous work, timer, custom scheduler, or
  hot path.
- Process/Tooling and cleanup: the affected 132-line Swift source parses
  directly and `git diff --check` passes. The raw diagnostics-publication
  timeout is removed; two result-hittability bounds remain a separate later
  slice. The remaining app/UI test inventory is 494 direct numeric
  timeout/deadline occurrences across 24 files, so parent SYNC-036 remains
  open. The status ledger records `stage=completed`, every fixture, signing,
  build, test, and log exit code as 0, and a present result bundle; xcresult
  independently reports `status=succeeded` and `testsCount=2`. The fresh build
  retains 80 warning lines from unrelated pre-existing sources, fixture
  preparation emits one destination warning, and the affected source emits
  none. The runner log contains 48 xcodebuild CFURL no-scheme messages and five
  CFPrefs shared-memory refresh messages, with no SwiftUI
  background-publication, InputMethodKit, NSXPC, main-thread-checker, sanitizer,
  or fatal diagnostic. Exact final process readback found no FlowTab, UI
  runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable result bundle, DerivedData, fixture variants, caches,
  and logs occupy 803 MB under
  `.build-local/evidence-driven-sync/SYNC-036AX` for exact post-commit cleanup.
- Commit: `14062289e7430da373a4c5a373b7a536e934b425`
  (`test(sync): migrate SYNC-036AX Search diagnostics publication`).

### SYNC-036AY Closure Record

- Design and Oracle: `FlowTabUITestSearchSelectionWrapPolicy` now owns
  compatible phase-specific bounds for the exact last result after ten Down
  transitions and the exact first result after wrap. Both phases bind one
  exact accessibility-identifier query and succeed only when the shared helper
  reads an existing, hittable candidate. Caller failure diagnostics add the
  expected identifier plus final candidate count, existence, and hittability
  to the shared owner's candidate indices, generation, source, and waiter
  evidence.
- Lifecycle and retained time policy: two seconds remains the terminal bound
  for the already-selected last result and five seconds remains the terminal
  bound for the first result after the wrap and scroll transition. XCUI exposes
  no candidate-publication callback, so the shared owner registers its named
  cancellable serial 100ms readback schedule before the immediate readback.
  Each helper invocation owns cancellation through `defer`; generation checks
  reject cancelled and replaced callbacks. Cadence controls observation
  opportunities only, and success remains the exact existence-plus-hittability
  condition.
- UI and Pressure validation: the fixed-path App and runner were signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The first signed invocation passed policy compatibility,
  initial exact-candidate evidence, scheduled readback and watchdog evidence,
  the 500-generation cancelled/replaced Pressure case, the 100-lifecycle
  serial-scheduler Pressure case, and the affected visible Search-wrap path,
  6 tests with 0 failures in 22.644 seconds (22.657 seconds total). The visible
  path passed in 19.955 seconds; the two Pressure cases passed in 0.434 and
  0.307 seconds. Unit and Behavior are not relevant because this slice changes
  only a UI-test caller policy and preserves the shared observer and product
  selection behavior.
- Process/Tooling and cleanup: the affected 156-line Swift source parses
  directly and `git diff --check` passes. Both raw result-hittability bounds
  are removed, closing every direct numeric timeout/deadline in this file. The
  remaining app/UI test inventory is 492 occurrences across 23 files, so
  parent SYNC-036 remains open. The status ledger records `stage=completed`,
  every fixture, signing, build, test, and log exit code as 0, and a present
  result bundle; xcresult independently reports `status=succeeded` and
  `testsCount=6`. The fresh build retains 80 warning lines from unrelated
  pre-existing sources, fixture preparation emits one destination warning,
  and the affected source emits none. The runner log contains 48 xcodebuild
  CFURL no-scheme messages and thirteen CFPrefs shared-memory refresh messages,
  with no SwiftUI background-publication, InputMethodKit, NSXPC,
  main-thread-checker, sanitizer, or fatal diagnostic. Exact final process
  readback found no FlowTab, UI runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable result bundle,
  DerivedData, fixture variants, caches, and logs occupy 802 MB under
  `.build-local/evidence-driven-sync/SYNC-036AY` for exact post-commit cleanup.
- Commit: `8291807572001aa3522afe8b480791eef9acd370`
  (`test(sync): migrate SYNC-036AY Search result hittability`).

### SYNC-036AZ Closure Record

- Design and Oracle: `FlowTabUITestHomeActivationPolicy` now owns the
  compatible Home-tab navigation bound for the real window-activation
  workflow. The exact Home-tab identifier query is captured once and success
  requires the shared helper to read an existing, hittable candidate. Failure
  now combines the shared owner's candidate indices, generation, source, and
  waiter evidence with final query count, existence, and hittability. Fixture
  bundle identity, Home row identity, CGWindowID parsing, and exact frontmost
  window evidence remain unchanged downstream Oracles.
- Lifecycle and retained time policy: ten seconds remains a named terminal
  failure bound owned by the Home-activation scenario. XCUI exposes no
  tab-publication callback, so the shared owner registers its named
  cancellable serial 100ms readback schedule before an immediate exact
  candidate readback. The helper owns cancellation through `defer`, generation
  checks reject stale callbacks, and the real workflow invocation owns FlowTab
  plus fixture teardown. Cadence supplies readback opportunities; exact
  existence and hittability alone decide success.
- UI validation: the fixed-path App and runner were signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The first signed invocation passed policy compatibility and the
  affected real three-application Home workflow, 2 tests with 0 failures in
  28.081 seconds (28.085 seconds total). The real topology passed in 27.449
  seconds, including exact Chrome application-row selection, exact `Mail`
  Home-window identity, CGWindowID extraction, and frontmost-window readback.
  Unit and Behavior are not relevant because this slice changes only a UI-test
  caller policy. Additional Pressure is not required for this caller-only
  change; the unchanged shared hittability owner retains its deterministic and
  lifecycle Pressure coverage.
- Process/Tooling and cleanup: the affected 189-line Swift source parses
  directly and `git diff --check` passes. The raw Home-navigation bound is
  removed; App-row publication, Home-window projection, and exact activation
  bounds remain separate later slices. The remaining app/UI test inventory is
  491 direct numeric timeout/deadline occurrences across 23 files, so parent
  SYNC-036 remains open. The status ledger records `stage=completed`, every
  fixture, signing, build, test, and log exit code as 0, and a present result
  bundle; xcresult independently reports `status=succeeded` and `testsCount=2`.
  The fresh build retains 80 warning lines from unrelated pre-existing
  sources, fixture preparation emits one destination warning, and the affected
  source emits none. The runner log contains 48 xcodebuild CFURL no-scheme
  messages and five CFPrefs shared-memory refresh messages, with no SwiftUI
  background-publication, InputMethodKit, NSXPC, main-thread-checker, sanitizer,
  or fatal diagnostic. Exact final process readback found no FlowTab, UI
  runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable result bundle, DerivedData, fixture variants, caches,
  and logs occupy 803 MB under
  `.build-local/evidence-driven-sync/SYNC-036AZ` for exact post-commit cleanup.
- Commit: `9dd6ea0071618f59cfad0f26cc4125b85754f7bf`
  (`test(sync): migrate SYNC-036AZ Home activation navigation`).

### SYNC-036BA Closure Record

- Design and Oracle: `FlowTabUITestHomeActivationPolicy` now owns the
  compatible fixture-App row publication bound. The caller binds the exact
  bundle-derived Home accessibility identifier and accepts only XCTest's
  element-existence evidence. Failure now reports the target App name, exact
  expected identifier, final existence, and final hittability. Downstream
  Home-window and CGWindow identity rules remain unchanged.
- Lifecycle and retained time policy: 20 seconds remains a named terminal
  failure bound owned by the Home-activation scenario. `waitForExistence`
  performs the initial readback, covering a row published during FlowTab
  launch, and XCTest owns the subsequent platform wait plus cancellation. The
  real workflow invocation owns FlowTab and all fixture teardown. The bound
  cannot produce success; the exact `exists` condition is required.
- UI validation: the fixed-path App and runner were signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The first signed invocation passed the expanded policy
  compatibility test and the affected real three-application Home workflow,
  2 tests with 0 failures in 30.675 seconds (30.677 seconds total). The real
  topology passed in 30.013 seconds. Its exact Chrome row was absent at the
  first XCTest observation opportunity and present at the next, after which
  the same exact `Mail` CGWindow activation completed; delayed publication
  therefore changed completion time while preserving the result. Unit and
  Behavior are not relevant because this slice changes only a UI-test caller
  policy. Pressure is not relevant for the one-shot XCTest-managed existence
  wait, which adds no custom observer, scheduler, timer, retry, or hot path.
- Process/Tooling and cleanup: the affected 216-line Swift source parses
  directly and `git diff --check` passes. The raw App-row publication bound is
  removed; Home-window projection and exact activation bounds remain separate
  later slices. The remaining app/UI test inventory is 490 direct numeric
  timeout/deadline occurrences across 23 files, so parent SYNC-036 remains
  open. The status ledger records `stage=completed`, every fixture, signing,
  build, test, and log exit code as 0, and a present result bundle; xcresult
  independently reports `status=succeeded` and `testsCount=2`. The fresh build
  retains 80 warning lines from unrelated pre-existing sources, fixture
  preparation emits one destination warning, and the affected source emits
  none. The runner log contains 48 xcodebuild CFURL no-scheme messages and five
  CFPrefs shared-memory refresh messages, with no SwiftUI
  background-publication, InputMethodKit, NSXPC, main-thread-checker, sanitizer,
  or fatal diagnostic. Exact final process readback found no FlowTab, UI
  runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable result bundle, DerivedData, fixture variants, caches,
  and logs occupy 805 MB under
  `.build-local/evidence-driven-sync/SYNC-036BA` for exact post-commit cleanup.
- Commit: `e87a575b7c6498065c5f6d3accf219ac693c007d`
  (`test(sync): migrate SYNC-036BA Home activation App row`).

### SYNC-036BB Closure Record

- Design and Oracle: `performAndWaitForHomeWindowRow` now creates the shared
  Home projection owner before tapping the exact fixture-App row. Its initial
  snapshot is retained as diagnostic evidence while acceptance is disabled.
  After the single trigger returns, an immediate readback can resolve only a
  post-trigger snapshot containing a row whose label or value
  case-insensitively contains the exact target title. The returned element
  retains its full accessibility identifier for downstream PID/CGWindowID
  parsing.
- Lifecycle and retained time policy: 12 seconds remains a named terminal
  failure bound owned by the Home-activation scenario. The helper owns owner
  cancellation through `defer`. XCUI provides no Home-projection publication
  callback, so the owner retains the shared named cancellable serial 100ms
  readback schedule after the immediate trigger readback; generation checks
  reject stale or cancelled work. Watchdog failure performs the shared final
  non-resolving readback and reports acceptance, exact expectation, complete
  row identifiers/labels/values, static titles, source, generation, and waiter
  result.
- UI and Pressure validation: the fixed-path App and runner were signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The first signed invocation passed policy compatibility,
  initial and scheduled evidence, post-trigger prefix rejection, synchronous
  post-trigger exact-row evidence, watchdog final readback, 100-lifecycle
  cancellation Pressure, and the affected real three-application Home
  workflow, 7 tests with 0 failures in 32.246 seconds (32.249 seconds total).
  The real topology passed in 30.094 seconds and Pressure in 0.245 seconds. Its
  pre-trigger readback observed the prior PID `13250` projection without
  resolving; the trigger readback accepted the target PID `13256` `Mail` row,
  whose exact CGWindow identity subsequently became frontmost. Unit and
  Behavior are not relevant because this slice changes UI-test observation
  infrastructure and its real fixture caller rather than product state rules.
- Process/Tooling and cleanup: all three affected Swift sources parse directly
  and `git diff --check` passes. The observation owner, deterministic tests,
  and Home-activation caller are 415, 283, and 238 lines respectively; the
  415-line owner retains one projection-observation responsibility. The raw
  Home-window projection bound is removed; exact frontmost-window activation
  remains a separate later slice. The remaining app/UI test inventory is 489
  direct numeric timeout/deadline occurrences across 23 files, so parent
  SYNC-036 remains open. The status ledger records `stage=completed`, every
  fixture, signing, build, test, and log exit code as 0, and a present result
  bundle; xcresult independently reports `status=succeeded` and `testsCount=7`.
  The fresh build retains 80 warning lines from unrelated pre-existing
  sources, fixture preparation emits one destination warning, and all affected
  sources emit none. The runner log contains 48 xcodebuild CFURL no-scheme
  messages and fifteen CFPrefs shared-memory refresh messages, with no SwiftUI
  background-publication, InputMethodKit, NSXPC, main-thread-checker, sanitizer,
  or fatal diagnostic. Exact final process readback found no FlowTab, UI
  runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable result bundle, DerivedData, fixture variants, caches,
  and logs occupy 804 MB under
  `.build-local/evidence-driven-sync/SYNC-036BB` for exact post-commit cleanup.
- Commit: `8d28d07ed21a2f8e3e86fee1e0765cfcea7124f9`
  (`test(sync): migrate SYNC-036BB Home window projection`).

### SYNC-036BC Closure Record

- Status: completed. The canonical XCTest-owned Xcode Helper Accessibility
  path entered the selected test bodies and both signed validation invocations
  completed successfully. The earlier automation initialization failures and
  corrected recovery evidence remain recorded in the
  [UI automation initialization record](SYNC-036BC_UI_AUTOMATION_BLOCKER.md).
- Design and Oracle: the final Home window-row click continues to use the
  SYNC-034Q activation owner. Registration and initial readback precede the
  click, matching baseline evidence is held behind the trigger gate, and
  success requires the expected fixture bundle plus exact topmost on-screen
  CGWindowID. Workspace activation, target-process AX focused/main-window
  notifications, and the named serial readback fallback only initiate fresh
  evidence collection.
- Lifecycle and retained time policy: ten seconds is now the named Home exact
  window-activation watchdog and remains solely a terminal failure bound. The
  helper invocation owns generation, observers, AX registrations, scheduled
  readback, cancellation, and final evidence diagnostics; the workflow
  invocation owns FlowTab and fixture teardown.
- Unit/Behavior: no production state rule changes in this test-only policy
  slice. The selected UI-test target reran the shared deterministic initial,
  trigger-gated, exact-identity, slow-readback, cancellation/replacement, and
  final-watchdog diagnostics regressions.
- UI and Pressure: a direct canonical XCTest authorization probe passed 1/1
  in 1.141 seconds. The canonical signed wrapper then reported every stage
  completed, `xcresult status=succeeded`, and 8/8 tests with zero failures in
  30.978 seconds. The real Finder/Chrome/Notes fixture topology accepted the
  exact target window in 29.358 seconds. The 100-lifecycle cancellation and
  replacement Pressure regression passed in 0.197 seconds, while the delayed
  readback regression confirmed scheduler delay changes latency alone.
- Diagnostics: the successful full run contained the three expected
  multiple-destination warnings, one pre-existing Space fixture immutable-
  local warning, 33 scheme-less CFURL diagnostics, and 17 CFPrefs shared-state
  diagnostics. Targeted severe runtime, sanitizer, main-thread-checker, XPC,
  and input-method diagnostic readback was empty.
- Process/Tooling: direct Swift parsing and `git diff --check` pass. The raw
  numeric timeout/deadline inventory is 488 occurrences across 22 files.
  Exact post-run `pgrep -x` readback finds no FlowTab, UI-test runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. The exact 805 MB result-bundle, DerivedData, fixture-variant, cache,
  attachment, and log root was removed after the commit; absence readback
  passed.
- Commit: `2f4d6cd9f51543d3d71ccaa1cf00061037fce177`
  (`test(sync): migrate SYNC-036BC Home exact window activation`).

### SYNC-036BD Closure Record

- Status: completed.
- Design and Oracle: the shared Search-result expectation now supports one
  atomic committed-window contract requiring exact scope, percent-decoded
  query, title, and App name. Both runtime-projection callers establish the
  generation-owned observer before typing, hold matching baseline evidence
  behind the trigger gate, and request an immediate readback when input
  delivery returns. The mock path retains its exact accessibility identifier
  assertion as an additional independent readback.
- Lifecycle and retained time policy: eight seconds is the named runtime-
  projection window-result publication watchdog and remains solely a terminal
  failure bound. The helper invocation owns readback scheduling, generation,
  cancellation, and final atomic projection diagnostics; each test invocation
  owns FlowTab and fixture cleanup. XCUI supplies no result-publication event,
  so the existing named serial readback cadence remains the platform fallback.
- Unit/Behavior: no product runtime state changed in this UI-test observation
  slice. Eight selected deterministic tests passed for policy compatibility,
  exact diagnostics parsing, already-satisfied initial state, trigger gating,
  stale query/scope and duplicate rejection, delayed scheduling, final
  watchdog readback, and lifecycle cancellation.
- UI and Pressure: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App. The canonical UI wrapper then reported every
  stage completed, `xcresult status=succeeded`, and 10/10 selected tests with
  zero failures in 34.252 seconds. The mock projection path passed in 7.301
  seconds and the Finder/Chrome/Notes real fixture topology passed in 24.813
  seconds. The 100-lifecycle stale-generation Pressure regression passed in
  0.199 seconds; the delayed-scheduler regression passed in 0.167 seconds and
  resolved only after its scheduled evidence readback.
- Diagnostics: build-for-testing contains one destination warning plus 80
  unrelated pre-existing compiler warnings, fixture preparation contains its
  destination warning plus the pre-existing immutable-local warning, and test
  execution contains one destination warning. None names an affected source.
  The logs contain 97 known scheme-less CFURL diagnostics and 22 CFPrefs
  shared-state diagnostics. Targeted severe runtime, sanitizer, main-thread-
  checker, XPC, and input-method diagnostic readback was empty.
- Process/Tooling: all three affected Swift sources parse directly and
  `git diff --check` passes. They remain single-purpose files of 620, 594, and
  107 lines. The raw numeric timeout/deadline inventory is 486 occurrences
  across 22 files. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test runner, xcodebuild, or Space fixture process. Startup `prompts.zip`
  and the three pre-existing local Skill/reference edits remain unchanged and
  outside this slice. The exact 804 MB result-bundle, DerivedData, fixture-
  variant, cache, attachment, and log root was removed after the commit;
  absence readback passed.
- Commit: `4f1c5500b1e1e806b79202340406df9c3bba752a`
  (`test(sync): migrate SYNC-036BD committed Search window publication`).

### SYNC-036BE Closure Record

- Status: completed.
- Design and Oracle: both runtime-projection callers now establish the shared
  Search-input readiness owner before their launch workflow. Success requires
  the exact runtime marker identifying `flowtab.switcher.search.input`, a
  `SearchSystemTextView` first responder, and a key window, followed by the
  exact accessibility identity readback. Element presence alone carries no
  keyboard-readiness result.
- Lifecycle and retained time policy: the shared owner registers file events
  before its initial readback and retains the named cancellable runtime-log
  cadence to close cross-process file-event gaps. The consumed helper cancels
  immediately; test teardown cancels permission or launch paths that return
  before consumption. Its 35-second marker watchdog and five-second exact-
  identity publication watchdog remain failure bounds with final evidence.
- Unit/Behavior: no product runtime state changed in this UI-test caller
  slice. Six selected deterministic tests passed for observer-before-readback,
  already-satisfied evidence, later event delivery, inexact evidence rejection,
  named scheduled readback, cancellation/duplicate suppression, and final
  watchdog diagnostics.
- UI and Pressure: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App. The canonical UI wrapper then reported every
  stage completed, `xcresult status=succeeded`, and 8/8 selected tests with
  zero failures in 32.630 seconds. The mock path passed in 6.695 seconds and
  the Finder/Chrome/Notes real fixture topology passed in 24.277 seconds. The
  100-lifecycle cancellation and duplicate-event Pressure regression passed in
  0.601 seconds; later event and scheduled-readback regressions passed in 0.204
  and 0.285 seconds and resolved only from their evidence source.
- Diagnostics: build-for-testing contains one destination warning plus 80
  unrelated pre-existing compiler warnings, fixture preparation contains its
  destination warning plus the pre-existing immutable-local warning, and test
  execution contains one destination warning. None names the affected source.
  The logs contain 97 known scheme-less CFURL diagnostics and 18 CFPrefs
  shared-state diagnostics. Targeted severe runtime, sanitizer, main-thread-
  checker, XPC, and input-method diagnostic readback was empty.
- Process/Tooling: the affected Swift source parses directly and
  `git diff --check` passes; it remains a 115-line scenario owner. The raw
  numeric timeout/deadline inventory is 484 occurrences across 22 files.
  Exact post-run `pgrep -x` readback finds no FlowTab, UI-test runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. The exact rebuildable result-bundle, DerivedData, fixture-variant,
  cache, attachment, and log root under
  `.build-local/evidence-driven-sync/SYNC-036BE` was removed after the commit;
  absence readback passed.
- Commit: `459eb4bb736683e05b1e391b5197a1656c891408`
  (`test(sync): migrate SYNC-036BE Search input readiness`).

### SYNC-036BF Closure Record

- Status: completed.
- Design and Oracle: the mock runtime-projection caller now uses the shared
  foreground-activation policy. The readiness helper performs an exact
  `XCUIApplication.State.runningForeground` observation, retains the
  installed-App activation fallback, and returns only that state result. The
  assertion now reports the final App state if readiness remains unmet before
  Search-input and committed-result evidence can be consumed.
- Lifecycle and retained time policy: the compatible 12-second duration is
  `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` and remains solely
  a terminal failure bound. The primary and capped fallback waits are
  synchronous XCUI state observations with no recurring work to cancel. The
  test invocation owns App termination and readiness-helper lifetime.
- UI validation: the canonical installer rebuilt the fixed-path App and
  verified its Apple Development signature for TeamIdentifier `96PUA726W9`.
  The canonical UI wrapper reported every stage completed and the result
  bundle independently recorded `status=succeeded`, two tests, and zero
  failures. The shared policy compatibility test passed in 0.537 seconds and
  the affected mock Search projection path passed in 6.543 seconds; the two
  selected tests completed in 7.080 seconds.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only a UI-test caller. Pressure is not relevant because no observer,
  timer, scheduler, retry loop, repeated asynchronous work, or hot path
  changed. The UI path exercises foreground state, exact keyboard-readiness,
  and committed Search-result evidence.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes; it remains a 126-line scenario owner and has
  no raw numeric timeout/deadline literal. The remaining inventory is 483
  occurrences across 21 files. The build-for-testing log retains 80 unrelated
  pre-existing warning lines and names no affected source; the test log
  contains five CFPrefs shared-state diagnostics. Targeted severe runtime,
  sanitizer, main-thread-checker, XPC, and input-method diagnostic readback is
  empty. Exact post-run `pgrep -x` readback finds no FlowTab, UI-test runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. The exact rebuildable result-bundle, DerivedData, cache, and log root
  under `.build-local/evidence-driven-sync/SYNC-036BF` was removed after the
  commit; absence readback passed.
- Commit: `1ba92c4bbfc29d80a6d9753a566aa6f18f9c735c`
  (`test(sync): migrate SYNC-036BF mock App foreground readiness`).

### SYNC-036BG Closure Record

- Status: completed.
- Design and Oracle: the shared Space Fixture launch helper now owns both
  foreground bounds and all three direct fixture launch callers route through
  it. The helper launches, observes exact
  `XCUIApplication.State.runningForeground` evidence immediately through
  XCUI's state wait, activates only when the initial observation remains
  unmet, and performs the terminal exact-state readback. Expiry reports the
  final fixture App state. Workflow readiness, visible metadata, fullscreen
  target identity, and configured tab labels remain independent later Oracles.
- Lifecycle and retained time policy: three seconds is the named initial
  foreground-observation cap that decides when to invoke activation; ten
  seconds is the named terminal fixture-foreground watchdog. Neither duration
  can establish success. Both waits are synchronous XCUI state observations
  with no recurring work to cancel, and each test invocation owns fixture
  termination.
- UI validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The canonical
  UI wrapper prepared fresh fixture variants, reported every stage completed,
  and its result bundle independently recorded `status=succeeded`, four tests,
  and zero failures. The tabbed-workflow, fullscreen-target, configured-title,
  and shared policy paths passed in 10.735, 3.974, 5.112, and 0.219 seconds;
  all selected tests completed in 20.040 seconds.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only shared UI-test launch orchestration and its callers. Pressure is
  not relevant because no observer, timer, scheduler, retry loop, repeated
  asynchronous work, or hot path changed. The three real fixture UI paths
  cover initial foreground evidence followed by exact readiness and visible
  topology readbacks.
- Diagnostics and Process/Tooling: all three relevant Swift sources parse
  directly and `git diff --check` passes. They contain 88, 405, and 90 lines;
  the 405-line helper remains a single Space Fixture application-support
  owner. The remaining raw numeric timeout/deadline inventory is 480
  occurrences across 21 files. Fixture preparation retains one destination
  warning and build-for-testing retains 80 unrelated pre-existing warning
  lines; none names an affected source. The test log contains eight CFPrefs
  shared-state diagnostics and no scheme-less CFURL diagnostic. Targeted
  severe runtime, sanitizer, main-thread-checker, XPC, and input-method
  diagnostic readback is empty. Exact post-run `pgrep -x` readback finds no
  FlowTab, UI-test runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact rebuildable result-bundle,
  DerivedData, fixture-variant, cache, and log root under
  `.build-local/evidence-driven-sync/SYNC-036BG` was removed after the commit;
  absence readback passed.
- Commit: `d1534a7d13711b3e7bdeb55367a4a48d730cd218`
  (`test(sync): migrate SYNC-036BG fixture foreground readiness`).

### SYNC-036BH Closure Record

- Status: completed.
- Design and Oracle: the configured-content owner is established before
  fixture launch and records an immediate App-state readback. A non-running
  App produces an immediate missing-content snapshot without invoking XCUI
  element retries. Once the App is running, one snapshot must contain all five
  exact title, subtitle, selected-tab, summary, and background-tab identifiers
  with their exact labels. Fixture foreground and the independent workflow
  readiness/summary gates still complete before the test consumes the resolved
  content evidence.
- Lifecycle and retained time policy: XCUI exposes no accessibility-content
  publication callback, so the owner uses the shared named serial 100ms
  readback cadence after its immediate state snapshot. Five seconds is the
  named configured-content publication watchdog and remains solely a terminal
  failure bound. Resolution, watchdog, explicit test teardown, and deinit all
  cancel the current generation and its pending one-shot timer. Expiry reports
  generation, source, wait result, App state, and every final identifier,
  existence, expected-label, and observed-label value.
- Deterministic and Pressure validation: nine selected UI-target regressions
  passed for policy compatibility, exact/missing/wrong-label rules,
  observer-before-readback initial evidence, named scheduled readback,
  watchdog final readback, scheduler rearming/cancellation, and reentrancy.
  The 500-generation stale/cancelled-event Pressure regression passed in 0.221
  seconds, and the 100-lifecycle scheduled-readback Pressure regression passed
  in 0.297 seconds. The named delayed readback resolved in 0.297 seconds from
  scheduled evidence.
- UI validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The final
  canonical UI wrapper prepared current fixture variants, reported every stage
  completed, and its result bundle independently recorded
  `status=succeeded`, ten tests, and zero failures in 6.446 seconds. The real
  tabbed-workflow path passed in 4.566 seconds. Its observer began before
  launch, its initial App-state snapshot returned immediately, and exact
  content evidence arrived during launch; scheduling affected completion time
  only.
- Validation history: the first instrumented implementation passed the same
  10/10 tests but exposed 10 seconds of XCUI retry cost while reading five
  elements from a non-running App; the real path took 15.103 seconds. Adding
  the initial App-state gate removed those retries. The next build exited 65
  before tests with concrete compiler evidence for a missing explicit closure
  return; that return was added before the final successful invocation.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes; it is a 333-line single-purpose scenario and
  observation owner with no raw numeric timeout/deadline literal. The remaining
  inventory is 475 occurrences across 20 files. Final fixture, build, and test
  logs contain only their destination-selection warning; the test log contains
  20 CFPrefs shared-state diagnostics and no scheme-less CFURL diagnostic.
  Targeted severe runtime, sanitizer, main-thread-checker, XPC, and input-method
  diagnostic readback is empty. Exact post-run `pgrep -x` readback finds no
  FlowTab, UI-test runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact rebuildable result-bundle,
  DerivedData, fixture-variant, cache, and log root under
  `.build-local/evidence-driven-sync/SYNC-036BH` was removed after the commit;
  absence readback passed.
- Commit: `36448a674b4731124651ceee18c70312d0a35d85`
  (`test(sync): migrate SYNC-036BH configured fixture content`).

### SYNC-036BI Closure Record

- Status: completed.
- Design and Oracle: the Home permission-boundary caller now uses the shared
  FlowTab foreground-activation policy. The readiness helper performs an
  immediate exact `XCUIApplication.State.runningForeground` readback, retains
  its installed-App activation fallback, and returns only the exact state
  result. Expiry reports the final App state before Home navigation begins.
- Lifecycle and retained time policy: the compatible 12-second
  `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` duration remains
  solely a terminal failure bound. Both XCUI state waits are synchronous with
  no recurring work to cancel; the test invocation owns App termination.
- UI validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The canonical
  UI wrapper reported every stage completed, and its result bundle
  independently recorded `status=succeeded`, two tests, and zero failures.
  The affected Home permission-boundary path passed in 7.764 seconds, the
  shared policy compatibility test passed in 0.218 seconds, and both selected
  tests completed in 7.982 seconds.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only one UI-test readiness caller. Pressure is not relevant because
  no observer, timer, scheduler, retry loop, repeated asynchronous work, or hot
  path changed. The visible UI path continued through exact Home content,
  FlowTab row, hidden-window summary, and hidden-App count evidence.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes; it remains a 48-line single-purpose scenario.
  Its raw foreground timeout is removed, leaving 474 raw numeric
  timeout/deadline occurrences across 20 files. Build-for-testing retains one
  destination warning plus 80 unrelated pre-existing compiler warnings, and
  test execution retains its destination warning; none names the affected
  source. The test log contains five CFPrefs shared-state diagnostics and no
  scheme-less CFURL diagnostic. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method diagnostic readback is empty.
  Exact post-run `pgrep -x` readback finds no FlowTab, UI-test runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. The exact rebuildable result-bundle, DerivedData, cache, and log root
  under `.build-local/evidence-driven-sync/SYNC-036BI` was removed after the
  commit; absence readback passed.
- Commit: `88c09c7185c5055120fcb3a7949bfef8be81200e`
  (`test(sync): migrate SYNC-036BI Home permission foreground readiness`).

### SYNC-036BJ Closure Record

- Status: completed.
- Design and Oracle: the shared trigger-and-content helper establishes its
  content observer before waiting for and tapping the exact navigation button.
  Success requires both the first exact hittable button candidate and exact
  `flowtab.home.content` existence. The content owner accepts either its
  immediate initial readback or a later scheduled exact readback, so event
  publication timing changes completion latency only. The Home permission-
  boundary scenario now uses this helper for navigation; its row projection
  remains an independent later contract.
- Lifecycle and retained time policy: XCUI exposes no accessibility-content
  publication callback, so the helper uses the shared named serial 100ms
  readback cadence. Six seconds is the compatible
  `FlowTabUITestSupportWatchdogPolicy.tabNavigation` terminal bound for each
  exact trigger and content condition. The helper invocation owns observation
  generation, scheduled-readback cancellation, synchronous return, and final
  button/content diagnostics; the test invocation owns App termination.
- Deterministic and Pressure validation: ten selected UI-target support tests
  passed for shared policy compatibility, exact initial hittable evidence,
  observer-before-trigger scheduling, final watchdog evidence, already-
  satisfied content, named scheduled readback, reentrancy suppression,
  scheduler rearming/cancellation, 500-generation stale/cancelled-event
  Pressure, and 100-lifecycle scheduled-readback Pressure. The two hittable-
  owner regressions passed in 0.162 and 0.196 seconds; the two Pressure
  regressions passed in 0.235 and 0.281 seconds.
- UI validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The canonical
  UI wrapper reported every stage completed, and its result bundle
  independently recorded `status=succeeded`, 11 tests, and zero failures in
  8.496 seconds. The affected Home permission-boundary path passed in 6.456
  seconds. Its content readback owner started before the Home click, then
  resolved only from exact accessibility existence evidence.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only shared UI-test orchestration and one UI-test caller. UI and
  Pressure are covered by the signed invocation above.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes; they remain single-purpose files of 192 and 66
  lines. The remaining raw numeric timeout/deadline inventory is 472
  occurrences across 20 files. Build-for-testing retains one destination
  warning plus 80 unrelated pre-existing compiler warnings, and test execution
  retains its destination warning; none names an affected source. The test log
  contains 23 CFPrefs shared-state diagnostics and no scheme-less CFURL
  diagnostic. Targeted severe runtime, sanitizer, main-thread-checker, XPC,
  and input-method diagnostic readback is empty. Exact post-run `pgrep -x`
  readback finds no FlowTab, UI-test runner, xcodebuild, or Space fixture
  process. Startup `prompts.zip` and the three pre-existing local Skill/
  reference edits remain unchanged and outside this slice. The exact
  rebuildable result-bundle, DerivedData, cache, and log root under
  `.build-local/evidence-driven-sync/SYNC-036BJ` was removed after the commit;
  absence readback passed.
- Commit: `e1ec99fea05dee283c477dc68cbd896011301cfd`
  (`test(sync): migrate SYNC-036BJ Home navigation evidence`).

### SYNC-036BK Closure Record

- Status: completed.
- Design and Oracle: the Home permission-boundary scenario now establishes one
  projection owner before Home navigation. One readback snapshot must contain
  the exact bundle-derived FlowTab App-row identifier with existence and value
  `0w hidden`, plus the exact hidden-App statistic identifier with existence
  and value `1`. Missing identities, missing elements, and wrong values remain
  unmet evidence. Foreground readiness and exact Home content publication stay
  independent earlier contracts.
- Lifecycle and retained time policy: XCUI exposes no accessibility-projection
  publication callback, so the owner uses the shared named serial 100ms
  readback cadence after registering it before the immediate initial readback.
  Six seconds is the scenario-owned projection watchdog and remains solely a
  terminal failure bound. Resolution, watchdog expiry, explicit test teardown,
  and deinit cancel the current generation and pending one-shot schedule.
  Expiry reports expected and final identifiers, existence, values, generation,
  source, and wait result.
- Deterministic and Pressure validation: six owner-specific regressions passed
  for compatible policy, one exact atomic snapshot, wrong/missing identity and
  value rejection, already-satisfied initial evidence, preinstalled scheduled
  evidence, duplicate later-event suppression, explicit cancellation, and
  watchdog final readback. Seven shared regressions passed for initial
  observer ordering, named slow scheduling, final watchdog evidence,
  scheduler rearming/cancellation, reentrancy suppression, 500-generation
  stale/cancelled-event Pressure, and 100-lifecycle scheduled-readback
  Pressure. The two Pressure paths passed in 0.206 and 0.283 seconds; named
  delayed scheduling resolved in 0.258 seconds from exact evidence.
- UI validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The canonical
  UI wrapper reported every stage completed, and its result bundle
  independently recorded `status=succeeded`, 14 tests, and zero failures in
  8.945 seconds. The affected Home permission-boundary path passed in 6.333
  seconds. Its first row readback began before the navigation trigger; a later
  exact row/statistic snapshot resolved before the button tap, demonstrating
  that observer scheduling affects completion latency only.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only UI-test observation infrastructure, its deterministic tests,
  and one UI-test caller. UI and Pressure are covered by the signed invocation
  above.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the Xcode project plist lints, and `git diff --check` passes. The
  scenario, observation owner, and deterministic tests contain 111, 117, and
  294 lines. This scenario now has no raw numeric timeout/deadline literal; the
  remaining inventory is 469 occurrences across 19 files. Build-for-testing
  retains one destination warning plus 80 unrelated pre-existing compiler
  warnings, and test execution retains its destination warning; none names an
  affected source. The logs contain 29 CFPrefs shared-state and 80 known
  scheme-less CFURL diagnostics. Targeted severe runtime, sanitizer, main-
  thread-checker, XPC, and input-method diagnostic readback is empty. Exact
  post-run `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or
  Space fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. The exact
  rebuildable result-bundle, DerivedData, cache, and log root under
  `.build-local/evidence-driven-sync/SYNC-036BK` was removed after the commit;
  absence readback passed.
- Commit: `5c4593eb58265249b7695105e35bca1eae369dce`
  (`test(sync): migrate SYNC-036BK Home projection evidence`).

### SYNC-036BL Closure Record

- Status: completed.
- Design and Oracle: the mock Home initial-projection caller now uses the
  shared foreground-activation policy. The readiness helper performs an
  immediate exact `XCUIApplication.State.runningForeground` readback, retains
  its installed-App activation fallback, and returns only the exact state
  result. Expiry reports the final App state before Home navigation begins.
- Lifecycle and retained time policy: the compatible 12-second
  `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` duration remains
  solely a terminal failure bound. Both XCUI state waits are synchronous with
  no recurring work to cancel; the test invocation owns App termination and
  the independent initial-projection file/notification observation lifecycle.
- UI validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The canonical
  UI wrapper reported every stage completed, and its result bundle
  independently recorded `status=succeeded`, two tests, and zero failures.
  The affected mock Home initial-projection path passed in 9.120 seconds, the
  shared policy compatibility test passed in 0.266 seconds, and both selected
  tests completed in 9.386 seconds.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only one UI-test readiness caller. Pressure is not relevant because
  no observer, timer, scheduler, retry loop, repeated asynchronous work, or hot
  path changed. The signed UI path continued through exact Home content,
  ordered zero-count rows, and typed post-maintenance projection evidence.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes; it remains a 387-line single-purpose Home
  initial-projection owner. Its raw foreground timeout is removed, leaving 468
  raw numeric timeout/deadline occurrences across 19 files. Build-for-testing
  retains one destination warning plus 80 unrelated pre-existing compiler
  warnings, and test execution retains its destination warning; none names the
  affected source. The logs contain five CFPrefs shared-state and 80 known
  scheme-less CFURL diagnostics. Targeted severe runtime, sanitizer, main-
  thread-checker, XPC, and input-method diagnostic readback is empty. Exact
  post-run `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or
  Space fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. The exact
  rebuildable result-bundle, DerivedData, cache, and log root under
  `.build-local/evidence-driven-sync/SYNC-036BL` was removed after the commit;
  absence readback passed.
- Commit: `4920d7d2f1cc69853d1e26f72c7b7f590977d67b`
  (`test(sync): migrate SYNC-036BL Home initial foreground readiness`).

### SYNC-036BM Closure Record

- Status: completed.
- Design and Oracle: the mock Home initial-projection caller now uses the
  shared trigger-and-content helper. It establishes the exact
  `flowtab.tab.home.content` observer before waiting for and tapping the exact
  `flowtab.sidebar.tab.home` button. Success requires the first exact hittable
  button candidate and an initial or later exact Home-content existence
  readback. Initial visible rows and typed post-maintenance projection evidence
  remain independent later contracts.
- Lifecycle and retained time policy: XCUI supplies no accessibility-content
  publication callback, so the shared observation owner retains its named
  serial 100ms readback cadence. Six seconds is the compatible
  `FlowTabUITestSupportWatchdogPolicy.tabNavigation` terminal bound for each
  exact trigger and content condition. Resolution, expiry, and helper return
  cancel the generation and pending schedule; expiry reports final candidate
  count and content existence. The test invocation owns App termination and
  the independent projection-observation lifecycle.
- Deterministic and Pressure validation: ten selected UI-target support tests
  passed for exact initial candidate evidence, observer-before-trigger and
  scheduled readback, compatible policy values, initial and later condition
  evidence, cancellation and replacement under 500-generation Pressure,
  scheduled-readback reentrancy suppression, named slow scheduling, watchdog
  final readback, scheduler rearming/cancellation, and 100-lifecycle Pressure.
  The two Pressure paths passed in 0.213 and 0.288 seconds; named delayed
  scheduling resolved in 0.260 seconds from exact evidence.
- UI validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The canonical
  UI wrapper reported every stage completed, and its result bundle
  independently recorded `status=succeeded`, 11 tests, and zero failures. The
  affected mock Home initial-projection path passed in 8.496 seconds, and all
  selected tests completed in 10.724 seconds.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes one UI-test caller and reuses already-covered shared observation
  infrastructure. UI and Pressure are covered by the signed invocation above.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes; it remains a 392-line single-purpose Home
  initial-projection owner. Its two raw navigation literals are removed,
  leaving 466 raw numeric timeout/deadline occurrences across 19 files.
  Build-for-testing retains one destination warning plus 80 unrelated
  pre-existing compiler warnings, and test execution retains its destination
  warning; none names the affected source. The logs contain 23 CFPrefs
  shared-state and 80 known scheme-less CFURL diagnostics. Targeted severe
  runtime, sanitizer, main-thread-checker, XPC, and input-method diagnostic
  readback is empty. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test runner, xcodebuild, or Space fixture process. Startup `prompts.zip`
  and the three pre-existing local Skill/reference edits remain unchanged and
  outside this slice. The exact rebuildable result-bundle, DerivedData, cache,
  and log root under `.build-local/evidence-driven-sync/SYNC-036BM` was removed
  after the commit; absence readback passed.
- Commit: `b2cb951623cda3ddd5b8db6cdec27d6f58e73460`
  (`test(sync): migrate SYNC-036BM Home initial navigation evidence`).

### SYNC-036BN Closure Record

- Status: completed.
- Design and Oracle: one Home App-row projection owner now starts before Home
  navigation and captures its baseline with acceptance disabled. The first
  signed trace showed that hidden Home rows can already exist in the
  accessibility tree before the click, so only successful exact Home-content
  navigation enables acceptance and requests a `triggerReadback`. Resolution
  then requires one snapshot containing the exact mock Mail and Browser row
  identities, both existence states, both `0w` values, finite vertical
  positions, and strict Mail-before-Browser order. The resolved snapshot also
  supplies both baseline positions for the later application comparison.
- Lifecycle and retained time policy: XCUI exposes no App-row projection
  callback, so the owner uses the shared named serial 100ms readback cadence.
  Sixteen seconds is the named terminal watchdog, preserving the cumulative
  prior six-, six-, two-, and two-second failure bounds. Success remains
  exclusively the exact row condition. The owner reports acceptance state
  plus expected and final row identity, existence, value, and position
  evidence. Resolution, expiry,
  explicit test teardown, and deinit cancel the generation and pending
  one-shot schedule; the test invocation also owns App and typed projection-
  observer teardown.
- Deterministic and Pressure validation: seven owner-specific regressions
  passed for compatible watchdog policy, exact identity/existence/value/order
  rules, already-satisfied initial evidence, preinstalled scheduled evidence,
  disabled-baseline to trigger acceptance, cancellation, duplicate later-event
  suppression, and watchdog final evidence. Seven shared condition/scheduler
  regressions passed for initial/preinstalled delivery, named slow scheduling,
  final readback, rearming/cancellation, reentrancy suppression, 500-generation
  cancellation/replacement Pressure, and 100-lifecycle scheduled-readback
  Pressure. The Pressure paths passed in 0.215 and 0.298 seconds; named delayed
  scheduling resolved in 0.266 seconds.
- UI validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The final
  canonical UI wrapper reported every stage completed, and its result bundle
  independently recorded `status=succeeded`, 15 tests, and zero failures. The
  affected mock Home initial-projection path passed in 5.607 seconds, and all
  selected tests completed in 8.476 seconds. Its trace records baseline reads
  before the tap and the accepted exact row readback after navigation.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes UI-test observation infrastructure, deterministic UI-target tests,
  and one UI-test caller. UI and Pressure are covered by the signed invocation
  above.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the Xcode project plist lints, and `git diff --check` passes. The
  scenario, observation owner, and deterministic tests contain 426, 208, and
  316 lines. Four raw sequential timeout literals are removed, leaving 462 raw
  numeric timeout/deadline occurrences across 19 files. The final incremental
  build and test logs each retain one destination warning and no compiler
  warning; none names an affected source. The logs contain 31 CFPrefs shared-
  state and 16 known scheme-less CFURL diagnostics. Targeted severe runtime,
  sanitizer, main-thread-checker, XPC, and input-method diagnostic readback is
  empty. Exact post-run `pgrep -x` readback finds no FlowTab, UI-test runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. The exact rebuildable result bundles, DerivedData, caches, and logs
  under `.build-local/evidence-driven-sync/SYNC-036BN` were removed after the
  commit; absence readback passed.
- Commit: `44da451687b05d9004c93f2f89e0a9c048082376`
  (`test(sync): migrate SYNC-036BN Home initial row evidence`).

### SYNC-036BO Closure Record

- Status: completed.
- Design and Oracle: the application-level owner remains installed before App
  launch and atomically persists exact current-launch projection evidence. Once
  the initial exact Home row snapshot supplies the Mail and Browser vertical
  baselines, a second generation-owned row observation begins with acceptance
  disabled before the test consumes that application evidence. Exact
  `appSwitcherProjectionNotification` / `sourceGenerationAdvanced` evidence
  enables acceptance and requests an immediate readback. Resolution requires
  one snapshot with the exact Mail and Browser identities, both existence
  states, both `0w` values, strict Mail-before-Browser order, finite positions,
  and both positions within one point of their initial baselines. The same
  resolved snapshot supplies every final assertion.
- Lifecycle and retained time policy: XCUI exposes no App-row projection
  callback, so the owner retains the shared named serial 100ms readback
  cadence. Four seconds is the named terminal watchdog, preserving the two
  prior sequential two-second failure bounds. Success remains exclusively the
  typed application generation plus exact row readback. Expiry reports the
  application observation generation, expected identities/values/positions/
  accuracies, acceptance state, and final row snapshot. Resolution, expiry,
  explicit teardown, and deinit cancel the current generation and pending
  schedule; the test invocation owns App and cross-process route cleanup.
- Deterministic validation: ten owner-specific regressions passed for both
  compatible watchdog policies, exact identity/existence/value/order rules,
  position-baseline accuracy and invalid evidence, initial evidence,
  preinstalled scheduled evidence, trigger gating, the additional atomic
  snapshot predicate and diagnostics, cancellation/duplicate suppression, and
  watchdog final evidence. Eight shared observation/scheduler regressions
  passed for initial/preinstalled delivery, final readback, reentrancy,
  rearming/cancellation, named slow scheduling, 500-generation replacement
  Pressure, and 100-lifecycle scheduled-readback Pressure.
- UI and Pressure validation: the canonical installer rebuilt and verified the
  fixed-path Apple Development-signed App for TeamIdentifier `96PUA726W9`.
  The canonical signed UI wrapper completed every stage; its result bundle
  independently recorded `status=succeeded`, 19 tests, and zero failures. The
  affected mock Home path passed in 6.821 seconds, and the selected suite in
  10.777 seconds. A second canonical signed run repeated the affected path,
  atomic row predicate, named slow scheduling, 500-generation replacement,
  and 100-lifecycle scheduling tests five times each: 25 invocations and zero
  failures in 34.165 seconds. The five real UI iterations passed in 6.322,
  5.553, 5.769, 5.736, and 5.903 seconds, demonstrating that scheduling
  variation changes completion latency while preserving the result.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes UI-test observation infrastructure, deterministic UI-target tests,
  and one signed UI caller. UI and Pressure are covered by the signed
  invocations above.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly and `git diff --check` passes. The observation owner, deterministic
  tests, and scenario contain 281, 466, and 481 lines and retain clear single
  responsibilities. Two raw timeout literals are removed, leaving 460 raw
  numeric timeout/deadline occurrences across 19 files. The clean build
  retains 80 unrelated compiler warnings and one destination warning; no
  compiler warning names an affected source. The first test log contains 39
  known CFPrefs shared-state and 51 known scheme-less CFURL diagnostics; the
  repeated Pressure log contains 55 and 17 respectively. Targeted severe
  runtime, sanitizer, main-thread-checker, XPC, and input-method diagnostic
  readback is empty. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test runner, xcodebuild, or Space fixture process. Startup `prompts.zip`
  and the three pre-existing local Skill/reference edits remain unchanged and
  outside this slice. The exact rebuildable result bundles, DerivedData,
  caches, and logs under `.build-local/evidence-driven-sync/SYNC-036BO` were
  removed after the commit; absence readback passed.
- Commit: `6143d6925a3baadfc402ef1f6aba09e885216e57`
  (`test(sync): migrate SYNC-036BO Home applied row evidence`).

### SYNC-036BP Closure Record

- Status: completed.
- Design and Oracle: the Home permission convergence path now reuses the shared
  App-foreground policy. The helper performs an immediate exact
  `.runningForeground` state readback and retains its installed-App activation
  fallback. The exact state remains the sole success condition; failure
  reports the final `XCUIApplication.State` and stops this test invocation
  before independent Home navigation and simulated TCC-transition work.
- Lifecycle and retained time policy: the shared 12-second foreground
  activation watchdog is solely a terminal failure bound. The synchronous App
  state wait creates no observer, timer, retry, or recurring lifecycle. The
  test invocation continues to own App teardown and removal of its exact
  temporary permission-state path.
- Validation: the existing support-policy compatibility regression and the
  affected signed Home permission UI test both passed. The canonical installer
  rebuilt and verified the fixed-path Apple Development-signed App for
  TeamIdentifier `96PUA726W9`; the canonical UI wrapper completed every stage.
  Its result bundle independently recorded `status=succeeded`, two tests, and
  zero failures. The affected path passed in 9.393 seconds, and the selected
  suite in 9.607 seconds.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes one UI-test precondition caller. Pressure is not relevant because the
  reused state wait is synchronous and introduces no recurring or hot-path
  work. UI and Process/Tooling are covered by the signed invocation and checks
  below.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 166 lines and retains one clear
  permission-observation UI-test responsibility. One raw timeout literal is
  removed, leaving 459 raw numeric timeout/deadline occurrences across 19
  files. The clean build retains 80 unrelated compiler warnings and one
  destination warning; no compiler warning names the affected source. The test
  log contains five known CFPrefs shared-state and 51 known scheme-less CFURL
  diagnostics. Targeted severe runtime, sanitizer, main-thread-checker, XPC,
  and input-method diagnostic readback is empty. Exact post-run `pgrep -x`
  readback finds no FlowTab, UI-test runner, xcodebuild, or Space fixture
  process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036BP` were removed after the commit;
  absence readback passed.
- Commit: `e94ddf5ee4225820e8cb5a01830007b14feb1ed0`
  (`test(sync): migrate SYNC-036BP Home permission foreground readiness`).

### SYNC-036BQ Closure Record

- Status: completed.
- Design and Oracle: the Home permission path now routes its navigation through
  the shared trigger-and-content helper. It establishes the generation-owned
  exact `homeTabContent` observer before waiting for and tapping an exact
  `homeTabButton`, then succeeds only from an already-present or later exact
  content-existence readback. Failure reports the final button-candidate count
  and content-existence state and stops this test invocation before the
  independent permission-projection and transition contracts.
- Lifecycle and retained time policy: the shared six-second tab-navigation
  watchdog bounds each exact trigger/content condition, and the shared named
  serial 100ms cadence supplies cancellable XCUI readback because Accessibility
  exposes no navigation-completion callback. Resolution, expiry, explicit
  teardown, and deinit cancel the observation generation and pending schedule.
  The helper invocation owns those resources; the test owns App teardown and
  removal of its exact temporary permission-state path.
- Validation: the affected signed Home permission UI path, support-policy
  compatibility, initial readback, preinstalled event, exact candidate,
  scheduled readback, watchdog evidence, reentrancy, cancellation, and
  lifecycle regressions all passed. The canonical installer rebuilt and
  verified the fixed-path Apple Development-signed App for TeamIdentifier
  `96PUA726W9`; the canonical UI wrapper completed every stage. Its result
  bundle independently recorded `status=succeeded`, ten tests, and zero
  failures. The affected path passed in 7.768 seconds, and the selected suite
  in 9.661 seconds. The 500-generation replacement and 100-lifecycle
  scheduling Pressure tests passed in 0.259 and 0.283 seconds respectively.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes one UI-test caller while reusing already deterministic shared
  UI-target observation infrastructure. UI and Pressure are covered by the
  signed invocations above.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 179 lines and retains one clear
  permission-observation UI-test responsibility. Two raw timeout literals are
  removed, leaving 457 raw numeric timeout/deadline occurrences across 19
  files. The clean build retains 80 unrelated compiler warnings and one
  destination warning; no compiler warning names the affected source. The test
  log contains 21 known CFPrefs shared-state and 51 known scheme-less CFURL
  diagnostics. Targeted severe runtime, sanitizer, main-thread-checker, XPC,
  and input-method diagnostic readback is empty. Exact post-run `pgrep -x`
  readback finds no FlowTab, UI-test runner, xcodebuild, or Space fixture
  process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036BQ` were removed after the commit;
  absence readback passed.
- Commit: `4c9f6062680ef296dfeeac1ea83688c8723dee66`
  (`test(sync): migrate SYNC-036BQ Home permission navigation evidence`).

### SYNC-036BR Closure Record

- Status: completed.
- Design and Oracle: the test now resolves the exact
  `permissionOpenSettings` element and establishes its generation-owned
  Boolean condition observer before Home navigation. An immediate readback or
  later scheduled readback resolves only when that exact persistent element
  reports `exists=true`. The wait after exact navigation therefore consumes
  already-recorded evidence or continues observing the same generation;
  failure reports the exact identifier, final existence value, generation,
  source, and wait result before the independent permission transition.
- Lifecycle and retained time policy: the new shared six-second
  `permissionStateProjection` watchdog is solely a terminal failure bound. The
  shared named serial 100ms XCUI cadence remains necessary because
  Accessibility exposes no permission-projection callback. Resolution,
  watchdog expiry, explicit teardown, and deinit cancel the event input and
  pending schedule. The test invocation owns the observer, App teardown, and
  removal of its exact temporary permission-state path.
- Validation: the affected signed Home permission UI path, support-policy
  compatibility, initial and preinstalled-event readback, named scheduled
  readback, final watchdog readback, reentrancy, cancellation/replacement, and
  scheduled lifecycle regressions all passed. The canonical installer rebuilt
  and verified the fixed-path Apple Development-signed App for TeamIdentifier
  `96PUA726W9`; both canonical UI-wrapper invocations completed every stage.
  Their result bundles independently recorded `status=succeeded`: the primary
  suite ran eight tests with zero failures in 8.480 seconds and the two
  supplemental watchdog/scheduler tests passed in 0.654 seconds. The affected
  path passed in 7.187 seconds. The 500-generation replacement and
  100-lifecycle scheduling Pressure tests passed in 0.211 and 0.298 seconds.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes UI-test policy and observation orchestration. UI and Pressure are
  covered by the signed invocations above.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The permission scenario and policy sources
  contain 210 and 94 lines and retain clear single responsibilities. One raw
  timeout literal is removed, leaving 456 raw numeric timeout/deadline
  occurrences across 18 files. The clean primary build retains 80 unrelated
  compiler warnings and one destination warning; no compiler warning names an
  affected source. The primary test log contains 17 known CFPrefs shared-state
  and 51 known scheme-less CFURL diagnostics; the supplemental log contains
  four and 17 respectively. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method diagnostic readback is empty.
  Exact post-run `pgrep -x` readback finds no FlowTab, UI-test runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036BR` were removed after the commit;
  absence readback passed.
- Commit: `f358bafc01328df05e61717043899d6bc6660683`
  (`test(sync): migrate SYNC-036BR Home permission projection evidence`).

### SYNC-036BS Closure Record

- Status: completed.
- Design and Oracle: after the exact initial permission projection resolves,
  the test now establishes a fresh generation-owned observer of the exact
  `permissionOpenSettings` element before atomically writing the simulated TCC
  state. The observer requires `exists=false`; an immediate trigger readback
  runs after the write, and later scheduled readbacks continue only while the
  exact absence condition remains unmet. The independent runtime-log baseline
  is also captured before the write for its later persistent log-evidence
  contract. Failure reports the exact identifier, final existence value,
  generation, source, and wait result.
- Lifecycle and retained time policy: the shared six-second
  `permissionStateProjection` watchdog is solely a terminal failure bound. The
  shared named serial 100ms cadence remains the controlled fallback because
  Accessibility exposes no banner-removal callback. Resolution, watchdog
  expiry, explicit teardown, and deinit cancel the observation generation and
  pending schedule. The test invocation owns the observer, App teardown, and
  removal of its exact temporary permission-state path.
- Validation: the affected signed Home permission UI path plus initial,
  preinstalled-event, immediate trigger, named scheduled, final watchdog,
  reentrancy, cancellation/replacement, and lifecycle observation regressions
  all passed. The canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`; the canonical
  UI wrapper completed every stage. Its result bundle independently recorded
  `status=succeeded`, eight tests, and zero failures in 7.503 seconds. The
  affected path passed in 5.941 seconds; exact pretrigger gating passed in
  0.178 seconds. The 500-generation replacement and 100-lifecycle scheduling
  Pressure tests passed in 0.214 and 0.290 seconds.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes UI-test observation orchestration. UI and Pressure are covered by
  the signed invocation above.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 239 lines and retains one clear
  permission-observation UI-test responsibility. One raw timeout literal is
  removed, leaving 455 raw numeric timeout/deadline occurrences across 18
  files. The clean build retains 80 unrelated compiler warnings and one
  destination warning; no compiler warning names the affected source. The test
  log contains 17 known CFPrefs shared-state and 51 known scheme-less CFURL
  diagnostics. Targeted severe runtime, sanitizer, main-thread-checker, XPC,
  and input-method diagnostic readback is empty. Exact post-run `pgrep -x`
  readback finds no FlowTab, UI-test runner, xcodebuild, or Space fixture
  process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036BS` were removed after the commit;
  absence readback passed.
- Commit: `3e6b857f21e0f70741b8749522e681b23094397e`
  (`test(sync): migrate SYNC-036BS Home permission transition evidence`).

### SYNC-036BT Closure Record

- Status: completed.
- Design and Oracle: both defaulted runtime-log wait overloads now use the
  owner-named `FlowTabUITestRuntimeLogObservationPolicy.defaultWatchdog`.
  Success remains exclusively baseline-relative persisted contents satisfying
  every exact marker or the requested regular expression. The baseline's
  immediate readback closes already-written evidence races; file-event
  generations wake later reads, and the named scheduled fallback handles
  unavailable or coalesced file events. Failure continues to report the exact
  missing markers or pattern, file-event generations, character count, and
  bounded log tail.
- Lifecycle and retained time policy: the shared eight-second default watchdog
  remains solely a terminal failure bound and explicit caller overrides remain
  intact. The existing named 200ms readback cadence is condition-controlled.
  Each invocation owns the condition observer, file-event source, scheduled
  fallback, resolution/watchdog cancellation, and final diagnostics.
- Validation: the new policy-compatibility regression, baseline append and
  file-replacement events, exact marker/regex rules, event resolution,
  initial completed projection, separated receipt/completion, missing
  completion watchdog, lifecycle, and stale-callback tests all passed. The
  canonical installer rebuilt and verified the fixed-path Apple
  Development-signed App for TeamIdentifier `96PUA726W9`; the canonical UI
  wrapper completed every stage. Its result bundle independently recorded
  `status=succeeded`, eleven tests, and zero failures in 11.248 seconds. The
  representative marker and regex UI paths passed in 5.944 and 2.456 seconds.
  The 100-lifecycle observation and 100-iteration stale-callback Pressure tests
  passed in 0.175 and 0.416 seconds.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes shared UI-test observation infrastructure. UI and Pressure are
  covered by the signed invocation above.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The observation and regression sources contain
  377 and 716 lines; the latter remains below the 800-line guardrail with one
  runtime-log observation responsibility. Both anonymous default literals are
  removed. The call-site raw numeric timeout/deadline measure remains 455
  occurrences across 18 files. The clean build retains 80 unrelated compiler
  warnings and one destination warning; no compiler warning names an affected
  source. The test log contains 24 known CFPrefs shared-state and 51 known
  scheme-less CFURL diagnostics. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method diagnostic readback is empty.
  Exact post-run `pgrep -x` readback finds no FlowTab, UI-test runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036BT` were removed after the commit;
  absence readback passed.
- Commit: `7be7564c0a8e02a3a4db95246fcba00c2c9adbae`
  (`test(sync): name SYNC-036BT runtime log evidence watchdog`).

### SYNC-036BU Closure Record

- Status: completed.
- Design and Oracle: `openSettingsTab(in:)` now resolves the exact Settings
  content before the trigger and delegates to
  `tapFirstHittableAndWaitForExistence`. That shared owner installs the content
  observer before it waits for and taps the exact hittable Settings button.
  Success requires the exact Settings content to exist in an initial or later
  generation readback. Failure combines final candidate count and content
  existence in one navigation diagnostic.
- Lifecycle and retained time policy: the shared six-second `tabNavigation`
  watchdog remains solely a terminal bound for each exact trigger/content
  condition. The shared named serial 100ms XCUI cadence remains the controlled
  fallback because Accessibility exposes no tab-content projection callback.
  Resolution, expiry, explicit teardown, and deinit cancel the active
  generation and pending schedule; the helper invocation owns that lifecycle.
- Validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The final
  canonical UI wrapper invocation completed every stage, and its result bundle
  independently recorded `status=succeeded`, eleven tests, and zero failures
  in 40.875 seconds. Four signed Settings paths covered appearance controls,
  permission actions, TCC readback convergence, and Simplified Chinese status
  projection. Exact-candidate initial and preinstalled-event regressions,
  support-policy compatibility, initial readback, final watchdog evidence,
  cancellation/replacement Pressure, and scheduled-lifecycle Pressure also
  passed.
- Validation discovery: the first expanded eleven-test invocation passed ten
  tests. Its English path completed both exact Settings navigations, then the
  Search trigger receipt watchdog reported no baseline-relative marker. An
  exact English rerun reproduced the same result; persisted-log readback found
  only the 51-byte seeded line and no receipt or completion marker. The absent
  verbose evidence-channel launch configuration is recorded as independent
  candidate SYNC-036BV.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes shared UI-test navigation orchestration. UI and Pressure are covered
  by the signed invocation above.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 785 lines, remains below the
  800-line guardrail, and retains one shared UI-support responsibility. The
  call-site raw numeric timeout/deadline measure remains 455 occurrences across
  18 files. The final build and test logs contain two destination-selection
  warnings and no compiler warning naming the affected source. The test log
  contains 26 known CFPrefs shared-state and 17 known scheme-less CFURL
  diagnostics. Targeted severe runtime, sanitizer, main-thread-checker, XPC,
  and input-method diagnostic readback is empty. Exact post-run `pgrep -x`
  readback finds no FlowTab, UI-test runner, xcodebuild, or Space fixture
  process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036BU` were removed after the commit;
  absence readback passed.
- Commit: `7429c010eee2e5f59b7776e420b1c13de7832653`
  (`test(sync): migrate SYNC-036BU Settings navigation evidence`).

### SYNC-036BV Closure Record

- Status: completed.
- Design and Oracle: the English primary-surface path now reuses
  `FlowTabUITestSearchInputReadinessPolicy.applicationEvidenceLaunchArguments`,
  establishing INFO plus verbose persisted runtime evidence before launch. Its
  mock runtime supplies AX-backed window coverage so the existing committed
  Search-index contract can reach a valid first generation, while Screen
  Recording remains denied for the permission-layout surface. Trigger success
  still requires the preinstalled baseline-relative owner to observe the exact
  notification-name receipt and completion markers; Search input and Mock Mail
  accessibility identities remain independent visible Oracles. A deterministic
  regression fixes the shared launch-argument contract.
- Lifecycle and retained time policy: the trigger invocation owns the runtime
  log snapshot, both observation generations, file-event sources, scheduled
  fallback readbacks, cancellation, and cleanup. The named receipt and
  presentation-completion watchdogs remain terminal failure bounds with final
  persisted-log diagnostics. The test invocation owns the exact App process
  lifecycle and runtime-log cleanup.
- Validation discovery: the first three canonical signed runs stopped before
  test execution while the system `Enable UI Automation` authentication
  callback remained unresolved. After direct system authentication, attempt
  four entered the test and persisted both exact trigger markers. Its later
  Search assertion exposed the AX-denied incomplete-index topology and the
  runtime logged `start search deferred: awaiting committed search index`. A
  signed on-demand Search control using established AX-backed mock coverage
  passed three presentation cycles in 14.074 seconds. The deferred trigger's
  premature completion semantics are recorded as independent candidate
  SYNC-036BW.
- Validation: the final canonical wrapper completed every stage. Its result
  bundle independently records `status=succeeded` and `testsCount=12`; the test
  log reports twelve tests, zero failures, and 21.628 seconds. The affected
  English Settings/Logs/App Visibility/Search path passed in 18.102 seconds,
  and the marker/regex UI control passed in 2.354 seconds. Launch-policy,
  initial-readback, event, inexact-evidence, cancellation, duplicate,
  separated/out-of-order receipt-completion, and watchdog-diagnostic
  regressions all passed. The two 100-iteration cancellation and stale-callback
  Pressure tests passed in 0.142 and 0.144 seconds.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes one UI-test launch contract and its regression while reusing shared
  observation infrastructure. UI and Pressure are covered by the signed
  invocation above.
- Diagnostics and Process/Tooling: both affected Swift files parse directly
  and `git diff --check` passes. They contain 102 and 223 lines with clear
  English-layout and Search-readiness regression responsibilities. The raw
  numeric timeout/deadline inventory remains 455 occurrences across 18 files.
  Build and test logs contain one destination-selection warning each, 17 known
  scheme-less CFURL diagnostics, and 26 known CFPrefs shared-state diagnostics;
  no warning names an affected source. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method diagnostic readback is empty.
  Exact post-run `pgrep -x` readback finds no FlowTab, UI-test runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036BV` were removed after the commit;
  absence readback passed.
- Commit: `542ebdc198eb2339cead4f00d026e145ab01dbfb`
  (`test(sync): migrate SYNC-036BV English Search evidence`).

### SYNC-036BW Closure Record

- Status: completed.
- Design and Oracle: the Search notification path now establishes
  `FlowTabUITestSearchTriggerPresentationObservationOwner` before invoking the
  trigger, then evaluates an initial readback and generation-scoped event
  readbacks. Completion requires the exact switcher panel to be presented and
  key while Search is active and its freshness-barrier activation is no longer
  pending. Satisfied initial state and synchronous trigger completion resolve
  immediately; committed-index publication, Search-state changes, and exact
  panel key/exposure notifications can resolve deferred work in either order.
  `presentSearchHotkeySessionForTesting` now reports this same committed
  presentation state. Receipt remains a separate notification-name Oracle,
  and visible signed controls retain exact Search summary and result identities.
- Lifecycle and retained time policy: `SwitcherTriggerNotificationObserver`
  owns observation replacement, uninstall, and deinit cancellation. Each
  active generation owns its Combine subscription and exact-panel notification
  tokens, cancelling them on resolution, replacement, explicit cancellation,
  and owner teardown. The production observation is duration-free. The named
  UI receipt and presentation watchdogs remain terminal failure bounds; final
  readback diagnostics now preserve the pending generation, evidence
  generation, source, and full panel/Search snapshot. The existing named file
  event fallback cadence remains owned and cancelled by the UI runtime-log
  observation helper.
- Validation: the final canonical FlowTabTests wrapper completed every stage;
  its status readback records `completed`, exit code zero, and a present result
  bundle. The test log reports seven tests and zero failures in 0.218 seconds:
  five deterministic owner regressions cover observer-before-readback,
  satisfied initial state, synchronous completion, both event orders,
  cancellation, stale callbacks, and duplicates, while two controller paths
  cover deferred and immediately committed Search presentation. The
  100-iteration owner Pressure regression passed in 0.001 seconds.
- Signed UI and Pressure: the canonical installer produced the fixed-path
  Apple Development-signed App with TeamIdentifier `96PUA726W9` and a valid
  designated requirement. The canonical signed UI wrapper completed every
  stage; its status readback records `completed`, exit code zero, and a present
  result bundle. The test log reports six tests and zero failures in 33.373
  seconds. The English visible path passed in 18.969 seconds, the real
  three-cycle Search trigger path passed in 13.907 seconds, and initial
  completion, receipt/completion separation, stale-callback Pressure, and
  watchdog diagnostics passed in 0.121, 0.089, 0.145, and 0.142 seconds.
- Validation scope: Unit covers deterministic generation and cancellation
  rules; Behavior covers controller orchestration across the committed-index
  barrier; UI covers the affected visible Search paths; Pressure covers 100
  owner generations, stale UI callbacks, and three signed trigger cycles.
- Diagnostics and Process/Tooling: both new Swift sources parse directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  extracted bootstrap and new owner sources contain 667 and 593 lines; the
  281-line regression source retains one synchronization responsibility. The
  raw numeric timeout/deadline inventory remains 455 occurrences across 18
  files. No compiler warning names an affected source; build output retains
  repository-existing warnings. Test logs contain 85 known scheme-less CFURL,
  14 known CFPrefs shared-state, and three destination-selection diagnostics.
  Targeted severe runtime, sanitizer, main-thread-checker, XPC, and input-method
  readback is empty. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test runner, xcodebuild, or Space fixture process. Startup `prompts.zip`
  and the three pre-existing local Skill/reference edits remain unchanged and
  outside this slice. Rebuildable result bundles, DerivedData, caches, and logs
  under `.build-local/evidence-driven-sync/SYNC-036BW` were removed after the
  commit; absence readback passed.
- Commit: `1a54d0147e5dc09e9cd9dccb148c95cf6c14d70c`
  (`refactor(sync): migrate SYNC-036BW Search trigger evidence`).

### SYNC-036BX Closure Record

- Status: completed.
- Design and Oracle: the Settings permission path resolves both exact denied
  status labels and both exact permission actions before navigation, then
  starts one generation-owned observation. Its initial, scheduled, and
  explicit post-navigation readbacks produce one atomic four-field snapshot.
  Completion requires all four exact elements to exist together; all sixteen
  Boolean combinations are covered by a deterministic predicate regression.
  The later granted-state transition and persisted permission logs remain
  independent evidence contracts.
- Lifecycle and retained time policy: the test invocation owns observation
  start, explicit cancellation, App teardown, and removal of the exact
  temporary permission-state file. The observation owner cancels its current
  generation and pending schedule on resolution, watchdog expiry, explicit
  teardown, and deinit. The named serial 100ms XCUI cadence remains the
  controlled fallback because Accessibility supplies no Settings projection
  callback. The named five-second initial-projection watchdog is solely a
  terminal failure bound; diagnostics preserve generation, readback source,
  wait result, and all four final element states.
- Validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The canonical
  signed UI wrapper completed every stage; its status readback records
  `completed`, every exit code zero, and a present result bundle. The test log
  reports nine tests and zero failures in 8.409 seconds. The affected visible
  Settings permission path passed in 7.162 seconds; the policy and sixteen-state
  snapshot rules passed in 0.172 and 0.092 seconds. Initial/preinstalled event,
  named scheduled readback, final watchdog readback, and manual cancellable
  one-shot scheduling passed in 0.101, 0.198, 0.136, and 0.092 seconds. The
  500-generation cancellation/replacement and 100-lifecycle schedule Pressure
  regressions passed in 0.211 and 0.246 seconds. The manual one-shot test keeps
  callback delivery under caller control, demonstrating that delayed
  scheduling changes latency while the exact readback remains the success
  condition.
- Validation scope: Unit and Behavior are not relevant because the slice
  changes UI-test orchestration and its deterministic UI-target rules. UI and
  Pressure are covered by the signed invocation above.
- Diagnostics and Process/Tooling: the affected Swift source parses directly,
  `git diff --check` passes, and the file contains 336 lines with one permission
  observation responsibility. Four raw timeout arguments are removed, leaving
  451 occurrences across 18 files. The build retains 80 unrelated compiler
  warnings and two destination-selection warnings; no warning names the
  affected source. The test log contains 51 known scheme-less CFURL and 19
  known CFPrefs shared-state diagnostics. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method readback is empty. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036BX` were removed after the commit;
  absence readback passed.
- Commit: `ef0afd67fe4d7b99e1fa61b79c18e7af15e4eb3b`
  (`test(sync): migrate SYNC-036BX Settings permission evidence`).

### SYNC-036BY Closure Record

- Status: completed.
- Design and Oracle: after the exact denied Settings permission surface is
  ready, the test resolves both exact granted-state labels and establishes one
  generation-owned observer before the first permission action. The initial
  readback must exist and remain unsatisfied. Both permission actions and the
  atomic simulated-TCC write then precede an explicit trigger readback; only a
  later atomic snapshot with both exact granted labels completes the phase.
  All four Boolean combinations are covered by a deterministic predicate
  regression. The persisted permission logs remain an independent Oracle.
- Lifecycle and retained time policy: the test invocation owns the observer,
  App teardown, and exact temporary permission-state file. Resolution,
  watchdog expiry, explicit cancellation, and deinit cancel the generation
  and pending schedule. The named serial 100ms XCUI cadence remains the
  controlled fallback because Accessibility supplies no Settings projection
  callback. The shared six-second `permissionStateProjection` watchdog remains
  solely a terminal failure bound; diagnostics include generation, readback
  source, wait result, and both final granted-label states.
- Validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The canonical
  signed UI wrapper completed every stage; its status readback records
  `completed`, every exit code zero, and a present result bundle. The test log
  reports nine tests and zero failures in 7.678 seconds. The affected visible
  transition passed in 6.472 seconds, the four-state atomic predicate in 0.133
  seconds, and shared six-second policy compatibility in 0.149 seconds.
  Initial/preinstalled event, named scheduled readback, final watchdog
  readback, and manual cancellable scheduling passed in 0.091, 0.198, 0.142,
  and 0.085 seconds. The 500-generation cancellation/replacement and
  100-lifecycle schedule Pressure regressions passed in 0.157 and 0.253
  seconds. Manually controlled callback delivery confirms scheduling delay
  cannot produce success until the exact snapshot readback is satisfied.
- Validation scope: Unit and Behavior are not relevant because the slice
  changes UI-test orchestration and deterministic UI-target rules. UI and
  Pressure are covered by the signed invocation above.
- Diagnostics and Process/Tooling: the affected source parses directly,
  `git diff --check` passes, and its 425 lines retain one permission-observation
  responsibility. Both raw timeout arguments are removed, leaving 449
  occurrences across 17 files. The build retains 80 unrelated compiler
  warnings and two destination-selection warnings; no warning names the
  affected source. The test log contains 51 known scheme-less CFURL and 19
  known CFPrefs shared-state diagnostics. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method readback is empty. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036BY` were removed after the commit;
  absence readback passed.
- Commit: `7af4b9351c983e0af50cc16a661e026186a6aac1`
  (`test(sync): migrate SYNC-036BY permission transition evidence`).

### SYNC-036BZ Closure Record

- Status: completed.
- Design and Oracle: the exact active Command+Tab status query combines the
  existing accessibility identifier with the localized active label/value
  predicate. Its generation-owned observer starts after the main key is set to
  Space and before either takeover selection. The initial readback must exist
  and remain false. The existing exact shared-defaults marker and persistent
  runtime-log sequence then converge independently; an explicit trigger
  readback or later scheduled readback completes only when the exact active
  status exists. Hotkey trigger logs, restored marker state, and process exit
  remain separate Oracles.
- Lifecycle and retained time policy: the test invocation owns observer start,
  cancellation, and App cleanup. Resolution, watchdog expiry, explicit
  teardown, and deinit cancel the observation generation and pending schedule.
  The named serial 100ms XCUI cadence remains the controlled fallback because
  Accessibility supplies no Settings projection callback. The named
  five-second status watchdog is solely a terminal failure bound; diagnostics
  retain generation, readback source, exact identifier/existence, and wait
  result.
- Validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The canonical
  signed UI wrapper completed every stage; its status readback records
  `completed`, every exit code zero, and a present result bundle. The test log
  reports eight tests and zero failures in 37.255 seconds. The complete signed
  takeover, trigger, quit, relaunch-cleanup, and restore path passed in 36.074
  seconds; its XCTest trace reached teardown at 16.06 seconds and then retained
  the runner's post-termination crash-report audit. The policy regression
  passed in 0.166 seconds. Initial/preinstalled event, named scheduled
  readback, final watchdog readback, and manual cancellable scheduling passed
  in 0.103, 0.217, 0.146, and 0.083 seconds. The 500-generation
  cancellation/replacement and 100-lifecycle schedule Pressure regressions
  passed in 0.201 and 0.264 seconds. Manual callback control confirms delayed
  scheduling cannot satisfy the exact predicate.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes UI-test synchronization and its policy regression. UI and Pressure
  are covered by the signed invocation above.
- Diagnostics and Process/Tooling: the affected source parses directly,
  `git diff --check` passes, and its 260 lines retain one Command+Tab
  observation responsibility. One raw timeout argument is removed, leaving
  448 occurrences across 17 files. The build retains 80 unrelated compiler
  warnings and two destination-selection warnings; no warning names the
  affected source. The test log contains 51 known scheme-less CFURL and 17
  known CFPrefs shared-state diagnostics. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method readback is empty. The final
  shared-defaults readback finds no pending-restore key. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036BZ` were removed after the commit;
  absence readback passed.
- Commit: `53372ea0309fc5e74eb0fb0e79aa12274fd033ec`
  (`test(sync): migrate SYNC-036BZ takeover status evidence`).

### SYNC-036CA Closure Record

- Status: completed.
- Design and Oracle: the test now captures a baseline file-offset/event
  generation and starts one runtime-log observation owner before either
  Command+Tab takeover selection. Its initial baseline-relative readback must
  remain unresolved. File-event or scheduled readbacks complete only after all
  four exact post-baseline markers describe the updated Command+Tab binding,
  disabled system shortcuts, active takeover, and AppDelegate reload. The
  shared-defaults marker, active Settings status, later hotkey delivery,
  restored marker, and process exit remain independent Oracles.
- Lifecycle and retained time policy: the test invocation explicitly owns and
  cancels both the runtime-log baseline/file-event source and observation
  owner. Resolution, watchdog expiry, explicit teardown, and deinit also cancel
  file events and the serial fallback schedule. The shared named 200ms cadence
  remains the controlled fallback for unavailable or coalesced file events.
  The named ten-second configuration-log watchdog is solely a terminal failure
  bound; diagnostics include missing markers, baseline/current file-event
  generations, character count, and bounded log tail.
- Validation: the canonical installer rebuilt and verified the fixed-path
  Apple Development-signed App for TeamIdentifier `96PUA726W9`. The canonical
  signed UI wrapper completed every stage; its status readback records
  `completed`, every exit code zero, and a present result bundle. The test log
  reports seven tests and zero failures in 36.357 seconds. The complete signed
  takeover/restore path passed in 35.711 seconds and reached functional
  teardown at 15.69 seconds before XCTest's crash-report audit. The new policy,
  post-baseline file append, file replacement/reset, event resolution, shared
  defaults, and 100-lifecycle initial/event/cancel/watchdog Pressure paths
  passed in 0.146, 0.092, 0.091, 0.092, 0.085, and 0.140 seconds.
- Validation scope: Unit and Behavior are not relevant because the slice
  changes UI-test synchronization and its policy regression. UI and Pressure
  are covered by the signed invocation above.
- Diagnostics and Process/Tooling: the affected source parses directly,
  `git diff --check` passes, and its 297 lines retain one Command+Tab
  observation responsibility. One raw timeout argument is removed, leaving
  447 occurrences across 17 files. The build retains 80 unrelated compiler
  warnings and two destination-selection warnings; no warning names the
  affected source. The test log contains 51 known scheme-less CFURL and 15
  known CFPrefs shared-state diagnostics. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method readback is empty. The final
  shared-defaults readback finds no pending-restore key. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CA` were removed after the commit;
  absence readback passed.
- Commit: `f0d0d8ade06895e83ab2747c091edffc0c661ac8`
  (`test(sync): migrate SYNC-036CA takeover log evidence`).

### SYNC-036CB Closure Record

- Status: completed.
- Design and Oracle: the test now captures a fresh runtime-log file-offset/event
  generation and starts a dedicated observation owner before App activation and
  Command+Tab synthesis. Its initial baseline-relative readback must remain
  unresolved. File-event or scheduled readbacks complete only after both exact
  post-baseline markers prove the forward hotkey was received while the panel
  was hidden and the Forward effect was applied. Configuration publication,
  active Settings status, restored marker, and process exit remain independent
  Oracles.
- Lifecycle and retained time policy: the test invocation explicitly owns and
  cancels both the runtime-log baseline/file-event source and observation
  owner. Resolution, watchdog expiry, explicit teardown, and deinit also cancel
  file events and the serial fallback schedule. The shared named 200ms cadence
  remains the controlled fallback for unavailable or coalesced file events.
  The named ten-second trigger-log watchdog is solely a terminal failure bound;
  diagnostics include missing markers, baseline/current file-event
  generations, character count, and bounded log tail.
- Validation: the canonical installer rebuilt the fixed-path App, whose signing
  metadata resolves TeamIdentifier `96PUA726W9`. The canonical signed UI wrapper
  completed every stage; its status readback records `completed`, every exit
  code zero, and a present result bundle. The test log reports seven tests and
  zero failures in 35.653 seconds. The complete signed takeover/restore path
  passed in 35.001 seconds and reached functional teardown at 14.87 seconds
  before XCTest's crash-report audit. The new policy, post-baseline file append,
  file replacement/reset, event resolution, 100-lifecycle
  initial/event/cancel/watchdog Pressure, and shared runtime-log policy paths
  passed in 0.144, 0.092, 0.091, 0.092, 0.140, and 0.093 seconds.
- Validation scope: Unit and Behavior are not relevant because the slice
  changes UI-test synchronization and its policy regression. UI and Pressure
  are covered by the signed invocation above.
- Diagnostics and Process/Tooling: the affected source parses directly,
  `git diff --check` passes, and its 334 lines retain one Command+Tab
  observation responsibility. One raw timeout argument is removed, leaving
  446 occurrences across 17 files. The build retains 80 unrelated compiler
  warnings and two destination-selection warnings; no warning names the
  affected source. The test log contains 51 known scheme-less CFURL and 15
  known CFPrefs shared-state diagnostics. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method readback is empty. The final
  shared-defaults readback finds no pending-restore key. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CB` were removed after the commit;
  absence readback passed.
- Commit: `d14f30d4784b52939552768073ece63758c66cda`
  (`test(sync): migrate SYNC-036CB trigger log evidence`).

### SYNC-036CC Closure Record

- Status: completed.
- Design and Oracle: before App activation and Command-Q synthesis, the test
  now starts a generation-owned application-state observer and requires its
  immediate baseline to be an exact running state. An immediate post-trigger
  readback plus scheduled readbacks can complete only on exact
  `XCUIApplication.State.notRunning`; pending-restore marker convergence and
  the later default-reset cleanup remain independent Oracles.
- Lifecycle and retained time policy: the trigger invocation owns the observer
  and explicitly cancels it on every return. Resolution, watchdog expiry,
  explicit teardown, and deinit cancel the serial schedule. XCTest exposes no
  termination notification for this target, so the shared named 100ms XCUI
  readback cadence remains a controlled conditional observation strategy. The
  named eight-second graceful-termination watchdog is solely a terminal failure
  bound and reports generation, observation source, final application state,
  and waiter result.
- Validation: the canonical installer rebuilt the fixed-path App, verified it
  as valid on disk and satisfying its Designated Requirement, and reported the
  Apple Development identity with TeamIdentifier `96PUA726W9`. The canonical
  signed UI wrapper completed every stage; its status readback records
  `completed`, every exit code zero, and a present result bundle. The test log
  reports seven tests and zero failures in 36.259 seconds. The complete signed
  takeover/restore path passed in 35.236 seconds and reached functional
  teardown at 15.11 seconds before XCTest's crash-report audit. The new policy,
  cancellation/replacement Pressure, initial/event evidence, named scheduled
  readback, watchdog final readback, and 100-lifecycle scheduled Pressure paths
  passed in 0.127, 0.205, 0.103, 0.195, 0.136, and 0.257 seconds.
- Validation scope: Unit and Behavior are not relevant because the slice
  changes UI-test synchronization and its policy regression. UI and Pressure
  are covered by the signed invocation above.
- Diagnostics and Process/Tooling: the affected source parses directly,
  `git diff --check` passes, and its 394 lines retain one Command+Tab
  observation responsibility. One raw timeout argument is removed, leaving
  445 occurrences across 17 files. The final incremental build emits no
  compiler warning and retains two destination-selection warnings. The test
  log contains 17 known scheme-less CFURL and 15
  known CFPrefs shared-state diagnostics. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method readback is empty. The final
  shared-defaults readback finds no pending-restore key. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CC` were removed after the commit;
  absence readback passed.
- Commit: `0e7e6c549815ab98f1c40ae66ed9f188e7a8e9f9`
  (`test(sync): migrate SYNC-036CC graceful exit evidence`).

### SYNC-036CD Closure Record

- Status: completed.
- Design and Oracle: the shared termination helper now accepts a caller-owned
  termination request while retaining exact initial, immediate post-trigger,
  waiter, and final application-state evidence. One composed cleanup contract
  first requests Command-Q, then escalates to `terminate()` only when the
  graceful phase's final readback remains running. Initial `.notRunning`
  evidence suppresses both requests. Final success requires an exact
  `.notRunning` readback; both phase records remain available on failure.
- Lifecycle and retained time policy: the deferred test invocation owns both
  synchronous XCTest state waits and the final assertion. The named six-second
  graceful bound controls forced-termination escalation; scheduling pressure
  may select that fallback without changing the final state Oracle. The
  separate named six-second forced-exit watchdog is solely a terminal failure
  bound. No recurring timer, observer token, or retry survives either helper
  invocation.
- Validation: the canonical installer rebuilt the fixed-path App, verified it
  as valid on disk and satisfying its Designated Requirement, and reported the
  Apple Development identity with TeamIdentifier `96PUA726W9`. The canonical
  signed UI wrapper completed every stage; its status readback records
  `completed`, every exit code zero, and a present result bundle. The test log
  reports 13 tests and zero failures in 36.615 seconds. The complete signed
  takeover/restore path passed in 35.528 seconds and reached functional
  teardown at 15.40 seconds before XCTest's crash-report audit. The cleanup
  initial, graceful, escalated, 100-lifecycle Pressure, dual-watchdog, and
  policy paths passed in 0.122, 0.091, 0.083, 0.083, 0.091, and 0.092 seconds.
  The six pre-existing termination initial/immediate/delayed/wait-boundary,
  watchdog, and repeated-lifecycle contracts also passed in 0.083--0.092
  seconds each.
- Validation scope: Unit and Behavior are not relevant because the slice
  changes shared UI-test process synchronization and its deterministic
  regressions. UI and Pressure are covered by the signed invocation above.
- Diagnostics and Process/Tooling: both affected sources parse directly,
  `git diff --check` passes, and the 547-line termination-evidence file plus
  419-line Command+Tab file retain their respective single responsibilities.
  Two raw timeout arguments are removed, leaving 443 occurrences across 17
  files. The final incremental build retains two unrelated compiler warnings
  and two
  destination-selection warnings; no warning names either affected source.
  The test log contains 17 known scheme-less CFURL and 27 known CFPrefs
  shared-state diagnostics. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method readback is empty. The final
  shared-defaults readback finds no pending-restore key. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CD` were removed after the commit;
  absence readback passed.
- Commit: `77275291c3f7ab24ab1eec8c70eff4aa3f76005c`
  (`test(sync): migrate SYNC-036CD fallback cleanup evidence`).

### SYNC-036CE Closure Record

- Status: completed.
- Design and Oracle: default-reset App cleanup now delegates to the shared
  application-termination evidence helper. It captures exact initial state
  before `terminate()`, reads state immediately after the trigger, conditionally
  uses XCTest's persistent `.notRunning` wait, and always captures final state.
  Only the final exact `.notRunning` readback satisfies the assertion; failure
  reports target, initial, post-trigger, waiter, and final evidence.
- Lifecycle and retained time policy: the reset helper invocation owns the
  synchronous state wait and final assertion. The named six-second
  default-reset exit watchdog is solely a terminal failure bound. Immediate
  completion skips the wait, and no recurring timer, observer, or retry survives
  the invocation.
- Validation: the canonical installer rebuilt the fixed-path App, verified it
  as valid on disk and satisfying its Designated Requirement, and reported the
  Apple Development identity with TeamIdentifier `96PUA726W9`. The canonical
  signed UI wrapper completed every stage; its status readback records
  `completed`, every exit code zero, and a present result bundle. The test log
  reports eight tests and zero failures in 36.353 seconds. The complete signed
  takeover/default-reset path passed in 35.721 seconds and reached functional
  teardown at 15.61 seconds before XCTest's crash-report audit. The new policy
  passed in 0.083 seconds; the six termination initial/immediate/delayed,
  wait-boundary, watchdog, and 100-lifecycle Pressure contracts passed in
  0.083--0.124 seconds.
- Validation scope: Unit and Behavior are not relevant because the slice
  changes UI-test cleanup synchronization and its policy regression. UI and
  Pressure are covered by the signed invocation above.
- Diagnostics and Process/Tooling: the affected source parses directly,
  `git diff --check` passes, and its 438 lines retain one Command+Tab
  observation responsibility. Its final raw timeout argument is removed,
  leaving 442 occurrences across 16 files. The build retains 80 unrelated
  compiler warnings and two destination-selection warnings; no warning names
  the affected source. The test log contains 51 known scheme-less CFURL and 17
  known CFPrefs shared-state diagnostics. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, and input-method readback is empty. The final
  shared-defaults readback finds no pending-restore key. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  result bundles, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CE` were removed after the commit;
  absence readback passed.
- Commit: `d42e56aba3c329e55fa5456f531880171cb1ebdc`
  (`test(sync): migrate SYNC-036CE defaults reset exit evidence`).

### SYNC-036CF Closure Record

- Status: completed.
- Design and Oracle: both Space Fixture tests now resolve the exact window-mode
  accessibility query and start a generation-owned observer before launching
  the fixture. The synchronous initial readback must report the marker absent.
  After the caller-owned launch/readiness trigger, an immediate trigger
  readback and scheduled readbacks can resolve only when the exact queried
  identifier exists with its exact `Standard Window` or `Fullscreen Target`
  label. The first path keeps fixture workflow readiness and metadata as an
  independent Oracle; the second combines existence and label in one atomic
  evidence predicate.
- Lifecycle and retained time policy: each assertion invocation owns the
  observer, trigger, synchronous wait, and explicit cancellation, while each
  test invocation owns exact fixture-process termination. Resolution,
  watchdog expiry, cancellation, and deinit stop the shared serial schedule.
  XCTest exposes no accessibility-publication callback, so the shared named
  100ms XCUI conditional-readback cadence remains. The named five-second
  publication watchdog is solely a terminal failure bound and reports expected
  identifier/label, generation, source, final identifier/existence/label, and
  waiter result.
- Validation: the canonical installer rebuilt and installed the fixed-path
  App, verified it as valid on disk and satisfying its Designated Requirement,
  and reported the Apple Development identity with TeamIdentifier
  `96PUA726W9`. The canonical signed UI wrapper rebuilt the real Space Fixture
  topology and signed its XCTest Runner with the same development identity;
  runner validation passed. Its status readback records `completed`, every
  fixture/signing/build/test exit code zero, and a present result bundle. The
  log reports 12 tests and zero failures in 11.065 seconds. The configured-title
  and fullscreen-target real-fixture paths passed in 5.217 and 4.150 seconds.
  Policy, initial exact evidence, 20 delayed wrong-identity/wrong-label
  observations followed by exact trigger evidence, 100 cancel/restart stale
  generations, and watchdog final evidence passed in 0.132--0.143 seconds.
  The shared condition initial/event, cancel/replacement, named schedule,
  watchdog, and 100-lifecycle Pressure contracts passed in 0.142--0.267
  seconds.
- Validation scope: Unit and Behavior are not relevant because the slice
  changes UI-test synchronization and its deterministic policy/owner
  regression. UI and Pressure are covered by the signed invocation above.
- Diagnostics and Process/Tooling: the three affected Swift files parse
  directly, the Xcode project plist validates, and `git diff --check` passes.
  The new owner and regression files contain 149 and 196 lines; the existing
  412-line fixture-app file retains one fixture lifecycle responsibility. Two
  raw timeout arguments are removed, leaving 440 occurrences across 16 files.
  The validation build retains 80 unrelated compiler warnings, fixture
  preparation retains one unrelated warning, and three destination-selection
  warnings are expected; no warning names an affected source. The test log
  contains 119 known scheme-less CFURL and 24 known CFPrefs shared-state
  diagnostics. Targeted severe runtime, sanitizer, main-thread-checker, XPC,
  and input-method readback is empty. Exact post-run `pgrep -x` readback finds
  no FlowTab, UI-test runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable result bundles, DerivedData,
  caches, and logs under `.build-local/evidence-driven-sync/SYNC-036CF` were
  removed after the commit; absence readback passed.
- Commit: `0c1c6272a7548067f91d9b98464cf4ad845d1319`
  (`test(sync): migrate SYNC-036CF fixture mode evidence`).

### SYNC-036CG Closure Record

- Status: completed.
- Design and Oracle: each Home sidebar permission status is now rendered by
  the existing explicit AppKit accessibility text boundary and publishes a
  stable status identifier plus its exact localized label. The preflight
  resolves the card, both permission rows, both status elements, the missing
  banner, settings-action candidates, and dismiss action before launch or Home
  navigation, then starts one generation-owned observer. Its immediate,
  trigger, and scheduled readbacks resolve only a complete atomic projection.
  Both exact `Granted`/`已授予` labels prove success. A terminal missing
  projection requires two exact status labels with at least one
  `Missing`/`未授权`, the exact banner, a hittable settings action, and the
  dismiss action; it resolves immediately and produces complete failure
  evidence. App readiness and Home navigation remain independent Oracles.
- Evidence-first correction: the first canonical build exposed the exact
  missing `where:` argument label. The next signed run proved that the card and
  both row identifiers existed while card-scoped static-text labels remained
  empty. A subsequent signed run proved that identifiers attached to the
  SwiftUI status texts were merged out of the macOS accessibility tree. The
  focused AppKit accessibility elements then appeared under both exact status
  identifiers in the real XCTest tree and supplied the terminal labels used by
  the final Oracle.
- Lifecycle and retained time policy: the helper installs the observation
  before App launch or Home navigation, performs an initial readback, and owns
  explicit cancellation. Resolution, watchdog expiry, cancellation, and deinit
  stop the shared serial schedule; the calling test owns App and fixture
  termination. XCTest exposes no accessibility-projection callback, so the
  shared named 100ms XCUI conditional-readback cadence remains. The named
  six-second permission-state watchdog is solely a terminal failure bound and
  reports generation, source, App state, every card/row/status/banner/action
  readback, and waiter result.
- Validation: the canonical installer rebuilt and installed the fixed-path
  App, verified it as valid on disk and satisfying its Designated Requirement,
  and reported the Apple Development identity with TeamIdentifier
  `96PUA726W9`. The canonical signed UI wrapper rebuilt the real Space Fixture
  topology and signed its XCTest Runner with the same identity. The final
  status readbacks for attempts 004 through 007 all record `completed`, every
  applicable fixture/signing/build/test exit code zero, and a present result
  bundle. The real authorized-readback and fixture-topology test passed in
  11.660 seconds. Five exact projection/initial/missing/100-generation
  Pressure/watchdog tests plus eight shared observer and scheduler lifecycle
  tests and the affected Home path passed as 14 tests with zero failures in
  17.618 seconds. The focused current Home status-publication UI regression
  passed in 5.101 seconds. The final zero-candidate action invariant rerun
  passed in 0.178 seconds.
- Validation scope: Unit and Behavior are not relevant because the slice
  changes the Home accessibility publication contract and UI-test
  synchronization owner. UI and Pressure are covered by the signed invocations
  above.
- Diagnostics and Process/Tooling: the affected Swift files parse directly,
  the Xcode project plist validates, and `git diff --check` passes. The new
  observation and regression files contain 374 and 325 lines; the extracted
  fixture-app file contains 376 lines, and the 445-line Home overview file
  retains one overview-component responsibility. Three raw timeout arguments
  are removed, leaving 437 occurrences across 16 files. Validation emits only
  two unrelated existing Sendable warning classes plus expected
  destination-selection warnings; no warning names an affected source. The
  four final test logs contain 68 known scheme-less CFURL and 37 known CFPrefs
  shared-state diagnostics. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, runner-launch, and input-method scanning is empty.
  Exact post-run `pgrep -x` readback finds no FlowTab, UI-test runner,
  xcodebuild, or Space fixture process. Startup `prompts.zip` and the three
  pre-existing local Skill/reference edits remain unchanged and outside this
  slice. Rebuildable results, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CG` were removed after the commit;
  absence readback passed.
- Commit: `8514fc58c68a604241eede1b2f9c0f65af5d8b1e`
  (`test(sync): migrate SYNC-036CG permission evidence`).

### SYNC-036CH Closure Record

- Status: completed.
- Design and Oracle: the existing AppKit Settings Search permission summary
  now publishes a stable accessibility identifier. The assertion resolves the
  exact scope control, permission summary, and Window-option query before
  Settings navigation, then starts one generation-owned observation. Its
  initial, trigger, and scheduled readbacks accept only one atomic app-only
  projection: scope value `app`, disabled scope control, an exact English or
  Chinese Accessibility-required summary, and no Window option. The disabled
  permission-specific projection is persistent evidence that Window scope is
  unavailable; elapsed absence is no longer a success condition.
- Lifecycle and retained time policy: the assertion helper installs and owns
  the observer before navigation and explicitly cancels it after resolution.
  Resolution, watchdog expiry, cancellation, and deinit stop the shared serial
  schedule. XCTest exposes no accessibility-projection callback, so the shared
  named 100ms XCUI conditional-readback cadence remains. The named six-second
  projection watchdog is solely a failure upper bound and reports generation,
  source, control existence/value/enabled state, summary existence/text,
  Window-option existence, and waiter result. App readiness and Settings
  navigation remain independent evidence contracts.
- Validation: the canonical installer rebuilt and installed the fixed-path
  App, verified it as valid on disk and satisfying its Designated Requirement,
  and reported the Apple Development identity with TeamIdentifier
  `96PUA726W9`. The canonical signed UI wrapper ran six policy, exact-state,
  initial-state, delayed-readback/cancellation, 100-generation Pressure, and
  watchdog regressions plus the affected mock-permission UI path. All seven
  tests passed with zero failures in 4.997 seconds; the affected UI path passed
  in 4.216 seconds. `status.json` records `completed`, every applicable
  signing/build/test exit code zero, and a present result bundle.
- Validation scope: Unit and Behavior are not relevant because the slice adds
  an AppKit accessibility publication identifier and changes only its UI-test
  synchronization owner. UI and Pressure are covered by the signed invocation
  above.
- Diagnostics and Process/Tooling: the affected Swift files parse directly,
  the Xcode project plist validates, and `git diff --check` passes. The new
  observation and regression files contain 174 and 215 lines; the touched
  Settings Search test and production files contain 480 and 204 lines. Two raw
  timeout arguments are removed, leaving 435 occurrences across 16 files. No
  build warning names an affected source; targeted severe runtime, sanitizer,
  main-thread-checker, XPC, runner-launch, and input-method scanning is empty.
  The test log contains 15 known CFPrefs shared-state diagnostics and no
  scheme-less CFURL diagnostic. Exact post-run `pgrep -x` readback finds no
  FlowTab, UI-test runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable results, DerivedData, caches,
  and logs under `.build-local/evidence-driven-sync/SYNC-036CH` were removed
  after the commit; absence readback passed.
- Commit: `678702d1b7c08f01816ee87f00afa5d0a9437cd0`
  (`test(sync): migrate SYNC-036CH search scope evidence`).

### SYNC-036CI Closure Record

- Status: completed.
- Design and Oracle: the assertion resolves XCTest's exact fixed-title
  `FlowTab` window and the exact Logs-content element before Command-W, starts
  one generation-owned observer, and requires an initial atomic open-Logs
  projection. Its immediate trigger and scheduled readbacks accept closure
  only while the App remains foreground/background running and both the exact
  titled window and Logs content are absent. Finder activation, status-item
  reopening, restored Logs selection, and accessory activation policy remain
  independent readbacks after closure resolves.
- Evidence-first correction: the first signed path showed that a window-level
  AppKit accessibility identifier was not published as an XCTest window
  identifier: the App remained running and Logs content existed while that
  query reported no window. The production identifier experiment was fully
  reverted, preserving the window boundary, and the observer now uses the
  exact stable title already exposed by XCTest. The next canonical build found
  a Swift line-break parsing error in that subscript; the corrected expression
  passed direct parsing before the final signed run.
- Lifecycle and retained time policy: the assertion helper installs and owns
  the observer before Command-W and cancels it explicitly. Resolution,
  watchdog expiry, cancellation, and deinit stop the shared serial schedule.
  XCTest exposes no target-window-close callback, so the shared named 100ms
  XCUI conditional-readback cadence remains. The named six-second close
  watchdog is solely a failure upper bound and reports generation, source,
  App state/running classification, exact window existence, Logs-content
  existence, and waiter result.
- Validation: the canonical installer rebuilt and installed the fixed-path
  App, verified it as valid on disk and satisfying its Designated Requirement,
  and reported the Apple Development identity with TeamIdentifier
  `96PUA726W9`. The final canonical signed UI invocation ran six policy,
  exact-state, initial-state, delayed-readback/cancellation, 100-generation
  Pressure, and watchdog regressions plus the affected status-item workflow.
  All seven tests passed with zero failures in 7.309 seconds; the affected real
  UI path passed in 6.539 seconds. `status.json` records `completed`, every
  applicable signing/build/test exit code zero, and a present result bundle.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only the UI-test synchronization contract. UI and Pressure are
  covered by the signed invocation above.
- Diagnostics and Process/Tooling: both new Swift files parse directly, the
  Xcode project plist validates, and `git diff --check` passes. The owner and
  regression files contain 182 and 217 lines. The touched 775-line UI workflow
  remains below the 800-line guardrail and retains one Home/Logs workflow
  responsibility. One raw timeout argument is removed, leaving 434 occurrences
  across 16 files. The final build retains 62 unrelated existing source-warning
  lines and no warning names an affected source. Targeted severe runtime,
  sanitizer, main-thread-checker, XPC, runner-launch, permission-denial, and
  input-method scanning is empty; the runner log contains 15 known CFPrefs
  shared-state diagnostics. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test runner, xcodebuild, or Space fixture process. Startup `prompts.zip`
  and the three pre-existing local Skill/reference edits remain unchanged and
  outside this slice. Rebuildable results, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CI` were removed after the commit;
  absence readback passed.
- Commit: `8740bb701de69323305c3c79b5e3dd6943be9417`
  (`test(sync): migrate SYNC-036CI window close evidence`).

### SYNC-036CJ Closure Record

- Status: completed.
- Design and Oracle: the status-item workflow resolves XCTest's exact
  fixed-title `FlowTab` window and exact Logs-content element before the tap,
  then starts a generation-owned observer from the Finder-frontmost closed
  projection. Evidence acceptance remains gated until the trigger closure
  returns. An immediate trigger readback closes the synchronous race, while
  scheduled readbacks accept only one atomic projection with the App still
  running and both the exact window and Logs content present. Exact
  NSWorkspace frontmost identity and regular-to-accessory policy logs remain
  independent evidence contracts.
- Shared infrastructure and lifecycle: the prior close snapshot now has the
  neutral `StatusItemWindowProjectionSnapshot` name and shares one fixed-title
  identity with the reopen owner; its state rules and close owner remain
  unchanged. The reopen assertion installs before the tap and owns explicit
  cancellation. Resolution, watchdog expiry, cancellation, and deinit stop the
  shared serial schedule. XCTest exposes no target-window publication callback,
  so the named 100ms XCUI conditional-readback cadence remains. The named
  eight-second reopen watchdog is solely a failure upper bound and reports
  generation, source, acceptance gate, App state/running classification, exact
  window existence, Logs-content existence, and waiter result.
- Validation: the canonical installer rebuilt and installed the fixed-path
  App, verified it as valid on disk and satisfying its Designated Requirement,
  and reported the Apple Development identity with TeamIdentifier
  `96PUA726W9`. The canonical signed UI wrapper ran the six prior close-owner
  regressions, five reopen policy/initial/gated-delayed/cancellation/
  100-generation Pressure/watchdog regressions, and the affected status-item
  UI path. The final-source rerun passed all 12 tests with zero failures in
  6.434 seconds; the real UI path passed in 5.013 seconds. Both status ledgers
  record `completed`, every applicable signing/build/test exit code zero, and
  a present result bundle.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only UI-test synchronization infrastructure. UI and Pressure are
  covered by the signed invocation above, including expanded regression of the
  shared close projection.
- Diagnostics and Process/Tooling: all five affected Swift files parse
  directly, the Xcode project plist validates, and `git diff --check` passes.
  The close owner/tests contain 185 and 224 lines, the reopen owner/tests
  contain 171 and 199 lines, and the 776-line Home/Logs workflow remains below
  the 800-line guardrail with one workflow responsibility. One raw timeout
  argument is removed, leaving 433 occurrences across 16 files. The fresh
  build retains 80 unrelated existing source-warning lines; the final
  incremental build is warning-free. Targeted severe runtime, sanitizer,
  main-thread-checker, XPC, runner-launch, permission-denial, and input-method
  scanning is empty; the runner log contains 25 known CFPrefs shared-state
  diagnostics. Exact post-run `pgrep -x` readback finds no FlowTab, UI-test
  runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable results, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CJ` were removed after the commit;
  absence readback passed.
- Commit: `5d3eb5211ad089ce686377a1ce718b7fd9599cc4`
  (`test(sync): migrate SYNC-036CJ window reopen evidence`).

### SYNC-036CK Closure Record

- Status: completed.
- Design and Oracle: the test now freezes the runtime-log byte anchors and
  file-event generation, installs a dedicated activation-policy owner, and
  performs its initial readback before the status-item tap. The persistent
  post-baseline Oracle requires the exact temporary-regular marker followed by
  the exact stable-accessory marker. Reversed and partial contents remain
  unresolved. An immediate trigger-return readback closes the synchronous
  event race; exact window reopening and NSWorkspace frontmost identity remain
  independent evidence contracts.
- Lifecycle and retained time policy: the status-item helper owns the
  generation-scoped condition owner and cancels it on resolution, watchdog,
  assertion exit, and deinit through the shared owner. The test invocation
  explicitly cancels the baseline file-event source. Cross-process App logging
  has no completion callback, so baseline filesystem events plus the shared
  named cancellable 200ms readback cadence remain. The named eight-second
  transition watchdog is solely a failure upper bound and reports the first
  unmatched ordered marker, baseline/current event generations, character
  count, final log tail, and waiter result.
- Validation: the canonical installer rebuilt and installed the fixed-path
  App, verified it as valid on disk and satisfying its Designated Requirement,
  and reported the Apple Development identity with TeamIdentifier
  `96PUA726W9`. The canonical signed UI wrapper ran six exact-order/initial/
  event/trigger/cancellation/100-generation Pressure/watchdog regressions, the
  shared real file-append event test, the shared runtime-log lifecycle Pressure
  test, and the affected status-item workflow. All nine tests passed with zero
  failures in 6.461 seconds; the real UI path passed in 5.340 seconds.
  `status.json` records `completed`, every applicable signing/build/test exit
  code zero, and a present result bundle.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only UI-test cross-process observation. UI, file-event integration,
  and Pressure are covered by the signed invocation above.
- Diagnostics and Process/Tooling: all three affected Swift files parse
  directly, the Xcode project plist validates, and `git diff --check` passes.
  The new owner and regression files contain 171 and 244 lines; the touched
  773-line Home/Logs workflow remains below the 800-line guardrail with one
  workflow responsibility. One raw timeout argument is removed, leaving 432
  occurrences across 16 files. The fresh build retains 80 unrelated existing
  source-warning lines and no warning names an affected source. Targeted severe
  runtime, sanitizer, main-thread-checker, XPC, runner-launch,
  permission-denial, and input-method scanning is empty; the runner log
  contains 19 known CFPrefs shared-state diagnostics. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  results, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CK` were removed after the commit;
  absence readback passed.
- Commit: `557d6a6da37b7aa28671bb03a537a41c87339540`
  (`test(sync): migrate SYNC-036CK activation policy evidence`).

### SYNC-036CL Closure Record

- Status: completed.
- Design and Oracle: the status-menu workflow resolves the exact Quit item,
  then routes its tap through the shared application-termination owner. The
  owner reads the exact App state before the trigger, immediately after the
  trigger, and once more after the conditional XCTest state wait. Success is
  decided solely by the final `.notRunning` readback; the waiter result remains
  diagnostic. Existing exact initial, immediate, delayed, wait-boundary, and
  watchdog regressions preserve the shared state-transition contract.
- Lifecycle and retained time policy: the synchronous test invocation owns the
  resolved menu item, trigger, and termination wait, with no recurring work to
  outlive it. The named eight-second `statusItemMenuQuitWatchdog` remains solely
  a terminal failure upper bound and reports target, initial state, whether the
  trigger was requested, immediate post-trigger state, waiter result, and final
  state. The policy regression requires that bound to remain finite, positive,
  and compatible with the prior user-visible workflow.
- Validation: the canonical installer rebuilt and installed the fixed-path
  App; independent code-signing readback reports it valid on disk and satisfying
  its Designated Requirement. The canonical signed UI wrapper ran the named
  policy regression, five exact-state regressions covering initial, immediate,
  delayed, wait-boundary, and watchdog outcomes, the 100-lifecycle Pressure
  regression, and the affected status-menu workflow. All eight tests passed
  with zero failures in 24.366 seconds; the real UI path passed in 23.542
  seconds. `status.json` records
  `completed`, every applicable signing/build/test exit code zero, and a
  present result bundle.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only the UI-test process-lifecycle synchronization contract. UI and
  Pressure are covered by the signed invocation above.
- Diagnostics and Process/Tooling: both affected Swift files parse directly
  and `git diff --check` passes. The shared termination owner contains 565
  lines and retains one process-lifecycle responsibility; the 785-line
  Home/Logs workflow remains below the 800-line guardrail. One raw timeout
  argument is removed, leaving 431 occurrences across 16 files. The fresh
  build retains 80 unrelated existing source-warning lines and no warning
  names an affected source. Targeted severe runtime, fatal-error, assertion,
  and test-failure scanning is empty; the test log contains 17 known CFPrefs
  shared-state diagnostics and no scheme-less CFURL diagnostic. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test runner, xcodebuild, or Space
  fixture process. Startup `prompts.zip` and the three pre-existing local
  Skill/reference edits remain unchanged and outside this slice. Rebuildable
  results, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CL` were removed after the commit;
  absence readback passed.
- Commit: `7e0a46aed1af07f81f38c84abe6943e45bc0d04b`
  (`test(sync): migrate SYNC-036CL status menu quit evidence`).

### SYNC-036CM Closure Record

- Status: completed.
- Design and Oracle: the granted-permission Home assertion resolves Home
  content, the exact permission banner, and the exact permission action before
  navigation, then starts one generation-owned observer. Evidence acceptance
  remains gated until the Home-tab trigger returns, and an immediate trigger
  readback closes the synchronous race. Success requires one atomic snapshot
  where Home content exists and both permission elements are absent; elapsed
  absence is no longer a success signal.
- Lifecycle and retained time policy: XCTest exposes no Home-projection
  publication callback, so the shared named 100ms XCUI readback cadence remains
  as a conditional fallback. The assertion helper owns the observation and
  cancels it explicitly; resolution, watchdog expiry, cancellation, and deinit
  stop the schedule. The compatible named two-second
  `homeHiddenProjectionWatchdog` remains solely a terminal failure upper bound
  and reports the trigger gate plus the final content, banner, action,
  generation, source, and waiter evidence.
- Validation: the canonical installer rebuilt and signed the fixed-path App,
  verified it as valid on disk and satisfying its Designated Requirement, and
  reported the Apple Development identity with TeamIdentifier `96PUA726W9`.
  The canonical signed UI wrapper ran two exact policy/truth-table regressions,
  six shared initial/event/reentrancy/cancellation/scheduled/watchdog owner
  regressions, two scheduler cancellation/reentrancy regressions, and the
  affected Home workflow. All 11 tests passed with zero failures in 5.986
  seconds; the real UI path passed in 4.426 seconds. `status.json` records
  `completed`, every applicable signing/build/test exit code zero, and a
  present result bundle.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only the UI-test permission-projection synchronization contract. UI
  and Pressure are covered by the signed invocation above.
- Diagnostics and Process/Tooling: both affected Swift files parse directly
  and `git diff --check` passes. The 545-line permission-observation file
  retains one responsibility; the 789-line Home/Logs workflow remains below
  the 800-line guardrail. One raw timeout argument is removed, leaving 430
  occurrences across 16 files. The fresh build retains 80 unrelated existing
  source-warning lines and no warning names an affected source. Targeted severe
  runtime, fatal-error, assertion, and test-failure scanning is empty; the test
  log contains 23 known CFPrefs shared-state diagnostics and no scheme-less
  CFURL diagnostic. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test runner, xcodebuild, or Space fixture process. Startup `prompts.zip`
  and the three pre-existing local Skill/reference edits remain unchanged and
  outside this slice. Rebuildable results, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CM` were removed after the commit;
  absence readback passed.
- Commit: `663c3a998a62f5216ebc4579f0bb277efcaa501d`
  (`test(sync): migrate SYNC-036CM granted permission evidence`).

### SYNC-036CN Closure Record

- Status: completed.
- Design and Oracle: the shared hidden-permission projection now accepts a
  scenario label and serves both the granted-permission and persisted-reminder
  paths. For the persistence path it resolves the exact App and UI elements and
  starts observation before relaunch. The initial readback records `.notRunning`
  without querying unavailable UI. After relaunch plus Home navigation returns,
  an immediate readback closes the synchronous race. Success requires one
  atomic foreground/background-running snapshot with Home content present and
  both the permission banner and action absent.
- Evidence-first correction: the first signed run passed all 12 tests, while
  its trace showed that prelaunch XCUI element queries invoked XCTest's internal
  retries and extended the persistence path to 14.855 seconds. Adding exact App
  state to the snapshot makes non-running readback immediate and defers element
  reads until they are observable. The final identical test set preserved the
  result and reduced that path to 8.425 seconds.
- Lifecycle and retained time policy: XCTest exposes no Home-projection
  publication callback, so the shared named 100ms XCUI readback cadence remains
  as a conditional fallback after the immediate readbacks. The assertion helper
  owns explicit cancellation; resolution, watchdog expiry, cancellation, and
  deinit stop the schedule. The compatible named two-second
  `homeHiddenProjectionWatchdog` remains solely a terminal failure upper bound
  and reports scenario, trigger gate, exact App state, Home content, banner,
  action, generation, source, and waiter evidence.
- Validation: the canonical installer rebuilt and signed the fixed-path App,
  verified it as valid on disk and satisfying its Designated Requirement, and
  reported the Apple Development identity with TeamIdentifier `96PUA726W9`.
  The final canonical signed UI wrapper ran the two shared policy/state-rule
  regressions, six shared initial/event/reentrancy/cancellation/scheduled/
  watchdog owner regressions, two scheduler cancellation/reentrancy regressions,
  and both affected Home workflows. All 12 tests passed with zero failures in
  12.881 seconds; the granted path passed in 3.125 seconds and the persisted
  reminder path passed in 8.425 seconds. `status.json` records `completed`,
  every applicable signing/build/test exit code zero, and a present result
  bundle.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only UI-test process/projection synchronization. UI and Pressure are
  covered by the signed invocation above.
- Diagnostics and Process/Tooling: both affected Swift files parse directly
  and `git diff --check` passes. The 576-line permission-observation file
  retains one responsibility; the 796-line Home/Logs workflow remains below
  the 800-line guardrail. One raw timeout argument is removed, leaving 429
  occurrences across 16 files. The fresh build retains 80 unrelated existing
  source-warning lines; the final incremental build is warning-free. Targeted
  severe runtime, fatal-error, assertion, and test-failure scanning is empty;
  the final test log contains 26 known CFPrefs shared-state diagnostics and no
  scheme-less CFURL diagnostic. Exact post-run `pgrep -x` readback finds no
  FlowTab, UI-test runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable results, DerivedData, caches,
  and logs under `.build-local/evidence-driven-sync/SYNC-036CN` were removed
  after the commit; absence readback passed.
- Commit: `bff3ba34437a3ad811b7cf7796bd0ff48652c7df`
  (`test(sync): migrate SYNC-036CN reminder persistence evidence`).

### SYNC-036CO Closure Record

- Status: completed.
- Design and Oracle: the shared hidden-permission projection is installed
  before the exact Dismiss trigger. Its initial readback requires a running App,
  loaded Home content, and the exact Open Settings and Dismiss controls. The
  banner container remains diagnostic-only because signed XCTest published the
  two controls while reporting that container absent. After the tap returns,
  an immediate readback accepts only one atomic running-App snapshot with Home
  content present and the banner, Open Settings, and Dismiss elements absent.
- Evidence-first correction: canonical attempt 001 ran 13 selected tests and
  failed with two concrete signals. The Dismiss path reported
  `applicationState=rawValue 4`, `homeContentExists=true`,
  `permissionBannerExists=false`, and `permissionActionExists=true` while the
  exact Dismiss button was present, establishing the exact controls as the
  stable visible baseline. The persisted-reminder path raised
  `NSInternalInconsistencyException: Invalid usage accessing unfinished future
  result from nested run loop` while a scheduled readback entered
  `homeContent.exists` during `XCUIApplication.launch`, establishing deferred
  schedule activation as a lifecycle requirement. `status.json` records exit
  65, 13 executed tests, two failures with one unexpected, and a present bundle
  at
  `.build-local/evidence-driven-sync/SYNC-036CO/attempt-001/results/FlowTabUITests.xcresult`.
  The reproduction used
  `./scripts/testing/run-ui-tests-local.sh --build-root
  ./.build-local/evidence-driven-sync/SYNC-036CO/ui-build --output-root
  ./.build-local/evidence-driven-sync/SYNC-036CO/attempt-001
  --skip-space-fixtures` with exact `-only-testing` filters for the two Home
  hidden-projection policy/state tests, six shared condition-owner tests, two
  scheduler tests, `testHomePermissionBannerHiddenWhenPermissionsGranted`,
  `testPermissionReminderTogglePersistsAcrossRelaunch`, and
  `testPermissionDismissPersistsAcrossRelaunch`.
- Lifecycle and retained time policy: the generic deferred-readback
  registration now lives with the shared condition owner and is reused by the
  Home app-selection and permission-projection owners. Registration and the
  initial readback precede the trigger; the named 100ms main-RunLoop schedule is
  activated only if the immediate post-trigger readback remains unsatisfied.
  Resolution, watchdog expiry, explicit cancellation, and deinit all cancel the
  schedule. XCTest exposes no Home-projection callback, so the named cadence is
  retained as a conditional fallback. The named two-second hidden-projection
  watchdog remains solely a terminal failure upper bound with scenario, trigger
  gate, App state, exact element projection, generation, source, and final
  waiter evidence.
- Validation: the canonical installer supplied the fixed-path signed App, and
  the canonical signed UI wrapper verified the rebuilt runner as valid on disk
  and satisfying its Designated Requirement under
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`. Attempt 002 ran the
  original 13 tests plus all six Home app-selection deferred-readback initial,
  synchronous-trigger, scheduled, cancellation-Pressure, watchdog, and
  monotonic-budget regressions. All 19 tests passed with zero failures in
  21.972 seconds. The granted path passed in 3.159 seconds, the full Dismiss
  workflow in 8.867 seconds, and the reminder persistence path in 8.049
  seconds. `status.json` records `completed`, every applicable
  signing/build/test/log exit code zero, and a present result bundle.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes only UI-test synchronization infrastructure and a UI-test workflow.
  UI and Pressure are covered by the signed invocation above. The untouched
  relaunch half of the Dismiss test still emits its existing hittable-element
  watchdog diagnostic while satisfying the old negative assertion; that
  separate persistence contract remains the next slice.
- Diagnostics and Process/Tooling: all four affected Swift files parse
  directly and `git diff --check` passes. The shared condition owner is 385
  lines, Home app selection is 127 lines, permission observation is 631 lines,
  and the Home/Logs workflow remains at 796 lines. One raw `timeout: <number>`
  argument is removed, leaving 428 occurrences across 16 files. The incremental
  build is warning-free; targeted fatal-error, assertion, nested-RunLoop,
  unexpected-failure, and test-failure scanning is empty. The successful test
  log contains 41 known CFPrefs shared-state diagnostics and 16 xcodebuild
  CFURL no-scheme diagnostics. Exact post-run `pgrep -x` readback finds no
  FlowTab, UI-test runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable results, DerivedData, caches,
  and logs under `.build-local/evidence-driven-sync/SYNC-036CO` were removed
  after the commit; absence readback passed.
- Commit: `685af5fd2724e9f0e3b40ae1895dc5b587caf305`
  (`test(sync): migrate SYNC-036CO permission dismiss evidence`).

### SYNC-036CP Closure Record

- Status: completed.
- Design and Oracle: the Dismiss persistence path now installs the shared
  hidden-permission projection owner before launching its second
  `XCUIApplication`. The initial readback records exact `.notRunning` state and
  avoids unavailable element queries. Relaunch plus exact Home navigation form
  one trigger; after that closure returns, an immediate readback accepts only a
  foreground/background-running App with Home content present and the banner,
  Open Settings, and Dismiss elements all absent. This atomic projection is the
  persistence Oracle.
- Lifecycle and retained time policy: the assertion invocation owns observer
  and deferred-schedule cancellation, while the test owns both App lifecycles.
  The shared named 100ms main-RunLoop readback cadence is activated only after
  the initial and immediate trigger readbacks remain unsatisfied because XCTest
  exposes no Home-projection callback. The named two-second
  `homeHiddenProjectionWatchdog` remains solely a terminal failure upper bound
  and reports scenario, acceptance gate, App state, every exact element,
  generation, source, and final waiter result. Resolution, expiry, explicit
  cancellation, and deinit stop recurring work.
- Validation: the canonical installer rebuilt the fixed-path App and signed it
  with `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; codesign reported valid on disk and satisfying its Designated
  Requirement. The canonical signed UI wrapper ran two shared policy/state
  rules, six shared condition-owner initial/reentrancy/cancellation/scheduled/
  watchdog regressions, two scheduler cancellation/reentrancy regressions, and
  all three affected Home permission workflows. All 13 tests passed with zero
  failures in 20.126 seconds. The granted path passed in 3.480 seconds, the
  complete Dismiss persistence workflow in 7.074 seconds, and reminder
  persistence in 8.198 seconds. `status.json` records `completed`, every
  applicable signing/build/test/log exit code zero, and a present result
  bundle.
- Validation scope: Unit and Behavior are not relevant because this slice
  changes one UI-test synchronization call path and preserves production
  behavior. UI and Pressure are covered by the signed invocation above. The
  prior Dismiss workflow's expected negative-wait watchdog diagnostic is absent
  from the successful log.
- Diagnostics and Process/Tooling: the affected Swift file parses directly and
  `git diff --check` passes. The Home/Logs workflow is exactly 800 lines and
  retains one UI-workflow responsibility. One raw `timeout: <number>` argument
  is removed, leaving 427 occurrences across 16 files. The fresh UI build
  retains 80 unrelated existing warning lines, and no warning names the
  affected source. Targeted fatal-error, assertion, nested-RunLoop,
  unexpected-failure, watchdog-expiry, and test-failure scanning is empty. The
  successful test log contains 29 known CFPrefs shared-state diagnostics and 48
  xcodebuild CFURL no-scheme diagnostics. Exact post-run `pgrep -x` readback
  finds no FlowTab, UI-test runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable results, DerivedData,
  caches, and logs under `.build-local/evidence-driven-sync/SYNC-036CP` were
  removed after the commit; absence readback passed.
- Commit: `c9d94a130bb645203bcf8c837761a5408e4234ca`
  (`test(sync): migrate SYNC-036CP dismiss persistence evidence`).

### SYNC-036CQ Closure Record

- Status: completed.
- Design and Oracle: the shared Logs projection snapshot can now carry the
  exact selected level in addition to tab/container existence and the complete
  seeded-row identifier multiset. The dynamic filtering helper first resolves
  one atomic DEBUG baseline containing all four seeded rows. It then installs a
  new target generation before dropdown selection and accepts only one WARN
  snapshot containing exactly warning and error while prohibiting debug and
  info. A matching row set with a stale level, partial rows, prohibited rows,
  duplicate identifiers, or the wrong total remains unsatisfied.
- Lifecycle and retained time policy: baseline readiness and the target
  transition use the same Logs projection owner and named eight-second
  terminal watchdog. The target registration and initial readback precede the
  dropdown trigger. Its named 100ms main-RunLoop schedule remains dormant until
  the trigger returns and the immediate readback remains unsatisfied, avoiding
  XCUI reentrancy during option selection. XCTest exposes no accessibility
  projection callback, so this cancellable cadence remains a conditional
  observation opportunity. Resolution, watchdog expiry, explicit cancellation,
  and deinit stop each generation; the assertion invocation owns both phases.
- Deterministic rules and Pressure: the owner tests now require exact selected
  level together with exact row multiplicities and reject a matching row set
  paired with stale `DEBUG`. They retain partial, prohibited, duplicate,
  initial, scheduled, cancellation, watchdog-diagnostic, and 100-lifecycle
  coverage. Generic condition-owner reentrancy/stale-generation Pressure,
  scheduler cancellation, and all six deferred-readback initial/trigger/
  scheduled/cancellation/watchdog/monotonic-budget regressions also passed.
- Validation: the canonical installer rebuilt and signed the fixed-path App
  with `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; codesign reported valid on disk and satisfying its Designated
  Requirement. The canonical signed UI wrapper ran 19 selected tests with zero
  failures in 44.166 seconds. The four launch-level Logs projection workflow
  passed in 14.387 seconds, the full dynamic-filter/clear/relaunch workflow in
  27.443 seconds, and the three direct Logs owner regressions in 0.185, 0.087,
  and 0.092 seconds. `status.json` records `completed`, every applicable
  signing/build/test/log exit code zero, and a present result bundle.
- Validation scope: Unit and Behavior are not relevant because production Logs
  state rules and behavior are unchanged; this slice strengthens UI-test
  synchronization and its Oracle. UI and Pressure are covered by the signed
  invocation above.
- Diagnostics and Process/Tooling: all three affected Swift files parse
  directly and `git diff --check` passes. Logs projection, its deterministic
  tests, and Home/Logs are 422, 256, and 796 lines with one responsibility per
  file. Four raw `timeout: <number>` arguments are removed, leaving 423
  occurrences across 16 files. The fresh UI build retains 80 unrelated existing
  warning lines, and no warning names an affected source. Targeted fatal-error,
  assertion, nested-RunLoop, unexpected-failure, watchdog-expiry, and
  test-failure scanning is empty. The successful test log contains 40 known
  CFPrefs shared-state diagnostics and 48 xcodebuild CFURL no-scheme
  diagnostics. Exact post-run `pgrep -x` readback finds no FlowTab, UI-test
  runner, xcodebuild, or Space fixture process. Startup `prompts.zip` and the
  three pre-existing local Skill/reference edits remain unchanged and outside
  this slice. Rebuildable results, DerivedData, caches, and logs under
  `.build-local/evidence-driven-sync/SYNC-036CQ` were removed after the commit;
  absence readback passed.
- Commit: `aa0962733fb4050d1db75c47cc24608b45ff00f5`
  (`test(sync): migrate SYNC-036CQ log level projection evidence`).

### SYNC-036CR Closure Record

- Status: completed. The diagnostic-session toggle round trip now resolves
  from an atomic accessibility projection in both directions; parent
  SYNC-036 remains open for its remaining owner-specific slices.
- Implementation evidence: the diagnostic-session owner reads App foreground
  state, Logs content, exact toggle existence/value, exact status existence,
  and active status text as one snapshot. Each direction resolves its initial
  projection, installs the target generation before the toggle action, defers
  the named 100ms cancellable schedule until that action returns, performs an
  immediate trigger readback, and accepts only the exact target projection. A
  named five-second watchdog is a failure upper bound with last evidence. Four
  deterministic regressions cover the active/inactive truth table, incomplete
  projection, trigger gate, initial and scheduled resolution, explicit
  cancellation, repeated post-resolution callbacks, final watchdog readback,
  and 100 owner lifecycles. The production 15-minute session expiration remains
  a product contract owned by `RuntimeDiagnosticSessionDeadlineCoordinator`
  and its injected clock/scheduler tests.
- Build and signing: the canonical installer completed successfully with build
  root `.build-local/evidence-driven-sync/SYNC-036CR/ui-build`. The fixed-path
  App was signed by `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, has
  TeamIdentifier `96PUA726W9`, is valid on disk, and satisfies its Designated
  Requirement.
- Unit and Behavior contract validation: the canonical
  `run-flowtabtests-local.sh` wrapper executed the two diagnostic-session store
  rules and all six deadline policy/coordinator tests. All eight passed with
  zero failures in 0.010 seconds. This includes exact minute transitions,
  initial expiration readback, an early wake that cannot prove expiration,
  superseded-wake cancellation, expiration after slow scheduling, 100 rapid
  restarts retaining one owned wake, explicit start/expiration, and
  nonmutating readback. `status.json` records `completed` and every applicable
  exit code zero. The build retained 80 unrelated existing warning lines; none
  names the validated diagnostic-session sources, and targeted fatal-error,
  failed-test, unexpected-failure, watchdog-expiry, and assertion scanning is
  empty. Exact post-run process readback found no FlowTab or xcodebuild process.
- Attempt 001: the canonical UI wrapper stopped in build-for-testing because
  two escaping test readback closures needed explicit `self`; no tests ran.
  The closures were corrected, direct Swift parsing and `git diff --check`
  passed, and the next canonical build completed.
- Attempt 002: the canonical wrapper built and signed the Runner, then executed
  19 selected tests. All four new diagnostic-session rules/Pressure tests, the
  seven shared condition-owner tests, one scheduler lifecycle test, and six
  deferred-readback Home-selection tests passed. The affected Logs workflow
  alone failed before its first UI assertion because `XCUIApplication.launch()`
  could not activate the target from `Running Background`; result: 18 passed,
  one failed, zero unexpected failures in 64.505 seconds.
- Attempts 003 and 004: an exact `test-without-building` rerun of only the
  affected Logs workflow reproduced the same launch-boundary failure after
  60 seconds. During attempt 004, a desktop capture contained no authentication
  dialog, while unified AppKit evidence showed the signed target publishing and
  ordering its window. `CGSessionCopyCurrentDictionary` reported
  `CGSSessionScreenIsLocked=1`, and `NSWorkspace` reported PID 369,
  `com.apple.loginwindow`, as frontmost. This proves the terminal wait was
  blocked by the locked graphical session rather than the migrated Oracle or a
  pending XCTest permission prompt. Exact post-attempt process readback found
  no FlowTab, Runner, fixture, or xcodebuild process. The temporary desktop
  capture and separately launched diagnostic target were removed.
- Attempt 005: after the console session became interactive, the canonical
  signed UI wrapper reused the unchanged fixed-path build and ran the complete
  19-test target set with zero failures and zero unexpected failures in 27.119
  seconds. The affected full Logs workflow passed in 24.562 seconds. The set
  includes all four diagnostic-session truth-table/lifecycle/Pressure rules,
  seven shared condition-owner rules, one scheduler lifecycle rule, and all
  six deferred-readback Home-selection rules. `status.json` records
  `stage=completed`, signing and test/log exit codes zero, and a present result
  bundle. The Runner was signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and codesign reported it
  valid on disk and satisfying its Designated Requirement.
- Exact locked-session reproducer after the successful build:

  ```sh
  ./scripts/testing/run-ui-tests-local.sh test-without-building \
    --build-root ./.build-local/evidence-driven-sync/SYNC-036CR/ui-build \
    --output-root ./.build-local/evidence-driven-sync/SYNC-036CR/attempt-004 \
    --skip-space-fixtures \
    -only-testing:FlowTabUITests/FlowTabUITests/testLogsPageShowsSeededLogsAndClearRemovesOutput
  ```

- Validation scope: Unit and Behavior retain the eight canonical
  diagnostic-session store/deadline rules above. UI and Pressure are covered
  by attempt 005, including the affected visible workflow and 100 observation
  lifecycles. Slow scheduling is allowed to delay readback; only the exact
  projection can resolve either transition.
- Diagnostics and Process/Tooling: all three affected Swift files parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The observation owner, its deterministic tests, and Home/Logs are
  258, 291, and 800 lines with one responsibility per file. Three raw
  `timeout: <number>` arguments are removed, leaving 420 occurrences across 16
  UI-test files. The successful test-without-building log contains no warning,
  fatal-error, assertion, unexpected-failure, watchdog-expiry, or test-failure
  line; it retains 39 known CFPrefs shared-state diagnostics and 16 xcodebuild
  CFURL no-scheme diagnostics. Exact post-run `pgrep -x` readback finds no
  FlowTab, UI-test Runner, xcodebuild, or Space fixture process. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. Rebuildable results, DerivedData, caches,
  and logs under `.build-local/evidence-driven-sync/SYNC-036CR` were removed
  after evidence capture; absence readback passed.
- Commit: `99dc15500ef02ca7c8377fba55e2b7dbdd1379c8`
  (`test(sync): migrate SYNC-036CR diagnostic session evidence`).

### SYNC-036CS Closure Record

- Status: completed. Current-process Logs clearing now resolves from one exact
  post-trigger accessibility projection, while the relaunch-persistence
  contract remains isolated for its next owner-specific slice.
- Design and Oracle: before the exact Clear action, the owner resolves one
  foreground WARN baseline with Logs content, an existing and hittable Clear
  control, the lines container, no empty hint, and exactly the warn/error
  seeded-row multiset. It installs a fresh target generation before tapping
  that already-proven control. Success requires the same exact WARN level,
  Clear restored to hittable, lines container absent, empty hint present, and
  no seeded rows in one snapshot. The workflow then directly reads the log
  files and confirms every captured fingerprint absent; elapsed time cannot
  satisfy either UI or disk evidence.
- Lifecycle and retained time policy: the target registration and initial
  readback precede the tap. Scheduled XCUI readbacks remain deferred until the
  tap returns, followed by an immediate trigger readback. The shared named
  100ms main-RunLoop cadence activates only while the exact projection remains
  unsatisfied because XCTest exposes no SwiftUI accessibility-projection
  callback. The assertion invocation owns cancellation; resolution, watchdog
  expiry, explicit cancellation, and deinit stop recurring work. The named
  five-second watchdog remains solely a terminal failure bound and reports the
  target, trigger gate, App/control/container/hint/level/row state, generation,
  source, and final waiter evidence.
- Production and Behavior validation: `RuntimeLogFileStore.clearAndWait()`
  already serializes file deletion before publishing its monotonic `.cleared`
  generation, and `RuntimeLogLinesViewModel` applies that evidence before
  publishing empty lines. The canonical `run-flowtabtests-local.sh` wrapper ran
  the ordered append/flush/clear and cancellation rule, the complete initial/
  append/clear/post-clear ViewModel rule, and stale/duplicate/cancelled
  generation rejection. All three passed with zero failures in 0.655 seconds;
  `status.json` records `completed`, all exit codes zero, and a present result
  bundle.
- UI and Pressure validation: the canonical installer built the fixed-path App
  and signed it with `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`,
  TeamIdentifier `96PUA726W9`; codesign reported valid on disk and satisfying
  its Designated Requirement. The canonical signed UI wrapper ran 18 selected
  tests with zero failures in 27.407 seconds. The affected full Logs workflow,
  including direct disk fingerprint readback and relaunch readback, passed in
  24.894 seconds. The set also covers all four new atomic-state/trigger/
  lifecycle/watchdog rules, 100 clear-owner lifecycles, five shared condition-
  owner rules, two scheduler lifecycle rules, and all six deferred-readback
  initial/trigger/scheduled/cancellation/watchdog/monotonic-budget rules.
  `status.json` records `completed`, every signing/build/test/log exit code
  zero, and a present result bundle.
- Diagnostics and Process/Tooling: both new Swift files plus Home/Logs parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The owner, deterministic tests, and Home/Logs are 334, 326, and 800
  lines with one responsibility per file. Two raw `timeout: <number>` arguments
  are removed, leaving 418 occurrences across 16 UI-test files. The app-test
  and UI builds retain 80 and 63 unrelated existing warning lines; none names
  an affected source. Targeted fatal-error, assertion, nested-RunLoop,
  unexpected-failure, watchdog-expiry, and test-failure scanning is empty. The
  successful UI log retains 37 known CFPrefs shared-state diagnostics and 48
  xcodebuild CFURL no-scheme diagnostics. Exact post-run `pgrep -x` readback
  finds no FlowTab, UI-test Runner, xcodebuild, or Space fixture process.
  Startup `prompts.zip` and the three pre-existing local Skill/reference edits
  remain unchanged and outside this slice. Rebuildable results, DerivedData,
  caches, and logs under `.build-local/evidence-driven-sync/SYNC-036CS` were
  removed after evidence capture; absence readback passed.
- Commit: `2398f7b37a233bb7b5c860f86783dc586ec00e45`
  (`test(sync): migrate SYNC-036CS logs clear projection evidence`).

### SYNC-036CT Closure Record

- Status: completed. The persisted Logs-clear assertion now resolves from exact
  source-process termination, a prelaunch observation generation, one loaded
  relaunched accessibility projection, and the existing direct disk readback;
  parent SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: the source App first reaches exact `.notRunning` evidence.
  Before launching its successor, the relaunch owner records an unavailable
  initial snapshot without issuing XCUI queries. After launch and exact Logs
  navigation return, success requires one foreground snapshot containing Logs
  content, the exact Clear control present and hittable, exact `DEBUG`, exactly
  one loaded content branch, and no seeded-row identities. Either a lines
  container or the empty hint can supply the loaded branch because the new
  process may publish unrelated logs. The workflow then directly reads the log
  files and confirms every captured seeded fingerprint remains absent.
- Lifecycle and retained time policy: the target generation exists before
  launch. Its named 100ms main-RunLoop schedule remains dormant until the
  trigger returns, and an immediate trigger readback precedes scheduled
  readbacks. XCTest exposes no SwiftUI accessibility-projection callback, so
  the cancellable cadence remains a conditional observation opportunity. The
  named eight-second relaunch watchdog is solely a terminal failure bound and
  reports target, trigger gate, App/control/content/level/row state,
  generation, source, and final waiter evidence. Resolution, watchdog expiry,
  explicit cancellation, and deinit stop recurring work; the assertion owns
  registration and schedule cancellation, while the test owns both App
  lifecycles.
- Production and Behavior validation: production Logs persistence remains
  governed by `RuntimeLogFileStore.clearAndWait()` and monotonic
  `RuntimeLogLinesViewModel` evidence. The canonical FlowTabTests wrapper ran
  the store append/flush/clear/cancellation rule, the ViewModel initial/append/
  clear rule, and stale/duplicate/cancelled generation rejection. All three
  passed with zero failures in 0.630 seconds. `status.json` records
  `stage=completed`, every exit code zero, and a present result bundle.
- UI and Pressure validation: the canonical installer rebuilt the fixed-path
  App with `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; codesign reported valid on disk and satisfying its Designated
  Requirement. Direct signed XCTest authorization succeeded. The canonical UI
  wrapper ran 27 selected tests with zero failures and zero unexpected
  failures in 23.530 seconds; xcodebuild completed the test operation in
  24.830 seconds. The affected full clear/termination/relaunch/disk-readback
  workflow passed in 20.108 seconds. The set covers the six current-process and
  relaunch projection truth-table/trigger/lifecycle/watchdog rules, shared
  condition-owner and scheduler cancellation/Pressure, all six deferred Home
  selection rules, and seven exact application-termination rules.
- Diagnostics and Process/Tooling: the three affected Swift files parse
  directly and scoped `git diff --check` passes. The owner, deterministic
  tests, and Home/Logs are 480, 489, and 799 lines with one responsibility per
  file. One raw `timeout: <number>` argument is removed, leaving 417
  occurrences across 16 UI-test files. The app-test and UI builds retain 80
  and 63 unrelated existing warning lines; none names an affected source.
  Targeted fatal-error, assertion, nested-RunLoop, unexpected-failure,
  watchdog-expiry, and test-failure scanning is empty. The successful UI log
  retains 55 known CFPrefs shared-state diagnostics and 48 xcodebuild CFURL
  no-scheme diagnostics. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test Runner, xcodebuild, or Space fixture process. Startup `prompts.zip`
  and the three pre-existing local Skill/reference edits remain unchanged and
  outside this slice. The 936 MB of rebuildable results, DerivedData, caches,
  and logs under `.build-local/evidence-driven-sync/SYNC-036CT` were removed
  after evidence capture; absence readback passed.
- Commit: `0f17da1afd0787d673efe90b228d1c4d58aa2075`
  (`test(sync): migrate SYNC-036CT logs clear relaunch evidence`).

### SYNC-036CU Closure Record

- Status: completed. The initial seeded Logs assertion now resolves from one
  post-trigger accessibility projection and returns the fingerprints captured
  by that same projection; parent SYNC-036 remains open for its remaining
  owner-specific slices.
- Design and Oracle: before the exact Logs-tab action, the assertion resolves
  all queried elements, installs one generation owner, and records an initial
  readback. An initially matching projection remains gated until the action
  returns. Success requires one foreground snapshot containing Logs content,
  the privacy notice, exact `DEBUG`, lines present, the empty hint absent, and
  exactly one row for each of the four seeded identifiers. Every resolved row
  must exclude its seeded cleartext marker, contain `message.type=structured`,
  and expose a nonempty top-level `message.fingerprint=` value. Fingerprints
  are returned in expected-identity order even when XCUI reports rows in a
  different order. The direct runtime-log file readback independently proves
  that every projected fingerprint was persisted.
- Lifecycle and retained time policy: after the trigger returns, the owner
  requests an immediate readback and activates its schedule only while the
  target remains unresolved. XCTest exposes no SwiftUI accessibility-
  projection callback, so the shared named cancellable 100ms main-RunLoop
  cadence remains a conditional observation opportunity. The named five-
  second tab-navigation and eight-second exact-projection watchdogs are solely
  terminal failure bounds; failure reports the target, trigger gate, App,
  content/privacy/lines/empty/level/row evidence, generation, source, and final
  waiter observation. Resolution, watchdog expiry, explicit cancellation, and
  deinit stop recurring work; the assertion invocation owns both registration
  and schedule cancellation.
- Unit and Behavior validation: the deterministic UI-target rules cover the
  complete atomic state matrix, exact row multiplicity, redaction/metadata/
  fingerprint parsing, accessibility-row reordering, initial and trigger
  evidence, delayed scheduled resolution, cancellation, duplicate callback
  rejection, and watchdog final-readback diagnostics. The canonical
  FlowTabTests wrapper ran four independent in-process Oracles: launch-time
  seeded-log publication, sensitive-value redaction metadata and fingerprints,
  monotonic log-store changes with cancellation, and ViewModel initial/append/
  clear evidence. All four passed with zero failures in 0.916 seconds;
  xcodebuild completed the test operation in 3.714 seconds, `status.json`
  records `completed`, every exit code is zero, and the result bundle is
  present.
- UI and Pressure validation: the canonical installer rebuilt the fixed-path
  App with `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; codesign reported valid on disk and satisfying its Designated
  Requirement. Direct signed XCTest authorization succeeded. The authoritative
  current-source UI attempt ran 20 selected tests with zero failures and zero
  unexpected failures in 15.601 seconds; xcodebuild completed in 16.292
  seconds. The affected full seeded/filtered/diagnostic/clear/relaunch/disk
  workflow passed in 12.920 seconds. The selected set covers all three seeded-
  projection rules including 100 alternating lifecycle iterations, three
  existing Logs projection rules, seven shared condition/scheduler rules, and
  six deferred Home-selection rules. A prior 20-test attempt also passed and
  was superseded after two additional deterministic assertions. Space-fixture
  topology is not applicable to this Logs-only accessibility projection.
- Diagnostics and Process/Tooling: all three affected Swift files parse
  directly and scoped `git diff --check` passes. The owner, deterministic
  tests, and Home/Logs are 799, 630, and 795 lines with one responsibility per
  file. Five raw `timeout: <number>` arguments are removed, leaving 412
  occurrences across 16 UI-test files. The app-test and fresh UI builds retain
  80 and 63 unrelated existing warning lines; the authoritative incremental UI
  build emits zero warning lines and no warning names an affected source.
  Targeted fatal-error, assertion, nested-RunLoop, unexpected-failure,
  watchdog-expiry, and test-failure scanning is empty. The successful current
  UI log retains 41 known CFPrefs shared-state diagnostics and 16 xcodebuild
  CFURL no-scheme diagnostics. Exact post-run `pgrep -x` readback finds no
  FlowTab, UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 938 MB slice evidence root contained
  only rebuildable results, DerivedData, caches, and logs; it has no downstream
  dependency after this durable record. The exact root was removed after
  evidence capture; absence readback passed, and the post-cleanup exact process
  readback remained empty.
- Commit: `41d4f22db1054e0ae42073cd80b0f2a38e0f21f7`
  (`test(sync): migrate SYNC-036CU seeded logs readiness evidence`).

### SYNC-036CV Closure Record

- Status: completed. Runtime-log command delivery while Logs remains visible
  now resolves from a pre-trigger empty baseline and one exact post-trigger
  accessibility projection; parent SYNC-036 remains open for its remaining
  owner-specific slices.
- Design and Oracle: a baseline owner first requires one foreground projection
  containing Logs content, an existing hittable Clear control, exact `INFO`,
  lines absent, the empty hint present, and zero static-text rows containing
  `runtime log observation probe`. A separate target generation is installed
  before the cross-process distributed command. Its initial readback must still
  satisfy that baseline. After the command returns, success requires the same
  foreground content/control/level evidence, lines present, the empty hint
  absent, and exactly one matching marker row whose projected content contains
  the marker. Runtime-log store generation and ViewModel publication provide
  the in-process Oracle; the independently queried visible row provides the UI
  Oracle.
- Lifecycle and retained time policy: the owner requests an immediate
  post-trigger readback and activates its recurring schedule only while the
  target remains unresolved. XCTest cannot subscribe to the tested App's
  internal log-store generation, so the shared named cancellable 100ms main-
  RunLoop cadence remains a conditional accessibility observation opportunity.
  The named five-second projection watchdog is solely a terminal failure bound
  and reports the target, trigger gate, App/content/control/level/branch state,
  matching-row count and content lengths, generation, source, and final waiter
  evidence. Resolution, watchdog expiry, explicit cancellation, and deinit stop
  recurring work; the assertion invocation owns registration and schedule
  cancellation.
- Unit and Behavior validation: the three deterministic UI-target rules cover
  baseline and delivered truth tables, inconsistent content branches,
  background/missing/disabled/wrong-level states, zero and duplicate markers,
  initially matching gated state, immediate and delayed evidence, early and
  repeated callbacks, cancellation, watchdog final readback, and 100
  alternating owner lifecycles. The canonical FlowTabTests wrapper ran the
  named event-delivery watchdog rule, monotonic store changes with cancellation,
  ViewModel initial/append/clear/post-clear publication, and stale/duplicate/
  cancelled generation rejection. All four passed with zero failures in 0.757
  seconds; xcodebuild completed the test operation in 2.476 seconds.
  `status.json` records `completed`, every exit code is zero, and the result
  bundle is present.
- UI and Pressure validation: the canonical installer rebuilt the fixed-path
  App with `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; codesign reported valid on disk and satisfying its Designated
  Requirement. Direct signed XCTest authorization succeeded. The canonical UI
  wrapper ran 18 selected tests with zero failures and zero unexpected failures
  in 12.357 seconds; xcodebuild completed in 13.719 seconds. The complete
  seeded-clear-command-live-update workflow passed in 9.360 seconds, and the
  new 100-lifecycle owner Pressure passed in 0.187 seconds. The selected set
  also covers the shared condition owner's initial/reentrant/cancelled/
  scheduled/watchdog rules, scheduler rearming/cancellation, and all six
  deferred-generation initial/trigger/scheduled/cancellation/watchdog/
  monotonic-budget rules. Space-fixture topology is not applicable to this
  Logs-only projection.
- Diagnostics and Process/Tooling: both new Swift files plus Home/Logs parse
  directly, the project file passes `plutil -lint`, and scoped
  `git diff --check` passes. The owner, deterministic tests, and Home/Logs are
  369, 398, and 796 lines with one responsibility per file. One raw
  `timeout: <number>` argument is removed, leaving 411 occurrences across 16
  UI-test files. The app-test and UI build-for-testing logs each retain the
  same 80 unrelated existing warning lines; the UI execution phase emits zero
  warning lines and no warning names an affected source. Targeted fatal-error,
  assertion, nested-RunLoop, unexpected-failure, watchdog-expiry, and
  test-failure scanning is empty. The successful UI log retains 37 known
  CFPrefs shared-state diagnostics and 48 xcodebuild CFURL no-scheme
  diagnostics. Exact post-run `pgrep -x` readback finds no FlowTab, UI-test
  Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 1.3 GB slice evidence root contained
  only rebuildable results, DerivedData, caches, fixture products, and logs; it
  has no downstream dependency after this durable record. The exact root was
  removed after evidence capture; absence readback passed, and the post-cleanup
  exact process readback remained empty.
- Commit: `95ae0feab605f9b0835a831d61038ad8193cb731`
  (`test(sync): migrate SYNC-036CV runtime log delivery evidence`).

### SYNC-036CW Closure Record

- Status: completed. The live-update workflow now proves current-process Clear
  completion from one exact pre-trigger baseline and one post-trigger empty
  projection before it sends the runtime-log command; parent SYNC-036 remains
  open for its remaining owner-specific slices.
- Design and Oracle: the live-update path now reuses the Logs clear owner. Its
  foreground baseline requires Logs content, an existing hittable Clear
  control, exact `INFO`, lines present, the empty hint absent, and exactly one
  seeded INFO identity. A target generation is installed before the Clear tap;
  the recurring XCUI readback is deferred until the tap returns and receives
  an immediate trigger readback. Success requires one atomic foreground
  projection with the same content/control/level evidence, Clear re-enabled,
  lines absent, the empty hint present, and zero seeded identities. Runtime
  `clearAndWait()`, store generation, and ViewModel publication remain the
  independent in-process Oracle.
- Lifecycle and retained time policy: XCTest exposes no SwiftUI accessibility-
  projection callback, so the existing named cancellable 100ms main-RunLoop
  cadence remains a conditional readback opportunity. It starts only if the
  immediate post-trigger readback remains unresolved. The named five-second
  watchdog is solely the terminal failure bound and reports target, trigger
  gate, App/content/control/level/branch/row state, generation, source, and
  final waiter evidence. Resolution, watchdog expiry, cancellation, and deinit
  stop recurring work; the assertion invocation owns registration and schedule
  cancellation.
- Unit and Behavior validation: the new single-INFO truth table proves the
  exact level and row multiset and rejects missing, duplicate, extra, wrong-
  level, and already-cleared projections. Existing clear-owner rules cover
  atomic branch consistency, initial and post-trigger evidence, delayed
  scheduling, cancellation, duplicate callbacks, watchdog final readback, and
  100 alternating lifecycles. The canonical FlowTabTests wrapper ran
  `testRuntimeLogClearAndWaitDeletesFilesAndRestartReadsNoEntries`,
  `testRuntimeLogStorePublishesMonotonicChangesAndCancelsObservation`, and
  `testRuntimeLogViewModelAppliesInitialAppendAndClearEvidence`. All three
  passed with zero failures in 0.348 seconds; xcodebuild completed the test
  operation in 1.990 seconds. Its `status.json` recorded `completed`, every exit
  code zero, and a present result bundle.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app` with the local Apple Development
  identity. Host-context codesign reports the fixed App valid and its
  Designated Requirement identifies
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; both UI runs directly used the signed XCTest authorization.
  The targeted visible-path attempt passed one test with zero failures in
  6.730 seconds; xcodebuild completed in 8.503 seconds. The authoritative
  consolidated attempt passed all 14 selected tests with zero failures and
  zero unexpected failures in 8.494 seconds; xcodebuild completed in 9.179
  seconds, and the affected visible workflow passed in 6.161 seconds. The
  clear-owner 100-lifecycle Pressure case passed in 0.142 seconds, and the
  shared scheduled-readback lifecycle Pressure case passed in 0.283 seconds.
  The set also covers shared initial, reentrant, cancelled/replaced, named-
  schedule, watchdog, scheduler-rearm, and cancellation rules. Space-fixture
  topology is not applicable to this Logs-only accessibility projection.
- Diagnostics and Process/Tooling: both affected Swift files parse directly,
  and scoped `git diff --check` passes. Home/Logs and the deterministic clear-
  projection tests are 789 and 548 lines with one responsibility per file.
  Three raw `timeout: <number>` arguments are removed, leaving 408 occurrences
  across 16 UI-test files. The fresh UI build retains 80 unrelated existing
  warning lines; the authoritative incremental build and both execution phases
  emit zero lowercase warning lines, and no warning names an affected source.
  Each execution emits one destination-selection warning. Targeted fatal-error,
  assertion, nested-RunLoop, unexpected-failure, watchdog-expiry, and test-
  failure scanning is empty. The targeted and consolidated successful logs
  retain 3 and 29 known CFPrefs diagnostics plus 48 and 16 xcodebuild CFURL no-
  scheme diagnostics. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. The startup
  `prompts.zip` and three pre-existing local Skill/reference edits remain
  unchanged and outside the slice. The 570 MB slice evidence root contained
  only rebuildable DerivedData, result bundles, caches, fixture products, and
  logs; it has no downstream dependency after this durable record. The exact
  root was removed after evidence capture; absence readback passed, and the
  post-cleanup exact process readback remained empty.
- Commit: `878798864b298144c6ce50fbd31e3627e7a3bf14`
  (`test(sync): migrate SYNC-036CW live update clear evidence`).

### SYNC-036CX Closure Record

- Status: completed. The live-update workflow now installs its initial Logs
  projection owner before the exact tab action and advances to Clear only after
  one atomic single-INFO projection resolves; parent SYNC-036 remains open for
  its remaining owner-specific slices.
- Design and Oracle: the helper resolves all XCUI elements and records an
  initial generation readback before asking the shared hittable-element owner
  to locate and tap the Logs tab. An initially matching projection remains
  gated until the trigger returns. A projection that arrives while the trigger
  is in flight is recovered by the immediate trigger readback. Success requires
  one foreground snapshot containing Logs content, an existing hittable Clear
  control, exact `INFO`, lines present, the empty hint absent, and exactly one
  seeded INFO identity. The following Clear owner independently revalidates
  that same populated state before mutation. Launch-time seeded-log publication,
  store generation, and ViewModel publication remain the in-process Oracles.
- Lifecycle and retained time policy: the named five-second tab-hittability
  watchdog and eight-second exact-projection watchdog remain terminal failure
  bounds. XCTest exposes no SwiftUI accessibility-projection callback, so the
  shared named cancellable 100ms main-RunLoop cadence remains a conditional
  observation opportunity and is activated only if the immediate post-trigger
  readback is unresolved. Success, trigger failure, watchdog expiry, explicit
  cancellation, and deinit stop recurring work; the assertion invocation owns
  both the projection registration and deferred schedule.
- Unit and Behavior validation: two new deterministic rules cover an incomplete
  pre-navigation initial readback, matching evidence observed before trigger
  completion, the immediate trigger readback, and an initially matching state.
  Existing truth-table, trigger-gate, cancellation, watchdog, hittable-element,
  scheduler, and 100-lifecycle rules cover the remaining owner state space. The
  canonical FlowTabTests wrapper ran launch-time UI-test seeded-log bootstrap,
  monotonic store-generation publication with cancellation, and ViewModel
  initial/append/clear publication. All three passed with zero failures in
  1.070 seconds; xcodebuild completed the test operation in 2.785 seconds.
  `status.json` records `completed`, every exit code is zero, and the result
  bundle is present.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app`; host-context codesign reports it
  valid with `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and
  TeamIdentifier `96PUA726W9`. The UI wrapper directly used the signed XCTest
  authorization and passed all 19 selected tests with zero failures and zero
  unexpected failures in 8.667 seconds; xcodebuild completed in 9.751 seconds.
  The affected complete navigation/clear/runtime-delivery workflow passed in
  5.478 seconds. The clear-owner 100-lifecycle Pressure case passed in 0.142
  seconds, and the shared scheduled-readback lifecycle Pressure case passed in
  0.294 seconds. The selected set also covers the named watchdog policy,
  incomplete and initially matching projections, pre-trigger evidence,
  hittable initial/delayed/watchdog evidence, shared initial/reentrant/
  cancelled/replaced/scheduled/watchdog rules, and scheduler rearming and
  cancellation. Space-fixture topology is not applicable to this Logs-only
  accessibility projection.
- Diagnostics and Process/Tooling: all three affected Swift files parse
  directly, and scoped `git diff --check` passes. Home/Logs, the projection
  owner, and deterministic tests are 799, 564, and 649 lines with one
  responsibility per file. Two raw `timeout: <number>` arguments are removed,
  leaving 406 occurrences across 16 UI-test files. The app-test and UI build
  logs each retain 80 unrelated existing warning lines; the UI execution phase
  emits zero lowercase warning lines and one destination-selection warning,
  and no warning names an affected source. Targeted fatal-error, assertion,
  nested-RunLoop, unexpected-failure, watchdog-expiry, and test-failure
  scanning is empty. The successful app-test and UI logs retain 1 and 39 known
  CFPrefs diagnostics plus 64 and 48 xcodebuild CFURL no-scheme diagnostics.
  Exact post-run `pgrep -x` readback finds no FlowTab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 1.3 GB slice evidence root contained
  only rebuildable results, DerivedData, caches, fixture products, and logs; it
  has no downstream dependency after this durable record. The exact root was
  removed after evidence capture; absence readback passed, and the post-cleanup
  exact process readback remained empty.
- Commit: `8164b08403e0fe6d7bc535f11a3cfe744ac45be1`
  (`test(sync): migrate SYNC-036CX initial logs evidence`).

### SYNC-036CY Closure Record

- Status: completed. The Logs action-buttons workflow now installs its exact
  projection owner before navigation and resolves only when both actions are
  ready in one foreground accessibility readback; parent SYNC-036 remains open
  for its remaining owner-specific slices.
- Design and Oracle: the helper resolves Logs content, Open Directory, and
  Clear Logs elements and records an initial generation before the exact Logs-
  tab trigger. Matching evidence remains gated until the trigger returns, and
  an immediate trigger readback recovers a projection published during the
  action. Success requires one foreground snapshot containing Logs content and
  both exact buttons present and hittable. `RuntimeLogsSection` hosting the two
  shared compact action controls remains the independent Behavior Oracle.
- Lifecycle and retained time policy: the named five-second tab-hittability
  watchdog and eight-second exact-projection watchdog are terminal failure
  bounds. XCTest exposes no SwiftUI accessibility-projection callback, so the
  shared named cancellable 100ms main-RunLoop cadence remains a conditional
  observation opportunity and activates only when the immediate post-trigger
  readback remains unresolved. Resolution, trigger failure, watchdog expiry,
  explicit cancellation, and deinit stop recurring work; the assertion
  invocation owns the projection registration and deferred schedule. Failure
  reports the target, trigger gate, foreground/content/control state, source,
  generation, and final waiter evidence.
- Unit and Behavior validation: five deterministic UI-target rules cover the
  complete atomic truth table, background and partial projections, either
  disabled action, pre-navigation evidence, initially matching state, trigger
  readback, repeated callbacks, cancellation, watchdog final evidence, and 100
  alternating owner lifecycles. The canonical FlowTabTests wrapper ran the
  compact action-button title/accessibility/width rule and the runtime Logs
  shared-control rule. Both passed with zero failures in 0.014 seconds;
  xcodebuild completed the test operation in 2.218 seconds. `status.json`
  records `completed`, every exit code is zero, and the result bundle is
  present.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app`; host-context codesign reports it
  valid with `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and
  TeamIdentifier `96PUA726W9`. The wrapper directly used the signed XCTest
  authorization and passed all 17 selected tests with zero failures and zero
  unexpected failures in 6.336 seconds; xcodebuild completed in 7.457 seconds.
  The affected navigation/action-readiness path passed in 3.479 seconds, the
  new 100-lifecycle owner Pressure passed in 0.142 seconds, and the shared
  scheduled-readback lifecycle Pressure passed in 0.287 seconds. The selected
  set also covers the shared initial, reentrant, cancelled/replaced, named-
  schedule, watchdog, scheduler-rearm, and cancellation rules. Space-fixture
  topology is not applicable to this Logs-only accessibility projection.
- Diagnostics and Process/Tooling: both new Swift files plus Home/Logs parse
  directly, the project file passes `plutil -lint`, and scoped
  `git diff --check` passes. The owner, deterministic tests, and Home/Logs are
  252, 267, and 798 lines with one responsibility per file. Two raw
  `timeout: <number>` arguments are removed, leaving 404 occurrences across 16
  UI-test files. The app-test and UI build-for-testing logs each retain 80
  unrelated existing warning lines, fixture preparation retains one unrelated
  source warning, and the UI execution phase emits zero lowercase warning
  lines plus one destination-selection warning. No warning names an affected
  source. Targeted fatal-error, assertion, nested-RunLoop, unexpected-failure,
  watchdog-expiry, and test-failure scanning is empty. The successful app-test
  log retains no CFPrefs diagnostic and 64 xcodebuild CFURL no-scheme
  diagnostics; the UI log retains 35 and 48 respectively. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test Runner, xcodebuild, or Space
  fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 1.3 GB slice evidence root contained
  only rebuildable results, DerivedData, caches, fixture products, and logs; it
  has no downstream dependency after this durable record. The exact root was
  removed after evidence capture; absence readback passed, and the post-cleanup
  exact process readback remained empty.
- Commit: `fcdc1965eae8eb6223f1b1511829c33166bb6578`
  (`test(sync): migrate SYNC-036CY logs action readiness`).

### SYNC-036CZ Closure Record

- Status: completed. The sidebar content-switch workflow now installs a fresh
  exact projection owner before each Home, Logs, and Settings action and
  advances only after the requested accessibility content is uniquely visible;
  parent SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: each transition resolves all three content elements and
  records an initial generation before locating and tapping the exact target
  button. Matching evidence remains gated until that trigger returns, and an
  immediate trigger readback recovers a projection published during the tap.
  Success requires the App foreground plus exactly one visible content target:
  the requested Home, Logs, or Settings content, with both non-target contents
  absent. `HomeRootView.tabContainer` accessibility visibility and the exact
  `HomeTabState` selection remain the independent in-process Oracles.
- Lifecycle and retained time policy: the existing named six-second support
  tab-navigation duration is solely the terminal bound for exact button
  hittability and exact target projection. XCTest exposes no SwiftUI
  accessibility-projection callback, so the shared named cancellable 100ms
  main-RunLoop cadence remains a conditional observation opportunity. It stays
  dormant through the trigger and activates only if the immediate trigger
  readback is unresolved. Resolution, trigger failure, watchdog expiry,
  explicit cancellation, and deinit stop recurring work; each transition
  assertion owns its registration and deferred schedule. Failure reports the
  target, trigger gate, App state, all three content states, visible-target set,
  generation, source, and final waiter evidence.
- Unit and Behavior validation: Unit is not relevant because this slice changes
  only the UI-test observation boundary. Six deterministic UI-target rules
  cover all three exact target truth tables, background/empty/duplicate
  projections, pre-trigger and initially matching evidence, delayed scheduled
  evidence, idempotent schedule activation, cancellation, repeated callbacks,
  watchdog final readback, and 100 alternating owner lifecycles. The canonical
  FlowTabTests wrapper ran the HomeTab/AppWindowCoordinator exact selection-
  before-activation rule; it passed with zero failures in 0.376 seconds, and
  xcodebuild completed in 2.631 seconds. `status.json` records `completed`,
  every exit code is zero, and the result bundle is present.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; host-context codesign reports the App valid and satisfying its
  Designated Requirement. The canonical wrapper directly used signed XCTest
  authorization and passed all 18 selected tests with zero failures and zero
  unexpected failures in 8.324 seconds; xcodebuild completed in 9.811 seconds.
  The affected Home-to-Logs-to-Settings workflow passed in 5.247 seconds, the
  new 100-lifecycle owner Pressure passed in 0.196 seconds, and the shared
  scheduled-readback lifecycle Pressure passed in 0.291 seconds. The selected
  set also covers the support watchdog policy, exact hittable-element evidence,
  shared initial/reentrant/cancelled/replaced/scheduled/watchdog rules, and
  scheduler rearming and cancellation. Space-fixture topology is not applicable
  to this single-App sidebar accessibility path.
- Diagnostics and Process/Tooling: both new Swift files plus Home/Logs parse
  directly, the project file passes `plutil -lint`, and scoped
  `git diff --check` passes. The owner, deterministic tests, and Home/Logs are
  285, 330, and 798 lines with one responsibility per file. Six raw
  `timeout: <number>` arguments are removed, leaving 398 occurrences across 16
  UI-test files. The app-test and UI build-for-testing logs each retain 80
  unrelated existing warning lines, fixture preparation retains one unrelated
  source warning, and the UI execution phase emits zero lowercase warning
  lines plus one destination-selection warning. No warning names an affected
  source. Targeted fatal-error, assertion, nested-RunLoop, unexpected-failure,
  watchdog-expiry, and test-failure scanning is empty. The successful app-test
  log retains no CFPrefs diagnostic and 64 xcodebuild CFURL no-scheme
  diagnostics; the UI log retains 37 and 48 respectively. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test Runner, xcodebuild, or Space
  fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 1.3 GB slice evidence root contained
  only rebuildable results, DerivedData, caches, fixture products, and logs; it
  has no downstream dependency after this durable record. The exact root was
  removed after evidence capture; absence readback passed, and the post-cleanup
  exact process readback remained empty.
- Commit: `12e5caaa7c7b0bb070f38452b52efaa41228c4ef`
  (`test(sync): migrate SYNC-036CZ sidebar tab projections`).

### SYNC-036DA Closure Record

- Status: completed. Every shared `openLogsTab(in:)` caller now enters Logs
  through the trigger-preinstalled exact sidebar projection owner; parent
  SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: the helper delegates one Logs target transition to the
  SYNC-036CZ owner. It resolves Home, Logs, and Settings elements and records
  an initial generation before the exact Logs button action. Matching evidence
  remains gated until the action returns, and an immediate trigger readback
  recovers a projection published during the tap. Success requires the App
  foreground with Logs content exposed and Home and Settings content absent.
  `HomeRootView.tabContainer`, `HomeTabState`, the status-item last-tab state,
  and the seeded-log projection remain independent in-process and caller
  Oracles.
- Lifecycle and retained time policy: the existing named six-second support
  tab-navigation duration remains solely the exact trigger/projection terminal
  bound. XCTest exposes no SwiftUI accessibility-projection callback, so the
  shared named cancellable 100ms main-RunLoop cadence remains a conditional
  observation opportunity after an unresolved trigger readback. Resolution,
  trigger failure, watchdog expiry, explicit cancellation, and deinit stop
  recurring work. The helper invocation owns registration and cancellation;
  the status-item, English-layout, and post-clear relaunch tests retain their
  App and domain-resource cleanup.
- Unit and Behavior validation: Unit is not relevant because this slice changes
  only shared UI-test orchestration. The inherited six deterministic sidebar-
  owner rules cover target/background/empty/duplicate projections, initial,
  pre-trigger, immediate and delayed evidence, idempotent activation,
  cancellation, repeated callbacks, watchdog final readback, and 100
  alternating lifecycles. The canonical FlowTabTests wrapper ran exact
  HomeTab selection-before-activation and status-item last-selected-tab
  preservation. Both passed with zero failures in 0.347 seconds; xcodebuild
  completed in 2.562 seconds. `status.json` records `completed`, every exit
  code is zero, and the result bundle is present.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; host-context codesign reports the App valid and satisfying its
  Designated Requirement. The canonical wrapper directly used signed XCTest
  authorization and passed all 20 selected tests with zero failures and zero
  unexpected failures in 37.276 seconds; xcodebuild completed in 38.433
  seconds. The English layout, seeded-log clear/relaunch, and status-item
  restore callers passed in 18.054, 12.015, and 4.265 seconds. The sidebar
  owner 100-lifecycle Pressure passed in 0.176 seconds, delayed scheduled
  evidence passed in 0.149 seconds, and shared scheduled-readback Pressure
  passed in 0.277 seconds. The selected set also covers the support watchdog,
  exact hittable-element, shared initial/reentrant/cancelled/replaced/
  scheduled/watchdog, scheduler-rearm, and cancellation rules. Space-fixture
  topology is not applicable to these single-App sidebar paths.
- Diagnostics and Process/Tooling: Support and the shared sidebar owner parse
  directly, and scoped `git diff --check` passes. Support, the owner, and its
  deterministic tests are 767, 285, and 330 lines with one responsibility per
  file. This slice removes two named sequential waits while retaining the six-
  second terminal policy; the raw `timeout: <number>` inventory remains 398
  occurrences across 16 UI-test files. The app-test and UI build-for-testing
  logs each retain 80 unrelated existing warning lines, fixture preparation
  retains one unrelated source warning, and UI execution emits zero lowercase
  warning lines plus one destination-selection warning. No warning names an
  affected source. Targeted fatal-error, assertion, nested-RunLoop, unexpected-
  failure, watchdog-expiry, and test-failure scanning is empty. The successful
  app-test log retains no CFPrefs diagnostic and 64 xcodebuild CFURL no-scheme
  diagnostics; the UI log retains 43 and 48 respectively. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test Runner, xcodebuild, or Space
  fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 1.3 GB slice evidence root contained
  only rebuildable results, DerivedData, caches, fixture products, and logs; it
  has no downstream dependency after this durable record. The exact root was
  removed after evidence capture; absence readback passed, and the post-cleanup
  exact process readback remained empty.
- Commit: `2cf9bc1fcdd622054305bf1fe9d50f14983edef0`
  (`test(sync): migrate SYNC-036DA shared logs navigation`).

### SYNC-036DB Closure Record

- Status: completed. The English primary-surfaces path no longer starts a
  second five-second Logs-content wait after `openLogsTab(in:)`; parent
  SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: successful return from the shared SYNC-036DA helper is
  the navigation-completion evidence. Its observer is established before the
  exact Logs-tab trigger and accepts only one foreground projection with Logs
  exposed and Home and Settings absent. The caller then immediately requires
  the localized `Start a 15-minute diagnostic session` text as its independent
  English visible-content Oracle. Scheduler or accessibility-publication load
  can extend the shared observation without changing the accepted state.
- Lifecycle and retained time policy: this caller adds no observer, timer,
  retry, cadence, or duration. It inherits the shared helper's named six-second
  terminal watchdog and conditional cancellable 100ms XCUI readback cadence;
  the helper invocation owns resolution and cancellation on every exit.
- Unit, Behavior, and Pressure: not relevant because this slice removes one
  redundant UI-test wait and changes no production state rule, application
  orchestration, accessibility contract, observer, timer, scheduler, repeated
  work, or hot path. The already-validated shared projection owner remains the
  complete deterministic and lifecycle-pressure contract for Logs navigation.
- UI validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app`; host-context verification
  reports identifier `io.github.potato-dumplings.flowtab`,
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`, a valid bundle, and a satisfied Designated Requirement. The
  final canonical signed wrapper invocation at `ui-visible-006` entered
  `testEnglishPrimarySurfacesExposeUsableLayoutAnchors` and passed 1/1 with
  zero failures in 16.677 seconds; xcodebuild completed in 17.342 seconds.
  Its status ledger records `completed`, every applicable exit code is zero,
  and the result bundle is present; xcresult independently records
  `status=succeeded` and `testsCount=1`.
- Authorization recovery evidence: `ui-attempt-001`, `ui-attempt-002`,
  `ui-auth-probe-003`, and `ui-auth-probe-004` stopped before any selected test
  body with `Timed out while enabling automation mode` and exit 65. Narrow
  TCC/XCTest system-log readback shows the user authentication completed at
  2026-08-10 21:09:27 +0800 and Automation Mode was successfully enabled,
  approximately seven seconds after the fourth runner's initialization
  watchdog expired. The immediately following canonical
  `ui-auth-probe-005` entered
  `testHomeActivationWatchdogPolicyCompatibility` and passed 1/1 in 0.419
  seconds; xcodebuild completed in 1.685 seconds. This independently
  classifies the earlier results as authorization initialization evidence.
  A clean reproduction uses `install-ui-test-app.sh --build-root
  ./.build-local/evidence-driven-sync/SYNC-036DB/ui-install`, followed by
  `run-ui-tests-local.sh --build-root
  ./.build-local/evidence-driven-sync/SYNC-036DB/ui-tests --output-root
  <fresh-output-root> -only-testing:FlowTabUITests/FlowTabUITests/testEnglishPrimarySurfacesExposeUsableLayoutAnchors`.
- Diagnostics and Process/Tooling: the affected Swift file parses directly and
  `git diff --check` passes. It is 101 lines. One raw `timeout: <number>`
  argument is removed, leaving 397 occurrences across 16 UI-test files. The
  initial full build retains 80 unrelated existing lowercase warning lines
  plus one destination warning; fixture preparation retains one unrelated
  source warning plus one destination warning. The final UI execution emits
  zero lowercase warning lines, one destination warning, 16 xcodebuild CFURL
  no-scheme diagnostics, and three CFPrefs shared-state diagnostics. No warning
  names the affected source, and targeted fatal-error, sanitizer,
  main-thread-checker, SwiftUI background-publication, XPC, input-method,
  assertion, crash, and test-failure scanning is empty. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test Runner, xcodebuild, or Space
  fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 1.2 GB slice evidence root contains
  only rebuildable results, DerivedData, caches, fixture products, and logs;
  it has no downstream dependency after this durable record. The exact root is
  removed after evidence capture, followed by absence and exact-process
  readback.
- Commit: `4df8552562e322043e9522f3657a0920324aeec2`
  (`test(sync): migrate SYNC-036DB English logs evidence`).

### SYNC-036DC Closure Record

- Status: completed. All 35 `openSettingsTab(in:)` call sites now enter
  Settings through the shared trigger-preinstalled exact sidebar projection
  owner; parent SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: the helper resolves Home, Logs, and Settings elements and
  records an initial generation before the exact Settings button action.
  Matching evidence remains gated until the action returns, and an immediate
  trigger readback recovers a projection published during the tap. Success
  requires the App foreground with Settings content exposed and Home and Logs
  content absent. `HomeRootView.tabContainer`, `HomeTabState`, each caller's
  permission or settings control, persisted preference, and runtime-log
  evidence remain independent production and caller Oracles.
- Lifecycle and retained time policy: the named six-second support tab-
  navigation duration remains solely the exact trigger/projection terminal
  bound. XCTest exposes no SwiftUI accessibility-projection callback, so the
  shared named cancellable 100ms main-RunLoop cadence remains a conditional
  observation opportunity after an unresolved trigger readback. Resolution,
  trigger failure, watchdog expiry, explicit cancellation, and deinit stop
  recurring work. The helper invocation owns registration and cancellation;
  callers retain App, relaunch, permission-state file, runtime-log, and domain-
  resource cleanup.
- Unit and Behavior validation: Unit is not relevant because production APIs
  and state rules are unchanged. The canonical FlowTabTests wrapper ran the
  exact HomeTab selection-before-activation Behavior Oracle; it passed 1/1
  with zero failures in 0.376 seconds, and xcodebuild completed in 2.501
  seconds. Its status ledger records `completed`, both exit codes are zero,
  and the result bundle is present; xcresult independently records
  `status=succeeded` and `testsCount=1`.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; host-context codesign reports the App valid and satisfying its
  Designated Requirement. The canonical signed wrapper passed all 10 selected
  tests with zero failures in 37.665 seconds; xcodebuild completed in 38.663
  seconds. The English double-Settings path passed in 17.053 seconds, the
  launch-at-login two-App relaunch path in 9.450 seconds, the nested permission
  projection path in 5.802 seconds, and the no-explicit-readiness terminal-
  permission path in 4.350 seconds. The six shared-owner rules cover exact
  target/background/empty/duplicate projections, initial and pre-trigger
  evidence, immediate and delayed completion, cancellation, repeated
  callbacks, and final-watchdog diagnostics. Its 100-lifecycle Pressure path
  passed in 0.165 seconds, and delayed scheduled evidence passed in 0.150
  seconds. Space-fixture topology is not applicable to these single-App
  Settings-navigation contracts.
- Diagnostics and Process/Tooling: Support and the shared sidebar owner parse
  directly, and `git diff --check` passes. They are 753 and 285 lines with one
  responsibility per file. The helper replaces two sequential named waits
  with one exact projection contract, so the raw `timeout: <number>` inventory
  remains 397 occurrences across 16 UI-test files. The app-test and UI build-
  for-testing logs each retain 80 unrelated existing lowercase warning lines
  plus one destination warning; fixture preparation retains one unrelated
  source warning plus one destination warning. UI execution emits zero
  lowercase warning lines, one destination warning, 48 xcodebuild CFURL no-
  scheme diagnostics, and 24 CFPrefs shared-state diagnostics. No warning
  names either affected source, and targeted fatal-error, sanitizer, main-
  thread-checker, SwiftUI background-publication, XPC, input-method,
  assertion, crash, unexpected-failure, watchdog-expiry, and test-failure
  scanning is empty. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 1.3 GB slice evidence root contains
  only rebuildable results, DerivedData, caches, fixture products, and logs;
  it has no downstream dependency after this durable record. The exact root is
  removed after evidence capture, followed by absence and exact-process
  readback.
- Commit: `a626180a0fd60f546d18b36d2b4cb930a3358ef6`
  (`test(sync): migrate SYNC-036DC shared settings navigation`).

### SYNC-036DD Closure Record

- Status: completed. The Settings appearance path no longer starts a second
  six-second Settings-content wait after `openSettingsTab(in:)`; parent
  SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: successful return from the shared SYNC-036DC helper is
  the navigation-completion evidence. Its observer is established before the
  exact Settings-tab trigger and accepts only one foreground projection with
  Settings exposed and Home and Logs absent. The caller then requires the
  exact Chinese `外观` heading as its independent visible-content Oracle and
  uses average-luminance readbacks as the independent light/dark transition
  evidence. Scheduler or accessibility-publication load can extend the shared
  observation without changing the accepted state.
- Lifecycle and retained time policy: this caller adds no observer, timer,
  retry, cadence, or duration. It inherits the shared helper's named six-second
  terminal watchdog and conditional cancellable 100ms XCUI readback cadence;
  the helper invocation owns resolution and cancellation on every exit. The
  existing exact-heading and luminance observation bounds remain terminal
  failure bounds for their independent visible-state Oracles and stay within
  the remaining parent SYNC-036 policy-naming inventory.
- Unit, Behavior, and Pressure: not relevant because this slice removes one
  redundant UI-test wait and changes no production state rule, application
  orchestration, accessibility contract, observer, timer, scheduler, repeated
  work, or hot path. The already-validated shared projection owner remains the
  complete deterministic and lifecycle-pressure contract for Settings
  navigation.
- UI validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; host-context codesign reports the App valid and satisfying its
  Designated Requirement. The canonical signed wrapper entered
  `testSettingsAppearanceThemeAndLanguageUpdateVisibleUI` and passed 1/1 with
  zero failures and zero unexpected failures in 17.965 seconds; xcodebuild
  completed in 19.468 seconds. The path exercised the exact Settings
  projection, Chinese Appearance heading, both average-luminance transitions,
  and the English-language update. Its status ledger records `completed`,
  every applicable exit code is zero, and the result bundle is present;
  xcresult independently records `status=succeeded` and `testsCount=1`.
- Diagnostics and Process/Tooling: the affected Swift file parses directly
  and `git diff --check` passes. It is 785 lines and remains within the
  existing single-responsibility guardrail. One raw `timeout: <number>`
  argument is removed, leaving 396 occurrences across 16 UI-test files. The
  build-for-testing log retains 80 unrelated existing lowercase warning lines
  plus one destination warning; fixture preparation retains one unrelated
  source warning plus one destination warning. UI execution emits zero
  lowercase warning lines, one destination warning, 48 xcodebuild CFURL no-
  scheme diagnostics, and three CFPrefs shared-state diagnostics. No warning
  names the affected source, and targeted fatal-error, sanitizer,
  main-thread-checker, SwiftUI background-publication, XPC, input-method,
  assertion, crash, unexpected-failure, watchdog-expiry, and test-failure
  scanning is empty. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 813 MB slice evidence root contains
  only rebuildable results, DerivedData, caches, fixture products, and logs;
  it has no downstream dependency after this durable record. The exact root is
  removed after evidence capture, followed by absence and exact-process
  readback.
- Commit: `cbc3191646cd89ec1c594de4fcc67c31590df3fc`
  (`test(sync): migrate SYNC-036DD Settings content evidence`).

### SYNC-036DE Closure Record

- Status: completed. The TCC-convergence, permission-action, and localized-
  status paths now share one initial denied Settings permission projection;
  parent SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: the helper resolves both exact Simplified Chinese denied
  statuses and both exact permission actions before navigation, starts one
  generation-owned observation, and records its initial readback. It then
  performs exact Settings navigation and requests an immediate trigger
  readback. Success requires all four fields in one atomic snapshot. The later
  granted-state projection and persisted permission logs in the convergence
  path remain independent transition and readback Oracles.
- Lifecycle and retained time policy: Accessibility exposes no Settings
  projection callback, so the shared named cancellable 100ms main-RunLoop
  cadence remains a conditional observation opportunity. The named five-
  second initial-projection watchdog remains solely the failure upper bound;
  its diagnostics report generation, evidence source, waiter result, and all
  four final fields. The helper invocation owns observation cancellation on
  resolution, expiry, assertion return, and teardown. Callers retain App and
  exact temporary permission-state resource ownership.
- Unit and Behavior: not relevant because this slice changes only UI-test
  synchronization and deterministic UI-target rules; production APIs, state
  transitions, and application orchestration are unchanged.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; host-context codesign reports the App valid and satisfying its
  Designated Requirement. The canonical signed wrapper passed all 13 selected
  tests with zero failures and zero unexpected failures in 16.332 seconds;
  xcodebuild completed in 17.998 seconds. The action-only, TCC-convergence, and
  localized-status visible paths passed in 4.892, 5.881, and 3.616 seconds.
  The policy and all sixteen atomic predicate combinations passed in 0.214
  and 0.163 seconds. Initial/preinstalled evidence, reentrant completion,
  named scheduled readback, final-watchdog diagnostics, scheduler rearm and
  cancellation all passed. The 500-generation cancelled/replaced Pressure and
  100-lifecycle scheduled-readback Pressure paths passed in 0.191 and 0.286
  seconds; delayed scheduling changed observation latency while the exact
  four-field readback remained the success condition. Space-fixture topology
  is not applicable to these single-App Settings paths.
- Result evidence: the status ledger records `completed`, every applicable
  exit code is zero, and the result bundle is present. xcresult independently
  records `status=succeeded` and `testsCount=13`.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The permission owner and Settings sources are
  651 and 779 lines, retaining one responsibility per file within their
  existing guardrails. Four raw `timeout: <number>` arguments are removed,
  leaving 392 occurrences across 16 UI-test files. The build-for-testing log
  retains 80 unrelated existing lowercase warning lines plus one destination
  warning; fixture preparation retains one unrelated source warning plus one
  destination warning. UI execution emits zero lowercase warning lines, one
  destination warning, 48 xcodebuild CFURL no-scheme diagnostics, and 29
  CFPrefs shared-state diagnostics. No warning names an affected source, and
  targeted fatal-error, sanitizer, main-thread-checker, SwiftUI background-
  publication, XPC, input-method, assertion, crash, unexpected-failure,
  watchdog-expiry, and test-failure scanning is empty. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test Runner, xcodebuild, or Space
  fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 811 MB slice evidence root contains
  only rebuildable results, DerivedData, caches, fixture products, and logs;
  it has no downstream dependency after this durable record. The exact root is
  removed after evidence capture, followed by absence and exact-process
  readback.
- Commit: `310cacbfa5566d931b53108bc5237a3b073b979c`
  (`test(sync): migrate SYNC-036DE Settings permission projection`).

### SYNC-036DF Closure Record

- Status: completed. Both English Settings paths now use one permission-action
  projection owner after language selection; parent SYNC-036 remains open for
  its remaining owner-specific slices.
- Design and Oracle: each caller resolves Settings content and both exact
  permission actions and establishes the observer before selecting English.
  The owner records an initial snapshot but gates acceptance until the
  language trigger and exact language-control value readback return. An
  immediate trigger readback then requires one foreground Settings projection
  where both actions exist, are hittable, and are at most 36 points high. The
  granted path also requires both exact English Manage labels in that same
  snapshot. Later Logs, App Visibility, and Search evidence in the primary
  path remain independent contracts.
- Lifecycle and retained time policy: XCTest exposes no accessibility-
  projection callback, so the named cancellable 100ms main-RunLoop cadence is
  activated only when the post-trigger readback remains unresolved. The named
  five-second permission-action watchdog is solely the terminal failure bound;
  diagnostics preserve target, trigger gate, generation, source, Settings
  presence, both labels, hittability values, heights, and final waiter state.
  The assertion invocation owns deferred registration, owner cancellation,
  resolution, and teardown; each caller owns its App.
- Unit and Behavior: not relevant because this slice changes UI-test
  synchronization and deterministic UI-target rules only; production APIs,
  state transitions, and application orchestration are unchanged.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; host-context codesign reports the App valid and satisfying its
  Designated Requirement. The canonical signed wrapper passed all eight
  selected tests with zero failures and zero unexpected failures in 23.217
  seconds; xcodebuild completed in 24.664 seconds. The English primary and
  granted-label paths passed in 14.587 and 7.586 seconds. Policy compatibility,
  atomic partial/mismatched-label rejection, pre-trigger evidence gating,
  delayed evidence, and final-watchdog diagnostics passed in 0.164, 0.159,
  0.157, 0.145, and 0.170 seconds. The 100-lifecycle cancellation and duplicate-
  callback Pressure path passed in 0.249 seconds. Delayed scheduling changes
  only completion latency because resolution still requires the exact atomic
  snapshot. Space-fixture topology is not applicable to these single-App
  Settings paths.
- Result evidence: the status ledger records `completed`, every applicable
  exit code is zero, and the result bundle is present. xcresult independently
  records `status=succeeded` and `testsCount=8`.
- Diagnostics and Process/Tooling: both new Swift sources and the affected
  caller parse directly; the project file passes `plutil -lint`, and
  `git diff --check` passes. The owner, deterministic regression, and caller
  sources are 336, 349, and 121 lines with one responsibility per file. Four
  raw `timeout: <number>` arguments are removed, leaving 388 occurrences
  across 16 UI-test files. The build-for-testing log retains 80 unrelated
  existing lowercase warning lines plus one destination warning; fixture
  preparation retains one unrelated source warning plus one destination
  warning. UI execution emits zero lowercase warning lines, one destination
  warning, 48 xcodebuild CFURL no-scheme diagnostics, and 18 CFPrefs shared-
  state diagnostics. No warning names an affected source, and targeted fatal-
  error, sanitizer, main-thread-checker, SwiftUI background-publication, XPC,
  input-method, assertion, crash, unexpected-failure, watchdog-expiry, and
  test-failure scanning is empty. Exact post-run `pgrep -x` readback finds no
  FlowTab, UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 813 MB slice evidence root contains
  only rebuildable results, DerivedData, caches, fixture products, and logs;
  it has no downstream dependency after this durable record. The exact root is
  removed after evidence capture, followed by absence and exact-process
  readback.
- Commit: `a594e01970b5cb977bbf846120b5f9e93bfba668`
  (`test(sync): migrate SYNC-036DF English permission actions`).

### SYNC-036DG Closure Record

- Status: completed. The two page-only Settings-to-App-Visibility callers now
  share one exact manager projection owner; parent SYNC-036 remains open for
  its remaining owner-specific slices. The six inventory-dependent
  transitions are classified separately as SYNC-036DH.
- Design and Oracle: the helper resolves Settings content, the exact Manage
  action, manager container, exact Back action, and the optional localized
  title before navigation. It records an initial snapshot and accepts an
  already-exact manager page. Otherwise it gates later evidence until the
  exact hittable Manage trigger attempt returns, then performs an immediate
  readback. Success requires one foreground Settings snapshot with the root
  Manage action absent, manager present, Back present and hittable, and, for
  the English path, the exact `App Visibility` title present. The navigation
  round trip retains manager disappearance and Manage reappearance as its
  independent return-transition Oracles.
- Lifecycle and retained time policy: Accessibility exposes no SwiftUI page-
  projection callback, so the named cancellable 100ms main-RunLoop cadence is
  activated only when the trigger readback remains unresolved. The named six-
  second watchdog is solely the trigger/projection failure upper bound;
  diagnostics preserve target, generation, source, acceptance gate, App and
  Settings states, Manage/manager/Back/title fields, trigger candidate count,
  and final waiter state. The helper invocation owns deferred registration,
  both observation inputs, cancellation, resolution, and teardown; each test
  owns its App.
- Unit and Behavior: not relevant because this slice changes UI-test
  synchronization and deterministic UI-target rules only; production APIs,
  state transitions, the named 180ms visual animation contract, and
  application orchestration are unchanged.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`; host-context codesign reports the App valid and satisfying its
  Designated Requirement. The final canonical signed wrapper passed all nine
  selected tests with zero failures and zero unexpected failures in 21.284
  seconds; xcodebuild completed in 22.854 seconds. The English page/title and
  forward/back page paths passed in 12.198 and 8.011 seconds. Policy,
  exact/partial/background/optional-title rules, initially satisfied state,
  pre-trigger gating, delayed evidence, and final-watchdog diagnostics passed
  in 0.140, 0.142, 0.210, 0.153, 0.141, and 0.142 seconds. The 100-lifecycle
  cancellation and duplicate-callback Pressure path passed in 0.146 seconds.
  Delayed scheduling changes only completion latency because the atomic page
  snapshot remains the success condition. Space-fixture topology is not
  applicable to these single-App Settings paths.
- Boundary and failure evidence: attempt 001 stopped during build-for-testing
  at the sole compiler diagnostic, an omitted local `manageButton` reference
  retained by the independent return-transition assertion; restoring that
  exact reference closed the error. The expanded attempt-002 probe then ran
  14 tests and reached every manager projection without a DG watchdog failure.
  Eleven passed; the Home Mail path and two Settings Mail paths expired only
  at their downstream exact row assertions after the manager page and Search
  field were already usable. The 85.345-second run had three expected-test
  failures and zero unexpected failures; xcodebuild completed in 93.287
  seconds. This concrete I/O-sensitive inventory boundary is recorded as
  SYNC-036DH rather than widening the page-projection owner.
- Result evidence: final attempt 003 records `stage=completed`, every
  applicable exit code is zero, and the result bundle is present. xcresult
  independently records `status=succeeded` and `testsCount=9`.
- Diagnostics and Process/Tooling: both new Swift sources and the two affected
  callers parse directly; the project file passes `plutil -lint`, and
  `git diff --check` passes. The owner, deterministic regression, English
  caller, and Settings Search sources are 313, 354, 122, and 482 lines with
  one responsibility per file. Five raw `timeout: <number>` arguments are
  removed, leaving 383 occurrences across 16 UI-test files. Final incremental
  build, fixture, and execution logs contain zero lowercase warning lines and
  one destination warning each. UI execution retains 16 xcodebuild CFURL no-
  scheme diagnostics and 20 CFPrefs shared-state diagnostics. No warning
  names an affected source, and targeted fatal-error, sanitizer, main-thread-
  checker, SwiftUI background-publication, XPC, input-method, assertion,
  crash, unexpected-failure, watchdog-expiry, and test-failure scanning is
  empty. Exact post-run `pgrep -x` readback finds no FlowTab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 958 MB slice evidence root contains
  only the recorded attempts, rebuildable results, DerivedData, caches,
  fixture products, and logs; it has no downstream dependency after this
  durable record. The exact root is removed after evidence capture, followed
  by absence and exact-process readback.
- Commit: `2e7f00a1db666f0e8e0ad0850326737f8aca14ff`
  (`test(sync): migrate SYNC-036DG App Visibility page`).

### SYNC-036DH Closure Record

- Status: completed. The six inventory-dependent Settings-to-App-Visibility
  transitions now wait for one explicit installed-App inventory readiness
  state; parent SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: `AppVisibilityManagerModel.reload()` publishes the exact
  main-actor sequence `idle → loading → ready`, applying Apps, persisted hidden
  identities, selection, and loading state before publishing `ready`. The View
  maps that state to a stable accessibility identifier on the always-
  materialized hidden-count text while preserving its visible and spoken
  content. The UI owner resolves the exact manager and ready marker, installs
  deferred observation before invoking the SYNC-036DG navigation owner,
  accepts an already-exact initial foreground target, gates later evidence
  until navigation returns, and performs an immediate trigger readback.
  Success requires one foreground snapshot containing both the manager and
  exact `flowtab.settings.app-visibility.inventory.ready` marker. Search and
  filter callers retain exact row and toggle readbacks as independent Oracles;
  focused Search queries use paste so the AppKit delegate commits the binding
  before those row readbacks.
- Lifecycle and retained time policy: XCTest exposes no target-process callback
  for the published SwiftUI state, so the named cancellable 100ms main-RunLoop
  cadence activates only after the post-navigation readback remains unresolved.
  The named 30-second inventory watchdog is solely the terminal failure upper
  bound; diagnostics preserve target, expected marker identity, acceptance
  gate, generation, source, App state, manager and marker existence, and final
  waiter evidence. The helper invocation owns deferred registration, both
  observations, resolution, cancellation, and teardown; each test owns its
  App, defaults, Search/filter action, and exact downstream assertions.
- Unit and Behavior validation: the canonical FlowTabTests wrapper passed all
  three selected tests with zero failures in 0.571 seconds. Identifier-policy
  coverage fixes the exact idle/loading/ready marker strings. Both model paths
  subscribe before `reload()`, observe exactly `[idle, loading, ready]` on the
  main thread, fulfill only on the loading-to-ready transition, read back the
  final Apps, selection, hidden count, and loading state, and explicitly cancel
  both Combine subscriptions. FlowTabCore is not applicable because the state
  and owner remain inside the FlowTab Settings feature boundary.
- UI and Pressure validation: the canonical signed wrapper's 12-test batch
  passed all seven deterministic owner tests plus four affected full UI paths.
  Initial-state acceptance, pre-navigation gating, delayed evidence, marker
  rules, watchdog diagnostics, and policy preservation passed in 0.200, 0.141,
  0.142, 0.142, 0.163, and 0.141 seconds. The 100-lifecycle cancellation and
  duplicate-callback Pressure path passed in 0.141 seconds. Home ordering,
  persisted-hidden stale inventory, pinyin Search, and current-App paths passed
  in 15.758, 27.365, 6.513, and 17.340 seconds. The remaining complete
  hide-from-Switcher/Search path passed its exact clean-process rerun in 21.608
  seconds. These five UI methods cover all six readiness transitions, and the
  delayed scheduling case changes observation latency while the exact marker
  remains the success condition. Space-fixture topology is not applicable to
  these single-App Settings paths.
- Boundary and failure evidence: attempt 001 ran 12 tests with eight passes and
  showed that exact offscreen rows are absent from the `LazyVStack`
  accessibility projection after inventory completion. Attempt 002 showed an
  empty XCUI value for the manager's SwiftUI `.contain` group. Attempt 003
  reached the exact ready marker while the displayed native Search text had not
  committed the SwiftUI query binding; the resulting snapshot retained the
  unfiltered inventory and led to the delegate-committing paste trigger.
  Attempt 004 passed both the deterministic marker probe and full Home path.
  Attempt 005 then passed 11 of 12 tests; its single failure occurred after DH
  configuration succeeded, when one downstream Switcher Browser query consumed
  three approximately 31-second XCUI snapshot evaluations and reported
  `Timed out while evaluating UI query`. With exact FlowTab, Runner, xcodebuild,
  and fixture processes absent, attempt 006 reran that same full test and passed
  every Settings, toggle, relaunch, Switcher, and Search assertion in 21.608
  seconds.
- Result evidence: Behavior status records `stage=completed`, every applicable
  exit code is zero, and the result bundle is present; xcresult independently
  records `status=succeeded` and `testsCount=3`. UI attempt 005 records the
  transparent batch result `status=failed`, `testsCount=12`, and one failed
  downstream query. UI attempt 006 records `stage=completed`, every applicable
  exit code zero, a present result bundle, and independent xcresult evidence
  `status=succeeded`, `testsCount=1`.
- Diagnostics and Process/Tooling: all affected Swift sources parse directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  model, View, app regression, UI owner, deterministic UI regression, Home
  caller, and Settings Search caller are 179, 615, 206, 259, 285, 802, and 496
  lines; new files stay below 400 lines and touched existing files retain their
  responsibilities. Sixteen raw `timeout: <number>` arguments are removed,
  leaving 367 occurrences across 16 UI-test files. The final UI logs contain
  three destination warnings, 16 xcodebuild CFURL no-scheme diagnostics, and
  three CFPrefs shared-state diagnostics. Behavior compilation retains four
  unrelated existing source warnings, one destination warning, 32 CFURL
  no-scheme diagnostics, and two CFPrefs shared-state diagnostics. No warning
  names a DH source, and targeted fatal-error, assertion, crash, watchdog-
  expiry, unexpected-failure, and final-test-failure scans are empty. Host-
  context deep strict codesign verification accepts the signed UI Runner.
  Exact post-run `pgrep -x` readback finds no FlowTab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 3.4 GB slice evidence root contains
  only rebuildable results, DerivedData, caches, fixture products, exported
  diagnostics, and logs; it has no downstream dependency after this durable
  record. The exact slice root and exported temporary snapshot are removed
  after evidence capture, followed by absence and exact-process readback.
- Commit: `9d25ff31e805d47c0b7c1f064108068b6c6a5c31`
  (`refactor(sync): migrate SYNC-036DH App Visibility inventory`).

### SYNC-036DI Closure Record

- Status: completed. All four App Visibility query-result transitions now use
  one committed-query projection owner; parent SYNC-036 remains open for its
  remaining owner-specific slices.
- Design and Oracle: query mutation is routed through the main-actor
  `updateQuery` transition. A different committed query publishes its value
  and advances `queryProjectionGeneration`; an identical query is a no-op.
  The list projects only that generation through the stable
  `flowtab.settings.app-visibility.list.query-generation.` accessibility
  prefix, preserving query privacy and existing visual and spoken behavior.
  The UI helper captures the exact list-marker baseline, installs deferred
  observation before focusing and pasting, gates acceptance until the trigger
  returns, and performs an immediate readback. Success requires one foreground
  manager snapshot with the exact field value, a prefixed marker different from
  baseline, and the exact target row. Subsequent row selection, toggle changes,
  Home ordering, relaunch, Switcher, and Search assertions remain independent
  Oracles.
- Lifecycle and retained time policy: Accessibility exposes no callback for the
  target process's SwiftUI list projection, so the named cancellable 100ms
  main-RunLoop cadence activates only while the post-trigger readback remains
  unresolved. The named six-second query-projection watchdog is solely the
  terminal failure upper bound; diagnostics preserve expected query, marker
  prefix, baseline and latest marker, target row identity, acceptance gate,
  generation source, App/manager/field/row state, and final waiter evidence.
  The helper invocation owns deferred registration, observation, resolution,
  cancellation, and teardown; each caller owns its App, defaults, row action,
  and downstream assertions.
- Unit and Behavior validation: the canonical FlowTabTests wrapper passed all
  four selected tests with zero failures in 0.562 seconds. The dedicated
  generation test passed in 0.008 seconds and observed exact query values
  `["", "Mail", "ceshi"]` with generations `[0, 1, 2]`, proving that the
  repeated `Mail` update produced no generation. Stable readiness identifiers,
  missing-hidden inventory, and shared pinyin matching passed in 0.001, 0.214,
  and 0.340 seconds. Both Combine observations are installed before mutation
  and explicitly cancelled. FlowTabCore is not applicable because the query
  state and owner remain inside the FlowTab Settings feature boundary.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`. The canonical signed wrapper passed all 11 selected tests with
  zero failures and zero unexpected failures in 70.541 seconds; xcodebuild
  completed in 71.675 seconds. Home, persisted-hidden two-launch, complete
  hide-from-Switcher/Search, and pinyin paths passed in 16.454, 26.299, 20.695,
  and 6.005 seconds. Initial-state acceptance, trigger gating, exact evidence,
  delayed evidence, policy, and final-watchdog diagnostics passed in 0.209,
  0.150, 0.141, 0.143, 0.143, and 0.165 seconds. The 100-lifecycle
  cancellation and duplicate-callback Pressure path passed in 0.139 seconds.
  Delayed scheduling changes only observation latency because the generation,
  exact field value, and row remain the success condition. Space-fixture
  topology is not applicable to these single-App Settings paths.
- Boundary and failure evidence: Behavior attempt 001 stopped at the exact
  compiler diagnostic that the new test used unsupported App-inventory service
  constructor arguments. Reusing the production initializer exposed the main-
  actor boundary in attempt 002. Marking the synchronous model test
  `@MainActor` closed that isolation contract, and attempt 003 built and passed
  all four tests. Neither failure entered test execution or changed production
  behavior.
- Result evidence: final Behavior and UI status ledgers record
  `stage=completed`, every applicable exit code is zero, and each result bundle
  is present. xcresult independently records `status=succeeded` with
  `testsCount=4` and `testsCount=11`, respectively.
- Diagnostics and Process/Tooling: all affected Swift sources parse directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  model, View, app regression, UI owner, deterministic UI regression, Home
  caller, and Settings Search caller are 195, 621, 243, 296, 350, 803, and 496
  lines; both new files stay below 400 lines and touched existing files retain
  their responsibilities. Four raw `timeout: <number>` arguments are removed,
  leaving 363 occurrences across 16 UI-test files. The final UI build and
  fixture logs retain 80 and one unrelated existing lowercase warning lines,
  plus three destination warnings. UI build, fixture, and execution contain
  32, 17, and 48 CFURL no-scheme diagnostics; execution retains 26 CFPrefs
  shared-state diagnostics. Behavior compilation retains 62
  unrelated existing warning lines, one destination warning, 48 CFURL
  no-scheme diagnostics, and three CFPrefs shared-state diagnostics. No warning
  names a DI source, and targeted fatal-error, assertion, crash, watchdog-
  expiry, unexpected-failure, and test-failure scans are empty. Host-context
  deep strict codesign verification accepts the signed UI Runner. Exact
  post-run `pgrep -x` readback finds no FlowTab, UI-test Runner, xcodebuild, or
  Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 1.0 GB slice evidence root contains
  only rebuildable results, DerivedData, caches, fixture products, and logs; it
  has no downstream dependency after this durable record. The exact root is
  removed after evidence capture, followed by absence and exact-process
  readback.
- Commit: `b5a76711432132d5b539d03ef03adccbecfaa2a6`
  (`refactor(sync): migrate SYNC-036DI App Visibility query`).

### SYNC-036DJ Closure Record

- Status: completed. Both Hidden-filter result transitions now use one exact
  committed-filter projection owner; parent SYNC-036 remains open for its
  remaining owner-specific slices.
- Design and Oracle: filter mutation is routed through the main-actor
  `updateFilter` transition. A different filter publishes its value and
  advances `filterProjectionGeneration`; an identical filter is a no-op. The
  filter container projects the exact filter and generation through the stable
  `flowtab.settings.app-visibility.filter-projection.` prefix while retaining
  its child action identities and existing visual and spoken behavior. The UI
  helper captures the exact `.all` marker baseline, installs deferred
  observation before tapping the exact Hidden action, gates acceptance until
  the trigger returns, and performs an immediate readback. Success requires
  one foreground manager snapshot with the exact hittable action, a
  `.hidden` marker different from baseline, and the exact target row.
  Subsequent row selection and toggle assertions remain independent Oracles.
- Lifecycle and retained time policy: Accessibility exposes no callback for the
  target process's SwiftUI filter projection, so the named cancellable 100ms
  main-RunLoop cadence activates only while the post-trigger readback remains
  unresolved. The named six-second filter-projection watchdog is solely the
  terminal failure upper bound; diagnostics preserve expected marker prefix,
  baseline and latest marker, action and target identities, acceptance gate,
  generation source, App/manager/action/row state, and final waiter evidence.
  The helper invocation owns deferred registration, observation, resolution,
  cancellation, and teardown; each caller owns its App, defaults, row action,
  and downstream assertion.
- Unit and Behavior validation: the canonical FlowTabTests wrapper passed all
  three selected tests with zero failures in 0.352 seconds. The dedicated
  generation test passed in 0.002 seconds and observed filters
  `[.all, .hidden, .running]` with generations `[0, 1, 2]`, proving that the
  repeated `.hidden` update produced no generation. Stored hidden-App
  inventory and the adjacent query-generation regression passed in 0.342 and
  0.008 seconds. Both Combine observations are installed before mutation and
  explicitly cancelled. FlowTabCore is not applicable because filter state and
  its owner remain inside the FlowTab Settings feature boundary.
- UI and Pressure validation: the canonical installer rebuilt
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)`, TeamIdentifier
  `96PUA726W9`. The canonical signed wrapper passed all nine selected tests with
  zero failures and zero unexpected failures in 35.476 seconds; xcodebuild
  completed in 37.070 seconds. The stale hidden-App and current-App real paths
  passed in 21.703 and 12.598 seconds, and both logs show the exact
  `flowtab.settings.app-visibility.filter.hidden` Button being found and
  tapped. Initial-state acceptance, trigger gating, exact evidence, delayed
  evidence, policy, and final-watchdog diagnostics passed in 0.302, 0.145,
  0.140, 0.142, 0.142, and 0.164 seconds. The 100-lifecycle cancellation and
  duplicate-callback Pressure path passed in 0.141 seconds. Delayed scheduling
  changes only observation latency because the generation-bearing filter
  marker and exact row remain the success condition. Space-fixture topology is
  not applicable to these single-App Settings paths.
- Result evidence: final Behavior and UI status ledgers record
  `stage=completed`, every applicable exit code is zero, and each result bundle
  is present. xcresult independently records `status=succeeded` with
  `testsCount=3` and `testsCount=9`, respectively.
- Diagnostics and Process/Tooling: all affected Swift sources parse directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  model, View, app regression, UI owner, deterministic UI regression, Settings
  Search caller, and UI identifier owner are 214, 627, 283, 294, 359, 489, and
  220 lines; both new files stay below 400 lines and touched existing files
  retain their responsibilities. Five raw `timeout: <number>` arguments are
  removed, leaving 358 occurrences across 16 UI-test files. UI build, fixture,
  and execution retain 80, one, and zero unrelated existing lowercase warning
  lines, one destination warning each, and 32, 17, and 48 CFURL no-scheme
  diagnostics; execution retains 20 CFPrefs shared-state diagnostics. Behavior
  compilation retains 80 unrelated existing warning lines, one destination
  warning, 64 CFURL no-scheme diagnostics, and three CFPrefs shared-state
  diagnostics. No warning names a DJ source, and targeted fatal-error,
  assertion, crash, watchdog-expiry, unexpected-failure, and final-test-failure
  scans are empty. Host-context deep strict codesign verification accepts the
  signed UI Runner. Exact post-run `pgrep -x` readback finds no FlowTab,
  UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 1.0 GB slice evidence root contains
  only rebuildable results, DerivedData, caches, fixture products, and logs; it
  has no downstream dependency after this durable record. The exact root is
  removed after evidence capture, followed by absence and exact-process
  readback.
- Commit: `9a6838eecba5efe35a654069d5ca1c564d374494`
  (`refactor(sync): migrate SYNC-036DJ App Visibility filter`).

### SYNC-036DK Closure Record

- Status: completed. All five App Visibility row-to-detail-toggle transitions
  now resolve the exact selected-App projection; parent SYNC-036 remains open
  for its remaining owner-specific slices. The separately owned persisted-
  preference-to-Switcher projection discovered during validation is recorded
  as SYNC-036DL.
- Design and Oracle: explicit row selection and automatic inventory selection
  route through the main-actor `selectApp` transition. A different App identity
  publishes the selection and advances `selectionProjectionGeneration`; an
  identical identity is a no-op. The detail container projects the exact
  encoded App identity and generation while retaining the stable contained
  `show-toggle` child identity and existing visual and spoken behavior. The UI
  helper resolves the exact row, target-specific marker, and toggle, installs
  deferred observation before selection, and performs an initial readback.
  An already-selected exact target resolves immediately. Otherwise acceptance
  is gated until the exact row trigger returns and followed by an immediate
  readback. Success requires one foreground manager projection containing the
  exact target row, target-specific detail marker, and exact hittable toggle.
- Lifecycle and retained time policy: Accessibility exposes no callback for the
  target process's SwiftUI detail projection, so the shared named cancellable
  100ms main-RunLoop cadence activates only while the trigger readback remains
  unresolved. The named six-second detail-projection watchdog is solely the
  terminal failure upper bound; diagnostics preserve the target row and marker
  identities, baseline and latest marker, acceptance gate, evidence source,
  App/manager/row/toggle state, and final waiter result. Each helper invocation
  owns deferred registration, observation, resolution, and cancellation;
  callers own App, defaults, toggle mutation/readback, and later orchestration.
- Unit and Behavior validation: the canonical FlowTabTests wrapper passed all
  four selected tests with zero failures in 0.333 seconds. The dedicated
  selection-generation test passed in 0.006 seconds and observed selections
  `[nil, Mail, nil, Browser]` with generations `[0, 1, 2, 3]`, including the
  duplicate-Mail no-op. The stored-hidden inventory regression passed in 0.317
  seconds and observed the automatic initial selection transition. Adjacent
  query and filter generation regressions passed in 0.007 and 0.004 seconds.
  Combine observations are installed before mutation and explicitly cancelled.
  FlowTabCore is not applicable because selection state and its owner remain
  inside the FlowTab Settings feature boundary.
- UI and Pressure validation: the canonical installer supplied the fixed-path
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. Signed UI attempt 001 passed all seven deterministic owner tests
  plus the Home, stored-hidden two-launch, and current-App paths. Initial target
  acceptance, trigger gating, exact-evidence rules, delayed evidence, policy,
  watchdog diagnostics, and 100-lifecycle cancellation/duplicate Pressure
  passed in 0.200, 0.147, 0.142, 0.141, 0.142, 0.163, and 0.141 seconds. The
  three completed real UI tests exercised four detail transitions and passed
  in 12.547, 14.132, and 8.898 seconds. Attempt 003's isolated signed visible-
  path regression exercised the fifth Mail selection, exact marker, exact
  toggle, and toggle readback and passed 1/1 in 8.153 seconds; xcodebuild test
  execution completed in 9.932 seconds. Delayed scheduling changes only
  observation latency because exact target identity and state remain the
  success condition. Space-fixture topology is not applicable to these
  single-App Settings paths.
- Boundary and failure evidence: attempt 001 passed 10 of 11 tests. In its
  remaining full orchestration path, the exact Mail detail row, marker, and
  toggle completed before the later relaunch phase reached the raw Switcher
  Browser-row query. That query issued three approximately 31-second XCUI
  snapshot evaluations and failed after 127.721 seconds. Exact standalone
  attempt 002 reproduced the same downstream line and evaluation pattern after
  the DK detail transition completed, failing 1/1 in 122.747 seconds. A
  two-second host sample during the stall recorded all 1,630 main-thread samples
  in the AppKit/CoreFoundation event-loop Mach wait and 12 background tasks in
  `LiveSwitcherModel.scheduleRuntimePreviewCaptures`,
  `GenericWindowScreenshotPreviewProvider.previews`, and
  `_dispatch_group_wait_slow`. This fixes the next owner boundary at the atomic
  initial Switcher App projection and preview-load pressure; SYNC-036DL carries
  that migration and the complete orchestration rerun. Its canonical reproducer
  is `./scripts/testing/run-ui-tests-local.sh --build-root
  ./.build-local/evidence-driven-sync/SYNC-036DL/ui-tests --output-root
  ./.build-local/evidence-driven-sync/SYNC-036DL/reproduction-001
  -only-testing:FlowTabUITests/FlowTabUITests/testSettingsAppVisibilityHidesMockAppFromSwitcherAndSearch`.
- Result evidence: Behavior status records `stage=completed`, every applicable
  exit code is zero, and the result bundle is present; xcresult records
  `status=succeeded` and `testsCount=4`. UI attempts 001 and 002 transparently
  record `status=failed` with 11 and one tests at the independent downstream
  boundary. UI attempt 003 records `stage=completed`, every applicable exit code
  zero, a present result bundle, and xcresult `status=succeeded` with
  `testsCount=1`.
- Diagnostics and Process/Tooling: all affected Swift sources parse directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  model, View, app regression, UI owner, deterministic UI regression, Home
  caller, Settings Search caller, and UI identifier owner are 234, 634, 342,
  329, 376, 796, 531, and 224 lines; both new files stay below 400 lines, the
  touched 803-line Home caller shrinks by seven lines, and existing
  responsibilities remain intact. Four detail fallback timeout calls are
  removed, leaving 354 literal timeout arguments across the UI target. Final UI
  build and fixture logs retain 80 and one unrelated existing warning lines;
  UI execution and Behavior retain three and four CFURL no-scheme diagnostics,
  and Behavior compilation retains 80 unrelated existing warning lines. No
  warning names a DK source. Targeted fatal-error, crash, watchdog-expiry, and
  unexpected-failure scans are clear outside the two durably recorded DL
  failures. Host-context deep strict codesign verification accepts the signed
  UI Runner. Exact post-run process readback finds no FlowTab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 1.9 GB slice root, 124 KB host sample,
  and 26 MB temporary Swift cache contain only rebuildable validation products
  and the evidence durably summarized above. They are removed before the slice
  commit, followed by exact absence and process readback.
- Commit: `dee5ed5551fc800abefe3f0f411d488e5f13c548`
  (`refactor(sync): migrate SYNC-036DK App Visibility detail`).

### SYNC-036DL Closure Record

- Status: completed. The persisted App-visibility handoff now resolves one
  atomic initial Switcher App projection after relaunch; parent SYNC-036 remains
  open for its remaining owner-specific slices.
- Design and Oracle: the helper resolves the exact diagnostics summary before
  launch, derives stable App-row identities from the exact bundle targets,
  installs a deferred projection observer, and performs the initial readback
  while the target process is still outside the foreground state. Evidence
  acceptance is gated until the launch trigger returns, followed by an
  immediate trigger readback. Success requires
  one foreground committed diagnostics projection whose exact bundle-identifier
  set contains Browser and excludes Mail. One subsequent exact identifier-set
  Accessibility readback independently confirms the required Browser row and
  excluded Mail row without combining snapshots from separate waits. Existing
  production filtering remains owned by `AppVisibilityFilter`, the Settings
  preference generations, and `LiveSwitcherSession.apps`; the already-filtered
  session diagnostics provide the independent UI Oracle.
- Lifecycle and retained time policy: target-process Accessibility supplies no
  projection callback, so the shared named cancellable 100ms main-RunLoop
  cadence activates only when the immediate trigger readback is unresolved.
  The named ten-second post-launch watchdog is solely the terminal failure upper
  bound. Diagnostics retain App state, summary identifier/value, required and
  excluded bundle identities, parsed entries, acceptance gate, evidence source,
  and final waiter result. Each relaunch helper invocation owns deferred
  registration, observer resolution, timer cancellation, and cleanup; the test
  owns defaults and both application processes.
- Unit and Behavior validation: canonical FlowTabCore validation selected the
  three `PreferencesTests.testAppVisibilityFilter*` rules and passed 3/3 in
  0.002 seconds, covering normalization, removal, and stable ranking. The
  canonical FlowTabTests wrapper passed the filter-generation,
  selection-generation, and hidden-App session/Search regressions 3/3 with zero
  failures in 0.009 seconds; the individual tests completed in 0.002, 0.002,
  and 0.005 seconds. These layers prove the deterministic preference-to-session
  state transition independently of XCUI timing.
- UI and Pressure validation: the canonical installer supplied the fixed-path
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. Signed UI attempt 001 passed the ten deterministic owner tests
  and the complete Settings-to-Switcher regression 11/11 in 16.902 seconds;
  the full visible path completed in 15.339 seconds. Its committed-summary
  readback began at 8.83 seconds, resolved by 8.88 seconds, and its single exact
  row-set query completed by 9.12 seconds. Initial state, exact and prefix-
  collision identity, atomic required/excluded state, foreground gating,
  delayed evidence, stale/duplicate readbacks, cancellation, named policy, and
  watchdog diagnostics all passed. Three signed repetitions of the full path
  passed 3/3 in 44.679 seconds at 14.890, 15.052, and 14.737 seconds. The owner
  also retained its 100-generation stale-readback Pressure regression.
- Fixture and boundary validation: the signed real Chrome Space fixture path
  `testSwitcherPanelOptionTabHidesDesktopProvisionalCGOnlyWorkflowWindow`
  passed 1/1 in 8.564 seconds, confirming compatibility with the shared
  Switcher projection owner and real fixture topology. The same full regression
  that previously reached repeated approximately 31-second raw Browser-row
  snapshots passed four consecutive times with the atomic committed-summary
  Oracle and one exact row-set readback. Scheduling and preview workload now
  affect evidence arrival latency while exact bundle and row identities remain
  the success condition.
- Result evidence: Behavior, main UI, UI Pressure, and fixture status records
  each retain `stage=completed`, zero for every applicable exit code, and a
  present result bundle. Their xcresults retain `status=succeeded` with 3, 11,
  3, and one tests. Main UI, Pressure, and fixture xcodebuild executions
  completed in 17.996, 45.387, and 9.270 seconds.
- Diagnostics and Process/Tooling: all four affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The identifier owner, Settings Search caller, projection owner, and
  deterministic regressions are 227, 533, 414, and 375 lines; the expanded
  414-line owner retains one clear responsibility. Three raw timeout calls are
  removed, leaving 351 literal timeout arguments across the UI target. Behavior
  compilation and the main UI build retain 80 unrelated existing warning lines
  each; main fixture preparation retains one destination warning. Remaining
  logs retain only the known CFURL no-scheme and CFPrefs shared-state
  diagnostics. Targeted fatal-error, assertion, crash, watchdog-expiry,
  unexpected-failure, and final-test-failure scans are empty. Host-context deep
  strict codesign verification accepts the signed UI Runner. Exact post-run
  process readback finds no FlowTab, UI-test Runner, xcodebuild, or Space
  fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 1.0 GB DL evidence root contains
  only rebuildable results, DerivedData, caches, fixture products, and logs. It
  is removed after this durable record, followed by exact absence and process
  readback.
- Commit: `bf9103f57a95914214bf89f3c83de6182295f6d4`
  (`refactor(sync): migrate SYNC-036DL Switcher app projection`).

### SYNC-036DM Closure Record

- Status: completed. A persisted disabled-Search launch now resolves the exact
  initial App-cycle presentation without creating an impossible Search-
  activation observer. Parent SYNC-036 remains open for its remaining owner-
  specific slices.
- Design and Oracle: the bootstrap captures one immutable launch-intent plus
  persisted-feature decision before installing the initial-presentation
  owners. The enabled branch retains its committed-index generation and
  readback contract. The disabled branch presents the exact global candidate,
  omits Search activation, keeps the App-cycle session alive, and publishes a
  terminal resolution only after the existing initial-presentation owner has
  produced candidate, attempt, and post-presentation evidence. A test-only
  transport atomically writes one schema-versioned receipt before posting its
  unique distributed notification. The receipt includes observation and
  runtime generations, accepted evidence source, global baseline/candidate/
  post modes, exact candidate/session/post item identities and process
  identity, presentation result, panel and session state, persisted feature
  readback, and active/pending Search state. UI success requires one internally
  consistent receipt with the required item identities, equal candidate,
  session, and post projections, a same-or-later post generation, presented
  App-cycle state, and exact disabled/inactive/not-pending Search values.
- Boundary, lifecycle, and retained time policy: the UI invocation creates a
  unique temporary path, removes its initial state, resolves it to an absolute
  standardized resource path at the TestingSupport launch boundary, and
  installs observation before launching the second process. Relative paths and
  absent UI-test sentinels are rejected. The target writes and notifies without
  retaining an owner. The UI invocation owns the distributed observer,
  readback scheduler, resolution, cancellation, file cleanup, and second
  process; the test owns defaults and the first process. Notification loss has
  a platform-fallback named cancellable 100ms file-readback cadence. The named
  zero-duration immediate-readback policy performs deterministic initial and
  final reads. The named eight-second watchdog is solely a terminal failure
  bound and reports path, file state, decode error, last receipt, evidence
  source, and waiter result. No product duration is introduced.
- Unit and Behavior validation: FlowTabCore is not applicable because the
  launch bootstrap, persisted preference, transport, and panel owner remain in
  the FlowTab TestingSupport/App boundary. The canonical FlowTabTests wrapper
  passed 10/10 selected tests with zero failures in 0.382 seconds; xcodebuild
  test execution completed in 2.452 seconds. The disabled-preference
  orchestration and atomic receipt passed in 0.142 seconds, absolute-route
  parsing in 0.001 seconds, enabled Search sizing in 0.123 seconds, and delayed
  committed-index evidence in 0.113 seconds. Six adjacent activation-owner
  tests cover initial readback, event arrival before and after presentation,
  100 stale events under Pressure, cancellation, and final watchdog evidence.
- UI and Pressure validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. Final signed UI attempt 004 rebuilt the current source and
  passed 4/4 in 7.534 seconds; xcodebuild execution completed in 8.626 seconds.
  The full Settings-persist, exact first-process termination, prelaunch
  observation, atomic terminal receipt, and exact second-process termination
  path passed in 7.021 seconds. Initial-state acceptance, stale wrong-baseline
  and wrong-feature rejection followed by delayed exact evidence plus duplicate
  notification, and cancellation/watchdog diagnostics passed in 0.147, 0.142,
  and 0.225 seconds. Final signed Pressure attempt 003 repeated the full path
  three times and passed 3/3 in 21.448 seconds at 6.932, 7.420, and 7.096
  seconds; xcodebuild completed in 22.116 seconds. A separate directly
  authorized signed run passed the full path 1/1 in 79.340 seconds after XCTest
  delayed its idle notification until 66.02 seconds, demonstrating that host
  scheduling changed latency while the exact receipt result remained stable.
- Fixture and independent boundary validation: the final signed real Finder,
  Chrome, and Notes topology passed the window-Search round trip 1/1 in 20.530
  seconds; xcodebuild completed in 21.230 seconds and the exact `Docs` window
  became frontmost after Search confirmation. Real app-Search attempt 001 had
  already reached the exact initial Search responder, while XCTest synthesized
  the two-token query as `fixturechrome`. Its failure snapshot independently
  retained Chrome Fixture in both the committed diagnostics App set and exact
  App-row projection. This fixes that failed attempt at the text-synthesis
  boundary; the single-token real window path closes the affected initial-
  Search and fixture-topology contract.
- Failure evidence: Behavior attempt 001 stopped at a missing-return compiler
  diagnostic in an abandoned Switcher AX-transport prototype, and attempt 003
  stopped at three out-of-scope test-helper compiler diagnostics; neither
  entered test execution. UI readback attempt 001 passed the full visible path
  and two owner tests, while one diagnostic assertion required an XCTest-
  specific `timedOut` spelling; the final contract checks the stable waiter
  field and attempts 002 through 004 pass. The abandoned AX-summary transport's
  Pressure attempt 001 passed two iterations, then its third summary query
  evaluated at 37.10 and 68.13 seconds and failed after 112.774 seconds.
  Replacing that query with the target-owned atomic file receipt produced two
  consecutive 3/3 Pressure passes. The real two-token fixture failure and its
  AX snapshot are classified above; the final single-token topology path
  passes.
- Result evidence: final Behavior, UI, Pressure, and fixture status ledgers all
  record `stage=completed`, zero applicable child and final exit codes, and a
  present result bundle. Their xcresults record `status=succeeded` with 10,
  four, one logical repeated test, and one fixture test; the Pressure execution
  log independently records all three iterations and zero failures.
- Diagnostics and Process/Tooling: all eight affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. Launch options, presentation bootstrap, Search bootstrap, target
  transport, Behavior regression, Settings Search caller, UI owner, and UI
  regression are 458, 344, 238, 162, 651, 590, 317, and 275 lines. All three
  new files stay below 400 lines; touched existing files retain one clear
  responsibility. Two raw timeout calls are removed, leaving 349 occurrences
  across 16 UI-test files. Final Behavior compilation retains 63 unrelated
  existing warning lines. Final UI build, UI execution, Pressure, fixture
  preparation, and fixture execution each retain one destination warning;
  execution logs retain only 16 known CFURL no-scheme diagnostics and 9, 9,
  and 3 CFPrefs shared-state diagnostics. Targeted fatal-error, assertion,
  crash, watchdog-expiry, unexpected-failure, and final-test-failure scans are
  empty. Host-context strict deep codesign verification accepts the signed UI
  Runner. Exact post-run `pgrep -x` readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 1.8 GB DM evidence root contains
  only rebuildable results, DerivedData, caches, fixture products, exported
  diagnostics, and logs. It is removed after this durable record, followed by
  exact absence and process readback.
- Commit: `08dcf5facf29c30977354aab36c03ea63b62c80e`
  (`refactor(sync): migrate SYNC-036DM disabled Search launch`).

### SYNC-036DN Closure Record

- Status: completed. Persisted Window- and App-scope Search phases now resolve
  from the exact committed query/result projection and matching visible row.
  Parent SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: the existing Search-result snapshot now carries the
  percent-decoded, order-preserving, deduplicated result IDs from the same
  diagnostics projection as committed scope and query, plus exact row identity,
  row existence, and App state. The shared owner starts before text input and
  takes an initial readback. The helper rejects an already-matching baseline,
  gates later evidence until input delivery returns, and requests an immediate
  trigger readback. Window success requires scope `window`, query `Inbox`,
  result `window:com.flowtab.mock.mail#mock-mail-inbox`, the matching Inbox row,
  and foreground App state in one snapshot. App success applies the same rule
  to scope `app`, query `Mail`, result `app:com.flowtab.mock.mail`, and the exact
  Mail row. Result computation and Accessibility materialization can complete
  at any later cadence without changing the accepted identity.
- Boundary, lifecycle, and retained time policy: the change remains entirely
  in the UI-test target and preserves production Search scheduling, persisted
  preferences, runtime topology, and Accessibility identifiers. The existing
  condition owner performs its initial readback synchronously and owns one
  generation plus a named cancellable 100ms main-RunLoop readback schedule
  because XCTest exposes no target-process Search-publication callback. The
  helper invocation cancels that owner on every exit; each test phase owns its
  App process and the test owns preference reset. The compatible named
  eight-second watchdog is solely a terminal failure upper bound and reports
  generation, source, scope, query, result IDs, row identity/existence, App
  state, and final waiter evidence. No product duration is introduced.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production state, orchestration, and module dependencies
  are unchanged. The signed UI-target deterministic run is the narrowest layer
  that compiles and executes the changed owner and caller.
- UI and Pressure validation: the canonical installer rebuilt and installed
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical owner attempt passed 8/8 tests in 1.053 seconds;
  xcodebuild completed in 2.521 seconds. Coverage includes policy
  compatibility, exact parser decoding/deduplication, trigger gating, wrong
  scope, background App, missing row, immediate exact evidence, delayed
  scheduled evidence, watchdog final readback, fresh committed identifiers,
  and 100 stale/cancel/restart generations. The complete signed Settings,
  Window Search, Settings, and App Search path passed 1/1 in 24.035 seconds;
  xcodebuild completed in 24.683 seconds. Signed UI Pressure then repeated that
  complete path three times and passed 3/3 in 68.768 seconds at 23.125, 22.995,
  and 22.648 seconds; xcodebuild completed in 69.867 seconds.
- Result evidence: all three canonical status ledgers record
  `stage=completed`, zero applicable child and final exit codes, signing success,
  and present result bundles. Their xcresults record `status=succeeded` with
  eight owner tests, one complete-path test, and one logical test repeated three
  times; the Pressure execution log independently records all three iterations
  and zero failures.
- Diagnostics and Process/Tooling: all four affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The shared owner, owner regressions, extracted invocation helper, and
  Settings caller are 740, 784, 71, and 638 lines; the new file stays below 400
  lines and each touched file retains one responsibility. Two raw timeout calls
  are removed, leaving 347 occurrences across 16 UI-test files. The complete
  build retains 81 unrelated existing warnings; owner, complete-path, and
  Pressure execution each retain one destination warning. Their execution logs
  retain only the known CFURL no-scheme and CFPrefs shared-state diagnostics;
  targeted fatal-error, assertion, crash, watchdog-expiry, unexpected-failure,
  and final-test-failure scans are empty. Sandbox-local trust-chain inspection
  cannot access the signing trust context; the exact host-context strict deep
  verification accepts the signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 481 MB DN evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `d2666acb5a42d776ec9c23b923b4d9386f84323b`
  (`refactor(sync): migrate SYNC-036DN committed Search rows`).

### SYNC-036DO Closure Record

- Status: completed. Each user-triggered mock Switcher phase now resolves from
  its committed foreground Mail projection and exact visible App row. Parent
  SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: `launchMockSwitcherSearchFromUserPath` now installs the
  existing generation-owned Switcher App-projection observer after application
  readiness and before posting the global trigger. Its synchronous initial
  readback captures the foreground App with an absent Switcher summary. Trigger
  completion remains the exact distributed-notification delivery result; the
  observer then performs an immediate readback and accepts only diagnostics
  whose App entries contain the exact `com.flowtab.mock.mail` bundle identity,
  followed by the exact Mail Accessibility row readback. The initial full-path
  attempt also exposed that querying `identifier` on the intentionally absent
  summary makes XCTest create a failure. The snapshot now records the exact
  identifier already used to construct the query, so `exists=false` remains a
  valid initial observation.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production trigger delivery, persisted Search
  preferences, Switcher identity semantics, and module dependencies. The
  Settings Search caller owns a named compatible eight-second watchdog solely
  as a terminal failure upper bound. The shared owner retains its named
  cancellable 100ms main-RunLoop readback cadence because XCTest exposes no
  target-process presentation callback. The helper invocation owns observer
  and deferred-scheduler cancellation on every exit; each phase owns its App
  process and the test owns preference reset. No product duration is added.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production rules, application orchestration, persistence,
  and module wiring are unchanged. The signed UI target is the narrowest layer
  that compiles and executes the changed helper and caller.
- UI and Pressure validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The initial owner attempt passed 11/11 policy and deterministic
  owner tests in 1.477 seconds; xcodebuild test execution completed in 3.755
  seconds. After the absent-summary correction, final canonical attempt 002
  rebuilt the current source and passed the complete three-process path plus
  all owner tests 12/12 in 22.846 seconds; the visible path completed in 21.469
  seconds and xcodebuild completed in 23.926 seconds. Its two pre-trigger
  summary readbacks occurred at 7.93 and 18.08 seconds, followed by exact
  committed-summary readbacks at 8.16 and 18.31 seconds. Signed UI Pressure
  repeated the complete path three times and passed 3/3 in 64.675 seconds at
  21.518, 21.588, and 21.569 seconds; xcodebuild completed in 65.742 seconds.
  The deterministic set covers policy compatibility, initial acceptance,
  exact and prefix-collision identity, partial and background state, trigger
  gating, delayed evidence, cancellation, stale and duplicate generation
  readbacks, 100-iteration owner Pressure, and final watchdog diagnostics.
- Failure and expanded compatibility evidence: full-path attempt 001 produced
  the stable absent-summary `identifier` failure in 12.209 seconds before any
  Search input, directly motivating the exact-query-identity readback above.
  Two additional runs of the already-migrated heavy App Visibility path reached
  the unchanged summary `exists` query and were terminated by XCTest's internal
  Accessibility snapshot bound after three approximately 31-second evaluations.
  The first also recorded an App idle-notification delay at 68.64 seconds; the
  attempts completed in 189.568 and 112.798 seconds. Both signed result bundles
  identify `Timed out while evaluating UI query` at the same readback line,
  before the shared owner's named watchdog can execute. This is expanded,
  non-gating host-platform evidence for the preview-heavy SYNC-036DL path. The
  owned user-triggered path used the same helper successfully in the final run
  and all three Pressure repetitions; the parent Goal stays active while later
  slices continue the repository-wide closure.
- Result evidence: owner attempt 001, final regression attempt 002, and Pressure
  attempt 001 each retain `stage=completed`, zero applicable child and final
  exit codes, signing success, and a present result bundle. Their xcresults
  retain `status=succeeded` with 11 tests, 12 tests, and one logical test
  repeated three times; the Pressure execution log independently records all
  three iterations and zero failures. Full-path attempt 001 and the two
  expanded adjacent attempts retain `stage=xcodebuild_test-without-building_failed`,
  exit code 65, and present failed xcresults for the classified evidence above.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The shared owner, deterministic regressions, and Settings caller are
  415, 375, and 665 lines; each remains within one clear responsibility. One
  raw literal timeout is removed, leaving 346 occurrences across 16 UI-test
  files and 13 in Settings Search. Final build, regression, and Pressure logs
  each retain one destination warning. Execution logs retain only the known
  CFURL no-scheme and CFPrefs shared-state diagnostics; targeted fatal-error,
  assertion, crash, watchdog-expiry, unexpected-failure, and final-test-failure
  scans of successful attempts are empty. Host-context strict deep codesign
  verification accepts the signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 882 MB DO evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `d95de213f7308f5443065998a0e73d68cd1a799c`
  (`refactor(sync): migrate SYNC-036DO user-path Switcher projection`).

### SYNC-036DP Closure Record

- Status: completed. The mock Settings Search user path now applies an
  owner-named foreground-readiness watchdog while retaining exact
  `XCUIApplication.State.runningForeground` evidence as its sole success
  condition. Parent SYNC-036 remains open for its remaining owner-specific
  slices.
- Design and Oracle: both Window- and App-scope launch phases call the shared
  readiness helper with the Settings Search path trace. The helper performs its
  exact-state readback and retains its bounded activation fallback. The caller
  accepts only `runningForeground`; watchdog failure reports the expected state
  and final App state. The raw duration no longer carries success meaning.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production launch behavior, trigger delivery,
  persisted Search preferences, process identity, and module dependencies. The
  compatible ten-second duration is owned by the Settings Search watchdog
  policy and solely bounds a terminal test failure. The calling test owns each
  application process and XCTest owns the state wait. This slice adds no
  observer, timer, retry schedule, or product duration.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production rules, application orchestration, and module
  wiring are unchanged. The signed UI target is the narrowest layer that
  compiles and executes the changed policy and caller.
- UI and Pressure validation: the canonical installer rebuilt and installed
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed the affected full path and
  policy regression 2/2 in 21.644 seconds at 21.483 and 0.162 seconds;
  xcodebuild completed in 23.177 seconds. Both user-path waits read
  `runningForeground` before and after the wait with exact frontmost bundle
  `io.github.potato-dumplings.flowtab`. Signed UI Pressure repeated the full
  path three times and passed 3/3 in 63.466 seconds at 20.851, 21.524, and
  21.090 seconds; xcodebuild completed in 64.541 seconds. All six Pressure
  foreground waits retained the same exact state and bundle evidence.
- Result evidence: both canonical status ledgers record `stage=completed`,
  zero applicable child and final exit codes, signing success, and present
  result bundles. Their xcresults record `status=succeeded`, zero error,
  warning, and test-failure counts, and respectively two tests plus one logical
  test repeated three times. The Pressure execution log independently records
  all three iterations and zero failures.
- Diagnostics and Process/Tooling: the affected Swift source parses directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  Settings Search file is 694 lines and retains one responsibility. One raw
  literal timeout is removed, leaving 345 occurrences across 16 UI-test files
  and 12 in Settings Search. The full build retains 81 unrelated existing
  warning lines; regression and Pressure execution each retain one destination
  warning. Their execution logs retain only 51 and 17 known CFURL no-scheme
  diagnostics plus 5 and 9 CFPrefs shared-state diagnostics. Targeted
  fatal-error, assertion, crash, watchdog-expiry, unexpected-failure, and
  final-test-failure scans are empty. Host-context strict deep codesign
  verification accepts the signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 481 MB DP evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `3be0d877ed27f57dc9b9936b7356b5c3369ece01`
  (`test(sync): migrate SYNC-036DP user-path foreground readiness`).

### SYNC-036DQ Closure Record

- Status: completed. The permission-limited Settings Search path now uses the
  shared Settings Search application-foreground watchdog and resolves only
  from exact `XCUIApplication.State.runningForeground` evidence. Parent
  SYNC-036 remains open for its remaining owner-specific slices.
- Design and Oracle: the test launches its mock-permission process, calls the
  shared readiness helper with a permission-scope trace, and accepts only
  `runningForeground`. The assertion reports expected and final App state if
  the watchdog expires. Its later app-only Search-scope observer retains its
  own initial readback, trigger gate, and atomic projection Oracle.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production launch behavior, simulated permission
  state, Settings projection, process identity, and module dependencies. The
  compatible ten-second duration is owned by the Settings Search watchdog
  policy and solely bounds terminal test failure. The test owns the App process
  and XCTest owns the exact-state wait. This slice adds no observer, timer,
  retry schedule, or product duration.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production rules, permission decisions, orchestration,
  and module wiring are unchanged. The signed UI target is the narrowest layer
  that compiles and executes the changed caller.
- UI and Pressure validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed the policy and affected UI path
  2/2 in 4.595 seconds at 0.152 and 4.442 seconds; xcodebuild completed in
  6.038 seconds. The path read `runningForeground` before and after its wait
  with exact frontmost bundle `io.github.potato-dumplings.flowtab`. Signed UI
  Pressure repeated the affected path three times and passed 3/3 in 11.259
  seconds at 3.947, 3.640, and 3.671 seconds; xcodebuild completed in 11.929
  seconds. Every repetition retained the same exact state and bundle evidence.
- Result evidence: both canonical status ledgers record `stage=completed`,
  zero applicable child and final exit codes, signing success, and present
  result bundles. Their xcresults record `status=succeeded`, zero error,
  warning, and test-failure counts, and respectively two tests plus one logical
  test repeated three times. The Pressure execution log independently records
  all three iterations and zero failures.
- Diagnostics and Process/Tooling: the affected Swift source parses directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  Settings Search file is 708 lines and retains one responsibility. One raw
  literal timeout is removed, leaving 344 occurrences across 16 UI-test files
  and 11 in Settings Search. The full build retains 81 unrelated existing
  warning lines; regression and Pressure execution each retain one destination
  warning. Their execution logs retain only 51 and 17 known CFURL no-scheme
  diagnostics plus 5 and 9 CFPrefs shared-state diagnostics. Targeted
  fatal-error, assertion, crash, watchdog-expiry, unexpected-failure, and
  final-test-failure scans are empty. Host-context strict deep codesign
  verification accepts the signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 481 MB DQ evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `b26836b0fcffce9c9913b32fe2e6238624560245`
  (`test(sync): migrate SYNC-036DQ permission foreground readiness`).

### SYNC-036DR Closure Record

- Status: completed. All three Settings Search feature-toggle paths now resolve
  the exact stable identifier independently of Accessibility control type and
  use a Settings Search-owned terminal watchdog. Parent SYNC-036 remains open
  for its remaining owner-specific slices.
- Design and Oracle: `requireSettingsSearchEnabledToggle` constructs the exact
  type-agnostic query after the separately completed Settings-navigation
  projection, records an immediate existence readback, and waits only while
  that exact query remains absent. It requires final existence before returning
  the element. This removes elapsed-time selection between Switch and Checkbox
  identities. Failure reports scenario, expected identifier, App state,
  initial and waiter evidence, and final existence, type, hittability, and
  value. Each caller retains its independent on/off and persistence readbacks.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production toggle behavior, Settings navigation,
  persisted Search preferences, and module dependencies. The compatible
  five-second duration is owned by the Settings Search watchdog policy and
  solely bounds terminal test failure. XCTest owns the exact-element wait and
  each test owns its App processes. No recurring observer, timer, retry
  schedule, or product duration is introduced.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production state, persistence, orchestration, and module
  wiring are unchanged. The signed UI target is the narrowest layer that
  compiles and executes the changed helper and all callers.
- UI and Pressure validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed the policy plus disabled,
  persisted-scope, and direct-scope-switch paths 4/4 in 36.315 seconds at
  0.201, 6.328, 20.425, and 9.362 seconds; xcodebuild completed in 37.725
  seconds. Each exact query was initially present and resolved to the actual
  Switch type. Signed UI Pressure repeated the direct-scope-switch path three
  times and passed 3/3 in 26.226 seconds at 9.063, 8.632, and 8.531 seconds;
  xcodebuild completed in 27.326 seconds. Every repetition retained the same
  exact identifier, initial-state acceptance, and Switch readback.
- Result evidence: both canonical status ledgers record `stage=completed`,
  zero applicable child and final exit codes, signing success, and present
  result bundles. Their xcresults record `status=succeeded`, zero error,
  warning, and test-failure counts, and respectively four tests plus one logical
  test repeated three times. The Pressure execution log independently records
  all three iterations and zero failures.
- Diagnostics and Process/Tooling: the affected Swift source parses directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  Settings Search file is 770 lines and retains one responsibility. Three raw
  literal timeouts are removed, leaving 341 occurrences across 16 UI-test files
  and 8 in Settings Search. The full build retains 81 unrelated existing
  warning lines; regression and Pressure execution each retain one destination
  warning. Their execution logs retain only 51 and 17 known CFURL no-scheme
  diagnostics plus 11 and 9 CFPrefs shared-state diagnostics. Targeted
  fatal-error, assertion, crash, watchdog-expiry, unexpected-failure, and
  final-test-failure scans are empty. Host-context strict deep codesign
  verification accepts the signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 481 MB DR evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `6b27f3cb9ab1b8accba4a42bc14f9b82b8e6f76f`
  (`test(sync): migrate SYNC-036DR Search toggle projection`).

### SYNC-036DS Closure Record

- Status: completed. App Visibility Back navigation now resolves from one exact
  manager-to-Settings projection transition. Parent SYNC-036 remains open for
  its remaining owner-specific slices.
- Design and Oracle: the shared page expectation now distinguishes a complete
  manager projection from a complete Settings-root projection over the same
  atomic snapshot. The reverse helper resolves all exact elements, installs its
  deferred observation before Back, and requires the initial foreground manager
  baseline. It gates root acceptance until the exact Back trigger returns and
  requests an immediate readback. Success requires one foreground snapshot with
  Settings content and a hittable Manage action while manager and Back are both
  absent. Partial animation projections cannot satisfy the transition.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production navigation, Accessibility identifiers,
  animation policy, and module dependencies. XCTest exposes no SwiftUI page-
  transition callback, so the existing named serial 100ms XCUI readback cadence
  remains active only after the trigger and only while unresolved. The helper
  owns deferred registration plus owner cancellation on every exit; the test
  owns the App process. The compatible six-second policy solely bounds exact
  Back discovery and terminal projection failure with target, acceptance gate,
  generation, source, and every final snapshot field.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production state transitions, orchestration, and module
  wiring are unchanged. Deterministic owner tests plus the signed UI path are
  the narrowest layers that execute this test-only transition.
- UI and Pressure validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed nine owner tests plus the
  affected reverse-navigation UI 10/10 in 6.842 seconds; the visible path took
  5.614 seconds, owner coverage took 1.228 seconds, and xcodebuild completed in
  8.276 seconds. Coverage includes manager and Settings-root truth tables,
  initial manager acceptance, trigger gating, pre-trigger root rejection,
  delayed evidence, watchdog final readback, cancellation, duplicate scheduled
  work, and 100 manager plus 100 Settings-root lifecycle generations. Signed UI
  Pressure repeated the visible path three times and passed 3/3 in 16.312
  seconds at 5.325, 5.657, and 5.329 seconds; xcodebuild completed in 16.988
  seconds.
- Result evidence: both canonical status ledgers record `stage=completed`,
  zero applicable child and final exit codes, signing success, and present
  result bundles. Their xcresults record `status=succeeded`, zero error,
  warning, and test-failure counts, and respectively ten tests plus one logical
  test repeated three times. The Pressure execution log independently records
  all three iterations and zero failures.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The shared owner, deterministic regressions, and Settings Search
  caller are 444, 582, and 759 lines; each retains one responsibility. Three raw
  literal timeouts are removed, leaving 338 occurrences across 16 UI-test files
  and 5 in Settings Search. The full build retains 81 unrelated existing
  warning lines; regression and Pressure execution each retain one destination
  warning. Their execution logs retain only 51 and 17 known CFURL no-scheme
  diagnostics plus 21 and 9 CFPrefs shared-state diagnostics. Targeted
  fatal-error, assertion, crash, watchdog-expiry, unexpected-failure, and
  final-test-failure scans are empty. Host-context strict deep codesign
  verification accepts the signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 481 MB DS evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `591e431e346f6e4e583837e119f051ec05ba9284`
  (`test(sync): migrate SYNC-036DS App Visibility Back projection`).

### SYNC-036DT Closure Record

- Status: completed. The hidden-App path's third FlowTab process now applies the
  Settings Search-owned foreground watchdog and advances only after exact
  `XCUIApplication.State.runningForeground` evidence. Parent SYNC-036 remains
  open for its remaining owner-specific slices.
- Design and Oracle: the caller retains the Search-readiness observer before
  launch, invokes the shared application-readiness helper with the
  `settings-app-visibility-hidden-search-readiness` trace, and treats only the
  helper's exact foreground-state result as success. Expiry reports expected
  and final application state and returns before consuming the independent
  Search-input, diagnostics, and hidden-row Oracles. The raw duration no longer
  carries success meaning.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves hidden-App defaults, stable application identity,
  three-process launch topology, Search behavior, and module dependencies. The
  compatible ten-second duration is owned by the Settings Search watchdog
  policy and solely bounds terminal failure. The test owns each application
  process; XCTest owns the synchronous state wait. This slice adds no recurring
  observer, timer, retry cadence, scheduler, or product duration.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production visibility rules, application orchestration,
  and module wiring are unchanged. The deterministic policy assertion and the
  signed affected UI path are the narrowest layers that execute this test-only
  readiness call site.
- UI validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed the policy compatibility test
  and affected hidden-App path 2/2 in 15.918 seconds; the visible path took
  15.792 seconds, policy coverage took 0.126 seconds, and xcodebuild completed
  in 17.331 seconds. The third process trace recorded foreground state both
  immediately before and after the shared readiness wait.
- Pressure validation: the required signed cohort passed 3/3 at 14.621,
  15.141, and 14.596 seconds, totalling 44.358 seconds; the corresponding
  xcodebuild intervals totalled 46.851 seconds. Each repetition completed the
  Settings mutation, exact Switcher projection, third-process foreground
  evidence, Search-input readiness, committed empty result, and exact hidden-
  row absence.
- Expanded host evidence: an earlier non-gating attempt reached the existing
  Switcher projection readback, then XCTest spent about 47 seconds evaluating
  `allElementsBoundByIndex` at
  `FlowTabUITests+SwitcherAppProjectionObservation.swift:280` and failed with
  `Timed out while evaluating UI query` before the third Search process or this
  slice's changed readiness call site ran. Its test and xcodebuild intervals
  were 57.581 and 64.635 seconds. The result bundle reports one test failure and
  no error or warning summaries. Exact process readback was empty afterward.
  Reproduction uses the canonical command with the retained build root:
  `./scripts/testing/run-ui-tests-local.sh test-without-building --build-root
  ./.build-local/evidence-driven-sync/SYNC-036DT/build/ui-tests --output-root
  <new-output-root> --skip-space-fixtures
  -only-testing:FlowTabUITests/FlowTabUITests/testSettingsAppVisibilityHidesMockAppFromSwitcherAndSearch`.
- Result evidence: the regression and three required Pressure status ledgers
  record `stage=completed`, zero applicable child and final exit codes, signing
  success, and present result bundles. Their xcresults record
  `status=succeeded`, zero error, warning, and test-failure counts, and two or
  one logical tests respectively. The expanded attempt's ledger records
  `xcodebuild_test-without-building_failed`, exit 65, successful signing, and a
  present result bundle; its exact failure is recorded above.
- Diagnostics and Process/Tooling: the affected Swift source parses directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  Settings Search caller is 775 lines and retains one responsibility. One raw
  literal timeout is removed, leaving 337 occurrences across 16 UI-test files
  and 4 in Settings Search. The full build retains 81 unrelated existing
  warning lines; every execution log retains one destination warning. The
  regression log retains 51 known CFURL no-scheme and 5 CFPrefs shared-state
  diagnostics; each Pressure log retains 17 and 3 respectively. Targeted
  fatal-error, assertion, crash, watchdog-expiry, unexpected-failure, and
  final-test-failure scans are empty for every successful run. Host-context
  strict deep codesign verification accepts both the fixed App and signed
  Runner. Exact post-run `pgrep -x` readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 849 MB DT evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `e8a66231ddcc665a955c14dfddf7d8c61ae0eb10`
  (`test(sync): migrate SYNC-036DT hidden-App Search foreground readiness`).

### SYNC-036DU Closure Record

- Status: completed. The hidden-App Search path now observes one exact
  post-trigger diagnostics projection instead of treating a prior standalone
  existence wait as readiness. Parent SYNC-036 remains open for its remaining
  owner-specific slices.
- Design and Oracle: the caller resolves the stable diagnostics element without
  waiting and delegates to the shared trigger helper. That helper registers its
  generation-owned observer and performs an initial readback with acceptance
  disabled before typing `Mail`; after the trigger returns it enables
  acceptance and requests an immediate readback. Success requires one snapshot
  in which the exact element exists and atomically reports `scope=app`, the
  percent-decoded query `Mail`, and an empty result set. The exact Mail-row
  absence remains an independent final readback after the committed empty
  projection.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production Search publication, hidden-App defaults,
  stable application identity, and module dependencies. Accessibility exposes
  no diagnostics-publication callback, so the shared named serial 100ms XCUI
  cadence remains active only while unresolved. The helper invocation owns
  observation and schedule cancellation through `defer`; the test owns each App
  process. The Settings Search-owned eight-second committed-result watchdog
  solely terminates failure and reports the acceptance gate, generation,
  source, expected keys, and final raw/value evidence.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production state transitions, application orchestration,
  and module wiring are unchanged. The UI-target owner regressions are the
  narrowest deterministic layer for the observation contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed 11/11 in 15.589 seconds: the
  affected three-process UI path took 14.316 seconds and the policy plus nine
  owner contracts took 1.272 seconds; xcodebuild completed in 17.000 seconds.
  Coverage includes matching initial state, exact and prefix matching, percent
  decoding, pre-trigger rejection, immediate post-trigger acceptance, atomic
  multi-key projection, delayed readback, cancellation, watchdog final
  evidence, and 100 stale-generation iterations.
- Pressure validation: one canonical signed invocation repeated the complete
  affected UI path three times and passed 3/3 in 41.142 seconds at 13.874,
  13.640, and 13.627 seconds; xcodebuild completed in 42.189 seconds. Every
  iteration completed the Settings mutation, exact Switcher projection,
  third-process foreground and input readiness, trigger-ordered atomic empty
  diagnostics projection, and exact hidden-row absence.
- Result evidence: both canonical status ledgers record `stage=completed`, zero
  applicable child and final exit codes, signing success, and present result
  bundles. Their xcresults record `status=succeeded`, zero error, warning, and
  test-failure counts, and respectively 11 tests plus one logical test repeated
  three times. The Pressure execution log independently records all three
  iterations and zero failures.
- Diagnostics and Process/Tooling: the affected Swift source parses directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  Settings Search caller, shared diagnostics owner, and owner regressions are
  775, 424, and 456 lines; each retains one responsibility. Two raw literal
  timeouts are removed, leaving 335 occurrences across 16 UI-test files and 2
  in Settings Search. The full build retains 81 unrelated existing warning
  lines; regression and Pressure execution each retain one destination warning.
  Their logs retain respectively 51 and 17 known CFURL no-scheme diagnostics
  plus 23 and 9 CFPrefs shared-state diagnostics. Targeted fatal-error,
  assertion, crash, watchdog-expiry, unexpected-failure, and final-test-failure
  scans are empty. Host-context strict deep codesign verification accepts both
  the fixed App and signed Runner. Exact post-run `pgrep -x` readback finds no
  Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 723 MB DU evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `88b43daaa29450874192dfddaf8a5dbf3d7cbd10`
  (`test(sync): migrate SYNC-036DU hidden-App empty Search projection`).

### SYNC-036DV Closure Record

- Status: completed. The current-App activation-policy path now resolves one
  exact post-navigation Settings projection instead of composing two
  independently timed element reads. Parent SYNC-036 remains open for its
  remaining owner-specific slices.
- Design and Oracle: the helper resolves the stable type-agnostic toggle
  identifier, exact localized hidden-count query, and Settings content before
  navigation. It installs its generation-owned observer with acceptance
  disabled, establishes the initial readback, invokes exact Settings
  navigation, enables acceptance only after the trigger returns, and requests
  an immediate readback. Success requires one foreground snapshot containing
  Settings content, the exact toggle reading off, and the exact
  `已隐藏 1 个应用` element. Later manager, Hidden-filter, and detail projections
  retain their independent Oracles.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production activation policy, configuration,
  window identity, Settings navigation, Accessibility identifiers, and module
  dependencies. Accessibility exposes no AppKit page-publication callback, so
  the shared named serial 100ms XCUI cadence remains active only while the
  projection is unresolved. The helper owns observation and schedule
  cancellation through `defer`; the test owns its App process. The owner-named
  twelve-second watchdog preserves the prior cumulative opportunity and solely
  terminates failure with acceptance, generation, source, exact identifier,
  toggle value, and final element evidence.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production state transitions, application orchestration,
  persistence, and module wiring are unchanged. The UI-target owner
  regressions are the narrowest deterministic layer for this observation
  contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. Canonical attempt 002 passed the policy, exact snapshot,
  initial, trigger-gate, delayed/cancel, stale-generation Pressure, watchdog-
  diagnostics, and affected visible path 8/8 in 9.197 seconds. The visible path
  took 8.238 seconds and deterministic owner coverage took 0.958 seconds;
  xcodebuild completed in 10.907 seconds.
- Pressure validation: one canonical signed invocation repeated the complete
  affected UI path three times and passed 3/3 in 20.583 seconds at 7.292,
  6.631, and 6.658 seconds; xcodebuild completed in 21.360 seconds. Every
  iteration accepted the same atomic foreground Settings, toggle-off, and
  exact hidden-count projection before continuing to the independent manager,
  Hidden-filter, and detail readbacks.
- Result evidence: both successful canonical status ledgers record
  `stage=completed`, zero applicable child and final exit codes, signing
  success, and present result bundles. Their xcresults record
  `status=succeeded`, empty issue summaries, and respectively eight tests plus
  one logical test with three successful repetitions. The execution logs
  independently record 8/8 and 3/3 with zero failures. Attempt 001 stopped at
  build-for-testing before any test ran because the new helper initially named
  a nonexistent `settingsContent` constant; it recorded exit 65 and no result
  bundle. The helper now uses the repository's stable `settingsTabContent`
  identifier, and direct Swift parsing plus successful attempt 002 close that
  compiler evidence.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The Settings Search caller, new owner, and owner regressions are 775,
  224, and 249 lines; each retains one responsibility. Two raw literal
  timeouts are removed, leaving 333 occurrences across 15 UI-test files and
  zero in Settings Search. The full build retains 62 unrelated existing
  warning lines; regression and Pressure execution each retain one destination
  warning. Their logs retain respectively 51 and 17 known CFURL no-scheme
  diagnostics plus 17 and 9 CFPrefs shared-state diagnostics. Targeted
  fatal-error, assertion, crash, watchdog-expiry, unexpected-failure, build-
  error, and final-test-failure scans are empty for the successful attempt.
  Host-context strict deep codesign verification accepts both the fixed App and
  signed Runner with the same Apple Development authority and TeamIdentifier.
  Exact post-run `pgrep -x` readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 728 MB DV evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `53a0105d74cea7af4dfe319401ad17f6f7bf7cc9`
  (`test(sync): migrate SYNC-036DV current-App Settings projection`).

### SYNC-036DW Closure Record

- Status: completed. The initial Settings Appearance controls now resolve from
  one exact post-navigation projection. Parent SYNC-036 remains open for its
  remaining owner-specific slices.
- Design and Oracle: the helper resolves both stable type-agnostic toggle
  identifiers, the exact localized current-App title and description, and
  Settings content before navigation. It registers a generation-owned deferred
  observer and establishes the initial readback with acceptance disabled. Once
  exact Settings navigation returns, it enables acceptance, requests an
  immediate readback, and activates the serial fallback only while unresolved.
  Success requires one foreground snapshot with Settings content, both exact
  toggles and readable values, the current-App toggle off, and both exact
  localized texts. The subsequent toggle mutations retain their independent
  exact control-state readbacks.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production Appearance state, defaults,
  configuration, Settings navigation, Accessibility identifiers, window
  identity, and module dependencies. Accessibility exposes no AppKit page-
  publication callback, so the shared named serial 100ms XCUI cadence remains
  the conditional fallback. The helper owns deferred registration, schedule,
  and observation cancellation through `defer`; the test owns its App process.
  The owner-named twenty-second watchdog preserves the prior cumulative
  opportunity and solely terminates failure with acceptance, generation,
  source, identifiers, toggle values, and final element evidence.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production state transitions, application orchestration,
  persistence, and module wiring are unchanged. The UI-target owner
  regressions are the narrowest deterministic layer for this observation
  contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed the policy, exact snapshot,
  initial, trigger-gate, delayed/cancel, stale-generation Pressure, watchdog-
  diagnostics, and affected visible path 8/8 in 6.555 seconds. The visible path
  took 5.616 seconds and deterministic owner coverage took 0.939 seconds;
  xcodebuild completed in 7.989 seconds.
- Pressure validation: one canonical signed invocation repeated the complete
  affected UI path three times and passed 3/3 in 16.235 seconds at 5.495,
  5.230, and 5.507 seconds; xcodebuild completed in 16.920 seconds. Every
  iteration accepted the same foreground Settings, exact two-toggle,
  current-App-off, title, and description projection before both toggle
  mutations completed.
- Result evidence: both canonical status ledgers record `stage=completed`, zero
  applicable child and final exit codes, signing success, and present result
  bundles. Their xcresults record `status=succeeded`, empty issue summaries,
  and respectively eight tests plus one logical test with three successful
  repetitions. The execution logs independently record 8/8 and 3/3 with zero
  failures.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The Settings caller, new owner, and owner regressions are 785, 326,
  and 280 lines; each retains one responsibility. Four raw literal timeouts are
  removed, leaving 329 occurrences across 15 UI-test files and 32 in Settings.
  The full build retains 80 unrelated existing warning lines; regression and
  Pressure execution each retain one destination warning. Their logs retain
  respectively 51 and 17 known CFURL no-scheme diagnostics plus 17 and 9
  CFPrefs shared-state diagnostics. Targeted fatal-error, assertion, crash,
  watchdog-expiry, unexpected-failure, build-error, and final-test-failure
  scans are empty. Host-context strict deep codesign verification accepts both
  the fixed App and signed Runner with the same Apple Development authority and
  TeamIdentifier. Exact post-run `pgrep -x` readback finds no Flow Tab, UI-test
  Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 724 MB DW evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `12742aed4189f94d131a2e658e4bb004d2f1b4b7`
  (`test(sync): migrate SYNC-036DW Settings Appearance projection`).

### SYNC-036DX Closure Record

- Status: completed. The post-selection English Settings content now resolves
  from one exact localization projection. Parent SYNC-036 remains open for its
  remaining owner-specific slices.
- Design and Oracle: the helper resolves Settings content, the exact English
  `Settings`, page subtitle, `Appearance`, and `Theme mode` elements, and the
  exact prior Chinese subtitle before the language trigger. It registers a
  generation-owned deferred observer and establishes the initial readback with
  acceptance disabled. The owned trigger selects English and reads back the
  exact language-control value; only then does the helper enable acceptance,
  request an immediate readback, and activate the serial fallback while
  unresolved. Success requires one foreground Settings snapshot containing
  every English element with the prior Chinese subtitle absent. The returned
  exact title and subtitle retain the independent geometry Oracle.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production localization rules, stored language,
  configuration, Settings navigation, Accessibility identifiers, window
  identity, and module dependencies. Accessibility exposes no AppKit
  localization-publication callback, so the shared named serial 100ms XCUI
  cadence remains the conditional fallback. The helper owns deferred
  registration, schedule, and observation cancellation through `defer`; the
  test owns its App process. The owner-named seventeen-second watchdog
  preserves the prior cumulative opportunity and solely terminates failure
  with acceptance, generation, source, every localized element, and final
  values.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production state transitions, application orchestration,
  persistence semantics, and module wiring are unchanged. The UI-target owner
  regressions are the narrowest deterministic layer for this observation
  contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed the policy, exact snapshot,
  initial, trigger-gate, delayed/cancel, stale-generation Pressure, watchdog-
  diagnostics, and affected complete theme/language path 8/8 in 15.524 seconds.
  The visible path took 14.583 seconds and deterministic owner coverage took
  0.940 seconds; xcodebuild completed in 17.032 seconds.
- Pressure validation: one canonical signed invocation repeated the complete
  affected UI path three times and passed 3/3 in 42.028 seconds at 14.229,
  14.094, and 13.703 seconds; xcodebuild completed in 42.710 seconds. Every
  iteration completed exact Settings navigation, both screenshot-luminance
  transitions, the English control-value readback, the atomic localized
  projection, and subtitle geometry evidence.
- Result evidence: both canonical status ledgers record `stage=completed`, zero
  applicable child and final exit codes, signing success, and present result
  bundles. Their xcresults record `status=succeeded`, empty issue summaries,
  and respectively eight tests plus one logical test with three successful
  repetitions. The execution logs independently record 8/8 and 3/3 with zero
  failures.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The Settings caller, new owner, and owner regressions are 787, 346,
  and 260 lines; each retains one responsibility. Four raw literal timeouts are
  removed, leaving 325 occurrences across 15 UI-test files and 28 in Settings.
  The full build retains 80 unrelated existing warning lines; regression and
  Pressure execution each retain one destination warning. Their logs retain
  respectively 51 and 17 known CFURL no-scheme diagnostics plus 17 and 9
  CFPrefs shared-state diagnostics. Targeted fatal-error, assertion, crash,
  watchdog-expiry, unexpected-failure, build-error, and final-test-failure
  scans are empty. Host-context strict deep codesign verification accepts both
  the fixed App and signed Runner with the same Apple Development authority and
  TeamIdentifier. Exact post-run `pgrep -x` readback finds no Flow Tab, UI-test
  Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 725 MB DX evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `7c1d3f07b9d2a995d777a96fa11c0a82a5b154fd`
  (`test(sync): migrate SYNC-036DX Settings English projection`).

### SYNC-036DY Closure Record

- Status: completed. The initial localized Settings Appearance page now
  resolves from one exact post-navigation projection. Parent SYNC-036 remains
  open for its remaining owner-specific slices.
- Design and Oracle: the helper resolves the exact Settings-content identifier
  and `外观` heading before navigation, registers a generation-owned deferred
  observer, and establishes an initial readback with acceptance disabled. It
  performs exact Settings navigation, enables acceptance only after the trigger
  returns, requests an immediate readback, and activates the serial fallback
  only while unresolved. Success requires one snapshot in which the App is
  foreground and Settings content and the exact localized heading coexist.
  Theme-control discovery and the later light/dark screenshot-luminance
  transitions remain independent contracts.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production localization, stored theme/language
  state, Settings orchestration, Accessibility identifiers, window identity,
  module dependencies, and path intent. Accessibility exposes no AppKit page-
  publication callback, so the shared named serial 100ms XCUI cadence remains
  the conditional fallback. The helper owns deferred registration, scheduling,
  and observation cancellation through `defer`; the test owns its App process.
  The owner-named five-second watchdog preserves the prior opportunity and
  solely terminates failure with acceptance, generation, source, exact
  identifiers, heading value, and final element evidence.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production state transitions, localization, application
  orchestration, persistence semantics, and module wiring are unchanged. The
  UI-target owner regressions are the narrowest deterministic layer for this
  observation contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed the policy, exact snapshot,
  initial, trigger-gate, delayed/cancel, stale-generation Pressure, watchdog-
  diagnostics, and affected complete theme/language path 8/8 in 14.032 seconds.
  The visible path took 13.125 seconds and deterministic owner coverage took
  0.907 seconds; xcodebuild completed in 15.503 seconds.
- Pressure validation: one canonical signed invocation repeated the complete
  affected UI path three times and passed 3/3 in 38.997 seconds at 13.373,
  12.962, and 12.662 seconds; xcodebuild completed in 39.698 seconds. Every
  iteration completed exact initial Settings Appearance projection, both
  screenshot-luminance transitions, English control-value readback, atomic
  English localization projection, and subtitle geometry evidence.
- Result evidence: both canonical status ledgers record `stage=completed`, zero
  applicable child and final exit codes, signing success, and present result
  bundles. Their xcresults record `status=succeeded`, empty issue summaries,
  and respectively eight tests plus one logical test with three successful
  repetitions. The execution logs independently record 8/8 and 3/3 with zero
  failures.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The Settings caller, new owner, and owner regressions are 796, 218,
  and 201 lines; each retains one responsibility. One raw literal timeout is
  removed, leaving 324 occurrences across 15 UI-test files and 27 in Settings.
  The full build retains 80 unrelated existing warning lines and no warning
  names a DY source; regression and Pressure execution each retain one
  destination warning. Their logs retain respectively 51 and 17 known CFURL
  no-scheme diagnostics plus 17 and 9 CFPrefs shared-state diagnostics.
  Targeted fatal-error, assertion, crash, watchdog-expiry, unexpected-failure,
  build-error, and final-test-failure scans are empty. Host-context strict deep
  codesign verification accepts both the fixed App and signed Runner with the
  same Apple Development authority and TeamIdentifier. Exact post-run
  `pgrep -x` readback finds no Flow Tab, UI-test Runner, xcodebuild, or Space
  fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 728 MB DY evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `183905aaca99c2114c8257af3b22d799cf1ea2fd`
  (`test(sync): migrate SYNC-036DY initial Appearance projection`).

### SYNC-036DZ Closure Record

- Status: completed. The two Settings theme-transition observations now expose
  their retained upper bound as an owner-named watchdog. Parent SYNC-036
  remains open for its remaining owner-specific slices.
- Design and Oracle: the pre-existing screenshot observer remains registered
  before each theme-selection trigger, gates acceptance until the trigger
  returns, performs an immediate readback, and starts its serial fallback only
  while unresolved. Exact Settings-content identity and sampled luminance are
  the sole success evidence: the light phase requires luminance above 0.45,
  while the dark phase requires a value below its threshold derived from the
  accepted light sample. The helper API now labels its terminal bound
  `watchdog`, and both phases consume the observation owner's named compatible
  five-second policy.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production theme behavior, stored configuration,
  Settings orchestration, Accessibility identity, window identity, module
  dependencies, and path intent. XCUI supplies no screenshot-change callback,
  so the existing named cancellable 100ms cadence remains a conditional
  readback fallback. The helper owns generation, scheduling, observation
  cancellation, and cleanup. Its five-second watchdog solely terminates
  failure and reports the final exact identifier, existence, luminance,
  generation, source, and waiter state.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production state transitions, application orchestration,
  persistence semantics, and module wiring are unchanged. The UI-target policy
  regression and existing observer regressions are the narrowest deterministic
  layer for this contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed the named policy, matching
  initial readback, trigger gate, delayed scheduling, stale-generation
  Pressure, watchdog diagnostics, and affected complete theme/language path
  7/7 in 14.251 seconds. The visible path took 13.447 seconds and deterministic
  owner coverage took approximately 0.804 seconds; xcodebuild completed in
  15.684 seconds.
- Pressure validation: one canonical signed invocation repeated the complete
  affected UI path three times and passed 3/3 in 38.641 seconds at 13.178,
  12.589, and 12.874 seconds; xcodebuild completed in 39.631 seconds. Every
  iteration accepted exact screenshot-luminance evidence for both transitions
  before completing the language and geometry Oracles.
- Result evidence: both canonical status ledgers record `stage=completed`, zero
  applicable child and final exit codes, signing success, and present result
  bundles. Their xcresults record `status=succeeded`, empty issue summaries,
  and respectively seven tests plus one logical test with three successful
  repetitions. The execution logs independently record 7/7 and 3/3 with zero
  failures.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly and `git diff --check` passes. The observation owner, owner
  regressions, and Settings caller are 200, 222, and 799 lines; each retains
  one responsibility. Two raw literal numeric timeouts are removed, leaving
  322 occurrences across 15 UI-test files and 25 in Settings. The full build
  retains 80 unrelated existing warning lines and no warning names an average-
  luminance or Settings source; regression and Pressure execution each retain
  one destination warning. Their logs retain respectively 51 and 17 known
  CFURL no-scheme diagnostics plus 15 and 9 CFPrefs shared-state diagnostics.
  Targeted fatal-error, assertion, crash, watchdog-expiry, unexpected-failure,
  build-error, and final-test-failure scans are empty. Host-context strict deep
  codesign verification accepts both the fixed App and signed Runner with the
  same Apple Development authority and TeamIdentifier. Exact post-run
  `pgrep -x` readback finds no Flow Tab, UI-test Runner, xcodebuild, or Space
  fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 726 MB DZ evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `b0244b9c8fe749041b1637991c6d4d4f129df4b4`
  (`test(sync): migrate SYNC-036DZ theme luminance watchdog`).

### SYNC-036EA Closure Record

- Status: completed. The first-launch Settings Window Behavior controls now
  resolve from one exact post-navigation projection. Parent SYNC-036 remains
  open for its remaining owner-specific slices.
- Design and Oracle: the helper resolves Settings content, the delay input,
  auto-restore switch, and hide-minimized switch through exact type-agnostic
  identifiers before navigation. It registers a generation-owned deferred
  observer and establishes an initial readback with acceptance disabled. It
  performs exact Settings navigation, enables acceptance only after the trigger
  returns, requests an immediate readback, and activates the serial fallback
  only while unresolved. Success requires one foreground snapshot in which all
  four elements coexist, the delay value is non-empty, and both switch states
  are readable. Delay commit, switch mutation, and relaunch persistence retain
  their independent value readbacks.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production Window Behavior state, persisted
  configuration, Settings orchestration, Accessibility identifiers, window
  identity, module dependencies, and path intent. Accessibility exposes no
  AppKit page-publication callback, so the shared named serial 100ms XCUI
  cadence remains the conditional fallback. The helper owns deferred
  registration, scheduling, observation cancellation, and cleanup; the test
  owns its App process. The owner-named fifteen-second watchdog preserves the
  prior cumulative opportunity and solely terminates failure with acceptance,
  generation, source, exact identifiers, values, switch states, and final
  element evidence.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because production state transitions, application orchestration,
  persistence semantics, and module wiring are unchanged. The UI-target owner
  regressions are the narrowest deterministic layer for this observation
  contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed the named policy, exact truth
  table, matching initial readback, trigger gate, delayed/cancelled readback,
  stale-generation Pressure, watchdog diagnostics, and affected complete
  persistence path 8/8 in 11.910 seconds. The visible path took 10.991 seconds
  and deterministic owner coverage took approximately 0.918 seconds;
  xcodebuild completed in 13.411 seconds.
- Pressure validation: one canonical signed invocation repeated the complete
  affected UI path three times and passed 3/3 in 30.252 seconds at 9.871,
  10.130, and 10.250 seconds; xcodebuild completed in 31.290 seconds. Every
  iteration accepted the same exact controls projection before delay commit,
  switch mutations, process relaunch, and persisted-value readback completed.
- Result evidence: both canonical status ledgers record `stage=completed`, zero
  applicable child and final exit codes, signing success, and present result
  bundles. Their xcresults record `status=succeeded`, empty issue summaries,
  and respectively eight tests plus one logical test with three successful
  repetitions. The execution logs independently record 8/8 and 3/3 with zero
  failures.
- Diagnostics and Process/Tooling: all four affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The observation owner, owner regressions, dedicated persistence path,
  and Settings aggregate are 304, 264, 90, and 737 lines; each retains one
  responsibility. Three raw literal numeric timeouts are removed, leaving 319
  occurrences across 15 UI-test files and 22 across the affected Settings
  sources. The full build retains 80 unrelated existing warning lines and no
  warning names an EA source; regression and Pressure execution each retain one
  destination warning. Their logs retain respectively 51 and 17 known CFURL
  no-scheme diagnostics plus 17 and 9 CFPrefs shared-state diagnostics.
  Targeted fatal-error, assertion, crash, watchdog-expiry, unexpected-failure,
  build-error, and final-test-failure scans are empty. Host-context strict deep
  codesign verification accepts both the fixed App and signed Runner with the
  same Apple Development authority and TeamIdentifier. Exact post-run
  `pgrep -x` readback finds no Flow Tab, UI-test Runner, xcodebuild, or Space
  fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 727 MB EA evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `f7874cbe1a8f8c2ca894f24ca9793c6c6db329ef`
  (`test(sync): migrate SYNC-036EA Window Behavior projection`).

### SYNC-036EB Closure Record

- Status: completed. The default initial Switcher presentation now resolves
  from exact cross-process runtime and presentation evidence. Parent SYNC-036
  remains open for its remaining owner-specific slices.
- Design and Oracle: the test creates its unique notification/readback route,
  removes prior readback state, and starts the resolution owner before launching
  the App. TestingSupport has already registered its runtime-projection observer
  before requesting readiness. It atomically writes one terminal resolution and
  posts the route's distributed notification. Success requires valid schema,
  observation generation, and source; one complete global candidate containing
  exact Mail and Minimized Notes item IDs; a successful presentation whose
  session IDs exactly equal that candidate; compatible same-or-later
  post-presentation generation, process, mode, and item readback; a presented
  panel; App-cycle mode; and enabled, inactive, non-pending Search state. No
  elapsed duration or Accessibility-row opportunity can establish success.
- Boundary, lifecycle, and retained time policy: the change stays within the UI-
  test target and reuses the typed TestingSupport transport, preserving
  production Window Behavior configuration, Switcher orchestration, exact item
  and window identity, module dependencies, and path intent. The distributed
  notification is the primary wakeup. A named cancellable 100ms file cadence
  remains solely for a missed cross-process notification and begins with the
  shared immediate readback. The owner-named eight-second watchdog only
  terminates failure and reports the last path, file/decode, generation,
  projection, presentation, session, panel, and Search evidence. Test teardown
  cancels the owner, removes the readback, and terminates the exact App through
  `defer`, including watchdog early return. The independent configuration path
  resets defaults in its own first launch.
- Contract split evidence: a canonical five-test invocation passed all three
  deterministic owner tests and the new default initial presentation in 2.434
  seconds. Its later configuration-to-filtered-projection test reached Settings
  mutation and the filtered launch, then failed after 198.668 seconds at its
  existing Mail Accessibility query with `Timed out while evaluating UI query`.
  That query and the following filtered negative projection are separate XCUI
  event sources and lifecycle owners retained for later slices; they do not
  participate in the EB completion Oracle.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  applicable because the typed TestingSupport transport, production state
  transitions, persisted configuration, Switcher presentation orchestration,
  and module wiring are unchanged. The UI-target owner regressions are the
  narrowest deterministic layer for this reused observation contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. After `Xcode Helper` Accessibility trust independently read back
  as `1`, the canonical EB-only regression passed initial-state, later exact
  evidence, cancellation/watchdog diagnostics, and the affected visible path
  4/4 in 62.287 seconds. The visible path took 61.862 seconds while the three
  deterministic owner tests took approximately 0.425 seconds; xcodebuild
  completed in 63.114 seconds.
- Pressure validation: one canonical signed invocation repeated the independent
  visible path three times and passed 3/3 in 125.766 seconds at 62.049, 1.831,
  and 61.886 seconds; xcodebuild completed in 126.461 seconds. App event-loop
  availability changed completion latency by approximately 34 times, while
  every iteration accepted the same exact candidate, generation,
  post-presentation, session, panel, and Search evidence.
- Result evidence: both canonical success status ledgers record
  `stage=completed`, zero applicable child and final exit codes, signing success,
  and present result bundles. Their xcresults record `status=succeeded`, empty
  issue summaries, and respectively four tests plus one logical test with three
  successful repetitions. The execution logs independently record 4/4 and 3/3
  with zero failures. The broader boundary invocation records five tests, one
  later-contract failure, and the exact timed-out XCUI query above.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. The Settings UI source remains a single 796-line
  responsibility. Three raw literal numeric timeouts are removed, leaving 316
  occurrences across 15 UI-test files and 19 across the Settings sources. The
  full build retains 80 unrelated existing warning lines and no warning names
  the affected Settings or initial-presentation sources; regression and
  Pressure execution each retain one destination warning. Their logs each
  retain 17 known CFURL no-scheme diagnostics and nine CFPrefs shared-state
  diagnostics. Targeted fatal-error, assertion, crash, watchdog-expiry,
  unexpected-failure, build-error, and final-test-failure scans are empty.
  Host-context strict deep codesign verification accepts both the fixed App and
  signed Runner with the same Apple Development authority and TeamIdentifier.
  Exact post-run `pgrep -x` readback finds no FlowTab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 866 MB resume evidence root and
  compact authorization blocker bundle contain only rebuildable results,
  DerivedData, caches, logs, and now-resolved recovery evidence. They are removed
  after this durable record, followed by exact absence and process readback.
- Commit: `bbd35172291b08918aabae6c4321ae9b9f3d1e24`
  (`test(sync): migrate SYNC-036EB initial Switcher presentation`).

### SYNC-036EC Closure Record

- Status: completed. Hide-minimized Settings mutation now starts only after one
  exact accepted Window Behavior controls projection. Parent SYNC-036 remains
  open for its remaining owner-specific slices.
- Design and Oracle: the shared assertion resolves Settings content, the delay
  input, auto-restore switch, and hide-minimized switch through exact
  type-agnostic identifiers before navigation. It establishes the existing
  generation-owned observer's initial readback with acceptance disabled,
  triggers exact Settings navigation, enables acceptance after the trigger
  returns, requests an immediate readback, and schedules fallback readback only
  while unresolved. Success requires one foreground snapshot containing all
  four controls with readable values and switch states. The accepted exact
  hide-minimized control is then set to enabled, and its synchronous exact state
  readback independently confirms the mutation. A dedicated Settings-only UI
  test exercises this contract, while the combined behavior path consumes the
  same assertion before its separately owned filtered launch and projection.
- Boundary, lifecycle, and retained time policy: the change remains in the UI-
  test target and preserves production preferences, Settings orchestration,
  Accessibility and window identity, module dependencies, and path intent.
  Accessibility supplies no AppKit page-publication callback, so the existing
  named cancellable 100ms cadence remains a conditional readback fallback. The
  helper invocation owns observer generation, scheduling, cancellation, and
  cleanup. Each test invocation owns exact App termination through `defer`,
  including an observation failure return. The existing owner-named fifteen-
  second watchdog solely terminates failure and reports the complete final
  controls projection.
- Contract split evidence: the dedicated Settings path is the independent
  visible Oracle for EC. The combined caller compiles against and invokes the
  same shared assertion; its subsequent filtered App launch, positive Mail
  projection, and negative Minimized Notes projection retain distinct event
  sources and lifecycle ownership for later slices.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not relevant
  because production state transitions, preference persistence, application
  orchestration, and module wiring are unchanged. The UI-target owner
  regressions are the narrowest deterministic layer for the reused observation
  contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed named-policy, exact-truth-table,
  matching-initial-readback, post-trigger generation, later-evidence and
  cancellation, stale-generation Pressure, watchdog-diagnostics, and affected
  Settings UI coverage 8/8 in 5.542 seconds. The visible path took 4.633 seconds
  and the seven deterministic owner tests took approximately 0.909 seconds;
  xcodebuild's test operation completed in 7.200 seconds after a successful
  build-for-testing stage.
- Pressure validation: one canonical signed invocation repeated the dedicated
  visible path three times and passed 3/3 in 13.010 seconds at 4.728, 4.127, and
  4.155 seconds; xcodebuild completed in 14.109 seconds. Every iteration
  accepted one exact controls projection and the same enabled-state readback.
- Result evidence: both canonical status ledgers record `stage=completed`, zero
  applicable child and final exit codes, signing success, and present result
  bundles. Their xcresults record `status=succeeded`, empty issue summaries,
  and respectively eight tests plus one logical test with three successful
  repetitions. The execution logs independently record 8/8 and 3/3 with zero
  failures.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly and `git diff --check` passes. The Settings aggregate, observation
  owner, and dedicated persistence source are 800, 337, and 106 lines; each
  retains one responsibility. One raw literal numeric timeout is removed,
  leaving 315 occurrences across 15 UI-test files and 18 across the Settings
  sources. The full build retains 80 unrelated existing warning lines and no
  warning names an EC source; regression and Pressure execution each retain one
  destination warning. Their logs retain respectively 51 and 17 known CFURL
  no-scheme diagnostics plus 17 and 9 CFPrefs shared-state diagnostics.
  Targeted fatal-error, assertion, crash, watchdog-expiry, unexpected-failure,
  build-error, and final-test-failure scans are empty. Host-context strict deep
  codesign verification accepts both the fixed App and signed Runner with the
  same Apple Development authority and TeamIdentifier. Exact post-run
  `pgrep -x` readback finds no FlowTab, UI-test Runner, xcodebuild, or Space
  fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 726 MB EC evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `fe0a708a36d5ac7f9703b68fe54fb671022abb80`
  (`test(sync): migrate SYNC-036EC hide-minimized Settings readiness`).

### SYNC-036ED Closure Record

- Status: completed. The filtered initial Switcher now resolves Mail inclusion
  and Minimized Notes exclusion from one complete runtime and presentation
  projection. Parent SYNC-036 remains open for its remaining owner-specific
  slices.
- Design and Oracle: the initial-presentation expectation now carries explicit
  required and excluded item sets. Acceptance requires valid schema,
  observation generation, and source; a present and complete global candidate
  containing every required ID and no excluded ID; a successful presentation
  whose session IDs exactly equal that candidate; compatible same-or-later
  post-presentation generation, process, mode, and item readback; a presented
  App-cycle panel; and the configured Search state. The filtered caller requires
  exact Mail and excludes exact Minimized Notes. Candidate completeness makes
  the excluded-item readback authoritative, while session and post-presentation
  equality carry that exact set through the visible presentation.
- Boundary, lifecycle, and retained time policy: the change stays within the UI-
  test target and preserves production Window Behavior state, preference
  persistence, Switcher filtering and presentation orchestration, exact item
  and window identity, module dependencies, and path intent. The shared
  assertion prepares a unique route and starts its owner before App launch.
  The distributed notification remains the primary wakeup; a named cancellable
  100ms file cadence remains solely for a missed cross-process notification.
  The helper invocation owns owner cancellation, atomic-readback removal, and
  exact App termination. The combined test separately owns its Settings App
  through `defer`. The owner-named eight-second watchdog solely terminates
  failure and reports required/excluded IDs plus the final path, file/decode,
  generation, projection, session, panel, and Search evidence.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not relevant
  because the typed production transport, production state transitions,
  preference persistence, application orchestration, and module wiring are
  unchanged. The UI-target expectation and owner regressions are the narrowest
  deterministic layer for this contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The canonical regression passed excluded-item and incomplete-
  projection truth-table coverage, initial and later exact evidence,
  cancellation/watchdog diagnostics, the refactored baseline, and the complete
  affected filtered path 6/6 in 9.072 seconds. The filtered path took 6.014
  seconds, the baseline took 2.587 seconds, and the four deterministic tests
  took approximately 0.469 seconds; xcodebuild completed in 10.569 seconds
  after a successful build-for-testing stage.
- Pressure validation: one canonical signed invocation repeated the complete
  persisted-configuration and filtered-presentation path three times and passed
  3/3 in 232.310 seconds at 77.836, 77.483, and 76.990 seconds; xcodebuild
  completed in 233.032 seconds. Each iteration recorded the platform's App
  event-loop idle notification arriving late and continued after approximately
  one minute. Completion latency reached approximately 12.9 times the 6.014-
  second regression path, while every iteration accepted the same complete
  required/excluded candidate, session, post-presentation, panel, and Search
  evidence.
- Result evidence: both canonical status ledgers record `stage=completed`, zero
  applicable child and final exit codes, signing success, and present result
  bundles. Their xcresults record `status=succeeded`, empty issue summaries,
  and respectively six tests plus one logical test with three successful
  repetitions. The execution logs independently record 6/6 and 3/3 with zero
  failures.
- Diagnostics and Process/Tooling: all four affected Swift sources parse
  directly and `git diff --check` passes. The initial-presentation owner,
  regressions, Settings aggregate, and Settings Search source are 396, 309,
  754, and 776 lines; each retains one responsibility. Three raw literal
  numeric timeouts are removed, leaving 312 occurrences across 15 UI-test files
  and 15 across the Settings sources. The full build retains 80 unrelated
  existing warning lines and no warning names an ED source; regression and
  Pressure execution each retain one destination warning. Their logs retain
  respectively 51 and 17 known CFURL no-scheme diagnostics plus 14 and 9
  CFPrefs shared-state diagnostics. The Pressure log additionally records
  exactly three late App event-loop idle notifications and three matching
  monitor-recovery messages. Targeted fatal-error, assertion, crash,
  watchdog-expiry, unexpected-failure, build-error, and final-test-failure scans
  are empty. Host-context strict deep codesign verification accepts both the
  fixed App and signed Runner with the same Apple Development authority and
  TeamIdentifier. Exact post-run `pgrep -x` readback finds no FlowTab, UI-test
  Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 728 MB ED evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `17a97aeffcf740db3d395e69c7dc2bab0a0192fa`
  (`test(sync): migrate SYNC-036ED filtered Switcher projection`).

### SYNC-036EE Closure Record

- Status: completed. The Simplified Chinese denied-permission path now creates
  its exact Settings projection observer immediately after canonical App launch
  and before Settings navigation. Parent SYNC-036 remains open for its
  remaining owner-specific slices.
- Design and Oracle: the fixed-path launch helper launches and activates the
  App, uses its owner-named foreground-activation bound, and records the final
  exact application state. The caller then resolves both localized denied
  status queries and both exact permission-action identifiers and starts the
  shared condition owner before triggering Settings navigation. That owner
  performs an initial readback, retains preinstalled scheduled readback,
  requests an immediate post-trigger readback, and accepts only one atomic
  snapshot containing all four elements. Settings navigation independently
  requires the foreground Settings-only sidebar projection. The duplicate raw
  ten-second caller wait no longer participates in readiness or success.
- Boundary, lifecycle, and retained time policy: the change stays within the
  UI-test target and preserves production localization, permission state,
  Settings orchestration, Accessibility and window identity, module
  dependencies, configuration compatibility, and path intent. XCUI exposes no
  Settings accessibility-projection callback, so the existing named
  cancellable 100ms scheduler remains a conditional readback fallback after an
  immediate check. The helper invocation owns observation cancellation through
  `defer`; XCTest teardown owns the App process. The owner-named five-second
  watchdog solely terminates unresolved projection failure and reports final
  generation, source, waiter, and four-element evidence. The launch helper's
  named activation bound remains a terminal foreground failure bound with exact
  final-state trace.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  relevant because no deterministic production rule, permission
  interpretation, launch orchestration, persistence behavior, or module wiring
  changed. Deterministic UI-target owner tests are the lowest responsible layer
  for this test synchronization contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The direct XCTest/Xcode Helper Accessibility request completed
  successfully. The canonical affected regression passed the named watchdog,
  complete 16-state predicate, adjacent permission-action path, and Simplified
  Chinese visible path 4/4 in 7.779 seconds. The affected path took 3.657
  seconds, its adjacent path took 3.839 seconds, the two deterministic tests
  took approximately 0.283 seconds, and xcodebuild completed in 9.214 seconds
  after a successful build-for-testing stage. A second canonical invocation
  passed initial/preinstalled event, reentrant and replaced-event ordering,
  cancellation, named scheduled readback, 2,000-owner lifecycle Pressure, and
  watchdog final-evidence coverage 6/6 in 0.903 seconds; xcodebuild completed in
  1.602 seconds.
- Pressure validation: one canonical signed invocation repeated the complete
  affected Simplified Chinese UI path three times and passed 3/3 in 10.123
  seconds at 3.271, 3.472, and 3.380 seconds; xcodebuild completed in 10.790
  seconds. Scheduling and Accessibility latency varied while every iteration
  accepted the same foreground Settings projection and exact four-element
  denied-permission snapshot.
- Result evidence: all three canonical status ledgers record
  `stage=completed`, zero applicable child and final exit codes, signing
  success, and present result bundles. Their xcresults record
  `status=succeeded`, empty issue summaries, and respectively four owner/UI
  tests, six deterministic owner tests, and one logical UI test with three
  successful repetitions. The execution logs independently record 4/4, 6/6,
  and 3/3 with zero failures.
- Diagnostics and Process/Tooling: the affected Settings Swift source parses
  directly and `git diff --check` passes. The Settings aggregate, permission
  observation source, and generic condition-owner regressions are 753, 651,
  and 395 lines; each retains one responsibility. One raw literal numeric
  timeout is removed, leaving 311 occurrences across `FlowTabTests` and
  `FlowTabUITests`, including 14 in the affected Settings source. The full build
  retains 80 unrelated existing warning lines and no warning names an EE
  source. Regression build/execution retain 85 known CFURL no-scheme
  diagnostics and 10 CFPrefs shared-state diagnostics; owner regression retains
  17 and 12; Pressure retains 17 and 9. Targeted fatal-error, assertion, crash,
  watchdog-expiry, unexpected-failure, build-error, and final-test-failure
  scans are empty. Host-context strict deep codesign verification accepts both
  the fixed App and signed Runner with the same Apple Development team.
  Exact post-run process readback finds no Flow Tab, UI-test Runner, xcodebuild,
  or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 727 MB EE evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `1ad52853b411f4f614264982e2c559107e99ea33`
  (`test(sync): migrate SYNC-036EE localized permission readiness`).

### SYNC-036EF Closure Record

- Status: completed. Every representative main-hotkey iteration now proves
  exact launch registration and foreground input readiness before sending the
  real key. Parent SYNC-036 remains open for its remaining owner-specific
  slices.
- Design and Oracle: the test creates its runtime-log baseline before launching
  each fresh App, which starts the filesystem event source and captures file
  anchors plus event generation before any registration record can arrive. It
  then requires the two exact main-monitor Carbon success records for signature
  `1179926850` and IDs 1 and 2, the persisted `register main=<shortcut>` record,
  and launch registration generation 1. Immediately before input it performs
  an exact `.runningForeground` XCUIApplication readback under the shared named
  foreground-activation watchdog. Only then does it activate the App, type the
  configured Option+Space, Control+grave, or Command+B input, and accept all
  three post-baseline `hotkeyPressed`, global-presentation recovery, and
  `HotKey Forward` records. Elapsed time cannot satisfy registration,
  foreground readiness, receipt, routing, or presentation.
- Diagnosis and retained barrier: removing the pre-input foreground readback
  failed two consecutive signed runs at the Control+grave iteration. Both App
  logs recorded successful Carbon registrations before XCTest synthesized the
  key, while the runtime-log watchdog reported all three receipt/presentation
  markers missing. The first run executed the six observation-owner companions
  successfully before the affected failure; the isolated reproduction failed
  identically. Restoring the state readback on the same topology passed the
  complete three-case path in 73.880 seconds. This A/B evidence classifies the
  call as an XCTest automation-state barrier, with `.runningForeground` as its
  independent Oracle and the duration solely as a terminal failure bound.
- Boundary, lifecycle, and retained time policy: the implementation remains in
  the UI-test target and preserves production hotkey identities, registration,
  routing, presentation orchestration, persistence, window semantics, module
  dependencies, configuration compatibility, and path intent. Baseline scope
  owns the vnode source and file descriptors; each runtime-log wait owns and
  cancels its notification registration plus named serial 200ms fallback. The
  named eight-second runtime-log watchdog reports generations, missing markers,
  character count, and bounded final tail. The shared ten-second foreground
  policy reports final XCUIApplication state and only bounds an unmet state
  condition. Each matrix iteration owns exact App termination.
- Unit and Behavior validation: FlowTabCore, Unit, and Behavior are not
  relevant because no deterministic production rule, production scheduler,
  registration implementation, persistence behavior, or module wiring changed.
  The deterministic UI-target runtime-log owner tests are the lowest
  responsible layer for this synchronization contract.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`; the direct XCTest/Xcode Helper Accessibility request completed
  successfully. The final canonical regression passed the runtime-log policy,
  append, reset, immediate/event resolution, lifecycle Pressure, live mock
  trigger, and affected signed matrix 7/7 in 75.590 seconds. Individual owner
  paths took 0.134, 0.139, 0.092, 0.140, 0.096, and 1.913 seconds; the complete
  matrix took 73.076 seconds. Xcodebuild completed in 77.040 seconds after a
  successful build-for-testing stage.
- Pressure validation: the same final signed build repeated the complete
  three-configuration matrix three times and passed 3/3 in 217.079 seconds at
  73.112, 70.791, and 73.175 seconds; xcodebuild completed in 218.115 seconds.
  Scheduling and Accessibility latency varied while every iteration accepted
  the same registration, foreground-state, hotkey-receipt, route, and
  presentation evidence.
- Result evidence: both final canonical status ledgers record
  `stage=completed`, zero applicable child and final exit codes, successful
  Runner signing, and present result bundles. The regression xcresult records
  `status=succeeded`, empty issue summaries, and seven tests. The Pressure
  xcresult records `status=succeeded`, empty issue summaries, one logical UI
  test and three successful repetitions. Execution logs independently record
  7/7 and 3/3 with zero failures.
- Diagnostics and Process/Tooling: the affected Settings Swift source parses
  directly and `git diff --check` passes. The Settings source, runtime-log
  owner, file-event source, and owner-regression source are 773, 377, 382, and
  731 lines; each retains one responsibility. One raw literal numeric timeout
  is classified under the shared policy, leaving 310 occurrences across
  `FlowTabTests` and `FlowTabUITests`, including 13 in the affected Settings
  source. The full build retains 80 unrelated existing warning lines and no
  warning names an EF source. Regression execution retains 54 known CFURL
  no-scheme diagnostics and 16 CFPrefs shared-state diagnostics; Pressure
  retains 18 and 9. The three regression and nine Pressure option-hittability
  fallback messages are the established SYNC-034R conditional scroll/readback
  path and each resolves to the exact option. Final fatal-error, assertion,
  crash, unexpected-failure, build-error, product-watchdog, and test-failure
  scans are empty. Host-context strict deep codesign verification accepts both
  the fixed App and signed Runner with the same Apple Development authority and
  TeamIdentifier. Exact post-run process readback finds no Flow Tab, UI-test
  Runner, xcodebuild, or Space fixture process.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 1.9 GB EF evidence root contains
  only rebuildable results, DerivedData, caches, and logs. It is removed after
  this durable record, followed by exact absence and process readback.
- Commit: `38bde325308135df7a54db8b2faa48c9820ff540`
  (`test(sync): migrate SYNC-036EF main hotkey readiness`).

### SYNC-036EG Closure Record

- Status: completed. Every quit-hotkey matrix iteration now receives one typed
  schema-version-3 initial-presentation resolution containing its exact
  selected App and settled post-presentation input-readiness evidence before
  XCTest sends the real quit key. Parent SYNC-036 remains open for its
  remaining owner-specific slices.
- Design and Oracle: the App first accepts one complete global projection
  candidate containing Browser, presents a session whose item IDs exactly
  equal that candidate, and captures its process, source generation,
  presentation generation, session IDs, and selected App ID. It installs the
  input-readiness observer before requesting maintenance. Resolution then
  requires a later projection generation with the same mode and process,
  exact candidate/session items and selected App, the same presentation
  generation, a presented user-visible panel, and settled panel diagnostic,
  initial-visibility, panel-recovery, active-space-transition,
  activation-suppression, and terminate-protection owners. Initial candidate
  completeness makes this exact target authoritative while an unrelated
  later runtime repair may remain pending. Only then does TestingSupport
  atomically write the readback and post its distributed notification.
- Cross-process acceptance and quit identity: the UI owner accepts schema 3,
  compatible non-equal input generations, complete required/excluded items,
  exact candidate/session/post-presentation/input-readiness identities,
  `appCycle` mode, panel presentation, and the configured inactive Search
  state. The launch helper returns the live App together with that typed
  resolution. The caller uses the same selected App ID for the post-baseline
  quit receipt and selected-row disappearance Oracles. Native XCTest
  `typeKey` supplies Option+Z and the persisted fallback Option+W inputs.
- Lifecycle and retained time policy: the App-side owner performs an immediate
  readback, observes projection and presentation notifications before the
  maintenance request, and uses a named cancellable 100ms conditional cadence
  only while unresolved. Its three-second watchdog performs a final readback
  and reports every unmet condition. The cross-process owner starts before
  App construction and launch; distributed notification is its primary
  wakeup, with a named cancellable 100ms file cadence for missed delivery and
  an eight-second terminal watchdog carrying the final path, decode,
  generation, projection, selected identity, panel, mode, and Search evidence.
  Resolution, replacement, cancellation, and watchdog paths remove all
  notification tokens, retry/watchdog work, and readback files. Each matrix
  iteration owns exact App termination.
- Boundary diagnosis: the earlier XCUI projection attempt reproducibly spent
  about 31 seconds inside one synchronous diagnostics-value query, beyond its
  outer watchdog's ability to report a timely failure. An early App-side
  version then exposed a later projection marked incomplete only for an
  unrelated space-topology repair; the dedicated regression now proves that a
  later exact target remains acceptable after the complete initial candidate.
  A combined shared-caller run later missed the App readback. Exact sampling
  of FlowTab PID 51021 showed an idle AppKit main thread and twelve Swift
  concurrency tasks in `LiveSwitcherModel.scheduleRuntimePreviewCaptures`,
  `RuntimeWindowPreviewProvider.captureWindowPreviewOutcomes`, and
  `_dispatch_group_wait_slow`. These callers use synthetic mock window
  identities, so their launch arguments now select mock window previews. Real
  ScreenCaptureKit preview behavior stays with its existing preview owner.
- Unit and Behavior validation: FlowTabCore Unit is not relevant because no
  core rule, scheduler, path resolution, user configuration, or module wiring
  changed. The final canonical FlowTabTests invocation passed App publication
  plus initial, later exact, scheduled readback, unrelated-repair, stale event,
  cancellation, final-watchdog-readback, and 2,000-generation replacement
  Pressure coverage 7/7 in 0.372 seconds; xcodebuild completed in 1.964
  seconds. The 2,000-generation case proves replaced work cannot resolve the
  current owner.
- UI validation: the canonical installer rebuilt the final source layout and
  installed `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The final canonical signed regression passed six schema/owner
  tests plus disabled Search, default Window Behavior, hide-minimized Window
  Behavior, and the complete two-case quit-hotkey matrix 10/10 in 61.491
  seconds; the visible paths took 5.620, 1.848, 5.881, and 47.584 seconds,
  and xcodebuild completed in 62.978 seconds.
- Pressure validation: the final signed build repeated the complete exact
  quit-hotkey matrix three times and passed 3/3 in 141.919 seconds at 47.100,
  48.238, and 46.581 seconds; xcodebuild completed in 142.660 seconds. Every
  iteration accepted the same selected-App/readback contract, delivered both
  native quit inputs, observed exact quit receipt, and observed the selected
  row disappear.
- Result, signing, and process evidence: all three final canonical status
  ledgers record `stage=completed`, zero applicable child and final exit
  codes, and present result bundles. Their xcresults record
  `status=succeeded`, empty failure summaries, and respectively seven tests,
  ten tests, and one logical UI test with three successful repetitions. The
  installer and signed runner both satisfy strict deep codesign verification
  under the same Apple Development team. Exact post-run `pgrep -x` readback
  finds no FlowTab, UI-test Runner, xcodebuild, or Space fixture process.
- Diagnostics and Process/Tooling: the project file passes `plutil -lint`, all
  affected Swift sources parse directly, and `git diff --check` passes. The
  TestingSupport observation/transport and schema/readback responsibilities
  are split into 625- and 225-line files; every touched Swift source remains
  at or below the 800-line guardrail. Four raw numeric timeout calls are
  removed, leaving 306 occurrences across 15 test files and nine in the
  affected Settings source. The final incremental Behavior build has two
  unrelated warning lines and none names an EG source; the clean UI build has
  63 unrelated warning lines and none names an EG source. Final targeted
  fatal-error, assertion, crash, input-resolution-watchdog,
  unexpected-failure, build-error, and test-failure scans are empty. The one
  regression and three Pressure hittability-watchdog diagnostics are the
  established SYNC-034R conditional scroll/readback route and each resolves
  to the exact option.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 5.9 GB EG evidence root contains
  only rebuildable result bundles, DerivedData, caches, logs, and the sampled
  diagnostic already summarized above. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject: `test(sync): migrate SYNC-036EG quit target readiness`.

### SYNC-036EH Closure Record

- Status: completed. Each persisted in-app hotkey case now establishes its
  runtime-log event source before launch, proves exact Carbon registration,
  performs a named final foreground readback immediately before native key
  input, and accepts completion only from exact focused-session and
  presentation evidence. Parent SYNC-036 remains open for its remaining
  owner-specific slices.
- Design and Oracle: the pre-launch baseline accepts the in-app input-source
  registration, signature `1179932494` IDs 101 and 102, the exact persisted
  Option+B or Control+B shortcut, and registration generation 1. After App
  activation, `.runningForeground` must be the final XCUIApplication state.
  A fresh input baseline then requires the accepted in-app route, a ready
  focused-current-App session for bundle
  `io.github.potato-dumplings.flowtab` with exactly two windows, initial
  advance, presentation recovery, a presented panel, and the final
  `InApp Window Forward` receipt. Elapsed time cannot satisfy either barrier.
- Lifecycle and retained time policy: constructing each log baseline starts
  its filesystem source before the corresponding launch or key input and
  records initial file anchors plus event generation. Immediate readback
  closes the already-satisfied case. Filesystem events are the primary wakeup;
  the shared named cancellable 200ms conditional cadence covers missed events
  while unresolved, and its eight-second watchdog solely terminates failure
  with missing markers, generations, source, waiter result, and the final
  bounded log tail. Each baseline explicitly cancels on success and through
  `defer`; each matrix iteration owns exact App termination through `defer`.
- Baseline and authorization evidence: the signed baseline passed 1/1 in
  49.634 seconds and showed the duplicated adjacent foreground reads before
  both keys. The first two final Runner attempts reached XCTest automation
  setup and ended at its authorization deadline before executing a test body.
  The host system log then recorded `Finished enabling Automation Mode` about
  eight seconds after the second Runner deadline. Reissuing the same exact
  XCTest command through the Codex authorization prompt started all selected
  test bodies and completed successfully.
- Unit and Behavior validation: not relevant for this test-owner migration;
  production registration, routing, window-session construction, persistence,
  scheduling, module wiring, and path resolution are unchanged. Existing
  runtime-log tests remain the lowest responsible owner coverage for policy,
  initial readback, append/event delivery, file replacement, ordering,
  cancellation, final watchdog readback, and lifecycle Pressure.
- UI and owner validation: the canonical installer supplied
  `/Users/lk/Applications/Flow Tab UITest.app`, signed by
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` with TeamIdentifier
  `96PUA726W9`. The final canonical regression passed runtime-log policy,
  append, file replacement, event resolution, lifecycle Pressure, live mock
  trigger delivery, watchdog policy, and the affected signed two-case matrix
  8/8 in 51.502 seconds. The matrix took 48.499 seconds; xcodebuild completed
  in 52.595 seconds.
- Pressure validation: the same signed build repeated the complete two-case
  matrix three times and passed 3/3 in 145.321 seconds at 47.900, 49.457, and
  47.965 seconds; xcodebuild completed in 146.378 seconds. All six native key
  paths accepted the same registration, foreground, focused-session, and
  presentation evidence while scheduling and Accessibility latency varied.
- Result, signing, and process evidence: both final canonical status ledgers
  record `stage=completed`, zero applicable child and final exit codes,
  successful Runner signing, and present result bundles. The regression
  xcresult records `status=succeeded`, empty issue summaries, and eight tests;
  the Pressure xcresult records `status=succeeded`, empty issue summaries,
  one logical test and three successful repetitions. Host-context strict deep
  codesign verification accepts both the fixed App and signed Runner. Exact
  post-run `pgrep -x` readback finds no Flow Tab, UI-test Runner, xcodebuild,
  or Space fixture process.
- Diagnostics and Process/Tooling: the project file passes `plutil -lint`, both
  affected Swift sources parse directly, and `git diff --check` passes. The
  original Settings owner is reduced from 793 to 711 lines, while the focused
  in-app hotkey owner is 126 lines. One raw numeric timeout is removed, leaving
  305 occurrences across 15 test files and eight in the affected Settings
  source. Final fatal-error, assertion, crash, unexpected-failure,
  build-error, product-watchdog, and test-failure scans are empty. The two
  regression and six Pressure hittability-watchdog diagnostics are the
  established SYNC-034R conditional scroll/readback route and every occurrence
  resolves to the exact option.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 235 MB EH evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject: `test(sync): migrate SYNC-036EH in-app hotkey readiness`.

### SYNC-036EI Closure Record

- Status: completed. The post-input quit owner now exposes its retained
  cross-process completion bound as
  `FlowTabUITestSettingsQuitHotkeyWatchdogPolicy.runtimeCompletion`, while
  completion continues to depend exclusively on exact runtime evidence. The
  independent selected-row disappearance readback remains for SYNC-036EJ.
- Oracle and classification: each runtime-log filesystem source and byte/file
  anchor exists before native Option+Z or fallback Option+W input. Success
  requires the same selected App ID in the mock termination receipt, the sent
  quit request, the workspace termination record, and the post-refresh record.
  The retained ten seconds is a watchdog that only terminates failure; it
  cannot produce success. The policy test preserves its compatible finite,
  positive value.
- Lifecycle and diagnostics: each completion baseline starts its file event
  source before input, performs the shared immediate readback after the owner
  attaches, and explicitly cancels after success plus at loop-scope exit.
  Filesystem events remain the primary wakeup. The shared named cancellable
  200ms readback cadence covers missed file events only while unresolved, and
  the terminal diagnostic records missing markers, baseline/current event
  generations, source, waiter result, and the final bounded log tail. Each
  matrix iteration owns App termination through `defer`.
- Baseline evidence: the unchanged signed two-case matrix passed 1/1 in
  47.060 seconds; xcodebuild completed in 48.158 seconds. Both native input
  paths reached all four runtime markers before the subsequent selected-row
  nonexistence predicate readback, confirming that the raw ten-second value was
  a terminal bound around an independent Oracle.
- Unit and Behavior validation: FlowTabCore Unit and app Behavior are not
  relevant because production termination, projection, persistence,
  scheduling, module wiring, and path resolution are unchanged. Six existing
  UI-target owner tests cover runtime-log policy compatibility, immediate and
  event resolution, append, file replacement, cancellation, final watchdog
  readback, lifecycle Pressure, and live mock delivery; the new policy test
  covers the owner-specific retained bound.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The final canonical regression passed six runtime-log owner
  tests, the new policy test, and the complete signed quit matrix 8/8 in
  49.471 seconds. The matrix took 46.651 seconds, the policy test took 0.096
  seconds, and xcodebuild completed in 50.545 seconds after successful
  build-for-testing.
- Pressure validation: the same signed build repeated the complete two-case
  matrix three times and passed 3/3 in 136.078 seconds at 46.029, 44.662, and
  45.387 seconds; xcodebuild completed in 136.793 seconds. Every iteration
  accepted the same selected identity and four-marker chain while scheduling
  and Accessibility completion time varied.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable child and final exit codes, successful
  Runner signing, and present result bundles. The regression xcresult records
  `status=succeeded`, empty issue summaries, and eight tests; the Pressure
  xcresult records `status=succeeded`, empty issue summaries, one logical test
  and three successful repetitions. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It remains below the file-size guardrail at
  736 lines. One raw numeric timeout is removed, leaving 304 occurrences
  across 15 test files and seven in the affected Settings source. The final
  build retains two unrelated existing warnings in Space-fixture UI sources.
  Final fatal-error, assertion, crash, unexpected-failure, build-error,
  product-watchdog, and test-failure scans are empty. The one baseline, one
  regression, and three Pressure hittability-watchdog diagnostics are the
  established SYNC-034R conditional scroll/readback route and each resolves to
  the exact option.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 6.3 MB EI evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject: `test(sync): migrate SYNC-036EI quit completion watchdog`.

### SYNC-036EJ Closure Record

- Status: completed. Each quit-hotkey matrix iteration now owns an exact
  selected-row disappearance observer before native input and accepts only
  post-trigger Accessibility evidence. Parent SYNC-036 remains open for its
  remaining owner-specific slices.
- Oracle and design: the caller preserves the selected App ID from the typed
  initial-presentation resolution and constructs its exact dynamic row query
  before Option+Z or fallback Option+W. The generation owner registers before
  input and performs an initial phase-gated readback that cannot turn an
  already-absent pre-trigger row into success. Immediately after XCTest sends
  the key, the trigger transition performs another readback. Resolution
  requires that exact query to report `exists == false` in the post-trigger
  phase; elapsed time cannot satisfy the contract.
- Lifecycle and retained time policy: when the trigger readback remains
  unresolved, the owner activates the shared named cancellable 100ms XCUI
  readback cadence because Accessibility exposes no row-disappearance event.
  The retained six seconds is named
  `FlowTabUITestSettingsQuitHotkeyWatchdogPolicy.selectedRowDisappearance` and
  only terminates failure. Its final non-resolving readback reports selected
  App ID, exact row identifier, phase, generation, source, waiter result, and
  final existence evidence. Resolution stops the cadence, replacement rejects
  stale generations, and explicit success plus loop-scope `defer` cancellation
  cover every exit while each matrix iteration owns App termination.
- Baseline evidence: the unchanged signed two-case matrix passed 1/1 in
  46.423 seconds; xcodebuild completed in 47.226 seconds. Both exact row
  predicates were already satisfied at their first evaluation after the four
  runtime termination markers, confirming that the raw six seconds was a
  terminal bound around an independent Accessibility Oracle.
- Unit and Behavior validation: FlowTabCore Unit and app Behavior are not
  relevant because production projection, termination, persistence,
  scheduling, module wiring, and path resolution are unchanged. Five
  deterministic UI-target owner tests cover pre-trigger rejection, immediate
  trigger readback, delayed scheduled evidence, cancellation, final watchdog
  readback, duplicate trigger handling, and stale-generation rejection across
  200 replacements. Shared observation-owner regression coverage remains in
  the same signed invocation.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The final canonical regression passed the five new owner tests,
  five shared owner tests, two watchdog-policy tests, and the complete signed
  quit matrix 13/13 in 48.283 seconds. The matrix took 46.497 seconds and
  xcodebuild completed in 49.054 seconds after successful build-for-testing.
- Pressure validation: the same signed build repeated the complete two-case
  matrix three times and passed 3/3 in 133.671 seconds at 44.952, 44.672, and
  44.048 seconds; xcodebuild completed in 134.983 seconds. Every iteration
  resolved from the same exact selected-row evidence while scheduling and
  Accessibility completion time varied.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable child and final exit codes, successful
  Runner signing, and present result bundles. The baseline, regression, and
  Pressure xcresults record `status=succeeded` with empty issue summaries;
  they contain respectively one test, thirteen tests, and one logical test
  with three successful repetitions. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Diagnostics and Process/Tooling: the project file passes `plutil -lint`, all
  three affected Swift sources parse directly, and `git diff --check` passes.
  Settings remains below the file-size guardrail at 780 lines; the focused
  owner and its tests are 121 and 184 lines. One raw numeric timeout is
  removed, leaving 303 occurrences across 15 test files and six in Settings.
  The final incremental build retains two unrelated warnings in Space-fixture
  UI sources. Final fatal-error, assertion, crash, unexpected-failure,
  build-error, product-watchdog, selected-row-watchdog, and test-failure scans
  are empty. The one baseline, one regression, and three Pressure
  hittability-watchdog diagnostics are the established SYNC-034R conditional
  scroll/readback route and each resolves to the exact option. CFPrefs refresh
  messages are non-failing runner diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 6.3 MB EJ evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EJ selected row disappearance`.

### SYNC-036EK Closure Record

- Status: completed. The right-side hotkey-key dropdown test now resolves its
  compatible scoped/raw option identities from one deterministic evidence
  snapshot instead of allowing a two-second deadline to choose the query.
  Parent SYNC-036 remains open for its remaining owner-specific slices.
- Oracle and identity semantics: before the menu tap, the caller constructs
  both the exact scoped identifier
  `flowtab.settings.hotkey.main-key.option.space` and the compatible raw
  identifier `space`. Every readback records both existence values and applies
  stable scoped-first priority. A phase-gated initial readback cannot satisfy
  the post-tap contract. The immediate trigger readback, or a later scheduled
  readback, succeeds only when one candidate exists and returns that exact
  XCUI element for the unchanged right-side frame, tap, and selected-value
  assertions.
- Lifecycle and retained time policy: the generation owner registers before
  `tapElement(control)`. When the immediate post-tap readback remains
  unresolved, it activates the shared named cancellable 100ms XCUI cadence
  because the generated menu window exposes no projection callback. The prior
  sequential two- plus three-second bounds are retained as the single named
  five-second
  `FlowTabUITestSettingsDropdownOptionWatchdogPolicy.projection` terminal
  failure bound. Its final non-resolving readback reports both identifiers,
  both existence values, selected identity, phase, generation, source, and
  waiter result. Resolution, cancellation, replacement, and watchdog paths
  stop scheduled work; the test invocation owns observer and App cleanup.
- Baseline evidence: the unchanged signed path passed 1/1 in 8.066 seconds;
  xcodebuild completed in 9.355 seconds. XCTest opened the menu at 4.24
  seconds, evaluated the scoped option for the first time at 5.28 seconds,
  chose that scoped query, and then started a second three-second wait on the
  same element. This directly demonstrated the deadline-driven identity
  branch and redundant observation.
- Unit and Behavior validation: FlowTabCore Unit and app Behavior are not
  relevant because production dropdown placement, menu construction,
  accessibility identifiers, configuration, scheduling, module wiring, and
  path resolution are unchanged. Seven deterministic UI-target tests cover
  the combined policy bound, initially satisfied post-trigger resolution,
  scoped priority, raw compatibility fallback, delayed scheduled evidence,
  cancellation, a final watchdog readback that cannot create success,
  duplicate trigger handling, and stale-generation rejection across 200
  replacements.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The final canonical regression passed seven new owner tests,
  five shared condition-owner tests, the shared support-policy test, and the
  affected signed menu path 14/14 in 7.888 seconds. The visible path took
  6.238 seconds, resolved the scoped element from the immediate post-tap
  snapshot, and xcodebuild completed in 8.555 seconds after successful
  build-for-testing.
- Pressure validation: the same signed build repeated the affected menu path
  three times and passed 3/3 in 17.690 seconds at 5.947, 5.943, and 5.799
  seconds; xcodebuild completed in 18.427 seconds. Every iteration observed
  both candidates immediately after the menu tap and used the exact scoped
  element while launch, navigation, and Accessibility latency varied.
- Result, signing, and process evidence: the baseline and two final status
  ledgers record `stage=completed`, zero applicable child and final exit codes,
  successful Runner signing, and present result bundles. The baseline and
  final regression/Pressure xcresults record `status=succeeded` with empty
  issue summaries;
  they contain respectively one test, fourteen tests, and one logical test
  with three successful repetitions. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Diagnostics and Process/Tooling: the project file passes `plutil -lint`, all
  three affected Swift sources parse directly, and `git diff --check` passes.
  Settings remains below the guardrail at 794 lines; the focused owner/helper
  is 213 lines and its tests are 268 lines. Two raw numeric timeouts are
  removed, leaving 301 occurrences across 15 test files and four in Settings.
  The final incremental build retains two unrelated warnings in
  Space-fixture UI sources. Final fatal-error, assertion, crash,
  unexpected-failure, build-error, dropdown-watchdog, product-watchdog, and
  test-failure scans are empty. CFPrefs refresh messages are non-failing Runner
  diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 7.3 MB EK evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EK dropdown option identity`.

### SYNC-036EL Closure Record

- Status: completed. The affected Settings path now establishes exact
  main-key-control observation before navigation and accepts only a
  post-navigation Accessibility readback. Parent SYNC-036 remains open for
  its remaining owner-specific slices.
- Oracle and design: the helper constructs the exact
  `flowtab.settings.hotkey.main-key` query and starts a generation-owned
  observer before `openSettingsTab`. Its phase-gated initial readback records
  pre-trigger state without resolving. When navigation returns, an immediate
  trigger readback resolves only when that exact element reports
  `exists == true`; the returned element then preserves the existing frame,
  dropdown, option-identity, tap, and selected-value assertions.
- Lifecycle and retained time policy: if the trigger readback is unresolved,
  the owner activates the shared named cancellable 100ms XCUI cadence because
  Accessibility exposes no Settings-control publication callback. The
  existing six-second
  `FlowTabUITestSupportWatchdogPolicy.settingsControlDiscovery` remains solely
  as the terminal failure bound. Its final non-resolving readback reports the
  exact identifier, phase, generation, source, waiter result, and existence.
  Resolution, replacement, cancellation, and watchdog paths stop scheduled
  work; the helper invocation owns observation cleanup and the XCTest case
  owns App termination.
- Baseline evidence: the unchanged signed visible path passed 1/1 in 6.320
  seconds; xcodebuild completed in 7.122 seconds. Settings projection had
  completed before the raw six-second predicate began, and XCTest delayed the
  first exact-control evaluation by about one second; that first evaluation
  found the control already present.
- Unit and Behavior validation: FlowTabCore Unit and app Behavior are not
  relevant because production Settings projection, control identity,
  configuration, scheduling, module wiring, and path resolution are
  unchanged. Five deterministic UI-target owner tests cover gated initial
  state, immediate trigger evidence, delayed scheduled evidence,
  cancellation, a final watchdog readback that cannot create success,
  duplicate trigger handling, and stale-generation rejection across 200
  replacements. Five shared condition-owner tests and the compatible support
  watchdog-policy test ran in the same signed invocation.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The final canonical regression passed all eleven owner/policy
  tests and the affected signed visible path 12/12 in 6.418 seconds. The
  visible path took 4.925 seconds and xcodebuild completed in 7.641 seconds
  after successful build-for-testing.
- Pressure validation: the same signed build repeated the affected visible
  path three times and passed 3/3 in 14.332 seconds at 4.735, 4.503, and 5.093
  seconds; xcodebuild completed in 15.119 seconds. Every iteration resolved
  from the exact post-navigation main-key-control readback while launch,
  navigation, scheduling, and Accessibility latency varied.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable child and final exit codes, successful
  Runner signing, and present result bundles. The baseline, regression, and
  Pressure xcresults record `status=succeeded` with empty issue summaries;
  they contain respectively one test, twelve tests, and one logical test with
  three successful repetitions. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Diagnostics and Process/Tooling: the project file passes `plutil -lint`, all
  three affected Swift sources parse directly, and `git diff --check` passes.
  Settings remains at the 800-line guardrail; the focused owner/helper
  and its tests are 150 and 174 lines. One raw numeric timeout is removed,
  leaving 300 occurrences across 15 test files and three in Settings. The
  final incremental build retains two unrelated warnings in Space-fixture UI
  sources. Final fatal-error, assertion, crash, unexpected-failure,
  build-error, settings-control-watchdog, product-watchdog, and test-failure
  scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 3.7 MB EL evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EL settings control readiness`.

### SYNC-036EM Closure Record

- Status: completed. The intrinsic-width UI path now establishes observation
  of the exact language control before Settings navigation and accepts only
  post-navigation Accessibility evidence. Parent SYNC-036 remains open for
  its remaining owner-specific slices.
- Oracle and design: the caller reuses the generation-owned exact-element
  helper with `flowtab.settings.appearance.app-language`. Its phase-gated
  initial readback records pre-navigation state without resolving. The
  `openSettingsTab` trigger retains its independent exact Settings-projection
  contract, then an immediate trigger readback resolves only when the language
  control exists. The unchanged `[100, 120)` frame-width assertions remain the
  independent visible-layout Oracle.
- Lifecycle and retained time policy: if the immediate trigger readback is
  unresolved, the helper activates the named cancellable 100ms XCUI cadence
  because Accessibility exposes no language-control publication callback. The
  compatible six-second
  `FlowTabUITestSupportWatchdogPolicy.settingsControlDiscovery` remains solely
  as a terminal failure bound with exact identifier, phase, generation,
  source, waiter result, and final existence diagnostics. Resolution,
  replacement, cancellation, and watchdog paths stop scheduled work; the
  helper invocation owns observer cleanup and the XCTest case owns App
  termination.
- Baseline evidence: the unchanged signed path passed 1/1 in 4.199 seconds;
  xcodebuild completed in 5.462 seconds. Exact Settings content existed at
  1.89 seconds, the raw six-second wait began at 1.90 seconds, and XCTest did
  not first evaluate the exact language control until 2.93 seconds. That first
  evaluation found it present, confirming the duration was observation
  opportunity around independent existence and frame evidence.
- Unit and Behavior validation: FlowTabCore Unit and app Behavior are not
  relevant because production Settings projection, localization, intrinsic
  layout, configuration, scheduling, module wiring, and path resolution are
  unchanged. The signed regression reran all five deterministic exact-element
  owner tests covering gated initial state, immediate and scheduled evidence,
  cancellation, terminal readback diagnostics, duplicate trigger handling,
  and stale-generation rejection across 200 replacements, plus the compatible
  support-watchdog policy test.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The final canonical regression passed the six owner/policy
  tests and affected visible path 7/7 in 4.465 seconds. The visible path took
  3.594 seconds and xcodebuild completed in 5.179 seconds after successful
  build-for-testing.
- Pressure validation: the same signed build repeated the affected visible
  path three times and passed 3/3 in 9.356 seconds at 3.219, 3.058, and 3.080
  seconds; xcodebuild completed in 10.026 seconds. Every iteration accepted
  the same exact language-control existence and live frame bounds while
  launch, navigation, scheduling, and Accessibility latency varied.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable child and final exit codes, successful
  Runner signing, and present result bundles. The baseline, regression, and
  Pressure xcresults record `status=succeeded` with empty issue summaries;
  they contain respectively one test, seven tests, and one logical test with
  three successful repetitions. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. Settings remains at the 800-line guardrail.
  One raw numeric timeout is removed, leaving 299 occurrences across 15 test
  files and two in Settings. Final compiler-warning, fatal-error, assertion,
  crash, unexpected-failure, build-error, settings-control-watchdog,
  product-watchdog, and test-failure scans are empty. CFPrefs refresh messages
  are non-failing Runner diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 680 KB EM evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EM language control readiness`.

### SYNC-036EN Closure Record

- Status: completed. The permission-off Settings path now observes the exact
  in-app modifier/key controls as one post-navigation disabled projection.
  Parent SYNC-036 remains open for its remaining owner-specific slices.
- Oracle and design: both exact XCUI queries and the Settings-content query
  are constructed before `openSettingsTab`. A phase-gated initial readback
  records pre-navigation state without resolving. The immediate trigger
  readback, or a later scheduled readback, succeeds only when the App is
  foreground, Settings content exists, both exact controls exist, and both
  controls report `isEnabled == false` in that same observation. The returned
  modifier/key elements retain the final independent disabled-state readbacks.
- Lifecycle and retained time policy: scheduled XCUI work is registered before
  navigation but activates only when the immediate trigger evidence remains
  incomplete. Accessibility exposes no permission-driven control-projection
  callback, so the shared named cancellable 100ms cadence remains the fallback.
  The two prior sequential five-second opportunities are combined as the
  owner-named ten-second
  `FlowTabUITestSettingsInAppControlsProjectionPolicy.projectionWatchdog`
  terminal bound. Its final non-resolving readback reports acceptance,
  generation, source, waiter result, App/Settings state, and both controls'
  existence/enabled values. Resolution, cancellation, replacement, and
  watchdog paths stop scheduled work; the helper owns observer cleanup and the
  XCTest case owns App termination.
- Baseline evidence: the unchanged signed path passed 1/1 in 5.370 seconds;
  xcodebuild completed in 6.101 seconds. Settings content existed at 1.82
  seconds. The modifier wait then delayed its first exact evaluation until
  2.86 seconds, and the sequential key wait delayed its first evaluation until
  3.97 seconds. Both were already present, after which disabled-state reads
  occurred at 4.05 and 4.10 seconds. This demonstrated two independent
  scheduler opportunities before the actual joint state evidence.
- Unit and Behavior validation: FlowTabCore Unit and app Behavior are not
  relevant because production permission rules, Settings projection,
  controls, configuration, scheduling, module wiring, and path resolution are
  unchanged. Seven deterministic UI-target tests cover the named watchdog,
  exact truth table, initially satisfied but gated state, immediate and delayed
  evidence, cancellation, terminal final readback, duplicate requests, and
  stale-generation rejection across 200 replacements. Five shared condition
  owner tests ran in the same signed invocation.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The final canonical regression passed seven focused tests, five
  shared owner tests, and the affected signed permission-off path 13/13 in
  5.649 seconds. The visible path took 3.757 seconds and xcodebuild completed
  in 6.913 seconds after successful build-for-testing.
- Pressure validation: the same signed build repeated the affected visible
  path three times and passed 3/3 in 9.375 seconds at 3.100, 3.102, and 3.174
  seconds; xcodebuild completed in 10.102 seconds. Every iteration accepted
  the same joint exact-control disabled projection while launch, navigation,
  scheduling, and Accessibility latency varied.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable child and final exit codes, successful
  Runner signing, and present result bundles. The baseline, regression, and
  Pressure xcresults record `status=succeeded` with empty issue summaries;
  they contain respectively one test, thirteen tests, and one logical test
  with three successful repetitions. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact post-run `pgrep -x` readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or Space fixture process.
- Diagnostics and Process/Tooling: the project file passes `plutil -lint`, all
  three affected Swift sources parse directly, and `git diff --check` passes.
  Settings remains below the guardrail at 799 lines; the focused owner/helper
  and tests are 236 and 263 lines. Two raw numeric timeouts are removed,
  leaving 297 occurrences across 14 test files and none in Settings. The final
  incremental build retains two unrelated warnings in Space-fixture UI
  sources. Final fatal-error, assertion, crash, unexpected-failure,
  build-error, in-app-controls-watchdog, product-watchdog, and test-failure
  scans are empty. CFPrefs refresh messages are non-failing Runner diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 3.6 MB EN evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EN disabled controls projection`.

### SYNC-036EO Closure Record

- Status: completed. The English primary Search path now observes its exact
  input and Mock Mail row as one post-trigger accessibility projection. Parent
  SYNC-036 remains open for its remaining owner-specific slices.
- Oracle and design: the ordered exact identifiers, XCUI queries, deferred
  readback registration, and generation-owned condition observer are created
  before the Search trigger is posted. A phase-gated initial readback records
  pre-trigger state without resolving. The immediate trigger readback, or a
  later scheduled readback, succeeds only when one snapshot contains both
  expected identifiers in order and both exact queries report `exists == true`.
  Runtime-log trigger receipt and presentation delivery remain the independent
  trigger-helper evidence.
- Lifecycle and retained time policy: scheduled XCUI work is registered before
  the trigger but activates only when the immediate post-trigger projection is
  incomplete. Accessibility exposes no collection-publication callback, so the
  shared named cancellable 100ms cadence remains the conditional fallback. The
  two prior sequential five-second opportunities are combined as the named
  ten-second
  `FlowTabUITestEnglishSearchProjectionPolicy.projectionWatchdog` terminal
  bound. Its final non-resolving readback reports phase, generation, source,
  waiter result, expected identifiers, observed identifiers, and each existence
  value. Resolution, cancellation, replacement, and watchdog paths stop
  scheduled work; the helper owns observation cleanup and the XCTest case owns
  App termination.
- Baseline evidence: the unchanged signed path passed 1/1 in 11.772 seconds;
  xcodebuild completed in 12.804 seconds. The Search trigger completed around
  8.37 seconds, while the two sequential predicate waits delayed their first
  exact evaluations until about 9.40 and 10.51 seconds. This demonstrated two
  scheduler opportunities after the trigger instead of one atomic projection.
- Unit and Behavior validation: FlowTabCore Unit and app Behavior are not
  relevant because production Search state, trigger delivery, configuration,
  scheduling, module wiring, window identity, and path resolution are
  unchanged. Seven deterministic UI-target tests cover the named watchdog,
  ordered complete-projection truth table, initially satisfied but gated state,
  immediate and delayed evidence, cancellation, terminal final readback,
  duplicate trigger completion, and stale-generation rejection across 200
  replacements. Five shared condition-owner tests ran in the same signed
  invocation.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The canonical regression passed seven focused tests, five shared
  owner tests, and the affected signed English path 13/13 in 11.679 seconds.
  The visible path took 10.181 seconds and xcodebuild completed in 12.880
  seconds after successful build-for-testing. Its post-trigger collection
  readback observed the input and Mock Mail row around 9.02 and 9.07 seconds in
  the same condition evaluation.
- Pressure validation: the same signed build repeated the affected visible
  path three times and passed 3/3 in 28.992 seconds at 9.835, 9.457, and 9.700
  seconds; xcodebuild completed in 29.726 seconds. Every iteration accepted the
  same ordered two-element projection while launch, navigation, scheduling,
  and Accessibility latency varied.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable child and final exit codes, successful
  Runner signing, and present result bundles. The baseline, regression, and
  Pressure xcresults record `status=succeeded`; they contain respectively one
  test, thirteen tests, and one logical test with three successful repetitions.
  Host-context strict deep codesign accepts both the fixed App and signed
  Runner. Exact post-run process readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Diagnostics and Process/Tooling: the project file passes `plutil -lint`, all
  three affected Swift sources parse directly, and `git diff --check` passes.
  EnglishLayout is 144 lines; the focused owner/helper and tests are 192 and
  261 lines. Two raw numeric timeouts are removed, leaving 295 occurrences
  across 14 UI-test files; the two remaining EnglishLayout values belong to
  separate launch-readiness contracts. The final incremental build retains two
  unrelated warnings in Space-fixture UI sources. Final fatal-error, assertion,
  crash, unexpected-failure, build-error, collection-projection-watchdog,
  product-watchdog, and test-failure scans are empty. CFPrefs refresh messages
  are non-failing Runner diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 4.0 MB EO evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EO English Search projection`.

### SYNC-036EP Closure Record

- Status: completed. Both English orchestration tests now use the shared named
  FlowTab foreground-readiness terminal policy and path-specific final-state
  diagnostics. Parent SYNC-036 remains open for its remaining owner-specific
  slices.
- Oracle and design: `waitForFlowTabUITestApplicationToBecomeReady` retains its
  immediate exact `XCUIApplication.State.runningForeground` readback and its
  installed-App activation fallback. The English-owned assertion helper records
  the returned Boolean and reports the path description plus final App state
  when readiness remains unmet. Subsequent exact Settings, permission-action,
  Logs, App Visibility, and Search evidence remains unchanged.
- Lifecycle and retained time policy: the compatible 12-second
  `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` duration is solely
  a terminal failure bound. Both XCUI state waits are synchronous and create no
  observer, timer, scheduler, polling, or cancellation lifecycle; each XCTest
  invocation owns App termination.
- Baseline evidence: the unchanged signed pair passed 2/2 in 16.812 seconds;
  the granted-permission and primary-surface paths took 7.112 and 9.700
  seconds, and xcodebuild completed in 39.570 seconds. Their foreground state
  checks completed immediately around 1.12 and 0.82 seconds, confirming the raw
  ten-second arguments were failure opportunity rather than success evidence.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The canonical regression passed the shared policy test and both
  affected signed paths 3/3 in 16.878 seconds. The granted-permission and
  primary-surface paths took 6.950 and 9.706 seconds; the policy test took 0.222
  seconds and xcodebuild completed in 17.582 seconds. The final foreground
  checks again completed immediately around 1.30 and 0.78 seconds.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production state, configuration, scheduling, module wiring, window identity,
  and path resolution are unchanged. Pressure is not relevant because no
  observer, timer, scheduler, retry loop, repeated asynchronous work, or hot
  path changed.
- Result, signing, and process evidence: both status ledgers record
  `stage=completed`, zero applicable child and final exit codes, successful
  Runner signing, and present result bundles. The baseline and regression
  xcresults record `status=succeeded` with two and three tests respectively.
  Host-context strict deep codesign accepts both the fixed App and signed
  Runner. Exact post-run process readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Diagnostics and Process/Tooling: EnglishLayout parses directly and
  `git diff --check` passes. It remains a single-purpose 172-line test file and
  contains no raw numeric timeout. Two raw numeric timeouts are removed, leaving
  293 occurrences across 13 UI-test files. The incremental build and final
  targeted fatal-error, assertion, crash, unexpected-failure, build-error,
  foreground-readiness-watchdog, product-watchdog, and test-failure scans are
  empty. XCTest output retains only destination-selection and non-failing
  CFPrefs refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 736 KB EP evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EP English foreground readiness`.

### SYNC-036EQ Closure Record

- Status: completed. The shared Space Fixture permission preflight now uses the
  named FlowTab foreground-readiness terminal policy and reports the final App
  state before stopping later independent contracts. Parent SYNC-036 remains
  open for its remaining owner-specific slices.
- Oracle and design: the permission-projection observer remains installed
  before FlowTab launch or permission resolution. The shared readiness helper
  performs an immediate exact `XCUIApplication.State.runningForeground`
  readback and retains its installed-App activation fallback. The resolver
  records the returned Boolean, reports the unmet permission-preflight
  condition plus final App state, and returns before Home navigation if the
  state remains unsatisfied. Exact Home-button, terminal granted-permission
  projection, fixture launch, and AX-suppression evidence remain independent.
- Lifecycle and retained time policy: the compatible 12-second
  `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` duration is solely
  a terminal failure bound. Both XCUI state waits are synchronous and create no
  recurring work. The two public preflight entry paths continue to own the
  preinstalled permission observer, and the launching variant owns App
  termination.
- Baseline evidence: the unchanged signed real-topology path passed 1/1 in
  11.533 seconds and xcodebuild completed in 12.591 seconds after canonical
  fixture preparation. The affected permission-preflight foreground readback
  completed immediately around 1.66 seconds; subsequent exact Home,
  granted-permission, fixture-foreground, and AX-suppression evidence all
  resolved.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The final canonical signed invocation prepared current fixture
  variants and passed the shared policy test plus affected real-topology path
  2/2 in 11.046 seconds. The real path took 10.906 seconds, the policy test took
  0.140 seconds, and xcodebuild completed in 11.734 seconds. The permission-
  preflight foreground readback again completed immediately around 1.21
  seconds.
- Validation history: the first post-build combined invocation returned 65
  after the changed foreground and permission-preflight evidence had already
  passed; the later independent Application AX-suppression watchdog reported
  both completion and authorized external-AX evidence unobserved. Exact process
  cleanup succeeded, then an unchanged exact retry passed the real path 1/1 in
  11.195 seconds. The final combined invocation passed both tests, closing the
  downstream fixture-observation variation with explicit evidence.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production state, configuration, scheduling, module wiring, window identity,
  and path resolution are unchanged. Pressure is not relevant because no
  observer, timer, scheduler, retry loop, repeated asynchronous work, or hot
  path changed.
- Result, signing, and process evidence: baseline, exact retry, and final status
  ledgers record `stage=completed`, zero applicable fixture/signing/test/final
  exit codes, and present result bundles; the failed historical invocation
  records its exact test exit 65. Baseline and final xcresults record
  `status=succeeded` with one and two tests respectively. Host-context strict
  deep codesign accepts both the fixed App and signed Runner. Exact post-run
  process readback finds no Flow Tab, UI-test Runner, xcodebuild, Space fixture,
  or Chrome fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It remains a single-responsibility 381-line
  helper/owner file. One raw numeric timeout is removed, leaving 292 occurrences
  across 13 UI-test files; the file's remaining raw ten-second Home-navigation
  wait is a separate next contract. Final baseline/final fatal-error, assertion,
  crash, unexpected-failure, build-error, permission-preflight-watchdog,
  product-watchdog, and test-failure scans are empty. Fixture preparation and
  test execution retain only destination-selection warnings and non-failing
  CFPrefs refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 134 MB EQ evidence root contains
  only rebuildable result bundles, fixture-preparation records, and logs. It is
  removed after this durable record, followed by exact absence and process
  readback.
- Commit subject:
  `test(sync): migrate SYNC-036EQ fixture permission foreground readiness`.

### SYNC-036ER Closure Record

- Status: completed. The shared Space Fixture permission preflight now owns a
  named exact-Home navigation terminal policy and path-specific final query
  diagnostics. Parent SYNC-036 remains open for its remaining owner-specific
  slices.
- Oracle and design: the exact `flowtab.sidebar.tab.home` button query is
  retained by the caller. `tapFirstHittable` installs its generation-owned
  observer before the tap, performs an immediate query-count, existence, and
  hittability readback, and succeeds only when one exact existing candidate is
  hittable. It then taps that resolved element. Failure reports the shared
  owner’s final snapshot plus the exact identifier and final candidate count.
  The already-preinstalled permission observer independently requires the
  terminal granted projection after the tap.
- Lifecycle and retained time policy: the compatible ten-second
  `FlowTabUITestSupportWatchdogPolicy.spaceFixtureNavigation` duration is
  solely a terminal failure bound. XCUI exposes no candidate-publication
  callback, so the shared named cancellable 100ms cadence remains active only
  while no exact candidate is hittable. Resolution, watchdog, explicit cancel,
  replacement, and deinit stop scheduled work. The helper invocation owns the
  hittable generation and scheduler; the permission-preflight caller owns its
  separate projection observer and App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036EQ final
  invocation exercised the unchanged raw-ten-second call. Its real-topology
  path passed in 10.906 seconds; the Home query began around 1.22 seconds,
  produced an existing hittable exact candidate around 1.42–1.45 seconds, and
  tapped it around 1.52 seconds before the granted-permission and downstream
  fixture evidence resolved.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The canonical signed regression passed the shared policy test,
  two hittable-owner evidence tests, and the new direct permission-preflight UI
  path 4/4 in 4.535 seconds. The direct path took 3.993 seconds, established the
  exact candidate around 2.10–2.20 seconds, tapped it around 2.31 seconds, and
  terminated only after exact granted-permission projection. The initial and
  watchdog owner tests took 0.207 and 0.176 seconds, the policy test took 0.159
  seconds, and xcodebuild completed in 5.230 seconds.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production state, configuration, scheduling, module wiring, window identity,
  and path resolution are unchanged. Pressure is not relevant because the
  existing observation, timer, scheduler, retry, cancellation, and hot-path
  mechanics were not changed; their initial and final-evidence regressions ran
  in the signed target.
- Result, signing, and process evidence: the status ledger records
  `stage=completed`, zero build/signing/test/final exit codes, and a present
  result bundle. The xcresult records `status=succeeded` with four tests.
  Host-context strict deep codesign accepts both the fixed App and signed
  Runner. Exact post-run process readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or Space fixture process.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly and `git diff --check` passes. The policy, permission owner/helper,
  and permission tests are 100, 390, and 331 lines. One raw numeric timeout is
  removed, leaving 291 occurrences across 12 UI-test files and none in the
  permission owner/helper. The incremental build retains two unrelated
  warnings in Space-fixture UI sources. Final fatal-error, assertion, crash,
  unexpected-failure, build-error, Home-navigation-watchdog, product-watchdog,
  and test-failure scans are empty. Destination-selection and CFPrefs refresh
  messages are non-failing diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 572 KB ER evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036ER fixture permission Home readiness`.

### SYNC-036ES Closure Record

- Status: completed. The System App MRU synchronization owner now owns one
  named exact-application-order terminal policy for initial launch, process
  relaunch, and every repeated reopen. Parent SYNC-036 remains open for its
  remaining owner-specific slices.
- Oracle and design: every invocation constructs the same ordered eight-bundle
  expectation and installs its generation-owned diagnostics observer before the
  global switcher trigger. The gated initial readback cannot resolve before the
  trigger returns; the first accepted post-trigger snapshot must equal the full
  ordered permutation exactly. Watchdog failure retains the expected order,
  final parsed and encoded diagnostics, observation generation and source,
  waiter result, and switcher hierarchy.
- Lifecycle and retained time policy: the compatible ten-second
  `FlowTabUITestSystemAppMRUPolicy.appOrderWatchdog` duration is solely a
  terminal failure bound. XCUI exposes no diagnostics-publication callback, so
  the shared named cancellable 100ms cadence remains active only while the
  exact permutation is unresolved. Resolution, watchdog, explicit cancel,
  replacement, and deinit stop scheduled work. Each helper invocation owns its
  observation generation and cancellation; the workflow owns FlowTab and all
  eight fixture applications.
- Baseline evidence: the unchanged signed eight-application path passed 1/1 in
  35.155 seconds and xcodebuild testing completed in 36.454 seconds after
  canonical fixture preparation. It resolved the exact initial order, rebuilt
  the exact order after terminating and relaunching FlowTab, and resolved all
  ten repeated reopen orders before cleaning up every fixture.
- UI validation: the canonical signed regression passed the named-policy test,
  exact-permutation rule, five shared observation lifecycle tests, and the full
  eight-application path 8/8 in 36.199 seconds; xcodebuild testing completed in
  37.023 seconds. The full visible path took 34.809 seconds and again resolved
  initial, relaunched, and ten repeated-reopen exact orders. The deterministic
  tests covered initial preinstalled evidence, reentrant readback, cancelled
  and replaced events under pressure, named scheduling, and final watchdog
  readback.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production MRU state, module wiring, configuration, exact window identity,
  and path resolution are unchanged. The visible path’s ten reopen iterations
  provide repeated-path stress. Separate Pressure is not relevant because the
  existing observer, scheduler, retry, cancellation, and hot-path mechanics are
  unchanged.
- Result, signing, and process evidence: baseline and regression status ledgers
  record `stage=completed`, zero applicable fixture/build/signing/test/final
  exit codes, and present result bundles. Their xcresults record
  `status=succeeded` with one and eight tests. Host-context strict deep codesign
  accepts both the fixed App and signed Runner. Exact post-run process readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or System MRU fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It remains a single-purpose 284-line test file.
  Three raw numeric timeouts are removed, leaving 288 occurrences across 12 UI-
  test files; the file’s five remaining durations belong to separate runtime-
  log, process-termination, relaunch-readiness, and fixture-activation
  contracts. The incremental build retains destination-selection warnings and
  one unrelated existing Space-fixture warning. Final fatal-error, assertion,
  crash, unexpected-failure, build-error, app-order-watchdog, product-watchdog,
  and test-failure scans are empty. CFPrefs refresh messages are non-failing
  Runner diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 1.2 MB ES evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036ES System MRU app order`.

### SYNC-036ET Closure Record

- Status: completed. The System App MRU owner now supplies one named bootstrap-
  log terminal policy to both its initial-launch and process-relaunch evidence
  phases. Parent SYNC-036 remains open for its remaining owner-specific slices.
- Oracle and design: each phase creates a runtime-log baseline before launching
  FlowTab. The baseline anchors existing log file identities and byte offsets
  while its file-event source is already active. Success requires baseline-
  relative contents containing both exact markers `collectAppRank` and
  `bootstrapFallback=0`; the wait performs an immediate readback and accepts
  file-event or scheduled readback only when that conjunction is true. Expiry
  reports missing markers, baseline/current file-event generations, character
  count, and the final 4,000-character content tail.
- Lifecycle and retained time policy: the compatible ten-second
  `FlowTabUITestSystemAppMRUPolicy.appRankBootstrapLogWatchdog` duration is
  solely a terminal failure bound. The file-event source is the primary wakeup;
  the shared named cancellable 200ms cadence covers unavailable or missed file
  events. Resolution, watchdog, explicit cancel, replacement, and deinit stop
  scheduled work. Each shared wait invocation owns its observation cancellation,
  each baseline owns its file-event source, and the workflow owns FlowTab and
  all eight fixtures.
- Baseline evidence: the unchanged signed eight-application path passed 1/1 in
  35.215 seconds and xcodebuild testing completed in 36.131 seconds after
  canonical fixture preparation. Both raw-ten-second waits resolved their exact
  baseline-relative marker conjunction before the initial termination and the
  repeated-reopen phase respectively; the path then completed every downstream
  exact-order assertion and fixture cleanup.
- UI and Pressure validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The canonical signed regression passed the two policy tests,
  post-baseline file-event append, truncation/replacement identity reset, event
  resolution and cancellation, the 2,000-iteration runtime-log lifecycle
  Pressure path including final-watchdog diagnostics, and the full eight-app UI
  path 7/7 in 35.342 seconds. The full path took 34.348 seconds and xcodebuild
  testing completed in 36.125 seconds.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production rank collection, logging, module wiring, configuration, exact
  window identity, and path resolution are unchanged. A separate repeated
  signed UI Pressure run is not relevant because the observation and scheduler
  mechanics are unchanged; their deterministic 2,000-iteration lifecycle
  Pressure test passed in the signed target.
- Result, signing, and process evidence: baseline and regression status ledgers
  record `stage=completed`, zero applicable fixture/build/signing/test/final
  exit codes, and present result bundles. Their xcresults record
  `status=succeeded` with one and seven tests. Host-context strict deep codesign
  accepts both the fixed App and signed Runner. Exact post-run process readback
  finds no Flow Tab, UI-test Runner, xcodebuild, or System MRU fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It remains a single-purpose 294-line test file.
  Two raw numeric timeouts are removed, leaving 286 occurrences across 12 UI-
  test files; its three remaining durations belong to separate FlowTab process-
  termination, relaunch-readiness, and fixture-activation contracts. Final
  fatal-error, assertion, crash, unexpected-failure, build-error, runtime-log-
  watchdog, product-watchdog, and test-failure scans are empty. Build and test
  output retains only destination-selection warnings and non-failing CFPrefs
  refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 776 KB ET evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036ET System MRU bootstrap logs`.

### SYNC-036EU Closure Record

- Status: completed. The System App MRU relaunch boundary now delegates first-
  process termination to the shared exact application-state evidence contract.
  Parent SYNC-036 remains open for its remaining owner-specific slices.
- Oracle and design: the shared helper reads the exact initial
  `XCUIApplication.State` before deciding whether to call `terminate()`, reads
  immediately after the trigger, invokes XCTest’s persistent `.notRunning`
  state wait only while the process is still running, and always performs a
  final readback after the wait boundary. Only final `.notRunning` satisfies
  termination. The waiter result remains diagnostic evidence; failure reports
  the exact target, initial state, trigger-request flag, post-trigger state,
  waiter completion, and final state before the relaunch phase proceeds.
- Lifecycle and retained time policy: the compatible five-second
  `FlowTabUITestApplicationTerminationPolicy.watchdogFailureObservationTimeout`
  duration is solely a terminal failure bound. The synchronous helper
  invocation owns the conditional XCTest state wait and leaves no observer,
  timer, scheduler, retry, or recurring cancellation work.
- Baseline evidence: the immediately preceding committed SYNC-036ET regression
  exercised the private raw-five-second helper and full process-session rebuild.
  Its signed eight-app path passed in 34.348 seconds, and all seven selected
  tests passed in 35.342 seconds with xcodebuild testing complete in 36.125
  seconds. The first FlowTab process terminated before a distinct relaunched
  process rebuilt the exact MRU order.
- UI and Pressure validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The canonical signed regression passed the local shared-policy
  compatibility assertion, already-stopped, immediate, delayed, final-boundary,
  watchdog-diagnostic, and 100-target repeated-lifecycle contracts plus the
  full eight-app UI path 8/8 in 36.773 seconds. The visible path took 35.673
  seconds and xcodebuild testing completed in 37.680 seconds. It terminated
  initial FlowTab PID 66817, relaunched PID 66820, rebuilt and repeatedly
  reopened the exact order, then terminated the relaunched process and all
  fixtures.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production process lifecycle, MRU state, module wiring, configuration, exact
  window identity, and path resolution are unchanged. The deterministic
  100-target repeated-lifecycle path satisfies Pressure for the shared
  synchronous termination algorithm.
- Result, signing, and process evidence: the status ledger records
  `stage=completed`, zero fixture/build/signing/test/final exit codes, and a
  present result bundle. The xcresult records `status=succeeded` with eight
  tests. Host-context strict deep codesign accepts both the fixed App and signed
  Runner. Exact post-run process readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or System MRU fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. Removing the private helper leaves the focused
  source at 296 lines. One raw numeric timeout is removed, leaving 285
  occurrences across 12 UI-test files; its two remaining durations belong to
  separate relaunch-readiness and fixture-activation contracts. Final fatal-
  error, assertion, crash, unexpected-failure, build-error, termination-
  watchdog, product-watchdog, and test-failure scans are empty. Build and test
  output retains only destination-selection warnings and non-failing CFPrefs
  refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 496 KB EU evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EU System MRU termination`.

### SYNC-036EV Closure Record

- Status: completed. The System App MRU relaunch path now uses the shared named
  FlowTab foreground-readiness terminal policy and reports its final App state
  before any rebuilt-order trigger. Parent SYNC-036 remains open for its
  remaining owner-specific slices.
- Oracle and design: after exact first-process termination, the shared launch
  helper launches the new `XCUIApplication`, activates the fixed installed App,
  and attempts an exact `.runningForeground` state readback. The caller then
  performs the same persistent exact-state wait through the readiness helper;
  its installed-App activation fallback remains available. Only
  `.runningForeground` satisfies readiness. The caller records the Boolean and
  reports the unmet System MRU relaunch condition plus final App state before
  triggering the rebuilt-order observer.
- Lifecycle and retained time policy: the compatible twelve-second
  `FlowTabUITestSupportWatchdogPolicy.foregroundActivation` duration is solely
  a terminal failure bound. Both XCUI state waits are synchronous and create no
  observer, timer, scheduler, polling, retry, or cancellation lifecycle. The
  workflow owns the relaunched App and every fixture through cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036EU regression
  exercised the raw-twelve-second caller after exact first-process termination.
  Its signed eight-app path passed in 35.673 seconds; all eight selected tests
  passed in 36.773 seconds and xcodebuild testing completed in 37.680 seconds.
  Foreground readiness resolved before the rebuilt-order trigger and every
  downstream exact order completed.
- UI validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The canonical signed regression passed the shared operation-
  policy test and full eight-app terminate/relaunch/order path 2/2 in 35.789
  seconds. The visible path took 35.596 seconds and xcodebuild testing completed
  in 36.516 seconds. Launch tracing records successful activation with final
  state raw value 4, then an immediate completed foreground wait before the
  rebuilt-order notification.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production state, module wiring, configuration, exact window identity, and
  path resolution are unchanged. Pressure is not relevant because no observer,
  timer, scheduler, retry, repeated asynchronous work, or hot path changed.
- Result, signing, and process evidence: the status ledger records
  `stage=completed`, zero fixture/build/signing/test/final exit codes, and a
  present result bundle. The xcresult records `status=succeeded` with two tests.
  Host-context strict deep codesign accepts both the fixed App and signed
  Runner. Exact post-run process readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or System MRU fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It remains a focused 303-line test file. One raw
  numeric timeout is removed, leaving 284 occurrences across 12 UI-test files;
  its sole remaining duration belongs to the separate fixture-activation
  contract. Final fatal-error, assertion, crash, unexpected-failure, build-
  error, foreground-readiness-watchdog, product-watchdog, and test-failure
  scans are empty. Build and test output retains only destination-selection
  warnings and non-failing CFPrefs refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 436 KB EV evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EV System MRU relaunch readiness`.

### SYNC-036EW Closure Record

- Status: completed. The System App MRU owner now supplies one named fixture-
  activation terminal policy to every initial and relaunch order-establishment
  operation. Its source file contains no raw numeric timeout. Parent SYNC-036
  remains open for the remaining owner-specific files.
- Oracle and design: `assertTriggerMakesApplicationFrontmost` constructs the
  owner for one exact fixture bundle identifier, installs
  `NSWorkspace.didActivateApplicationNotification` observation before calling
  `XCUIApplication.activate()`, and performs an initial frontmost readback.
  Success requires the exact expected bundle identity from either that initial
  readback or a later activation-notification readback. Expiry performs a final
  watchdog readback and reports expected plus observed frontmost identity,
  generation, source, and waiter result.
- Lifecycle and retained time policy: the compatible five-second
  `FlowTabUITestSystemAppMRUPolicy.fixtureActivationWatchdog` duration is solely
  a terminal failure bound. Each helper invocation owns its notification token,
  generation, XCTest expectation, cancellation, and final readback. Resolution,
  watchdog, explicit cancel, replacement, and deinit remove observation inputs.
  The workflow owns all eight fixture Apps and the ordered identity lists.
- Baseline evidence: the immediately preceding committed SYNC-036EV regression
  exercised the raw-five-second call across eight initial and eight relaunch
  activations. Its signed full path passed in 35.596 seconds; both selected
  tests passed in 35.789 seconds and xcodebuild testing completed in 36.516
  seconds before every downstream exact order and cleanup completed.
- UI and Pressure validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The canonical signed regression passed named-policy,
  activation-notification, initial-readback, final-watchdog, 500-iteration
  cancellation/replacement Pressure, 100-iteration scheduled-readback lifecycle
  Pressure, and the full 16-activation eight-app UI path 7/7 in 35.381 seconds.
  The visible path took 34.221 seconds and xcodebuild testing completed in
  36.447 seconds.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production MRU state, activation behavior, module wiring, configuration,
  exact window identity, and path resolution are unchanged. The visible path
  supplies repeated real activation stress, while the deterministic 500- and
  100-iteration tests cover shared observer and scheduler Pressure.
- Result, signing, and process evidence: the status ledger records
  `stage=completed`, zero fixture/build/signing/test/final exit codes, and a
  present result bundle. The xcresult records `status=succeeded` with seven
  tests. Host-context strict deep codesign accepts both the fixed App and signed
  Runner. Exact post-run process readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or System MRU fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It remains a focused 313-line test file. Its
  raw numeric timeout inventory is now empty; one occurrence is removed from
  the repository UI-test inventory, leaving 283 occurrences across 11 files.
  Final fatal-error, assertion, crash, unexpected-failure, build-error,
  fixture-activation-watchdog, product-watchdog, and test-failure scans are
  empty. Build and test output retains only destination-selection warnings and
  non-failing CFPrefs refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 492 KB EW evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EW System MRU fixture activation`.

### SYNC-036EX Closure Record

- Status: completed. The Noisy Option+Tab workflow now supplies one named exact-
  preview terminal policy to the `normal1`, `fullscreen1`, `normal2`, and
  `fullscreen2` phases. Parent SYNC-036 remains open for the remaining owner-
  specific files.
- Oracle and design: every phase reads the persistent switcher diagnostics and
  succeeds only when the preview contains the exact four expected titles with
  the exact expected count. The shared observation owner registers its named
  cancellable XCUI readback scheduler before the initial readback, so an
  already-published projection and a later scheduled readback are both
  observable. Stale callbacks from a cancelled generation cannot resolve a
  replacement owner. Expiry performs a final readback and reports expectation,
  identifier, existence, selected bundle, parsed titles, preview/raw values,
  generation, source, and waiter evidence.
- Lifecycle and retained time policy: the compatible eight-second
  `FlowTabUITestNoisyOptionTabPolicy.exactPreviewProjectionWatchdog` duration is
  solely a terminal failure bound. XCTest exposes no projection callback for
  this diagnostics element, so the shared named 100ms readback cadence remains
  a condition-polling fallback. The condition is checked immediately; each
  phase invocation owns cancellation of its scheduler, expectation, and
  generation, while the workflow owns FlowTab and fixture App cleanup.
- Baseline evidence: the canonical signed raw-eight-second path passed the real
  noisy four-window Space/fullscreen topology 1/1 in 28.807 seconds;
  xcodebuild testing completed in 30.007 seconds. All four exact preview phases
  resolved before downstream exact selection, activation, and cleanup.
- UI and Pressure validation: the canonical installer rebuilt and signed
  `/Users/lk/Applications/Flow Tab UITest.app` with
  `Apple Development: gobestsoft@qq.com (RF9WCUVKH8)` and TeamIdentifier
  `96PUA726W9`. The first combined run compiled the changed UI-test source and
  signed the Runner, then macOS automation-mode initialization expired before
  any test body started. Exact process readback was empty, and the direct
  `test-without-building` retry passed named policy, parsing, exact set/count,
  matching initial state, scheduled arrival, real-noisy expectation, final-
  watchdog diagnostics, 100-generation stale-readback Pressure, and the real
  four-phase UI path 9/9 in 25.678 seconds. The visible path took 24.205 seconds
  and xcodebuild testing completed in 26.427 seconds.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production filtering, MRU state, configuration, module wiring, exact window
  identity, and path resolution are unchanged. Deterministic owner tests cover
  cancellation, generation replacement, initial state, scheduled completion,
  and watchdog behavior; the signed real topology supplies UI orchestration
  evidence.
- Result, signing, and process evidence: the successful status ledger records
  `stage=completed`, zero fixture/signing/test/final exit codes, and a present
  result bundle. The xcresult records `status=succeeded` with nine tests. Host-
  context strict deep codesign accepts both the fixed App and signed Runner.
  Exact post-run process readback finds no Flow Tab, UI-test Runner, xcodebuild,
  or fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It remains a focused 337-line test file. Its
  raw numeric timeout inventory falls to seven; one occurrence is removed from
  the repository UI-test inventory, leaving 282 occurrences across 11 files.
  Successful-run fatal-error, assertion, crash, unexpected-failure, build-error,
  product-watchdog, and test-failure scans are empty. Build output retains two
  pre-existing Swift warnings; test output retains destination-selection and
  non-failing CFPrefs refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 143 MB EX evidence root contains
  only rebuildable result bundles and logs, including the standalone compile
  log and the preserved pre-test automation-initialization attempt. It is
  removed after this durable record, followed by exact absence and process
  readback.
- Commit subject:
  `test(sync): migrate SYNC-036EX noisy preview projection`.

### SYNC-036EY Closure Record

- Status: completed. Each Noisy Option+Tab confirmation now uses a phase-scoped
  exact-element disappearance owner and the Noisy Option+Tab policy supplies
  its named terminal bound. Parent SYNC-036 remains open for the remaining
  owner-specific files.
- Oracle and design: the helper constructs the exact diagnostics query and
  starts its owner before posting `.confirm`. Its initial readback records state
  while the phase gate prevents pre-trigger absence from resolving. Exact
  command delivery completes the trigger phase, followed by an immediate
  readback and, only while unresolved, scheduled readbacks. Success requires a
  current-generation post-trigger snapshot with `exists == false`; the selected
  exact CG window and frontmost workflow evidence remain independent downstream
  Oracles. Expiry performs a final non-resolving readback and reports the phase,
  identifier, existence, generation, source, and waiter result.
- Lifecycle and retained time policy: the compatible four-second
  `FlowTabUITestNoisyOptionTabPolicy.switcherDismissalWatchdog` duration is
  solely a terminal failure bound. XCUI exposes no panel-dismissal callback, so
  the shared named 100ms cadence remains a condition-polling fallback after the
  immediate trigger readback. Each helper invocation owns the generation,
  deferred scheduler registration, expectation, explicit cancellation, and
  final readback. Resolution, cancel, replacement, and watchdog paths stop
  scheduled work; the workflow owns FlowTab and fixture App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036EX signed
  regression exercised the raw-four-second predicate in all four real noisy
  confirmation phases. Its full 9/9 path passed in 25.678 seconds, the visible
  topology took 24.205 seconds, and xcodebuild testing completed in 26.427
  seconds before every exact-window and cleanup Oracle resolved.
- UI and Pressure validation: the canonical wrapper rebuilt the changed UI-
  test source, regenerated the real fixture variants, and signed the Runner.
  The signed regression passed named-policy, phase-gated initial, immediate
  trigger, delayed scheduled, cancellation, final-watchdog, 200-generation
  replacement Pressure, and the real four-confirmation Space/fullscreen path
  7/7 in 23.982 seconds. The visible path took 23.049 seconds and xcodebuild
  testing completed in 24.771 seconds.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production confirmation, activation, filtering, configuration, module
  wiring, exact window identity, and path resolution are unchanged. The
  deterministic owner suite supplies lifecycle and scheduler Pressure; the
  signed real topology supplies UI orchestration evidence.
- Result, signing, and process evidence: the status ledger records
  `stage=completed`, zero fixture/build/signing/test/final exit codes, and a
  present result bundle. The xcresult records `status=succeeded` with seven
  tests. Host-context strict deep codesign accepts both the fixed App and signed
  Runner. Exact post-run process readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It remains a focused 384-line test file. Its
  raw numeric timeout inventory falls to six; one occurrence is removed from
  the repository UI-test inventory, leaving 281 occurrences across 11 files.
  Fatal-error, assertion, crash, unexpected-failure, build-error, product-
  watchdog, and test-failure scans are empty. Build output retains one pre-
  existing Swift warning; test output retains destination-selection and non-
  failing CFPrefs refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 452 KB EY evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EY noisy switcher dismissal`.

### SYNC-036EZ Closure Record

- Status: completed. All four Noisy Option+Tab post-confirm activation checks
  now use an owner-named exact-selected-window terminal policy. Parent SYNC-036
  remains open for the remaining owner-specific files.
- Oracle and design: the shared activation owner installs workspace activation
  and target-process AX focused/main-window observation before its immediate
  readback. Success requires the exact workflow bundle identifier and selected
  topmost on-screen CGWindowID. The persistent initial readback covers an
  activation that completed during panel dismissal; later notification or
  scheduled evidence can only resolve the same exact identity. The selected
  title, fixture topology, binding-confidence, and window-request checks remain
  independent downstream Oracles. Expiry reads back final frontmost bundle,
  window number/title/frame, active title, expected-title observability,
  generation, source, and waiter result.
- Lifecycle and retained time policy: the compatible twelve-second
  `FlowTabUITestNoisyOptionTabPolicy.exactSelectedWindowActivationWatchdog`
  duration is solely a terminal failure bound. CGWindow/Space publication has
  no complete notification, so the shared named 100ms serial cadence remains a
  conditional fallback while exact evidence is unmet. Each shared helper
  invocation owns workspace and AX registrations, one pending readback,
  generation rejection, cancellation, final readback, and cleanup; the
  workflow owns FlowTab and fixture App lifecycle.
- Baseline evidence: the immediately preceding committed SYNC-036EY regression
  exercised the raw-twelve-second activation bound across all four real noisy
  phases. Its full 7/7 path passed in 23.982 seconds, the visible topology took
  23.049 seconds, and xcodebuild testing completed in 24.771 seconds.
- UI and Pressure validation: the canonical wrapper rebuilt the changed UI-
  test source, regenerated fixture variants, and signed the Runner. The signed
  regression passed named-policy, observer-before-readback, exact initial,
  baseline-gated event, exact bundle/window identity, five slow readbacks,
  final-watchdog diagnostics, 100-generation cancellation/replacement
  Pressure, and the real four-activation path 8/8 in 23.094 seconds. The visible
  path took 22.052 seconds and xcodebuild testing completed in 24.320 seconds.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production activation, filtering, configuration, module wiring, exact window
  identity, and path resolution are unchanged. The deterministic shared-owner
  suite supplies observer and scheduler Pressure; the signed real topology
  supplies UI orchestration evidence.
- Result, signing, and process evidence: the status ledger records
  `stage=completed`, zero fixture/build/signing/test/final exit codes, and a
  present result bundle. The xcresult records `status=succeeded` with eight
  tests. Host-context strict deep codesign accepts both the fixed App and signed
  Runner. Exact post-run process readback finds no Flow Tab, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It remains a focused 390-line test file. Its
  raw numeric timeout inventory falls to five; one occurrence is removed from
  the repository UI-test inventory, leaving 280 occurrences across 11 files.
  Fatal-error, assertion, crash, unexpected-failure, build-error, product-
  watchdog, and test-failure scans are empty. Build and test output retains
  destination-selection warnings and non-failing CFPrefs refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 464 KB EZ evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036EZ noisy exact-window activation`.

### SYNC-036FA Closure Record

- Status: completed. The three post-confirm runtime-log contracts in every
  Noisy Option+Tab phase now share an owner-named terminal policy, and the
  phase explicitly releases its pre-confirm baseline after the final exact
  reconciliation readback. Parent SYNC-036 remains open for the remaining
  owner-specific files.
- Oracle and design: the baseline is created before `.confirm`. Exact command
  delivery remains the trigger evidence. Success then requires, from content
  after that same baseline, the selected App/PID/CGWindowID/title sticky
  `window-request`, a ready topology collection with a nonzero affected-window
  count and changed nonzero display/Space/window signature, and the same
  CGWindowID's transition to `verifiedFocusReadback`. Each regex wait performs
  an immediate readback and listens for file events; elapsed time does not
  satisfy any contract. Expiry reports the missing regex, file-event
  generations, bounded final log tail, observation source, and waiter result.
- Evidence-first calibration: the first signed eight-test regression passed
  all seven policy/observer/Pressure tests, then its normal1 reconciliation
  final readback at `19:49:26.845` lacked the exact selected-window marker.
  The runtime source recorded that same PID/CGWindowID transition at
  `19:49:26.917`, 72ms after the former eight-second bound. This demonstrated
  scheduler and I/O load changing the outcome while the product event still
  arrived, and calibrated the owner watchdog to twelve seconds. That
  diagnostic attempt executed 8 tests with 1 failure in 26.784 seconds; the
  real path took 23.557 seconds and xcodebuild testing completed in 37.474
  seconds.
- Lifecycle and retained time policy: the twelve-second
  `FlowTabUITestNoisyOptionTabPolicy.postConfirmReconciliationWatchdog` is
  solely a terminal failure bound. The shared named cancellable 200ms cadence
  remains a conditional fallback for missed file delivery. Every waiter owns
  its file registration, schedule, generation, cancellation, and final
  readback. The phase owns and explicitly cancels the shared baseline after
  all three conditions; deinitialization supplies safety cleanup. The workflow
  owns FlowTab and fixture App lifecycle.
- Baseline evidence: the immediately preceding committed SYNC-036EZ regression
  exercised the three raw-eight-second waits in all four real noisy phases. Its
  full 8/8 path passed in 23.094 seconds, the visible topology took 22.052
  seconds, and xcodebuild testing completed in 24.320 seconds.
- UI and Pressure validation: the canonical wrapper rebuilt the changed
  source and signed the Runner. Two host automation-mode initialization
  attempts ended before any test body ran and were classified by the wrapper
  with result bundles and zero residual processes. Reinstalling the stable
  fixed-path signed App restored automation. The already-built updated bundle
  then passed policy compatibility, regex delivery, file append and
  replacement, initial/event/cancel/watchdog contracts, and 100-iteration
  lifecycle Pressure 7/7 in 3.872 seconds; xcodebuild completed in 4.664
  seconds. Selector inspection caught an unmatched real-UI filter, and the
  corrected exact Noisy Option+Tab selector passed the four-phase real topology
  1/1 in 31.070 seconds; xcodebuild completed in 31.833 seconds. Together the
  affected validation is 8/8 with independent result bundles.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production request routing, reconciliation, topology signatures, binding
  confidence, configuration, module wiring, exact window identity, and path
  resolution are unchanged. The deterministic shared-observer tests supply
  scheduler and lifecycle Pressure; the signed fixture path supplies UI
  orchestration evidence.
- Result, signing, and process evidence: both successful status ledgers record
  `stage=completed`, zero fixture/signing/test/final exit codes, and present
  result bundles. Host-context strict deep codesign accepts the reinstalled
  fixed App and the signed Runner. Exact post-run process readback finds no
  Flow Tab, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It remains a focused 397-line test file. Three
  raw numeric timeout arguments are removed, leaving two in this file and 277
  across 11 UI-test files. Fatal-error, assertion, crash, product-watchdog,
  unexpected-failure, and test-failure scans of both successful executions are
  empty. Fixture and test logs retain only destination-selection warnings and
  non-failing CFPrefs refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 435 MB FA evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FA noisy reconciliation evidence`.

### SYNC-036FB Closure Record

- Status: completed. Both remaining pre-confirm Noisy Option+Tab log checks now
  use an owner-named terminal policy, and the entry workflow explicitly owns
  the prelaunch runtime-log baseline through fixture cleanup. Parent SYNC-036
  remains open for the remaining owner-specific files.
- Oracle and design: the baseline starts before the real fixture workflow,
  FlowTab launch, and global trigger. Success requires post-baseline content
  matching a nonzero filtered CG-only/fullscreen artifact or presentation
  duplicate, then each selected exact CGWindowID/title entry with
  `stickyBinding` and observed or topology-inferred Space evidence. Each wait
  installs its file-event observation before initial readback; persistent log
  content closes event gaps. Expiry reports the missing regex, baseline and
  current file-event generations, bounded final log tail, source, and waiter
  result.
- Lifecycle and retained time policy: the compatible eight-second
  `FlowTabUITestNoisyOptionTabPolicy.preConfirmEvidenceWatchdog` is solely a
  terminal failure bound. Filesystem delivery has no completion callback, so
  the shared named cancellable 200ms cadence remains a conditional fallback.
  Every regex waiter owns and cancels its observation, schedule, and final
  readback. `runSwitcherPanelOptionTabWindowStateRoundTrip` owns the shared
  baseline and cancels it with `defer` after fixture orchestration and cleanup;
  baseline deinitialization remains safety cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FA validation
  exercised both raw-eight-second pre-confirm waits in all four real phases.
  Its deterministic policy/observer/Pressure selection passed 7/7 in 3.872
  seconds, and its exact real topology passed 1/1 in 31.070 seconds with
  xcodebuild testing completed in 31.833 seconds.
- UI and Pressure validation: the canonical wrapper rebuilt both touched UI-
  test sources, regenerated the real fixture variants, and signed the Runner.
  Named-policy compatibility, regex delivery, initial/file-event readback,
  append/replacement handling, cancellation, final-watchdog diagnostics, and
  100-iteration lifecycle Pressure passed together with the four-phase signed
  Noisy Option+Tab path 8/8 in 29.264 seconds. The visible real topology took
  26.153 seconds and xcodebuild testing completed in 30.046 seconds.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production filtering, layer projection, topology evidence, configuration,
  module wiring, exact window identity, and path resolution are unchanged. The
  deterministic shared-owner tests supply lifecycle Pressure; the signed
  fixture topology supplies UI orchestration evidence.
- Result, signing, and process evidence: the status ledger records
  `stage=completed`, zero fixture/build/signing/test/final exit codes, and a
  present result bundle. Host-context strict deep codesign accepts both the
  fixed App and signed Runner. Exact post-run process readback finds no Flow
  Tab, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The Noisy owner is exactly 400 lines. The
  oversized runtime-truth entrypoint remains at its prior 812 lines and this
  slice isolates its change to baseline lifetime ownership. Both raw numeric
  timeout arguments are removed, leaving none in the Noisy owner and 275
  across 10 UI-test files. Fatal-error, assertion, crash, product-watchdog,
  unexpected-failure, and test-failure scans are empty. Fixture, build, and
  test output retain one destination-selection warning each and 18 non-failing
  CFPrefs refresh diagnostics.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 468 KB FB evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FB noisy pre-confirm evidence`.

### SYNC-036FC Closure Record

- Status: completed. Ordinary and noisy Option+Tab initial fullscreen-sibling
  readiness now use one runtime-truth policy owned outside the oversized entry
  file. Parent SYNC-036 remains open for the remaining owner-specific files.
- Oracle and design: both helpers start after fixture launch and before FlowTab
  launch. The ordinary scope accepts only the target bundle as frontmost plus
  its topmost on-screen CG window matching the exact title or titleless
  fullscreen geometry. The noisy collection accepts only a matching on-screen
  workflow window from the target process while allowing its independent CG
  siblings. Workspace activation and target-process AX focused/main-window
  observation are registered before the initial readback. Expiry reports the
  expected scope/title and the final frontmost bundle, window identities,
  titles, frames, generation, source, and waiter result.
- Lifecycle and retained time policy: the compatible twelve-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.optionTabInitialTopology` is solely
  a terminal failure bound. CGWindow/Space publication has no complete
  callback, so the shared named cancellable 100ms cadence remains a conditional
  fallback. Each helper invocation owns its registration, generation,
  scheduled readback, final readback, cancellation, and diagnostics; the real
  fixture workflow owns FlowTab and fixture App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FB regression
  exercised the noisy raw-twelve-second branch along with its remaining noisy
  contracts 8/8 in 29.264 seconds. The real noisy topology took 26.153 seconds
  and xcodebuild testing completed in 30.046 seconds.
- Evidence-first validation split: the first rebuilt 15-test attempt compiled
  the new policy and passed the policy, both owner suites, both stale-event
  Pressure paths, and noisy UI. The following ordinary test reused the same
  fixture bundle after the noisy topology and its independent Switcher App
  projection reported the exact target entry with `windowCount=0` instead of
  2. The initial topology owner had already resolved. Cleanup left no related
  process. Running the ordinary topology in its own canonical invocation then
  passed 1/1 in 16.606 seconds, with xcodebuild testing completed in 17.907
  seconds. This retains the exact cross-test topology-reuse diagnostic while
  isolating mutually exclusive real Space fixtures.
- UI and Pressure validation: a clean second result bundle passed the policy,
  both deterministic owner suites, exact initial/wrong-bundle/titleless/event/
  slow-scheduling/final-watchdog rules, both stale-generation Pressure paths,
  and noisy UI 14/14 in 26.537 seconds. The noisy real topology took 24.708
  seconds and xcodebuild testing completed in 27.272 seconds. Together the two
  clean result bundles pass the affected validation 15/15.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production fixture launch, Space topology, window matching, configuration,
  module wiring, and path resolution are unchanged. The deterministic UI-
  target owner suites supply scheduler and stale-generation Pressure; the two
  signed fixture invocations supply ordinary and noisy UI evidence.
- Result, signing, and process evidence: both successful status ledgers record
  `stage=completed`, zero fixture/signing/test/final exit codes, and present
  result bundles. The initial build stage also completed before its later UI
  assertion. Host-context strict deep codesign accepts both the fixed App and
  signed Runner. Exact final process readback finds no Flow Tab, UI-test
  Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: the new 16-line policy source and affected
  entry source parse directly, the project file passes `plutil -lint`, and
  `git diff --check` passes. The policy is registered exactly once in the UI
  group and Sources phase. The oversized entrypoint remains at its prior 812
  lines. Two raw numeric timeout arguments are removed, leaving 273 across 10
  UI-test files. Fatal-error, assertion, crash, product-watchdog, unexpected-
  failure, and test-failure scans of both successful invocations are empty.
  Fixture/build/test logs retain destination-selection warnings and 3/29 non-
  failing CFPrefs refresh diagnostics in the ordinary/noisy result bundles.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 157 MB FC evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FC initial Option+Tab topology`.

### SYNC-036FD Closure Record

- Status: completed. The first ordinary Option+Tab window-cycle active-Space
  check now consumes a runtime-truth watchdog policy owned outside the
  oversized entry file. Parent SYNC-036 remains open for the remaining
  owner-specific literals.
- Oracle and design: after exact global-trigger delivery, exact Switcher App
  projection, and exact transition into window-cycle, success requires the
  persistent target-process topmost CG window to match the primary fullscreen
  title or its titleless fullscreen geometry on the active Space. FlowTab may
  remain frontmost. Workspace activation and target-process AX focused/main-
  window observation precede the initial readback. Expiry reports the expected
  title and final frontmost bundle, target CGWindowID/title/frame, generation,
  source, and waiter result.
- Lifecycle and retained time policy: the compatible four-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.optionTabInitialWindowStateTopology`
  is solely a terminal failure bound. CGWindow/Space publication has no
  complete callback, so the shared named cancellable 100ms cadence remains a
  conditional fallback. The helper invocation owns observer registration,
  generation, scheduled and final readbacks, cancellation, and diagnostics;
  the real workflow owns FlowTab and fixture App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FC ordinary
  invocation exercised the raw-four-second branch and passed 1/1 in 16.606
  seconds, with xcodebuild testing completed in 17.907 seconds.
- Evidence-first validation split: the rebuilt nine-test invocation compiled
  the policy and passed all eight policy/observer tests. Its real UI test then
  stopped before this owner when the independent Switcher App projection
  reported the exact fixture entry with `windowCount=0` while fixture CGWindow
  readback still contained both expected windows. The isolated canonical real-
  topology invocation passed 1/1 in 18.734 seconds, with xcodebuild testing
  completed in 19.576 seconds, and exercised the new policy call. Cleanup after
  both invocations left no related process.
- UI and Pressure validation: a separate clean canonical invocation passed the
  policy plus exact initial, wrong-frontmost-bundle, titleless-fullscreen,
  FlowTab-frontmost active-Space, slow-scheduling, stale-generation Pressure,
  and final-watchdog diagnostics 8/8 in 1.122 seconds; xcodebuild testing
  completed in 1.849 seconds. Together the two clean result bundles pass the
  affected validation 9/9.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production Switcher state, Space topology, window matching, configuration,
  module wiring, and path resolution are unchanged. The deterministic UI-
  target suite supplies scheduler and stale-generation Pressure; the signed
  fixture invocation supplies the affected visible UI path.
- Result, signing, and process evidence: both successful status ledgers record
  `stage=completed`, zero signing/test/final exit codes, and present result
  bundles; the real fixture ledger also records zero fixture exit codes. Host-
  context strict deep codesign accepts both the fixed App and signed Runner.
  Exact final process readback finds no Flow Tab, UI-test Runner, xcodebuild, or
  fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  policy file contains 23 lines. The oversized entrypoint remains at its prior
  812 lines. One raw numeric timeout argument is removed, leaving 272 across 10
  UI-test files. Fatal-error, assertion, sanitizer, main-thread-checker, XPC,
  permission-denial, crash, and test-failure scans of both successful
  invocations are empty. Fixture and test logs retain three expected
  destination-selection warnings and 3/16 non-failing CFPrefs refresh
  diagnostics in the real/deterministic result bundles.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 156 MB FD evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FD initial window-state topology`.

### SYNC-036FE Closure Record

- Status: completed. Both ordinary Option+Tab confirmations now establish the
  exact-window activation observer before posting the confirm command. Parent
  SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: `triggerAndWaitForFrontmostWorkflowWindow` registers
  workspace activation and target-process AX focused/main-window sources
  before its initial readback. A matching pre-trigger state remains behind the
  acceptance gate until the exact confirm command and delivery evidence
  return. Success then requires the fixture bundle to be frontmost with the
  selected exact standard or fullscreen `CGWindowID`; title and frame remain
  diagnostic evidence. The panel-dismissal predicate stays an independent
  persistent readback after exact activation resolves.
- Lifecycle and retained time policy: the compatible twelve-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.optionTabConfirmedWindowActivation`
  is solely a terminal failure bound. CGWindowList has no exact publication
  callback and AX events may be unavailable or coalesced, so the shared named
  cancellable 100ms cadence remains a conditional fallback. Each helper
  invocation owns its generation, workspace token, AX observer and run-loop
  source, schedule, expectation, final readback, and cancellation; the real
  workflow owns FlowTab and fixture App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FD isolated
  ordinary invocation exercised both post-confirm raw-twelve-second waits and
  passed 1/1 in 18.734 seconds, with xcodebuild testing completed in 19.576
  seconds.
- Deterministic and Pressure validation: the rebuilt canonical invocation
  passed the policy plus exact initial state, registration-before-readback and
  trigger gating, wrong-bundle/window rejection, five delayed readbacks, 100
  cancel/restart/stale-generation iterations with duplicate delivery, and
  final-watchdog diagnostics 7/7 in 1.161 seconds. Xcodebuild testing completed
  in 1.857 seconds.
- Evidence-first UI validation: the first isolated real invocation stopped
  before this owner when the independent Switcher App projection reported the
  exact fixture entry with `windowCount=0` while fixture CGWindow readback
  retained both expected windows. It failed 1/1 in 16.423 seconds, with
  xcodebuild testing completed in 26.836 seconds. Exact process cleanup was
  empty. A clean isolated retry exercised both new trigger-gated confirmation
  paths and passed 1/1 in 17.449 seconds, with xcodebuild testing completed in
  18.359 seconds. Together the clean deterministic and UI result bundles pass
  the affected validation 8/8.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production confirmation, activation, window identity, configuration, module
  wiring, and path resolution are unchanged. The deterministic UI-target suite
  supplies observer lifecycle, scheduling, cancellation, duplicate-event, and
  stale-generation Pressure; the signed fixture invocation supplies the
  affected visible UI path.
- Result, signing, and process evidence: both successful status ledgers record
  `stage=completed`, zero signing/test/final exit codes, and present result
  bundles; the real fixture ledger also records zero fixture exit codes. Host-
  context strict deep codesign accepts both the fixed App and signed Runner.
  Exact final process readback finds no Flow Tab, UI-test Runner, xcodebuild, or
  fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly,
  the project file passes `plutil -lint`, and `git diff --check` passes. The
  policy file contains 30 lines and the oversized entrypoint remains at its
  prior 812 lines. Two raw numeric timeout arguments are removed, leaving 270
  across 10 UI-test files. Fatal-error, assertion, sanitizer, main-thread-
  checker, XPC, permission-denial, crash, and test-failure scans of both
  successful invocations are empty. Build, fixture, and test logs retain four
  expected destination-selection warnings and 14/3 non-failing CFPrefs refresh
  diagnostics in the deterministic/real result bundles.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 155 MB FE evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FE confirmed window activation`.

### SYNC-036FF Closure Record

- Status: completed. Both ordinary Option+Tab confirmations now start exact
  diagnostics-element disappearance observation before the confirm action.
  Parent SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: the new confirmation helper starts
  `FlowTabUITestElementNonExistenceObservationOwner` before delegating the
  single confirm action to the independent exact-window activation owner. Its
  initial `exists` readback remains gated while activation evidence resolves.
  After the trigger scope returns it performs an immediate exact diagnostics-
  element readback and accepts only `exists == false`; delayed scheduling can
  alter completion latency without supplying success.
- Lifecycle and retained time policy: the compatible four-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.optionTabSwitcherDismissal` is solely
  a terminal failure bound. XCUI exposes no element-disappearance notification,
  so the shared named cancellable 100ms cadence activates only while the post-
  trigger element remains present. The outer helper owns the disappearance
  generation, deferred schedule, expectation, final readback, and cancellation;
  the nested activation helper independently owns workspace/AX observation and
  its cancellation. The real workflow owns FlowTab and fixture App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FE isolated
  ordinary invocation exercised both post-confirm raw-four-second predicate
  waits and passed 1/1 in 17.449 seconds, with xcodebuild testing completed in
  18.359 seconds.
- Deterministic and Pressure validation: the rebuilt canonical invocation
  passed the policy plus gated initially absent evidence, immediate post-trigger
  readback, delayed scheduled evidence, five unresolved slow-scheduling
  readbacks followed by the same successful result, cancellation, watchdog
  final readback, and 200 cancel/restart/stale-generation iterations with a
  duplicate trigger 7/7 in 1.060 seconds. Xcodebuild testing completed in 1.765
  seconds.
- Evidence-first UI validation: the first isolated real invocation stopped
  before the new helper when the independent Switcher App projection reported
  the exact fixture entry with `windowCount=0` while fixture CGWindow readback
  retained both expected windows. It failed 1/1 in 16.418 seconds, with
  xcodebuild testing completed in 27.780 seconds. Exact process cleanup was
  empty. A clean isolated retry exercised both new confirmation/dismissal
  paths and passed 1/1 in 14.124 seconds, with xcodebuild testing completed in
  15.327 seconds. Together the clean deterministic and UI result bundles pass
  the affected validation 8/8.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production dismissal, activation, confirmation, configuration, module
  wiring, and path resolution are unchanged. The deterministic UI-target suite
  supplies scheduling, cancellation, duplicate-trigger, stale-generation, and
  lifecycle Pressure; the signed fixture invocation supplies the affected
  visible UI path.
- Result, signing, and process evidence: both successful status ledgers record
  `stage=completed`, zero signing/test/final exit codes, and present result
  bundles; the real fixture ledger also records zero fixture exit codes. Host-
  context strict deep codesign accepts both the fixed App and signed Runner.
  Exact final process readback finds no Flow Tab, UI-test Runner, xcodebuild, or
  fixture process.
- Diagnostics and Process/Tooling: all four affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The new 54-line helper has one file reference, one UI group
  membership, and one Sources membership. The policy and regression files
  contain 37 and 218 lines; the oversized entrypoint shrinks from 812 to 808
  lines. Two raw numeric timeout arguments are removed, leaving 268 across 10
  UI-test files. Fatal-error, assertion, sanitizer, main-thread-checker, XPC,
  permission-denial, crash, and test-failure scans of both successful
  invocations are empty. Build, fixture, and test logs retain four expected
  destination-selection warnings, one unrelated existing unused-local
  warning, and 14/3 non-failing CFPrefs refresh diagnostics in the
  deterministic/real result bundles.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 156 MB FF evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FF confirmation dismissal`.

### SYNC-036FG Closure Record

- Status: completed. The second ordinary Option+Tab presentation now starts
  exact workflow-window activation observation before the global relaunch.
  Parent SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: the new relaunch helper starts
  `FlowTabUITestWorkflowWindowActivationObservationOwner` before delegating the
  single global relaunch. A matching baseline remains gated while relaunch
  delivery and exact Switcher App/window-state readiness resolve. After the
  trigger scope returns, an immediate readback accepts only the target bundle
  identifier plus the previously selected standard CGWindowID; scheduling
  pressure can alter completion latency without supplying success.
- Lifecycle and retained time policy: the compatible four-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.optionTabRelaunchWindowTopology` is
  solely a terminal failure bound. The activation owner observes workspace
  activation and target-process AX focused/main-window events. Its shared named
  cancellable 100ms fallback activates only while exact readback remains
  unresolved because CGWindowList exposes no exact publication callback and AX
  events may be coalesced. The helper invocation owns the generation,
  observations, deferred schedule, expectation, final readback, and
  cancellation; the real workflow owns FlowTab and fixture App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FF isolated
  ordinary invocation exercised the post-relaunch raw-four-second exact-window
  wait and passed 1/1 in 14.124 seconds, with xcodebuild testing completed in
  15.327 seconds.
- Deterministic and Pressure validation: the rebuilt canonical invocation
  passed policy compatibility plus exact initial state, observer-before-
  readback and trigger gating, exact bundle/window rejection, five unresolved
  slow-scheduling readbacks followed by the same successful result,
  cancellation/replacement and stale-generation Pressure, and watchdog final
  readback 7/7 in 1.066 seconds. Xcodebuild testing completed in 2.311 seconds;
  the Pressure case completed 100 lifecycle iterations.
- Evidence-first UI validation: the first isolated real invocation stopped
  before the new helper when the independent Switcher App projection reported
  the exact fixture entry with `windowCount=0` while fixture CGWindow readback
  retained both expected windows. It failed 1/1 in 16.568 seconds, with
  xcodebuild testing completed in 27.136 seconds. Exact process cleanup was
  empty. A clean isolated retry exercised the new pre-relaunch owner and exact
  identity readback and passed 1/1 in 16.365 seconds, with xcodebuild testing
  completed in 17.552 seconds. Together the clean deterministic and UI result
  bundles pass the affected validation 8/8.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production relaunch, Switcher readiness, Space topology, window identity,
  configuration, module wiring, and path resolution are unchanged. The
  deterministic UI-target suite supplies event ordering, readback,
  cancellation, stale-generation, slow-scheduling, and lifecycle Pressure; the
  signed fixture invocation supplies the affected visible UI path.
- Result, signing, and process evidence: both successful status ledgers record
  `stage=completed`, zero signing/test/final exit codes, and present result
  bundles; the real fixture ledger also records zero fixture exit codes. Host-
  context strict deep codesign accepts both the fixed App and signed Runner.
  Exact final process readback finds no Flow Tab, UI-test Runner, xcodebuild, or
  fixture process.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The new 43-line helper has one file reference, one UI group
  membership, and one Sources membership. The policy file contains 44 lines;
  the oversized entrypoint shrinks from 808 to 802 lines. One raw numeric
  timeout argument is removed, leaving 267 across 10 UI-test files. Fatal-
  error, assertion, sanitizer, main-thread-checker, XPC, permission-denial,
  crash, and test-failure scans of both successful invocations are empty.
  Build, fixture, and test logs retain four expected destination-selection
  warnings and 14/3 non-failing CFPrefs refresh diagnostics in the
  deterministic/real result bundles.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 156 MB FG evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FG relaunch window topology`.

### SYNC-036FH Closure Record

- Status: completed. Ordinary and noisy Window Search initial fullscreen-
  sibling readiness now share a Window-Search-owned named runtime-truth policy.
  Parent SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: both helpers run after fixture launch and before FlowTab
  launch. The ordinary scope accepts only the target bundle as frontmost plus
  its topmost on-screen CG window matching the exact title or titleless
  fullscreen geometry. The noisy collection accepts only a matching on-screen
  workflow window from the target process while allowing independent CG
  siblings. Workspace activation and target-process AX focused/main-window
  observation are registered before the initial readback. Expiry reports the
  expected scope/title and final frontmost bundle, window identities, titles,
  frames, generation, source, and waiter result.
- Lifecycle and retained time policy: the compatible twelve-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.windowSearchInitialTopology` is
  solely a terminal failure bound. CGWindow/Space publication has no complete
  callback, so the shared named cancellable 100ms cadence remains a conditional
  fallback. Each helper invocation owns its registration, generation,
  scheduled readback, final readback, cancellation, and diagnostics; the real
  fixture workflow owns FlowTab and fixture App cleanup.
- Baseline evidence: the committed SYNC-034AE canonical run exercised both raw
  twelve-second branches and the complete ordinary/noisy Window Search paths
  2/2 in 113.697 seconds. The ordinary path completed in 39.818 seconds and the
  noisy path in 73.879 seconds while requiring exact Search, CGWindow, and
  final fixture-selection evidence.
- Deterministic and Pressure validation: the rebuilt canonical invocation
  passed policy compatibility plus both complete observation-owner suites
  13/13 in 1.898 seconds; xcodebuild testing completed in 3.126 seconds. The
  suites cover exact initial state, wrong bundle/window, titleless fullscreen
  geometry, active-Space evidence while FlowTab is frontmost, event and five-
  readback slow scheduling, final-watchdog diagnostics, and 100 cancel/restart/
  stale-generation iterations for each of the single-window and collection
  owners.
- Real UI validation: the ordinary topology passed 1/1 in 20.119 seconds, with
  xcodebuild testing completed in 20.800 seconds. The independently prepared
  noisy topology passed 1/1 in 29.353 seconds, with xcodebuild testing completed
  in 30.461 seconds. Together the three clean result bundles pass the affected
  validation 15/15 without cross-test fixture reuse.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production fixture launch, Search behavior, Space topology, window matching,
  configuration, module wiring, and path resolution are unchanged. The
  deterministic UI-target suites supply scheduler, cancellation,
  stale-generation, and lifecycle Pressure; the two signed fixture invocations
  supply ordinary and noisy visible UI evidence.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable fixture/signing/build/test/final exit
  codes, and present result bundles. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact final process readback finds no
  Flow Tab UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The policy file contains 51 lines and the
  oversized entrypoint remains at 802 lines. Two raw numeric timeout arguments
  are removed, leaving 265 across 10 UI-test files. Build, fixture, and test
  logs retain six expected destination-selection warnings and 26/3/3 non-
  failing CFPrefs refresh diagnostics in the deterministic/ordinary/noisy
  invocations. Compiler-warning, fatal-error, assertion, sanitizer, main-
  thread-checker, XPC, permission-denial, crash, watchdog-expiry, and test-
  failure scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 788 KB FH evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FH initial Window Search topology`.

### SYNC-036FI Closure Record

- Status: completed. Ordinary and noisy Window Search first-presentation
  topology now use one Search-presentation-owned named runtime-truth policy.
  Parent SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: both helpers run after exact Search trigger delivery,
  Search-input readiness, workflow-window row validation, and committed-index
  readiness. The ordinary scope accepts only the target process's topmost
  active-Space CG window matching the exact title or titleless fullscreen
  geometry while FlowTab may be frontmost. The noisy collection accepts only a
  matching on-screen workflow window from the target process while allowing
  independent CG siblings. The persistent post-presentation topology, rather
  than elapsed time, is the success Oracle.
- Lifecycle and retained time policy: the compatible four-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.windowSearchInitialPresentationTopology`
  is solely a terminal failure bound. Workspace activation and target-process
  AX focused/main-window observation register before the initial readback.
  CGWindow/Space publication has no complete callback, so the shared named
  cancellable 100ms cadence remains a conditional fallback. Each helper
  invocation owns its registration, generation, scheduled readback, final
  readback, cancellation, and diagnostics; the real fixture workflow owns
  FlowTab and fixture App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FH canonical
  invocations exercised both raw-four-second first-presentation branches. The
  ordinary path passed 1/1 in 20.119 seconds with xcodebuild completion in
  20.800 seconds; the separately prepared noisy path passed 1/1 in 29.353
  seconds with xcodebuild completion in 30.461 seconds.
- Deterministic and Pressure validation: the rebuilt canonical invocation
  passed policy compatibility plus both complete observation-owner suites
  13/13 in 1.988 seconds; xcodebuild testing completed in 3.158 seconds. The
  suites cover exact initial state, wrong bundle/window, titleless fullscreen
  geometry, active-Space evidence while FlowTab is frontmost, event and five-
  readback slow scheduling, final-watchdog diagnostics, and 100 cancel/restart/
  stale-generation iterations for each of the single-window and collection
  owners.
- Real UI validation: the ordinary topology passed 1/1 in 28.927 seconds, with
  xcodebuild testing completed in 29.666 seconds. The independently prepared
  noisy topology passed 1/1 in 29.360 seconds, with xcodebuild testing completed
  in 30.046 seconds. The slower ordinary scheduling changed completion latency
  while preserving the same exact Space/CGWindow result. Together the three
  clean result bundles pass the affected validation 15/15 without cross-test
  fixture reuse.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production Search state, committed-index readiness, Space topology, window
  matching, configuration, module wiring, and path resolution are unchanged.
  The deterministic UI-target suites supply scheduler, cancellation,
  stale-generation, and lifecycle Pressure; the two signed fixture invocations
  supply ordinary and noisy visible UI evidence.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable fixture/signing/build/test/final exit
  codes, and present result bundles. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact final process readback finds no
  Flow Tab, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The policy file contains 58 lines and the
  oversized entrypoint remains at 802 lines. Two raw numeric timeout arguments
  are removed, leaving 263 across 10 UI-test files. Build, fixture, and test
  logs retain six expected destination-selection warnings and 26/3/3 non-
  failing CFPrefs refresh diagnostics in the deterministic/ordinary/noisy
  invocations. Compiler-warning, fatal-error, assertion, sanitizer, main-
  thread-checker, XPC, permission-denial, crash, watchdog-expiry, and test-
  failure scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 788 KB FI evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FI initial Search presentation topology`.

### SYNC-036FJ Closure Record

- Status: completed. All six ordinary/noisy Window Search confirmations now
  start exact workflow-window activation observation before `.searchConfirm`.
  Parent SYNC-036 remains open for panel dismissal and the remaining owner-
  specific literals.
- Oracle and design: the new Search-confirmation helper starts
  `FlowTabUITestWorkflowWindowActivationObservationOwner` before posting the
  single command. A matching baseline remains gated until exact command
  delivery returns. The immediate post-trigger readback then accepts only the
  target bundle identifier plus the selected CGWindowID; scheduling pressure
  and independent CG siblings cannot supply success.
- Lifecycle and retained time policy: the compatible twelve-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.windowSearchConfirmedWindowActivation`
  is solely a terminal failure bound. The owner observes workspace activation
  and target-process AX focused/main-window events. Its shared named cancellable
  100ms fallback activates only while exact readback remains unresolved because
  CGWindowList exposes no exact publication callback and AX events may be
  coalesced. Each helper invocation owns its generation, observations, deferred
  schedule, expectation, final readback, and cancellation; the real workflow
  owns FlowTab and fixture App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FI canonical
  invocations exercised both ordinary and all four noisy raw-twelve-second
  post-confirm activation waits. The ordinary path passed 1/1 in 28.927 seconds
  with xcodebuild completion in 29.666 seconds; the independently prepared
  noisy path passed 1/1 in 29.360 seconds with xcodebuild completion in 30.046
  seconds.
- Deterministic and Pressure validation: the rebuilt canonical invocation
  passed policy compatibility plus exact initial state, observer-before-
  readback and trigger gating, exact bundle/window rejection, five unresolved
  slow-scheduling readbacks followed by the same successful result,
  cancellation/replacement and stale-generation Pressure, and watchdog final
  readback 7/7 in 1.080 seconds. Xcodebuild testing completed in 1.897 seconds;
  the Pressure case completed 100 lifecycle iterations.
- Real UI validation: the ordinary two-confirmation topology passed 1/1 in
  20.035 seconds, with xcodebuild testing completed in 20.792 seconds. The
  independently prepared noisy four-confirmation topology passed 1/1 in 30.750
  seconds, with xcodebuild testing completed in 31.496 seconds. Together the
  three clean result bundles pass the affected validation 9/9 without cross-
  test fixture reuse.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production Search confirmation, activation, window identity, configuration,
  module wiring, and path resolution are unchanged. The deterministic UI-
  target suite supplies event ordering, readback, cancellation,
  stale-generation, slow-scheduling, and lifecycle Pressure; the two signed
  fixture invocations supply ordinary and noisy visible UI evidence.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable fixture/signing/build/test/final exit
  codes, and present result bundles. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact final process readback finds no
  Flow Tab, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The new 28-line helper has one file reference, one UI group
  membership, and one Sources membership. The policy file contains 65 lines;
  the oversized entrypoint shrinks from 802 to 792 lines. Three raw numeric
  timeout arguments are removed, leaving 260 across 10 UI-test files. Build,
  fixture, and test logs retain six expected destination-selection warnings,
  one unrelated existing unused-local warning, and 14/3/3 non-failing CFPrefs
  refresh diagnostics in the deterministic/ordinary/noisy invocations. Fatal-
  error, assertion, sanitizer, main-thread-checker, XPC, permission-denial,
  crash, watchdog-expiry, and test-failure scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 3.6 MB FJ evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FJ Search confirmation activation`.

### SYNC-036FK Closure Record

- Status: completed. All six ordinary/noisy Window Search confirmations now
  start exact Search-input disappearance observation before `.searchConfirm`.
  Parent SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: the composed confirmation helper starts an outer
  `FlowTabUITestElementNonExistenceObservationOwner` before the nested exact-
  window activation owner posts the single command. Initial absence remains
  gated while activation evidence resolves. The outer owner then performs an
  immediate exact Search-input readback and accepts only `exists == false`;
  scheduling and Space-animation pressure cannot supply success.
- Lifecycle and retained time policy: the compatible four-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.windowSearchInputDismissal` is solely
  a terminal failure bound. XCUI exposes no element-disappearance notification,
  so the shared named cancellable 100ms cadence activates only while the post-
  trigger element remains present. The composed helper invocation owns the
  disappearance generation, deferred schedule, expectation, final readback,
  and cancellation; the nested activation owner independently owns workspace/
  AX observation and cancellation. The real workflow owns FlowTab and fixture
  App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FJ canonical
  invocations exercised both ordinary and all four noisy raw-four-second post-
  confirm disappearance waits. The ordinary path passed 1/1 in 20.035 seconds
  with xcodebuild completion in 20.792 seconds; the independently prepared
  noisy path passed 1/1 in 30.750 seconds with xcodebuild completion in 31.496
  seconds.
- Deterministic and Pressure validation: the rebuilt canonical invocation
  passed policy compatibility plus gated initial absence, immediate post-
  trigger readback, delayed scheduled evidence, five unresolved slow-
  scheduling readbacks followed by the same successful result, cancellation,
  watchdog final readback, and 200 cancel/restart/stale-generation iterations
  with duplicate delivery 7/7 in 1.139 seconds. Xcodebuild testing completed in
  2.129 seconds.
- Real UI validation: the ordinary two-dismissal topology passed 1/1 in 20.401
  seconds, with xcodebuild testing completed in 21.172 seconds. The independently
  prepared noisy four-dismissal topology passed 1/1 in 24.821 seconds, with
  xcodebuild testing completed in 25.541 seconds. Runtime window coordinates
  varied under real Space animation while exact input disappearance and
  selected CGWindow identity remained stable. Together the three clean result
  bundles pass the affected validation 9/9 without cross-test fixture reuse.
- Validation scope: FlowTabCore Unit and app Behavior are not relevant because
  production dismissal, activation, Search confirmation, configuration, module
  wiring, and path resolution are unchanged. The deterministic UI-target suite
  supplies scheduling, cancellation, duplicate-delivery, stale-generation, and
  lifecycle Pressure; the two signed fixture invocations supply ordinary and
  noisy visible UI evidence.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable fixture/signing/build/test/final exit
  codes, and present result bundles. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact final process readback finds no
  Flow Tab, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The confirmation helper grows from 28 to 57 lines while retaining one
  responsibility and its existing single UI group/Sources membership. The
  policy file contains 72 lines; the oversized entrypoint shrinks from 792 to
  789 lines. Three raw numeric timeout arguments are removed, leaving 257
  across 10 UI-test files. Build, fixture, and test logs retain six expected
  destination-selection warnings and 14/3/3 non-failing CFPrefs refresh
  diagnostics in the deterministic/ordinary/noisy invocations. Compiler-
  warning, fatal-error, assertion, sanitizer, main-thread-checker, XPC,
  permission-denial, crash, watchdog-expiry, and test-failure scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 720 KB FK evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FK Search input dismissal`.

### SYNC-036FL Closure Record

- Status: completed. The ordinary second Window Search presentation and all
  three noisy subsequent presentations now start exact workflow-window
  activation observation before relaunch. Parent SYNC-036 remains open for the
  remaining owner-specific literals.
- Oracle and design: the new relaunch helper starts
  `FlowTabUITestWorkflowWindowActivationObservationOwner` before invoking the
  existing relaunch/readiness closure. A matching pre-trigger baseline remains
  gated while Search trigger delivery, keyboard readiness, exact Search data,
  and committed-generation readbacks resolve. After the trigger scope returns,
  an immediate readback accepts only the target bundle identifier plus the
  previously selected exact CGWindowID; scheduling and Space-animation pressure
  can alter completion latency without supplying success.
- Lifecycle and retained time policy: the compatible four-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.windowSearchRelaunchWindowTopology`
  is solely a terminal failure bound. The activation owner observes workspace
  activation and target-process AX focused/main-window events. Its shared named
  cancellable 100ms fallback activates only while exact readback remains
  unresolved because CGWindowList exposes no exact publication callback and AX
  events may be coalesced. Each helper invocation owns the generation,
  observations, deferred schedule, expectation, final readback, and
  cancellation; the real workflow owns FlowTab and fixture App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FK canonical
  invocations exercised the ordinary raw-four-second relaunch topology check
  and all three noisy checks. The ordinary path passed 1/1 in 20.401 seconds
  with xcodebuild completion in 21.172 seconds; the independently prepared
  noisy path passed 1/1 in 24.821 seconds with xcodebuild completion in 25.541
  seconds.
- Deterministic and Pressure validation: the rebuilt canonical invocation
  passed policy compatibility plus exact initial state, observer-before-
  readback and trigger gating, exact bundle/window rejection, five unresolved
  slow-scheduling readbacks followed by the same successful result,
  cancellation/replacement and stale-generation Pressure, and watchdog final
  readback 7/7 in 1.124 seconds. Xcodebuild testing completed in 2.041 seconds;
  the Pressure case completed 100 lifecycle iterations.
- Real UI validation: the ordinary one-relaunch topology passed 1/1 in 18.131
  seconds, with xcodebuild testing completed in 19.269 seconds. The independently
  prepared noisy three-relaunch topology passed 1/1 in 26.897 seconds, with
  xcodebuild testing completed in 27.656 seconds. Runtime window coordinates
  varied during real Space transitions while every accepted bundle and
  CGWindowID remained exact. Together the three clean result bundles pass the
  affected validation 9/9 without cross-test fixture reuse.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production relaunch, Search readiness, Space topology,
  window identity, configuration, module wiring, and path resolution are
  unchanged. The deterministic UI-target suite supplies event ordering,
  readback, cancellation, stale-generation, slow-scheduling, and lifecycle
  Pressure; the two signed fixture invocations supply the affected visible UI
  paths.
- Result, signing, and process evidence: all three status ledgers record
  `stage=completed`, zero applicable fixture/signing/build/test/final exit
  codes, and present result bundles. Host-context strict deep codesign accepts
  both the fixed App and signed Runner. Exact final process readback finds no
  Flow Tab UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The new 35-line helper has one file reference, one UI group
  membership, and one Sources membership. The policy file contains 79 lines;
  the oversized entrypoint remains at 789 lines. Two raw numeric timeout
  arguments are removed, leaving 255 across 10 UI-test files. Build, fixture,
  and test logs retain six expected destination-selection warnings and 14/3/3
  non-failing CFPrefs refresh diagnostics in the deterministic/ordinary/noisy
  invocations. The build also retains one pre-existing unused-value compiler
  warning in `FlowTabUITests+SpaceFixtureEdgeInputsWorkflow.swift`, introduced
  by commit `939960af` and outside this owner. Fatal-error, assertion, sanitizer,
  main-thread-checker, XPC, permission-denial, crash, watchdog-expiry, generic
  compiler-error, and test-failure scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 3.6 MB FL evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FL Search relaunch window topology`.

### SYNC-036FM Closure Record

- Status: completed. All six ordinary/noisy initial and subsequent Window
  Search presentations now start diagnostics-accessibility publication
  observation before the Search trigger. Parent SYNC-036 remains open for the
  remaining owner-specific literals.
- Oracle and design: the new presentation-readiness owner composes the existing
  baseline-scoped keyboard-readiness runtime-log observer with
  `FlowTabUITestElementExistenceObservationOwner`. Both start before trigger
  delivery. Diagnostics evidence remains gated until the exact keyboard-ready
  marker resolves; an immediate readback then accepts only
  `flowtab.testing.switcher.summary.exists == true`. Scheduler and animation
  pressure can delay that evidence without supplying success.
- Lifecycle and retained time policy: the compatible twelve-second
  `FlowTabUITestRuntimeTruthWatchdogPolicy.windowSearchDiagnosticsPublication`
  is solely a terminal failure bound. XCUI exposes no element-publication
  notification, so the shared named cancellable 100ms cadence activates only
  while the post-keyboard-readiness readback remains absent. Each presentation
  invocation owns both generations, baseline snapshot, runtime-log/file-system
  observers, deferred schedule, expectation, final readback, cancellation, and
  teardown; the real workflow owns FlowTab and fixture App cleanup. Expiry
  reports both owners plus the exact identifier, phase, generation, source,
  waiter result, and final `exists` readback.
- Baseline evidence: the immediately preceding committed SYNC-036FL canonical
  invocations exercised the six raw twelve-second diagnostics waits. The
  ordinary path passed 1/1 in 18.131 seconds with xcodebuild completion in
  19.269 seconds; the independently prepared noisy path passed 1/1 in 26.897
  seconds with xcodebuild completion in 27.656 seconds.
- Deterministic and Pressure validation: the rebuilt canonical invocation
  passed policy compatibility; baseline-gated keyboard initial/event evidence;
  exact-element initial, post-trigger, delayed, five-unresolved-readback slow-
  scheduling, cancellation, final-watchdog, replacement, stale-generation, and
  duplicate-delivery rules 13/13 in 1.805 seconds. Xcodebuild testing completed
  in 2.609 seconds. The runtime-log and element owners completed 100 and 200
  lifecycle Pressure iterations, respectively.
- Real UI validation: the ordinary two-presentation topology passed 1/1 in
  17.024 seconds, with xcodebuild testing completed in 17.756 seconds. The
  independently prepared noisy four-presentation topology passed 1/1 in
  24.831 seconds, with xcodebuild testing completed in 25.532 seconds. The
  three core clean result bundles pass the affected validation 15/15 without
  cross-test fixture reuse. A direct host-authorization confirmation of the
  ordinary selector also passed 1/1 in 21.200 seconds with xcodebuild
  completion in 22.034 seconds and is retained as supplemental evidence.
- Independent pre-owner readiness evidence: two noisy invocations stopped at
  the existing fixture-workflow readiness owner before any Search trigger.
  They observed all expected windows `[1,2,3,4]`, both fullscreen windows
  `[2,3]`, and the resolved desktop condition; the sole unmet condition was
  `applicationAXExposure` with `axSuppressionRequired=true` and
  `axExposureResolved=false`. The attempts failed in 25.286/25.039 seconds,
  with xcodebuild completion in 36.064/36.921 seconds. Their status ledgers
  record test exit 65 and complete result bundles. The subsequent exact noisy
  invocation reached the slice owner and passed with the same signed fixture
  topology, closing the affected validation without changing fixture logic.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production presentation, accessibility identifiers,
  keyboard focus, configuration, module wiring, and path resolution are
  unchanged. The deterministic UI-target suite supplies event ordering,
  independent readback, cancellation, replacement, stale-generation, slow-
  scheduling, watchdog, and lifecycle Pressure; the signed ordinary/noisy
  fixture invocations supply the affected visible UI paths.
- Result, signing, and process evidence: the four successful status ledgers
  record `stage=completed`, zero applicable fixture/signing/build/test/final
  exit codes, and present result bundles. Host-context strict deep codesign
  accepts both the fixed App and the Runner re-signed by the final noisy
  invocation. Exact final process readback finds no Flow Tab UITest App,
  UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all four affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. The new 103-line composed helper has one file reference, one UI group
  membership, and one Sources membership. The element-observation test file
  contains 206 lines, the policy file 86 lines, and the oversized entrypoint
  shrinks from 789 to 779 lines. One raw numeric timeout argument is removed,
  leaving 254 across 10 UI-test files. The three core successful invocations
  retain six expected destination-selection warnings and 26/3/3 non-failing
  CFPrefs refresh diagnostics; the supplemental ordinary invocation retains
  two warnings and three diagnostics. The two pre-owner attempts retain four
  expected destination warnings and 3/3 CFPrefs diagnostics. The initial build
  retains one pre-existing unused-value compiler warning in
  `FlowTabUITests+SpaceFixtureEdgeInputsWorkflow.swift`, introduced by commit
  `939960af` and outside this owner. Successful fatal-error, assertion,
  sanitizer, main-thread-checker, XPC, permission-denial, crash, watchdog-
  expiry, generic compiler-error, and test-failure scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 327 MB FM evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FM Search diagnostics publication`.

### SYNC-036FN Closure Record

- Status: completed. All six ordinary/noisy initial and subsequent Window
  Search presentations now observe one atomic committed Window Search
  projection before accepting readiness. Parent SYNC-036 remains open for the
  remaining owner-specific literals.
- Oracle and design: the presentation-readiness owner starts committed-
  projection observation before each Search trigger. One exact diagnostics
  accessibility value supplies the parsed `searchResults` plus readiness,
  result-state, degradation, current-generation coverage, and freshness-
  barrier fields. Clean topology requires the exact workflow-window count;
  noisy topology requires every expected title while allowing unrelated rows.
  A matching initial projection remains gated until the exact keyboard-ready
  marker resolves, followed by one immediate trigger readback, so an old
  matching value cannot satisfy the new presentation.
- Lifecycle and retained time policy: XCUI exposes no diagnostics-value
  publication callback, so the presentation owner activates its shared named
  cancellable 100ms cadence only when the immediate post-marker readback is
  unresolved. The compatible eight-second
  `windowSearchCommittedProjectionPublication` policy is solely a terminal
  failure bound and preserves the previous cumulative two-by-four-second
  failure opportunity. Each invocation owns projection generation,
  observation, deferred scheduling, expectation, final readback,
  cancellation, and teardown; the real workflow owns FlowTab and fixture App
  cleanup. Expiry reports the exact expected App, titles and count plus the
  final identifier, raw value, parsed rows, five index fields, phase,
  generation, source, and waiter result.
- Baseline evidence: the immediately preceding committed SYNC-036FM canonical
  invocations exercised the two sequential raw four-second result/index waits.
  Deterministic coverage passed 13/13 in 1.805 seconds with xcodebuild
  completion in 2.609 seconds. The ordinary path passed 1/1 in 17.024 seconds
  with xcodebuild completion in 17.756 seconds; the independently prepared
  noisy path passed 1/1 in 24.831 seconds with xcodebuild completion in 25.532
  seconds.
- Deterministic and Pressure validation: the final rebuilt canonical
  invocation passed policy compatibility; post-keyboard matching-baseline
  gating and trigger readback; atomic clean/noisy row rules; five unresolved
  slow-scheduling readbacks followed by the same exact success; cancellation;
  final-watchdog diagnostics; and stale/duplicate-generation Pressure 13/13 in
  1.998 seconds. Xcodebuild testing completed in 2.899 seconds. The new
  projection owner completed 200 lifecycle iterations; its underlying result
  and diagnostics owners retain 100 lifecycle iterations each.
- Real UI validation: the ordinary two-presentation topology passed 1/1 in
  16.863 seconds, with xcodebuild testing completed in 17.547 seconds. The
  independently prepared noisy four-presentation topology passed 1/1 in
  24.420 seconds, with xcodebuild testing completed in 25.140 seconds. The
  three final clean result bundles pass the affected validation 15/15 without
  cross-test fixture reuse.
- Validation refinement: the first deterministic invocation passed 12/13; its
  sole failure was a test-only assertion that expected one platform-specific
  rendering of the XCTWaiter result. The assertion now checks the stable
  `waitResult=` diagnostic contract. The subsequent canonical invocations all
  pass; production behavior was unchanged by this refinement.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production Search indexing, exact window identity,
  accessibility publication, configuration, module wiring, and path
  resolution are unchanged. The deterministic UI-target suite supplies
  atomicity, ordering, initial-state gating, slow scheduling, cancellation,
  stale/duplicate generation, watchdog, and lifecycle Pressure; the two signed
  fixture invocations supply all affected visible UI paths.
- Result, signing, and process evidence: all three final status ledgers record
  `stage=completed`, zero applicable fixture/signing/build/test/final exit
  codes, and present result bundles. Host-context strict deep codesign accepts
  the fixed App and the Runner re-signed by the final noisy invocation. Exact
  final process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all five affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. Both new files have exactly one file reference, one UI group
  membership, and one Sources membership; the projection owner contains 174
  lines and its tests 295. The composed presentation owner contains 201 lines,
  the policy file 96, and the oversized entrypoint shrinks from 779 to 688
  lines. Two raw numeric timeout arguments are removed, leaving 252 across 10
  UI-test files. The three final invocations retain six expected destination-
  selection warnings and 26/3/3 non-failing CFPrefs refresh diagnostics. The
  initial build retains one pre-existing unused-value compiler warning in
  `FlowTabUITests+SpaceFixtureEdgeInputsWorkflow.swift`, introduced by commit
  `939960af` and outside this owner. Fatal-error, assertion, sanitizer, main-
  thread-checker, XPC, permission-denial, crash, watchdog-expiry, generic
  compiler-error, and test-failure scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 164 MB FN evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FN Search committed projection`.

### SYNC-036FO Closure Record

- Status: completed. All six ordinary/noisy Window Search query publications
  now establish their observation owner and capture a committed projection
  baseline before posting the exact query command. Parent SYNC-036 remains
  open for the remaining owner-specific literals.
- Oracle and design: one diagnostics accessibility-value read supplies the
  committed `searchResultsScope`, decoded `searchResultsQuery`, result rows,
  and result IDs. Success requires exact `window` scope, exact query, target
  title, and target App name after command delivery. A matching initial
  projection remains gated until a later scope/query/result transition, so an
  old committed value cannot satisfy a new query. A non-matching baseline can
  resolve only when the exact committed target appears. Unrelated application
  state is excluded from projection-transition identity.
- Lifecycle and retained time policy: XCUI exposes no diagnostics-value
  publication callback. The helper performs one immediate post-trigger
  readback and activates the shared named cancellable 100ms cadence only while
  the committed condition remains unmet. The compatible eight-second
  `windowSearchQueryProjectionPublication` policy is solely a terminal
  failure bound. Each invocation owns state generation, baseline, deferred
  schedule, expectation, final readback, cancellation, and teardown; the real
  workflow owns FlowTab and fixture App cleanup. Expiry reports phase, trigger
  gate, baseline, transition state, expected scope/query/title/App, final
  projection, generation, source, and waiter result.
- Baseline evidence: the immediately preceding committed SYNC-036FN canonical
  invocations used the post-trigger eight-second result wait. Deterministic
  coverage passed 13/13 in 1.998 seconds with xcodebuild completion in 2.899
  seconds. The ordinary path passed 1/1 in 16.863 seconds with xcodebuild
  completion in 17.547 seconds; the noisy path passed 1/1 in 24.420 seconds
  with xcodebuild completion in 25.140 seconds.
- Deterministic and Pressure validation: the final current-source invocation
  passed policy compatibility; pre-trigger baseline capture; immediate
  post-trigger resolution without schedule activation; matching-baseline
  transition gating; atomic wrong/partial scope, query, title, and App
  rejection; five unresolved slow-scheduling readbacks followed by the same
  exact success; cancellation; final-watchdog diagnostics; and stale/duplicate
  generation Pressure 8/8 in 1.293 seconds. Xcodebuild testing completed in
  2.323 seconds. The new query owner completed 200 lifecycle iterations.
- Shared publication validation: one current-source canonical invocation
  rebuilt fixture variants and passed the shared mock and real-fixture callers
  2/2 in 23.141 seconds, with xcodebuild testing completed in 23.958 seconds.
  The mock publication passed in 4.584 seconds and the real fixture publication
  in 18.557 seconds.
- Affected UI validation: the current-source ordinary topology passed 1/1 in
  16.258 seconds, with xcodebuild testing completed in 17.046 seconds; both
  exact query paths executed. The current-source noisy Pressure topology
  passed 1/1 in 32.065 seconds, with xcodebuild testing completed in 32.766
  seconds; all four exact query paths executed. The longer noisy schedule
  changed completion time while preserving every result.
- Pre-owner attempt evidence: two current-source ordinary attempts stopped
  before the SYNC-036FO query owner and contain no `searchQuery` marker. The
  first expired in the existing SYNC-036FN presentation projection with the
  exact fixture rows present but the committed-index readback reporting
  `degradedStaleCommitted`, current-generation coverage zero, and freshness
  barrier requested. The second expired in the existing switcher-trigger
  presentation owner after host runtime-log churn omitted its exact completion
  marker. Their status ledgers record test exit 65 and present result bundles.
  Subsequent exact ordinary invocations using the freshly rebuilt fixture
  variants reached both query paths and passed.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production Search scheduling, indexing, diagnostics
  publication, exact window identity, configuration, module wiring, and path
  resolution are unchanged. The deterministic UI-target suite supplies
  atomicity, ordering, initial-state gating, slow scheduling, cancellation,
  stale/duplicate generation, watchdog, and lifecycle Pressure; the signed
  mock, real-fixture, ordinary, and noisy invocations supply affected visible
  UI paths.
- Result, signing, and process evidence: all four final status ledgers record
  `stage=completed`, zero applicable fixture/signing/build/test/final exit
  codes, and present result bundles. Host-context strict deep codesign accepts
  both the fixed App and the Runner re-signed by the final noisy invocation.
  Exact final process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all six affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. Both new files have exactly one file reference, one UI group
  membership, and one Sources membership. The query owner contains 234 lines
  and its tests 323; the shared result owner shrinks from 740 to 686 lines, the
  policy file contains 106, the runtime-truth entrypoint 696, and the shared
  publication file 126. One raw numeric timeout argument is removed, leaving
  251 across 10 pre-existing UI-test files. The four final invocations retain
  nine expected destination-selection warnings and 16/6/3/3 non-failing
  CFPrefs refresh diagnostics. Fatal-error, assertion, sanitizer, main-thread-
  checker, XPC, permission-denial, crash, watchdog-expiry, compiler-error,
  compiler-warning, and test-failure scans are empty after excluding the
  expected destination warnings.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 347 MB FO evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FO Search query projection`.

### SYNC-036FP Closure Record

- Status: completed. All six ordinary/noisy Window Search result-selection
  applications now establish one combined observation owner before writing the
  result payload or posting the command. Parent SYNC-036 remains open for the
  remaining owner-specific literals.
- Oracle and design: success requires one exact post-baseline runtime-log line
  ending in `select search result command applied resultID=<exact ID>` and the
  exact percent-decoded `searchSelectedResult` diagnostics value in the same
  owner readback generation. TestingSupport emits the applied marker only
  after model selection, accessibility-anchor synchronization, and panel-size
  update. Generic command completion, a missed marker, suffix extension, a
  different result ID, and a wrong diagnostics projection are rejected. An
  already-matching diagnostics baseline remains insufficient until the exact
  post-trigger applied marker arrives.
- Lifecycle and retained time policy: the runtime-log baseline captures file
  identity and byte offsets before observer registration and the trigger. File
  events request immediate readback; the shared named cancellable 200ms
  cadence covers coalesced filesystem delivery while the exact condition is
  unresolved. One named six-second
  `windowSearchResultSelectionApplication` watchdog replaces the sequential
  four-plus-two-second bounds and serves solely as a terminal failure limit.
  Each invocation owns the baseline, file-event observer, fallback schedule,
  condition generation, expectation, final readback, cancellation, and
  teardown; each signed workflow owns FlowTab and fixture App cleanup. Expiry
  reports applied-marker presence, expected suffix, log generations and tail,
  decoded/raw diagnostics, observation generation/source, and waiter result.
- Baseline evidence: the immediately preceding committed SYNC-036FO
  current-source invocations retained the generic four-second delivery wait
  followed by a raw two-second diagnostics wait. Deterministic coverage passed
  8/8 in 1.293 seconds with xcodebuild completion in 2.323 seconds. The
  ordinary path passed 1/1 in 16.258 seconds with xcodebuild completion in
  17.046 seconds; the noisy path passed 1/1 in 32.065 seconds with xcodebuild
  completion in 32.766 seconds.
- Deterministic and Pressure validation: the final current-source invocation
  passed policy compatibility; observer-before-initial-readback ordering;
  matching-baseline event gating; exact suffix and decoded projection;
  generic-completion, missed, extended-suffix, wrong-ID, and wrong-projection
  rejection; five unresolved slow-scheduling readbacks followed by the same
  exact success; cancellation; final-watchdog diagnostics; and stale/duplicate
  generation Pressure 7/7 in 1.168 seconds. Xcodebuild testing completed in
  2.006 seconds. The new owner completed 200 lifecycle iterations.
- Affected UI validation: the current-source ordinary topology passed 1/1 in
  17.210 seconds, with xcodebuild testing completed in 17.992 seconds; both
  exact result-selection paths executed. The current-source noisy Pressure
  topology passed 1/1 in 25.370 seconds, with xcodebuild testing completed in
  26.180 seconds; all four exact result-selection paths executed. Scheduling
  and topology load changed completion time while preserving every result.
- Corrected pre-validation attempt: the first current-source deterministic
  invocation stopped during compilation because the global command-posting
  helper was called as an instance member. Its status ledger records
  `stage=xcodebuild_build-for-testing_failed`, build/final exit 65, and no
  result bundle or executed test. Removing that instance qualification was a
  one-token correction; all subsequent deterministic and signed invocations
  rebuilt the same source and passed.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production selection, confirmation, exact window identity,
  configuration, module wiring, and path resolution are unchanged. The
  deterministic UI-target suite supplies exact evidence composition,
  ordering, initial-state gating, slow scheduling, cancellation,
  stale/duplicate generation, watchdog, and lifecycle Pressure; the signed
  ordinary and noisy invocations supply the affected visible UI paths.
- Result, signing, and process evidence: all three final status ledgers record
  `stage=completed`, zero applicable fixture/signing/build/test/final exit
  codes, and present result bundles. Host-context strict deep codesign accepts
  both the fixed App and the Runner re-signed by the final noisy invocation.
  Exact final process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all five affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. Both new files have exactly one file reference, one UI group
  membership, and one Sources membership. The combined owner contains 167
  lines and its tests 321; the policy file contains 116, the runtime-truth
  entrypoint 695, and Support 740. One raw numeric timeout argument is removed,
  leaving 250 across 10 pre-existing UI-test files. The three final
  invocations retain seven expected destination-selection warnings and
  14/3/3 non-failing CFPrefs refresh diagnostics. One compiler warning in the
  deterministic build reports the already-unused `searchInput` local in the
  unchanged `FlowTabUITests+SpaceFixtureEdgeInputsWorkflow.swift`; the same
  unused local is present in the committed baseline. Fatal-error, assertion,
  sanitizer, main-thread-checker, XPC, permission-denial, crash,
  watchdog-expiry, generic compiler-error, and test-failure scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 3.8 MB FP evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FP Search result selection`.

### SYNC-036FQ Closure Record

- Status: completed. Every Option+Tab `selectApp` application now establishes
  one combined observation owner before writing the payload or posting the
  command. Parent SYNC-036 remains open for the remaining owner-specific
  literals.
- Oracle and design: success requires one exact post-baseline runtime-log line
  ending in `select app command applied appID=<exact bundle ID>` and the exact
  `selected=<bundle ID>` diagnostics value in the same owner readback.
  TestingSupport emits the applied marker only after session selection,
  accessibility-anchor synchronization, and panel-size update. Generic
  completion, a missed marker, an extended suffix, a different App ID, and a
  wrong selected projection are rejected. An already-matching diagnostics
  baseline remains insufficient until the exact post-trigger marker arrives.
- Lifecycle and retained time policy: the runtime-log baseline captures file
  identity and byte offsets before observer registration and the trigger. File
  events request immediate readback; the shared named cancellable 200ms
  cadence covers coalesced filesystem delivery while the exact condition is
  unresolved. One named eight-second `switcherAppSelectionApplication`
  watchdog replaces the sequential four-plus-four-second bounds and serves
  solely as a terminal failure limit. Each invocation owns the baseline,
  file-event observer, fallback schedule, condition generation, expectation,
  final readback, cancellation, and teardown; each signed workflow owns
  FlowTab and fixture App cleanup. Expiry reports applied-marker presence,
  expected suffix, log generations and tail, selected/raw diagnostics,
  observation generation/source, and waiter result.
- Baseline evidence: the immediately preceding committed SYNC-036FP source
  retained generic command completion followed by a separate selected-value
  wait. The four-topology signed invocation rebuilt current fixture variants:
  provisional-hidden passed in 8.986 seconds, noisy passed in 21.721 seconds,
  and ordinary passed in 16.071 seconds. Space-backed reached and passed its
  app-selection boundary, then expired at the independent post-confirm
  activation-recovery marker in 24.419 seconds. The suite executed 4 tests
  with that single failure in 71.197 seconds; xcodebuild completed in 80.032
  seconds.
- Deterministic and Pressure validation: the final current-source invocation
  passed policy compatibility; observer-before-initial-readback ordering;
  matching-baseline event gating; exact applied suffix and selected
  projection; generic-completion, missed, extended-suffix, wrong-ID, and
  wrong-projection rejection; five unresolved slow-scheduling readbacks
  followed by the same exact success; cancellation; final-watchdog
  diagnostics; and stale/duplicate-generation Pressure 7/7 in 1.079 seconds.
  Xcodebuild testing completed in 2.454 seconds. The new owner completed 200
  lifecycle iterations.
- Affected UI validation: the current-source ordinary topology passed 1/1 in
  19.404 seconds, with xcodebuild testing completed in 20.262 seconds; both
  exact app-selection paths executed. The current-source noisy Pressure and
  provisional-hidden topologies passed 2/2 in 30.030 seconds, with xcodebuild
  testing completed in 30.773 seconds; noisy passed in 22.333 seconds and
  provisional-hidden in 7.697 seconds. Their five exact app-selection paths
  completed without owner expiry. Scheduling and topology load changed
  completion time while preserving every result.
- Independent failure boundary: the combined current-source invocation
  completed all seven exact app-selection paths without owner expiry.
  Space-backed then reproduced the baseline activation-recovery-marker expiry;
  its isolated invocation also advanced past `selectApp` into `advanceDown`
  before the same post-confirm assertion expired in 24.905 seconds. The
  combined ordinary path separately encountered a pre-trigger fixture
  projection with `com.example.fixture.chrome:0`; its isolated invocation
  rebuilt the same source, completed both app selections, and passed. These
  failures remain evidence for later owner-specific migration slices and do
  not alter the SYNC-036FQ application Oracle.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production app selection, Switcher presentation, exact
  window identity, configuration, module wiring, and path resolution are
  unchanged. The deterministic UI-target suite supplies exact evidence
  composition, ordering, initial-state gating, slow scheduling, cancellation,
  stale/duplicate generation, watchdog, and lifecycle Pressure; the signed
  ordinary, noisy, provisional-hidden, and Space-backed boundary invocations
  supply the affected visible UI paths.
- Result, signing, and process evidence: the deterministic, isolated ordinary,
  and combined noisy/provisional status ledgers record `stage=completed`, zero
  applicable signing/build/test/final exit codes, and present result bundles.
  The baseline, combined diagnostic, and isolated Space-backed ledgers record
  test exit 65 with present result bundles at the independent failures above.
  Host-context strict deep codesign accepts both the fixed App and the Runner
  re-signed by the final signed invocation. Exact final process readback finds
  no Flow Tab UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all six affected Swift sources parse
  directly, the project file passes `plutil -lint`, and `git diff --check`
  passes. Both new files have exactly one file reference, one UI group
  membership, and one Sources membership. The combined owner contains 161
  lines and its tests 306; the policy file contains 126, the Space-backed file
  311, the runtime-truth entrypoint 701, and Support 727. The removed helper
  has no remaining reference. The new named policy replaces the targeted raw
  waits while the repository-wide raw numeric timeout inventory remains 250
  across UI tests. The three completed invocations retain six expected
  destination-selection warnings and 14/3/6 non-failing CFPrefs refresh
  diagnostics. The deterministic build retains one pre-existing unused-value
  compiler warning in
  `FlowTabUITests+SpaceFixtureEdgeInputsWorkflow.swift`. Fatal-error,
  assertion, sanitizer, main-thread-checker, XPC, permission-denial, crash,
  generic compiler-error, and test-failure scans are empty for the completed
  ledgers.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The 589 MB FQ evidence root contains only
  rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FQ app selection`.

### SYNC-036FR Closure Record

- Status: completed. Every shared ordinary, noisy, and Space-backed Option+Tab
  App-layer-to-window-layer entry now uses one atomic selection-transition
  observation owner. Parent SYNC-036 remains open for the remaining
  owner-specific literals.
- Oracle and design: the helper first requires one exact baseline containing
  `selected=<target bundle ID>` and `mode=appCycle`. It establishes the
  transition owner before posting the raw `advanceDown` notification, gates
  acceptance until the trigger returns, and succeeds only when one diagnostics
  readback contains the same exact selected bundle together with
  `mode=windowCycle(<same exact bundle ID>)`. The shared transition rule rejects
  a wrong bundle, a partial or mismatched atomic state, an unchanged baseline,
  and evidence from a stale owner generation.
- Lifecycle and retained time policy: accessibility values provide no state
  publication callback, so the existing named serial 100ms scheduler performs
  immediate and cancellable condition readbacks while the exact transition is
  unresolved. One named twelve-second `switcherWindowCycleEntry` watchdog
  preserves the cumulative failure opportunity of the three replaced
  four-second bounds and serves solely as a terminal failure limit. Each helper
  invocation owns the baseline, observation registration, schedule,
  generation, trigger gate, final readback, cancellation, and teardown; each
  signed workflow owns FlowTab and fixture App cleanup. Expiry reports the
  expected transition, exact baseline, final diagnostics identifier/raw/value
  map, observation generation/source, and waiter result.
- Baseline evidence: the immediately preceding committed SYNC-036FQ source
  retained generic command completion, a mode-prefix wait, and a separate
  selected-value wait. Its current-source ordinary topology passed in 19.404
  seconds with xcodebuild completion in 20.262 seconds, and its noisy topology
  passed in 22.333 seconds. The isolated Space-backed path advanced beyond the
  window-layer entry and reproduced the independent post-confirm
  activation-recovery-marker expiry in 24.905 seconds.
- Deterministic and Pressure validation: the final current-source invocation
  passed policy compatibility; exact App-layer and window-layer baseline
  rules; observer-before-trigger and synchronous trigger-readback resolution;
  exact atomic-state matching; post-trigger gating; five unresolved slow
  scheduler readbacks followed by the same exact success; cancellation;
  final-watchdog diagnostics; and stale/duplicate-generation Pressure 9/9 in
  1.197 seconds. Xcodebuild testing completed in 1.925 seconds. The stale-owner
  Pressure case completed 100 lifecycle iterations.
- Affected UI validation: the final current-source ordinary topology passed
  1/1 in 14.199 seconds, with xcodebuild testing completed in 14.868 seconds;
  both exact window-layer entries executed. The noisy Pressure topology passed
  1/1 in 29.073 seconds, with xcodebuild testing completed in 29.778 seconds;
  all four exact window-layer entries executed. Scheduling and topology load
  changed completion time while preserving every transition result.
- Independent failure boundary: the final current-source Space-backed
  invocation completed its exact atomic window-layer transition, then selected
  the exact recovered CG-only window and confirmed it. The subsequent
  independent runtime-log Oracle expired while awaiting the
  `focus-recovery exhausted` marker in 24.028 seconds; xcodebuild completed in
  33.051 seconds. This matches the committed baseline boundary and leaves that
  later owner for its dedicated migration slice. The final policy was
  calibrated to the compatible twelve-second cumulative bound before the
  final-source deterministic and signed invocations.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production input handling, selection state, exact window
  identity, configuration, module wiring, and path resolution are unchanged.
  The deterministic UI-target suite supplies state-rule, ordering,
  initial-state, slow-scheduling, cancellation, stale/duplicate generation,
  watchdog, and lifecycle Pressure evidence. Signed ordinary, noisy, and
  Space-backed invocations supply the affected visible UI paths and real
  fixture topology.
- Result, signing, and process evidence: the deterministic, isolated ordinary,
  and isolated noisy status ledgers record `stage=completed`, zero applicable
  signing/build/test/final exit codes, and present result bundles. The
  Space-backed boundary ledger records test exit 65 with a present result
  bundle after the successful transition described above. Host-context strict
  deep codesign accepts both the fixed App and the Runner re-signed by the
  final invocation. Exact final process readback finds no Flow Tab UITest App,
  UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all four affected Swift sources parse
  directly and `git diff --check` passes. The policy file contains 136 lines,
  the shared transition helper 272, its tests 528, and the runtime-truth
  entrypoint 673. The separate selected-App assertion has no remaining
  reference in the affected owner. Two raw numeric timeout arguments are
  removed, leaving 248 across 10 pre-existing UI-test files. The three
  completed invocations retain six expected destination-selection warnings
  and 18/3/3 non-failing CFPrefs refresh diagnostics. Fatal-error, assertion,
  sanitizer, main-thread-checker, permission-denial, crash, owner-watchdog,
  compiler-error, compiler-warning, and test-failure scans are empty for the
  completed ledgers.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 383 MB FR evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FR window-cycle entry`.

### SYNC-036FS Closure Record

- Status: completed. All eight affected ordinary, noisy, Space-backed, and
  provisional-hidden `selectApp` applications now resolve exact selection and
  App-strip projection through one pre-trigger owner. Parent SYNC-036 remains
  open for the remaining owner-specific literals.
- Oracle and design: success requires one exact post-baseline runtime-log line
  ending in `select app command applied appID=<exact bundle ID>` plus one raw
  diagnostics readback containing both `selected=<same exact bundle ID>` and
  the required parsed `apps` entry. Ordinary and provisional paths require the
  exact bundle/count entry; noisy topology requires the exact bundle identity
  while allowing its observed CG-sibling count. Missing entries, a wrong exact
  count, a bundle-prefix collision, a wrong selected value, a generic or
  extended log suffix, and a matching pre-trigger projection remain
  unresolved. The log baseline and owner registration precede the trigger,
  and owner startup performs the initial readback.
- Lifecycle and retained time policy: runtime-log file events request
  immediate evidence readback, while the shared named cancellable 200ms
  fallback covers coalesced filesystem delivery only while the atomic
  condition remains unresolved. One named twelve-second
  `switcherAppSelectionProjectionApplication` watchdog replaces the prior
  sequential eight-plus-four-second failure bounds. Expiry reports exact
  applied-marker presence and suffix, expected and observed App projection,
  final log identity/generation/tail, raw selected/App diagnostics, owner
  generation/source, and waiter result. Each helper invocation owns the log
  baseline, file observer, fallback schedule, condition generation,
  cancellation, and teardown; each signed workflow owns FlowTab and fixture
  App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FR source
  passed the 17 existing policy, App-projection, and App-selection tests in
  2.520 seconds, with xcodebuild testing completed in 3.309 seconds. The
  durable signed FR baseline passed ordinary in 14.199 seconds and noisy in
  29.073 seconds; its Space-backed path reached the later independent
  activation-recovery-marker boundary after completing exact selection and
  window-layer entry.
- Deterministic and Pressure validation: the final current-source invocation
  passed 18/18 in 2.604 seconds, with xcodebuild testing completed in 3.368
  seconds. Coverage includes policy compatibility; observer-before-initial
  readback; exact applied/selected/App projection composition; missing entry,
  wrong count, and prefix-collision rejection; topology-dependent count;
  initial, event, and fallback readback; five unresolved slow-scheduling
  readbacks followed by exact success; cancellation; final-watchdog
  diagnostics; and stale/duplicate-generation Pressure. App selection and the
  existing shared App-projection owners completed 200 and 100 lifecycle
  iterations, respectively. A direct host-authorized signed XCTest rerun of
  the 200-generation App-selection Pressure case passed 1/1 in 0.231 seconds,
  with xcodebuild testing completed in 1.361 seconds.
- Affected UI validation: final current-source ordinary passed 1/1 in 20.158
  seconds, with xcodebuild testing completed in 20.856 seconds, and completed
  two exact App selections. Noisy Pressure topology passed 1/1 in 22.901
  seconds, with xcodebuild testing completed in 23.946 seconds, and completed
  four exact App selections. Provisional-hidden passed 1/1 in 8.213 seconds,
  with xcodebuild testing completed in 8.982 seconds, and completed one exact
  App selection. Scheduling and topology load changed completion time while
  preserving every result.
- Independent failure boundary: the final Space-backed invocation completed
  its one exact combined App-selection/App-projection Oracle, entered the
  window layer, selected the exact recovered CG-only window at
  `selectAttempt.1`, and confirmed it. The later runtime-log watchdog then
  reproduced the committed-baseline `focus-recovery exhausted` marker expiry
  in 24.329 seconds; xcodebuild completed in 33.540 seconds with a present
  result bundle. No App-selection owner watchdog expired. This later
  activation-recovery contract remains assigned to its dedicated migration
  slice.
- Final-source review: an intermediate adapter represented App projection
  through an application-state snapshot. Review replaced it with a
  diagnostics-only readback that carries only the exact accessibility
  identifier, existence, raw `apps` value, and parsed entries. The final
  deterministic invocation and all four signed topologies were rerun after
  that correction.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production selection, Switcher behavior, exact App/window
  identity, configuration, module dependencies, and path-intent resolution
  remain unchanged. The deterministic UI-target suite supplies exact state
  composition, ordering, initial-state, slow-scheduling, cancellation,
  stale/duplicate generation, watchdog, and lifecycle Pressure evidence.
  Signed ordinary, noisy, provisional-hidden, and Space-backed invocations
  supply the affected visible UI paths and real fixture topologies.
- Result, signing, and process evidence: deterministic, ordinary, noisy,
  provisional-hidden, and direct-authorization ledgers record
  `stage=completed`, zero final/test exit codes, and present result bundles.
  The Space-backed boundary ledger records test exit 65 with a present result
  bundle after the successful FS Oracle described above. Host-context strict
  deep codesign accepts both the fixed App and the Runner re-signed by the
  direct Pressure invocation. Exact final process readback finds no Flow Tab
  UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all six affected Swift sources parse
  directly and `git diff --check` passes. The policy file contains 141 lines,
  App-projection observation 468, App-selection observation 186, its tests
  403, the runtime-truth entrypoint 658, and the provisional/Space-backed file
  304. The two targeted post-selection raw waits and the prior eight-second
  selection policy have no affected caller reference. Three raw numeric
  timeout arguments are removed, leaving 245 across the same 10 pre-existing
  UI-test files. Each final-source completed invocation retains one expected
  destination-selection warning; their non-failing CFPrefs refresh counts are
  36/3/3/3, and the direct authorization rerun adds one expected destination
  warning plus two refresh diagnostics. The deterministic rebuild retains one
  pre-existing unused-value compiler warning in the unchanged
  `FlowTabUITests+SpaceFixtureEdgeInputsWorkflow.swift`. Fatal-error,
  assertion, sanitizer, main-thread-checker, permission-denial, crash,
  App-selection-watchdog, generic compiler-error, and test-failure scans are
  empty for every completed ledger.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 372 MB FS evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FS app projection`.

### SYNC-036FT Closure Record

- Status: completed. Nine affected preview entries now resolve exact
  window-cycle entry and initial preview publication through one pre-trigger
  owner: two ordinary Runtime Truth entries, four noisy Pressure entries, one
  Space-backed entry, and the two standard/fullscreen open-window-layer
  mutation entries. Parent SYNC-036 remains open for the remaining
  owner-specific literals.
- Oracle and design: success requires one raw diagnostics readback containing
  the exact target in `selected`, exact `windowCycle(<target bundle ID>)` in
  `mode`, and a `preview` value whose bundle prefix equals that same selected
  bundle. Ordinary topology additionally requires the exact title set. Noisy
  fullscreen topology requires every standard title plus at least one title
  from the fixture's real fullscreen set. A partial title set, mixed
  selected/mode state, matching titles under another preview bundle prefix,
  and a matching pre-trigger projection remain unresolved.
- Lifecycle and retained time policy: the selection-transition owner is
  established before Down, performs its initial readback, rejects evidence
  until input synthesis or the command trigger returns, then performs an
  explicit trigger readback. XCUI provides no accessibility-value publication
  callback, so the shared named cancellable 100ms readback scheduler remains
  active only while the atomic condition is unresolved. Runtime Truth uses one
  named twenty-second terminal watchdog; exact and noisy mutation entries use
  named eleven- and fifteen-second watchdogs. Expiry reports the expected
  baseline transition and preview rule, final identifier/raw/value map,
  observed preview bundle/titles, owner generation/source, and waiter result.
  Each helper invocation owns scheduling, generation, cancellation, and
  teardown; each signed workflow owns FlowTab and fixture App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FS source
  passed the 16 existing policy, selection-transition, and preview-projection
  tests in 2.398 seconds, with xcodebuild testing completed in 3.113 seconds.
  Durable signed FS baselines passed ordinary Runtime Truth in 20.158 seconds
  and noisy topology in 22.901 seconds; its Space-backed path reached the
  later independent activation-recovery-marker boundary in 24.329 seconds.
  The committed mutation baseline passed the standard path in 40.516 seconds
  and fullscreen path in 42.018 seconds.
- Deterministic and Pressure validation: the final current-source invocation
  passed 20/20 in 2.959 seconds, with xcodebuild testing completed in 4.052
  seconds. Coverage includes policy compatibility; atomic diagnostics
  parsing; observer-before-initial-readback and post-trigger gating; exact
  selected/mode/preview composition; partial-title, wrong-state, and wrong
  preview-bundle rejection; trigger readback; five unresolved slow-scheduling
  readbacks followed by exact success; cancellation; final-watchdog
  diagnostics; and stale/duplicate callbacks across 100 owner generations.
  The independent preview-projection Pressure owner also completed its 100
  lifecycle iterations.
- Affected signed UI validation: ordinary Runtime Truth passed 1/1 in 15.171
  seconds, with xcodebuild testing completed in 15.862 seconds, and completed
  both exact preview entries. Noisy Pressure topology passed 1/1 in 22.373
  seconds, with xcodebuild testing completed in 23.452 seconds, and completed
  all four exact entries. The standard open-window-layer mutation passed 1/1
  in 38.289 seconds, with xcodebuild testing completed in 39.358 seconds. The
  fullscreen mutation passed 1/1 in 45.665 seconds, with xcodebuild testing
  completed in 46.770 seconds. Scheduling and topology load changed
  completion time while preserving every transition result.
- Independent failure boundary: the final Space-backed invocation completed
  its exact combined entry/projection Oracle, selected the exact recovered
  CG-only window at `selectAttempt.1`, and confirmed it. The subsequent
  independent runtime-log watchdog reproduced the committed-baseline
  `focus-recovery exhausted` marker expiry in 24.137 seconds; xcodebuild
  completed in 33.650 seconds with a present result bundle. No preview-entry
  watchdog expired. The later activation-recovery contract remains assigned
  to its dedicated migration slice.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production selection, preview publication, exact window
  identity, configuration, module dependencies, and path-intent resolution
  remain unchanged. The deterministic UI-target suite supplies exact state
  composition, ordering, initial-state, slow-scheduling, cancellation,
  stale/duplicate generation, watchdog, and lifecycle Pressure evidence.
  Signed ordinary, noisy, Space-backed, standard-mutation, and
  fullscreen-mutation invocations supply the affected visible UI paths and
  real fixture topology.
- Result, signing, and process evidence: the deterministic build/test,
  ordinary, noisy, standard-mutation, and fullscreen-mutation ledgers record
  `stage=completed`, zero applicable fixture/signing/build/test/final exit
  codes, and present result bundles where applicable. The Space-backed
  boundary ledger records test exit 65 with a present result bundle after the
  successful FT Oracle described above. Host-context strict deep codesign
  accepts both the installed App and the Runner re-signed by the final
  invocation. Exact final process readback finds no Flow Tab UITest App,
  UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all eight affected Swift sources parse
  directly and `git diff --check` passes. The runtime policy file contains 145
  lines, preview-projection source 354, its tests 298, shared selection source
  440, its tests 722, transition helper 323, Runtime Truth entrypoint 640, and
  the pre-existing oversized multi-app workflow decreases to 850 lines. The
  four targeted post-entry raw timeout arguments and the prior runtime entry
  policy have no affected caller reference, leaving 241 raw numeric timeout
  arguments across the same 10 pre-existing UI-test files. The deterministic
  rebuild retains one pre-existing unused-value compiler warning in the
  unchanged `FlowTabUITests+SpaceFixtureEdgeInputsWorkflow.swift`. Completed
  ledgers contain only expected destination-selection and non-failing CFPrefs
  diagnostics; fatal-error, assertion, sanitizer, main-thread-checker,
  permission-denial, crash, preview-entry-watchdog, generic compiler-error,
  and test-failure scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 185 MB FT evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FT preview projection`.

### SYNC-036FU Closure Record

- Status: completed. The two ordinary, four noisy, one Space-backed, and one
  provisional-hidden global presentations now publish diagnostics and apply
  exact App selection through one pre-trigger owner. Parent SYNC-036 remains
  open for the remaining owner-specific literals.
- Oracle and design: the owner captures its runtime-log baseline and performs
  an initial diagnostics readback before global presentation. Its trigger
  closure then posts global presentation, writes the exact bundle payload, and
  posts `selectApp`. Success requires one post-trigger generation containing
  published diagnostics, the exact post-baseline
  `select app command applied appID=<bundle>` suffix, the exact selected bundle,
  and either the ordinary/provisional exact App-strip count entry or the noisy
  topology's exact bundle identity. Unpublished diagnostics, a pre-baseline
  marker, a wrong or prefix-colliding bundle, and a partial atomic projection
  remain unresolved.
- Lifecycle and retained time policy: runtime-log filesystem events request
  immediate readback. Because XCUI exposes no diagnostics-value publication
  callback and filesystem delivery can coalesce, the existing named
  cancellable 200ms fallback remains active only while the atomic condition is
  unresolved. The sequential eight- and twelve-second bounds are replaced by
  one named twenty-second terminal watchdog, preserving their cumulative
  observation opportunity. Expiry reports diagnostics publication, exact
  applied-marker and App-projection expectations, final log generation/tail,
  raw diagnostics/value map, owner generation/source, and waiter result. Each
  helper invocation owns its baseline, observer, schedule, generation,
  cancellation, and teardown; each signed workflow owns FlowTab and fixture
  App cleanup.
- Baseline evidence: the immediately preceding committed SYNC-036FT source
  passed the eight existing App-selection and runtime-policy tests in 1.252
  seconds, with xcodebuild testing completed in 2.366 seconds.
- Deterministic and Pressure validation: the final source rebuilt successfully
  and passed 9/9 targeted tests in 1.259 seconds, with xcodebuild testing
  completed in 2.344 seconds. Coverage includes policy compatibility;
  observer-before-initial-readback; unpublished diagnostics rejection;
  post-baseline exact applied suffix; exact selected/App projection;
  topology-dependent counts; synchronous event and scheduled readback; five
  unresolved slow-scheduling observations followed by exact success;
  cancellation; final-watchdog diagnostics; and stale/duplicate callbacks
  across 200 owner generations.
- Implementation correction evidence: the first signed ordinary invocation
  exposed a pre-trigger trace that requested `.value` from diagnostics before
  publication and failed with an exact no-match snapshot in 13.537 seconds.
  The trace now uses the same existence-aware diagnostics snapshot for its
  initial readback. The final source was rebuilt, its deterministic suite was
  rerun, and every affected signed path below used the corrected readback.
- Affected signed UI validation: ordinary Runtime Truth passed 1/1 in 13.482
  seconds, with xcodebuild testing completed in 14.213 seconds, and completed
  both exact App selections. Noisy Pressure topology passed 1/1 in 23.012
  seconds, with xcodebuild testing completed in 24.067 seconds, and completed
  all four selections. Provisional-hidden passed 1/1 in 7.834 seconds, with
  xcodebuild testing completed in 8.906 seconds, and completed its exact
  selection. Scheduling and topology load changed completion time while all
  eight publication/selection results remained identical.
- Independent failure boundary: the final Space-backed invocation completed
  its combined diagnostics/App-selection Oracle, entered window state,
  selected the exact recovered CG-only window at `selectAttempt.1`, and
  confirmed it. Its subsequent independent runtime-log watchdog reproduced
  the existing missing `focus-recovery exhausted` marker boundary in 23.451
  seconds; xcodebuild completed in 32.928 seconds with a present result bundle.
  No App-selection watchdog expired. The later activation-recovery contract
  remains assigned to its dedicated migration slice.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production presentation, selection, exact App/window
  identity, configuration compatibility, module dependencies, and path-intent
  resolution remain unchanged. The deterministic UI-target suite supplies
  atomic state, ordering, initial-state, slow-scheduling, cancellation,
  stale/duplicate generation, watchdog, and lifecycle Pressure evidence.
  Signed ordinary, noisy, Space-backed, and provisional-hidden invocations
  supply the affected visible UI paths and real fixture topology.
- Result, signing, and process evidence: the final deterministic build/test,
  ordinary, noisy, and provisional ledgers record `stage=completed`, zero
  applicable fixture/signing/build/test/final exit codes, and present result
  bundles where applicable. The Space-backed boundary ledger records test
  exit 65 with a present result bundle after the successful FU Oracle described
  above. Host-context strict deep codesign accepts both the installed App and
  the Runner re-signed by the final invocation. Exact final process readback
  finds no Flow Tab UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all five affected Swift sources parse
  directly and `git diff --check` passes. The runtime policy, App-selection
  owner, its tests, Runtime Truth entrypoint, and provisional/Space-backed
  source contain 146, 189, 446, 656, and 306 lines. The two targeted raw
  diagnostics waits and the prior twelve-second policy have no affected caller
  reference, leaving 239 raw numeric timeout arguments across nine
  pre-existing UI-test files. Completed final ledgers contain only expected
  destination-selection and non-failing CFPrefs diagnostics; fatal-error,
  assertion, sanitizer, main-thread-checker, permission-denial, crash,
  App-selection-watchdog, generic compiler-error, and test-failure scans are
  empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 356 MB FU evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FU diagnostics publication`.

### SYNC-036FV Closure Record

- Status: completed. The Space-backed confirmation path now resolves exact
  Switcher diagnostics dismissal through a pre-trigger owner. Parent SYNC-036
  remains open for the remaining owner-specific literals.
- Oracle and design: the owner starts before `confirm` and its synchronous
  initial readback must prove that the exact diagnostics element exists. After
  the existing command-delivery Oracle returns, the caller marks trigger
  completion and accepts only an exact post-trigger `exists == false`
  readback. Pre-trigger absence fails with the element identity and complete
  owner evidence instead of being accepted as confirmation success.
- Lifecycle and retained time policy: Accessibility exposes no target-process
  dismissal callback, so the existing named cancellable 100ms XCUI readback
  cadence activates only when the immediate post-trigger readback remains
  unresolved. The compatible four-second
  `optionTabSwitcherDismissal` watchdog remains solely a terminal failure
  bound and reports phase, generation, source, final existence, and waiter
  result. Resolution and the test invocation own scheduler cancellation;
  workflow teardown owns FlowTab and fixture termination. The subsequent
  window-request, CG activation-route, readback-mismatch, and
  recovery-exhaustion runtime-log observers retain independent owners.
- Baseline evidence: the immediately preceding committed SYNC-036FU source
  selected the exact recovered CG-only window, confirmed it, and passed the
  raw dismissal predicate before reproducing the later independent missing
  `focus-recovery exhausted` marker boundary in 23.813 seconds. xcodebuild
  completed in 33.489 seconds with a present result bundle.
- Deterministic and Pressure validation: the final source rebuilt successfully
  and passed 7/7 targeted runtime-policy and shared nonexistence-owner tests in
  1.099 seconds, with xcodebuild testing completed in 2.143 seconds. Coverage
  includes compatible policy value; gated initial and immediate trigger
  readback; delayed scheduled evidence; five unresolved slow-scheduling
  readbacks followed by exact success; cancellation; final watchdog readback
  and diagnostics; duplicate trigger suppression; and stale callback rejection
  across 200 replacement generations.
- Affected signed UI and independent failure boundary: the final Space-backed
  invocation proved the exact diagnostics baseline before confirmation,
  selected `Recovered Window` at `selectAttempt.1`, posted `confirm`, and
  resolved exact diagnostics absence at its immediate post-trigger readback.
  It then reproduced only the later independent missing
  `focus-recovery exhausted` marker boundary in 22.792 seconds; xcodebuild
  completed in 31.880 seconds with a present result bundle. No dismissal
  watchdog expired. Scheduling changed completion time while the dismissal
  result and later boundary remained identical to the committed baseline.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production confirmation, dismissal, activation, exact
  window identity, configuration compatibility, module dependencies, and
  path-intent resolution remain unchanged. The deterministic UI-target suite
  supplies ordering, initial/trigger state, slow scheduling, cancellation,
  watchdog, and lifecycle Pressure evidence; the signed invocation supplies
  the affected visible UI path and real Space-backed fixture topology.
- Result, signing, and process evidence: the deterministic build and test
  ledgers record `stage=completed`, zero applicable signing/build/test/final
  exit codes, and a present result bundle where applicable. Baseline and final
  Space-backed ledgers record test exit 65 with present result bundles only
  after their successful dismissal Oracles. Host-context strict deep codesign
  accepts both the installed App and the Runner re-signed by the final
  invocation. Exact final process readback finds no Flow Tab UITest App,
  UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 355 lines and remains within the
  single-responsibility guardrail. The targeted raw four-second predicate wait
  has no caller reference, leaving 238 raw numeric timeout arguments across
  nine pre-existing UI-test files. Completed deterministic evidence contains
  only expected destination-selection and non-failing CFPrefs diagnostics;
  fatal-error, assertion, sanitizer, main-thread-checker, permission-denial,
  crash, dismissal-watchdog, generic compiler-error, and unexpected
  test-failure scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 366 MB FV evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FV space-backed dismissal`.

### SYNC-036FW Closure Record

- Status: completed. The provisional-hidden manual-Down path now retains its
  exact single-window App-cycle marker under an owner-named watchdog and an
  explicitly cancelled pre-trigger log baseline. Parent SYNC-036 remains open
  for the remaining owner-specific literals.
- Oracle and design: before posting Down, the caller creates a runtime-log
  baseline that anchors every existing log file and starts its filesystem
  event source. After the independent command-delivery Oracle returns, the
  marker owner performs an immediate readback and accepts only the exact
  post-baseline App name, `key=downArrow`, `windows=1`, and `mode=appCycle`
  composition. Later diagnostics mode and inactive-preview assertions remain
  independent state readbacks.
- Lifecycle and retained time policy: runtime-log file events request
  immediate readback, while the existing named cancellable 200ms fallback
  covers coalesced filesystem delivery only while the exact marker remains
  absent. `provisionalHiddenSingleWindowAdvance` retains the compatible four
  seconds solely as the terminal failure upper bound; expiry reports the
  unmet exact regex, baseline/file-event generations, final character count,
  and log tail. The shared wait helper cancels its observer and fallback on
  resolution or expiry, the caller explicitly cancels the baseline after the
  wait, and workflow teardown owns FlowTab and fixture termination.
- Baseline evidence: the immediately preceding committed SYNC-036FV source
  passed the affected provisional-hidden signed path 1/1 in 8.106 seconds,
  with xcodebuild testing completed in 9.299 seconds. Its exact marker was
  already present on the first post-command runtime-log readback.
- Deterministic and Pressure validation: the final source rebuilt successfully
  and passed 6/6 targeted runtime-policy, real file-event, file-replacement,
  expectation/event-resolution, and lifecycle tests in 0.925 seconds, with
  xcodebuild testing completed in 1.978 seconds. Coverage includes immediate
  initial and notification readback, exact marker composition, cancellation,
  ignored post-resolution callbacks, final watchdog readback and diagnostics,
  and initial/event resolution across 100 owner lifecycles.
- Affected signed UI validation: the final provisional-hidden invocation
  passed 1/1 in 7.668 seconds, with xcodebuild testing completed in 8.334
  seconds. It completed exact diagnostics publication and App selection,
  delivered Down, matched the exact single-window App-cycle marker, cancelled
  the log baseline, and independently confirmed that no window cycle or
  active preview was projected. Scheduling changed completion time while the
  result remained identical to the committed baseline.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production Down handling, provisional-window filtering,
  diagnostics state, configuration compatibility, module dependencies, exact
  window identity, and path-intent resolution remain unchanged. The
  deterministic UI-target suite supplies event, initial-state, cancellation,
  watchdog, file-identity, and lifecycle Pressure evidence; the signed
  invocation supplies the visible path and real fixture topology.
- Result, signing, and process evidence: the baseline, deterministic build and
  test, and final signed UI ledgers all record `stage=completed`, zero
  applicable fixture/signing/build/test/final exit codes, and present result
  bundles where applicable. Host-context strict deep codesign accepts both
  the installed App and the Runner re-signed by the final invocation. Exact
  final process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The runtime policy and provisional/Space-backed
  source contain 156 and 358 lines. The targeted raw four-second timeout has
  no caller reference, leaving 237 raw numeric timeout arguments across nine
  pre-existing UI-test files. Completed ledgers contain only expected
  destination-selection and non-failing CFPrefs diagnostics; fatal-error,
  assertion, sanitizer, main-thread-checker, permission-denial, crash,
  runtime-log-watchdog, generic compiler-error, and test-failure scans are
  empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 760 KB FW evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FW single-window advance`.

### SYNC-036FX Closure Record

- Status: completed. Provisional CG-only exclusion projection after FlowTab
  launch now retains its exact marker under an owner-named watchdog and an
  explicitly cancelled prelaunch log baseline. Parent SYNC-036 remains open
  for the remaining owner-specific literals.
- Oracle and design: `beforeFlowTabLaunch` creates the runtime-log baseline,
  anchors every existing log file, and starts the filesystem event source.
  After launch, the marker owner performs an immediate readback and accepts
  only the exact escaped fixture App name plus
  `hidden-provisional-cg windows=1` from post-baseline content. Global
  presentation, App selection, manual Down, and later diagnostics assertions
  remain independent contracts.
- Lifecycle and retained time policy: runtime-log file events request
  immediate readback, while the existing named cancellable 200ms fallback
  covers coalesced delivery only while the exact marker remains absent.
  `provisionalHiddenProjectionPublication` retains the compatible eight
  seconds solely as a terminal failure upper bound; expiry reports the unmet
  exact regex, baseline/file-event generations, final character count, and
  log tail. The helper cancels its observer/fallback on resolution or expiry,
  the caller explicitly cancels the prelaunch baseline after the assertion,
  and workflow teardown owns both Apps.
- Baseline evidence: the immediately preceding committed SYNC-036FW source
  passed the affected provisional-hidden signed path 1/1 in 7.903 seconds,
  with xcodebuild testing completed in 8.964 seconds. The exact exclusion
  marker resolved before global presentation.
- Deterministic and Pressure validation: the final source rebuilt successfully
  and passed 6/6 targeted runtime-policy, real file-event, file-replacement,
  expectation/event-resolution, and lifecycle tests in 0.913 seconds, with
  xcodebuild testing completed in 1.974 seconds. Coverage includes initial and
  notification readback, anchored append/replacement semantics, exact marker
  composition, cancellation, ignored post-resolution callbacks, final
  watchdog readback and diagnostics, and initial/event resolution across 100
  owner lifecycles.
- Affected signed UI validation: the final provisional-hidden invocation
  passed 1/1 in 7.760 seconds, with xcodebuild testing completed in 8.428
  seconds. It matched the exact provisional exclusion marker, cancelled the
  prelaunch baseline, then independently completed diagnostics publication,
  App selection, the single-window App-cycle marker, and inactive-preview
  readback. Scheduling changed completion time while every result remained
  identical to the committed baseline.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production provisional-window filtering, projection,
  presentation, exact window identity, configuration compatibility, module
  dependencies, and path-intent resolution remain unchanged. The
  deterministic UI-target suite supplies event, initial-state, cancellation,
  watchdog, file-identity, and lifecycle Pressure evidence; the signed
  invocation supplies the visible path and real fixture topology.
- Result, signing, and process evidence: the baseline, deterministic build and
  test, and final signed UI ledgers all record `stage=completed`, zero
  applicable fixture/signing/build/test/final exit codes, and present result
  bundles where applicable. Host-context strict deep codesign accepts both
  the installed App and the Runner re-signed by the final invocation. Exact
  final process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The runtime policy and provisional/Space-backed
  source contain 166 and 361 lines. The targeted raw eight-second timeout has
  no caller reference, leaving 236 raw numeric timeout arguments across nine
  pre-existing UI-test files. Completed ledgers contain only expected
  destination-selection and non-failing CFPrefs diagnostics; fatal-error,
  assertion, sanitizer, main-thread-checker, permission-denial, crash,
  runtime-log-watchdog, generic compiler-error, and test-failure scans are
  empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 752 KB FX evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FX provisional projection`.

### SYNC-036FY Closure Record

- Status: completed. Space-backed CG-only window-layer projection before
  Switcher presentation now retains its exact marker under an owner-named
  watchdog and a closure-scoped prelaunch log-baseline lifetime. Parent
  SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: `beforeFlowTabLaunch` creates the runtime-log baseline,
  anchors every existing log file, and starts the filesystem event source.
  After launch, the marker owner performs an immediate readback and accepts
  only one post-baseline `window-entries` record that binds the escaped App
  name and title to `ax=0`, `sticky=0`, `source=nil`, observed or inferred
  Space evidence, and `publicAXRecovery=1`. Switcher presentation, exact App
  and window selection, selected-source evidence, dismissal, and activation
  evidence remain independent contracts.
- Lifecycle and retained time policy: runtime-log file events request
  immediate readback, while the existing named cancellable 200ms fallback
  covers coalesced delivery only while the exact marker remains absent.
  `spaceBackedWindowLayerProjectionPublication` retains the compatible eight
  seconds solely as a terminal failure upper bound; expiry reports the unmet
  exact regex, baseline/file-event generations, final character count, and
  log tail. Each wait helper cancels its observer/fallback on resolution or
  expiry. The workflow closure installs cancellation before its first
  consumer, shares that prelaunch baseline with the later exact selected-source
  consumer, and cancels it on every closure exit; workflow teardown owns both
  Apps.
- Baseline evidence: the immediately preceding committed SYNC-036FX source
  resolved the exact Space-backed projection, App and window selection, and
  diagnostics dismissal before reaching the later independent
  `focus-recovery exhausted` watchdog. That invocation spent 22.848 seconds
  in the test and xcodebuild testing completed in 32.357 seconds. The expected
  later-boundary failure produced exit 65 and a present result bundle.
- Deterministic and Pressure validation: the final source rebuilt
  successfully and passed 6/6 targeted runtime-policy, real file-event,
  file-replacement, expectation/event-resolution, and lifecycle tests in
  0.864 seconds, with xcodebuild testing completed in 1.991 seconds. Coverage
  includes initial and notification readback, anchored append/replacement
  semantics, exact marker composition, cancellation, ignored post-resolution
  callbacks, final watchdog readback and diagnostics, and initial/event
  resolution across 100 owner lifecycles.
- Affected signed UI validation: the final Space-backed invocation again
  resolved the exact projection, then independently completed diagnostics
  publication, exact App and `Recovered Window` selection, selected-source
  evidence, and diagnostics dismissal. It reached only the same later
  `focus-recovery exhausted` watchdog after 23.036 seconds, with xcodebuild
  testing completed in 32.077 seconds, exit 65, and a present result bundle.
  The final log tail independently read back the same window as CG-only,
  non-sticky, source-less, Space-backed, and public-AX-recovered. Scheduling
  changed completion time while the FY result and later boundary remained
  identical to the committed baseline.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production runtime projection, presentation, activation,
  exact window identity, configuration compatibility, module dependencies,
  and path-intent resolution remain unchanged. The deterministic UI-target
  suite supplies event, initial-state, cancellation, watchdog, file-identity,
  and lifecycle Pressure evidence; the signed invocation supplies the visible
  path and real fixture topology.
- Result, signing, and process evidence: final build and deterministic test
  ledgers record `stage=completed` with zero applicable exit codes. Baseline
  and final signed ledgers record only the documented downstream recovery
  watchdog as `xcodebuild_test-without-building_failed`, with successful
  fixture preparation and signing plus present result bundles. Host-context
  strict deep codesign accepts both the installed App and the Runner re-signed
  by the final invocation. Exact final process readback finds no Flow Tab
  UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The runtime policy and Space-backed source
  contain 176 and 365 lines. The targeted raw eight-second timeout has no
  caller reference, leaving 235 raw numeric timeout arguments across nine
  pre-existing UI-test files. Fatal-error, assertion, sanitizer,
  main-thread-checker, permission-denial, crash, compiler-error, and target
  projection-watchdog scans are empty; baseline and final logs contain the
  documented later recovery-watchdog failure only.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 366 MB FY evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FY space-backed projection`.

### SYNC-036FZ Closure Record

- Status: completed. Exact window-layer source readback for the independently
  selected Space-backed CG-only window now uses an owner-named watchdog.
  Parent SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: XCUI diagnostics first resolve `Recovered Window` and its
  exact CG-backed window number. The source-readback owner then performs an
  immediate readback from the prelaunch anchored log baseline and accepts only
  a post-baseline `window-entries` record whose escaped App/title, `id`, and
  `cg` equal that independent selection and whose state is `ax=0`, `sticky=0`,
  `source=nil`, Space-backed, and `publicAXRecovery=1`. Confirmation,
  dismissal, window-request, activation-route, mismatch, and recovery evidence
  remain independent contracts.
- Lifecycle and retained time policy: runtime-log file events request
  immediate readback, while the existing named cancellable 200ms fallback
  covers coalesced delivery only while the exact selected-source marker
  remains absent. `spaceBackedSelectedWindowLayerSourceReadback` retains the
  compatible eight seconds solely as a terminal failure upper bound; expiry
  reports the unmet exact regex, baseline/file-event generations, final
  character count, and log tail. Each wait helper cancels its
  observer/fallback on resolution or expiry. The workflow closure owns shared
  prelaunch-baseline cancellation on every exit, and workflow teardown owns
  both Apps.
- Baseline evidence: committed SYNC-036FY source resolved the exact selected
  `Recovered Window` source record, diagnostics dismissal, and early
  activation evidence before reaching the later independent
  `focus-recovery exhausted` watchdog. That invocation spent 22.952 seconds
  in the test and xcodebuild testing completed in 32.542 seconds. The expected
  later-boundary failure produced exit 65 and a present result bundle.
- Deterministic and Pressure validation: the final source rebuilt
  successfully and passed 6/6 targeted runtime-policy, real file-event,
  file-replacement, expectation/event-resolution, and lifecycle tests in
  0.917 seconds, with xcodebuild testing completed in 2.015 seconds. Coverage
  includes initial and notification readback, anchored append/replacement
  semantics, exact marker composition, cancellation, ignored post-resolution
  callbacks, final watchdog readback and diagnostics, and initial/event
  resolution across 100 owner lifecycles.
- Affected signed UI validation: the final invocation selected
  `Recovered Window` as `cg:95725:58490`; the independent runtime-log readback
  bound the same title and CG number to `id=cg:95725:58490`, `ax=0`,
  `sticky=0`, `source=nil`, inferred fullscreen Space evidence, and
  `publicAXRecovery=1`. It then completed dismissal and early activation
  contracts before reaching only the same later recovery watchdog after
  23.219 seconds, with xcodebuild testing completed in 32.299 seconds, exit
  65, and a present result bundle. Scheduling changed completion time while
  the exact FZ result and later boundary remained identical to baseline.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production projection, selection, activation, exact window
  identity, configuration compatibility, module dependencies, and path-intent
  resolution remain unchanged. The deterministic UI-target suite supplies
  event, initial-state, cancellation, watchdog, file-identity, and lifecycle
  Pressure evidence; the signed invocation supplies the visible path and real
  fixture topology.
- Result, signing, and process evidence: final build and deterministic test
  ledgers record `stage=completed` with zero applicable exit codes. Baseline
  and final signed ledgers record only the documented downstream recovery
  watchdog as `xcodebuild_test-without-building_failed`, with successful
  fixture preparation and signing plus present result bundles. Host-context
  strict deep codesign accepts both the installed App and the Runner re-signed
  by the final invocation. Exact final process readback finds no Flow Tab
  UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The runtime policy and Space-backed source
  contain 186 and 367 lines. The targeted raw eight-second timeout has no
  caller reference, leaving 234 raw numeric timeout arguments across nine
  pre-existing UI-test files. Fatal-error, assertion, sanitizer,
  main-thread-checker, permission-denial, crash, compiler-error, and selected
  source-watchdog scans are empty; baseline and final logs contain the
  documented later recovery-watchdog failure only.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 366 MB FZ evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036FZ selected window source`.

### SYNC-036GA Closure Record

- Status: completed. Exact Space-backed CG-only `window-request` publication
  after confirmation now uses an owner-named watchdog, and the shared
  post-selection activation baseline has explicit closure-scoped cancellation.
  Parent SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: after XCUI diagnostics independently select
  `Recovered Window`, the workflow creates and anchors the activation log
  baseline before `confirm`. After exact diagnostics dismissal, the request
  owner accepts only a post-trigger marker binding the fixture App ID, selected
  window number/title, `ax=0`, `sticky=false`, `source=nil`, and
  `publicAXRecovery=1`. CG activation route, binding mismatch, and recovery
  exhaustion remain independent later contracts.
- Lifecycle and retained time policy: runtime-log file events request
  immediate readback, while the existing named cancellable 200ms fallback
  covers coalesced delivery only while the exact request marker remains
  absent. `spaceBackedWindowRequestPublication` retains the compatible eight
  seconds solely as a terminal failure upper bound; expiry reports the unmet
  exact regex, baseline/file-event generations, final character count, and
  log tail. Each wait helper cancels its observer/fallback on resolution or
  expiry. A `defer` is installed immediately after activation-baseline creation
  and cancels the shared baseline for success, assertion failure, or early
  closure exit; the dismissal owner and workflow teardown retain their own
  bounded cleanup responsibilities.
- Baseline evidence: committed SYNC-036FZ source resolved the exact request,
  route, and mismatch contracts before reaching the later independent
  `focus-recovery exhausted` watchdog. That invocation spent 22.748 seconds
  in the test and xcodebuild testing completed in 32.329 seconds. The expected
  later-boundary failure produced exit 65 and a present result bundle.
- Deterministic and Pressure validation: the final source rebuilt
  successfully and passed 6/6 targeted runtime-policy, real file-event,
  file-replacement, expectation/event-resolution, and lifecycle tests in
  0.916 seconds, with xcodebuild testing completed in 2.004 seconds. Coverage
  includes initial and notification readback, anchored append/replacement
  semantics, exact marker composition, cancellation, ignored post-resolution
  callbacks, final watchdog readback and diagnostics, and initial/event
  resolution across 100 owner lifecycles.
- Affected signed UI validation: the final invocation selected
  `Recovered Window` as `cg:96314:58687`, then recorded the exact request as
  `appID=com.example.fixture.chrome`, `windowID=cg:96314:58687`, `ax=0`,
  `sticky=false`, `source=nil`, and `publicAXRecovery=1`. It independently
  reached CG route and binding-mismatch evidence before only the same later
  recovery watchdog after 23.170 seconds, with xcodebuild testing completed in
  32.287 seconds, exit 65, and a present result bundle. Scheduling changed
  completion time while the exact GA result and later boundary remained
  identical to baseline.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production confirmation, request routing, activation,
  exact window identity, configuration compatibility, module dependencies,
  and path-intent resolution remain unchanged. The deterministic UI-target
  suite supplies event, initial-state, cancellation, watchdog, file-identity,
  and lifecycle Pressure evidence; the signed invocation supplies the visible
  path and real fixture topology.
- Result, signing, and process evidence: final build and deterministic test
  ledgers record `stage=completed` with zero applicable exit codes. Baseline
  and final signed ledgers record only the documented downstream recovery
  watchdog as `xcodebuild_test-without-building_failed`, with successful
  fixture preparation and signing plus present result bundles. Host-context
  strict deep codesign accepts both the installed App and the Runner re-signed
  by the final invocation. Exact final process readback finds no Flow Tab
  UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The runtime policy and Space-backed source
  contain 196 and 371 lines. The targeted raw eight-second timeout has no
  caller reference, leaving 233 raw numeric timeout arguments across nine
  pre-existing UI-test files. Fatal-error, assertion, sanitizer,
  main-thread-checker, permission-denial, crash, compiler-error, and request
  watchdog scans are empty; baseline and final logs contain the documented
  later recovery-watchdog failure only.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 365 MB GA evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GA window request publication`.

### SYNC-036GB Closure Record

- Status: completed. Exact CG activation-route publication for the selected
  Space-backed window now uses an owner-named watchdog. Parent SYNC-036
  remains open for the remaining owner-specific literals.
- Oracle and design: the workflow anchors its shared activation log baseline
  before `confirm`, independently resolves the exact request, then accepts
  only a post-trigger `focus-attempt route=cg` marker whose PID is present and
  whose `windowID` suffix and `targetCG` both equal the independently selected
  CG window number. Binding mismatch and recovery exhaustion remain separate
  later contracts.
- Lifecycle and retained time policy: runtime-log file events request
  immediate readback, while the existing named cancellable 200ms fallback
  covers coalesced delivery only while the exact route marker remains absent.
  `spaceBackedCGActivationRoutePublication` retains the compatible eight
  seconds solely as a terminal failure upper bound; expiry reports the unmet
  exact regex, baseline/file-event generations, final character count, and
  log tail. Each wait helper cancels its observer/fallback on resolution or
  expiry. The workflow closure owns shared activation-baseline cancellation on
  every exit, and workflow teardown owns both Apps.
- Baseline evidence: committed SYNC-036GA source resolved the exact CG route
  and mismatch contracts before reaching the later independent
  `focus-recovery exhausted` watchdog. That invocation spent 22.590 seconds
  in the test and xcodebuild testing completed in 32.132 seconds. The expected
  later-boundary failure produced exit 65 and a present result bundle.
- Deterministic and Pressure validation: the final source rebuilt
  successfully and passed 6/6 targeted runtime-policy, real file-event,
  file-replacement, expectation/event-resolution, and lifecycle tests in
  0.922 seconds, with xcodebuild testing completed in 2.028 seconds. Coverage
  includes initial and notification readback, anchored append/replacement
  semantics, exact marker composition, cancellation, ignored post-resolution
  callbacks, final watchdog readback and diagnostics, and initial/event
  resolution across 100 owner lifecycles.
- Affected signed UI validation: the final invocation selected
  `Recovered Window` as `cg:96926:58876`, then recorded repeated exact route
  markers with `result=focusedButUnverified`, `pid=96926`,
  `windowID=cg:96926:58876`, and `targetCG=58876`. It independently reached
  binding-mismatch evidence before only the same later recovery watchdog after
  23.086 seconds, with xcodebuild testing completed in 32.204 seconds, exit
  65, and a present result bundle. Scheduling changed completion time while
  the exact GB result and later boundary remained identical to baseline.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production routing, retry behavior, exact window identity,
  configuration compatibility, module dependencies, and path-intent
  resolution remain unchanged. The deterministic UI-target suite supplies
  event, initial-state, cancellation, watchdog, file-identity, and lifecycle
  Pressure evidence; the signed invocation supplies the visible path and real
  fixture topology.
- Result, signing, and process evidence: final build and deterministic test
  ledgers record `stage=completed` with zero applicable exit codes. Baseline
  and final signed ledgers record only the documented downstream recovery
  watchdog as `xcodebuild_test-without-building_failed`, with successful
  fixture preparation and signing plus present result bundles. Host-context
  strict deep codesign accepts both the installed App and the Runner re-signed
  by the final invocation. Exact final process readback finds no Flow Tab
  UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The runtime policy and Space-backed source
  contain 206 and 373 lines. The targeted raw eight-second timeout has no
  caller reference, leaving 232 raw numeric timeout arguments across nine
  pre-existing UI-test files. Fatal-error, assertion, sanitizer,
  main-thread-checker, permission-denial, crash, compiler-error, and route
  watchdog scans are empty; baseline and final logs contain the documented
  later recovery-watchdog failure only.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 365 MB GB evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GB CG activation route`.

### SYNC-036GC Closure Record

- Status: completed. Exact CG binding-readback mismatch publication for the
  selected Space-backed window now uses an owner-named watchdog. Parent
  SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: the workflow anchors its shared activation log baseline
  before `confirm`, independently resolves the exact request and CG route,
  then accepts only a post-trigger `binding-readback-mismatch` marker whose
  route is `cg`, reason is `targetCGNotVisible`, and whose `windowID` suffix
  and `targetCG` both equal the independently selected CG window number.
  Recovery exhaustion remains a separate later contract.
- Lifecycle and retained time policy: runtime-log file events request
  immediate readback, while the existing named cancellable 200ms fallback
  covers coalesced delivery only while the exact mismatch marker remains
  absent. `spaceBackedCGActivationReadbackMismatchPublication` retains the
  compatible eight seconds solely as a terminal failure upper bound; expiry
  reports the unmet exact regex, baseline/file-event generations, final
  character count, and log tail. Each wait helper cancels its
  observer/fallback on resolution or expiry. The workflow closure owns shared
  activation-baseline cancellation on every exit, and workflow teardown owns
  both Apps.
- Baseline evidence: committed SYNC-036GB source resolved the exact CG
  readback-mismatch contract before reaching the later independent
  `focus-recovery exhausted` watchdog. That invocation spent 22.721 seconds
  in the test and xcodebuild testing completed in 32.799 seconds. The expected
  later-boundary failure produced exit 65 and a present result bundle.
- Deterministic and Pressure validation: the final source rebuilt
  successfully and passed 6/6 targeted runtime-policy, real file-event,
  file-replacement, expectation/event-resolution, and lifecycle tests in
  0.931 seconds, with xcodebuild testing completed in 2.020 seconds. Coverage
  includes initial and notification readback, anchored append/replacement
  semantics, exact marker composition, cancellation, ignored post-resolution
  callbacks, final watchdog readback and diagnostics, and initial/event
  resolution across 100 owner lifecycles.
- Affected signed UI validation: the final invocation selected
  `Recovered Window` as `cg:97518:59065`, then recorded repeated exact
  mismatch markers with `route=cg`, `reason=targetCGNotVisible`,
  `windowID=cg:97518:59065`, and `targetCG=59065`. It reached only the same
  later recovery watchdog after 23.036 seconds, with xcodebuild testing
  completed in 32.261 seconds, exit 65, and a present result bundle.
  Scheduling changed completion time while the exact GC result and later
  boundary remained identical to baseline.
- Validation scope: FlowTabCore Unit, app Behavior, and FlowTabTests are not
  relevant because production readback, retry behavior, exact window
  identity, configuration compatibility, module dependencies, and
  path-intent resolution remain unchanged. The deterministic UI-target suite
  supplies event, initial-state, cancellation, watchdog, file-identity, and
  lifecycle Pressure evidence; the signed invocation supplies the visible
  path and real fixture topology.
- Result, signing, and process evidence: final build and deterministic test
  ledgers record `stage=completed` with zero applicable exit codes. Baseline
  and final signed ledgers record only the documented downstream recovery
  watchdog as `xcodebuild_test-without-building_failed`, with successful
  fixture preparation and signing plus present result bundles. Host-context
  strict deep codesign accepts both the installed App and the Runner re-signed
  by the final invocation. Exact final process readback finds no Flow Tab
  UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The runtime policy and Space-backed source
  contain 216 and 375 lines. The targeted raw eight-second timeout has no
  caller reference, leaving 231 raw numeric timeout arguments across nine
  pre-existing UI-test files. Fatal-error, assertion, sanitizer,
  main-thread-checker, permission-denial, crash, compiler-error, and mismatch
  watchdog scans are empty; baseline and final logs contain the documented
  later recovery-watchdog failure only.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 366 MB GC evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GC CG readback mismatch`.

### SYNC-036GD Closure Record

- Status: completed. Terminal recovery-watchdog evidence for the selected
  Space-backed CG-only window now uses the structured SYNC-006 contract and an
  owner-named test watchdog. Parent SYNC-036 remains open for the remaining
  owner-specific literals.
- Pre-change failing signal and root cause: committed source repeatedly
  selected `Recovered Window` as `cg:98279:59158`, published the exact
  request, CG route, `targetCGNotVisible` mismatch, and after five seconds the
  terminal `focus-recovery state=watchdogExpired` marker. The UI helper still
  searched for the removed `focus-recovery exhausted ... attempts=` spelling,
  so the independent runtime condition arrived while XCTest failed only after
  its raw eight-second observation bound. The test spent 23.037 seconds and
  xcodebuild testing completed in 32.207 seconds, exit 65, with a present
  result bundle.
- Oracle and design: the workflow keeps its shared activation-log baseline
  anchored before `confirm`, independently resolves request, route, and
  mismatch evidence, then accepts only a structured watchdog failure whose
  unmet condition is `exactTargetWindowFocusedOrFrontmost`; escaped App ID,
  `windowID` suffix, and `targetCG` equal the selected fixture target;
  `lastTrigger` is `watchdogReadback`; and the final live-process observation
  has `conditionSatisfied=0`, `targetVisible=0`, and `focusedCG=nil`.
  Scheduling and the number of polling attempts cannot establish this result.
- Lifecycle and retained time policy: runtime-log file events request an
  immediate readback. The existing named cancellable 200ms fallback covers
  coalesced delivery only while the exact terminal marker remains absent.
  `spaceBackedCGActivationRecoveryFailurePublication` retains the compatible
  eight seconds solely as the test observation failure upper bound above the
  production-owned five-second recovery watchdog; expiry reports the unmet
  exact regex, generations, character count, and final log tail. The helper
  cancels its observer/fallback on resolution or expiry, the workflow closure
  owns shared-baseline cancellation, the production coordinator owns its
  generation/observers/poll/watchdog, and workflow teardown owns both Apps.
- Existing-test semantic guard: the protected result remains exact
  Space-backed unverified activation. The user objective requires watchdogs to
  report the unmet condition and final observation. Authorization
  `60ab2d146297d60a` records the equivalent transition from the retired marker
  to the structured coordinator failure while preserving the selected App and
  window identity.
- Pre- and post-change Unit/Behavior validation: before the test edit, 6/6
  targeted focus-recovery tests passed in 0.004 seconds, with xcodebuild
  testing completed in 2.486 seconds. After the edit the same 6/6 passed in
  0.007 seconds, with xcodebuild testing completed in 1.367 seconds. They
  cover immediate state, observer-before-action event completion, slow polling
  cadence, cancellation, final watchdog diagnostics, final-readback boundary
  completion, and projection-driven orchestration.
- Deterministic UI-target and lifecycle Pressure validation: the final source
  rebuilt successfully and passed 6/6 runtime-policy, real file-event,
  file-replacement, expectation/event-resolution, and 100-owner lifecycle
  tests in 0.925 seconds, with xcodebuild testing completed in 1.971 seconds.
  Initial/event resolution, cancellation, ignored post-resolution callbacks,
  final watchdog readback, and diagnostic composition remain covered.
- Affected signed UI validation: the final invocation selected
  `Recovered Window` as `cg:98610:59264` and passed 1/1 in 18.707 seconds,
  with xcodebuild testing completed in 19.439 seconds. The terminal marker
  bound App ID `com.example.fixture.chrome`, PID 98610,
  `windowID=cg:98610:59264`, `targetCG=59264`, `pollingAttempt=9`,
  `lastTrigger=watchdogReadback`, the unmet exact-target condition, and final
  `focusedCG=nil`, `frontmostCG=59263`, `visibleCG=59263` evidence. Different
  scheduling changed completion time while the exact result remained the same.
- Validation scope: FlowTabCore Unit is not relevant because the contract
  belongs to the AppKit/ApplicationServices runtime boundary. The targeted
  FlowTabTests supply deterministic Unit/Behavior evidence; the UI-target
  suite supplies observation lifecycle Pressure; and the signed invocation
  supplies the visible path and real Space/fullscreen fixture topology. The
  canonical process-level runtime-topology Pressure scenario is not relevant
  because production activation cadence, observer ownership, retry behavior,
  state transitions, and hot-path work are unchanged.
- Result, signing, and process evidence: all post-change build, Behavior,
  deterministic UI, and signed UI ledgers record `stage=completed`, zero
  applicable exit codes, and present result bundles for test actions. The
  pre-change UI ledger records only the reproduced stale-marker failure, with
  successful fixture preparation/signing and a present result bundle.
  Host-context strict deep codesign accepts both the installed App and the
  Runner re-signed by the final invocation. Exact process readback finds no
  Flow Tab UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The runtime policy and Space-backed source
  contain 226 and 380 lines. The targeted raw eight-second timeout has no
  caller reference, leaving 230 raw numeric timeout arguments across eight
  pre-existing UI-test files. The final regex independently matches the exact
  pre-change structured marker. Fatal-error, assertion, sanitizer,
  main-thread-checker, permission-denial, crash, and compiler-error scans are
  empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 671 MB GD evidence root contains
  only rebuildable DerivedData, result bundles, caches, and logs. It is removed
  after this durable record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GD recovery failure evidence`.

### SYNC-036GE Closure Record

- Status: completed. Four mock-runtime Switcher interaction tests now share a
  file-owned foreground-readiness watchdog and one diagnostic assertion.
  Parent SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: the assertion delegates to the shared launch-readiness
  helper, whose immediate readback and platform wait accept only
  `XCUIApplication.State.runningForeground`. Its existing bounded activation
  fallback can request another readback, while elapsed time cannot establish
  readiness. Expiry identifies the scenario and reports
  `unmetCondition=runningForeground` plus the final App state. Later Home,
  preview, runtime-log, dismissal, and repeated-presentation evidence remain
  independent contracts.
- Lifecycle and retained time policy: the file-owned
  `foregroundReadiness` policy retains the compatible ten seconds solely as a
  terminal failure upper bound. Each test invocation owns its synchronous
  XCUIApplication wait and App teardown. The shared helper retains no
  persistent observer or timer after the bounded platform call returns.
- Existing-test semantic guard: all four protected results remain their exact
  Home order, first Control+Tab selection/preview, delayed-entry prewarmed
  preview, and twelve-cycle presentation outcomes. Authorization
  `8726f6021ac45452` records the owner extraction and additive diagnostics
  required by the user objective while preserving those Oracles.
- Pre-change signed UI and Pressure baseline: all 4/4 affected tests passed in
  43.767 seconds, with xcodebuild testing completed in 44.484 seconds. The
  repeated-presentation Pressure path completed all twelve cycles and passed
  in 23.742 seconds.
- Deterministic policy validation: the rebuilt UI-test target passed the
  additive compatibility and finite-positive policy test 1/1 in 0.182
  seconds, with xcodebuild testing completed in 1.344 seconds.
- Final signed UI and Pressure validation: all 4/4 affected tests passed in
  44.238 seconds, with xcodebuild testing completed in 44.921 seconds. The
  individual paths passed in 4.464, 12.514, 24.087, and 3.172 seconds. The
  repeated-presentation path again completed all twelve cycles. Scheduling
  changed completion time while every exact result remained unchanged.
- Validation scope: affected signed UI supplies the visible interaction paths,
  and its twelve-cycle repeated-presentation case supplies the applicable
  Pressure evidence. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, and
  process-level Pressure are not relevant because production launch,
  activation, UI behavior, state transitions, module wiring, configuration,
  and path resolution are unchanged.
- Result, signing, and process evidence: the post-change build and both test
  ledgers record `stage=completed` with zero applicable exit codes; both test
  actions retain present result bundles before cleanup. Host-context strict
  deep codesign accepts the installed App and final Runner. Exact process
  readback finds no Flow Tab UITest App, UI-test Runner, xcodebuild, or fixture
  process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 365 lines, below the 400-line
  source guardrail. Four caller literals are removed, leaving 226 raw numeric
  timeout arguments across eight pre-existing UI-test files. Fatal-error,
  unexpected-failure, failed-signing, crash, and abort scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 9.2 MB GE evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GE interaction foreground readiness`.

### SYNC-036GF Closure Record

- Status: completed. The mock-runtime Home phase now uses the shared
  trigger-and-content owner, while parent SYNC-036 remains open for the
  remaining owner-specific literals.
- Oracle and design: the test resolves the exact Home button query and Home
  content before navigation. `tapFirstHittableAndWaitForExistence` installs
  the exact content-existence observer first, performs its initial readback,
  then waits for and taps the first exact existing, hittable Home button.
  Success requires both exact conditions; elapsed time cannot establish either
  one. App-row publication and Home/Option+Tab order remain later independent
  Oracles.
- Lifecycle and retained time policy: the owner uses the shared named serial
  100ms XCUI readback cadence only while Home content remains absent, and
  cancels it on initial resolution, later resolution, watchdog, or helper
  return. `FlowTabUITestSupportWatchdogPolicy.tabNavigation` supplies a named
  six-second terminal bound for each exact condition. It replaces each raw
  five-second failure bound and only extends the opportunity to observe the
  same evidence under slow scheduling. The test owns App teardown.
- Existing-test semantic guard: authorization `90b965ddc9a50bca` preserves the
  exact Home button-hittable, Home-content-exists, App-order, and Switcher-order
  results while applying the user-required observer-before-trigger ordering,
  policy ownership, and final diagnostics.
- Pre-change signed UI baseline: the affected Home/Option+Tab order test passed
  1/1 in 12.436 seconds, with xcodebuild testing completed in 13.531 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 9/9
  shared policy, initial-readback, preinstalled-event, scheduled-readback,
  reentrancy, final-watchdog-readback, exact-hittable, 100-cancel/replacement,
  and 100-scheduled-lifecycle tests in 1.687 seconds, with xcodebuild testing
  completed in 2.784 seconds.
- Final signed UI validation: the affected Home/Option+Tab order test passed
  1/1 in 11.322 seconds, with xcodebuild testing completed in 12.036 seconds.
  The trace shows Home-content readback installed before Home-button discovery
  and tap, followed by all six exact App rows and the unchanged Switcher order.
- Validation scope: deterministic UI-target tests supply the observation and
  lifecycle Pressure evidence, and the affected signed UI supplies the visible
  navigation and ordering path. FlowTabCore Unit, app Unit/Behavior,
  FlowTabTests, and process-level Pressure are not relevant because shared
  infrastructure, production behavior, module wiring, configuration, and path
  resolution are unchanged.
- Result, signing, and process evidence: the post-change build and both test
  ledgers record `stage=completed` with zero applicable exit codes; both test
  actions retain present result bundles before cleanup. Host-context strict
  deep codesign accepts the installed App and final Runner. Exact process
  readback finds no Flow Tab UITest App, UI-test Runner, xcodebuild, or fixture
  process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 381 lines, below the 400-line
  source guardrail. The two caller literals are removed, leaving 224 raw
  numeric timeout arguments across eight pre-existing UI-test files.
  Fatal-error, unexpected-failure, failed-signing, crash, and abort scans are
  empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 700 KB GF evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GF Home navigation`.

### SYNC-036GG Closure Record

- Status: completed. The six-row Home runtime-order phase now resolves one
  atomic Accessibility projection, while parent SYNC-036 remains open for the
  remaining owner-specific literals.
- Oracle and design: the test resolves all six exact Home-row identifiers and
  starts the shared observation before Home navigation with evidence
  acceptance disabled. Exact navigation completion enables acceptance and
  requests an immediate readback. Success requires one snapshot containing
  all six exact identities with existing rows and finite frame positions.
  Values and prior declaration order remain unconstrained; the snapshot's
  ascending-frame identities are mapped one-to-one back to App IDs and
  compared with the independently published Option+Tab App order. Elapsed
  time cannot establish either projection or equality.
- Lifecycle and retained time policy: the owner uses the shared named serial
  100ms XCUI readback cadence because Accessibility supplies no atomic layout
  callback. Initial, trigger, and scheduled readbacks all use the same
  acceptance gate. Resolution, expiry, explicit cancellation, and deinit
  cancel scheduled work; the test's `defer` owns observation cancellation and
  App teardown. `FlowTabUITestHomeRuntimeOrderProjectionPolicy.watchdog`
  retains 36 seconds solely as the terminal failure upper bound matching the
  former cumulative six-by-six-second opportunity. Expiry reports acceptance,
  expected identities, frame-order policy, and the final atomic snapshot.
- Existing-test semantic guard: authorization `7122979c146802bd` preserves the
  six exact Home-row identities and the actual Home-versus-Option+Tab order
  contract while applying the user-required observer-before-trigger and
  single-projection evidence rules.
- Pre-change signed UI baseline: the affected Home/Option+Tab order test passed
  1/1 in 11.617 seconds, with xcodebuild testing completed in 12.317 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 16/16
  policy, exact ordered projection, unconstrained value/order projection,
  missing/nonfinite rejection, initial/trigger/scheduled readback,
  cancellation, final-watchdog diagnostics, reentrancy, 100-owner
  cancel/replacement Pressure, and 100-scheduled-lifecycle Pressure tests in
  2.520 seconds, with xcodebuild testing completed in 3.661 seconds.
- Signed UI feedback and final validation: the first post-change invocation
  exposed a row-identifier/App-ID domain mismatch in 7.169 seconds, with
  xcodebuild testing completed in 16.010 seconds. The exact one-to-one identity
  mapping was restored at the comparison boundary. The rebuilt final source
  then passed the affected test 1/1 in 6.829 seconds, with xcodebuild testing
  completed in 7.916 seconds. Scheduling changed completion time while the
  exact projection and cross-surface ordering result remained stable.
- Validation scope: deterministic UI-target tests supply expectation,
  observation, cancellation, diagnostics, and lifecycle Pressure evidence;
  the affected signed UI supplies the visible Home and Option+Tab path.
  FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and
  process-level Pressure are not relevant because production behavior,
  runtime order, fixture topology, module wiring, configuration, path
  resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: the final build, owner/Pressure, and
  affected UI ledgers record `stage=completed` with zero applicable exit codes;
  both final test actions retain present result bundles before cleanup. The
  intermediate UI ledger records the identity-domain assertion feedback with
  successful signing and a present result bundle. Host-context strict deep
  codesign accepts the installed App and final Runner. Exact process readback
  finds no Flow Tab UITest App, UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly and `git diff --check` passes. The observation, focused owner-test,
  and affected interaction sources contain 326, 533, and 398 lines; the
  production-path test source remains below the 400-line guardrail and the
  533-line file remains one focused observation-owner test responsibility.
  The six caller literals are removed, leaving 223 raw numeric timeout
  arguments across eight pre-existing UI-test files. Fatal-error,
  unexpected-failure, failed-signing, crash, abort, sanitizer,
  main-thread-checker, permission-denial, and compiler-error scans are empty
  across the successful final evidence.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 171 MB GG evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GG atomic Home order`.

### SYNC-036GH Closure Record

- Status: completed. The fresh Option+Tab phase now resolves the exact ordered
  Switcher App projection through the shared owner, while parent SYNC-036
  remains open for the remaining owner-specific literals.
- Oracle and design: the independently captured atomic Home order supplies the
  expected App IDs. Inside the existing Option-key modifier scope, the generic
  helper registers observation and performs a gated initial readback before
  typing Tab. Trigger return enables acceptance and requests an immediate
  readback. Success requires one existing summary projection whose parsed
  bundle identifiers exactly equal the Home order; window counts remain
  outside this ordering contract. Scheduled readbacks can reveal a later valid
  projection, while elapsed time cannot establish publication or equality.
- Lifecycle and retained time policy: the shared owner uses the named serial
  100ms XCUI readback cadence because Accessibility supplies no atomic
  Switcher-projection callback. Resolution, expiry, explicit cancellation, and
  deinit cancel scheduled work; the helper's `defer` owns observation
  cancellation and the test owns modifier scope and App teardown.
  `FlowTabUITestSwitcherAppProjectionPolicy.runtimeOrderWatchdog` retains five
  seconds solely as the compatible terminal failure upper bound. Expiry
  reports acceptance, expected ordered App IDs, and the final raw and parsed
  projection.
- Existing-test semantic guard: authorization `42d77ebf7d6b1094` preserves the
  actual Home-versus-Option+Tab App order result, with exact ordered App IDs as
  the Oracle and window counts remaining independent of this assertion.
- Pre-change signed UI baseline: the affected Home/Option+Tab order test passed
  1/1 in 6.789 seconds, with xcodebuild testing completed in 7.473 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 16/16
  policy, parser, exact ordered-ID/count-independence, wrong-order, missing,
  prefix-collision, initial, trigger-gated, scheduled, cancellation,
  final-watchdog, reentrancy, 100-owner cancel/replacement Pressure, and
  100-scheduled-lifecycle Pressure tests in 2.642 seconds, with xcodebuild
  testing completed in 3.803 seconds.
- Final signed UI validation: the affected test passed 1/1 in 5.695 seconds,
  with xcodebuild testing completed in 6.372 seconds. The trace records the
  first summary readback before the Option+Tab trigger, then the trigger and
  exact post-trigger projection readback. Scheduling changed completion time
  while the exact cross-surface ordering result remained stable.
- Validation scope: deterministic UI-target tests supply expectation,
  observation, cancellation, diagnostics, and lifecycle Pressure evidence;
  the affected signed UI supplies the visible Home and Option+Tab path.
  FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and
  process-level Pressure are not relevant because production behavior,
  runtime order, fixture topology, module wiring, configuration, path
  resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: the build, owner/Pressure, baseline,
  and final UI ledgers record `stage=completed` with zero applicable exit
  codes; every test action retains a present result bundle before cleanup.
  Host-context strict deep codesign accepts the installed App and final Runner.
  Exact process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: all three affected Swift sources parse
  directly and `git diff --check` passes. The shared projection owner,
  focused owner-test, and affected interaction sources contain 509, 433, and
  399 lines; each 400-800-line file retains one focused responsibility and the
  interaction source remains below the 400-line guardrail. The caller literal
  is removed, leaving 222 raw numeric timeout arguments across eight
  pre-existing UI-test files. Fatal-error, unexpected-failure, failed-signing,
  crash, abort, sanitizer, main-thread-checker, permission-denial, and
  compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 984 KB GH evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GH Switcher order`.

### SYNC-036GI Closure Record

- Status: completed. The Control+Tab selected-window and preview-count
  projection keeps its evidence-driven owner and now uses a file-owned named
  watchdog. Parent SYNC-036 remains open for the remaining owner-specific
  literals.
- Oracle and design: `performAndWaitForSwitcherDiagnostics` registers the
  summary observation and performs a gated initial readback before typing Tab
  inside the existing Control-key modifier scope. Trigger return enables
  acceptance and requests an immediate readback. Success requires one atomic
  summary containing both `selectedWindow=mock-current-secondary` and
  `previewImages=2`; scheduled readbacks can reveal a later valid snapshot,
  while elapsed time cannot establish either value. Exact preview-element,
  window-card, and runtime-log evidence remain later independent contracts.
- Lifecycle and retained time policy: the unchanged shared owner uses the
  named serial 100ms XCUI readback cadence because Accessibility supplies no
  atomic summary callback. Resolution, expiry, explicit cancellation, and
  deinit cancel scheduled work; the helper's `defer` owns observation cleanup,
  the modifier closure owns Control-key scope, and the test owns App teardown.
  `controlTabDiagnostics` retains five seconds solely as the compatible
  terminal failure upper bound. Expiry reports acceptance, both expected keys,
  and the final summary readback.
- Existing-test semantic guard: authorization `ba5d85151b9cfc3c` preserves the
  exact selected-window and preview-count result while naming its failure
  bound. The same authorization records the syntax-only explicit parentheses
  around the unchanged Home identity mapping after the compiler identified
  an ambiguous trailing-closure spelling.
- Pre-change signed UI baseline: the affected Control+Tab test passed 1/1 in
  4.716 seconds, with xcodebuild testing completed in 5.425 seconds.
- Deterministic policy validation: the final rebuilt target passed the
  compatible `[10, 5]` finite-positive policy rule 1/1 in 0.204 seconds, with
  xcodebuild testing completed in 1.299 seconds.
- Final signed UI validation: the affected test passed 1/1 in 4.341 seconds,
  with xcodebuild testing completed in 5.011 seconds. The trace again records
  the summary readback before Control+Tab, then the trigger and exact atomic
  selected-window/preview-count readback. Scheduling changed completion time
  while the exact result remained stable.
- Validation scope: the deterministic UI-target policy test supplies named
  bound evidence and the affected signed UI supplies the visible Control+Tab
  path. Observer lifecycle Pressure, FlowTabCore Unit, app Unit/Behavior,
  FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant
  because observer implementation, evidence predicates, production behavior,
  fixture topology, module wiring, configuration, path resolution, and
  hot-path work are unchanged.
- Result, signing, and process evidence: the final build, policy, baseline, and
  final UI ledgers record `stage=completed` with zero applicable exit codes;
  every test action retains a present result bundle before cleanup.
  Host-context strict deep codesign accepts the installed App and final Runner.
  Exact process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 399 lines, below the 400-line
  source guardrail. The first successful build surfaced the trailing-closure
  ambiguity warning; the explicit-parentheses correction kept the same Oracle,
  and the final successful build has no source warning or compiler error.
  The caller literal is removed, leaving 221 raw numeric timeout arguments
  across eight pre-existing UI-test files. Fatal-error, unexpected-failure,
  failed-signing, crash, abort, sanitizer, main-thread-checker, and
  permission-denial scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 3.5 MB GI evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GI ControlTab diagnostics`.

### SYNC-036GJ Closure Record

- Status: completed. The selected secondary-window preview now resolves from
  exact post-trigger Accessibility evidence, while parent SYNC-036 remains
  open for the remaining owner-specific literals.
- Oracle and design: the test resolves the exact secondary preview identity
  and starts the shared element-existence observation before Control+Tab. Its
  immediate initial readback must prove that identity absent and scheduled
  work remains deferred. After the existing exact selected-window and
  preview-count projection resolves, trigger completion requests an immediate
  readback. Success requires the same exact preview identity to exist; a later
  valid readback can resolve through the shared scheduled cadence. The two
  exact window cards and runtime-log assertions retain independent Oracles.
- Lifecycle and retained time policy: the shared owner uses the named serial
  100ms XCUI readback cadence because Accessibility supplies no element-change
  callback. It checks immediately before scheduling and schedules only while
  the exact preview remains absent. Resolution, expiry, explicit cancellation,
  and deinit cancel scheduled work. The test's `defer` owns observation
  cleanup, the modifier closure owns Control-key scope, and the test owns App
  teardown. `controlTabSelectedPreview` retains five seconds solely as the
  compatible terminal failure upper bound. Expiry reports exact identity,
  phase, source, final existence, and wait result.
- Existing-test semantic guard: authorization `887bdaeee2821d40` preserves the
  exact secondary preview identity, visible selection result, independent
  summary/window/log assertions, and compatible failure bound while applying
  the explicitly requested observer-before-trigger and initial/readback
  evidence rules.
- Pre-change signed UI baseline: the affected Control+Tab test passed 1/1 in
  4.460 seconds, with xcodebuild testing completed in 5.172 seconds. The direct
  host-authorization rerun also passed 1/1 in 4.587 seconds, with xcodebuild
  testing completed in 5.655 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 7/7
  finite-positive policy, post-trigger readback, delayed scheduled evidence,
  slow-scheduling stability, cancellation, final-watchdog diagnostics, and
  200-replacement Pressure tests in 1.089 seconds, with xcodebuild testing
  completed in 1.897 seconds.
- Final signed UI validation: the affected test passed 1/1 in 3.530 seconds,
  with xcodebuild testing completed in 4.215 seconds. The trace records the
  exact preview absent before Control+Tab and present on the immediate
  post-trigger readback. Scheduling changed completion time while the exact
  identity and visible result remained stable.
- Validation scope: deterministic UI-target tests supply policy, observation,
  cancellation, diagnostics, and repeated-lifecycle Pressure evidence; the
  affected signed UI supplies the visible Control+Tab path. FlowTabCore Unit,
  app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level Pressure
  are not relevant because production behavior, fixture topology, module
  wiring, configuration, path resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: the build, owner/Pressure, baseline,
  direct-authorization, and final UI ledgers record `stage=completed` with zero
  applicable exit codes; every test action retains a present result bundle
  before cleanup. Host-context strict deep codesign accepts the installed App
  and final Runner. Exact process readback finds no Flow Tab UITest App,
  UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The shared existence owner and interaction
  sources contain 197 and 399 lines; the interaction source remains below the
  400-line guardrail. The caller literal is removed, leaving 220 raw numeric
  timeout arguments across eight pre-existing UI-test files. Fatal-error,
  unexpected-failure, failed-signing, crash, abort, sanitizer,
  main-thread-checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 4.8 MB GJ evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GJ selected preview evidence`.

### SYNC-036GK Closure Record

- Status: completed. The current-app two-card projection now resolves from one
  observer-before-Control+Tab snapshot, while parent SYNC-036 remains open for
  the remaining owner-specific literals.
- Oracle and design: the shared card helper starts before the nested
  Control+Tab trigger and requires its immediate initial snapshot to contain
  zero cards. Matching snapshots observed while diagnostics and exact-preview
  evidence are still resolving remain gated. Trigger return enables acceptance
  and requests an immediate readback. Success requires exactly two cards, the
  `Current Primary`/`Current Secondary` title multiset, and two distinct stable
  identifiers in one snapshot. The caller uses those same card records for the
  unioned-width and image-marker assertions, eliminating the later independent
  collection rebuild.
- Lifecycle and retained time policy: XCUI supplies no atomic card-collection
  callback, so the shared owner retains the named serial 100ms readback cadence
  with an immediate initial check. The acceptance gate prevents trigger-period
  readbacks from establishing success. Resolution, expiry, explicit
  cancellation, and deinit cancel scheduled work; the helper's `defer` owns
  cancellation, the modifier closure owns Control-key scope, and the test owns
  App teardown. `controlTabWindowCards` retains five seconds solely as the
  compatible terminal failure upper bound. Expiry reports the expected title,
  count, identity constraints and final atomic snapshot.
- Existing-test semantic guard: authorization `6b6247c5ab20723e` preserves the
  two-card title multiset, distinct stable identities, bounds and image result,
  plus the compatible failure bound while applying the explicitly requested
  observer-before-trigger, initial-readback, and single-snapshot rules.
- Pre-change signed UI baseline: the affected Control+Tab test passed 1/1 in
  3.631 seconds, with xcodebuild testing completed in 4.347 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 3/3
  compatible-policy, trigger-gated exact-identity/delayed-evidence, and
  100-generation lifecycle Pressure tests in 0.517 seconds, with xcodebuild
  testing completed in 1.635 seconds. The shared cases also cover stale and
  excluded titles, duplicate identities, cancellation, ignored late callbacks,
  and watchdog final readback.
- Final signed UI validation: the affected test passed 1/1 in 3.959 seconds,
  with xcodebuild testing completed in 4.638 seconds. The trace records an
  empty collection before Tab, gated matching collections during the nested
  trigger, and the accepted immediate two-card readback after exact diagnostics
  and preview evidence. Scheduling changed completion time while the card
  identities and visible result remained stable.
- Validation scope: deterministic UI-target tests supply expectation,
  trigger-gating, cancellation, diagnostics, and lifecycle Pressure evidence;
  the affected signed UI supplies the visible Control+Tab path. FlowTabCore
  Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level
  Pressure are not relevant because production behavior, fixture topology,
  module wiring, configuration, path resolution, and hot-path work are
  unchanged.
- Result, signing, and process evidence: the build, owner/Pressure, baseline,
  and final UI ledgers record `stage=completed` with zero applicable exit codes;
  both test actions retain present result bundles before cleanup. Host-context
  strict deep codesign accepts the installed App and final Runner. Exact
  process readback finds no Flow Tab UITest App, UI-test Runner, xcodebuild, or
  fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The shared card owner, focused owner-test, and
  interaction sources contain 295, 286, and 399 lines; every source remains
  below the 400-line guardrail. The caller literal is removed, leaving 219 raw
  numeric timeout arguments across eight pre-existing UI-test files.
  Fatal-error, unexpected-failure, failed-signing, crash, abort, sanitizer,
  main-thread-checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 3.1 MB GK evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GK window card projection`.

### SYNC-036GL Closure Record

- Status: completed. The final Control+Tab runtime-log contract now uses its
  shared named watchdog and explicit baseline cancellation, while parent
  SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: before Control+Tab, `makeRuntimeLogFileSnapshot()` starts
  the file-system source and captures each log file's identity/byte anchor plus
  the source generation. The later wait immediately reads only post-anchor
  bytes, closing any event gap before callback registration, then combines file
  events with the shared scheduled fallback. Success still requires all five
  exact latency, elapsed-delay, hotkey, initial-advance, and
  preview-batch-completed markers. Elapsed time cannot establish delivery.
- Lifecycle and retained time policy: the local test now explicitly cancels
  its runtime-log baseline/file-event source with `defer`. The wait invocation
  owns condition-observer and scheduled-fallback cancellation on resolution,
  expiry, explicit teardown, and deinit. The shared named 200ms readback cadence
  remains the platform fallback for coalesced or missed file notifications.
  `FlowTabUITestRuntimeLogObservationPolicy.defaultWatchdog` retains eight
  seconds solely as the compatible terminal failure upper bound. Expiry reports
  missing markers, baseline/current generation, character count, final log
  tail, wait result, and log path.
- Existing-test semantic guard: authorization `7cf8bc30fdc7da71` preserves all
  five exact post-baseline markers, file-anchor/generation semantics, immediate
  readback, event/fallback observation, and terminal diagnostics while applying
  the explicitly requested named-policy and lifecycle ownership rules.
- Pre-change signed UI baseline: the affected Control+Tab test passed 1/1 in
  4.023 seconds, with xcodebuild testing completed in 4.741 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 5/5
  shared policy, post-baseline append, replacement/truncation, event/final
  readback contract, cancellation, watchdog, and 100-generation lifecycle
  Pressure tests in 0.804 seconds, with xcodebuild testing completed in 1.933
  seconds.
- Final signed UI validation: the affected test passed 1/1 in 3.757 seconds,
  with xcodebuild testing completed in 4.429 seconds. The same five exact
  post-baseline markers resolved under the shared failure bound; scheduling
  changed completion time while the orchestration result remained stable.
- Validation scope: deterministic UI-target tests supply policy, file-event,
  anchor/reset, cancellation, diagnostics, and lifecycle Pressure evidence;
  the affected signed UI supplies the visible Control+Tab path. FlowTabCore
  Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, and process-level
  Pressure are not relevant because production behavior, observer
  implementation, evidence predicate, fixture topology, module wiring,
  configuration, path resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: the build, owner/Pressure, baseline,
  and final UI ledgers record `stage=completed` with zero applicable exit codes;
  both test actions retain present result bundles before cleanup. Host-context
  strict deep codesign accepts the installed App and final Runner. Exact
  process readback finds no Flow Tab UITest App, UI-test Runner, xcodebuild, or
  fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 399 lines, below the 400-line
  source guardrail. The caller literal is removed, leaving 218 raw numeric
  timeout arguments across eight pre-existing UI-test files. Fatal-error,
  unexpected-failure, failed-signing, crash, abort, sanitizer,
  main-thread-checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 3.2 MB GL evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GL runtime log watchdog`.

### SYNC-036GM Closure Record

- Status: completed. Every repeated-presentation dismissal now resolves through
  one observer-before-confirm exact-element transition, while parent SYNC-036
  remains open for the remaining owner-specific literals.
- Oracle and design: each of the twelve loop iterations resolves the exact
  diagnostics-summary element and starts a fresh nonexistence owner before
  `.confirm`. Its immediate baseline readback must prove that iteration's
  presentation exists. Command delivery then returns, the owner enables
  post-trigger evidence, requests an immediate readback, and accepts only exact
  summary absence. The preview-transition and post-baseline runtime-log Oracles
  remain independent prerequisites within the same iteration.
- Lifecycle and retained time policy: Accessibility supplies no switcher-
  dismissal callback, so the shared owner retains its named serial 100ms XCUI
  readback cadence only while the exact summary remains present. The helper
  invocation owns resolution, expiry, explicit cancellation, and deinit cleanup
  before the next loop iteration starts; the test owns App teardown.
  `delayedPresentationDismissal` retains five seconds solely as the compatible
  terminal failure upper bound. Baseline mismatch and expiry report the exact
  identity, phase, evidence source, final existence, generation, and wait result.
- Existing-test semantic guard: authorization `491fa1414ec1d823` preserves the
  twelve presentations, exact preview/log prerequisites, delivered confirm
  command, exact summary-absence result, and compatible failure bound while
  applying the explicitly requested observer-before-trigger, initial-readback,
  and lifecycle-ownership rules.
- Pre-change signed UI Pressure baseline: the affected twelve-cycle test passed
  1/1 in 24.323 seconds, with xcodebuild testing completed in 25.063 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 7/7
  compatible-policy, post-trigger readback, delayed scheduled evidence, slow-
  scheduling stability, cancellation, final-watchdog diagnostics, and 200-
  replacement lifecycle Pressure tests in 1.031 seconds, with xcodebuild
  testing completed in 2.240 seconds.
- Final signed UI Pressure validation: the affected twelve-cycle test passed
  1/1 in 10.800 seconds, with xcodebuild testing completed in 11.487 seconds.
  Its trace contains twelve exact before/after confirm-delivery pairs, with an
  existing exact diagnostics summary before each trigger and an immediate
  absence readback after each delivery. Scheduling changed completion time
  while all twelve visible results remained stable.
- Validation scope: deterministic UI-target tests supply policy, immediate and
  scheduled evidence, cancellation, final diagnostics, and replacement-
  generation Pressure evidence; the affected signed UI supplies the repeated
  visible presentation/dismissal path. FlowTabCore Unit, app Unit/Behavior,
  FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant
  because production behavior, fixture topology, module wiring, configuration,
  path resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: the build, owner/Pressure, baseline,
  and final UI ledgers record `stage=completed` with zero applicable exit codes;
  all three test actions retain present result bundles before cleanup. Host-
  context strict deep codesign accepts the installed App and final Runner.
  Exact process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: both affected Swift sources parse directly
  and `git diff --check` passes. The shared nonexistence owner and interaction
  sources contain 162 and 399 lines; the interaction source remains below the
  400-line guardrail. The caller literal is removed, leaving 217 raw numeric
  timeout arguments across eight pre-existing UI-test files. Fatal-error,
  unexpected-failure, failed-signing, crash, abort, sanitizer, main-thread-
  checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 3.7 MB GM evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GM presentation dismissal`.

### SYNC-036GN Closure Record

- Status: completed. The Option+Tab App-card click dismissal now resolves from
  one observer-before-tap exact-element transition, while parent SYNC-036
  remains open for the remaining owner-specific literals.
- Oracle and design: after the independent exact switcher-open and Mail-card
  readiness conditions resolve, the helper starts a fresh diagnostics-summary
  nonexistence owner. Its immediate baseline readback must prove the current
  Option+Tab presentation exists. The exact Mail-card tap then runs inside the
  trigger closure; after it returns, the owner enables post-trigger evidence,
  requests an immediate readback, and accepts only exact summary absence.
- Lifecycle and retained time policy: Accessibility supplies no switcher-
  dismissal callback, so the shared owner retains its named serial 100ms XCUI
  readback cadence only while the exact summary remains present. The helper
  invocation owns resolution, expiry, explicit cancellation, and deinit cleanup;
  the test owns App teardown. `optionTabAppClickDismissal` retains two seconds
  solely as the compatible terminal failure upper bound. Baseline mismatch and
  expiry report the exact identity, phase, evidence source, final existence,
  generation, and wait result.
- Existing-test semantic guard: authorization `ce35ec9301b8f64c` preserves the
  exact Mail card, click trigger, visible summary-absence result, and compatible
  failure bound while applying the explicitly requested observer-before-trigger,
  initial-readback, and lifecycle-ownership rules.
- Pre-change signed UI baseline: the affected test passed 1/1 in 5.975 seconds,
  with xcodebuild testing completed in 6.697 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 7/7
  compatible-policy, post-trigger readback, delayed scheduled evidence, slow-
  scheduling stability, cancellation, final-watchdog diagnostics, and 200-
  replacement lifecycle Pressure tests in 1.084 seconds, with xcodebuild
  testing completed in 2.241 seconds.
- Final signed UI validation: the affected test passed 1/1 in 4.987 seconds,
  with xcodebuild testing completed in 5.648 seconds. The trace records the
  exact summary existence readback before Mail-card tap and exact absence on
  the immediate post-tap readback. Scheduling changed completion time while
  the visible result remained stable.
- Validation scope: deterministic UI-target tests supply policy, immediate and
  scheduled evidence, cancellation, final diagnostics, and replacement-
  generation Pressure evidence; the affected signed UI supplies the visible
  Option+Tab click/dismissal path. FlowTabCore Unit, app Unit/Behavior,
  FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant
  because production behavior, input routing, fixture topology, module wiring,
  configuration, path resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: the build, owner/Pressure, baseline,
  and final UI ledgers record `stage=completed` with zero applicable exit codes;
  all three test actions retain present result bundles before cleanup. Host-
  context strict deep codesign accepts the installed App and final Runner.
  Exact process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 615 lines, within the single-
  responsibility guardrail. The caller literal is removed, leaving 216 raw
  numeric timeout arguments across seven pre-existing UI-test files. Fatal-
  error, unexpected-failure, failed-signing, crash, abort, sanitizer, main-
  thread-checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 3.5 MB GN evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GN App click dismissal`.

### SYNC-036GO Closure Record

- Status: completed. The Control+Tab window-card click dismissal now resolves
  from one observer-before-tap exact-element transition, while parent SYNC-036
  remains open for the remaining owner-specific literals.
- Oracle and design: after the independent exact in-App switcher-open and
  secondary-window-card readiness conditions resolve, the helper starts a fresh
  diagnostics-summary nonexistence owner. Its immediate baseline readback must
  prove the current Control+Tab presentation exists. The exact secondary-window
  card tap then runs inside the trigger closure; after it returns, the owner
  enables post-trigger evidence, requests an immediate readback, and accepts
  only exact summary absence.
- Lifecycle and retained time policy: Accessibility supplies no switcher-
  dismissal callback, so the shared owner retains its named serial 100ms XCUI
  readback cadence only while the exact summary remains present. The helper
  invocation owns resolution, expiry, explicit cancellation, and deinit cleanup;
  the test owns App teardown. `controlTabWindowClickDismissal` retains two
  seconds solely as the compatible terminal failure upper bound. Baseline
  mismatch and expiry report the exact identity, phase, evidence source, final
  existence, generation, and wait result.
- Existing-test semantic guard: authorization `1f26cd659e7068d7` preserves the
  exact secondary-window card identity, click trigger, visible summary-absence
  result, and compatible failure bound while applying the explicitly requested
  observer-before-trigger, initial-readback, and lifecycle-ownership rules.
- Pre-change signed UI baseline: the affected test passed 1/1 in 6.264 seconds,
  with xcodebuild testing completed in 7.328 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 7/7
  compatible-policy, post-trigger readback, delayed scheduled evidence, slow-
  scheduling stability, cancellation, final-watchdog diagnostics, and 200-
  replacement lifecycle Pressure tests in 1.011 seconds, with xcodebuild
  testing completed in 2.148 seconds.
- Final signed UI validation: the affected test passed 1/1 in 4.957 seconds,
  with xcodebuild testing completed in 5.616 seconds. The trace records the
  exact summary existence readback before secondary-window-card tap and exact
  absence on the immediate post-tap readback. Scheduling changed completion
  time while the visible result and exact window identity remained stable.
- Validation scope: deterministic UI-target tests supply policy, immediate and
  scheduled evidence, cancellation, final diagnostics, and replacement-
  generation Pressure evidence; the affected signed UI supplies the visible
  Control+Tab click/dismissal path. FlowTabCore Unit, app Unit/Behavior,
  FlowTabTests, Space-fixture UI, and process-level Pressure are not relevant
  because production behavior, input routing, fixture topology, window identity,
  module wiring, configuration, path resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: the build, owner/Pressure, baseline,
  and final UI ledgers record `stage=completed` with zero applicable exit codes;
  all three test actions retain present result bundles before cleanup. Host-
  context strict deep codesign accepts the installed App and final Runner.
  The final isolated exact process readback finds no Flow Tab UITest App,
  UI-test Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 616 lines, within the single-
  responsibility guardrail. The caller literal is removed, leaving 215 raw
  numeric timeout arguments across seven pre-existing UI-test files. Fatal-
  error, unexpected-failure, failed-signing, crash, abort, sanitizer, main-
  thread-checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 620 KB GO evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GO window click dismissal`.

### SYNC-036GP Closure Record

- Status: completed. The Search result-row click dismissal now resolves from
  one observer-before-tap exact-element transition, while parent SYNC-036
  remains open for the remaining owner-specific literals.
- Oracle and design: after the independent exact diagnostics-summary and
  Browser-result readiness conditions plus row-frame assertion resolve, the
  helper starts a fresh diagnostics-summary nonexistence owner. Its immediate
  baseline readback must prove the current Search presentation exists. The exact
  Browser result-row tap then runs inside the trigger closure; after it returns,
  the owner enables post-trigger evidence, requests an immediate readback, and
  accepts only exact summary absence.
- Lifecycle and retained time policy: Accessibility supplies no Search-
  dismissal callback, so the shared owner retains its named serial 100ms XCUI
  readback cadence only while the exact summary remains present. The helper
  invocation owns resolution, expiry, explicit cancellation, and deinit cleanup;
  the test owns App teardown. `searchResultClickDismissal` retains two seconds
  solely as the compatible terminal failure upper bound. Baseline mismatch and
  expiry report the exact identity, phase, evidence source, final existence,
  generation, and wait result.
- Existing-test semantic guard: authorization `3afbb8105f1c139c` preserves the
  exact Browser result identity, row-frame evidence, click trigger, visible
  summary-absence result, and compatible failure bound while applying the
  explicitly requested observer-before-trigger, initial-readback, and lifecycle-
  ownership rules.
- Pre-change signed UI baseline: the affected test passed 1/1 in 6.294 seconds,
  with xcodebuild testing completed in 7.359 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 7/7
  compatible-policy, post-trigger readback, delayed scheduled evidence, slow-
  scheduling stability, cancellation, final-watchdog diagnostics, and 200-
  replacement lifecycle Pressure tests in 1.072 seconds, with xcodebuild
  testing completed in 2.161 seconds.
- Final signed UI validation: the affected test passed 1/1 in 5.156 seconds,
  with xcodebuild testing completed in 5.811 seconds. The trace records the
  exact summary existence readback before Browser result-row tap and exact
  absence on the immediate post-tap readback. Scheduling changed completion
  time while the visible result and exact Search result identity remained stable.
- Validation scope: deterministic UI-target tests supply policy, immediate and
  scheduled evidence, cancellation, final diagnostics, and replacement-
  generation Pressure evidence; the affected signed UI supplies the visible
  Search click/dismissal path. FlowTabCore Unit, app Unit/Behavior, FlowTabTests,
  Space-fixture UI, and process-level Pressure are not relevant because
  production behavior, Search selection, input routing, fixture topology,
  module wiring, configuration, path resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: the build, owner/Pressure, baseline,
  and final UI ledgers record `stage=completed` with zero applicable exit codes;
  all three test actions retain present result bundles before cleanup. Host-
  context strict deep codesign accepts the installed App and final Runner.
  Exact process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly
  and `git diff --check` passes. It contains 617 lines, within the single-
  responsibility guardrail. The caller literal is removed, leaving 214 raw
  numeric timeout arguments across seven pre-existing UI-test files. Fatal-
  error, unexpected-failure, failed-signing, crash, abort, sanitizer, main-
  thread-checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 620 KB GP evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GP Search click dismissal`.

### SYNC-036GQ Closure Record

- Status: completed. The five shared Search mock query paths now obtain
  foreground readiness through one file-owned exact-state assertion, while
  parent SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: `launchSearchMockApplication` prepares the independent
  Search-input readiness observation before launch, launches the configured mock
  App, then delegates foreground readiness to the existing shared helper. That
  helper reads the persistent `XCUIApplication.State.runningForeground` state
  immediately, retains its bounded activation fallback, and succeeds solely from
  that exact state. Search-input and exact query-result element evidence remain
  independent later Oracles.
- Lifecycle and retained time policy: `searchMockForegroundReadiness` retains ten
  seconds solely as the compatible foreground-state failure upper bound. Each
  launcher invocation owns the synchronous XCUIApplication waits and each test
  owns App teardown; the helper retains no observer or timer after return.
  Expiry reports `unmetCondition=runningForeground` and the final App state.
- Existing-test semantic guard: authorization `2f083a9f8214451f` preserves exact
  foreground readiness, all five Search query/result Oracles, and the compatible
  terminal bound while adding named ownership and final-state diagnostics.
- Pre-change signed UI baseline: all five affected Search mock query tests passed
  5/5 in 25.332 seconds, with xcodebuild testing completed in 26.040 seconds.
  The direct signed-XCTest authorization probe also passed its representative
  exact query path 1/1 in 4.798 seconds, with xcodebuild testing completed in
  5.909 seconds.
- Deterministic policy validation: the rebuilt target passed the file-owned and
  shared support compatibility tests 2/2 in 0.361 seconds, with xcodebuild
  testing completed in 1.055 seconds.
- Final signed UI validation: all five affected Search mock query tests passed
  5/5 in 24.618 seconds, with xcodebuild testing completed in 25.285 seconds.
  Every launch trace records exact `runningForeground` completion before Search-
  input and result evidence; scheduling changed completion time while every
  functional result remained stable.
- Validation scope: deterministic UI-target tests supply named-policy
  compatibility, and affected signed UI supplies foreground, Search-input, and
  exact result evidence. FlowTabCore Unit, app Unit/Behavior, FlowTabTests,
  Space-fixture UI, observer Pressure, and process-level Pressure are not relevant
  because observer infrastructure, production launch/activation behavior,
  Search behavior, fixture topology, module wiring, configuration, path
  resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: baseline, authorization, build, policy,
  and final UI ledgers record `stage=completed` with zero applicable exit codes;
  all four test actions retain present result bundles before cleanup. Host-
  context strict deep codesign accepts the installed App and final Runner. The
  final isolated exact process readback finds no Flow Tab UITest App, UI-test
  Runner, xcodebuild, or fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly and
  `git diff --check` passes. It contains 638 lines, within the single-
  responsibility guardrail. The shared-launcher literal is removed, leaving 213
  raw numeric timeout arguments across seven pre-existing UI-test files. Fatal-
  error, unexpected-failure, failed-signing, crash, abort, sanitizer, main-
  thread-checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 868 KB GQ evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GQ Search foreground readiness`.

### SYNC-036GR Closure Record

- Status: completed. The real Space-fixture App Search result now resolves from
  one observer-before-query committed projection, while parent SYNC-036 remains
  open for the remaining owner-specific literals.
- Oracle and design: after the independently observed initial Search input is
  ready, `performAndWaitForCommittedSearchResultRow` starts before `typeText` and
  captures an immediate baseline snapshot. Acceptance remains gated until the
  query trigger returns. The final UI trace confirms the exact fixture row was
  already visible in the empty-query baseline, then accepts only a later atomic
  snapshot with `scope=app`, the independently derived fixture query,
  `app:<fixture bundle ID>`, the exact row identity and existence, and FlowTab in
  `.runningForeground`. Search confirmation remains a later independent Oracle.
- Lifecycle and retained time policy: Accessibility supplies no atomic Search-
  projection callback, so the shared owner retains its named serial 100ms XCUI
  readback cadence only while the committed snapshot is unresolved. The helper
  invocation owns observation resolution, expiry, cancellation, and deinit
  cleanup; the workflow owns both Apps and fixture teardown.
  `spaceFixtureSearchResultPublication` retains five seconds solely as the
  compatible terminal failure upper bound. Expiry reports the expected scope,
  query, result ID, row identity, final projection, App state, generation, and
  evidence source.
- Existing-test semantic guard: authorization `7cd48f3910cb8704` preserves the
  real fixture query, exact App row identity, confirmation path, and compatible
  bound while adding observer-before-trigger committed-projection evidence.
- Pre-change signed real-topology baseline: the affected Space-fixture Search and
  confirmation path passed 1/1 in 18.060 seconds, with xcodebuild testing
  completed in 19.138 seconds.
- Deterministic owner and Pressure validation: the rebuilt target passed 8/8
  compatible-policy, query gating, committed visible-row, immediate/scheduled
  readback, slow scheduling, cancellation, watchdog-final-readback, and stale-
  generation Pressure tests in 1.222 seconds, with xcodebuild testing completed
  in 2.253 seconds.
- Final signed real-topology UI validation: the affected Space-fixture Search and
  confirmation path passed 1/1 in 17.478 seconds, with xcodebuild testing
  completed in 18.189 seconds. The initial snapshot observed the exact row before
  query input without resolving; the later exact committed projection resolved
  deterministically. Scheduling changed completion time while query, result,
  fixture identity, confirmation, and cleanup results remained stable.
- Validation scope: deterministic UI-target tests supply policy, trigger gating,
  atomic projection, slow-scheduling, cancellation, final diagnostics, and stale-
  generation Pressure evidence; signed real-topology UI supplies fixture
  readiness, exact query/result identity, confirmation, and process cleanup.
  FlowTabCore Unit, app Unit/Behavior, FlowTabTests, production process Pressure,
  and unrelated UI are not relevant because shared owner implementation,
  production Search/confirmation behavior, module wiring, configuration, and
  path resolution are unchanged.
- Result, signing, fixture, and process evidence: baseline, build, owner/Pressure,
  and final UI ledgers record `stage=completed` with zero applicable build,
  signing, fixture, log, and test exit codes; all three test actions retain
  present result bundles before cleanup. Host-context strict deep codesign accepts
  the installed App and final Runner. The final isolated exact process readback
  finds no Flow Tab UITest App, UI-test Runner, xcodebuild, Space fixture, or
  generated fixture application process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly and
  `git diff --check` passes. It contains 655 lines, within the single-
  responsibility guardrail. The raw result-existence literal is removed, leaving
  212 raw numeric timeout arguments across seven pre-existing UI-test files.
  Fatal-error, unexpected-failure, failed-signing, crash, abort, sanitizer, main-
  thread-checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 760 KB GR evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GR fixture Search result`.

### SYNC-036GS Closure Record

- Status: completed. Search-header mock-runtime foreground readiness now uses the
  existing file-owned exact-state assertion, while parent SYNC-036 remains open
  for later header/chip and owner-specific literals.
- Oracle and design: after launching the same Search mock configuration used by
  the shared query paths, the test delegates to
  `assertSearchMockApplicationIsForegroundReady`. The shared helper performs the
  persistent state readback and bounded activation fallback, and success requires
  exact `.runningForeground`. Header existence, highlighted-chip existence, and
  chip width bounds remain independent later Oracles.
- Lifecycle and retained time policy: `searchMockForegroundReadiness` retains ten
  seconds solely as the compatible foreground-state failure upper bound. The
  invocation owns synchronous platform waits and the test owns App teardown; no
  observer or timer remains after return. Expiry reports
  `unmetCondition=runningForeground` and final App state.
- Existing-test semantic guard: authorization `653a1ac685d98ec3` preserves the
  Search-header launch, exact foreground state, header/highlight existence, and
  size assertions while routing the bound through its named owner.
- Pre-change signed UI baseline: the affected Search-header layout test passed
  1/1 in 4.717 seconds, with xcodebuild testing completed in 5.747 seconds.
- Deterministic policy validation: the rebuilt target passed the compatible-
  policy test 1/1 in 0.226 seconds, with xcodebuild testing completed in 1.291
  seconds.
- Final signed UI validation: the affected Search-header layout test passed 1/1
  in 4.676 seconds, with xcodebuild testing completed in 5.336 seconds. Exact
  foreground readiness, header/highlight existence, and width bounds remained
  stable while scheduling changed completion time.
- Validation scope: the deterministic UI-target test supplies policy evidence,
  and affected signed UI supplies exact state plus visible layout evidence.
  FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, Pressure,
  and unrelated UI are not relevant because owner implementation, production
  launch/activation, layout behavior, module wiring, configuration, path
  resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: baseline, build, policy, and final UI
  ledgers record `stage=completed` with zero applicable exit codes; all three test
  actions retain present result bundles before cleanup. Host-context strict deep
  codesign accepts the installed App and final Runner. The final isolated exact
  process readback finds no Flow Tab UITest App, UI-test Runner, xcodebuild, or
  fixture process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly and
  `git diff --check` passes. It contains 655 lines, within the single-
  responsibility guardrail. The caller literal is removed, leaving 211 raw
  numeric timeout arguments across seven pre-existing UI-test files. Fatal-error,
  unexpected-failure, failed-signing, crash, abort, sanitizer, main-thread-
  checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 544 KB GS evidence root contains
  only rebuildable result bundles and logs. It is removed after this durable
  record, followed by exact absence and process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GS Search header readiness`.

### SYNC-036GT Closure Record

- Status: completed. The Search-header layout path now resolves header and
  highlighted-chip readiness from one observer-before-launch atomic collection,
  while parent SYNC-036 remains open for the remaining owner-specific literals.
- Oracle and design: the helper resolves the two exact XCUI identifiers and
  starts its observation before App launch. Its initial readback cannot resolve
  the condition. Launch plus exact `.runningForeground` readiness runs as the
  trigger, after which one immediate readback accepts only an ordered snapshot
  where both identifiers exist. The returned highlighted-chip element supplies
  the unchanged independent width bounds.
- Lifecycle and retained time policy: Accessibility supplies no atomic
  collection callback, so the shared owner retains its named serial 100ms XCUI
  cadence only while the post-trigger projection remains unresolved. The helper
  invocation owns resolution, watchdog expiry, explicit cancellation, and deinit
  cleanup; the test owns App teardown. `searchHeaderProjection` retains ten
  seconds solely as the compatible cumulative terminal failure upper bound from
  the two former five-second waits. Expiry reports both expected identities,
  phase, evidence source, and final atomic readback.
- Existing-test semantic guard: authorization `3026056ae00b216f` preserves the
  exact Search header, highlighted App chip, and chip-width Oracles while applying
  the explicitly requested observer-before-trigger, initial-readback, atomic-
  projection, and lifecycle-ownership rules.
- Pre-change signed UI baseline: the affected Search-header layout test passed
  1/1 in 4.837 seconds, with xcodebuild testing completed in 6.026 seconds.
- Resolved authorization interruption: three host-context post-change attempts
  retained signed Runner and result-bundle evidence but stopped before every test
  body with `Timed out while enabling automation mode` and exit 65; xcodebuild
  elapsed 70.110, 69.726, and 69.673 seconds. Recovery reset the Xcode Helper
  Accessibility decision, posted
  `com.apple.Xcode-Helper.TCC.prompt`, and required the helper-owned
  `com.apple.Xcode-Helper.TCC.value=1` readback before resuming the same canonical
  wrapper command. The reproducible invocation is
  `./scripts/testing/run-ui-tests-local.sh test-without-building --skip-space-fixtures --output-root <fresh-project-local-root> -only-testing:FlowTabUITests/FlowTabUITests/testSearchHeaderHighlightedAppChipStaysContentSizedForShortTitle`.
  The successful runs below supersede the initialization-only attempts; none of
  the three failures provides product or runtime evidence.
- Deterministic owner and Pressure validation: the rebuilt target passed 7/7
  compatible-policy, exact/missing/order projection, mandatory post-trigger
  readback, delayed scheduled evidence, cancellation, final-watchdog diagnostics,
  and 200-replacement lifecycle Pressure tests in 1.178 seconds, with xcodebuild
  testing completed in 2.338 seconds.
- Final signed UI validation: the affected Search-header layout test passed 1/1
  in 7.312 seconds, with xcodebuild testing completed in 14.449 seconds. Its trace
  records both exact existence readbacks before the App opens, then exact
  `.runningForeground`, header existence, and highlighted-chip existence in the
  immediate post-launch projection. Slower pre-launch Accessibility scheduling
  changed completion time while the exact visible identities and width result
  remained stable.
- Validation scope: deterministic UI-target tests supply named-policy, atomic-
  projection, trigger gating, scheduled evidence, cancellation, final
  diagnostics, and replacement-generation Pressure evidence; affected signed UI
  supplies foreground plus visible header/highlight and layout evidence.
  FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, production
  process Pressure, and unrelated UI are not relevant because shared owner
  implementation, production launch/layout behavior, module wiring,
  configuration, path resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: the baseline, build, owner/Pressure, and
  final UI ledgers record `stage=completed` with zero applicable exit codes; all
  three test actions retained present result bundles before cleanup. Host-context
  strict deep codesign accepts the installed App and final Runner. The final
  isolated exact process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, Space fixture, or generated fixture application process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly and
  `git diff --check` passes. It contains 671 lines, within the single-
  responsibility guardrail. Both caller literals are removed, leaving 209 raw
  numeric timeout arguments across seven pre-existing UI-test files. Fatal-error,
  unexpected-failure, failed-signing, crash, abort, sanitizer, main-thread-
  checker, permission-denial, and compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 454 MB GT evidence root contained
  only rebuildable result bundles and logs, including the three superseded
  initialization attempts. It was removed after this durable record; exact root
  absence, test-asset absence, and empty process readback all pass.
- Commit subject:
  `test(sync): migrate SYNC-036GT Search header projection`.

### SYNC-036GU Closure Record

- Status: completed. All five shared mock Search query paths now resolve from
  one committed query/result-row collection observation, while parent SYNC-036
  remains open for the remaining owner-specific literals.
- Oracle and design: each test supplies the requested query and independently
  expected mock bundle identities to the shared helper. The helper validates a
  nonempty one-to-one result-ID/row-ID mapping, installs the generation-owned
  observer, and captures an immediate baseline before `typeText`. Acceptance is
  gated until input delivery returns. The post-trigger readback succeeds only
  when one foreground snapshot reports the exact scope, percent-decoded query,
  every requested committed result ID, and the complete unique requested
  Accessibility row set. The single-row compatibility entry point delegates to
  the same collection rule, preserving Settings and real-fixture callers.
- Lifecycle and retained time policy: Accessibility exposes no atomic Search
  projection callback, so the existing owner retains its named serial 100ms
  XCUI readback cadence only while unresolved. Resolution and expiry cancel the
  scheduler; `defer` and deinit provide explicit fallback cleanup. The helper
  invocation owns observation cancellation and each test owns App teardown.
  `searchMockSingleResultProjection` retains five seconds, while
  `searchMockMultipleResultProjection` retains the prior cumulative ten seconds,
  solely as terminal failure upper bounds. Expiry reports expected identities,
  acceptance state, generation/source, final scope/query/result IDs, visible row
  IDs, and App state.
- Existing-test semantic guard: authorization `bdaffe24b18a91d0` preserves all
  five exact mock Search result Oracles, compatible bounds, and existing
  single-row caller semantics while adding observer-before-trigger, committed
  query, complete collection, unique identity, cancellation, and final-readback
  requirements.
- Pre-change signed UI baseline: all five affected mock Search query tests passed
  5/5 in 23.054 seconds, with xcodebuild testing completed in 24.177 seconds.
  The original shared `cs` path visibly performed two independent five-second
  waits after typing; the other four each performed one.
- Authentication evidence: the first post-build deterministic invocation signed
  the Runner and produced a result bundle, then stopped before every test body
  with `LocalAuthentication Code=-2` and xcodebuild exit 65 after 14.988 seconds.
  Reissuing the same canonical wrapper command obtained successful system
  authentication and passed 7/7 in 1.390 seconds, with xcodebuild testing
  completed in 15.046 seconds. This initialization-only attempt supplies no
  product result and is retained solely in this diagnostic record. The compact
  reproduction command is
  `./scripts/testing/run-ui-tests-local.sh test-without-building --skip-space-fixtures --output-root <fresh-project-local-root> -only-testing:FlowTabUITests/FlowTabUITests/testCommittedSearchResultRowsRequireOneAtomicProjection`.
- Deterministic owner and Pressure validation: the final rebuilt target passed
  7/7 compatible-policy, unique nonempty mapping, complete/partial/duplicate and
  order-independent collection rules, gated baseline, delayed scheduled
  evidence, 100 stale-generation replacements, and final-watchdog diagnostics
  in 1.179 seconds, with xcodebuild testing completed in 1.923 seconds. The other
  nine parser, initial-state, committed-query, fallback-text, app-set, exact-
  identifier, delayed-scheduler, cancellation, and 100-generation shared-owner
  regressions passed 9/9 in 1.422 seconds, with xcodebuild testing completed in
  2.150 seconds.
- Final signed UI validation: the five affected mock Search paths passed 5/5 in
  19.164 seconds, with xcodebuild testing completed in 19.869 seconds. Each trace
  records the summary and requested row readbacks before typing, then the exact
  committed query/result/row readback after input. The two-result `cs` path reads
  both identities through one collection owner. The final real Space-fixture
  Search and confirmation path passed 1/1 in 16.877 seconds, with xcodebuild
  testing completed in 17.600 seconds. The Settings Window/App compatibility
  path and real-fixture path also passed together 2/2 in 37.651 seconds before
  the final duplicate-row hardening, with xcodebuild testing completed in 38.389
  seconds.
- Validation scope: deterministic UI-target tests cover the shared observation
  model, policy, lifecycle, slow scheduling, watchdog diagnostics, and Pressure.
  Signed mock UI covers Chinese, Pinyin-initial, shared-result, code-subsequence,
  and segmented-Chinese visible paths. Signed real-fixture UI covers the exact
  external App identity and confirmation topology. FlowTabCore Unit, app Unit/
  Behavior, FlowTabTests, and production process Pressure are not relevant
  because production Search behavior, confirmation behavior, fixture identity,
  module wiring, configuration, path resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: the final build, owner/Pressure, mock
  UI, and Space-fixture UI invocations all complete with exit zero and present
  result bundles. Host-context strict deep codesign accepts both the installed
  App and final Runner. The final isolated exact process readback finds no Flow
  Tab UITest App, UI-test Runner, xcodebuild, Space fixture, or generated fixture
  application process.
- Diagnostics and Process/Tooling: all five affected Swift files parse directly,
  the project file passes `plutil`, and `git diff --check` passes. The shared
  source, caller, and existing test files contain 717, 723, and 791 lines,
  respectively, retaining their clear single responsibilities below the 800-line
  guardrail; the new focused test file contains 206 lines. The six caller
  literals are removed, leaving 203 raw numeric timeout arguments across the
  seven pre-existing UI-test files. Final fatal-error, unexpected-failure,
  signing, crash, abort, sanitizer, main-thread-checker, permission-denial, and
  compiler-error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 138 MB GU evidence roots contain
  only rebuildable result bundles and logs, including the superseded
  authentication attempt. They are removed after this durable record, followed
  by exact root absence, test-asset absence, and empty process readback.
- Commit subject:
  `test(sync): migrate SYNC-036GU mock Search projection`.

### SYNC-036GV Closure Record

- Status: completed. All six pointer hover/click paths now share one file-owned
  foreground-readiness policy and exact-state assertion, while parent SYNC-036
  remains open for their later owner-specific presentation and interaction
  literals.
- Oracle and design: each path launches its configured mock App and delegates to
  `assertPointerInteractionApplicationIsForegroundReady`. The existing shared
  readiness helper performs an immediate `XCUIApplication.State` readback,
  retains its bounded activation fallback, and succeeds solely from the
  persistent `.runningForeground` state. Switcher/Search presentation, exact
  card/result identity, selection, frame, and dismissal evidence remain later
  independent Oracles.
- Lifecycle and retained time policy:
  `pointerInteractionForegroundReadiness` retains ten seconds solely as the
  compatible terminal failure upper bound. Each invocation owns the synchronous
  platform waits and each test owns App teardown; no observer or timer remains
  after return. Expiry reports `unmetCondition=runningForeground` and the final
  application state.
- Existing-test semantic guard: authorization `f5c92fbe478dd6da` preserves all
  six pointer presentation, identity, selection, frame, and dismissal Oracles
  plus the compatible terminal bound while adding named ownership and exact
  final-state diagnostics.
- Pre-change signed UI baseline: all six affected pointer hover/click paths
  passed 6/6 in 32.048 seconds, with xcodebuild testing completed in 32.877
  seconds.
- Final build and signed UI validation: the rebuilt UI target completed
  successfully. The compatible-policy test and all six affected paths passed
  7/7 in 32.532 seconds, with xcodebuild testing completed in 33.474 seconds.
  Every trace reaches exact `.runningForeground` before the independently owned
  presentation, visible identity, hover-selection, frame, or dismissal evidence.
- Validation scope: the deterministic policy test supplies finite-positive and
  compatible-bound evidence. Signed UI covers Option+Tab App hover/click,
  Control+Tab exact-window hover/click, and Search result hover/click paths.
  FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture UI, observer
  Pressure, and production process Pressure are not relevant because observer
  infrastructure, production launch/activation/input/Search behavior, fixture
  topology, module wiring, configuration, path resolution, and hot-path work are
  unchanged.
- Result, signing, and process evidence: baseline, build, and final test
  invocations complete with exit zero and present result bundles. Host-context
  strict deep codesign accepts the installed App and final Runner. The final
  isolated exact process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, Space fixture, or generated fixture application process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly and
  `git diff --check` passes. It contains 745 lines and retains one clear
  responsibility below the 800-line guardrail. The six caller literals are
  removed, leaving 197 raw numeric timeout arguments across the seven
  pre-existing UI-test files. Final fatal-error, unexpected-failure, signing,
  crash, abort, sanitizer, main-thread-checker, permission-denial, and compiler-
  error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 740 KB GV evidence roots contain
  only rebuildable result bundles and logs. They are removed after this durable
  record, followed by exact root absence, test-asset absence, and empty process
  readback.
- Commit subject:
  `test(sync): migrate SYNC-036GV pointer foreground readiness`.

### SYNC-036GW Closure Record

- Status: completed. Both Option+Tab pointer paths now establish one exact App-row
  collection owner before the global trigger, while parent SYNC-036 remains open
  for their independent initial-selection, later hover-selection, and dismissal
  literals.
- Oracle and design: the helper resolves the requested unique App-row identities,
  starts the generation-owned observer, requires its initial readback, and then
  invokes the notification-backed global-switcher presentation trigger. After
  trigger completion it performs an immediate readback and succeeds only from
  one complete requested collection: Browser plus Mail for hover, or Mail for
  click. Initial selected identity, later moved-pointer selection, exact frame,
  click result, and panel dismissal remain independent subsequent Oracles.
- Lifecycle and retained time policy: Accessibility exposes no atomic collection
  publication callback, so the shared named cancellable 100ms XCUI readback
  cadence activates only while the post-trigger condition remains unresolved.
  `optionTabSingleAppRowProjection` retains five seconds and
  `optionTabAppRowCollectionProjection` retains the prior cumulative ten seconds
  solely as terminal failure upper bounds. The helper invocation owns observer
  and scheduler cancellation through `defer`, and each test owns App teardown.
  Expiry reports the phase, expected identities, every final existence readback,
  source, generation, wait result, and cancellation state.
- Existing-test semantic guard: authorization `f162feefee7e86a0` preserves exact
  panel presentation, App-row identities and requested order, initial and moved
  selection, frame, hover/click result, dismissal, and compatible watchdog bounds.
- Pre-change signed UI baseline: both affected Option+Tab pointer paths passed
  2/2 in 10.473 seconds, with xcodebuild testing completed in 11.329 seconds.
- Final build, deterministic, Pressure, and signed UI validation: the rebuilt UI
  target completed successfully. Exact/missing/order rejection, mandatory
  post-trigger evidence, delayed evidence, cancellation, watchdog final readback,
  200 owner replacements, and compatible policy passed 7/7 in 1.042 seconds,
  with xcodebuild testing completed in 1.837 seconds. Both affected signed paths
  then passed 2/2 in 7.456 seconds, with xcodebuild testing completed in 8.174
  seconds. Their traces show the pre-trigger absent-row readback followed by the
  requested complete post-trigger projection.
- Validation scope: deterministic XCTest covers the shared owner state rules and
  lifecycle Pressure; signed UI covers the affected Option+Tab App hover and
  click paths. FlowTabCore Unit, app Unit/Behavior, FlowTabTests, Space-fixture
  UI, and production process Pressure are not relevant because shared owner
  implementation, production presentation/input behavior, fixture topology,
  module wiring, configuration, path resolution, and hot-path work are unchanged.
- Result, signing, and process evidence: baseline, build, owner, and final test
  invocations complete with exit zero and present result bundles. Host-context
  strict deep codesign accepts the installed App and final Runner. The final
  isolated exact process readback finds no Flow Tab UITest App, UI-test Runner,
  xcodebuild, Space fixture, or generated fixture application process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly and
  `git diff --check` passes. It contains 796 lines and retains one clear
  responsibility below the 800-line guardrail. The three caller literals are
  removed, leaving 194 raw numeric timeout arguments across the seven
  pre-existing UI-test files. Final fatal-error, unexpected-failure, signing,
  crash, abort, sanitizer, main-thread-checker, permission-denial, and compiler-
  error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 680 KB GW evidence roots contain
  only rebuildable result bundles and logs. They are removed after this durable
  record, followed by exact root absence, test-asset absence, and empty process
  readback.
- Commit subject:
  `test(sync): migrate SYNC-036GW Option Tab App rows`.

### SYNC-036GX Closure Record

- Status: completed. The Option+Tab pointer-hover path now uses one file-owned
  selected-App projection watchdog for both the initial Browser state and the
  moved-pointer transition to Mail. Parent SYNC-036 remains open for later
  owner-specific window, Search, recovery, fixture, and pagination literals.
- Oracle and design: initial selection delegates to the shared diagnostics owner,
  which registers its scheduler before an immediate persistent `selected`
  readback. Moved-pointer selection starts the same owner before either hover,
  gates all evidence until the trigger returns, and requests an immediate
  post-trigger readback. Success requires the exact Browser or Mail bundle
  identity in the diagnostics snapshot; scheduling speed cannot change the
  accepted value.
- Lifecycle and retained time policy: Accessibility exposes no callback for a
  diagnostics value change, so the shared named cancellable 100ms XCUI readback
  cadence runs only while the exact value is unresolved.
  `optionTabSelectedAppProjection` retains three seconds solely as the compatible
  terminal failure upper bound. Each helper invocation cancels its owner through
  `defer`, and the test owns App teardown. Expiry reports acceptance state,
  expected key/value, source, generation, wait result, cancellation state, and
  the final raw and parsed diagnostics snapshot.
- Existing-test semantic guard: authorization `b8d795153eccb226` preserves exact
  initial Browser and post-hover Mail selection, row identities, pointer frames,
  hover behavior, and compatible terminal bounds.
- Pre-change signed UI baseline: the affected Option+Tab hover path passed 1/1
  in 4.036 seconds, with xcodebuild testing completed in 5.320 seconds.
- Final build, deterministic, Pressure, and signed UI validation: the rebuilt UI
  target completed successfully. Compatible policy, exact selected identity,
  initial synchronous evidence, observer-before-trigger gating, delayed evidence,
  atomic multi-key matching, cancellation, watchdog final readback, and 100 owner
  replacements with stale-event rejection passed 10/10 in 1.697 seconds, with
  xcodebuild testing completed in 5.916 seconds. The affected signed path then
  passed 1/1 in 4.025 seconds, with xcodebuild testing completed in 5.108 seconds.
- Validation scope: deterministic XCTest covers the shared diagnostics owner
  rules, scheduler/cancellation lifecycle, and replacement Pressure; signed UI
  covers the affected Option+Tab pointer transition. FlowTabCore Unit, app
  Unit/Behavior, FlowTabTests, Space-fixture UI, and production process Pressure
  are not relevant because shared owner implementation, production selection and
  input behavior, fixture topology, module wiring, configuration, path resolution,
  and hot-path work are unchanged.
- Result, signing, and process evidence: baseline, build, owner, and final test
  invocations complete with exit zero and present result bundles. Host-context
  strict deep codesign accepts the installed App and final Runner. The isolated
  exact process readback finds no Flow Tab UITest App, UI-test Runner, xcodebuild,
  Space fixture, or generated fixture application process.
- Diagnostics and Process/Tooling: the affected Swift source parses directly and
  `git diff --check` passes. It contains 798 lines and retains one clear
  responsibility below the 800-line guardrail. The two caller literals are
  removed, leaving 192 raw numeric timeout arguments across the seven
  pre-existing UI-test files. Final fatal-error, unexpected-failure, signing,
  crash, abort, sanitizer, main-thread-checker, permission-denial, and compiler-
  error scans are empty.
- Evidence lifecycle: `.build-local/test-assets` remains absent. Startup
  `prompts.zip` and the three pre-existing local Skill/reference edits remain
  unchanged and outside this slice. The exact 668 KB GX evidence roots contain
  only rebuildable result bundles and logs. They are removed after this durable
  record, followed by exact root absence, test-asset absence, and empty process
  readback.
- Commit subject:
  `test(sync): migrate SYNC-036GX Option Tab selection`.
