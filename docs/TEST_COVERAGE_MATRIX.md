# Test Coverage Matrix

Updated: 2026-05-25

## Purpose

This document gives a product-scenario view of FlowTab test coverage. It complements the existing layer-focused documents:

- [UNIT_AND_BEHAVIOR_TEST_COVERAGE.md](UNIT_AND_BEHAVIOR_TEST_COVERAGE.md)
- [UI_AUTOMATION_TEST_COVERAGE.md](UI_AUTOMATION_TEST_COVERAGE.md)
- [TEST_COVERAGE_CHECKLIST.md](TEST_COVERAGE_CHECKLIST.md)
- [SPACE_FIXTURE_APP_WORKFLOW.md](SPACE_FIXTURE_APP_WORKFLOW.md)

Use this matrix when deciding whether a feature has enough automated evidence across unit, behavior/integration, and UI/end-to-end layers.

## Status Legend

- `Strong`: the layer has direct automated coverage for the scenario.
- `Partial`: the layer has related automated coverage, but it does not prove the full user-visible or runtime outcome.
- `Not relevant`: the layer does not naturally apply to this scenario.
- `Gap`: the scenario still needs coverage in this layer.

## Coverage Matrix

| Product scenario | Unit coverage | Behavior / integration coverage | UI / E2E automation | Pressure / real topology | Main remaining gap |
| --- | --- | --- | --- | --- | --- |
| Core grouping and switcher session state | Strong: grouping, selection, window-layer entry, commit, remembered-window, edge guards | Partial: higher-level session orchestration is covered through `LiveSwitcherModel` and panel behavior | Partial: switcher UI paths exercise selected session outcomes | Not relevant unless a change touches hot paths | No major known gap for pure session rules; keep new rules at unit level first. |
| Hotkey configuration normalization | Strong: main hotkey, quit key fallback, in-app conflict fallback, stored conflict load migration, notification payload, derived Carbon fields, and supported key/modifier matrix axes | Strong: AppDelegate launch/reload, monitor recreation, callback routing, conflict skip, and stored conflict migration before monitor registration | Partial: Settings UI small matrix covers option/control/command main triggers, explicit/fallback quit, and explicit/fallback In-App triggers | Not relevant | UI/E2E intentionally uses a representative matrix rather than exhaustive every-key combinations; Command-Tab takeover is covered in its own scenario row. |
| Global hotkey monitor lifecycle | Partial: configuration-derived fields are unit-tested | Strong: Carbon event parsing, press/release callback routing, registration fallback, unregister-on-stop | Partial: Settings UI triggers representative main shortcuts: `Option + Space`, Control + Grave, and `Command + B` | Not relevant | Full lifecycle cleanup and registration fallback remain behavior-level assertions rather than repeated through UI automation. |
| In-app window hotkey | Strong: preference resolution and conflict avoidance | Strong: AppDelegate registration/conflict handling and focused-window session release-to-commit | Strong: Settings UI triggers explicit `Option + B` and conflict-fallback `Control + B` focused-current-app sessions, and real fixture UI proves `Control+Tab` starts from a fullscreen Space sibling, selects a normal sibling, then returns to the fullscreen sibling through the focused app's window-only panel | Strong for the representative fullscreen/off-space fixture path with app-level AX window children suppressed | Broader lifecycle refresh while the focused app's window set changes remains covered by runtime rows rather than a separate in-app UI matrix. |
| Command-Tab takeover | Not relevant for most pure logic beyond helper resolution | Strong: takeover success/failure, abnormal-exit recovery, restore-on-terminate | Strong: `testSettingsCommandTabTakeoverTriggersSwitcherAndRestoresSystemShortcut` configures Command-Tab through Settings, observes takeover, presses real `Command + Tab`, and verifies the takeover marker clears after normal exit | Not relevant | No major known automated gap for the Command-Tab takeover path; keep restore evidence tied to real keyboard takeover runs. |
| Settings: appearance | Strong: theme/language preference resolution, Settings chrome app strings, unified Settings color resolver target-appearance behavior, custom dropdown controls resolving to the application theme when host/system appearance differs, stable-id title refresh for language changes, the app-visibility manage action restyling after language changes in dark theme, `testSystemThemeStateMatchesCurrentSystemAppearanceWhenAppDefaultsContainAppleInterfaceStyle` proving app-scoped `AppleInterfaceStyle` pollution does not override the inherited system appearance, and `testSettingsLanguageChangeRebuildsSettingsBridgeWithLocalizedText` proving language changes rebuild the AppKit Settings bridge with localized text | Strong: app content can build with persisted preferences; `testSettingsAppKitPageRefreshesLocalizedTextWhenLanguageChanges` proves the same AppKit Settings page instance refreshes page chrome, visible page subtitle layout, card chrome, row labels, and permission actions after language changes; `testSettingsRootLanguageChangeRebuildsSettingsBridgeWithLocalizedText`, `testSettingsRootThemeChangeRebuildsSettingsBridgeWithTargetAppearance`, and `testSettingsRootFollowSystemThemeChangeRebuildsSettingsBridgeLikeExplicitSwitch` prove the root Settings entry rebuilds for explicit theme changes and follow-system OS appearance notifications | Strong: toggles and theme/language persistence are UI-tested; `testSettingsAppearanceThemeAndLanguageUpdateVisibleUI` proves visible theme luminance and English copy refresh after changing Settings | Not relevant | Settings action button, dropdown, segmented control, and card chrome now share resolved presentation inputs; low-level tests own the theme/state matrix while UI keeps representative user paths. |
| Settings: page layout and control chrome | Strong: Settings layout breakpoint, unified action/select/segmented metrics, flexible control-width constraints, English content-sized select/permission controls, App Visibility intrinsic manage action layout, FlowDropdown directional placement/fallback geometry, arrow/body/scroll separation, side-menu complete-row shrinking and centering, wide SwiftUI/AppKit page expansion, layer-backed Settings card/control frame alignment, post-language-switch Chinese card relayout, and follow-system theme notification relayout are covered in-process by `testSettingsPageNarrowLayoutUsesSingleColumnAndFlexibleControlWidths`, `testSettingsEnglishLayoutUsesContentSizedControlsAndPermissionButtons`, `testFlowDropdownPlacementResolverKeepsDefaultBelowAndUsesSideOnlyWhenFullyVisible`, `testFlowDropdownPlacementResolverFallsBackVerticallyByAvailableRows`, `testFlowDropdownMenuBodyAndScrollAreaExcludeArrowForAllDirections`, `testFlowDropdownSidePlacementShrinksToCompleteRowsAndCentersInContentWhenTriggerIsOffCenter`, `testSettingsPageContentExpandsToWideSwiftUIProposal`, `testSettingsCardLayoutDoesNotResetBackingLayerOrigin`, `testSettingsLayerBackedChromeControlsDoNotResetBackingLayerOrigin`, `testSettingsLanguageSwitchRelayoutsChineseCardsWithoutOverlap`, and `testSettingsRootFollowSystemThemeChangeRebuildsSettingsBridgeLikeExplicitSwitch` | Strong: app content construction, Settings page measurement, and hotkey select placement wiring are covered through `testContentViewBodyBuildsWithPersistedPreferences` and `testHotkeyKeySelectsPreferRightPlacementAndOtherSelectsDefaultBelow` | Partial: default Settings open-and-toggle paths and right-side hotkey key dropdown placement are UI-tested; vertical fallback and window resizing are intentionally kept below UI because they are deterministic AppKit geometry rules | Not relevant | Add a dedicated UI resize case only if a real user path regresses at a specific window size or localization. |
| Settings: window behavior | Strong: auto-enter delay normalization, window behavior preference defaults | Strong: delayed auto-enter uses configured delay and switcher behavior toggles are covered below the UI layer | Strong: Settings values persist across relaunch, and `testSettingsWindowBehaviorHideMinimizedAppsAffectsSwitcherAppLayer` proves the hide-minimized setting changes switcher app-layer output | Not relevant for the hide-minimized UI coverage; run pressure only when changing switcher timing, filtering cost, or repeated runtime paths | Auto-enter timing and restore-minimized variants remain behavior-tested; add representative UI cases only if those user paths regress or become higher risk. |
| Settings: search | Strong: search preference normalization, Accessibility-gated default scope availability, and search matching rules | Strong: search coordinator, query editing, scope routing, result application | Strong: search disabled blocks launch-time auto-search; default scope control persists and switches; no-Accessibility Settings hides the window-scope option; default window and restored app scopes now open matching results through the user path | Strong for search pressure scenarios | No major known Settings search UI/E2E gap; keep new search settings changes tied to visible runtime behavior, not only control persistence. |
| Settings: app visibility exclusions | Strong: hidden app ID normalization, filtering rules, hidden-last presentation rank, and hidden-app count singular/plural copy are covered in `FlowTabCore` plus app-level localization tests | Strong: `LiveSwitcherModel` behavior proves hidden apps are removed before app-layer session construction and both app/window search indexing; settings manager behavior keeps stored hidden IDs visible when current inventory omits them | Strong: Settings app-visibility manager hides a mock app, relaunches prove it is absent from the Option+Tab app panel and app search results, and the hidden filter still shows a stored hidden app missing from the current inventory | Strong for related Option+Tab fast-start and search pressure checks when this filtering path changes | Real installed-app inventory diversity is intentionally covered by a general scanner plus mock-runtime UI proof; add real-topology coverage only if macOS inventory reconciliation becomes behaviorally complex. |
| Settings: permission controls | Strong: permission copy/state helpers, launch override parsing, launch-at-login default/status reconciliation, and localized permission status layout | Strong: prompt gating, launch behavior, and launch-at-login app-start sync | Strong for banner visibility, Settings routing, localized permission status visibility, reminder persistence, disabled in-app controls, and launch-at-login default-off/toggle/persistence path | Not relevant | External System Settings outcomes and real macOS Login Items approval state are intentionally not asserted; app-owned entry points and mocked login-item service are automated. |
| Home app and window list | Strong: runtime snapshot assembly, filtering helpers, nested zero-window helper app suppression, shared window-recency overlay, hidden-last Home app presentation, localized app/window count copy, and Home window activation target resolution | Strong: runtime snapshot/provider behavior, shared Home/Switcher runtime snapshot service, shared Home candidate recency ordering, Home app visibility presentation, and Home activation dispatch with scoped contexts | Strong: mock Home selection, hidden-app Home badge/sort order, nested helper topology, real activation-generated Home window-recency ordering, real multi-app Home counts, per-app window isolation, and real Home window-row activation are automated | Strong: real fixture workflow covers multi-app counts, window list isolation, Home recency after exact fixture-window activation, fullscreen-only handling, and exact window activation | No major known Home workflow gap; keep new Home runtime behavior tied to fixture UI proof where system topology matters. |
| Logs and diagnostics | Strong: log level ordering, preference normalization, diagnostics filtering, and custom dropdown binding for runtime log level selection | Strong: runtime log write/read/since/clear behavior and noisy-category filtering | Strong for seeded logs, level dropdown selection, level visibility, clear behavior; Partial for open-directory button | Not relevant | Finder/open-directory side effect is intentionally not asserted; only the visible entry point is automated. |
| Switcher standard panel flow | Strong: `SwitcherSession` navigation and commit rules plus pointer-selection gate movement rules | Strong: panel controller routing, global hotkey flow, pointer hover selection for app/window targets, pointer click-to-commit routing for app/window targets, fast app-layer startup including first-frame nested zero-window helper suppression, shared-runtime selected-app window snapshot source, responsive selected-app window preview paging, interruptions, occlusion, active-space handling | Strong for mock panel entry/exit, Option+Tab app hover selection and stationary-hover rejection, Option+Tab app click-to-commit close, Control+Tab window hover selection and stationary-hover rejection, Control+Tab window click-to-commit close, Option+Tab nested helper topology, initial stale-occlusion presentation recovery without hard panel reorder, mock large-window pagination, real fixture app strip, preview isolation, duplicate-title preview anchors, and Option+Tab app window-state activation from a fullscreen Space sibling through the four-window Noisy round trip with staged recency-prefix assertions | Strong: deterministic fast-start pressure covers app-layer startup under a slow full snapshot, 1,000-window frontmost-app startup, 1,000-window selected-app background apply/window-layer entry with current-page-only preview capture, rapid initial-visibility open/close recovery cancellation, and tab-switch stress covers repeated switching | Broader lifecycle refresh while a switcher session is open remains tracked under runtime snapshot coverage. |
| Search panel and result activation | Strong: matching, tokenization, pinyin, cursor editing, pointer-selection gate movement rules, ranking-related behavior | Strong: search coordinator, search-mode routing, selected-result application, pointer hover selection focusing search results, pointer click-to-commit routing for selected results, and query workload matrix coverage for short-prefix, high-hit, segmented long, Chinese, no-result, and clear-query search paths | Strong for mock app search plus content-sized header highlight chips, search-result hover selection, stationary-hover rejection, and search-result click-to-commit close; Strong for real workflow app-scope, window-scope activation, duplicate-title, identical-window, edge-title, and non-cooperative same-app search from a fullscreen Space sibling to a normal sibling and back | Strong: search performance baselines and representative fullscreen/off-space fixture activation exist | No major known search UI/E2E gap for stable fixture inputs; keep lifecycle refresh and system shortcut paths tracked separately. |
| Terminate selected app | Partial: key configuration is unit-tested | Strong: terminate flow keeps app until exit, polling timeout, workspace termination refresh, panel quit shortcut routing | Strong: Settings UI changes explicit `Option + Z` and fallback `Option + W` quit shortcuts with mock runtime, and `testSwitcherPanelQuitShortcutKeepsRealFixtureAppUntilProcessTerminates` proves the default switcher shortcut against a delayed real fixture process | Strong for real-process fixture lifecycle; pressure not relevant unless termination refresh cadence changes | No major known UI/E2E gap for selected-app termination; keep new process-lifecycle variants tied to fixture proof. |
| Runtime snapshot, AX/CG mapping, and window records | Strong for deterministic helper rules, including Space topology classification, Space topology snapshot/diff affected-window rules, WindowRecord derived-index projection, and reconciliation coordinator dirty/retry decisions | Strong: assembly, filtering, CG/AX binding, sticky binding, title fallback, ambiguous matching, wrapper/content rebinding, AX-only fullscreen content fallback binding, desktop sibling binding/exposure, private bridge fallback, shared Home/Switcher runtime snapshot service, Space topology provider consumption during CG collection, provider recording of Space topology diffs through the reconciliation coordinator, app-local window recency overlay for user-selectable/activatable candidates, global/focused session runtime-truth parity, deterministic bounded AX app collection concurrency pressure, and real Chrome-style multi-window same-space AX-subset retention via `testRuntimeSnapshotProviderWindowListKeepsMultipleStickySpaceOneEntriesWhenAXReturnsSubsetFromFullscreenSpace` | Strong: real fixture workflow proves selected Home/Switcher/search runtime paths plus duplicate-title, identical-window, edge-title, fullscreen/off-space AX inputs, and suppressed app-level AX window children runtime truth across Control+Tab, Option+Tab, and window search | Strong for deterministic bounded AX app collection concurrency and representative fullscreen/off-space fixture activation; Partial for broader lifecycle refresh topologies | Running app/window lifecycle refresh, production coordinator consumption by Home/Switcher, and real active-space topology diff consumption remain separate real-topology gaps. |
| Runtime activation and recovery | Partial: deterministic target/fallback helpers | Strong: app activation, window activation, minimized restore, missing-window fallback, focus verification, CG-only public AX recovery, CG-only no-route guards, and recovery retries | Strong: real app-scope search activation, real window-scope search activation, real fullscreen/off-space window-scope search activation, real in-app fullscreen-starting sibling roundtrip activation, real Option+Tab fullscreen-starting sibling roundtrip activation, real same-app window-search fullscreen-starting sibling roundtrip activation, real edge-title search activation, real Home window-row activation, and selected-app real-process termination are covered | Strong for the representative fullscreen Space fixture activation path with app-level AX window children suppressed; Partial for broader lifecycle refresh topologies | No major known direct activation gap for the representative fullscreen Space topology; lifecycle refresh remains tracked under runtime snapshot coverage. |
| Window previews and icons | Strong: preview sizing, responsive page sizing, cache, title-bar style, icon cache, Terminal provider rendering, wrapping, and CG/window-id matching | Strong: preview cache reuse, current-page-only preview requests, session-pinned visible previews, batched current-page preview publication, deferred current-page display until batch commit including failure fallback, deferred non-current preview preheat, current-page preview item generation, provider resolver routing, Terminal special-provider fallback, and provider behavior including batched shareable-content lookup mode | Partial: switcher preview isolation, duplicate same-title window anchors, and mock large-window pagination with current-page screenshot presence exist in UI automation | Strong for deterministic 1,000-window selected-app current-page preview-capture pressure | Pixel-level preview correctness is intentionally not broadly asserted; current UI proof checks accessibility anchors, isolation, whether visible cards have screenshots, and does not yet automate live Terminal screenshot-vs-scripting visual parity. |
| App launch, lifecycle, and status item | Partial: launch-option parsing, content construction, visibility default, permission override parsing | Strong: AppDelegate launch/teardown, observer cleanup, stress runner startup, close-window persistence, activation-policy application, status-item open behavior, and status-item quit menu behavior | Strong for launch smoke/performance, tab navigation, default regular-app display setting, status-item reopen after window close, and status-item quit menu workflow | Partial: launch performance is measured | Status-item workflows now have representative UI proof; broader system-menu quit variants remain covered by platform behavior rather than bespoke UI automation. |
| Space fixture workflow infrastructure | Strong: fixture configuration parsing, suppressible app-level AX children, and window planning | Strong: fixture window coordinator, application AX children publishing/suppression, and resolved workflow logic | Strong for fixture app UI; Partial for FlowTab real multi-app consumption | Strong where real workflow tests are selected | A unified multi-app launcher and broader FlowTab real workflow suite are still pending. |
| Performance and pressure gates | Strong for search pressure helper scenarios, search query workload matrix coverage, deterministic Option+Tab fast-start/window-scale pressure, and bounded runtime AX app collection concurrency | Partial: app stress runner startup is covered | Strong for UI tab-switch stress measurement | Strong for search external sampling, deterministic Option+Tab fast-start p95, 1,000-window background/window-layer pressure, bounded preview-capture counts, runtime AX app collection concurrency, rapid initial-visibility open/close recovery cancellation, and tab-switch UI metrics | Keep baselines current when search, repeated switching, preview capture, or runtime sampling changes. |

## Current High-Value Gaps

No high-value cross-layer gaps are currently tracked here.

## Maintenance Rules

- Update this file when adding a feature, fixing a user-visible bug, or changing the meaning of an existing test.
- Keep detailed per-test scenario text in the layer-specific coverage documents; keep this file focused on cross-layer coverage and gaps.
- Do not mark UI/E2E as `Strong` when the test only proves a setting value persisted. Mark it `Partial` until the changed setting is proven through the resulting user-visible behavior.
- Do not use this matrix to skip required FlowTab layers. Use `skills/flowtab-engineering/references/risk-calibration.md` for layer relevance decisions.
